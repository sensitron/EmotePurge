/**
 * Outbound links the product makes about itself.
 *
 * The repository is public and the licence at the repo root is GNU AGPL v3 — both verified rather
 * than assumed, because this is the one claim on the landing page that a visitor can check in a
 * single click, and a broken or private link would discredit the two claims next to it.
 *
 * A tool that asks for a 7TV write token owes that check. Everything else the public page could
 * offer as reassurance (numbers, reviews, screenshots) does not exist and must not be invented;
 * the source does exist.
 */
export const SOURCE_URL = 'https://github.com/sensitron/EmotePurge';
