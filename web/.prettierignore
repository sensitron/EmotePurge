# Build and tooling artifacts. Prettier already ignores node_modules itself.
/.angular
/dist
/coverage
/test-results
/.audit-out

# Generated and managed by npm — formatting would be pure diff noise.
/package-lock.json

# Markdown stays hand-maintained repo-wide: the project docs live in docs/ and thus
# outside this Prettier scope. Formatting only web/**/*.md would split the docs
# into two formatting worlds.
*.md
