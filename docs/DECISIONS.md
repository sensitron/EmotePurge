# Entscheidungslog

Die vollständige Historie der Architektur- und Infrastruktur-Entscheidungen dieses Projekts. Bis zum 2026-07-30 stand diese Liste im Änderungslog von [`../CLAUDE.md`](../CLAUDE.md), das damit auf 93 KB angewachsen war; sie ist hierher **wortgleich** verschoben worden (Befund S3-27 des Reviews vom 2026-07-29 — `git show` belegt, dass verschoben und nicht gekürzt wurde). `CLAUDE.md` führt jetzt nur noch die daraus abgeleiteten *geltenden Regeln*.

**Regel:** Ein Feature-Commit, der eine Konvention, einen Vertrag oder eine Topologie ändert, enthält seinen Log-Eintrag **im selben Commit**.

Sortierung: absteigend nach Datum; innerhalb eines Datums in der ursprünglichen Reihenfolge des Logs. Einträge aus der Anfangsphase (Modul 1 und Modul A) tragen kein Datum, weil sie im Original keines hatten — sie stehen am Ende und wurden bewusst **nicht** nachträglich datiert.

Zwei Dinge sind beim Verschieben hinzugekommen, beide außerhalb des historischen Texts: die Überschrift (Datum + Titel) und die `**Betrifft:**`-Zeile, die das Log per `grep <dateiname> docs/DECISIONS.md` durchsuchbar macht. Dass der erste Absatz jedes Eintrags seinen Titel dadurch ein zweites Mal nennt, ist der Preis dafür, den Originaltext unangetastet zu lassen — bewusst so.

---

### 2026-09-05 — Das volle Verbindungslimit wird sichtbar: `GET /api/live/status` statt eines Umbaus auf fetch-SSE

**Betrifft:** `src/EmotePurge.Core/Messaging/ILiveEventStream.cs`, `src/EmotePurge.Infrastructure/Redis/RedisLiveEventStream.cs`, `src/EmotePurge.Api/Endpoints/LiveEndpoints.cs`, `web/src/app/core/live/live-update.service.ts`, `web/src/app/core/live/live-quota.service.ts`, `web/src/app/core/http/api-auth.interceptor.ts`, `web/src/app/features/shell/app-shell.ts`

Stufe 2 von Issue #42. Stufe 1 hat den Backend-Split gebracht (429 mit
`live_stream_quota_exhausted` gegen 503 mit `live_stream_unavailable`); der Nutzer sieht davon aber
nichts, weil die Seite bei einem abgelehnten Stream einfach verstummt — und Stille ist von „gerade
passiert nichts" nicht zu unterscheiden. Genau das hat beim Live-Test am 2026-08-31 eine halbe
Stunde Fehlersuche gekostet.

**Der neue Vertrag:** `GET /api/live/status` (authentifiziert, ohne Rate-Limit-Policy) antwortet mit
`{ openConnections, maxPerSubscriber, perSubscriberLimitReached }`. Kein Fehlercode, kein Stream —
eine Auskunft.

**Warum eine Nachfrage-Route und nicht der fetch-SSE-Umbau.** Die Stufe-1-Notiz nannte beide Wege.
`EventSource.onerror` reicht weder Statuscode noch Body durch — die ganze Mühe, die Stufe 1 in
Body und `Retry-After` gesteckt hat, ist vom Browser aus schlicht unerreichbar. Ein fetch-basierter
SSE-Reader käme an beides heran, aber der Preis wäre, das gesamte Reconnect-Verhalten, das heute der
Browser erledigt, in eigenen Code zu holen — für einen Hinweis. Die Route kostet einen Request pro
fatalem Close und lässt `LiveUpdateService` so schlank, wie der Eintrag zu seiner Einführung es
begründet hat.

**Und warum eine eigene Route statt eines zweiten GET auf die Stream-URL.** Der naheliegende Trick
wäre, die SSE-URL noch einmal per `HttpClient` zu holen: gleiche Herkunft, gleiches Cookie, und ein
429 käme mitsamt Body an. Er ist eine Falle. Antwortet die URL nämlich **200**, öffnet dieser zweite
Aufruf einen echten Stream — und belegt einen der sechs Slots, um herauszufinden, ob die Slots voll
sind. Bei `MaxPerSubscriber = 6` ist das kein theoretisches Problem.

**`LiveStreamQuota` trennt, was `LiveEventSubscribeStatus` zusammenfasst.** Prozessweites und
Per-Login-Limit teilen sich weiterhin `QuotaExhausted`, weil ein *Aufrufer* beide identisch
behandelt. Sobald ein *Mensch* am anderen Ende steht, ist der Unterschied entscheidend: „Schließ ein
paar Tabs" ist bei vollem Per-Login-Budget der richtige Rat und bei vollem Prozess-Limit schlicht
falsch — die Tabs gehören dann jemand anderem. Der Hinweis erscheint deshalb ausschließlich bei
`perSubscriberLimitReached`.

**Die Oberfläche: ein `HealthMarker` im Kopfbereich, kein Banner auf der Seite.** Die
Designsprache §4.4 ist da eindeutig — „Ein Banner ist für das, was *diese Seite* betrifft; alles
App-weite trägt der Kopfbereich, und zwar allein." Jede Seite aktualisiert sich aus diesem Stream,
die Tatsache ist also app-weit. Der Marker steht neben dem `workerStatus.stale`-Marker, folgt
derselben Regel („Der Text nennt die Folge, nicht das Subsystem") und schweigt im Normalfall ganz.

**Ein transienter Close darf nichts anzeigen** (§4.3). Ein Api-Neustart schickt jeden Stream durch
`closed`, und eine Warnung, die bei jedem Deploy aufblitzt, bringt man Leuten bei zu übersehen. Der
Hinweis erscheint deshalb erst, wenn der Server die Ursache **bestätigt** hat, und verschwindet
wieder, sobald irgendein Stream `open` erreicht — was beweist, dass wieder Platz war.

**`/api/live/status` steht in `EXPECTED_401_PATHS`.** Ein widerrufenes Login ist einer der Gründe,
aus denen ein Stream abgelehnt wird. Ohne die Ausnahme würde die Sonde jeden fatalen SSE-Close in
einen sofortigen Redirect auf `/login` verwandeln — ein Verhalten, das es vor der Sonde nicht gab.
Eine abgelaufene Sitzung fällt weiterhin beim nächsten echten Request auf, wie bisher.

**Was das Codex-Review zum Branch noch abgeräumt hat, alles drei im Neucode:** (1) Eine Sonde, die
noch unterwegs ist, während die Sichtbarkeits-Wiederholung den Stream wieder aufmacht, hätte ihre
Antwort *nach* dem Aufräumen abgeliefert — und weil `status` dann `open` bleibt, hätte sie niemand
mehr weggeräumt: eine Warnung über Live-Updates auf einer Seite, deren Live-Updates funktionieren.
Behoben über einen Generationszähler, der beim Öffnen hochgezählt wird; eine Antwort aus einer alten
Generation wird verworfen. (2) Die Beschriftung nennt jetzt nur noch die Folge. Die Ursache („zu
viele offene Tabs") passt nicht in eine Kopfzeile, die bei 360 px schon Wortmarke, „Abstimmungen"
und den 44-px-Kontotrigger trägt — eine Pille bricht nicht um. **Damit fehlt die Ursache in der
Oberfläche; wer sie sichtbar haben will, braucht eine andere Fläche als diese Zeile.** (3) Die
Meldung erscheint nach dem Laden, also hätte ein Screenreader gar nichts erfahren — sie hat jetzt
eine dauerhaft vorhandene `sr-only`-Live-Region neben der `aria-hidden`-Pille.

**Unangetastet, ausdrücklich:** die `_redisSubscribed`-Sperrklinke. Dass ein warmer Prozess während
eines Redis-Ausfalls weiter 200 liefert, ist Absicht (Eintrag vom 2026-09-01) — der Fan-out heilt
sich nach dem Reconnect selbst, und ein „ehrlicher" 503 würde offene Tabs totlegen. Hier geht es
allein darum, die beiden heute ununterscheidbaren Ablehnungen auseinanderzuziehen.

---

### 2026-09-04 — Ergebnistypen mit Invariante werden geschlossen, nicht dokumentiert

**Betrifft:** `src/EmotePurge.Core/Services/IChannelService.cs`, `src/EmotePurge.Core/Services/IChannelIdentityService.cs`, `src/EmotePurge.Infrastructure/Services/ChannelSyncGate.cs`, `src/EmotePurge.Infrastructure/EmotePurge.Infrastructure.csproj`

**Die Regel, die dieser Eintrag festschreibt:** Ein Ergebnistyp, dessen Nutzlast nur bei bestimmten
Status non-null ist, wird als `sealed class` mit **privatem** Konstruktor gebaut, und die Factories
sind der einzige Weg hinein. Sie weisen einen Erfolgsstatus im `Failed(...)`-Pfad ebenso zurück wie
einen undefinierten Enum-Wert (`Enum.IsDefined`) und eine null-Nutzlast im Erfolgspfad — mit
`throw`, nicht mit einem Kommentar.

**Warum `record` das nicht kann.** Issue #55 verlangte, die Invariante unmöglich falsch machbar zu
machen; der erste Anlauf ergänzte nur Factories nach dem Vorbild der `SevenTv`-Typen und ließ den
positionalen Konstruktor offen. Das Codex-Review zum Branch zeigte die Lücke am konkreten Aufruf:
`ChannelJoinResult.Failed(ChannelJoinStatus.Joined)` erzeugt einen „Erfolg" ohne Channel, und der
`switch` im Join-Endpoint dereferenziert ihn dann. Bei einem `record` ist der positionale
Konstruktor öffentlich und `with` sitzt obendrauf — beide Türen bleiben offen, solange der Typ ein
`record` ist. Dass dabei die Wertegleichheit verlorengeht, ist kein Verlust, sondern ehrlicher:
`ChannelJoinResult` trägt eine veränderliche EF-Entität, die ohnehin über die Referenz vergleicht,
und die Record-Gleichheit hätte eine Zusage vorgetäuscht, die sie nie gegeben hat.

**Beide Türen sind gemessen, nicht behauptet.** Eine Wegwerf-Probe, die Konstruktor und `with`
aufruft, erzeugt genau vier Compilerfehler (`CS1729`, `CS8858` je Typ). Damit das so bleibt, halten
Reflection-Tests fest, dass es keinen öffentlichen Instanzkonstruktor und kein
compilergeneriertes `<Clone>$` gibt — wer den Typ zurück in einen `record` verwandelt, macht sie
rot, statt das Loch still wieder zu öffnen.

**Noch nicht umgestellt:** Fünf Typen der `SevenTv`-Familie haben dieselbe Bauform und dieselbe
Lücke. Sie in denselben Commit zu ziehen, hätte das Diff unlesbar gemacht; sie stehen als eigenes
Vorhaben aus.

**Zweitens, im selben Zug: `Infrastructure` bekommt `InternalsVisibleTo` für seine Testassembly.**
Der Rename-Handover-Test synchronisierte über ein `Task.Delay(300)` — auf einem langsamen Läufer
weder verlässlich grün noch verlässlich rot. Statt der Wartezeit meldet `ChannelSyncGate` jetzt über
einen `internal Action<string>?` unmittelbar vor dem Warten am Zeilen-Gate, dass es dort ankommt;
der Test hängt daran eine `TaskCompletionSource` und weiß damit **beweisbar**, dass die Zeile
geladen und der Sync geparkt ist. Kosten im Produktivbetrieb: eine Null-Prüfung je Erwerb. Die
Naht sitzt bewusst nur am Zeilen-Gate — am Namens-Gate wartet der Aufrufer, bevor irgendetwas
gelesen wurde, dort bewiese das Parken nichts. `EmotePurge.Api` nutzt dieselbe Mechanik bereits.


### 2026-09-04 — Der Rename-Handover bekommt zwei Sperren: ein zweites Boot-Signal und ein Gate auf `Channel.Id`

**Betrifft:** `src/EmotePurge.Worker/BootRecoveryGate.cs`, `src/EmotePurge.Worker/Worker.cs`, `src/EmotePurge.Worker/TwitchIdentityReconcileWorker.cs`, `src/EmotePurge.Infrastructure/Services/ChannelSyncGate.cs`, `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs`

**Die Regeln, die dieser Eintrag festschreibt.** Erstens: Wer ein Redis-Kommando publiziert, das
der Worker selbst ausführen muss, wartet auf `BootRecoveryGate.CommandChannelSubscribed` — nicht
nur auf `Completed`. Zweitens: Wer eine `Channel`-Zeile schreibt, hält neben dem Namens-Gate auch
das Zeilen-Gate auf `Channel.Id` (`ChannelSyncGate.AcquireByChannelIdAsync`), in der festen
Reihenfolge Name → Id, und liest die Zeile darunter neu ein.

**Warum zwei Sperren für denselben Moment.** Beides sind Nebenläufigkeitsfenster um denselben
Vorgang: die Übergabe eines Kanals vom alten auf den neuen Login (Issue #54, aus dem
Codex-Review zu #44). Beide sind selbstheilend — das Konvergenznetz aus #41 schließt sie in
höchstens drei Minuten —, und genau deshalb standen sie zur Wahl. Umgesetzt sind sie trotzdem,
weil die Prävention in beiden Fällen billiger ist als der Zustand, den sie verhindert, und weil
die Klasse „Nebenläufigkeit über Prozessgrenzen" in diesem Repo mehrfach durch Reviews gerutscht
ist.

**Das erste Fenster: publizieren, bevor jemand zuhört.** `Worker.ExecuteAsync` hat
`RunBootRecoveryAsync` — mit `MarkCompleted()` im `finally` — **vor**
`redisSubscriber.SubscribeAsync` aufgerufen. Die TCS läuft mit `RunContinuationsAsynchronously`,
der Sofort-Lauf des `TwitchIdentityReconcileWorker` startete also echt parallel zum Subscribe.
Erkennt dieser erste Lauf einen Rename oder Merge, publiziert er das `LEAVE`/`JOIN`-Paar, während
noch kein Subscriber am Kanal hängt — und Redis Pub/Sub kennt kein Store-and-Forward:
`PublishAsync` liefert die Empfängerzahl, keinen Fehler, und verwirft die Nachricht wortlos.
Gewählt wurde ein **zweites, unabhängiges Signal** am selben Gate statt einer umgestellten
Reihenfolge. Der Grund ist, dass beide naheliegenden Alternativen etwas kaputt machen:
`SubscribeAsync` vor die Boot-Recovery zu ziehen öffnet ein neues Fenster (ein `LEAVE` würde einen
Kanal treffen, den die Boot-Recovery gleich danach wieder joint), und `MarkCompleted()` einfach
hinter den Subscribe zu schieben nimmt dem `finally` seine Zusage, dass eine gescheiterte
Boot-Recovery den Konvergenzpfad nie dauerhaft blockiert. Mit zwei Signalen bleibt `Completed`
wortgleich das, was es war — auch für `SevenTvPeriodicResyncWorker`, `SevenTvEventWorker` und das
`bootRecoveryCompleted`-Feld im Worker-Roster —, und nur der eine Wartende, der wirklich
publiziert, wartet auf mehr.

**Das zweite Fenster: das Sync-Gate kannte nur Namen.** `ChannelSyncGate` war nach Channel-*Name*
gekeyt, und `SyncChannelAsync` erwarb es **vor** `LoadChannelAsync`. Ein Name ist aber nicht der
Kanal: Während des Handovers bezeichnen alter und neuer Login dieselbe `Channel.Id`, zwei Syncs
halten zwei verschiedene Semaphoren und reconcilen dieselbe Zeile — mit einem Rennen auf dem
Unique-Index `(ChannelId, SevenTvEmoteId)` und einem Match-Cache-Eintrag unter einem Login, den
Twitch nicht mehr routet. Das Namens-Gate bleibt, weil es das Einzige ist, was ein Aufrufer in der
Hand hat, bevor er die Datenbank angefasst hat; das Zeilen-Gate kommt dahinter, sobald die Id
bekannt ist. Die feste Reihenfolge Name → Id ist es, die dabei kein Deadlock-Paar entstehen lässt.
Das Neu-Einlesen unter dem Zeilen-Gate ist kein Beiwerk: Es ist der einzige Grund, warum
`RefreshMatchCacheAsync` nach dem Warten den *aktuellen* Namen schreibt — und es erkennt die
Zeile, die eine Zusammenführung inzwischen gelöscht hat, bevor der Sync mit einer
`DbUpdateConcurrencyException` oder einer FK-Verletzung darauf aufläuft.

**Was ungedeckt bleibt.** Die Api ist ein anderer Prozess; ein `JOIN`, das sie publiziert, während
der Worker noch bootet, geht weiterhin verloren. Das ist nicht dasselbe Fenster — es lässt sich
prozessübergreifend nicht durch eine Wartebedingung schließen — und es heilt über denselben
periodischen Resync.


### 2026-09-04 — `Microsoft.OpenApi` bleibt auf der 2.x-Linie, bis .NET 11 kommt

**Betrifft:** `.github/dependabot.yml`, `src/EmotePurge.Api/EmotePurge.Api.csproj`

**Die Regel, die dieser Eintrag festschreibt:** Major-Updates von `Microsoft.OpenApi` werden in
`dependabot.yml` ignoriert, solange `Microsoft.AspNetCore.OpenApi` in der 10.x-Reihe steht. Der
Sprung auf 3.x gehört zum .NET-11-Wechsel und wird dort mitgezogen, nicht vorher einzeln.

**Warum das keine Bequemlichkeit ist, sondern eine harte Bindung.** `Microsoft.AspNetCore.OpenApi`
10.x ist an die 2.x-Linie gebunden: Der Quellcode, den ASP.NET Core 10 für `AddOpenApi()`
generiert, schreibt in `IOpenApiMediaType.Example`, und genau diese Property ist in 3.x read-only
geworden. Der Build bricht deshalb **auch ohne eigene Document-, Operation- oder
Schema-Transformer** — `Program.cs` ruft nur `AddOpenApi()` und `MapOpenApi()` auf, und das genügt
bereits. Microsofts eigene Empfehlung im zugehörigen Issue (`dotnet/aspnetcore#67505`, Duplikat
`#67940`) lautet wörtlich, auf 2.x zu pinnen, bis 3.x-Unterstützung nachgeliefert ist; der
Breaking-Change-Artikel auf Microsoft Learn führt den Wechsel ausdrücklich unter `aspnetcore-11.0`.

**Warum die Ignore-Regel und nicht nur ein geschlossener PR.** Dependabot hatte den Bump als
PR #47 vorgeschlagen; dessen CI war rot, was den Defekt zwar sichtbar machte, aber jede Woche neu
Aufmerksamkeit gekostet hätte — der Vorschlag kommt wieder, sobald 3.x eine neue Version
veröffentlicht. Die Regel greift bewusst nur auf `version-update:semver-major`: Updates innerhalb
der 2.x-Linie sollen weiterlaufen, und genau das ist im selben Arbeitspaket auch passiert
(2.11.0 → 2.12.2 über die `minor-and-patch`-Gruppe).


### 2026-09-02 — Kanal-Identität ist die Twitch-ID; der Name ist nur die Adresse

**Betrifft:** `src/EmotePurge.Core/Services/IChannelIdentityService.cs`, `src/EmotePurge.Core/Services/AuditActor.cs`, `src/EmotePurge.Core/Entities/AuditLogEntry.cs`, `src/EmotePurge.Infrastructure/Services/ChannelIdentityService.cs`, `src/EmotePurge.Infrastructure/Services/ChannelIdentityWarningState.cs`, `src/EmotePurge.Infrastructure/Persistence/ChannelQueries.cs`, `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs`, `src/EmotePurge.Worker/TwitchIdentityReconcileWorker.cs`, `src/EmotePurge.Worker/Program.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/ChannelIdentityServiceTests.cs`

**Die Regel, die dieser Eintrag festschreibt:** Wo eine Operation die *Identität* eines Kanals
meint — Join, Identitätsabgleich, Berechtigung —, wird per `TwitchChannelId` gesucht, sobald eine
bekannt ist. Der `ChannelName` bleibt die *Adresse*: für IRC, HTTP-Routen, `EmoteMatchCache`, die
Redis-Kommandos und jede Anzeige. Beides ist bewusst kein Entweder-oder — bestehende
namensbasierte Endpunkt-Lookups bleiben unangetastet, weil die Route nun einmal den Namen trägt
und der gespeicherte Name nach der Nachführung wieder stimmt. Der Grund für die Trennung ist, dass
Twitch die eine Hälfte für unveränderlich erklärt und die andere jederzeit vom Kanalinhaber
geändert werden kann, **ohne dass irgendjemand benachrichtigt wird**. Bis zu diesem Commit gab es
im ganzen Repo keine einzige Stelle, die einen gespeicherten `ChannelName` je aktualisiert hätte.

**Warum das trotzdem kein Bugfix ist, sondern zwei verschiedene Dinge.** Issue #34/#44 hat zwei
Pfade, und nur einer war real. Der *Anzeigepfad* — ein 7TV-Editor-Grant, der unter seinem
veralteten 7TV-Login in `/mine` steht — erzeugt den gemeldeten Effekt ganz ohne `Channel`-Zeile
und ist mit Task 3 behoben. Der *Datenpfad* — zwei Zeilen für denselben Kanal, weil jemand den
neuen Namen joint — ist am Code bewiesen, aber die Bestandsmessung in Prod (2026-08-29, read-only
über den SSH-Tunnel) hat gezeigt: alle 13 Zeilen tragen eine Twitch-ID, und für den gemeldeten
Fall existiert weder unter dem alten noch unter dem neuen Login eine Zeile. Diese Reconciliation
repariert also **nichts Bestehendes**; sie ist die Prävention, die greift, sobald ein *getrackter*
Kanal umbenannt wird. Das ist der Grund, warum sie sich Zeit lassen darf (60-Minuten-Takt) und
warum sie im Zweifel lieber nichts tut, als zu raten.

**Gefahren wird der Abgleich von `TwitchIdentityReconcileWorker`, dem neunten Hosted Service des
Workers** — Takt `Twitch:IdentityReconcileIntervalMinutes`, Default 60 Minuten, weil ein Tick einen
Helix-Request pro 100 Kanälen kostet und Umbenennungen selten genug sind. Er wartet auf
`BootRecoveryGate`, anders als `TwitchLivePollWorker`, der nur Abdeckungszeilen schreibt, die sonst
niemand anfasst: dieser hier kann genau die `Channel`-Zeilen umbenennen oder zusammenführen, die
die Boot-Recovery selbst liest und schreibt — die beiden dürfen sich nicht überlappen. Der erste
Lauf kommt dann **sofort** und nicht erst nach dem ersten Intervall, denn er ist zugleich der
Prod-Backfill für alle Zeilen, die älter sind als die Twitch-ID-Spalte; eine Stunde Wartezeit dafür
wäre nur Wartezeit. Ohne `Auth:Twitch:ClientId`/`ClientSecret` meldet er sich einmal ab, statt
stündlich am Token-Abruf zu scheitern.

**Helix `GET /helix/users` ist die Signalquelle, und die Alternativen scheitern jeweils an einer
Eigenschaft, nicht an Aufwand.** Der 7TV-Sync kennt nur 7TVs Kopie des Twitch-Logins — Dritthand
und nachweislich cache-verzögert (`docs/Untersuchung-7TV-WebSocket-2026-07-30.md`); im gemessenen
Fall hielt 7TV den toten Login noch, während dieselbe ID im selben Objekt korrekt war. Der
Live-Poll fragt per Login und ist für einen Rename **strukturell blind**: der umbenannte Kanal
fehlt in der Antwort, ununterscheidbar von „offline". `/mine` feuert nur, wenn der betroffene
Nutzer die Seite aufruft, und sieht nur dessen eigene Kanäle. Helix `/users` ist Twitchs eigene
Antwort, löst ID→Login direkt auf, braucht keinen Scope (App-Token) und nimmt 100 Parameter pro
Request — der gesamte Bestand passt in einen Aufruf pro Tick.

Ausschlaggebend ist dabei eine Vertragsseite, die an Produktionsdaten gemessen wurde und ohne die
der ganze Abgleich gefährlich wäre: **ein fehlender Eintrag in einer erfolgreichen Antwort heißt
„existiert nicht", ein fehlgeschlagener Request heißt „wir wissen es nicht".** Deshalb liefert
`ReconcileActiveChannelsAsync` bei fehlendem App-Token oder unerreichbarem Helix `null` statt einer
Null-Summary, und deshalb hat `LookupByLoginAsync` drei Zustände (`Found`/`NotFound`/`Unavailable`)
statt einer nullable Identität. Eine leere Antwort als „niemand existiert" zu lesen, hieße bei
einem Ausfall, den gesamten Bestand als tot zu melden.

**Die Merge-Invariante, und warum sie nicht bloß vorsichtig ist.** Zusammengeführt wird nur, wenn
die Verlierer-Zeile **null Emote-Zeilen** hat. Das ist keine willkürliche Schwelle, sondern der
belegte Normalzustand: Solange die alte Zeile die Twitch-ID hält, läuft der erste Sync der
Duplikat-Zeile in die `DbUpdateException` des Unique-Index `IX_Channels_TwitchChannelId` — und weil
derselbe fehlgeschlagene `SaveChangesAsync` die von `ReconcileAsync` eingefügten Emotes mit
zurückrollt, kann eine Duplikat-Zeile gar keine Emote-Historie ansammeln (seit Task 4 zusätzlich
durch einen expliziten Guard vor der Exception abgefangen). Historienverlust ist damit
*strukturell* ausgeschlossen statt sorgfältig vermieden: Es gibt nichts zu verlieren. Was die Zeile
sehr wohl ansammelt, sind `ChannelLiveDay`s — der Live-Poll läuft per Login und kennt den neuen
Namen —, und die ziehen mit um. Hält die Invariante nicht (nach heutigem Code unerreichbar, aber
Bestandsdaten sind Bestandsdaten), wird **nicht** gemerged: `MergesRefused++`, Warnung mit beiden
Zeilen-IDs, kein einziger Schreibzugriff, Entscheidung beim Menschen. Ein generischer Merge mit
`UsageStat`-Summierung über kollidierende `(EmoteId, Date)`-Paare wäre Code für einen Fall, den der
Bestand nicht erzeugen kann — im Ernstfall ungetestet und damit selbst das Risiko.

`ChannelLiveDay`-Kollisionen werden mit **MAX(LiveMinutes)** aufgelöst, nicht mit einer Summe:
beide Zeilen haben denselben physischen Kanal an demselben Kalendertag gemessen, eine Summe könnte
1440 Minuten pro Tag überschreiten.

**Kein Schema-Change, Backfill zur Laufzeit.** Fehlende Twitch-IDs werden nicht per Einmalskript
nachgetragen, sondern vom ersten Lauf derselben periodischen Reconciliation, die danach dauerhaft
läuft. Der Backfill ist damit idempotent, selbstheilend und **exakt so getestet wie der
Dauerbetrieb** — ein Migrationsskript hätte einen eigenen, nur einmal ausgeführten Codepfad
gehabt. Ein Backfill schreibt bewusst *keinen* Audit-Eintrag und rührt `TrackingResumedAt` nicht
an: Es hat sich nichts am Kanal geändert, wir haben nur aufgeschrieben, was immer schon galt —
sonst stünde nach dem ersten Tick ein Eintrag pro Bestandskanal im Log.

**Ein Rename setzt dagegen sehr wohl `TrackingResumedAt`.** Zwischen dem Rename auf Twitch und dem
nächsten Abgleich zeigte der IRC-Join auf einen Namen, der nicht mehr antwortet; in dieser Lücke
wurde nichts gezählt. Genau diese Art Lücke ist der Grund, aus dem das Feld existiert. `CreatedAt`
bleibt unangetastet — es ist derselbe Kanal.

**Nachher, nicht vorher, und LEAVE vor JOIN.** Beide Redis-Kommandos gehen erst nach dem Commit
raus, in dieser Reihenfolge. Der Worker löst beim JOIN die Zeile **per Name** auf, und diesen Namen
gibt es erst nach dem Commit; das LEAVE räumt `EmoteMatchCache` und EventAPI-Abo des alten Namens
ab, ohne die der Worker weiter Chat unter einem Namen mitschreibt, den Twitch nirgendwohin mehr
routet. Die Reihenfolge ist im Test festgenagelt, nicht bloß im Kommentar.

**Rename und Merge sind auditpflichtig, und der Actor ist neu.** `AuditActions.ChannelRename` und
`AuditActions.ChannelMerge` werden unter `AuditActor.System` geschrieben — dem ersten Actor dieses
Repos, hinter dem kein Nutzer steht. Das Audit-Log ist hier nicht Beiwerk: Beide Aktionen
überschreiben genau den Namen, unter dem jede andere Ansicht den Kanal adressiert, und ein Merge
löscht zusätzlich eine Zeile. Ohne Eintrag wäre nachträglich nicht feststellbar, dass der Kanal
früher anders hieß (alter und neuer Login stehen vorerst allerdings nur im Worker-Log:
`AuditLogQueryService.ProjectDetail` reicht nur die Kinds `emoteCount`/`removedEntries`/`title`
durch, im Admin-Log steht also bloß „Kanal umbenannt". Backlog-Idee B7 behebt das **nicht** —
sie bringt Filter und CSV-Export über `DetailsJson`, lässt die Kind-Allowlist aber unangetastet;
sichtbar würden die Logins erst durch zwei zusätzliche Kinds plus ihre `DETAIL_KEYS`-Einträge). Der Actor ist ein Snapshot-String und nie ein Fremdschlüssel auf `User`
(s. `AuditLogEntry`), deshalb genügt das Literal `system` ohne Sonderfall im Frontend.

**Die Warn-Deduplizierung liegt in einem Singleton, nicht im Service.** Für tote Logins und tote
IDs (Fall 5/6), für eine durch eine fremde Zeile blockierte Umbenennung und für eine verweigerte
Zusammenführung wird nur beim Zustandswechsel gewarnt — die Messlatte ist „keine Warnungsflut im
Stundentakt, aber mindestens eine Warnung pro Prozesslauf". Vier Schlüsselräume in einem Set,
per Präfix getrennt, weil ein toter Login und eine tote ID verschiedene Tatsachen über denselben
Kanal sind und einander nicht stummschalten dürfen. Die verweigerte Zusammenführung hat dabei den
stärksten Anspruch auf Deduplizierung: Sie wartet per Definition auf einen Menschen, löst sich also
**nie** von selbst auf. Verloren geht dadurch nichts — `MergesRefused >= 1` lässt die Summary von
der Null-Summary abweichen, und die Summary-Zeile wird jeden Tick geloggt, sobald sie das tut. Der Service selbst ist scoped wie alles
mit `AppDbContext`, und der Worker öffnet pro Tick einen eigenen Scope; ein Set auf dem Service wäre
jeden Tick leer gewesen und hätte nichts dedupliziert. `ChannelIdentityWarningState` ist deshalb ein
Singleton daneben — dasselbe Muster wie `ChannelSyncGate`, `TwitchTokenRefreshGate` und
`DuplicateEmoteNameTracker`, und ausdrücklich kein `static` Feld, das zwischen Tests leckt.
Prozesslokal ist Absicht: Nach einem Neustart wird jeder weiterhin kaputte Kanal einmal erneut
gemeldet, und das ist die günstigere Fehlrichtung.

**Der Auth-Pfad wird weiterhin nicht kanonisiert** (Entscheidung 2 des Plans, hier nur bekräftigt,
weil die neue Regel das Gegenteil nahelegen könnte): `SevenTvEditorService` liefert Grants als
Autorisierungs-Input, und Autorisierung darf nicht von der Erreichbarkeit eines zweiten
Fremdsystems abhängen. Die ID-Auflösung sitzt im Anzeigepfad (`MyChannelsService`), nicht dort.
Aus demselben Grund bleibt `MyChannelsService.ResolveUntrackedGrantsAsync` neben
`LookupByLoginAsync` bestehen, obwohl beide App-Token plus `GetUsersAsync` kapseln: die eine fragt
per **ID**, die andere per **Login**, und ihre Degradationsverträge sind unvereinbar — die eine
fällt bei fehlendem Token still auf 7TV-Logins zurück und liefert trotzdem eine Liste, die andere
**muss** `Unavailable` melden. Eine gemeinsame Helper-Extraktion würde genau diese Unterscheidung
einebnen; das ist die Defektklasse aus #32/#37, die dieser Plan überhaupt erst behebt.

**Zwei Dinge, die erst der echte Postgres gezeigt hat.** Erstens: Beim Merge übernimmt die
Überlebende den Namen, den die Verlierer-Zeile im selben `SaveChangesAsync` noch hält. Tragend ist
dabei **nicht** eine allgemeine „Deletes zuerst"-Regel, sondern eine bestimmte Kante im
Kommandographen von EF Cores `CommandBatchPreparer`: ein Delete und ein Update auf **denselben Wert
eines Unique-Index** werden delete-vor-update geordnet. Der Integrationstest gegen den echten
`IX_Channels_ChannelName` belegt genau das, statt es anzunehmen — und die Grenze gehört dazu, damit
niemand verallgemeinert: zwei *Updates*, die einen Unique-Wert tauschen, bekommen diese Kante nicht
und brauchen weiterhin zwei Saves. Zweitens: Ein Duplikat-Paar wird in einem Durchlauf von **beiden
Enden** erreicht — von der ID-Zeile, die den Namen will, und von der ID-losen Zeile, die ihn hält.
Ohne eine Menge bereits erledigter Zeilen-IDs wurde derselbe verweigerte Merge zweimal gezählt und
zweimal gewarnt; der Testfall hat genau das aufgedeckt.

**Ein Fehlschlag kostet eine Zeile, nicht den Tick.** Jede Zeile wird einzeln committet, und ihr
Rumpf fängt `DbUpdateException` ab: eine Warnung, `ChangeTracker.Clear()`, weiter mit der nächsten
Zeile. Beide Hälften sind nötig. Der realistische Auslöser ist eine Race, die die Momentaufnahme
nicht schließen kann — zwischen „ist der Zielname frei?" und dem Schreiben legt ein paralleler Join
eine Zeile unter genau diesem Namen an —, und ohne den Catch blieben alle noch nicht besuchten
Zeilen liegen und die Summary ginge verloren. Das `Clear()` ist dabei keine Kosmetik: EF lässt die
gescheiterten Änderungen im Change Tracker stehen, der nächste `SaveChangesAsync` würde sie erneut
schicken und identisch scheitern — eine kaputte Zeile risse sonst alle nachfolgenden mit.
Schlägt dagegen der **Publish nach** dem Commit fehl, ist die Zeile bereits nachgeführt und der
nächste Tick sieht Fall 1: LEAVE/JOIN werden über diesen Pfad nie wieder veröffentlicht. Das
strandet den Worker aber nicht bis zum Neustart — `SevenTvPeriodicResyncWorker` liest die aktiven
Kanalnamen jeden Tick (60 s) frisch aus der DB und joint/synct den neuen Namen unabhängig von
diesem Publish; der alte Name bleibt zwar im IRC-Roster stehen, aber `RosterPrunePolicy` (Issue #41)
räumt ihn ab, sobald er zwei aufeinanderfolgende Ticks nicht mehr in der aktiven Liste steht — in
der Praxis binnen zwei bis drei Minuten, nicht bis zum Neustart. In dieser Übergangszeit zählt der
Worker Chat weiterhin auf den alten Namen; reparieren lässt sich dieser eine Publish-Fehler selbst
nicht, deshalb ist die laute Warnung die ganze Behandlung an dieser Stelle.

**Ausdrücklich nicht gebaut:** kein automatisches Leave/Purge für Kanäle, deren Konto Twitch nicht
mehr kennt (ein Bann kann aufgehoben werden — eine destruktive Entscheidung auf ein möglicherweise
transientes Signal); kein generischer `UsageStat`-Merge; kein Admin-Badge „Login tot seit …", das
eine persistierte Spalte samt Migration bräuchte.

**Und ein Randfall, der bewusst offen bleibt: der Login-Tausch.** Tauschen zwei *getrackte* Kanäle
ihre Namen — A hält `id1`/„a", B hält `id2`/„b", danach heißt A „b" und B „a" —, landen beide Zeilen
dauerhaft im Blocked-Zweig: jede will den Namen, den die andere noch hält, und keine der beiden
Umbenennungen kann als erste durchgehen. Bis jemand von Hand eingreift, zählt der Worker den Chat
des jeweils anderen Kanals auf die falsche Zeile. Das ist sehr selten und ausdrücklich **keine
Regression** — vorher passierte exakt dasselbe, nur ohne jede Warnung. Aufgelöst würde es einen
zweistufigen Schreibvorgang über einen Zwischennamen brauchen (zwei *Updates* auf denselben
Unique-Wert bekommen die oben beschriebene EF-Kante gerade nicht), und das für einen Fall, der
bisher nie beobachtet wurde; die deduplizierte Warnung ist die ganze Behandlung.

---

### 2026-09-02 — Der Join löst die Twitch-Identität vorab auf und lehnt tote Logins ab

**Betrifft:** `src/EmotePurge.Core/Services/IChannelService.cs`, `src/EmotePurge.Infrastructure/Services/ChannelService.cs`, `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs`, `src/EmotePurge.Api/Validation/ApiErrorCodes.cs`, `web/src/app/core/i18n/api-error.ts`, `web/public/i18n/de.json`, `web/public/i18n/en.json`, `tests/EmotePurge.Infrastructure.Tests/Integration/ChannelServiceTests.cs`

Der Eintrag oben macht die Twitch-ID zur Identität eines Kanals, führt sie aber erst *nachträglich*
im Stundentakt nach. Dieser hier schließt die Lücke am Eingang: `JoinAsync` fragt vor dem ersten
Schreibzugriff `IChannelIdentityService.LookupByLoginAsync` — der einzige Moment, in dem ein Mensch
ohnehin auf eine Antwort wartet — und behandelt die drei Zustände als drei verschiedene Aufgaben.

**`NotFound` lehnt den Join ab — aber nur, wenn er eine neue Zeile anlegen würde.** Bis hierher
wurde jeder syntaktisch gültige Name angenommen; ein Tippfehler erzeugte damit eine Zeile, die nie
synchronisiert und nie gezählt wird und die niemand bemerkt, bis jemand seine Statistik sucht. Genau
dagegen richtet sich die Ablehnung, und **weiter reicht sie nicht**: Führen wir den Namen bereits,
läuft der Join durch wie eh und je — Reaktivierung, `TrackingResumedAt`, `channel.join`,
`JOIN:`-Publish —, ohne Rename (es gibt nichts, worauf umbenannt werden könnte) und ohne die
gespeicherte `TwitchChannelId` anzurühren; eine Logzeile hält den Zustand fest.

Der Grund für diese Unterscheidung ist derselbe, aus dem der Eintrag oben **kein** automatisches
Leave oder Purge für unbekannte Konten baut: **Helix hält „gesperrt" und „gelöscht" nicht
auseinander** — beides ist `200` mit leerem `data` —, und ein Bann kann aufgehoben werden. Eine
pauschale Ablehnung hätte also ein möglicherweise transientes Signal benutzt, um einem Moderator das
Wieder-Beitreten zu einem Kanal zu verwehren, dessen gesamte Historie wir halten.

**Am 2026-09-02 gemessen statt angenommen** (App-Token, `GET /helix/users`), weil genau diese
Fremd-API-Annahme den Zweig trägt und dieselbe Klasse von Fehlschluss schon zweimal durchgerutscht
ist (Issues #33 und #37, wo Code und Mock sich die Annahme teilten):

| Abfrage | Antwort |
|---|---|
| `login=lwon` (auf Twitch gesperrt) | `200` · `{"data":[]}` |
| `login=<frei erfundener Name>` | `200` · `{"data":[]}` |
| `login=handofblood` (lebend) | `200` · vollständiger Datensatz |

Die beiden ersten Antworten sind zeichengleich. Ein gesperrter Kanal ist über diesen Endpunkt
**nicht** von einem nie existierenden zu unterscheiden — es gibt kein Feld, an dem man es
nachträglich festmachen könnte. Der Zweig ist damit nicht bloß vorsorglich, sondern die einzige
Möglichkeit, den Bann-Fall überhaupt zu überleben. Beide Einträge
sagen damit dasselbe: Auf „Twitch kennt dieses Konto gerade nicht" wird nichts Bestehendes
zerstört und nichts Bestehendes blockiert — nur nichts Neues angelegt.

Wo abgelehnt wird, antwortet der Endpunkt `404` mit dem neuen, sprachneutralen Code
`channel_not_on_twitch` — bewusst kein `400` (der Name ist wohlgeformt, er benennt nur niemanden)
und bewusst getrennt von `channel_not_found` (das heißt „wir tracken diesen Kanal nicht", nicht
„Twitch kennt ihn nicht"). Weil `JoinAsync` bislang direkt `Channel` lieferte, gibt es dafür jetzt
`ChannelJoinResult` (`Joined | ChannelNotOnTwitch`) statt eines zweiten Fehlerkanals.

**`Unavailable` ändert dagegen gar nichts** — exakt der bisherige Namenspfad, Zeile ohne ID, die
Reconciliation trägt sie später nach. Verfügbarkeit schlägt Strenge: ein Ausfall auf unserer Seite
ist keine Aussage über den Kanal, und die beiden Zustände dürfen an keiner Stelle zusammenfallen.
Dass `NotFound` und `Unavailable` sich für eine **bestehende** Zeile inzwischen gleich verhalten,
ist deshalb kein Anlass, die Zweige zusammenzulegen: Nur einer von beiden darf einen Join überhaupt
ablehnen, und diese Unterscheidung ist der Kern des dreiwertigen Lookups.

**`Found` sucht zuerst per ID, nicht per Name.** Trifft das eine Zeile mit anderem Namen, ist der
Join zugleich die Rename-Nachführung: Umbenennung auf den kanonischen Helix-Login,
`TrackingResumedAt`, Audit-Eintrag `channel.rename`, danach `LEAVE:<alt>` vor `JOIN:<neu>` — dieselbe
Reihenfolge und dieselbe Begründung wie beim periodischen Abgleich. Zwei Unterschiede zu jenem sind
Absicht: Der Actor ist der **echte angemeldete Nutzer** statt `AuditActor.System` (der Join geht von
einem Menschen aus), und der Pfad greift auch für **inaktive** Zeilen — die überspringt die
Reconciliation bewusst, womit ein verlassener, seither umbenannter Kanal sonst für immer unter
seinem toten Namen läge. Eine neu angelegte Zeile bekommt die ID sofort mit; eine bestehende Zeile
ohne ID bekommt sie still nachgetragen (kein Audit, kein `TrackingResumedAt` — es hat sich nichts
geändert, wir haben nur aufgeschrieben, was immer schon galt).

**Hält bereits eine andere Zeile den kanonischen Namen, wird nicht umbenannt.** Das ist das
Duplikat, das ein Rename hinterlässt; die Umbenennung liefe in `IX_Channels_ChannelName` und machte
aus einem Join eine 500. Zusammengeführt wird hier nichts — das ist die Aufgabe der Reconciliation,
die den emote-losen Normalfall **automatisch** zusammenführt und nur die beiden Ausnahmen verweigert
(die Verlierer-Zeile hat noch Emotes, oder die belegende Zeile trägt eine eigene, abweichende
Twitch-ID). Der Join läuft stattdessen auf die vorhandene Zeile wie vor dieser Änderung, mit einer
Warnung im Log. Denselben Weg nimmt der Spiegelfall, in dem gar keine Zeile die gemeldete ID hält,
die Zeile unter dem Namen aber eine abweichende trägt: nichts wird geschrieben, eine
`LogInformation` macht den Zustand sichtbar, bis der nächste Tick ihn auflöst.

**Ein Publish-Fehler nach dem Commit lässt sich auch hier an dieser Stelle nicht reparieren** —
dieselbe Lage, die der Eintrag oben für den Worker-Pfad beschreibt, und aus demselben Grund
unbehandelt: Die Zeile ist bereits umbenannt, ein zweiter Join findet folglich keinen Rename mehr
(`renamedFrom == null`) und publiziert nur noch `JOIN:<neu>`. Das ausgefallene `LEAVE:<alt>` wird
über diesen Pfad nie nachgeholt — aber wie beim Worker-Pfad oben schließt der periodische
7TV-Resync (`SevenTvPeriodicResyncWorker` + `RosterPrunePolicy`, Issue #41) die Lücke von selbst
binnen zwei bis drei Minuten, nicht erst bei einem Neustart: der neue Name wird im nächsten Tick
gejoint/gesynct, der alte fällt nach zwei stale Ticks aus dem Roster samt EventAPI-Abo. Anders als
beim Worker-Pfad merkt es der Nutzer immerhin sofort, weil sein Join in derselben Sekunde mit einem
Fehler endet.

**Kosten:** ein Helix-Request pro Join, über das gecachte App-Token. Der Join ist eine seltene,
bewusste Aktion hinter `ChannelManagementAuthorizationFilter` und dem Bookkeeping-Rate-Limit — kein
Pfad, auf dem sich das summiert.

---

### 2026-09-01 — SSH auf den VPS läuft über den Host-Alias `vps`, nicht über `emotepurge.app`

**Betrifft:** `CLAUDE.md`, `docs/Backup-und-Restore.md`

Die Doku führte SSH-Befehle bislang als `<VPS-USER>@<VPS-HOST>` mit dem Hinweis, die
Platzhalter durch die echten Zugangsdaten zu ersetzen. Naheliegend war dabei
`emotepurge.app` — und genau das **funktioniert seit dem Cloudflare-Umzug nicht mehr**
(s. Eintrag 2026-08-29): der Name zeigt auf Cloudflare, nicht auf den Origin, SSH läuft
ins Leere. Die Anleitung für die Prod-Migration war damit nicht bloß ungenau, sie führte
in einen Befehl, der scheitert — aufgefallen beim ersten Migrationsversuch nach dem Umzug.

Ab jetzt steht in beiden Dokumenten schlicht `vps`, der Host-Alias aus `~/.ssh/config` auf
der Devbox. Der Alias trägt Origin-Adresse und Benutzer; beides gehört nicht ins Repo, und
ein Alias hat gegenüber einem Platzhalter den Vorteil, dass er **kopierbar korrekt** ist
statt ausgefüllt werden zu müssen. Ändert sich die Origin-Adresse erneut, ist die
`ssh/config` die eine Stelle, die es weiß — die Doku muss dann gar nicht mit.

**Kein sudo für den Migrationsweg.** Der Tunnel greift auf `127.0.0.1:5433` auf dem VPS zu,
also auf den von Docker *veröffentlichten* Host-Port; den erreicht jeder Benutzer. Sudo
braucht nur, wer stattdessen direkt `docker exec` gegen `emotepurge-postgres` fährt (der
Nachschau-Weg) — nicht das Anwenden der Migration.

---

### 2026-09-01 — Bot-Nutzung bekommt eine zweite Spalte, keine zweite Zeile

**Betrifft:** `src/EmotePurge.Worker/IBotChatterDetector.cs`, `src/EmotePurge.Worker/BotChatterDetector.cs`, `src/EmotePurge.Worker/IEmoteUsageCounter.cs`, `src/EmotePurge.Worker/EmoteUsageCounter.cs`, `src/EmotePurge.Worker/TwitchChatManager.cs`, `src/EmotePurge.Worker/UsageFlushWorker.cs`, `src/EmotePurge.Worker/Program.cs`, `src/EmotePurge.Core/Services/IUsageStatFlushService.cs`, `src/EmotePurge.Core/Entities/UsageStat.cs`, `src/EmotePurge.Infrastructure/Services/UsageStatFlushService.cs`, `src/EmotePurge.Infrastructure/Migrations/20260901193259_AddUsageStatBotUseCount.cs`, `tests/EmotePurge.Worker.Tests/BotChatterDetectorTests.cs`, `tests/EmotePurge.Worker.Tests/EmoteUsageCounterTests.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/UsageStatFlushServiceTests.cs`, `docker-compose.yml`, `docker-compose.prod.yml`, `.env.example`, `docs/Architectur.md`, `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs`, `src/EmotePurge.Core/Services/IUsageStatQueryService.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/UsageStatQueryServiceTests.cs`, `src/EmotePurge.Core/Services/IEmoteSetStatusService.cs`, `src/EmotePurge.Infrastructure/Services/EmoteSetStatusService.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/EmoteSetStatusServiceTests.cs`, `web/src/app/core/emotes/bots-excluded-caption.ts`, `web/src/app/core/emotes/bots-excluded-caption.spec.ts`, `web/src/app/core/emotes/emote-set-status.model.ts`, `web/src/app/core/emotes/emote-admin.service.spec.ts`, `web/src/app/features/usage-stats/usage-stats-page.ts`, `web/src/app/features/usage-stats/usage-stats-page.html`, `web/public/i18n/de.json`, `web/public/i18n/en.json`, `web/e2e/support/mocks.ts`, `web/e2e/usage-atlas.e2e.spec.ts`

Issue #31: Jede Chat-Nachricht eines Bots wurde bislang wie die eines Menschen in `UsageStat.UseCount` verbucht — Bot und Mensch stecken in derselben Zahl und sind nachträglich nicht mehr trennbar, weil es keine Chatter-Dimension gibt, an der man das aufdröseln könnte. **Warten kostet unwiederbringlich Daten:** Jeder Tag ohne Trennung erzeugt weitere Zeilen, die für immer gemischt bleiben, und Bestandsdaten lassen sich nicht rückwirkend bereinigen. Das war das Einzige, was dieses Issue zeitkritisch gemacht hat.

**E1 — eine zweite Spalte, kein zweiter Zeilentyp.** `UsageStats` bekommt `BotUseCount` (`int`, `NOT NULL DEFAULT 0`) neben dem unverändert bedeutenden `UseCount`; der Unique-Index bleibt `(EmoteId, Date)`. Verworfen wurde die im Issue skizzierte Variante, die Dimension in den Unique-Index zu ziehen (`(EmoteId, Date, IsBot)`) — sie verdoppelt die Zeilenzahl und zwingt alle fünf Aggregat-Queries in eine neue Form, inklusive des `GroupBy`-Übersetzungsrisikos aus Regel 10, für dasselbe Ergebnis. Ebenfalls verworfen: Bot-Nachrichten einfach verwerfen statt zu zählen — eine Fehlklassifikation wäre dann endgültiger Datenverlust. Ausschlaggebend für die zweite Spalte ist, dass sie **beide Fehlerrichtungen reparierbar** hält: Ein übersehener Bot landet in `UseCount` — exakt der heutige Zustand, kein Rückschritt gegenüber vorher. Ein fälschlich als Bot erkannter Mensch landet in `BotUseCount` und ist nicht verloren, sondern liegt sichtbar daneben. Das senkt das Risiko eines notwendigerweise kleinen ersten Wurfs so weit, dass er vertretbar ist. Aus demselben Grund gibt es **keinen** Filter „nur Zeilen mit Human > 0" im Upsert: Ein Batch, in dem ein Emote ausschließlich von Bots kam, erzeugt eine Zeile mit `UseCount = 0, BotUseCount = n` statt gar keiner Zeile — Bot-Nutzung wird erhalten, nicht verworfen.

**E2 — Erkennung in dieser Runde: statische Liste, dann messen.** Zwei Schichten (`bot-badge` → sechs am 2026-09-01 gegen die Twitch-API verifizierte Konten-IDs, ergänzbar über `Twitch:AdditionalBotAccountIds`); die im Issue skizzierte dritte Schicht, ein Import aus einer dynamischen Bot-Datenbank, entfällt vorerst — nicht aus Bequemlichkeit, sondern weil eine Live-Messung gegen `twitchbots.info` am 2026-09-01 den Aufwand nicht rechtfertigt: von 18.450 gelisteten Bots (18.684 inklusive inaktiver) hängt der Großteil an genau einer `channelID`, nur 824 (903 inklusive inaktiver) sind Multi-Channel-Bots — die einzige Teilmenge, die für uns lohnt, weil ein Einzelchannel seinen eigenen Bot ohnehin über den Konfigschlüssel nachträgt. Diese Multi-Channel-Teilmenge ist zudem schlecht gepflegt (nur 17,6 % seit 2022 aktualisiert, Pflegegipfel 2018), und `GET /v2/channel/{id}/bots` lieferte für eine getestete große Channel-ID `total: 0` — der Endpunkt, der die Prüfung eigentlich billiger machen sollte, trägt nichts bei. Dabei eine Falle festgehalten, damit sie niemand erneut aufdeckt: **`?username=` wird von der API stillschweigend ignoriert** — ein Aufruf mit `?username=sery_bot` liefert die ungefilterte 18.450er-Gesamtliste zurück, inklusive `total: 18450`, was auf den ersten Blick wie ein Treffer aussieht. Sobald `BotUseCount` echte Zahlen liefert, entscheidet diese Messung — nicht eine Vermutung —, ob ein Import oder eine Pflegeoberfläche für die dritte Schicht überhaupt lohnt.

**E3 — kein Toggle im Frontend.** Die Standardanzeige zählt Menschen; ein dauerhaftes Bedienelement „mit Bots" wird nicht gebaut, weil es eine Frage beantwortet, die der Erstbesuch nicht stellt, und ohne Messung unbekannt ist, ob die Bot-Zahl überhaupt interessant ist. Die zweite Spalte hält die Option offen, falls die Zahlen später dafür sprechen. Stattdessen ein reiner Hinweis mit Datum nach dem Muster der bestehenden `liveDayCoverage`-Bildunterschriften — eine Ehrlichkeitsaussage, kein Steuerelement.

**E4 — das Hinweisdatum wird pro Channel aus `MIN(Date)` über Zeilen mit `BotUseCount > 0` abgeleitet, nicht aus einer gepflegten Konstante.** Eine Konstante würde den Bruch auch für Channels behaupten, in denen nie ein Bot erkannt wurde, und liefe auseinander, sobald Dev und Prod an verschiedenen Tagen deployen. **Bekannte Einschränkung, bewusst in Kauf genommen:** Das abgeleitete Datum ist die erste *Sichtung* eines Bots in den Daten, nicht der Beginn der Trennung selbst — der eigentliche Bruch ist ein Deploy-Ereignis, kein Datenereignis. Alte und neue Zeilen sind bei `BotUseCount = 0` ununterscheidbar, weil kein aus den Daten ableitbarer Diskriminator existiert; ein korrekter Fix bräuchte neuen Zustand (eine Markierung beim ersten Flush mit dem neuen Code), den diese Entscheidung bewusst vermeidet. Der naheliegende Alternativvorschlag, gegen `TrackedSince` (den letzten Join) zu vergleichen, trifft den Fall ebenfalls nicht — bei einem Join am 10.09. und dem ersten erkannten Bot erst am 11.09. würde der Hinweis weiterhin erscheinen und wäre weiterhin falsch, nur mit einem anderen Datum. Die Fehlrichtung des gewählten Verfahrens ist konservativ: In dem schmalen Fenster zwischen Deploy und erster Bot-Sichtung rät der Hinweis zur Vorsicht bei Zahlen, die in Wahrheit schon sauber sind — das ist der günstigere Fehler als der umgekehrte.

**Der Covering-Index `(EmoteId, Date) INCLUDE (UseCount)` bleibt unverändert; `BotUseCount` steht nicht in `IncludeProperties`.** Die fünf Aggregat-Lesequeries lesen weiterhin nur `UseCount`, der Index-Only-Scan bleibt erhalten, und der Index wird nicht für eine Spalte verbreitert, die heute niemand liest. Neu zu bewerten ist das erst, falls der aus E3 zurückgestellte Toggle je gebaut wird und eine Lesequery `BotUseCount` tatsächlich braucht.

**Watchdog-Reihenfolge in `TwitchChatManager.OnMessageReceived` ist jetzt ein Vertrag, nicht nur ein Zufall der Lesereihenfolge:** Die Bot-Klassifikation steht **nach** den beiden Watchdog-Schreibvorgängen (`_lastMessageReceivedUtcTicks`, `_lastMessageByChannelTicks`) und dem Debug-Log. Eine Bot-Nachricht beweist, dass der Socket lebt; würde sie vor der Watchdog-Buchführung aussortiert, erfände der Watchdog stille Verbindungen und erzwänge Reconnects — exakt das Fehlerbild, das am 2026-08-03 behoben wurde (s. Eintrag „Watchdog misst Frames statt Chat"). Die Klassifikation selbst läuft genau einmal pro Nachricht, nach dem frühen Return bei leerem `channelEmotes` und vor der Token-Schleife, nie pro Token oder Treffer.

**Konfigschlüssel `Twitch:AdditionalBotAccountIds`** (kommagetrennter Skalar oder indiziertes JSON-Array, nach dem Muster von `ChannelAccessService.GetAdminLogins`) ergänzt die sechs statischen IDs, ersetzt sie nie — die Lücke, die E2 bewusst offenlässt (ein channel-eigener Bot, der in keiner Fremdliste steht), ist damit ohne Release nachtragbar, auch in Prod.

**Prod-Reihenfolge:** Weil die Migration additiv ist (`AddColumn` mit `DEFAULT 0` auf der Spalte selbst, kein Rewrite der größten Tabelle unter Postgres ≥ 11), gilt der Standardweg aus `CLAUDE.md` — erst von Hand über den SSH-Tunnel migrieren, dann die neuen Images deployen. Das noch laufende alte Image schreibt seinen `UNNEST`-Upsert weiter ohne die neue Spalte; genau deshalb muss der Default auf der Spalte selbst stehen, nicht nur im EF-Modell.

**Die vier Lesestellen in `UsageStatQueryService`, die eine `UsageStat`-Zeile als „benutzt" lesen, brauchen ein `UseCount > 0`-Prädikat — Begründung hier, Umsetzung in einem eigenen Folge-Commit.** Bislang war „Zeile existiert" gleichbedeutend mit „Zeile hat `UseCount > 0`", weil der Flush nie eine Nullzeile geschrieben hat. Mit der Bot-Spalte gilt das nicht mehr: Eine Zeile mit `UseCount = 0, BotUseCount = n` (ein Emote, das an einem Tag ausschließlich von Bots gepostet wurde) sähe für diese Stellen wie „heute benutzt" aus, obwohl kein Mensch es benutzt hat. Das Prädikat ist auf allem, was vor diesem Commit geschrieben wurde, ein No-op — Bestandszeilen haben nie `UseCount = 0` — und bleibt im Index-Only-Scan, weil `UseCount` bereits Include-Spalte ist.

**Ausdrücklich nicht gebaut:** kein Import aus `twitchbots.info` (E2), keine Pflegeoberfläche für Bot-Listen, kein Log pro erkannter Bot-Nachricht (Hot Path), kein Frontend-Toggle (E3), keine Erweiterung des Unique-Index um eine Bot-Dimension, kein `HasDefaultValue` im EF-Modell (der DB-Default kommt bewusst aus der Migration, nicht aus dem Modell — EF-Inserts liefern ihren CLR-Wert ohnehin selbst).

### 2026-09-01 — Verlorenes Redis-LEAVE: symmetrischer Prune-Schritt statt Outbox

**Betrifft:** `src/EmotePurge.Worker/RosterPrunePolicy.cs`, `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs`, `src/EmotePurge.Infrastructure/Redis/ChannelResyncCooldown.cs`, `src/EmotePurge.Infrastructure/Services/ChannelService.cs`, `tests/EmotePurge.Worker.Tests/RosterPrunePolicyTests.cs`, `tests/EmotePurge.Infrastructure.Tests/Unit/ChannelResyncCooldownFailureModeTests.cs`

Issue #41: `ChannelService` committet die DB, bevor sie das JOIN/LEAVE/RESYNC-Kommando an den Worker publiziert. Wirft `PublishAsync` bei einem Redis-Ausfall, bekommt der Aufrufer einen 500, obwohl die DB-Zeile bereits im Zielzustand steht.

**Der Schaden war kleiner als das Issue zunächst nahelegte — verifiziert, nicht übernommen.** JOIN heilt sich von selbst: `SevenTvPeriodicResyncWorker` ruft für jeden aktiven Channel im 60-s-Takt `EnsureJoinedAsync`, das genau die verlorene Join-Absicht nachholt. RESYNC heilt sich ebenso von selbst: derselbe Tick resynct jeden aktiven Channel ohnehin unbedingt, ein verlorenes Kommando kostet also höchstens eine Minute Verzögerung. Ein „gesetzter Resync-Marker", wie ihn eine Formulierung im Issue nahelegte, existiert im Code nicht — `TriggerResyncAsync` schreibt nur einen Audit-Eintrag, `Channel.LastSyncedAtUtc` wird ausschließlich von `SevenTvSyncService` bei einem tatsächlich abgeschlossenen Sync gesetzt. **Nur LEAVE heilte nicht:** `TwitchChatManager._desiredChannels` wird ausschließlich durch den Redis-LEAVE-Handler in `Worker.cs` geräumt (drei Schritte: `EmoteMatchCache.RemoveChannel`, `ISevenTvEventClient.Unsubscribe`, `LeaveChannelAsync`); der periodische Tick iteriert nur über *aktive* Channels und hat nie geprunt, was aus der Liste gefallen ist. Ein verlorenes LEAVE ließ den Worker deshalb unbegrenzt im Chat, zählte Nutzung für einen laut DB inaktiven Channel weiter, hielt das 7TV-EventAPI-Abo und meldete den Channel im Admin-Roster.

**Fix: `RosterPrunePolicy` als eigene, pure Entscheidungsklasse**, aufgerufen aus `SevenTvPeriodicResyncWorker.ResyncOnceAsync` unmittelbar nach dem `ListActiveChannelNamesAsync`-Snapshot und vor der Sync-Schleife (nicht danach — die Schleife macht pro Tick mehrere 7TV-REST-Calls und hätte den Abstand zwischen DB-Snapshot und Prune-Entscheidung auf Sekunden gedehnt). Die drei Schritte für einen zu prunenden Channel sind exakt die des bestehenden Redis-LEAVE-Handlers in `Worker.cs` — dieselbe Reihenfolge, dieselben Aufrufe, damit es nur einen Ort gibt, an dem „ein Channel verlässt den Worker" definiert ist. In `SevenTvPeriodicResyncWorker` statt in `TwitchChatManager`: Erstere besitzt bereits den periodischen Tick und den DB-Zugriff über `IChannelService`; Letztere bliebe frei von einer DB-Abhängigkeit, die architektonisch nicht dorthin gehört (der Chat-Manager kennt nur Twitch, keine Channel-Aktivität).

**Randfall durchdacht: ein frisch joinender Channel darf nicht sofort geprunt werden.** `ChannelService.JoinAsync` committet vor dem Publish; der Worker fügt einen Channel erst zu `_desiredChannels` hinzu, wenn die Redis-Nachricht ankommt — zu diesem Zeitpunkt ist die DB-Zeile also bereits sicher committet. Trotzdem liest READ COMMITTED pro Statement einen eigenen Snapshot: startet `ListActiveChannelNamesAsync` *vor* diesem Commit, sieht es die neue Zeile nicht, während `_desiredChannels` Sekundenbruchteile später doch schon den Channel enthält — ein `roster \ activeChannels`-Diff würde ihn fälschlich prunen. Der erste Entwurf schützte davor, indem `RosterPrunePolicy.DetermineChannelsToPrune` **nur bestätigte** (`JoinConfirmed == true`) Roster-Einträge prunte — das schloss die Race, öffnete aber eine eigene Lücke, die die Codex-Zweitmeinung als P2 fand: Ein Eintrag, der nie bestätigt (gebannter Bot, gelöschter Channel, hängender Join), wurde bei **jedem** Tick übersprungen, unbegrenzt, weil nichts außer einem echten Twitch-JOIN `JoinConfirmed` je auf `true` setzt.

**Fix der Lücke: eine Karenz über aufeinanderfolgende Stale-Beobachtungen statt der dauerhaften `JoinConfirmed`-Bedingung.** `DetermineChannelsToPrune` bekommt jetzt zusätzlich die vom vorigen Aufruf zurückgegebene Stale-Menge herein und gibt die aktuelle mit heraus (`RosterPruneResult`); `SevenTvPeriodicResyncWorker` hält sie als einziges Instanzfeld zwischen den Ticks. Ein Eintrag, der in zwei aufeinanderfolgenden Ticks inaktiv ist, wird geprunt — unabhängig von `JoinConfirmed`. Bei 60 s Tickabstand sind das 60–120 s Karenz, weiterhin deutlich mehr als ein Twitch-JOIN-Roundtrip braucht, und die ursprüngliche Race bleibt damit ebenso abgedeckt wie zuvor: Ein frisch gejointer Channel ist im ersten Tick nach seinem Erscheinen im Snapshot bestenfalls einmal stale, nie zweimal hintereinander. Selbst falls das Zeitfenster doch getroffen würde, ist der Schaden begrenzt — der nächste Tick joint den fälschlich geprunten Channel über `EnsureJoinedAsync` wieder, weil er weiterhin in `activeChannels` steht. Ein Prune ist also bestenfalls unnötiges Flackern, nie ein dauerhafter Verlust wie beim ursprünglichen Bug — und anders als vorher gilt das jetzt auch für Einträge, die nie bestätigen.

**`ChannelResyncCooldown` degradiert fail-open, nicht fail-closed.** `TryBeginAsync`/`ReleaseAsync` fangen jetzt `RedisException`/`TimeoutException`; bei einem Ausfall wird der Cooldown übersprungen (`Acquired = true`) statt den Request mit 500 sterben zu lassen. Ausschlaggebend: `POST /{channelName}/resync` hängt bereits hinter einem eigenen, rein In-Process-basierten ASP.NET-Rate-Limiter (`RateLimitPolicyNames.ChannelResync`, fixes Fenster pro Nutzer) — der ist von einem Redis-Ausfall vollkommen unberührt und deckt exakt die Missbrauchsdimension „ein Nutzer spammt". `IChannelResyncCooldown` deckt die andere Dimension ab (viele Nutzer teilen sich ein Kanal-Budget) und ist laut eigenem Interface-Kommentar ausdrücklich Kosten-, nicht Sicherheitskontrolle: Der teure Teil eines Resyncs ist der `channel.synced`-Live-Event-Fan-out an alle offenen Tabs, keine Angriffsfläche. Ein Redis-Ausfall lässt also nur die kanalweite Budget-Teilung entfallen, während die Pro-Nutzer-Grenze weiter greift — ein begrenztes, temporäres Risiko gegenüber der Alternative, Self-Service-Resync für die gesamte Ausfalldauer komplett zu sperren.

**`PurgeAsync` bleibt unverändert.** Dort steht der Publish (LEAVE) bereits vor `SaveChangesAsync` — bei einem Redis-Ausfall bricht die Methode mit 500 ab, ohne dass die DB angefasst wurde; das ist konsistent, nur unverfügbar. Ein eigener 503 statt des generischen 500 wurde erwogen und zurückgestellt: das Issue selbst führt diesen Punkt als niedrigste Priorität der drei, und der heutige `ApiErrorCodes.UnexpectedError` ist zumindest ehrlich in der Aussage „nichts ist passiert". Kein neuer Fehlercode, keine i18n-Kette dafür in diesem Arbeitspaket (Regel 7 gilt erst, sobald einer gebraucht wird).

**Ausdrücklich nicht gebaut:** keine Outbox-Tabelle für drei Kommandos — der periodische Tick übernimmt die Rolle des Zustellers bereits vollständig, für JOIN/RESYNC ohnehin, jetzt auch für LEAVE. Kein direktes Abfangen von `RedisPublisher.PublishAsync` in `JoinAsync`/`LeaveAsync`/`TriggerResyncAsync` selbst: Der 500 bei einem echten Ausfall bleibt bestehen, ist aber jetzt harmlos, weil er nichts mehr über den tatsächlichen Zustand aussagt, den der periodische Tick ohnehin binnen einer Minute herstellt — Publish ist Beschleunigung, keine Voraussetzung mehr.

### 2026-09-01 — `ILiveEventStream.SubscribeAsync` gibt einen Status zurück statt `null` für drei verschiedene Zustände

**Betrifft:** `src/EmotePurge.Core/Messaging/ILiveEventStream.cs`, `src/EmotePurge.Infrastructure/Redis/RedisLiveEventStream.cs`, `src/EmotePurge.Api/Endpoints/LiveEndpoints.cs`, `src/EmotePurge.Api/Validation/ApiErrorCodes.cs`, `web/src/app/core/i18n/api-error.ts`, `web/public/i18n/{de,en}.json`

Issue #42, gefunden beim Live-Test von #37 am 2026-08-31: `RedisLiveEventStream.SubscribeAsync` gab `null` für drei fachlich verschiedene Zustände zurück — Redis nicht abonnierbar, prozessweites Verbindungslimit erreicht, Per-Login-Limit erreicht — und `LiveEndpoints.OpenAsync` übersetzte alles in denselben blanken 503 ohne Body und ohne Code. Ein Betreiber konnte einen echten Redis-Ausfall nicht von einem erschöpften Verbindungskontingent unterscheiden; im konkreten Fall hat das rund eine halbe Stunde Fehlersuche gekostet, weil der 503 nach Redis' Rückkehr bestehen blieb und wie ein kaputter Wiederanlauf aussah, obwohl nur das Kontingent belegt war.

**Derselbe Vertrag wie #32/#37.** `ILiveEventStream.SubscribeAsync` gibt jetzt `Task<LiveEventSubscribeResult>` zurück statt `Task<ILiveEventSubscription?>` — ein `LiveEventSubscribeStatus`-Enum (`Ok`/`InfrastructureUnavailable`/`QuotaExhausted`) plus ein Ergebnis-Record mit den zwei Factory-Methoden `Ok`/`Failed`, exakt das Muster von `SevenTvLookupStatus`/`SevenTvChannelStateResult`. `LiveEndpoints.OpenAsync` mappt `InfrastructureUnavailable` auf 503 (`live_stream_unavailable`) und `QuotaExhausted` auf 429 mit `Retry-After` (`live_stream_quota_exhausted`), beide mit sprachneutralem Fehlercode im Body statt eines leeren Statuscodes.

**Prozessweites und Per-Login-Limit teilen sich einen Statuswert, bewusst.** Anders als bei #32/#37, wo jede der drei/vier Ursachen einen eigenen Grund bekam, weil sie fachlich unterschiedliche Handlungsanweisungen an den Nutzer tragen (kein 7TV-Account vs. kein aktives Set vs. 7TV nicht erreichbar), unterscheiden sich `MaxSubscriptions` (prozessweit, 500) und `MaxPerSubscriber` (pro Login, 6) für einen Aufrufer nicht in der Handlungsanweisung: in beiden Fällen heißt die Antwort „kein Slot gerade frei, versuch es gleich noch mal" — ob der eigene Login sechs Tabs offen hält oder der Prozess insgesamt voll ist, ändert nichts daran, was der Client tun soll. Ein Betreiber, der die beiden doch auseinanderhalten will, hat dafür weiterhin die zwei unterschiedlichen Log-Zeilen in `RedisLiveEventStream` (`LogWarning` fürs Prozesslimit, `LogInformation` fürs Login-Limit) — die Trennung existiert also, nur nicht auf dem Wire, wo sie ohnehin niemand unterschiedlich behandeln würde. Ein dritter Statuswert hätte außerdem die API-Oberfläche (Enum-Wert, Fehlercode, zwei Locale-Einträge) für eine Unterscheidung vergrößert, die kein Aufrufer je auswertet.

**`retryAfterSeconds` ist beim neuen 429 ein fester Heuristikwert (30 s), keine gemessene Restzeit** — anders als bei `resync_cooldown_active`, dessen Cooldown eine echte ablaufende Sperre mit bekannter Restdauer ist. Weder das Prozess- noch das Login-Limit kennt einen Ablaufzeitpunkt; ein Slot wird frei, sobald ein Tab schließt oder ein Stream sein `MaxConnectionLifetime` (10 min) erreicht. 30 s ist ein Kompromiss zwischen „ein Reconnect-Sturm hämmert nicht sekündlich" und „ein freigewordener Slot (der Normalfall — Tabs schließen laufend) ist zeitnah wieder nutzbar".

**Frontend-sichtbarer Hinweis ist ausdrücklich Stufe 2, nicht Teil dieser Änderung** — `EventSource.onerror` reicht weder Statuscode noch Body durch, das Verhalten von `LiveUpdateService` bleibt unverändert. Die i18n-Kette (`api-error.ts`, beide Locale-Dateien) ist trotzdem vollständig gepflegt, weil Regel 7 das unabhängig vom aktuellen Frontend-Verhalten verlangt und `api-error-locales.spec.ts` sie erzwingt.

**Nebenfund mitgezogen:** Der Kommentar in `LiveEventStreamOptions.MaxPerSubscriber`, der das Sharing einer Verbindung pro URL im Frontend als „structural fix (future)" führte, ist korrigiert — das ist seit dem 2026-08-29-Commit zu `live.changed` in `LiveUpdateService.stream()` bereits umgesetzt.

**Die `_redisSubscribed`-Klinke bleibt, wie sie ist — geprüft und verworfen.** Bei der Live-Verifikation fiel auf, dass `EnsureRedisSubscribedAsync` nach dem ersten erfolgreichen Abonnieren nie wieder prüft: stoppt man danach Redis, bekommt ein neuer Client 200 plus Ping statt des frisch gebauten 503. Die naheliegende Vermutung, der Stream sei danach dauerhaft stumm, ist **gemessen widerlegt** (`RedisLiveEventStreamOutageTests`, eigener Container, echtes Stop/Start auf fester Host-Portbindung): StackExchange.Redis stellt das Kanal-Abo beim Reconnect selbst wieder her, und weil der Fan-out prozessweit über genau ein Abo läuft, empfängt eine **während** des Ausfalls aufgenommene Verbindung danach wieder mit — ohne Zutun des Clients. Ein 503 an dieser Stelle wäre für den Client **schlechter**, nicht besser: `LiveUpdateService` behandelt jeden Nicht-2xx-Handshake als endgültig (`readyState CLOSED`, genau ein Sichtbarkeits-Retry), ein vorübergehender Redis-Ausfall würde also einen Tab dauerhaft totlegen, der sich heute von allein erholt. Während des Ausfalls veröffentlichte Events sind ohnehin für alle verloren — Redis-Pub/Sub puffert nichts —, die mitten im Ausfall aufgenommene Verbindung steht also nicht schlechter da als die vor ihm geöffneten. Der Betreiber erkennt einen Redis-Ausfall weiterhin an `GET /api/health` (liest den Worker-Snapshot aus Redis, antwortet 503); dieser Pfad hängt nicht an der Klinke. Geändert wurde deshalb nur der Kommentar an `EnsureRedisSubscribedAsync`, der die Klinke bisher allein von der anderen Seite her begründete („das Flag darf nie verbrannt werden").

### 2026-09-01 — 7TV nullt `emote_set`: der Anker ist die Set-ID, nicht das eingebettete Objekt

**Betrifft:** `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs`, `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiDtos.cs`, `tests/EmotePurge.Infrastructure.Tests/Unit/SevenTvApiClientChannelStateTests.cs`

7TV hat angekündigt, `GET /v3/users/twitch/:id` künftig ohne das eingebettete `emote_set`-Objekt zu beantworten; wer den vollen Satz braucht, soll die Set-ID nehmen und `GET /v3/emote-sets/:id` nachladen. Kein Datum, kein Changelog, kein Issue — `SevenTV/API` ist seit 2024-12-17 archiviert. Das trifft unseren **einzigen** Weg an die Emote-Liste (Issue #43).

**Das Schadensbild wäre nicht als Ausfall aufgefallen.** `GetChannelStateForTwitchUserAsync` hatte für ein fehlendes `emote_set` bereits eine fertige Antwort: `NoActiveEmoteSet`. Am Tag der Umstellung hätte also jeder Kanal gemeldet, er habe kein aktives Emote-Set — kein Absturz, kein Alert, sondern ein plausibel klingender Fehlergrund, den wir seit #32/#37 dem Nutzer als sein Problem anzeigen. Betroffen ist die ganze Sync-Kette: Boot-Recovery, `SevenTvPeriodicResyncWorker`, jeder Erstsync. Die EventAPI-Deltas liefen weiter, sind ohne den REST-Vollsync als Reconciliation aber nicht tragfähig.

**Gemessen am 2026-09-01, live gegen 7tv.io.** Das Top-Level-`emote_set` ist noch befüllt (handofblood 933, xqc 966, avokati 996, pokimane 721 Emotes). Aber dasselbe Objekt eine Ebene tiefer — `user.connections[].emote_set` — ist bei allen geprüften Kanälen **bereits `null`**, während `connections[].emote_set_id` gefüllt bleibt und mit der Top-Level-ID übereinstimmt. Sie nullen das Objekt und behalten die ID; der Rollout hat begonnen, nur dieses eine Feld noch nicht erreicht.

**Der Migrationshinweis führt in die Irre.** „`emote_sets` will continue working exactly as expected" verschweigt zweierlei: Der Plural liegt nicht auf Top-Level, sondern unter `.user` — und seine Einträge tragen nur Metadaten (`id, name, flags, tags, capacity`), **keine Emotes und keine Markierung, welches Set aktiv ist**. handofblood hat 3 Sets, pokimane 10; die Position im Array trägt nichts. Wer dem Hinweis wörtlich folgt, synchronisiert das Halloween-Set. Die aktive Set-ID kommt ausschließlich aus `emote_set_id`.

**Deshalb kein Stichtag, sondern beidseitig tragfähig.** Die Set-ID wird aufgelöst über `emote_set.id` → Top-Level `emote_set_id` → die Connection mit `platform == "TWITCH"` **und** exakt passender Twitch-ID. Ist das Objekt da, bleibt es bei **einem** Request wie bisher; ist es weg und eine ID da, wird nachgeladen.

**Und nichts darüber hinaus** — das war der Befund der Codex-Zweitmeinung an diesem Arbeitspaket. Der erste Entwurf hatte als letzte Stufe „die erste `TWITCH`-Connection mit brauchbarer ID" vorgesehen, gedacht als Gürtel-und-Hosenträger. Sie ist es nicht: Jeder Kandidat jenseits der exakt angefragten Connection gehört einem **anderen** Kanal — eine YouTube-/Kick-Verbindung ebenso wie eine zweite Twitch-Verbindung desselben 7TV-Accounts. Erreichbar wäre so eine Stufe ohnehin nur dann, wenn die angefragte Connection kein eigenes Set hat, und genau das ist der Fall, den `NoActiveEmoteSet` zu melden hat. Stattdessen zuzugreifen hieße, dass `SevenTvSyncService` eine fremde Set-ID persistiert und die Emotes eines fremden Kanals in diesen hineinabgleicht — worauf dann Abstimmung und 7TV-Massenlöschung arbeiten würden. Stiller Datenmüll statt eines sichtbaren, wahrheitsgemäßen Fehlergrunds. Die Auflösung hält damit denselben Exakt-ID-Vertrag wie `ResolveSevenTvIdentityAsync` seit #34, und ein eigener Testfall (zwei `TWITCH`-Connections, die angefragte ohne Set) hält ihn fest.

**Die Fehlergrund-Semantik aus #32/#37 bleibt scharf.** `NoActiveEmoteSet` sagt jetzt nur noch, was es behauptet: es gibt **keine Set-ID**. Ein gescheiterter Nachladeschritt — Non-Success inklusive 404, leerer Body, Timeout — ist `Unavailable`, nie ein Vorwurf an den Kanalbesitzer. Dass dieser Zweig überhaupt erreichbar bleibt, ist nicht angenommen, sondern gemessen: 62 live gefundene 7TV-Accounts ohne aktives Set antworten durchweg mit einem schlichten `emote_set_id: null`, oben wie in `connections[]` — **nicht** mit der Null-ObjectID, die 7TV an anderer Stelle als Platzhalter liefert (s. Eintrag zum Platzhalter-Nutzer vom 2026-08-31). Die Abwehr gegen einen Sentinel aus lauter Nullen bleibt trotzdem drin, ist aber ausdrücklich unbelegte Vorsorge und nicht die Reparatur eines beobachteten Verhaltens.

**Am Mapper ist nichts zu ändern.** `/v3/emote-sets/:id` ist feldgleich: identische Top-Level-Keys, identische `data.host.files`-Keys, alle von `SevenTvEmoteJsonMapper` gelesenen Felder vorhanden, `capacity` vorhanden, keine Paginierung und kein Deckel (933/933 dicht an der 1000er-Kapazität). Der End-to-End-Beleg lief gegen echte 7TV-Zugänge mit temporär erzwungenem Fallback: gleiche Set-ID, gleiche Emote-Anzahl, gleiche Capacity, gleiche Namen, Bild-URLs und `AddedToSetAt`-Zeitstempel wie im Normalpfad. **v4-GQL ist kein Ersatz** — dort heißt `name` `defaultName`, `files[]` ist `images[]` mit `scale`/`mime` statt Dateinamen, ein Gegenstück zu `static_name` fehlt; `BuildImageUrl` müsste neu geschrieben werden.

**Die Log-Zeile zum Fallback steht bewusst auf einer Latch.** Sie soll den Rollout sichtbar machen, statt ihn raten zu lassen — aber sobald 7TV umgestellt hat, ist der Fallback der Dauerzustand *jedes* Kanals bei *jedem* Resync-Tick. Eine `Information`-Zeile dort wäre genau die Spam-Falle, wegen der `NoActiveEmoteSet` in derselben Methode seit #32 auf `Debug` steht (~1440 Zeilen pro Tag und Kanal). Der erste Eintritt pro Prozess loggt deshalb `Information`, jeder weitere `Debug`.

**Was der Umbau kostet:** nach der Umstellung verdoppelt sich die *Zahl* der REST-Requests pro Kanal und Tick. Das Datenvolumen bleibt, wo es war — die erste Antwort schrumpft ja um genau das Set, das die zweite bringt. Offen bleibt, ob `/v3/emote-sets/:id` selbst abkündigungsgefährdet ist: dokumentiert ist dazu nichts, und v3 wird sichtbar nicht mehr gepflegt.

### 2026-09-01 — Eine Prozentschwelle beantwortet keine Pixelfrage

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.html`, `web/e2e/usage-atlas.e2e.spec.ts`, `docs/UI-Designsprache.md`

Der Verteilungsstreifen schnitt seine Segment-Beschriftungen ab — gemeldet als „auf dem Handy und auf Deutsch steht da *20 % selte*". Jede Beschriftung saß in einem `<span>`, dessen `width` der Nutzungsanteil ihres Segments war, dazu `overflow-hidden whitespace-nowrap`. Was nicht hineinpasste, wurde weggeschnitten. Der Schutz davor war die Bedingung `band.share >= 0.09`, wörtlich auch in der Designsprache festgeschrieben: „Ein Segment wird nur beschriftet, wenn es mindestens 9 % breit ist."

**Die Regel stellte die falsche Frage.** Sie fragt „ist das Segment anteilig groß?", entscheiden muss aber „hat dieses Segment genug *Pixel* für dieses *Wort*" — und das hängt an zwei Größen, die ein Prozentwert nicht kennt: der Breite des Bildschirms und der Länge des Wortes in der aktiven Sprache. Jede Nachjustierung an der Zahl hätte das Problem nur zwischen Sprachen und Geräten hin- und hergeschoben.

**Gemessen statt geschätzt** (Wegwerf-Playwright-Sonde, `scrollWidth` gegen `getBoundingClientRect().width`, deutsche Locale):

| Viewport | Beschriftung | verfügbar | gebraucht |
|---|---|---|---|
| 393 px | „11 % regelmäßig" | 26 px | 105 px |
| 768 px | „11 % regelmäßig" | 38 px | 105 px |
| 1280 px | „11 % regelmäßig" | 68 px | 105 px |
| 1280 px | „12 % selten" | 74 px | 77 px |

Zwei Dinge daran haben den Fix entschieden. **Es war nie ein Mobile-Fehler:** abgeschnitten wurde auf jeder gemessenen Breite, auch auf 1280 px — auf Englisch nur unauffälliger, weil „regular" und „rare" kürzer sind als „regelmäßig" und „selten", und bei „12 % selten" fehlten am Desktop ganze 3 px, was wie ein Rendering-Zufall aussieht statt wie ein Fehler. **Und die naheliegende Reparatur wäre wirkungsarm gewesen:** eine Container-Query, die das Wort nur zeigt, wo es wirklich passt, hätte die richtige Frage gestellt — aber ein 11-%-Band erreicht die nötigen 105 px auf keinem realistischen Bildschirm, das Wort wäre also fast überall verschwunden statt gerettet worden.

**Deshalb trägt jetzt eine umbrechende Legende die Beschriftung** — Farbplättchen, Anteil, Bandname, nach Inhalt bemessen, mit Umbruch statt Beschnitt. Aufgegeben wird die Ausrichtung des Textes unter seinem eigenen Segment; sie war bei 11 % Breite ohnehin keine, und das Farbplättchen stellt die Verbindung zum Balken zuverlässiger her als eine Position, die nur bei großen Bändern trägt. Die Schwelle entfällt ersatzlos: `usageSegments()` filtert bereits auf `usage > 0`, es sind also höchstens drei Einträge, und `formatBandShare` fängt ein durch einen Namensfilter winzig gewordenes Band längst mit „<1 %" ab.

**Der Regressionsschutz misst den Fehler, nicht sein Symptom.** Der E2E-Fall prüft bei 393 px für jeden Legendeneintrag, dass `scrollWidth` seine sichtbare Breite nicht übersteigt — derselbe Messwert, mit dem der Fehler nachgewiesen wurde. Eine Zusicherung auf die Bandnamen allein hätte den ursprünglichen Zustand für gesund erklärt: die Namen *standen* da, sie waren nur halbiert. Der Fall schlägt damit auch an, wenn später eine längere Übersetzung hinzukommt.

**Ein Attribut ist neu im Markup:** `data-distribution-legend`. Das Repo kennt keine Test-Hooks und greift sonst über Rollen und Texte — hier reicht das nicht, weil „abgeschnitten" keine Rolle hat und die Bandnamen doppelt vorkommen, in der Legende und als Bandüberschrift im Bogen. Ein benannter Anker ist der ehrlichere Weg als ein Locator über Tailwind-Klassen, der beim nächsten Umbau still bricht.

### 2026-09-01 — Kein Third-Party-Login bei 7TV: geprüft, verworfen, und zwar endgültig genug für ein `grep`

**Betrifft:** `docs/Untersuchung-7TV-Token-Login-2026-07-30.md`, `docs/Architectur.md` (Grundsatz 4), `web/src/app/core/seven-tv/seven-tv-token.service.ts`, `web/src/app/shared/seven-tv/seven-tv-token-input.ts`

Die Frage, ob EmotePurge das 7TV-Schreib-Token per Login-Redirect beschaffen kann statt es Nutzer aus den DevTools kopieren zu lassen, ist jetzt zum dritten Mal gestellt worden: am 2026-07-24 blind entschieden, am 2026-07-30 belegt untersucht, am 2026-09-01 erneut aufgeworfen — beim Blick auf `https://7tv.app/api/docs#/Authentication/get_v4_auth_login`. Die Untersuchung von damals hatte genau das vorhergesagt und empfohlen, das Ergebnis hier festzuhalten, „damit die Frage nicht erneut aufkommt". Das ist versäumt worden; dieser Eintrag holt es nach.

**Es geht nicht, und der Grund steht im Server-Code**, nicht in einer Doku, die sich ändern könnte. `apps/api/src/http/v4/rest/auth.rs` im Monorepo `seventv/seventv`:

```rust
let allowed = [&config.api_origin, &config.old_website_origin, &config.website_origin];
if !allowed.iter().any(|a| root_origin_match(a, &return_to)) {
    return Err(ApiError::forbidden(ApiErrorCode::BadRequest, "return_to origin mismatch"));
}
```

Die Allowlist ist fest verdrahtet (`7tv.app`, `old.7tv.app`, `7tv.io`) und hat keine Konfigurationsoption für weitere Origins. `client_id`, `redirect_uri` und `scope` gegenüber Twitch baut der Server aus seiner eigenen registrierten OAuth-App; der Aufrufer kann keines davon setzen. `return_to` ist der einzige Parameter mit Rückkanal-Charakter — und genau der wird geprüft.

**Live gegengeprüft am 2026-09-01**, ohne Anmeldung und ohne Token:

| Aufruf | Antwort |
|---|---|
| `GET /v4/auth/login` ohne Parameter | `400`, `missing field 'platform'` |
| `?platform=twitch` | `303` → `id.twitch.tv/…?client_id=jzsoiyuc…&redirect_uri=https%3A%2F%2F7tv.app%2Flogin%2Fcallback` |
| `?platform=twitch&return_to=https://example.org/cb` | **`403` `return_to origin mismatch`** |
| dasselbe **ohne** `Referer`-Header | ebenfalls **`403`** |
| `?platform=twitch&return_to=https://7tv.app/settings` | `303`, `return_to` wird als `state` durchgereicht |

Das schärft Punkt (1) der Untersuchung vom 2026-07-30: dort waren `Referer`, `Origin` und `return_to` gemeinsam als Prüfkette genannt. Tatsächlich genügt der `return_to`-Wert allein für die Ablehnung, der Referer spielt dafür keine Rolle. Am Ergebnis ändert das nichts, es macht die Tür nur enger.

**Zwei Beobachtungen, die dem nächsten Nachfragenden Zeit sparen.** Erstens: v4-Auth ist keine Neuerung, die man beobachten müsste — der älteste Commit an `auth.rs` datiert auf den 2024-12-11, der Endpunkt existierte also schon lange vor unserer Untersuchung. Zweitens: der Doku-Deep-Link taugt nicht mehr als Quelle. `https://7tv.app/api/docs` liefert für jeden Pfad dieselbe SvelteKit-App-Shell, die früher greifbare `v4docs.json` ist verschwunden, und im `main`-Branch gibt es überhaupt keinen OpenAPI-Generator für v4 mehr (`docs.rs` existiert nur unter `v3/`). Abrufbar ist nur noch die v3-Spec unter `https://api.7tv.app/v3/docs`. Wer die Frage erneut prüft, prüft sie am Quellcode und am Endpunkt, nicht an der Doku-Seite.

**Damit ist Grundsatz 4 („Zero-Knowledge für Schreib-Tokens", `docs/Architectur.md`) eine Folge, keine freie Wahl.** Der Token bleibt im `sessionStorage` des Browsers und erreicht das Backend nie — das ist sicherheitstechnisch das Beste, was aus dieser Lage zu holen ist, und es bleibt richtig. Es ist aber nicht so entstanden, dass wir zwischen serverseitiger Token-Verwaltung und Zero-Knowledge abgewogen hätten: die erste Option gab es nie. Eine echte App-Integration mit registriertem Client wäre der sauberere Weg — sie erspart dem Nutzer den DevTools-Umweg, das Self-XSS-Gewöhnungsrisiko und die 30-Tage-Lebensdauer eines Tokens, das bei jedem 7TV-Logout still ungültig wird.

**Was die Entscheidung umwerfen würde:** wenn 7TV eine Client-/App-Registrierung für Drittanbieter anbietet. Bis dahin ist jede Wiederaufnahme dieser Frage verlorene Zeit. Der einzige Hebel in unserer Hand wäre, 7TV danach zu fragen (Option D der Untersuchung) — **das ist am 2026-09-01 verworfen worden:** eine öffentliche Anfrage im `SevenTV/SevenTV`-Repo oder im Discord würde EmotePurge gegenüber 7TV sichtbar machen, und das ist derzeit nicht gewollt. Die Option bleibt technisch gangbar und kann jederzeit gezogen werden; sie ist nicht unmöglich, sondern nicht erwünscht.

### 2026-08-31 — Eine Geste darf richtungsabhängig abwählen, ein Gruppenknopf nicht

**Betrifft:** `web/src/app/shared/selection/list-selection.ts`, `web/src/app/shared/selection/list-selection.spec.ts`, `web/public/i18n/de.json`, `web/public/i18n/en.json`

Issue #40 bestand aus seinem Titel: „Multi Deselect would be good. Currently only mutli select with Shift possible." Die Sichtung des Auswahl-Codes hat die Lücke zuerst enger gefasst, als der Titel sie beschreibt. **Einzelnes Abwählen gab es längst** — ein normaler Klick auf ein markiertes Emote nimmt es wieder heraus. Gefehlt hat ausschließlich das Abwählen *mehrerer auf einen Schlag*: die Range-Schleife des Shift-Klicks kannte nur `add`, nie `delete`. Wer von vierzig markierten Emotes die ersten zehn wieder herausnehmen wollte, musste sie einzeln durchklicken.

**Die Geste bekommt eine Richtung, statt einen zweiten Modifier zu bekommen.** Ein Shift-Klick überträgt jetzt den Zustand der Ankerzelle auf den ganzen Bereich: Ist der Anker markiert, wird der Bereich markiert (unverändert); ist er es nicht, wird der Bereich abgewählt. Da derselbe Klick, der den Anker setzt, ihn auch umschaltet, wählt man ab, indem man das erste störende Emote wegklickt und dann mit Shift ans Ende des Bereichs — spiegelbildlich zum Markieren, ohne neue Taste. `ctrlKey`/`metaKey` wären frei gewesen und kommen im ganzen Repo nirgends vor; eine Zweifinger-Kombination wurde trotzdem verworfen, weil die einzige Stelle, an der die Gesten überhaupt erklärt werden, ein `sr-only`-Hinweis am Atlas-Viewport ist. Was dort nicht steht, findet niemand.

**Die Grenze verläuft zwischen Geste und Gruppenaktion.** Der Kommentar bei `selectMany` begründet seit jeher, dass der „alle markieren"-Knopf des ungenutzt-Bands nicht als Toggle arbeiten darf: sein zweiter Druck würde eine handgebaute Auswahl ohne Vorwarnung vernichten. Das gilt weiter und widerspricht der neuen Geste nicht. Eine Geste zeigt ihre Richtung an, bevor sie wirkt — der Klick auf die Ankerzelle nimmt sichtbar genau ein Emote aus der Auswahl, und erst der folgende Shift-Klick zieht das über den Bereich. Ein Knopf hat diese Zwischenstufe nicht. Aus demselben Grund wurde ein sichtbares „alle abwählen" neben „alle markieren" erwogen und **nicht** gebaut: Es hätte nur auf dem Desktop gewirkt, wo die Geste bereits reicht, denn der ganze Band-Knopf ist auf Touch ausgeblendet.

**Die Richtung wird gelesen, nicht gemerkt.** Sie ergibt sich aus dem aktuellen Zustand des Ankers im Moment des Shift-Klicks, nicht aus einem zweiten Feld neben `anchorKey`. Ein gemerktes Flag wäre eine zweite Wahrheit über dieselbe Zeile und könnte veralten — `retainVisible`, ein Refetch und `selectMany` bewegen die Auswahl unter dem Anker weg, ohne dass ein Klick stattfindet. Genau an dieser Klasse ist der Anker schon einmal aus dem entgegengesetzten Grund von einem Index auf einen Key umgestellt worden, weil er nach jedem Re-Sort auf die falsche Zeile zeigte.

**Die bestehenden Ränder bleiben, was sie waren:** ein Anker, der nicht mehr in `items()` steht (weggefiltert, gelöscht), fällt weiter auf einfaches Umschalten zurück statt zu raten; ohne Anker gilt dasselbe; und nach `selectMany` steht der Anker auf der zuletzt hinzugefügten, also markierten Zeile, weshalb ein folgender Shift-Klick weiterhin additiv ist. Der letzte Punkt ist der einzige, an dem die Änderung leicht ins Gegenteil hätte kippen können, und hält deshalb einen eigenen Testfall.

**Die Risikorichtung stimmt.** Ein Shift-Klick kann seit dieser Änderung Einträge aus der Auswahl *entfernen*, was er nie konnte. Für den Löschpfad ist das die harmlose Hälfte: es landen dadurch nie mehr Emotes im Auftrag, nur weniger. Die umgekehrte Änderung — eine Geste, die stillschweigend zusätzliche Emotes in eine unwiderrufliche Löschung aufnimmt — wäre ohne Bestätigungsschritt nicht vertretbar gewesen.

**Touch bleibt ausdrücklich außen vor, und zwar ohne Lücke.** Naheliegend ist der Einwand, dass Touch-Geräte kein Shift haben. Auf `pointer: coarse` gibt es aber überhaupt keine Auswahl: ein `effect` leert sie, ein Tap öffnet stattdessen den Verlauf, und Dock wie Band-Knopf sind ausgeblendet — begründet damit, dass der 7TV-Write-Token nur per Desktop-DevTools aus dem LocalStorage zu holen ist. Ein fehlendes Shift ist dort folglich kein Problem, weil nichts zu markieren ist. Auswahl auf Touch nachzurüsten ist ein eigenes Vorhaben mit der Token-Frage davor und gehört nicht in #40.

Beide Grids — der Usage-Atlas und das Voting-Ballot — instanziieren dieselbe `ListSelection`; die Voting-Seite bekommt das Verhalten ohne eigene Änderung. Der `sr-only`-Hinweis `usageStats.atlas.keyboardHint` musste in beiden Sprachdateien mit, weil er die Geste wörtlich beschreibt und danach falsch gewesen wäre.

### 2026-08-31 — Twitch entwertet App-Tokens nicht gegenseitig (gemessen)

**Betrifft:** `src/EmotePurge.Infrastructure/Twitch/TwitchAppTokenProvider.cs`

Issue #39: Der Kommentar am Kopf von `TwitchAppTokenProvider` begründete sein Single-Flight-Semaphor damit, Twitch entwerte „the previous app token on every new grant", weshalb parallele Grants sich gegenseitig widerrufen würden. Beide Hälften desselben Kommentars vertrugen sich nicht: Das Semaphor schützt nur **innerhalb eines Prozesses**, der Provider ist ein `AddSingleton` mit In-Memory-Cache, und es gibt keinen geteilten Zustand über Redis oder die DB. Api und Worker laufen als getrennte Container und halten je ein eigenes App-Token derselben Client-ID — stimmte der Kommentar, wäre das ein laufender Fehler mit sporadischen `401` in dem Prozess, der zuletzt *nicht* granted hat.

**Der Kommentar war falsch, und zwar nachgemessen statt nachgeschlagen.** Am 2026-08-31 gegen die echte Twitch-API mit der Dev-Client-ID (nicht der der Produktion): Zwei aufeinanderfolgende `client_credentials`-Grants liefern zwei **unterschiedliche** Tokens, und das ältere bleibt danach gültig — `GET https://id.twitch.tv/oauth2/validate` antwortet 200, und ein echter `GET https://api.twitch.tv/helix/users` mit demselben Token ebenfalls. Es gibt also keine gegenseitige Entwertung und keinen laufenden Fehler; zu korrigieren war nur die Begründung.

**Warum die Doku hier nicht weiterhalf** und die Messung nötig war: Twitchs Dokumentation äußert sich zu der Frage an keiner Stelle — weder `getting-tokens-oauth` noch `validate-tokens` noch `revoke-tokens` sagen etwas darüber, ob ein neuer Grant vorherige App-Tokens invalidiert oder wie viele gleichzeitig gültig sein dürfen. Die einzigen Quellen dazu sind zwei Beiträge im Entwicklerforum von 2020, nicht von Twitch-Mitarbeitern, die ein undokumentiertes Limit von **etwa 25** gleichzeitig gültigen App-Tokens pro Client-ID mit Verdrängung des ältesten nennen. Das steht im Code ausdrücklich als schwach belegter Hinweis und nicht als Zusicherung — es ist sechs Jahre alt und wurde von uns nicht verifiziert.

**Das Semaphor bleibt, mit der richtigen Begründung.** Es verhindert nicht gegenseitige Entwertung, sondern überflüssige gleichzeitige Grants. Falls das ~25er-Limit real ist, ist das sogar der wichtigere Grund: Ein Prozess, der pro Request grantet, verdrängt irgendwann seine eigenen älteren Tokens — genau das Symptom, das der ursprüngliche Kommentar fälschlich schon zwei Prozessen zugeschrieben hat.

**Nebenbefund aus derselben Messung, bewusst nicht ausgedeutet:** Beide Grants kamen mit *unterschiedlichen Restlaufzeiten* zurück (54,7 und 56,8 Tage statt der vollen 60), was nahelegt, dass Twitch bestehende Tokens aus einem Pool zurückgibt, statt bei jedem Grant frisch auszustellen. Für die Frage dieses Eintrags ändert das nichts, und wir haben es nicht weiter untersucht.

### 2026-08-31 — Ein Redis-Ausfall ist ein fehlender Wert, kein Fehler

**Betrifft:** `src/EmotePurge.Infrastructure/Redis/{ModRoleCache,TwitchLiveStatusStore,WorkerHealthReader,RedisLiveEventStream}.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/{ModRoleCacheTests,UserServiceTests,TwitchLiveStatusStoreTests,WorkerRosterReaderTests}.cs`, `tests/EmotePurge.Infrastructure.Tests/Unit/{ModRoleCacheFailureModeTests,RedisReaderFailureModeTests,RedisLiveEventStreamFailureModeTests}.cs`

Zweite Hälfte von Issue #37, unabhängig von der ersten: `GET /api/channels/mine` antwortete mit **500**, sobald Redis nicht erreichbar war. Der Cache hinter der Rollenauflösung ist eine Optimierung — eine ausgefallene Optimierung darf den Endpunkt nicht mitnehmen. Der Live-Test hat dann gezeigt, dass es nicht bei einem Endpunkt und nicht bei einer Fundstelle blieb.

**Ein Redis-Ausfall ist ein fehlender Wert, kein Fehler — an sechs Stellen, nicht an der einen aus dem Issue.** `ModeratedChannelsProvider` behandelt dieselbe `IConnectionMultiplexer`-Abhängigkeit seit dem 2026-08-30 fail-open und war die Vorlage; alles andere zog nach: `ModRoleCache` (alle Cache-Lese- und Schreibpfade inklusive der Subscriber-Basis), `TwitchLiveStatusStore.ReadAsync`, `WorkerHealthReader.ReadAsync`, `WorkerRosterReader.ReadAsync` und `RedisLiveEventStream.SubscribeAsync`. Überall dieselbe Form: `catch (Exception ex) when (ex is RedisException or TimeoutException)`, deutsche Warnzeile, fehlender Wert statt Ausnahme. Bemerkenswert ist der Schreibpfad: Nur den Lesezugriff abzusichern hätte den gemeldeten 500 **nicht** behoben, weil `SevenTvEditorService` unmittelbar nach dem erfolgreichen Live-Lookup in den Cache zurückschreibt und der Request eine Zeile später gestorben wäre.

**Ausgenommen bleibt `InvalidateUserAsync`, und zwar bewusst asymmetrisch.** Ihr Rückgabewert füttert einen Audit-Eintrag (`UserService.InvalidateRoleCacheAsync`, `AuditActions.UserInvalidateRoleCache`). Ein geschluckter Redis-Fehler würde dort „0 Einträge entfernt" für einen Rollen-Widerruf protokollieren, der nie stattgefunden hat — ein falsches Protokoll über eine sicherheitsrelevante Aktion, und ein Admin, der einen Fehlschlag für Erfolg hält. Fail-open ist hier die falsche Richtung; die Methode wirft weiter, und ein Test hält das fest, damit die Ausnahme nicht später aus Konsistenzstreben „mitrepariert" wird.

**Die eigentliche Erkenntnis liegt aber woanders: Die Degradation war überall schon entworfen — sie wurde nur nie erreicht.** In allen sechs Fällen behandelt der Aufrufer den fehlenden Wert bereits korrekt und seit jeher: `/api/worker/health` antwortet auf einen fehlenden Snapshot mit `{ status: "unknown", reasonCode: no_health_data }`, `/api/health` mit seinem 503-Totmannschalter (der Kommentar dort nennt „missing snapshot" ausdrücklich), `MyChannelsService` mit `LiveState: Unknown` und `livePolledAtUtc: null`, und `LiveEndpoints.OpenAsync` mit einem 503 **vor dem ersten Byte** — samt Begründung, dass ein begonnener SSE-Body keinen Statuscode mehr übrig lässt. Nicht eine einzige Antwortform musste erfunden werden; es fehlte durchgehend nur die Überführung der Redis-Ausnahme in genau den `null`-Wert, den diese Zweige erwarten. Daraus folgt die Suchanweisung für das nächste Mal: nicht fragen, welcher Endpunkt umfällt, sondern welcher Redis-Aufruf seinen eigenen Null-Zweig nicht erreicht.

**Gemessen, nicht angenommen.** Bei laufender Api und gestopptem Redis: vorher 14 unbehandelte Ausnahmen in wenigen Minuten, danach **null**. `/api/channels/mine` liefert 200 mit vollständiger Liste und `liveState: unknown` für alle Einträge, `/api/worker/health` 200 mit `unknown`, `/api/health` 503, der SSE-Stream bleibt offen statt zu werfen. Die Erholung nach `docker start` braucht **keinen** Api-Neustart — der Multiplexer verbindet sich neu und die Abos leben wieder. Jede dieser Fundstellen wurde einzeln über die Browser-Konsole entdeckt, nachdem die jeweils vorige behoben war; deshalb steht am Ende eine vollständige Inventur aller Redis-Berührungen auf Api-Request-Pfaden statt einer weiteren Einzelkorrektur.

**Zwei bewusst offene Ränder.** (1) Das entworfene 503 des SSE-Streams greift, wenn der Prozess noch nie erfolgreich abonniert hat. Fällt Redis *nach* einem geglückten Abo aus, bleibt das `_redisSubscribed`-Flag gesetzt, und neue Zuhörer bekommen einen offenen, aber stummen Stream statt eines 503 — für kurze Ausfälle die bessere Wahl als Reconnect-Geflacker, und nach Redis' Rückkehr fließen die Ereignisse ohne Zutun wieder. Das Flag wird ausschließlich **nach** einem erfolgreichen Subscribe gesetzt, ein Fehlschlag hinterlässt es also nicht verbrannt; ein Test hält genau das fest. (2) Die Api **bootet** weiterhin gar nicht, wenn Redis beim Start fehlt (`ConnectionMultiplexer.Connect` mit `AbortOnConnectFail` auf dem Default wirft schon bei der DI-Auflösung). Vorbestehend, von dieser Runde unberührt, und im Compose-Stack durch `depends_on: redis healthy` in der Praxis abgefangen — es trifft nur den Fall „Api startet neu, während Redis unten ist".

**Nicht behoben, weil kein vorentworfener Ausweg existiert — ausgelagert nach #41.** Die Inventur hat drei Stellen gefunden, an denen ein Redis-Ausfall eine echte Entwurfsentscheidung verlangt statt eines `catch`: `ChannelService.JoinAsync`/`LeaveAsync`/`TriggerResyncAsync` committen die DB **vor** dem Publish an den Worker und hinterlassen bei Redis-Ausfall eine Inkonsistenz (DB sagt „aktiv", der Worker hat den Join nie bekommen) — die einzige Stelle im Inventar, an der mehr kaputtgeht als nur der Statuscode; `ChannelResyncCooldown` müsste zwischen fail-open ohne Schutz und fail-closed ohne Self-Service-Resync wählen; `ChannelService.PurgeAsync` ist blockiert, aber konsistent, weil dort der Publish vor dem `SaveChangesAsync` steht. Sie hier mitzuerledigen hätte geheißen, drei Entscheidungen nebenbei zu treffen, die eine eigene Runde verdienen.

### 2026-08-31 — Ein Lookup-Status statt eines mehrdeutigen `null`

**Betrifft:** `src/EmotePurge.Core/SevenTv/{SevenTvModels,ISevenTvApiClient}.cs`, `src/EmotePurge.Core/Services/ISevenTvEditorService.cs`, `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs`, `src/EmotePurge.Infrastructure/Services/{SevenTvEditorService,MyChannelsService,ChannelAccessService,EmoteSetOwnershipService}.cs`, `tests/EmotePurge.Infrastructure.Tests/Integration/{MyChannelsServiceTests,EmoteSetOwnershipServiceTests}.cs`, `tests/EmotePurge.Infrastructure.Tests/Unit/{ChannelAccessServiceTests,SevenTvApiClientResolveIdentityTests}.cs`

Issue #37: Die Übersichtsseite zeigte Nutzern **ohne 7TV-Account** den Warnbanner „7TV-Editor-Status konnte nicht vollständig geprüft werden" — eine Vermutung an einer Stelle, an der eine Aussage möglich war. Es fehlte diesen Nutzern nichts; sie können ohne Account nirgends 7TV-Editor sein. Ursache war, dass mehrere unterschiedliche Ausgänge eines 7TV-Lookups in einem einzigen `null` zusammenliefen.

**Die Mehrdeutigkeit saß eine Schicht tiefer als im Issue beschrieben.** Nicht nur `SevenTvEditorService.GetEditorGrantsAsync` kollabierte zwei Ausgänge — `SevenTvApiClient.ResolveSevenTvIdentityAsync` tat es bereits selbst: „GraphQL fand keine Twitch-Connection" und der `catch` über `HttpRequestException`/`TaskCanceledException` gaben beide `null` zurück. Der Service *konnte* die Fälle also gar nicht unterscheiden, gleich wie man seinen Rückgabetyp schneidet. Die Unterscheidung entsteht deshalb jetzt im Api-Client und wird von dort durchgereicht; jede andere Verortung hätte nur die Kollabierung verschoben.

**Wiederverwendet statt neu erfunden:** `SevenTvLookupStatus` und der Result-Schnitt aus #32 (`Status` plus nullable Nutzlast, Nutzlast non-null genau bei `Ok`, zwei statische Factories als einziger Konstruktionsweg) passen unverändert. Die neuen `SevenTvIdentityResult`, `SevenTvEditorGrantsResult` und `SevenTvEditorGrantsLookupResult` folgen ihm. Ein eigenes Enum für diesen Pfad hätte zwei Vokabulare für dieselbe Frage etabliert.

**Derselbe Status, zwei bewusst verschiedene Antworten — das ist der eigentliche Vertrag dieses Eintrags.** `MyChannelsService` setzt `SevenTvUnavailable` nur noch bei `Unavailable`; `NoSevenTvAccount` ist dort ein normaler Ausgang und schweigt. `ChannelAccessService` dagegen bleibt bei **jedem** Status außer `Ok` fail-closed, `NoSevenTvAccount` eingeschlossen. Eine Autorisierungsprüfung ist keine Statusanzeige: dass jemand keinen 7TV-Account hat, muss Zugriff genauso verweigern wie ein nicht erreichbares 7TV. Die Verlockung, die Prüfung später auf `Unavailable` zu verengen, weil „kein Account ja kein Fehler ist", steht als Warnung im Code — sie wäre eine Rechteausweitung, kein Aufräumen.

**Der Wire-Vertrag bleibt ein `bool`.** `MyChannelsResultDto.SevenTvUnavailable` behält Namen und Typ und wechselt nur seine Bedeutung: von „kein Ergebnis" zu „ein echter Lookup-Fehler kann die Liste unvollständig gemacht haben". Ein sprachneutraler Status-Code nach dem Muster von `SevenTvSyncFailureReasons` wäre die konsistentere Wahl gewesen, wurde aber verworfen: Weil die Oberfläche bei „kein Account" **und** bei „alles in Ordnung" gleichermaßen schweigt, unterscheidet das Frontend nur zwei Fälle, nicht drei. Ein Code hätte Union-Type, Frontend-Modell und E2E-Mock für eine Differenzierung geändert, die niemand liest. Die Dreiteilung lebt im Backend, wo sie gebraucht wird; `/api/channels/mine` hat genau einen Konsumenten, das Feld ist also jederzeit billig aufzubohren, falls die Anzeige später doch unterscheiden will.

**Die eigentliche Falle steckte im Vertrag von 7TV, nicht im eigenen Code — und nur eine Live-Messung hat sie gezeigt.** Der erste Anlauf dieses Fixes war vollständig gebaut und mit 580 grünen Tests belegt, behob den gemeldeten Bug aber **nicht**: 7TV antwortet auf `userByConnection` für eine Twitch-ID ohne Account nicht mit `null`, sondern mit HTTP 200 und einem **Platzhalter-Nutzer** (`id: "00000000000000000000000000"`, leere `connections`). Die Prüfung `user is null` war damit toter Code; zurück kam `Ok` mit der Null-ObjectID, der Folgeaufruf `editor_of` auf diese ID lieferte `data: null` mit `LOAD_ERROR user not found`, und genau das galt als `Unavailable` — der Banner blieb also stehen, exakt für die Nutzergruppe, um die es in diesem Issue geht. Die Unit-Tests konnten das nicht fangen: Ihre Mocks trugen dieselbe Annahme über 7TV wie der Code, den sie prüfen sollten. Ein einzelner `curl` gegen `7tv.io/v3/gql` hat es in Sekunden gezeigt.

**Erkannt wird „kein Account" deshalb semantisch, nicht am Sentinel:** Es gilt als `NoSevenTvAccount`, wenn die Antwort keine TWITCH-Connection mit genau der abgefragten ID enthält — 7TV findet den Nutzer ja *über* diese Connection, ihr Fehlen ist der eigentliche Beleg. Ein hartkodierter Vergleich gegen die Null-ObjectID wurde verworfen: Er wäre genau das Gemessene, ginge aber still kaputt, sobald 7TV seinen Platzhalter ändert — und „still kaputt" ist die Eigenschaft, die diesen Bug überhaupt so lange hat leben lassen. `emote_set_id: null` bei vorhandener Connection bleibt ausdrücklich `Ok`: „Account ohne aktives Set" ist ein eigener Zustand, kein fehlender Account. Die gemessenen Payloads stehen wörtlich in `SevenTvApiClientResolveIdentityTests`, mit dem Vermerk im Test, dass sie gemessen und nicht erfunden sind. **Die Kehrseite derselben Erkenntnis** — im Codex-Review als P2 gefunden, nachdem der erste Anlauf sie übersehen hatte: Wenn der Platzhalter den „kein Account"-Fall vollständig abdeckt, bleibt für eine `null`-Antwort nichts mehr übrig als ein **Ausfall** (`data: null` mit `errors`, oder ein unlesbarer Body). Sie weiterhin als `NoSevenTvAccount` auszulegen hätte den Fehler dieses Issues umgekehrt und verschlimmert: Bei einem 7TV-Ausfall wäre der Banner ausgeblieben und die Editor-Channels hätten **stillschweigend** gefehlt, wo vorher wenigstens gewarnt wurde. `null` heißt deshalb `Unavailable` mit Warn-Log, und die Connection-Prüfung ist der einzige verbleibende Weg zu `NoSevenTvAccount` — dieselbe Unterscheidung, die `ResolveTwitchUserIdAsync` für seine eigene GraphQL-Antwort schon traf.

**Eine Verhaltensänderung als Folge, bewusst übernommen:** In `EmoteSetOwnershipService` (Tier 1) galt der Owner-Check für einen Channel, dessen Twitch-Account keinen 7TV-Account hat, bisher als *verfügbar* — die Platzhalter-ID wurde gegen den echten Set-Owner verglichen und ergab zuverlässig „nicht das eigene Set". Richtig war daran nur das Ergebnis, nicht die Begründung. Jetzt meldet der Check sich als **nicht verfügbar** samt Warnzeile, was der Wahrheit entspricht: ohne auflösbare 7TV-Identität lässt sich Eigentum nicht feststellen. Erreichbar ist der Fall nur, wenn unsere DB für einen Channel noch ein `ActiveEmoteSetId` führt, dessen Twitch-Account inzwischen keinen 7TV-Account mehr hat — eine veraltete Zeile also, bei der eine ehrliche Unsicherheit besser ist als eine zufällig richtige Behauptung.

### 2026-08-31 — Read-only Rate-Limit-Beobachtbarkeit über Redis-Zeit-Buckets

**Betrifft:** `src/EmotePurge.Core/Services/{IRateLimitTelemetry,IRateLimitTelemetryReader,RateLimitTelemetryExtensions}.cs`, `src/EmotePurge.Infrastructure/Redis/RateLimitTelemetryStore.cs`, `src/EmotePurge.Infrastructure/Telemetry/ProviderRequestTelemetryHandler.cs`, `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs`, `src/EmotePurge.Infrastructure/Services/{ModeratedChannelsProvider,SevenTvEditorService,VoteEligibilityService}.cs`, `src/EmotePurge.Api/RateLimiting/{RateLimitTelemetryMiddleware,RateLimitRejection}.cs`, `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs`, `src/EmotePurge.Api/Program.cs`, `tests/EmotePurge.Infrastructure.Tests/{Integration/RateLimitTelemetryStoreTests,Unit/ProviderRequestTelemetryHandlerTests,Integration/ModeratedChannelsProviderTests,Integration/VoteEligibilityServiceTests}.cs`, `tests/EmotePurge.Api.Tests/{AdminRateLimitsEndpointTests,AuthFilterMatrixTests,RateLimitRejectionTests}.cs`, `web/src/app/core/admin/{admin.model,admin.service,admin.service.spec}.ts`, `web/src/app/features/admin/admin-monitoring-page.ts`, `web/public/i18n/{de,en}.json`

Schritt 4 des Umsetzungsplans zu Issue #33 (`docs/superpowers/specs/2026-08-30-rate-limit-architecture-design.md`, Design 4): Nachdem Schritt 2 die lokalen Policies neu zugeschnitten und Schritt 3 den Moderated-Channels-Cache zusammengelegt hat, macht Schritt 4 sichtbar, was beide bewirken — ohne selbst etwas zu steuern. Es gibt in diesem Schritt keine Reservierung, keine Vorab-Ablehnung und keinen Observe-/Enforce-Schalter; die Spec ist an dieser Stelle ausdrücklich: gebaut wird eine Beobachtung, kein Koordinator.

**Speicher sind Redis-Zeit-Buckets in zwei Auflösungen**, geschrieben vom selben `RateLimitTelemetryStore`, der sie auch liest — dieselbe Ein-Klasse-für-beide-Seiten-Form wie beim bestehenden `TwitchLiveStatusStore`. Jedes Ereignis zählt doppelt: in einen 5-Sekunden-Bucket (TTL 5 min, gelesen von der Minutenauflösung) und in einen Minuten-Bucket (TTL 25 h, gelesen vom 24-h-Fenster). Eine Auflösung allein hätte eines der beiden Fenster falsch gemacht — Minuten-Buckets ließen „letzte Minute" beim Überschreiten einer Minutengrenze in einem Sprung springen, 5-Sekunden-Buckets bräuchten 17280 Reads für einen Tag. Die Minutenzahl summiert die letzten zwölf 5-Sekunden-Buckets und deckt damit real 55–60 s statt exakt 60, bewegt sich aber in 5-Sekunden-Schritten statt sprunghaft. Beide Fenster sind aus denselben Ereignissen summiert, das Minutenfenster ist also per Konstruktion immer im 24-h-Fenster enthalten. Jeder Bucket-Key trägt seine eigene TTL, erneut bei jedem Schreibzugriff gesetzt — es gibt keinen Aufräum-Job, und ein Bucket ohne Verfall bliebe in einem zeitindizierten Key-Raum für immer liegen.

**Fail-open, an jeder Schreib- und an der Lese-Stelle.** Jeder Schreibversuch fängt seine Exception nach einer strukturierten deutschen Logzeile ab; ein Lesefehler liefert `RateLimitTelemetrySnapshot.Unavailable` statt zu werfen. Die Begründung ist dieselbe wie beim bestehenden Redis-Verhalten im Produktpfad: eine Telemetrie, die einen Request zum Scheitern bringen kann, ist schlimmer als keine — das Zählen eines 429 darf selbst nie eines auslösen. Der Aufruf aus dem Produktpfad läuft deshalb nie per `await`, sondern über die synchronen `RecordPolicyDecision`/`RecordProviderResponse`/`RecordCacheLookup`-Erweiterungsmethoden in `RateLimitTelemetryExtensions`: Sie starten den Schreibvorgang fire-and-forget, hängen aber eine Continuation an, die eine später fehlschlagende Task beobachtet (`OnlyOnFaulted`) — sonst entstünde genau die unbeobachtete Exception, die synchrones `_ =` an sechs Aufrufstellen unauffällig hinterlassen hätte.

**Drei Erfassungspunkte, symmetrisch zu den drei Lesearten der Anzeige.** (1) `RateLimitTelemetryMiddleware` sitzt unmittelbar **vor** `UseRateLimiter` in der Pipeline, misst aber **nach** dem inneren Aufruf: Erst dann stehen Policy-Name und Partition (vom Partitioner auf dem Hinweg in `HttpContext.Items` hinterlegt) und die Ablehnungsmarkierung (von `RateLimitRejection.OnRejectedAsync` auf dem Rückweg gesetzt) beide zur Verfügung — von innerhalb des Limiters ist keins von beiden sichtbar, von einem Handler aus ist der Limiter unsichtbar. Die neue Markierung (`RateLimitRejection.RejectedItemKey`) ist bewusst getrennt vom bloßen HTTP-429: Die Resync-Cooldown-Route antwortet ebenfalls mit 429, aus ihrem eigenen Handler heraus, auf einen vom Limiter längst akzeptierten Request — sie als Policy-Ablehnung zu zählen würde eine Dauerbasis lokaler Ablehnungen in die Anzeige einziehen und die eine Zahl unlesbar machen, die im Ernstfall beobachtet wird. (2) Ein `DelegatingHandler` (`ProviderRequestTelemetryHandler`) hängt an allen drei typisierten Provider-`HttpClient`s — 7TV-REST, Twitch-Auth (`id.twitch.tv`), Twitch-Helix —, jeder mit seiner eigenen `CallSource`, weil eine Helix-Pagination und ein Token-Refresh zwar beide Twitch sind, aber nichts miteinander gemein haben. Nur ein tatsächlicher 429 zählt als Provider-Rate-Limit; ein 5xx ist ein Providerfehler, kein Budgetsignal. Ein Call ohne jede Antwort (Timeout, Verbindungsfehler) wird bewusst **nicht** gezählt — es gibt keinen Statuscode, unter dem er abzulegen wäre, und ihn in einen bestehenden Bucket zu mischen würde Transportfehler mit echten Provider-429ern vermengen. Die browserseitigen 7TV-GraphQL-Mutationen der Mass-Delete-Engine laufen nie durch diesen Handler, weil sie diesen Server nie verlassen — eine reale Lücke, die die Admin-Seite benennt statt sie zu verschweigen. (3) Drei Cache-Lesestellen zählen Hit/Miss beim eigentlichen Lookup: `ModeratedChannelsProvider` (inklusive des Double-Checks hinter dem Single-Flight-Gate — ein Hit dort ist eine Helix-Pagination, die das Gate gerade gespart hat), `SevenTvEditorService` (ein Miss kostet zwei 7TV-REST-Calls) und `VoteEligibilityService` (ein Miss pro Session, bei zehn Subs-Sessions einer Channel-Seite also bis zu zehn Helix-Calls).

**`Ratelimit-*`-Header laufen als Stichprobe mit, nie als geteilter Zustand.** Der `DelegatingHandler` liest `Ratelimit-Limit`/`-Remaining`/`-Reset` unverändert als Strings mit — bei Twitch vorhanden, bei 7TV nicht, dort bewusst `null` statt einer erfundenen Null. Die Werte werden nirgends geparst oder in eine Entscheidung überführt: Mit mehr als einer Api-Replica wären sie ohnehin kein gemeinsamer Zustand, sondern nur das, was der jeweilige Prozess zuletzt gesehen hat — genau deshalb baut dieser Schritt keinen Koordinator darauf.

**Der Endpoint `GET /api/admin/rate-limits`** liegt in der bestehenden `/api/admin`-Gruppe (Allowlist-Autorisierung wie alle anderen Admin-Routen) und ist bewusst ein eigener Endpoint statt eines neuen Felds auf `/api/health` — dessen „gleiche Form bei Ausfall"-Vertrag gilt dem Worker-Snapshot, ein zweites, unabhängiges Verfügbarkeits-Flag auf demselben Payload hätte verwischt, welches der beiden gerade gekippt ist. Bei nicht erreichbarem Redis liefert er trotzdem **`200`** mit `telemetryAvailable: false`: Die effektive Konfiguration (Capacity, Nachfüllrate bzw. Fenstergröße, Partitionierung, Queue-Limit — immer 0, s. `RateLimitRejection.QueueLimit`) kommt direkt aus `IOptions<RateLimitingOptions>`, unabhängig vom Zähler-Store, und bleibt deshalb auch im Degraded-Fall sichtbar. Die Policy-Liste wird aus der Konfiguration aufgebaut, nicht aus dem Snapshot: Eine Policy, die der Zähler-Store nie gesehen hat — weil niemand sie getroffen hat, oder weil `telemetryAvailable` false ist —, hat trotzdem ein Budget und schützt weiterhin eine Route; andersherum wäre eine ruhige Policy von einer nie registrierten nicht zu unterscheiden. Caches und Provider dagegen werden aus den Zählern selbst abgeleitet, nicht gegen eine feste Liste vervollständigt — ein Index weniger zum Synchronhalten, zum ehrlichen Preis, dass etwas, das über die gesamte Retention hinweg still war, aus der Anzeige verschwindet.

**Der Admin-UI-Abschnitt** auf der bestehenden Monitoring-Seite zeigt effektive Konfiguration je Policy, akzeptiert/abgelehnt je Policy (Minute/24 h), die letzte lokale Ablehnung, Cache-Hits/-Misses der drei Caches, Provider-429er je Provider und Call-Source mit `Retry-After` und letztem Zeitpunkt sowie die Twitch-Header-Stichprobe (ausdrücklich als Stichprobe beschriftet, 7TV ohne Prozentwert). Bei `telemetryAvailable: false` zeigt er Konfiguration plus Degraded-Hinweis statt Zahlen. Zwei statische, übersetzte Hinweistexte benennen die Monitoring-**Lücken** ausdrücklich, statt sie unkommentiert zu lassen: (1) Mass-Delete/Restore rufen 7TV direkt aus dem Browser auf und fehlen deshalb in den serverseitigen 7TV-Zahlen; (2) die policy-freien SSE-Reconnects und der Worker-Health-Poll erscheinen in keinem Policy-Zähler, weil beide Routen bewusst ohne `RequireRateLimiting` laufen (s. Entscheidung vom 2026-08-30 zu den Token-Buckets).

### 2026-08-30 — Ein gemeinsamer Redis-Listencache ersetzt den Bool-Moderatorcache

**Betrifft:** `src/EmotePurge.Core/Services/IModeratedChannelsProvider.cs`, `src/EmotePurge.Core/Services/IModRoleCache.cs`, `src/EmotePurge.Core/Twitch/ITwitchHelixClient.cs`, `src/EmotePurge.Infrastructure/Services/ModeratedChannelsProvider.cs`, `src/EmotePurge.Infrastructure/Services/{ModeratorCheckService,MyChannelsService,EmoteSetOwnershipService}.cs`, `src/EmotePurge.Infrastructure/Redis/ModRoleCache.cs`, `src/EmotePurge.Infrastructure/Twitch/TwitchHelixClient.cs`, `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs`, `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs`, `tests/EmotePurge.Infrastructure.Tests/{Integration,Unit,Fakes}/*`

Schritt 3 des Umsetzungsplans zu Issue #33 (`docs/superpowers/specs/2026-08-30-rate-limit-architecture-design.md`, Design 3): `modlist:{twitchUserId}` ist jetzt der einzige Redis-Cache für die moderierten Twitch-Channels eines Nutzers — eine JSON-Liste aus normalisiertem Login und der dazugehörigen, unveränderlichen Twitch-Channel-ID. Der neue `IModeratedChannelsProvider` (Infrastructure) ist seine einzige Quelle; kein Konsument paginiert Helix mehr selbst. Die TTL kommt bewusst aus dem bereits vorhandenen `Auth:ModCheckCacheTtlMinutes` (Default 10 min) statt aus einem eigenen Konfigurationswert: der Listencache ist der Nachfolger des alten `modcheck`-Bool-Caches, nicht eine zusätzliche Einrichtung daneben, und der Betrieb soll beim Deploy keinen neuen Schlüssel lernen müssen, den es vorher nicht gab.

**Gleichzeitige Misses laufen durch ein In-Process-Single-Flight pro Twitch-User**, mit erneutem Redis-Read direkt nach Eintritt ins Gate — wer als Zweiter ankommt, findet dort meist schon, was der Erste gerade geschrieben hat, und spart sich die eigene Pagination. Es gibt bewusst **keinen verteilten Lock**: Bei mehr als einer Api-Replica zahlt jede Replica bis zu einer Pagination pro TTL, statt sich die eine echte Pagination zu teilen. Das ist dieselbe bereits bekannte und akzeptierte Grenze wie beim In-Process-Semaphor des Twitch-Token-Refresh (`TwitchTokenRefreshGate`) — keine neue Schuld, sondern derselbe Kompromiss ein zweites Mal.

**Drei Konsumenten statt der zwei, die die Spec nennt.** `MyChannelsService` (Overview) und `ModeratorCheckService` (Autorisierung) sind die von Design 3 vorgesehenen Leser. Dazu kam beim Bauen `EmoteSetOwnershipService`: seine Tier-3-Prüfung holte bisher, unabhängig von den beiden anderen, ihre eigene ungecachte Helix-Pagination über denselben Nutzer. Ihn draußen zu lassen hätte die Spec-Absicht „einzige Quelle der moderierten Channels" unterlaufen — er war schlicht ein dritter Lader derselben Liste, den die Spec beim Schreiben nicht auf dem Schirm hatte, keine Erweiterung des Vertrags.

**Nur eine vollständige, erfolgreiche Pagination wird gecacht.** Timeout, 429, 5xx, ein Tokenfehler und eine Pagination, die nicht durchläuft, schreiben keinen Eintrag — der Aufrufer bekommt „nicht ermittelbar" zurück und verweigert für diesen Request, statt eine falsche Antwort für die volle TTL festzuschreiben. Dazu gehört eine Korrektur an `TwitchHelixClient.FetchModeratedChannelsAsync`: ein nach den zehn erlaubten Seiten noch vorhandener Cursor meldete bisher ein stilles Teilergebnis, meldet jetzt einen Fehlschlag. Ohne diese Korrektur hätte der neue Cache die Lücke von einer einzelnen Anfrage auf die gesamte TTL verlängert — eine abgeschnittene Liste wäre als vollständige Antwort persistiert worden, und jeder Channel jenseits der zehnten Seite hätte bis zum nächsten Miss als „nicht moderiert" gegolten.

**Eine leere Liste ist nicht dasselbe wie „nicht ermittelbar"**, und beide dürfen im Cache-Payload nicht zusammenfallen — nur dann bleibt ein Nutzer, der wirklich keinen Channel moderiert, von einem Nutzer unterscheidbar, dessen Anfrage gerade scheitert. Nach außen tragen kann diese Unterscheidung nur `MyChannelsService`, weil sein DTO das eigene Feld `HelixUnavailable` dafür hat; bei `ModeratorCheckService` und `EmoteSetOwnershipService` gibt es kein passendes Antwortfeld, dort bleibt sie eine Logzeile.

**Ein Cache-Hit prüft keinen Token** und liefert `ReauthRequired = false` unabhängig vom tatsächlichen Tokenzustand — ein Hit kostet bewusst keinen Token-Lookup. Ein kaputtes Refresh-Token fällt deshalb erst beim nächsten Miss auf (spätestens nach einer TTL) oder über die ohnehin laufende stündliche Live-Validierung benutzter Tokens. Akzeptierte Staleness, dieselbe Kategorie Kompromiss wie beim Single-Flight ohne verteilten Lock.

**Der alte Bool-Moderatorcache ist ersatzlos entfernt**, nicht nur umgebaut: `IModRoleCache.TryGetIsModeratorAsync`/`SetIsModeratorAsync` sind weg, ebenso ihre `modcheck:{user}:{channel}`-Keys. `InvalidateUserAsync` löscht stattdessen direkt den `modlist`-Key mit; der bisherige `modcheck:*`-SCAN entfällt, weil seit diesem Schritt niemand mehr in diese Keyform schreibt — Alt-Einträge aus einem vorherigen Deploy laufen einfach binnen der TTL (Default 10 min) aus, ohne dass sie je wieder gelesen würden. Sichtbare Nebenwirkung im Betrieb: der Admin-Invalidate-Endpoint meldet im Audit-Payload kleinere Zahlen als zuvor, und vor allem hört die Zahl auf, mit der Nutzung zu wachsen. `modcheck` war ein Key **pro Nutzer und Kanal** — wer in zwölf Kanälen rollengeprüft worden war, hatte zwölf davon; die Moderatorenseite ist jetzt ein einziger `modlist`-Key, der Payload liegt damit bei höchstens **drei** (`modlist` + `subcheck` + `7tveditor`) und meldet nur, was für diesen Nutzer tatsächlich belegt war — im Live-Test mit einem Konto ohne 7TV-Grant waren es zwei. Der Endpoint selbst (`POST /api/admin/users/{twitchUserId}/invalidate-role-cache` über `UserService.InvalidateRoleCacheAsync`) ist unverändert.

**Ein Redis-Ausfall ist ein Miss bzw. ein verlorener Schreibversuch**, nie ein Fehler, der den Request scheitern lässt: `ModeratedChannelsProvider` fängt `RedisException`/`TimeoutException` an Lese- und Schreibstelle einzeln ab, loggt deutsch und fällt auf den Live-Pfad zurück. Der Miss-Pfad ist damit die Wahrheit, auf die sich jede andere Fehlerbehandlung in diesem Service stützt — dieselbe Rolle, die er ohnehin schon für einen echten Cache-Miss spielt.

### 2026-08-30 — Zwei Token-Buckets ersetzen die Fixed-Window-Policy `ExternalApi`

**Betrifft:** `src/EmotePurge.Api/Program.cs`, `src/EmotePurge.Api/Endpoints/{ChannelEndpoints,EmoteEndpoints,UsageStatsEndpoints,VoteSessionEndpoints}.cs`, `src/EmotePurge.Api/RateLimiting/RateLimitingOptions.cs`, `src/EmotePurge.Api/RateLimiting/RateLimitPolicyNames.cs`, `src/EmotePurge.Api/RateLimiting/RateLimitRejection.cs`, `src/EmotePurge.Api/appsettings.json`

Schritt 2 des Umsetzungsplans zu Issue #33 (`docs/superpowers/specs/2026-08-30-rate-limit-architecture-design.md`, Design 2): Die Fixed-Window-Policy `ExternalApi` (40 Requests pro Minute, geteilt über die meisten authentifizierten Routen) ist vollständig entfernt. An ihre Stelle treten zwei Token-Buckets — `InteractiveRead` (300 Tokens, Nachfüllung 5/s, Partition per Twitch-User-ID mit IP-Fallback) und `Voting` (120 Tokens, Nachfüllung 2/s, Partition per Twitch-User-ID **und** `sessionId` aus der Route) — sowie die unveränderte Zuordnung der drei übrigen Policies (`Bookkeeping`, `ChannelResync`, `PublicHealth`) auf mehr Routen als bisher.

**Warum `ExternalApi` das Falsche gemessen hat.** Die Policy war als Surrogat für Providerbudgets gedacht, maß aber Requests gegen die eigene Api, nicht gegen 7TV oder Twitch — ein reiner DB-Read kostete dasselbe Permit wie `/channels/mine`, das mehrere Helix-Seiten paginiert. Die Baseline-Messung vom selben Tag (`docs/superpowers/2026-08-30-rate-limit-baseline-messung.md`) belegt das empirisch: Bei zehn dicht aufeinanderfolgenden Workspace-Rundgängen brach der siebte mit fünf lokalen 429ern ab, alle aus `ExternalApi`, keiner davon ein Antwortcode von Twitch oder 7TV. **Kein einziger** der beobachteten 429er war ein Provider-429 — die Issues #33 und #35 kamen restlos aus dieser einen lokalen Policy. Die neue Begründung für jede Policy ist deshalb ausdrücklich eine lokale Missbrauchsgrenze, kein Providerbudget-Surrogat: Providerkosten werden in Schritt 3 (gemeinsamer Cache) reduziert und in Schritt 4 (Beobachtbarkeit) gemessen, nicht in dieser Runde durch eine erfundene Zahl nachgebildet.

**Warum ein Token-Bucket statt eines weiteren Fixed Window.** Ein Fixed Window leert sich für alle Anfragen im selben Fenster gleichzeitig und füllt sich für alle gleichzeitig wieder — eine Anfrage, die spät in einem vollen Fenster ankommt, wird für den Rest des Fensters abgelehnt, ganz gleich wie wenig sie selbst verursacht hat. Der Burst beim Betreten eines Workspace (sieben Requests in einer Sekunde, in der Baseline-Messung wiederholt belegt) ist aus einem solchen Fenster heraus nicht von einer Schleife zu unterscheiden — beide sehen wie derselbe Ausschlag aus. Ein Bucket, der kontinuierlich nachfüllt, bildet stattdessen genau das ab, wonach interaktive Navigation tatsächlich aussieht: lange still, dann ein Burst beim Seitenwechsel, danach wieder Ruhe.

**Die vollständige Zuordnung**, aus dem tatsächlichen `grep -rn RequireRateLimiting src/EmotePurge.Api/` dieses Working Trees, nicht aus dem Plan übernommen:

| Policy | Routen |
|---|---|
| `InteractiveRead` | `GET /{channelName}`, `GET /{channelName}/permissions`, `GET /mine` (`ChannelEndpoints.cs`); die gesamte Emote-Gruppe auf Gruppenebene — `GET /active-set`, `GET /duplicate-names`, `GET /set-warning` (`EmoteEndpoints.cs`); die gesamte Usage-Stats-Gruppe auf Gruppenebene — `GET ""`, `GET /totals`, `GET /daily`, `GET /series` (`UsageStatsEndpoints.cs`); die Vote-Session-Liste `GET ""`, `GET /{sessionId}/results` und `GET /api/vote-sessions/mine` (`VoteSessionEndpoints.cs`) |
| `Voting` | `POST /{sessionId}/votes`, `DELETE /{sessionId}/votes/{emoteId}` (`VoteSessionEndpoints.cs`), partitioniert per User **und** Session |
| `Bookkeeping` | `GET /{channelName}/audit-log`, `POST /{channelName}/join`, `DELETE /{channelName}`, `DELETE /{channelName}/purge` (`ChannelEndpoints.cs`); `POST /sync-deleted`, `POST /sync-restored` (`EmoteEndpoints.cs`); `POST ""` (anlegen), `POST /{sessionId}/end`, `DELETE /{sessionId}` (`VoteSessionEndpoints.cs`) |
| `ChannelResync` | `POST /{channelName}/resync` (`ChannelEndpoints.cs`), unverändert |
| `PublicHealth` | `GET /api/health` (`WorkerHealthEndpoints.cs`), unverändert |

**Bewusst policy-frei bleiben** vier Gruppen von Routen, jede aus einem anderen Grund: die gesamte `/api/admin`-Gruppe einschließlich ihres SSE-Streams (Allowlist-gegated durch `GlobalAdminAuthorizationFilter`, internes Werkzeug für eine Handvoll benannter Logins — eine Missbrauchsfläche, die ein Request-Budget nicht schließen müsste); die gesamte `/api/auth`-Gruppe (ein Limit auf dem Login-Pfad kann Nutzer über genau die eine Anfrage aussperren, die sie nicht selbst wiederholen können, und eine abgelehnte Callback-Anfrage verlöre das OAuth-State-Cookie mit); `GET /api/worker/health` (der eigene 30-Sekunden-Dauerpoll der App für das Header-Badge — ein Budget hier würde die Grundlast der App selbst ablehnen); und die drei SSE-Endpunkte `GET /{channelName}/live`, `GET /live-events` sowie der admin-eigene Stream (jede Policy läuft mit `QueueLimit = 0`, ein 429 wäre für eine `EventSource` endgültig, weil sie danach nicht mehr retryt — die Verbindungsgrenze liegt stattdessen in `ILiveEventStream`).

**Fünf Management-Mutationen hängen jetzt erstmals auf `Bookkeeping`**, wo sie zuvor gar keine Policy hatten: Channel verlassen (`DELETE /{channelName}`), Channel purgen (`DELETE /{channelName}/purge`), Vote-Session anlegen (`POST` auf die Gruppe), Vote-Session beenden (`POST /{sessionId}/end`) und Vote-Session löschen (`DELETE /{sessionId}`). Alle fünf sind Schreiboperationen gegen die eigene Datenbank ohne Providerkosten, für die dieselbe großzügige Begründung gilt wie für `sync-deleted`/`sync-restored`: 120 pro Minute liegt weit über allem, was ein Mod-Team von Hand auslöst. Separat davon wechselt `POST /{channelName}/join` **von `ExternalApi` auf `Bookkeeping`** — kein Neuzugang, sondern ein Wechsel: Zum Zeitpunkt dieses Aufrufs hat der Aufrufer den Bot auf Twitch bereits als Moderator gesetzt, dieselbe bereits-auf-7TV-ausgeführte Handlung wie bei `sync-deleted`. Ein lokal verlorener `join` ließe die beiden Seiten auseinanderlaufen, ohne dass irgendetwas das anzeigt — dieselbe Asymmetrie, die `Bookkeeping` für die anderen vier Routen begründet.

**Konfigurierbarkeit.** Alle fünf Policies lesen ihre Werte aus der Konfigurationssektion `RateLimiting` (`RateLimitingOptions`, gebunden in `Program.cs`), überschreibbar einzeln per Environment-Variable (`RateLimiting__<Policy>__<Wert>`, z. B. `RateLimiting__InteractiveRead__TokenLimit`). `RateLimitingOptions.Validate()` läuft beim Boot und wirft mit einer deutschen Meldung, sobald ein Wert nicht größer als null ist — fail fast, weil eine versehentliche Kapazität von 0 keine laxere Grenze wäre, sondern ein Totalausfall jeder Route, die die Policy schützt, und im Log exakt wie ein echter Rate-Limit-Vorfall aussähe. Eine Änderung wirkt erst nach Neustart; es gibt bewusst keinen Write-Endpoint, denn ein Limit, das sich unter einem laufenden Prozess bewegen kann, ist im Nachhinein aus einer Logzeile nicht mehr nachvollziehbar.

**Was ausdrücklich nicht Teil dieser Entscheidung ist**, weil es andere, spätere Schritte derselben Spec sind: verteilter Provider-Budgetzustand (Twitch-App-Reservierungen o. ä.), Observe-/Enforce-Modi für eine 7TV-Grenze, providerbedingte 429-/503-Fehlerverträge und die dreiwertige Rollenauflösung. Der gemeinsame Moderated-Channels-Cache (Design 3) und die Read-only-Beobachtbarkeit (Design 4) sind eigene Rollout-Schritte mit eigenen `DECISIONS.md`-Einträgen.

### 2026-08-30 — Ein virtuelles Raster über gechunkte Zeilen trackt den Index

**Betrifft:** `web/src/app/features/voting/vote-session-detail-page.html`, `web/src/app/features/voting/vote-session-detail-page.ts`, `web/src/app/features/usage-stats/usage-stats-page.html`, `web/src/app/features/usage-stats/usage-stats-page.ts`, `web/e2e/vote-ballot.e2e.spec.ts`

Nach einem eigenen Vote blitzten auf der Abstimmungsseite sämtliche Emote-Bilder zweimal auf. Die Ursache liegt drei Ebenen tief und ist an keiner davon offensichtlich, deshalb steht sie hier: Beide virtuellen Raster gaben ihrem `*cdkVirtualFor` ein `rows()`, das seine Zeilen-Arrays bei jedem Recompute per `slice()` neu erzeugt (`chunkIntoRows`, `packAtlasRows`). Ohne `trackBy` legt `CdkVirtualForOf` seinen Differ auf **Objektidentität** an, meldet also nach jedem Reload „alle Zeilen entfernt, alle hinzugefügt". Die `_RecycleViewRepeaterStrategy` gibt die abgehängten Views per `pop()` wieder aus, sie landen an anderen Zeilenpositionen, und das innere `@for (… track emote.emoteId)` findet dort fremde IDs vor und baut jede Zelle neu. Jede neue Zelle ist eine neue `EmoteSprite`-Instanz, und die hält ihr `<img>` bis zum `load`-Ereignis auf `visibility: hidden` — auch bei einem Bild aus dem Cache. Das ist der Blitz, einer je Reload.

**Die Regel lautet deshalb: ein `*cdkVirtualFor` über gechunkte Zeilen bekommt ein `trackBy` auf den Index.** Nicht auf die Zeilenidentität, denn die ist per Konstruktion bei jedem Recompute neu; der Index ist die einzige stabile Größe. CDK behält damit die Zeilen-Views und aktualisiert nur deren Kontext, und der Inhaltsabgleich bleibt beim inneren `@for`, das ohnehin auf `emote.emoteId` trackt — auch bei einer Änderung der Spaltenzahl.

**Zweimal, nicht einmal, und das ist ein zweiter Befund.** Ein eigener Vote lädt sofort über `vote()` neu und ein zweites Mal rund 500 ms später, weil die Api `vote.changed` ohne Urheber-Ausnahme an den Kanal-Stream schickt und der Client sein eigenes Echo annimmt. Der Kommentar an der `liveReload`-Stelle behauptete bis hierher, die Debounce fange beides zusammen; sie debounct aber ausschließlich SSE-Ereignisse untereinander, während der Reload aus `vote()` an ihr vorbeiläuft. Der Kommentar ist korrigiert, das Verhalten bewusst nicht: `loadResults` ist idempotent, und nach dem `trackBy` ist der doppelte Abruf nicht mehr sichtbar. Er bleibt als offener Punkt bestehen.

**Der Regressionstest misst den DOM, nicht das Flackern.** `vote-ballot.e2e.spec.ts` markiert vor dem Vote jedes `<img>` im Raster mit einem Attribut und prüft danach, dass alle Markierungen überlebt haben — ein frisch erzeugtes Element trägt kein Attribut, an das nie jemand gebunden hat. Zwei Fallen dabei, beide beim Bauen zugeschlagen und deshalb im Test kommentiert: Es werden **nicht alle** Zeilen neu gebaut, sondern etwa die Hälfte (ohne Fix überlebten 10 von 24), weshalb eine einzelne Sonde den Fehler nicht fängt — die erste Fassung war grün mit und ohne Fix. Und das Warten auf den HTTP-Response des Reloads genügt nicht, weil Angular ihn danach erst noch anwendet; der Test wartet stattdessen auf eine Tally, die nur die zweite Antwort erzeugen kann.

### 2026-08-29 — Emote-Bilder holen die Größe, die sie zeigen

**Betrifft:** `web/src/app/shared/emotes/emote-image-loader.ts`, `web/src/app/shared/emotes/emote-sprite.ts`, `web/e2e/atlas-image-loading.measure.ts`, `web/playwright.measure.config.ts`, `docs/Untersuchung-Emote-Bildladen-2026-08-29.md`

Die Nutzungsseite lud für jede Zelle die 4x-Variante eines Emotes — rund 128 px, gerendert auf ~56 px in einer 64-px-Zelle. Bei einem großen Set sind das rund 5,9 MB, und der Engpass war nicht das CDN, sondern die Warteschlange des Browsers: 650 Bilder über eine Verbindung, gemessene Wartezeit vor dem Absenden p90 rund 3,4 s, einzelne Zellen über 5 s leer. Die vollständige Messung samt der widerlegten Alternativhypothesen steht in [`Untersuchung-Emote-Bildladen-2026-08-29.md`](Untersuchung-Emote-Bildladen-2026-08-29.md).

**Ein `IMAGE_LOADER`, kein neuer URL-Bau an der Aufrufstelle.** `EmoteSprite` stellt `NgOptimizedImage` einen Loader bereit, der die gespeicherte 4x-URL auf die kleinste Variante herunterschreibt, die die angeforderte Breite noch deckt (1x/2x/3x bei 32/64/96 px, sonst 4x). Angular baut daraus von selbst ein Density-`srcset`, also lädt ein Display mit normaler Pixeldichte die 2x-Variante und eines mit doppelter weiterhin 4x. Die Alternative — an jeder der sechs Aufrufstellen die passende URL selbst zusammensetzen — hätte die Größenlogik über das halbe Frontend verteilt und die Pixeldichte gar nicht berücksichtigen können.

**Der Loader ist auf die Komponente begrenzt, nicht global registriert.** Er steht in `providers` von `EmoteSprite` und wirkt damit nur über den Element-Injector auf das eigene `<img>`. Global registriert würde er auch `shared/ui/avatar.ts` treffen, das `NgOptimizedImage` für Twitch-Profilbilder nutzt — 7TV-förmige Umschreibungen auf einer Twitch-CDN-URL ergeben Unsinn. Das ist der Grund, warum hier kein `provideImageLoader` in `app.config.ts` steht.

**Was der Loader nicht anfasst, ist die eigentliche Feinheit.** Umgeschrieben wird ausschließlich, was auf `/4x_static.webp` oder `/4x.webp` endet, also genau die zwei Formen, die das Backend speichert. Die Hover-Animation des Sidecars läuft über `animatedEmoteUrl` und endet auf `/2x.webp`; sie fällt damit durch und bleibt unangetastet. Das ist Absicht: `1001d0b` hat die Animation bewusst auf 2x festgelegt, und sie wird einmal für ein einzelnes Emote geholt, nicht für jede Zelle — der Größenkompromiss des Atlas gilt für sie nicht. Jede andere URL läuft ebenfalls unverändert durch, und ohne `width` gibt der Loader die Eingabe zurück.

**Die Vorbedingung wurde geprüft, nicht angenommen.** Eine fehlende Größenvariante wäre kein sichtbarer Fehler, sondern eine dauerhaft leere Zelle — `EmoteSprite` hält das `<img>` bis zum `load`-Ereignis auf `visibility: hidden` und setzt bei `(error)` nur den Zustand zurück. Vor der Umstellung wurden deshalb alle 649 Emotes des Testsets gegen ihre 2x-Variante geprüft: 431 `_static`, 218 plain, kein einziger Fehlschlag.

**Gemessen, nicht geschätzt** (gemächliches Scrollen, Mediane): Bytes 5,87 → 3,50 MB, Wartezeit p90 3368 → 95 ms, Zelle leer p90 4803 → 959 ms, Zellen über 5 s leer 47 → 0 in jedem Lauf. Kontrolle, dass die Umschreibung greift: in allen Nachher-Läufen wurden ausschließlich 2x-URLs angefordert.

**Das Messskript liegt im Repo, weil die Streuung die Lehre ist.** `web/e2e/atlas-image-loading.measure.ts` läuft über eine eigene Config auf Port 4301 und fällt nicht unter den `*.spec.ts`-Glob der E2E-Suite — es trifft das echte 7TV-CDN, dauert über die nötigen Wiederholungen Minuten und behauptet nichts. Der Grund für die Aufbewahrung: Einzelläufe streuen hier stärker als der gemessene Effekt, ein erster Vergleich aus je einem Lauf zeigte Verbesserungen, von denen nach Wiederholung nur die Byte-Zahl übrig blieb. Wer nachmisst, braucht mehrere Läufe im Wechsel — und soll dafür nicht die CDP-Verkabelung neu bauen müssen.

**Nachtrag aus derselben Runde: breitenbasiertes `srcset` statt Dichtestufen, und warum `eager` dranstehen muss.** Die erste Fassung setzte kein `sizes`, Angular erzeugte also ein Density-`srcset` mit den zwei Kandidaten `1x` und `2x`. Der Browser nimmt den kleinsten Kandidaten mit Dichte >= `devicePixelRatio` — alles über 1,0 landet damit beim 2x-Kandidaten, und das ist die 4x-Datei. Gemessen half die Änderung deshalb nur bei exakt `devicePixelRatio` 1; bei 1,25/1,5/2 blieb alles beim Alten, und 125 % und 150 % sind Windows-Standardskalierungen. Ein Density-`srcset` kann eine Zwischenstufe grundsätzlich nicht ausdrücken, also steht jetzt ein explizites `ngSrcset="32w, 64w, 96w, 128w"` plus `sizes` am Bild. Explizit deshalb, weil Angulars automatischer Breiten-`srcset` nur viewport-relative Werte akzeptiert: ein `sizes` mit Pixelwert wirft ohne eigenes `ngSrcset` zur Laufzeit `NG02952`, und `IMAGE_CONFIG.breakpoints` wird auf diesem Pfad gar nicht gelesen.

**`loading="eager"` steht dabei nicht wegen des Ladezeitpunkts am `<img>`.** Dass `eager` am Scheduling nichts ändert, ist gemessen und in der Untersuchung festgehalten. Es steht dort, weil Angular `sizes` bei `lazy` ein `auto, ` voranstellt: solange ein frisch gerendertes Bild keine Layoutbreite hat, ist `auto` nicht auflösbar, der Browser nimmt den größten Kandidaten und lädt nach dem Layout ein zweites Mal. Gemessen bei `devicePixelRatio` 1: 680 Anfragen für 650 Bilder, davon 30 in 4x — mit `eager` 651 Anfragen und ausschließlich 2x. Ein Zurückdrehen auf `lazy` holt die Doppelabrufe zurück, deshalb hält `emote-sprite.spec.ts` beides fest. Offener Vorbehalt: `eager` ist nur auf Chromium gemessen; auf Safari, dessen Lazy-Schwelle klein ist, fordert es die gepufferten Zeilen früher an als bisher.

**Korrektur vom 2026-08-30: belegt sind die Bytes, nicht die Latenz.** Ein Codex-Review fand, dass die Messharness nach dem Scrollen pauschal drei Sekunden wartete und dann auswertete — noch laufende Requests fehlten in jeder Kennzahl, und zwar die langsamsten. In drei von vier Baseline-Läufen betraf das 13–18 % der Requests, in keinem Nachher-Lauf einen einzigen. Nach der Reparatur (Warten bis alle Requests abgeschlossen sind, Rest als `censored` ausgewiesen) und einer Neumessung mit je drei sauberen Läufen bleibt die Byte-Ersparnis exakt bestehen (8,92 → 6,54 MB bei `devicePixelRatio` 1,25), während Queue- und Zellenlatenz zwischen altem und neuem Stand nicht mehr unterscheidbar sind — beide Stände zeigen null Zellen über zwei Sekunden. Die Änderung ist damit eine Bandbreiten-, keine Latenzoptimierung; die Latenzzahlen in den Commit-Nachrichten dieses Branches sind überholt. Einzelheiten in Abschnitt 10 der Untersuchung.

**Nicht mitgekommen: der kleinere Virtual-Scroll-Puffer.** Er war gebaut und wurde nach isolierter Messung wieder entfernt — gegen den Loader allein kein belegbarer Zusatznutzen bei überlappenden Bereichen, dafür weniger Vorlaufstrecke.

### 2026-08-29 — Ein Sync, der nichts liefert, sagt warum

**Betrifft:** `src/EmotePurge.Core/SevenTv/{SevenTvModels.cs,ISevenTvApiClient.cs}`, `src/EmotePurge.Core/Services/SevenTvSyncFailureReasons.cs`, `src/EmotePurge.Core/Entities/Channel.cs`, `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs`, `src/EmotePurge.Infrastructure/Services/{SevenTvSyncService,EmoteSetStatusService,AdminChannelQueryService}.cs`, `src/EmotePurge.Infrastructure/Migrations/*_AddChannelSyncFailureReason.cs`, `web/src/app/core/emotes/seven-tv-sync-failure.ts`, `web/src/app/features/usage-stats/usage-stats-page.{ts,html}`, `web/src/app/features/admin/admin-channel-detail-page.ts`, `web/public/i18n/*.json`

Issue #32: Ein Channel mit 7TV-Konto, aber ohne aktives Emote-Set sah im Frontend aus wie jeder andere leere Channel — „entweder ist das 7TV-Emote-Set leer, oder der erste Sync läuft noch". Auch der Admin-Drilldown sagte nur, dass kein Sync stattgefunden habe.

**Vier Zustände, ein `null`.** `SevenTvApiClient` konnte auf vier fachlich verschiedene Arten nichts liefern: kein 7TV-Account (404, `LogInformation`), Account ohne aktives Set (`emote_set` fehlt im JSON — **ohne jedes Log**), API-/Netzwerkfehler (`LogWarning`), und „noch nie gesynct", das von den drei anderen nicht zu unterscheiden war. `SevenTvSyncService` reichte alle vier als `null` weiter, `SevenTvPeriodicResyncWorker` hatte zu `if (result is not null)` keinen `else`-Zweig — es wurde **nichts** geschrieben, auch `LastSyncedAtUtc` nicht. Die Oberfläche konnte also gar nichts anderes sagen als eine Vermutung mit „entweder … oder".

**Ergebnistyp statt `null`.** `GetChannelStateForTwitchUserAsync` und `ResolveTwitchUserIdAsync` geben jetzt `SevenTvChannelStateResult` bzw. `SevenTvTwitchUserIdResult` zurück, beide nie `null`, beide mit einem `SevenTvLookupStatus` (`Ok`, `NoSevenTvAccount`, `NoActiveEmoteSet`, `Unavailable`). Der stille Zweig hat sein Log bekommen. Ein zusätzlicher Fall ist dabei aufgefallen und getrennt worden: eine 200er-Antwort mit leerem Body ist kaputt, nicht „kein Set" — sie zählt als `Unavailable`, damit niemand aufgefordert wird, etwas zu reparieren, das in Ordnung ist.

**Enum innen, String außen.** Der Kontrollfluss im Backend läuft über das Enum (vollständiges `switch`), die API liefert die drei Codes `no_seventv_account`, `no_active_emote_set`, `seventv_unavailable` als Strings — dieselbe Begründung wie bei `ChannelLiveStates` und `ApiErrorCodes`: der Wert auf der Leitung ist der Wert, der im Code steht, unabhängig von Serializer-Einstellungen. Dazu kommt hier, dass das Enum ein `Ok` trägt, das nie auf die Leitung darf. Genau eine Abbildungsfunktion (`SevenTvSyncFailureReasons.FromStatus`) verbindet beide; ein unbekannter Status wirft, statt still zu „kein Fehler" zu werden.

**Zwei Spalten, nicht eine.** `Channel.LastSyncFailureReason` **und** `Channel.LastSyncAttemptAtUtc`. Der Grund allein wäre wertlos: ohne Versuchszeitpunkt liest sich ein drei Tage alter Grund wie eine Aussage über jetzt. Das Paar mit dem bestehenden `LastSyncedAtUtc` ist die eigentliche Diagnose — gleiche Werte heißen „der letzte Versuch hat geklappt", ein neuer Versuch neben einem alten Erfolg datiert den Ausfall. `LastSyncedAtUtc` bleibt dabei streng „letzter **erfolgreicher** Sync"; ein Fehlversuch bewegt es nicht.

**Der Reset ist der Teil, der sonst vergessen wird.** `LastSyncFailureReason = null` steht im selben Block wie `LastSyncedAtUtc = syncedAt`, nicht an einer zweiten Stelle: ein Channel, der auf 7TV ein Set aktiviert, muss sofort aufhören, als kaputt beschrieben zu werden — sonst überlebt der Empty-State das Problem, das er beschreibt. Live verifiziert, in beide Richtungen.

**Ein Fehlschlag löscht nichts.** Weder `ActiveEmoteSetId` noch Emote-Zeilen werden angefasst — dieselbe Asymmetrie, die schon die Empty-Set-Schutzlogik (S3-12) begründet: ein 7TV-Ausfall darf nicht das Mass-Delete-Panel wegnehmen oder ein ganzes Set archivieren. Bekannte Folge, bewusst in Kauf genommen: ein Channel, der sein Set *nachträglich* deaktiviert, behält seine alten Emote-Zeilen und zeigt weiter sein Raster; der Grund steht dann im Admin-Drilldown, aber der Nutzer sieht keinen Empty-State, weil es nichts Leeres zu zeigen gibt. Die Alternative wäre ein Wipe auf Verdacht.

**Die Schutzlogik gegen leere Sets schreibt bewusst gar nichts** — weder Grund noch Versuchszeit. Sie trifft keine Aussage über den Channel, sie verweigert die Aktion; der nächste Tick entscheidet. Ein Versuchszeitstempel dort würde eine Reconciliation behaupten, die nie stattgefunden hat.

**Der Worker bleibt unverändert.** Die Persistenz sitzt in `SevenTvSyncService`, wo der `AppDbContext` ohnehin liegt — der fehlende `else`-Zweig in `ResyncOnceAsync` wird damit gegenstandslos statt nachgerüstet. `SyncChannelAsync` behält seine Signatur, also auch `Worker.cs` und `SevenTvEventClient`.

**Zwei Logzeilen, zwei Level — die Aussage ist gedrosselt, die Diagnose ist leise.** `SevenTvSyncService` schreibt den Grund auf `Information`, aber **nur wenn er sich ändert**: der periodische Resync läuft alle 60 s über jeden kaputten Channel, eine bedingungslose Zeile würde alles andere im Log begraben, und der gespeicherte Wert ist ohnehin das, was die Oberfläche liest. Die zwei Zeilen im Client darunter stehen deshalb auf `Debug` statt `Information` — die für das fehlende Emote-Set und die für den fehlenden 7TV-Twitch-Match. Beide fallen pro Aufruf an, also rund 1.440-mal am Tag je betroffenem Channel, und beide tragen nur die Twitch-Id bzw. wiederholen, was die gedrosselte Zeile mit Channelnamen schon sagt. Beides wurde im Livetest gemessen — je 4 Client-Zeilen gegen 1 Grund-Zeile in wenigen Minuten — und danach abgesenkt; über den Loglevel ist die Information jederzeit zurückzuholen.

**Das Polling der Nutzungsseite bricht jetzt ab.** `awaitSync` lief bis zu 15 Ticks à 2 s, obwohl schon die erste Antwort mit einem Grund beweist, dass nie eine Set-Id kommt: 30 Sekunden Ladebanner, gefolgt von einer falschen Aussage. Auch `seventv_unavailable` bricht ab — eine wahre Aussage sofort schlägt eine halbe Minute Warten auf eine falsche. Der Rückweg ist gedeckt: die Seite hört auf `channel.synced` und lädt den Set-Status neu, und ein geglückter Erstsync setzt `HasChanges` (leere → gefüllte Set-Id), veröffentlicht also genau dieses Ereignis. Am 2026-08-29 an einem echten Channel gemessen, der während der offenen Seite auf 7TV ein Set bekam: sie hat ohne Reload umgeschlagen. Welches der beiden möglichen Ereignisse sie ausgelöst hat — der Resync mit `HasChanges` oder der unmittelbar folgende EventAPI-Dispatch — ist dabei **nicht** getrennt worden; belegt ist der Umschlag, nicht der Auslöser.

**Ein langsamer Recheck deckt das ab, was gar kein Ereignis auslöst.** Der Codex-Review vom selben Tag hat die Lücke gefunden, die das Abschalten des Pollings aufgerissen hat: Ein *fehlgeschlagener* Sync gibt `null` zurück und veröffentlicht deshalb nichts. Wechselt der Grund — `no_seventv_account` → `no_active_emote_set`, wenn sich jemand gerade bei 7TV registriert —, erfährt eine offene Seite das nie und fordert weiter dazu auf, ein Konto anzulegen, das es inzwischen gibt. Genau das ist während der Live-Verifikation dieses Branches unfreiwillig passiert. Dasselbe gilt, wenn ein Grund verschwindet, ohne dass sich sonst etwas ändert (`emoteSetSwitched` und `inventoryChanged` beide false → `HasChanges: false`). Solange die Seite einen Grund zeigt, fragt sie den Set-Status deshalb alle 30 Sekunden erneut ab und hört damit auf, sobald sich der Zustand ändert.

**Warum das kein Redis-Ereignis geworden ist.** Naheliegender wäre gewesen, bei einem Grund-Wechsel ein `channel.synced` zu senden. Das hätte aber die Grenze verschoben, die dieser Branch bewusst zieht: Persistenz liegt in `SevenTvSyncService`, Veröffentlichen ausschließlich im Worker. Um von dort zu senden, müsste entweder der Service selbst publizieren — dann läge das Publizieren an zwei Orten — oder `SyncChannelAsync` seine Signatur ändern, an der auch `Worker.cs` und `SevenTvEventClient` hängen. Das ist strukturelle Arbeit an einer tragenden Grenze für einen Fehler, dessen Schaden „falscher Satz bis zum nächsten Reload" ist. Der Recheck bleibt im Frontend und ändert keinen Vertrag.

**Und er ist nicht die Rückkehr dessen, was abgeschafft wurde.** Weg musste der Burst aus 15 Ticks à 2 Sekunden, der die Seite eine halbe Minute im Ladebanner hielt, um dann etwas Falsches zu sagen. Der Recheck läuft im 30-Sekunden-Takt, während die Seite bereits eine wahre Aussage zeigt — er hält sie nur aktuell, es gibt keinen Blindzustand dazwischen. 30 Sekunden, weil der Resync im Backend ohnehin nur alle 60 Sekunden läuft: schneller zu fragen könnte keine frischere Antwort sehen. Der Einwand aus den 429ern von #33/#35 trägt hier nicht — die kamen von Bursts (fünf Permits je Workspace-Aufruf, ein Ereignis je gelöschtem Emote), nicht von einer Anfrage pro halber Minute auf einer offenen Seite. Zwei Playwright-Fälle nageln beide Richtungen fest, mit echtem Warten statt Fake-Timern; sie kosten die Suite rund 30 Sekunden.

**Der alte Hinweistext ist mit weggefallen.** `usageStats.noActiveEmotesHint` sagte „entweder ist das 7TV-Emote-Set leer, oder der erste Sync läuft noch" — genau die Vermutung, die diese Änderung durch eine Aussage ersetzt. Er heißt jetzt nur noch, was er sicher weiß: das aktive Set enthält derzeit keine Emotes.

**Der Grund steht im Admin-Drilldown zweimal** — als Warnbanner über dem Verdikt und als Zeile „Letzter Fehlergrund" in der Datenbank-Liste. Bewusst so: das Banner erklärt das Verdikt (ein Channel ohne aktives Set liest sich zwangsläufig auch als `degraded`, weil seine Emote-Set-Subscription nie bestätigt werden kann), die `<dl>` ist der Ort, an dem ein Admin Feld für Feld nachliest — eine Lücke dort wäre schlechter als die Wiederholung. Am 2026-08-29 am laufenden System angesehen und so bestätigt.

**Live verifiziert** gegen zwei reale Channels. `reved` (Twitch-Id `32961320`) hat ein 7TV-Konto `01G9GVA6KR0007WHWD38QCENVY` ohne aktives Set und deckt `no_active_emote_set` ab; ein Twitch-Account ganz ohne 7TV-Konto deckt `no_seventv_account` ab — jeweils Worker-Log, beide Spalten, Empty-State und Admin-Drilldown. Der zweite Channel hat dabei unfreiwillig auch den Übergang zwischen den beiden Gründen gezeigt: nach dem 7TV-Login existierte das Konto, aber noch kein aktives Set. Genau dort hat die Drosselung ihren Vertrag bewiesen — sechs Ticks mit demselben Grund blieben stumm, der Wechsel des Grundes schrieb eine Zeile. `seventv_unavailable` ist als einziger Code nur durch Tests gedeckt: er bräuchte ein ausgefallenes 7TV, und der Persistenzpfad dahinter ist derselbe, der bei den anderen beiden live lief. Der Reset wurde gegengeprüft, indem einem gesunden Channel ein `seventv_unavailable` mit altem Versuchsdatum untergeschoben wurde — der nächste echte Sync hat ihn gelöscht und beide Zeitstempel gleichgezogen.

**Migration** `AddChannelSyncFailureReason`, zwei nullable Spalten, additiv — vom alten Image ignoriert und deshalb wie üblich **vor** dem Deploy von Hand nachzuziehen.

---

### 2026-08-29 — Ein Reload-Zyklus pro Channel, eine SSE-Verbindung pro URL

**Betrifft:** `web/src/app/core/live/live-update.service.ts`, `web/src/app/core/live/live-reload.ts`, `web/src/app/features/channel-workspace/channel-workspace-layout.ts`, `web/src/app/features/usage-stats/usage-stats-page.ts`, `web/e2e/channel-workspace.e2e.spec.ts`

Die 429er aus #33 und #35 kamen nicht von einer zu niedrigen Decke, sondern von der Zahl der Anfragen. Zwei Ursachen, beide im Frontend.

**`LiveUpdateService.stream()` war kalt.** Jeder Abonnent bekam seine eigene `EventSource`, und die Abonnenten sitzen übereinander: Workspace-Layout und die darin geroutete Seite hören auf dieselbe Channel-URL, die Admin-Monitoring-Seite auf bis zu drei. Das waren pro Nutzer mehrere Verbindungen, mehrere Auth-Handshakes und mehrere Slots im `ILiveEventStream`-Budget für denselben Datenstrom. `stream()` ist jetzt Multicast und ref-gezählt pro URL: der erste Abonnent öffnet, der letzte schließt. Kalt bleibt es trotzdem — nichts wird geöffnet, bis jemand abonniert, und eine URL, deren Abonnenten alle weg sind, wird beim nächsten neu aufgebaut. Ohne diesen Neuaufbau hätte der `switchMap` in `liveEvents` nach dem Zurücknavigieren ein totes Observable geliefert, also einen Nutzer ohne Live-Updates, der nichts davon merkt.

**Der Workspace-Reload lief ungedrosselt.** `channel-workspace-layout.ts` hing an `liveEvents` und holte `emotes/duplicate-names` bei *jedem* `channel.synced`. Ein 7TV-Massen-Löschen schiebt eines davon pro entferntem Emote, etwa alle 275 ms — das waren 22 der 38 gemessenen Ablehnungen vom 2026-08-28. Die Seite hängt jetzt an `liveReload` mit demselben Fenster wie die Usage-Seite.

**Die Debounce-Konstante ist geteilt, nicht zweimal dieselbe Zahl.** `CHANNEL_RELOAD_DEBOUNCE_MS = 1000` liegt in `live-reload.ts`, weil die beiden Seiten gleichzeitig montiert sind und auf derselben Verbindung hören: ein Wert heißt eine Burst-Grenze für beide. Die Vote-Session-Detailseite behält bewusst ihr kürzeres 500-ms-Fenster — dort ist die Auszählung genau das, worauf der Nutzer schaut, und sie ist nie gleichzeitig mit dem Massen-Löschen offen.

**`debounceTime`, nicht `throttle` — und das ist Absicht.** Bei einem Event alle 275 ms verstreicht das Fenster während des Laufs gar nicht; der ganze Massen-Löschvorgang kollabiert auf einen Refetch am Ende statt auf einen pro Sekunde. Für den Fall, auf den es ankommt, ist das die stärkere Drosselung. Die Resync-Bestätigung ist ein einzelnes Event und wird dadurch um genau eine Sekunde verzögert, weit innerhalb von `RESYNC_FEEDBACK_MS` (4000).

**Warum das Limit nicht angehoben wurde**, steht im Eintrag zur Beobachtbarkeit des Limiters vom selben Tag.

---

### 2026-08-29 — Der Rate-Limiter sagt, dass er abgelehnt hat: Log, Retry-After und ein Fehlercode

**Betrifft:** `src/EmotePurge.Api/RateLimiting/RateLimitRejection.cs`, `src/EmotePurge.Api/Program.cs`, `src/EmotePurge.Api/Validation/ApiErrorCodes.cs`, `tests/EmotePurge.Api.Tests/RateLimitRejectionTests.cs`, `web/src/app/core/i18n/api-error.ts`, `web/public/i18n/*.json`

Der Rate-Limiter hatte kein `OnRejected`. Eine Ablehnung war damit ein nackter 429: kein Body, kein `Retry-After`, keine Logzeile. Serverseitig war nicht erkennbar, dass überhaupt jemand abgelehnt worden war.

**Was das gekostet hat.** Die Issues #33 („A User with many Channels gets a rate limit error on first login") und #35 („User got a rate limit error when deleting emotes") brauchten zwei Untersuchungsrunden und einen Zugriff auf das Produktions-nginx-Log, nur um die Frage zu beantworten, *wer* die 429er schickt. Sie kamen aus unserer eigenen Policy `ExternalApi`; Cloudflare und nginx waren als Verdächtige nur deshalb im Rennen, weil unsere Antwort nichts über sich selbst aussagte. Die Auswertung des Logs vom 2026-08-28 ergab 38 Ablehnungen, 22 davon auf `emotes/duplicate-names`.

**Der neue Vertrag.** Jede vom Limiter abgelehnte Anfrage antwortet ab sofort mit `429`, einem `Retry-After`-Header in ganzen Sekunden und dem Body `{"errorCode":"rate_limit_exceeded","retryAfterSeconds":<int>}`. Das ist dieselbe Form, die `resync_cooldown_active` seit jeher hat, weshalb das Frontend keinen Sonderfall braucht: `apiErrorTranslationKey` erkennt den Code und rendert `errors.api.rate_limit_exceeded`.

**Der Status-Fallback bleibt und ändert seine Bedeutung.** `errors.status.rateLimited` deckt ab jetzt genau die 429er ab, die *nicht* von uns kommen — nginx, Cloudflare, ein zwischengeschalteter Proxy. Der Text der beiden Schlüssel ist bewusst wortgleich: welcher Dienst gedrosselt hat, ist eine Diagnose-Eigenschaft und keine, die dem Nutzer eine andere Handlungsanweisung gibt. `retryAfterSeconds` steht im Body und wird heute nicht angezeigt — dafür müsste `apiErrorTranslationKey` Parameter durchreichen, was jede Aufrufstelle anfasst.

**Die Logzeile nennt drei Dinge**, weil genau diese drei aus dem nginx-Log rekonstruiert werden mussten: welche Policy, welcher Pfad, welcher Partition-Key. Der Key ist für jeden angemeldeten Aufrufer die Twitch-User-Id und nur beim anonymen `/api/health` die Remote-IP — beides hält der Host ohnehin schon, in der Datenbank respektive im Access-Log des Reverse Proxy. Die Log-Kategorie ist der feste String `EmotePurge.Api.RateLimiting` statt `ILogger<Program>`: die Aggregation (S3-36) alarmiert darauf, und eine aus einem Typ abgeleitete Kategorie wandert mit dem Typ.

**Partitionierung und Ablehnung liegen in einer Klasse**, weil sie sich ein Geheimnis teilen: nur die Partitionsfabrik weiß, wie der Key zustande kam (Claim → IP → `unknown`), und nur die Ablehnung braucht ihn. Sie reicht ihn über `HttpContext.Items` weiter, statt die Kette ein zweites Mal nachzubauen — zwei Kopien einer Fallback-Kette, die auseinanderlaufen können, ausgerechnet an der Stelle, deren ganze Aufgabe es ist, genau zu sagen, was passiert ist.

**`Retry-After` auf .NET 10 ist eine Überschätzung, und das ist die richtige Richtung.** `FixedWindowRateLimiter` meldet als `MetadataName.RetryAfter` das ganze Fenster statt der Restzeit; wer es abwartet, ist immer über der Grenze. .NET 11 macht den Wert exakt, ohne dass hier etwas zu ändern wäre.

**Das Limit wurde nicht angehoben.** Naheliegend, und schon einmal getan — von 20 auf 40, was das Problem verschob statt es zu lösen. `ExternalApi` schützt nicht unsere Datenbank, sondern die app-weiten Helix- und 7TV-Kontingente: ein einzelner schleifender Account leert sie, und danach verliert jeder Moderator jedes Channels stillschweigend seine Rechte. Die Ursache lag in der Requestzahl, nicht in der Decke — s. den Eintrag zum Reload-Zyklus im selben Datum.

---

### 2026-08-29 — Emote-Bilder werden als 4x-Standbild gespeichert, die Animation zieht in den Sidecar

**Betrifft:** `src/EmotePurge.Infrastructure/SevenTv/SevenTvEmoteJsonMapper.cs`, `SevenTvApiDtos.cs`, `web/src/app/shared/emotes/emote-url.ts`, `emote-sprite-animated.ts`, `web/src/app/features/usage-stats/usage-stats-page.{ts,html}`, `tests/EmotePurge.Infrastructure.Tests/Unit/SevenTvDispatchParserTests.cs`

Die gespeicherte `ImageUrl` war seit jeher `2x.webp`. Bei einem animierten Emote ist das die vollständige Animation mit allen Frames — für eine Zelle von 64 px.

**Die Messung.** Am 2026-08-28 über HandOfBloods Satz von 931 Emotes, Dateigrößen aus 7TVs eigener API: 630 davon animiert, im Mittel 133 KB als `2x.webp`, Spitzenwerte 1.189 KB (`MALTE`), 1.070 KB (`HenningDance`), 1.044 KB (`teamVibe`). Die statischen 301 liegen bei 7 KB. Der ganze Satz kostete damit 73–84 MB, und der Atlas fordert virtualisiert 150–400 Zellen auf einmal an — 13–18 MB pro Bildschirmfüllung. Als `4x_static.webp` sind es 14,8 MB für denselben Satz, gemessen an denselben 250 Emotes über zwei Durchgänge.

**4x, nicht 2x.** Die Atlas-Zelle ist 64 CSS-px; erst eine 128-px-Quelle bleibt bei Device-Pixel-Ratio 2 scharf. Das kostet hier nichts, weil ohnehin das Standbild angefordert wird. Angulars Oversized-Warnung (NG0913) schlägt erst bei mehr als 1200 px Differenz an, wir liegen 64 px darüber — die Konsole bleibt still.

**Die Fallunterscheidung macht 7TV selbst.** Jeder Eintrag in `host.files` trägt neben `name` ein `static_name`: `4x_static.webp` bei einem animierten Emote, eine Wiederholung von `name` bei einem stillen. Wir lesen es und müssen nie entscheiden, ob ein Emote animiert ist — kein `animated`-Flag durch Entity, DTO und Migration, keine Prod-Migration. Geprüft am 2026-08-28: in 931 von 931 Emoten des REST-Satzes gesetzt, ebenso in allen zwölf Dateien jedes aufgezeichneten EventAPI-Frames unter `Unit/TestData`. Separat verifiziert, dass die Variante genau dann existiert, wenn `animated` wahr ist — 233 Stichproben, 152 animiert mit 200, 81 still mit 404, keine Ausnahme.

**Das `_static` im String ist damit tragend, nicht nebensächlich.** Das Frontend leitet daraus die animierte URL ab (`animatedEmoteUrl`): endet die gespeicherte URL auf `4x_static.webp`, gibt es eine Animation und sie liegt unter `2x.webp`; endet sie nicht darauf, ist das Emote still und es gibt nichts zu holen. Mapper und Ableitung verweisen deshalb wechselseitig aufeinander.

**Die Animation ist nicht weg, sie ist umgezogen** — auf die Flächen, die ein einzelnes Emote zeigen: Sidecar und Readout-Zeile, beide von `(mouseenter)="inspect(emote)"` gespeist. Zwei Details, beide aus dem Verhalten der bestehenden Komponente:

- **Ueberlagert, nicht getauscht.** `EmoteSprite` hält ein noch nicht geladenes Bild bewusst unsichtbar. Ein bloßes Umbinden von `[ngSrc]` hätte das Emote unter dem Zeiger für die Dauer eines 1,2-MB-Downloads ausgeblendet. Stattdessen hält das Standbild die Box — es ist bereits gezeichnet, weil der Atlas genau diese URL geladen hat — und die Animation legt sich darüber, sobald sie dekodiert ist.
- **200 ms Verweildauer.** Ein Zug quer über das Raster lässt diese Komponente Dutzende Emotes sehen. Ohne die Verzögerung feuerte jede überstrichene Zelle ihre Anfrage. `upgraded` ist auf URL-Identität geschlüsselt statt auf ein bloßes Boolean, aus demselben Grund wie `settled`, nur schärfer: ein von der Vorgängerzelle stehengebliebenes `true` hätte die nächste Animation sofort einhängen lassen und ihr genau die Verweildauer erspart, die sie verdienen sollte.

Mobil bleibt alles still, ohne Sonderfall: die Readout-Zeile ist `pointer-coarse:hidden`, der Sidecar `hidden lg:block`. Auf einem Touchgerät gibt es kein Hover, das eine Animation anfordern könnte.

**Bewusst nicht gebaut: ein eigener Bild-Cache.** Naheliegend, weil die URLs über die 7TV-ObjectID inhaltsadressiert und `immutable` sind und die Domain seit kurzem hinter Cloudflare hängt. Dagegen sprach die Messung: in 916 Anfragen an `cdn.7tv.app` kein einziges 429 — es gibt keine Drosselung, die uns ausbremst. Was es gibt, ist Streuung im Durchsatz (derselbe 13-MB-Burst einmal in 0,9 s, einmal in 14,1 s, beide Male `cache=hit`), und die trifft proportional zur Nutzlast. Ein Cache hätte also Infrastruktur gebaut, um 73 MB schnell auszuliefern, statt die 73 MB nicht zu schicken. Für Wiederkehrer hätte er ohnehin nichts gebracht: 7TV liefert `max-age=31536000, immutable`, der zweite Besuch kommt aus dem Browser-Cache. Die Frage wird nach dieser Änderung neu gestellt, an gemessenen Zahlen statt an einer Vermutung. Zur Größenordnung, falls doch: sechs große Channels ergeben 3.786 eindeutige Emotes, also 21 MB als Standbilder — die Sätze überlappen dabei kaum (3.227 der IDs kommen in genau einem Channel vor, Dedupe-Ersparnis 17,9 %).

**Bestehende Zeilen behalten ihre alte URL, bis ein Resync läuft.** Additive Änderung ohne Migration; der periodische Vollsync schreibt sie ohnehin nach.

---

### 2026-08-29 — emotepurge.app steht hinter Cloudflare, und nginx musste die echte Besucher-IP zurückbekommen

**Betrifft:** `docs/VPS-Reverse-Proxy.md`, nginx auf dem VPS (`sites-enabled/activitytracker-complete.conf`, Referenzkopie in `sensitron/infra-docs` unter `configs/nginx/`), mittelbar `src/EmotePurge.Api/Program.cs` (Rate-Limiter-Partition)

Die Kette ist seit kurzem **Client → Cloudflare → nginx → Kestrel**, nicht mehr Client → nginx → Kestrel. Die Origin-IP steht in keinem DNS-Eintrag. Der Umzug hat einen latenten Fehler in der nginx-Config scharf gestellt, der vorher folgenlos war.

**Der Fehler.** Die beiden `limit_req_zone`-Direktiven schlüsseln auf `$binary_remote_addr`. Ohne `set_real_ip_from`/`real_ip_header` ist das hinter Cloudflare eine Edge-IP, nicht der Besucher — gemetert wurde also nicht pro Nutzer, sondern die halbe Besucherschaft in einen gemeinsamen Eimer. Verschärfend: die Zone `general` teilt sich diese nginx-Instanz mit projektfremden vHosts.

**Die Messung.** Am 2026-08-27 sprangen die `limiting requests`-Einträge im error.log auf 2.001 (activitytracker.icu) plus 1.003 (emotepurge.app) an einem einzigen Tag, gegenüber 3–191 pro Tag davor. Dass beide Sites gleichzeitig ausschlagen, ist die Signatur des geteilten Eimers: eine Lastquelle genügt, um alle mitzureißen. Vereinzelte Treffer gab es schon vorher — 10r/s für eine SPA-Auslieferung ist knapp bemessen —, aber erst der geteilte Schlüssel machte daraus einen Ausfall.

**Der Fix**, am 2026-08-29 eingespielt und im access.log gegengeprüft: 15 IPv4- und 7 IPv6-Bereiche als `set_real_ip_from` plus `real_ip_header CF-Connecting-IP` im `http`-Kontext, also **vor** den Zonen. Nicht spoofbar, weil nginx den Header nur auswertet, wenn die Verbindung selbst aus einem der Bereiche kommt. Die Ranges ändern sich gelegentlich und sind gegen `cloudflare.com/ips-v4` bzw. `/ips-v6` abzugleichen.

**Drei Dinge, die ab jetzt bei jeder Fehlersuche gelten** — sie haben bei der Ursachenanalyse zu den Rate-Limit-Meldungen (Issues #33/#35) am meisten Zeit gekostet:

- **Ein 429 im Browser kommt nie von nginx.** `limit_req` antwortet mit **503**, ein Überlauf sieht also aus wie ein Ausfall, nicht wie ein Limit. Ein 429 stammt entweder vom Rate-Limiter der Api oder von Cloudflare selbst — und das Frontend kann die beiden nicht unterscheiden, weil `apiErrorTranslationKey` auf den Statuscode mappt und beide auf `errors.status.rateLimited` landen. Unterscheidungsmerkmal ist der Response-Header `cf-ray`.
- **Was Cloudflare abweist, erreicht den Origin nie** und steht folglich in keinem Log auf dem VPS. Fehlt eine erwartete Zeile im access.log, ist das der erste Verdacht — dann gehört der nächste Blick in die Security Events im CF-Dashboard, nicht weiter auf den Server.
- **SSH geht nicht über die Domain.** Cloudflare proxyt nur HTTP/HTTPS; Config-Deploys laufen über die Origin-IP.

**Für die Api ändert sich nichts, aber aus einem anderen Grund als vermutet.** Der Rate-Limiter partitioniert nach Twitch-User-Id und fällt nur bei anonymen Requests auf die IP zurück (`Program.cs`), praktisch also nur bei `/api/health`. Diese IP war seit dem Umzug allerdings falsch: `$proxy_add_x_forwarded_for` hängte die Edge-IP hinten an, und `UseForwardedHeaders` nimmt mit dem Default `ForwardLimit = 1` genau den letzten Eintrag. Der nginx-Fix repariert das mit, weil `$remote_addr` danach wieder die Client-IP ist — App-seitig war also nichts zu ändern, aber nicht, weil es egal gewesen wäre.

**Noch offen:** Ob SSE (`/api/channels/live-events`, `/api/channels/{c}/live`) durch Cloudflare unverändert durchkommt, ist nicht systematisch geprüft. `/api/` kommt als `cf-cache-status: DYNAMIC` durch, das Hardening-Snippet setzt `proxy_buffering off` und die Api sendet `X-Accel-Buffering: no` — aber ein LIVE-Badge, das ohne Reload umspringt, hat seit dem Umzug niemand bewusst beobachtet. Ebenfalls zu prüfen: ob Cloudflares Auto Minify und Rocket Loader aus sind.

### 2026-08-29 — Die Arbeitsweise steht global und versioniert, nicht dreifach kopiert

**Betrifft:** `CLAUDE.md` (Regeln 20–22, Abschnitt „Arbeitsweise"), `~/.claude/CLAUDE.md` → `sensitron/infra-docs`, `claude/`

Die Abschnitte „Subagents & Modelle", „Codex-Integration" und „Plan-Execution & Context-Hygiene", am selben Tag hier angelegt, sind noch am selben Tag wieder verschwunden — sie stehen jetzt global. Zurück bleibt ein Verweis plus das, was wirklich projektspezifisch ist: welche Test-Gates hier „fertig" bedeuten.

**Der Anlass war ein Schaden, kein Aufräumdrang.** Dieselben Regeln lagen in vier Fassungen vor — global, Homeport, ActivityTracker und seit heute früh hier — und sagten drei verschiedene Dinge. Die globale Datei war die einzige, die noch die alte Regel führte, und weil sie als einzige in *jeder* Sitzung geladen wird, wurde danach gehandelt: Der Plan zur Channel-Identität ging an einen Fable-Subagenten, obwohl zwei der drei Projektfassungen das ausschlossen. Kopien, die niemand gemeinsam pflegt, sind schlimmer als ein einzelner Ort, den man aufschlagen muss.

**Warum die globale Datei überhaupt kopiert wurde:** sie war unversioniert. Das war ein echter Grund, aber ein lösbarer — es ist ein Symlink-Problem, kein Dokumentationsproblem. `~/.claude/CLAUDE.md` und `~/.claude/rules` zeigen jetzt nach `sensitron/infra-docs` unter `claude/`; die immer geladene Anweisung hat damit Historie, und der Grund für die Kopien ist entfallen. Eine Änderung daran wirkt sofort, ist aber erst gesichert, wenn sie dort committet ist — das steht als Hinweis in der Datei selbst und ausführlich in `claude/README.md`.

**Die Grenze verläuft zwischen Werkzeug und Projekt.** Global: wie gearbeitet wird — Delegation, Modellwahl, Fable-Einsatz, Pläne ohne Code, Plan-Tasks als Subagent, Codex-Zweitmeinung. Projektdatei: was dieses Projekt ist — Stack, Schichtentreue, Domänenregeln — und ausschließlich die *Abweichung* von der Arbeitsweise, falls je eine nötig wird. Homeport hat eine solche und behält sie deshalb (Pläne dort vorerst von Opus, als offene Messfrage gekennzeichnet); hier gibt es keine.

**Der Preis, bewusst getragen:** Wer nur dieses Repo klont, sieht die Arbeitsweise nicht mehr. Das ist richtig so — es ist Werkzeugkonfiguration und kein Projektwissen; ein Mitwirkender ohne diese Claude-Installation braucht sie nicht.

### 2026-08-29 — Pläne beschreiben Absicht, nicht Code; Codex wird zur Pflicht-Zweitmeinung

**Betrifft:** `CLAUDE.md`, `~/.claude/CLAUDE.md`

Die Arbeitsweise aus Homeport und ActivityTracker ist hierher übernommen worden, weil sie dort gemessen und angepasst wurde. Zwei Punkte davon ändern, wie in diesem Repo gearbeitet wird.

**Pläne enthalten keinen fertigen Code.** Ein Plan beschreibt Absicht, Verträge, Grenzfälle und die Reihenfolge der Tasks. Was der Implementer nur abtippen soll, muss man nicht planen, sondern implementieren. Die Zahl dahinter stammt aus Homeport: die Pläne dort waren mit über 40.000 Zeilen fast doppelt so umfangreich wie der gesamte Produktivcode, und bei einem A/B-Test lieferten zwei verschiedene Modelle auf demselben Brief byte-identischen Code — das Denken war da längst erledigt, bezahlt wurde es zweimal. **Die Regel betrifft den Umfang, nicht das Modell:** Fable darf Pläne schreiben, sie sollen nur kein Listing enthalten, das abgetippt wird. Homeport weicht davon ab und lässt Pläne vorerst von Opus schreiben — das ist dort eine offene Messfrage und keine allgemeine Vorgabe. Die beiden am selben Tag entstandenen Pläne unter `docs/superpowers/plans/` sind noch im alten, ausformulierten Stil und bleiben so; nachträglich gekürzt wird nichts.

**Plan-Tasks laufen immer als Subagent.** Nie direkt in der Hauptsession. Jeder startet mit frischem Kontext, bekommt seinen Task plus den nötigen Kontext, und der Orchestrator prüft das Ergebnis.

**Der `Plan`-Subagent-Typ ist read-only** — er kann seine eigene Plandatei nicht schreiben. Beide Pläne dieses Tages mussten per Skript aus dem Agent-Transkript unter `/tmp/…/tasks/<id>.output` in die Datei gezogen werden, statt sie durch den Kontext der Hauptsession zu schleusen. Das steht jetzt als Handgriff in `CLAUDE.md`, damit die nächste Sitzung nicht dieselbe Runde dreht.

**Codex Sol wird zur Pflicht vor jedem Merge auf `main`**, Konzepte und Pläne zusätzlich adversarial. Widersprechen sich Opus-Review und Codex, entscheidet Fable als Schiedsrichter — und zwar nur bei echtem Widerspruch (gegensätzliche Bewertung derselben Stelle, unvereinbare Fixes, ein P1/P2-Finding, das der andere gar nicht sieht); reine Ergänzungen zählen nicht. Der Orchestrator löst solche Konflikte nie stillschweigend selbst auf. **Implementierung bleibt trotzdem bei Claude-Subagents** — nicht wegen des Kontingents, das derzeit kein Engpass ist, sondern weil derselbe A/B-Test byte-identischen Produktivcode lieferte: die Delegation kauft nichts. Voraussetzung für Regel 22 ist ein lokaler Plugin-Patch, der `disable-model-invocation` entfernt; er ist schon einmal ohne Versionssprung verschwunden, der Prüfbefehl steht deshalb in `CLAUDE.md`.

**Nicht übernommen:** Homeports Testregel („Komponenten testen, wenn sie eigene Logik tragen") widerspricht Regel 12, die isolierte Komponententests hier bewusst ausschließt. Das ist eine Entscheidung mit eigener Begründung und bleibt.

### 2026-08-09 — Die Bänder nennen ihren gemessenen Anteil und tragen eine Farbe

**Betrifft:** `web/src/app/shared/emotes/usage-bands.ts`, `web/src/app/shared/grid/atlas-grid.ts`, `web/src/app/features/usage-stats/usage-stats-page.{ts,html}`, `web/src/app/features/landing/set-shape.ts`, `web/public/i18n/*.json`, `web/e2e/{usage-atlas,landing}.e2e.spec.ts`, `docs/UI-Designsprache.md`

Die Kopfzeilen der Pareto-Bänder sagten „die erste Hälfte der Nutzung", „bis 80 % der Nutzung", „das letzte Fünftel der Nutzung". Drei Dinge stimmten daran nicht, und alle drei sind jetzt anders.

**Der Prozentwert ist gemessen, nicht definiert.** Die 50/80-Marken sind Schwellen der Berechnung, nicht der Anteil, den ein Band trägt: Der Schnitt liegt beim ersten Emote, das die kumulierte Summe über die Marke hebt, und nimmt dieses Emote ganz mit — plus alle mit demselben Zählerstand. Über die sechs Dev-Channels mit nennenswerter Nutzung gemessen sind das 50,0–53,0 % statt 50 % und 80,1–80,9 % statt 80 %; in der E2E-Fixture, wo zwei Emotes 1600 von 2102 Treffern halten, sind es 76 %. „Die erste Hälfte" behauptete also eine Genauigkeit, die die Zahl nie hatte. Der Nenner ist die Nutzung des **ganzen** Sets, der Zähler sind die **sichtbaren** Emotes des Bandes — bei aktivem Namensfilter schrumpfen Anteil und Anzahl gemeinsam, statt 52 % für drei übrig gebliebene Emotes zu behaupten. `groupIntoUsageBands` bekommt den Nenner deshalb hereingereicht statt ihn aus seinen `items` zu bilden.

**Beide Zahlen tragen ihre Einheit, und die Namen sind Klassennamen im Singular.** `TRAGEND · 53 % der Nutzung · 4 Emotes` statt `TRAGENDE EMOTES · die erste Hälfte der Nutzung · 4`. Die alte Zeile stellte einen Prozentanteil und eine Emote-Anzahl unbeschriftet nebeneinander. Und die Titel stehen nicht nur über einer Gruppe, sondern im Sidecar hinter dem Rang eines einzelnen Emotes, wo `#3 · Tragende Emotes` bricht; jetzt liest es sich als `#3 · Tragend`. Das Landing-Diagramm erbt die Namen über dieselben Übersetzungsschlüssel — dieselbe Einteilung, dieselbe Vokabel. Die drei lebenden Bänder teilen sich einen Schlüssel `bands.share`, weil ihr Hinweistext identisch ist; `countOne` ist ein eigener Schlüssel, weil Transloco hier ohne MessageFormat-Plugin läuft.

**Die Bänder haben jetzt eine Farbe, und der Verteilungsstreifen trägt sie mit.** Eine Helligkeitsrampe der Leitfarbe (`USAGE_BAND_FILL`), kein zweiter Farbton: Die Bänder sind eine Rangfolge, kein Nebeneinander von Kategorien. Kein neues Farbtoken — es sind Deckkraftstufen bestehender Farben. Der Streifen färbt jeden seiner 96 Balken nach Band statt wie bisher nur den heavy-Kopf; dazu kam ein **3-px-Sockel**, ohne den die Einfärbung nichts nützt: Ein Balken des toten Schwanzes ist einen Pixel hoch, und eine Farbe auf einem Pixel sieht niemand. Der Sockel stellt „nie benutzt" nebenbei richtig dar — das ist keine kleine Zahl, sondern eine eigene Kategorie. Darunter ein flacher Segmentbalken mit den Nutzungsanteilen, also derselbe Sachverhalt auf der anderen Achse.

**Was dabei verworfen wurde, und warum es nicht wiederkommt:** Der naheliegende Vorschlag waren gestrichelte Linien an den Bandgrenzen. Die x-Achse des Streifens ist aber der Rang, nicht die kumulierte Nutzung — die Marken sitzen dort, wo das Band endet. Gemessen: bei `knirpz` bei 0,5 % und 4,3 % der Breite, mit x-Achse nur über die genutzten Emotes bei 2,3 % und 18,4 %, bei `lililinanana` dagegen bei 24,4 % und 100 %. Es gibt keine Achsenwahl, bei der beide Linien über alle Channels hinweg lesbar sitzen, und die Fassung, die sie auseinanderzieht, kostet den toten Schwanz — also das, was die Seite zeigen soll. Ebenfalls geprüft und verworfen: zwei gestapelte Balken (zeigt die Pareto-Schere am deutlichsten, kostet die doppelte Höhe und wiederholt die Kopfzeilen) und eine Lorenz-Kurve (Marken sitzen stabil, aber die Kurve ist nach 5 % der Breite am Anschlag und danach flach — die Kopf-Knie-Schwanz-Form ginge verloren). Alle drei stehen als Prototyp mit echten Zahlen in `docs/superpowers/prototypes/2026-08-09-verteilung-baender.html`.

Spec: `docs/superpowers/specs/2026-08-09-usage-stats-baender-design.md`.

---

### 2026-08-08 — Die Kurve schweigt, wo sie nichts weiß, und Live-Zahlen tragen keinen Nenner

**Betrifft:** `web/src/app/shared/emotes/usage-series.ts`, `usage-sparkline.ts`, `emote-drilldown-dialog.ts`, `web/src/app/features/usage-stats/usage-stats-page.{ts,html}`, `web/public/i18n/*.json`

Drei zusammenhängende Änderungen an der Nutzungskurve, alle aus derselben Regel:

**1. Die Linie beginnt bei `firstSeenAt`, nicht am Zeitraumanfang.** Vorher lief sie über den ganzen Channel-Zeitraum, also auch über Tage, an denen es das Emote noch nicht gab — eine Nulllinie, die Nichtnutzung behauptet, wo nichts existierte. Am Drilldown von `GAMBA` beobachtet: zwölf flache Tage für ein Emote, das an Tag dreizehn dazukam. Das ist dieselbe Fehlerklasse, die `rangeStartsBeforeTracking` auf Seitenebene längst abfängt („we weren't counting yet" ≠ „this emote is dead"), nur eine Ebene tiefer. Die x-Achse bleibt der Channel-Zeitraum, damit die Kurven im Sidecar-Atlas untereinander vergleichbar bleiben; die Live-Bänder laufen weiter über die volle Breite. Bei `firstSeenAt = null` wird nichts verkürzt — der Wert heißt „unbekannt", nie „neu" (`Emote.cs:23-25`).

Voraussetzung dafür war, dass `firstSeenAt` das echte 7TV-Beitrittsdatum trägt und nicht unseren Sync-Zeitpunkt. Das ist seit dem v4-GraphQL-Umbau vom 2026-08-03 der Fall und wurde vor dem Entwurf über alle Schreibpfade geprüft.

Eine Ausnahme lässt die erste Regel nicht ganz greifen: ein Tag mit gemessener Nutzung wird nie verschwiegen, auch wenn er vor `firstSeenAt` liegt — ein Tag, an dem das Emote benutzt wurde, ist ein Tag, an dem es existiert hat. Nötig wird das, weil 7TV `firstSeenAt` eines wiederhinzugefügten Emotes auf dessen neuesten Set-Eintritt vorzieht, während unsere Zeile und ihre `UsageStat`-Historie unverändert erhalten bleiben (`SevenTvSyncService.cs:289` und `:310-318`) — ein Pfad, den die App selbst über Massenlöschung und das Wiederherstellen-Panel anbietet. Gefunden hat das nicht der Entwurf, sondern die abschließende Review dieses Branches: die Schreibpfad-Prüfung hatte jeden Pfad gefragt, ob er den Sync-Zeitpunkt einstempelt, nie aber, ob der Wert an der eigenen Historie des Emotes vorbei nach vorn wandern kann.

**2. Die Zeile unter der Kurve sagt etwas über das Emote.** „Live an 12 von 13 Tagen" war für jedes Emote identisch — eine Aussage über den Stream, angebracht an einem Emote. Sie wird zur Kehrseite: „An 9 von 12 Live-Tagen nicht benutzt", gezählt nur über Live-Tage innerhalb der Lebenszeit des Emotes. Das ist zugleich das bessere Signal für die einzige Entscheidung, für die es die Seite gibt. Drei Formen, weil der ehrliche Satz sich unterscheidet (ungenutzt / alle genutzt / keine zählbaren Live-Tage, aber sichtbare Bänder); die Auswahl liegt in `liveDayCaptionKey`, damit Sidecar und Dialog nicht auseinanderlaufen können. Die channelweite Zahl steht jetzt einmalig an der Tracking-Zeile.

**3. Live-Zahlen tragen keinen Nenner mehr.** „von 13 Tagen" behauptete für jeden Tag ohne `ChannelLiveDay`-Zeile, der Stream sei offline gewesen. `ChannelLiveDay.cs:8-9` verbietet genau diese Lesart: eine fehlende Zeile heißt „keine Daten". Zeilen entstehen erst seit dem Poll-Worker, ein Backfill existiert nicht. Der Nenner der neuen Zeile sind ausschließlich Tage, für die eine Live-Zeile vorliegt; die Zeile im Seitenkopf nennt gar keinen. Bei null Live-Tagen schweigen beide, statt „0" zu sagen.

Kein Backend-Eingriff, keine Migration. Der Drilldown vom Stimmzettel bleibt unverändert, weil `VoteSessionResult` kein `firstSeenAt` trägt — dort verhält sich der Dialog wie vorher, was zu seinem Bestand passt (er zeigt dort aus demselben Grund schon keine „Im Set"-Zeile).

Spec: `docs/superpowers/specs/2026-08-08-usage-curve-lifetime-design.md`.

---

### 2026-08-08 — Der Header trägt ein Element statt sechs, und das Profilbild reist im Cookie

**Betrifft:** `web/src/app/shared/ui/account-menu.ts`, `avatar.ts`, `display-preferences.ts`, `segmented-control.ts`, `web/src/app/features/shell/app-shell.ts`, `landing-page.html`, `login-page.ts`, `web/src/app/core/auth/auth.service.ts`, `auth.model.ts`, `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs`, `Auth/TwitchClaimTypes.cs`, `Program.cs`, `src/EmotePurge.Core/Twitch/TwitchProfileImage.cs`, `TwitchModels.cs`, `src/EmotePurge.Infrastructure/Twitch/TwitchApiDtos.cs`, `TwitchHelixClient.cs`, `docs/UI-Designsprache.md`

Drei Entscheidungen in einem Umbau.

**Ein Menü statt sechs Dauer-Controls.** Der Header rechts trug Theme-Icon, `DE EN`, Admin-Link, „Meine Abstimmungen", Username und Logout-Button — auf jedem Bildschirm, in jeder Sitzung. Gemessen an der Regel, dass die App-Kopfzeile nur trägt, was wirklich überall gebraucht wird, sind das fünf zu viel. Das Argument trägt am Desktop genauso wie auf dem Handy, während „der Header wird schlanker" dort schwächer wiegt, wo Platz ohnehin da ist. Mit dem Umbau entfallen der Burger, die Mobile-Disclosure und rund 100 Zeilen handgebautes Dismiss-Verhalten — das dritte der drei Duplikate, die der Doc-Kommentar von `popover.ts` selbst benennt. Der Header hat danach keinen `md:`-Zweig mehr. Der Login-Button bleibt bewusst **außerhalb** des Menüs: ein Aufruf zur Anmeldung gehört nicht hinter eine Klappe.

**Disclosure statt `role="menu"`.** Das Panel hält gemischte Kinder — zwei Router-Links, zwei Radiogroups, einen Button. `role="menu"` verlangt `menuitem`-Kinder; eine Radiogroup darin ist nicht valide. Trigger trägt `aria-expanded` + `aria-haspopup="dialog"` + `aria-label`, das Panel ist ein schlichter Container mit zwei `role="radiogroup"`-Inseln. Das ist ein **Rückbau** gegenüber `theme-menu.ts`, das `menuitemradio` benutzte — die Shell hatte dieselbe Entscheidung für ihre Disclosure aber bereits getroffen, und zwei Umgangsweisen für dieselbe Sache sind schlechter als eine korrekte. `aria-controls` steht bewusst **nicht** am Trigger: das Panel existiert im geschlossenen Zustand nicht im DOM, und eine nicht auflösbare IDREF ist ungültiges ARIA.

**Claim statt DB-Spalte.** Twitch liefert `profile_image_url` in derselben Helix-Antwort, die beim Login ohnehin geholt und bisher weggeworfen wurde. Sie wandert als Claim ins Session-Cookie, nicht als Spalte in `User`: keine Migration, kein Refresh-Konzept für ein Bild, das niemand aktuell haben muss, und `/api/auth/me` bleibt DB- und HTTP-frei. Der Preis ist bekannt und akzeptiert — wer beim Deploy angemeldet ist, hat den Claim nicht, sieht bis zum nächsten Login das Monogramm, und das ist kein Fehler. Ein Avatar in der Admin-Nutzerliste wäre der Moment für die Spalte; dieser Umbau ist es nicht.

**Nachtrag vom selben Tag — die Einstellungen ziehen eine Ebene tiefer, und „Abstimmungen" zieht aus dem Menü heraus.** Beides nach einer Runde am gebauten Zustand, beides Umkehrungen einer Vorgabe des Plans.

Theme und Sprache standen zuerst offen im Panel, als zwei volle Segmentgruppen. Am Gerät war der Befund: sie sind das größte Element in einem Menü, dessen Inhalt man einmal einstellt und danach nie wieder. Das ist die **Bemerkenswert-wie-oft-Regel** — bisher nur auf Farbe angewandt — auf **Platz** übertragen. Fünf Varianten wurden als eigenständige HTML-Seite mit den echten Tokens gebaut und die Panelhöhe gemessen statt geschätzt. Das Ergebnis hat die naheliegende Lösung widerlegt: **„einfach kleiner machen" brachte 20 px von 322** (6 %) und hätte das 44-px-Daumenziel gekostet — die Größe war nicht der Hebel, der Ort war es. Gewählt wurde eine Zeile „Einstellungen", die eine Unteransicht öffnet: 190 px, und der Gewinn wächst mit jedem Eintrag, der später dazukommt. **Der Unterschied zur verworfenen Alternative liegt im Wort „überall":** sie hätte auch dort verkleinert, wo ein Daumen bedient. Panelzeilen und Segmente folgen deshalb der Menüzeilen-Regel aus §7.1 der Designsprache — `min-h-11 sm:min-h-9`, 44 px auf schmalen Viewports, 36 px darüber. Dass die erste Fassung dieses Umbaus starre 44 px setzte, war schlicht ein Verstoß gegen eine bestehende Regel, aufgefallen erst bei der Abnahme am Desktop. **Ausgeloggt gilt die Ebene nicht** — dort ist nichts anderes im Panel, und eine Zeile, die eine Unteransicht mit ihrem einzigen Inhalt öffnet, wäre eine Tür vor einer Tür. Der Ebenenwechsel tauscht den ganzen Panelinhalt, deshalb setzt die Komponente den Fokus selbst (auf „Zurück" hinein, auf die Zeile heraus); ohne das fiele er auf `<body>` und das Panel wäre erreichbar, aber nicht bedienbar.

**Der gewählte Zustand der Segmente bleibt der gefüllte Akzent — ein leiserer Ton war gebaut und ist am Gerät durchgefallen.** Die Überlegung war, dass ein Vollton einer Auswahl gehört, die weiter zählt, während man die Seite liest (ein Zeitraumfilter entscheidet, was jede Zahl darunter bedeutet), eine Präferenz dagegen bestätigt und nicht gelesen wird. `SegmentedControl` bekam dafür ein `tone`-Input mit `'quiet'` = dieselbe Leitfarbe als getönte Fläche (`accent-wash`). Am Handy war dann nicht mehr klar genug erkennbar, welches Segment aktiv ist, und das ist bei einem Auswahlcontrol kein Stilmangel, sondern ein Funktionsmangel. Zurückgenommen samt `tone`-Input — eine Variante, die niemand benutzt, ist toter Code.

Der Grund für das Scheitern ist benennbar und gilt über diesen Fall hinaus: `accent-wash` unterscheidet sich von `surface-inset` vor allem im **Farbton**, kaum in der **Helligkeit** (hell: `#e6f5f2` gegen `#e5e8eb`). Auf einem kleinen, hellen, oft schräg gehaltenen Display ist Helligkeit das, was ankommt. **Wer einen Auswahlzustand leiser machen will, muss den Helligkeitsunterschied behalten** — ein reiner Tonwechsel reicht nicht. Die dritte Möglichkeit, gewählt = `bg-surface`, ist unabhängig davon ausgeschieden: `surface` tauscht die Richtung zwischen den Modi (dunkel *dunkler* als `surface-inset`, hell heller), sodass dunkel der Hover einer nicht gewählten Kachel heller geworden wäre als die gewählte.

**„Meine Abstimmungen" steht als „Abstimmungen" wieder außerhalb des Menüs.** Der Plan hatte es hineingelegt und die Alternative als offene Entscheidung notiert; am gebauten Zustand fiel sie andersherum aus. Der Preis ist benannt und akzeptiert: auf 360 px weicht im Worker-Warnzustand die Wortmarke, weil dann vier Dinge um eine Zeile konkurrieren. Damit das kein Überlagern wird, trägt die linke Gruppe `min-w-0` und die Wortmarke `truncate` — ein Kind, das sich nicht verkleinern lässt, läuft sonst aus seiner Box und landet auf dem, was rechts steht.

**Ein Altfehler ist dabei aufgefallen und in der Primitive geheilt:** `align` ist ein Alt-HTML-Attribut, also hinterlässt `<app-popover align="end">` ein `align="end"` im DOM, aus dem das UA-Stylesheet `text-align: end` für den ganzen Teilbaum macht. Das galt für **jedes** Popover; die bestehenden Panels entkamen nur, weil ihre Zeilen durchweg ein explizites `text-left` tragen. `popover.ts` setzt jetzt `text-start`, damit keine künftige Fläche das wissen muss.

Zwei Nebenentscheidungen: Die URL wird beim Setzen des Claims von `-300x300` auf `-70x70` umgeschrieben (32 CSS px bei DPR 2), **bewacht** — greift das Muster nicht, geht die URL unverändert durch, und das ist die einzige Stelle im Code, die etwas über Twitchs URL-Form annimmt. Und `https://static-cdn.jtvnw.net` kommt in die `img-src`-Allowlist der CSP; ohne diesen Zusatz lädt kein Bild, unabhängig vom Claim. Die Listen sind bewusst schmal — das ist ein begründeter Zusatz, kein Aufweichen.

---

### 2026-08-08 — Die Farbwelt bekommt eine maschinenlesbare Zweitfassung, und die handgeschriebene bleibt die Instanz

**Betrifft:** `DESIGN.md` (neu, Repo-Root) · `.impeccable/design.json` (neu, gitignored) · `web/.claude/CLAUDE.md` · `CLAUDE.md`

**Der Anlass war ein Werkzeug, das dieses Projekt für visuell unbebaut hielt.** Der Kontextlader des Impeccable-Skills sucht eine `DESIGN.md` am Wurzelverzeichnis; er fand keine, meldete `designPath: null` und daraufhin `WORLD_DISCOVERY_REQUIRED` — die Aufforderung, vor jeder neuen Fläche erst eine visuelle Welt zu erfinden. Dass `PRODUCT.md` auf `docs/UI-Designsprache.md` verweist, hilft dem Resolver nicht: er liest den Dateinamen, nicht die Prosa. Praktische Folge war, dass jeder Skill-Befehl in diesem Repo ohne Kenntnis der eigenen Vorgaben startete — bei einem Regelwerk, das Slate verbietet, Hover-Richtungen festlegt und die Kartenklasse abgeschafft hat, ist das kein kosmetischer Mangel.

**`DESIGN.md` ist deshalb abgeleitet, nicht maßgeblich, und das steht im Dokument selbst.** Die normativen Farbwerte stehen weiterhin an genau einer Stelle, `web/src/styles.css`; `DESIGN.md` hält eine Momentaufnahme davon, damit Werkzeuge sie parsen können. Wer den Tokenblock ändert, erzeugt sie neu. Bei Widerspruch gewinnt `docs/UI-Designsprache.md` — so steht es jetzt auch in `web/.claude/CLAUDE.md`, das den UI-Auftrag trägt. Das ist der Preis dafür, ein zweites Dokument über dieselbe Sache zu führen, und er ist bewusst gezahlt: die Alternative wäre gewesen, die Designsprache in das Schema zu zwingen.

**Zusammenlegen wäre schiefgegangen, weil das Schema die Hälfte des Inhalts nicht trägt.** Das DESIGN.md-Format kennt acht Abschnitte in fester Reihenfolge — Overview, Colors, Typography, Layout, Elevation & Depth, Shapes, Components, Do's and Don'ts. Für die z-Leiter, den `h-14`-Höhenvertrag, den Stretched-Link-Kontrakt, die ARIA-Muster, die i18n-Pflichten und die Audit-Gates gibt es dort keinen Ort; sie hätten als schemafremde Abschnitte angehängt werden müssen, und jeder Verweis in `CLAUDE.md`, `PRODUCT.md`, `README.md` und den Review-Dokumenten wäre nachzuziehen gewesen. Die beiden Dateien überschneiden sich damit nicht: das eine Dokument führt die Farbwelt, das andere die Verträge.

**Drei Modellierungsentscheidungen, die das Schema erzwungen hat.** *Erstens:* Das Frontmatter kennt einen Wert je Farbrolle, dieses Projekt hat zwei vollständige Modi — dunkel ist deshalb kanonisch (es ist `:root`), hell steht als `lightCounterpart` im Sidecar und in der Prosa. *Zweitens:* Die semantischen Töne (`success`, `warning`, `danger`, `info`) binden an Tailwinds Paletten, die intern OKLCH führen; in Hex umgerechnet stünden Näherungswerte neben echten Werten, deshalb stehen sie **nicht** im Frontmatter, sondern nur als Rollenbeschreibung. *Drittens:* Die Komponenten-Untertoken des Schemas kennen acht Eigenschaften und darunter weder Schatten noch Fokusringe — die vollständigen Schnipsel liegen im Sidecar.

**Der eigentliche Gewinn sind nicht die Hex-Werte, sondern dreizehn benannte Regeln.** Guide-Regel, Dunkler-Regel, Rollen-Regel, Vier-Ebenen-Regel, Mono-für-Daten-Regel, Eine-Breite-Regel, Höhenvertrag-Regel, Ein-Schatten-Regel, Keine-Karte-Regel, Quadratische-Zelle-Regel, Bemerkenswert-wie-oft-Regel, Rahmen-schweigt-Regel, Auslösen-Vollziehen-Regel. Alle sind aus dem Bestand abgeleitet und nicht erfunden; sie fassen zusammen, was bisher über die Kommentare im Tokenblock und die Prosa der Designsprache verteilt lag. Ein Werkzeug, das nur `DESIGN.md` liest, kennt danach die Doktrin und nicht bloß die Palette.

**Das Sidecar `.impeccable/design.json` wird nicht versioniert** (`.gitignore:50` schließt das ganze Verzeichnis aus). Es trägt Schatten, Bewegungskurven, Breakpoints, die hellen Gegenwerte, Tonleitern und zehn Komponenten als eigenständige HTML/CSS-Schnipsel. Nach einem frischen Clone fehlt es und ist mit `$impeccable document` neu zu erzeugen. Das ist hingenommen, weil es Werkzeug-Cache ist und kein Vertrag — wer das ändern will, nimmt `.impeccable/design.json` von der Ignore-Regel aus.

### 2026-08-07 — Die zuletzt gestellte Frage gewinnt, nicht die zuerst beantwortete — und „alle Zeit" wird aufgelöst, bevor sie zur Anfrage wird

**Betrifft:** `web/src/app/core/http/latest-only.ts` (neu) · `web/src/app/core/http/latest-only.spec.ts` (neu) · `web/src/app/features/usage-stats/usage-stats-page.ts` · `web/src/app/shared/datetime/date-range-menu.ts` · `web/e2e/usage-range-resolution.e2e.spec.ts` (neu)

**Das Symptom war eine Kurve, die ihren eigenen Zeitraum verschwieg.** Im Sidecar der Nutzungsseite stand die Sparkline eines Emotes fast leer da, mit einer einzelnen Spitze am rechten Rand; der Drilldown-Dialog daneben zeigte für dasselbe Emote, zur selben Zeit, eine über die volle Breite laufende Kurve und darüber „26. Jul bis 7. Aug". Ein Neuladen der Seite brachte das Richtige zurück. Genau diese Sprunghaftigkeit ist der Fingerabdruck eines Wettlaufs und nicht einer Fehlrechnung — beim zweiten Aufruf sind die Antworten warm und kommen in anderer Reihenfolge zurück.

**Die Ursache liegt in einem Platzhalter, den zwei Effects nacheinander korrigieren.** `from()` startet als „365 Tage zurück", weil der Trackingbeginn des Channels erst mit dem Set-Status über das Netz eintrifft; ein zweiter Effect zieht den Wert danach auf diesen Beginn vor. Beide Male lief derselbe Ladepfad, und `loadChannelSeries` hatte weder `switchMap` noch Generationszähler: die erste, ein Jahr breite Anfrage blieb offen, und wer zuletzt antwortete, schrieb ins Signal. Verdeckt wurde das dadurch, dass der Sidecar seine x-Achse aus dem `from`/`to` **der Antwort** nimmt (`inspectedPoints`) — er kann also gar nicht bemerken, dass die Antwort nicht mehr zur gewählten Range gehört, und zeichnet 366 Stützstellen, von denen 13 Daten tragen. Der Drilldown war deshalb korrekt, weil er seine Daten selbst holt, erst beim Klick, wenn längst die aufgelöste Range gilt. Die Datumswerte selbst waren nie verschoben: die Offsets sind gegen dasselbe `from` gerechnet, das die Achse spannt.

**Erste Änderung: `latestOnly()` in `core/http/`.** Ein Operator mit Zustand, eine Instanz je Anzeigewert; jede Subscription zieht beim Abonnieren eine Generationsnummer, nur die höchste darf noch liefern. Bewusst kein `switchMap`: die Anfragen entstehen in Effects und Ereignishandlern, nicht in einem Strom, auf den sich umschalten ließe, und die `HttpClient`-Observables der `UsageStatService` sind mit `shareReplay({ refCount: false })` zwischengespeichert — ein Abbestellen würde die laufende Anfrage ohnehin nicht abbrechen. Verwerfen ist damit die ganze Aufgabe. Eine überholte Anfrage wird still beendet statt durchgereicht, auch im Fehlerfall: der Fehlschlag einer Anfrage, auf die niemand mehr wartet, darf keinen Fehlerzustand über gültige Daten legen. Angewandt auf **beide** Ladepfade der Seite — `loadTotals` trug denselben Wettlauf, dort nur unsichtbar, weil ein wenige Tage alter Channel über 13 und über 366 Tage dieselben Zahlen liefert.

**Zweite Änderung, und sie kehrt eine ausdrückliche frühere Entscheidung um: die Seite wartet jetzt.** Der Doc-Kommentar zu `allTimeStart` begründete bis hierhin das Gegenteil — der Boden sei „ausdrücklich nicht ein Warten auf `earliest`", damit der erste Raster-Aufbau nicht dahinter in der Schlange steht. Der Preis dieser Wahl war eine 366-Tage-Aggregation über den ganzen Satz **pro Seitenaufruf**, deren Antwort immer weggeworfen wurde, dazu der doppelte Verbrauch aus dem `ExternalApi`-Kontingent (40/min) und eben das offene Zeitfenster, in dem zwei Antworten um dasselbe Signal konkurrieren. Dagegen steht ein zusätzlicher serieller Rundlauf bis zum ersten Inhalt. Der Tausch ist bewusst so herum entschieden — er ist eine Zeile (`rangeResolved` in `load()`) und damit umkehrbar, falls sich die Verzögerung am echten Gerät als spürbar erweist.

**Warum das Gate Werte vergleicht und nicht fragt, ob der Status da ist.** Der erste Versuch prüfte genau das, und der E2E-Test hat ihn widerlegt: Effects laufen in ihrer Erzeugungsreihenfolge, der Ladepfad ist älter als die Korrektur, und im Durchlauf unmittelbar nach dem Set-Status ist der Status folglich schon bekannt, während `from()` noch der Platzhalter ist. Ein „ist er gelandet?" hätte genau diesen Zwischenzustand durchgewunken — die Anfrage wäre wieder zweimal rausgegangen, nur eine Stufe später. `rangeResolved` vergleicht deshalb `from()` mit `allTimeStart(trackedSinceDate())`; das ist unabhängig davon, welcher Effect zuerst läuft. Aufgelöst wird auch bei einem **fehlgeschlagenen** Set-Status, sonst bliebe die Seite für immer im Skelett stehen; ohne Trackingbeginn ist der Platzhalter die ehrliche Antwort. Gemerkt wird der Channel, nicht ein Flag, damit ein Wechsel zu einem anderen Channel die Range wieder als vorläufig gilt.

**Die Testaufteilung folgt der Regel-12-Grenze.** Das Verwerfen überholter Antworten ist pure Logik und liegt als Unit-Test bei `latestOnly`. Was dort nicht sichtbar ist — welche Anfragen eine Seite beim Erstaufruf überhaupt verlässt — steht als E2E-Test mit gemockten `/api/**`-Antworten: eine Series- und eine Totals-Anfrage, beide gegen den Trackingbeginn, plus der Fall des fehlgeschlagenen Set-Status als Wächter gegen genau den Deadlock, den das Warten einführen könnte. Isolierte Komponententests bleiben außen vor, wie in der Konvention vorgesehen.

### 2026-08-07 — Der Ziehbereich eines Sheets ist seine ganze Oberkante, und die Geste wird an ihrem Ende gemessen

**Betrifft:** `web/src/app/shared/ui/dialog-shell.ts` · `web/src/app/shared/ui/sheet-drag.ts` · `web/src/app/shared/ui/sheet-drag-policy.ts` · `web/src/app/shared/ui/sheet-drag.spec.ts` · `web/e2e/touch-mobile.e2e.spec.ts` · `docs/UI-Designsprache.md`

**Anlass war der erste Test mit einem echten Daumen** — der Eintrag darunter beschreibt die Umgebung, die das möglich gemacht hat. Der Griff war am Balken bemessen worden, nicht am Finger: 4 px Balken plus Polster, 24 px insgesamt. Das ist nach WCAG 2.5.8 ein zulässiges Ziel und trotzdem das Falsche, weil ein Ziehgriff kein Knopf ist, den man trifft, sondern eine Fläche, auf der man landet.

**Der Ziehbereich ist jetzt die ganze Oberkante:** die sticky Leiste auf `min-h-11` (44 px, das Komfortziel aus §10 der Designsprache) und darunter der Kopfblock — Thumbnail und Überschrift — als zweite Zone mit demselben `data-sheet-handle`. Das ist auch die Zusage, die ein natives Sheet gibt: ein Zug, der oben beginnt, meint das Sheet und nicht seinen Inhalt. Ausdrücklich **nicht** dazu gehört der Rest der Fläche. `SheetDrag` lässt dort weiterhin eine Geste zu, solange die Pane oben angeschlagen ist, aber das trägt nur, wenn es nichts zu scrollen gibt: sobald der Inhalt länger ist als das Sheet, nimmt der Browser eine Abwärtsbewegung als Scroll für sich und bricht den Pointer ab. Verlässlich ist allein, was `touch-none` trägt.

**Die 16 px zwischen den beiden Zonen gehören dem Kopfblock, nicht der Hülle** (`-mt-4 pt-4` schluckt das `gap-4` und gibt es als eigenes Padding zurück). Optisch ändert sich nichts; was sich ändert, ist die Zuständigkeit. Bei der Hülle waren diese Pixel weder Griff noch `touch-none` — ein unsichtbarer Streifen, der gegen das Scrollen des Browsers antreten muss, und zwar genau dort, wohin ein Daumen zielt: zwischen dem Balken, den er sieht, und der Überschrift, die er liest. Zwei Ziehflächen mit einem toten Streifen dazwischen sind schlechter als eine kleine, weil das Versagen wie Willkür aussieht. Der E2E-Test prüft deshalb nicht nur, dass es zwei Zonen gibt, sondern dass sie sich berühren.

**Auf einem feinen Zeiger löst sich der neue Kopf-Wrapper per `display: contents` wieder auf.** Der Desktop-Dialog ist damit exakt so gekastet wie vorher, und ein Dialog, der weder `[dialogTitle]` noch `[dialog-header]` rendert, sammelt keine Lücke ein — ein `flex`-Kind ohne Inhalt hätte trotzdem einen Abstand erzeugt.

**Zweiter Teil, und der eigentliche Fehler: die Geste wurde über ihren Verlauf gemessen statt an ihrem Ende.** Beide Zahlen waren davon betroffen. Die Geschwindigkeit war `distance / (pointerup − pointerdown)`, also über die ganze Geste gemittelt — jede Millisekunde, die der Finger vor dem Losziehen nur auflag, teilt sich in das Ergebnis hinein. Ein Wisch über 40 px in 70 ms ist 0,57 px/ms und liest sich mit einer halben Sekunde Zögern davor als 0,08, unter der Schwelle von 0,5. Gemessen wird jetzt über die letzten 100 ms (`VELOCITY_WINDOW_MS`), ein Fenster statt des letzten Ereignispaars, weil ein einzelner Frame ohne Bewegung sonst als Vollbremsung durchginge; die verstrichene Zeit läuft bis zum Loslassen, damit ein Finger, der anhält und dann abhebt, nicht die Geschwindigkeit behält, die er einmal hatte. Der Weg wiederum stammte aus dem letzten *gemeldeten* `pointermove` — Browser fassen die zusammen und verwerfen, was sie nicht rechtzeitig zustellen können, sodass die gemerkte Position umso weiter hinterherhinkt, je schneller gezogen wird. Er wird jetzt zur Loslass-Position gerechnet. Beides trifft dieselbe Sorte Geste: eine hastige ist eine kurze, sie hat nichts als diese beiden Zahlen, an denen sie beurteilt werden kann. **Die Schwellwerte selbst waren nie das Problem** — 0,5 px/ms steht unverändert; nur die Strecke ist am Gerät von 96 auf 72 px nachjustiert worden, weil das Sheet dem Finger bis dahin längst gefolgt ist und ein Zurückfedern dann wie eine Weigerung liest und nicht wie eine unfertige Geste.

**Ein methodischer Nachtrag, weil er die Beweislage dieses Eintrags betrifft.** Ein Teil der Rückmeldungen vom Gerät kam gegen ein altes Bundle zustande: NPMs „Cache Assets" hatte den `.js`-Dateien drei Stunden `max-age` mitgegeben, und diese Einträge liegen im Browser, wo das Abschalten der Proxy-Option sie nicht mehr erreicht (Fehlerbild und Abhilfe stehen in [Testumgebung-Mobile-2026-08-07.md](Testumgebung-Mobile-2026-08-07.md)). Die Befunde zur Geschwindigkeit sind davon unberührt — sie sind als Testfall reproduzierbar und stünden auch ohne Gerät. Die Begründungen zum toten Streifen und zur Strecke sind deshalb bewusst strukturell formuliert statt aus dem beobachteten Symptom heraus: warum diese Pixel niemandem gehören dürfen, gilt unabhängig davon, ob der Daumen an jenem Abend darüber gestolpert ist. Wer eine Verhaltensänderung am Gerät prüft, baut zuerst eine sichtbare Probe ein — eine Farbe, einen Rahmen — und misst erst danach das Verhalten.

### 2026-08-07 — Mobile-Tests laufen gegen die lokale Entwicklungsumgebung unter einem zweiten Namen, nicht gegen eine Staging-Stage

**Betrifft:** `web/angular.json` · `web/package.json` · `src/EmotePurge.Api/Properties/launchSettings.json` · `docs/Testumgebung-Mobile-2026-08-07.md` (neu) · `CLAUDE.md`

**Das Problem:** Mobile-Ansichten waren bis hierhin nur auf Produktion prüfbar. Der ganze Branch `feat/mobile-ansicht` — Sheet-Dialoge, Ziehgriff, der `pointer: coarse`-Vertrag des Eintrags darunter — ist an einem emulierten Zeiger in den Entwicklertools entwickelt worden, und genau diese Emulation hat den Capture-Fehler in `SheetDrag` erst erzeugt und dann verdeckt. Ein echtes Gerät fehlte im Kreislauf.

**Warum keine Staging-Stage.** Naheliegend wäre ein zweiter Stack aus GHCR-Images gewesen, auf dem VPS oder auf dem NAS. Beides ist verworfen: die CI veröffentlicht Images ausschließlich von `main` (`.github/workflows/publish.yml`), ein Branch wie `feat/mobile-ansicht` erzeugt also gar kein Image — Staging hätte entweder einen zweiten Veröffentlichungspfad gebraucht oder lokale Builds mit der Hand geschoben. Dazu kommt die Schleifenlänge: eine Ansicht auf dem Handy zurechtzurücken heißt, dieselbe Datei zwanzigmal zu speichern; jedes `docker compose up -d --build` dazwischen kostet Minuten. Und der vermutete Hauptgewinn einer Stage — „dort liegen meine Testdaten" — existiert nicht: `appsettings.json` zeigt auf `Host=localhost`, `docker-compose.yml` veröffentlicht Postgres auf `127.0.0.1:5432`. Der lokal gestartete Api und der Container-Api reden längst mit **derselben** Datenbank und demselben Volume.

**Gebaut wurde deshalb: derselbe Dev-Server, ein zweiter Hostname davor.** Das Handy ruft `https://dev.home.sensitron.me` auf, AdGuards bestehender Wildcard-Rewrite (`*.home.sensitron.me → 192.168.178.5`) zeigt auf den Nginx Proxy Manager, der mit dem vorhandenen Let's-Encrypt-Wildcard TLS terminiert und an `:4200` durchreicht. Kein neuer Dienst, kein DNS-Eintrag, kein geöffneter Port, nichts von außen erreichbar. Der Preis ist, dass es nur im heimischen WLAN funktioniert — bewusst akzeptiert, ein Tunnel oder Tailscale wäre eine dauerhafte Komponente für einen seltenen Fall.

**Warum `/api` weiterhin über den Angular-Dev-Proxy läuft und nicht über eine zweite NPM-Location.** Zwei Proxy-Hops sind unschöner als einer, aber die Alternative hätte Kestrel auf `0.0.0.0` gebunden, eine zweite Firewall-Regel gebraucht und eine Topologie geprüft, die weder der Entwicklungs- noch der Produktionsumgebung entspricht (in Produktion liefert die Api das Frontend selbst aus `wwwroot/` aus). Der vermutete Bruchpunkt der gewählten Fassung war `X-Forwarded-Proto`: `AuthEndpoints.cs` setzt das `Secure`-Flag des State-Cookies aus `Request.IsHttps`, das in dieser Kette allein aus dem weitergereichten Header stammt. **Gemessen am Tag der Einrichtung hält die Kette das aus** — die Login-Antwort über `https://dev.home.sensitron.me` trägt `Set-Cookie: ep_oauth_state=…; secure`, und ein Callback mit diesem Cookie kommt an der State-Prüfung vorbei (er scheitert erst am Token-Tausch). Beide Hops reichen den Header also durch. Der Ausweg (NPM splittet `/` und `/api`) ist in der Doku beschrieben, aber weder gebaut noch nötig.

**Was beim ersten Live-Test tatsächlich schiefging, gehört ins Log, weil das Symptom auf die falsche Maschine zeigt:** Der Login endete in `InvalidOAuthState` — die Meldung kam aber von der **Produktions**-Api. Die neue Redirect-URI war in der Twitch-App noch nicht gespeichert; Twitch beantwortet eine unbekannte `redirect_uri` nicht mit einer Fehlerseite, sondern leitet `?error=redirect_mismatch&state=…` an eine *registrierte* Adresse weiter, hier also nach `emotepurge.app`. Deren Callback sieht einen `state` ohne `code` und trifft denselben Zweig in Zeile 60. Wer den Fehlercode für sich nimmt, sucht ihn in der falschen Umgebung.

**Und der Grund dafür war ein Schiefstand, der älter ist als diese Umgebung: `dotnet run` authentifizierte sich gegen die Produktions-App.** Es gibt zwei Twitch-Apps — `EmotePurgeDev` und die zu `emotepurge.app` gehörende. `docker compose` nahm über `TWITCH_CLIENT_ID` aus der `.env` schon immer die Entwicklungs-App, die User-Secrets des Api-Projekts hielten dagegen die Produktions-Zugangsdaten; ein und derselbe Rechner sprach also je nach Startweg mit zwei verschiedenen Apps, ohne dass es irgendwo stand. Die neue Redirect-URI landete folgerichtig in der einen App, während der laufende Prozess die andere benutzte. **Konsequenz, gezogen am selben Tag:** die User-Secrets tragen jetzt ebenfalls `EmotePurgeDev`, und diese App führt alle drei Redirect-URLs (`localhost:5151`, `localhost:8080`, `dev.home.sensitron.me`). Lokal berührt damit nichts mehr die Produktions-App. Die Alternative — die LAN-Adresse einfach zusätzlich in der Produktions-App zu registrieren — wäre ein Klick gewesen und hätte den Schiefstand konserviert.

**Warum ein zusätzliches Profil statt geänderter Standardwerte.** `npm start`, `dotnet run` ohne Profil und `appsettings.Development.json` bleiben unangetastet; der Handy-Modus ist ein zweites Gleis (`--launch-profile lan`, `npm run start:lan`). Der Dev-Server bindet nur in dieser Configuration auf `0.0.0.0` — ein Standardwert hätte jeden gewöhnlichen `ng serve` ins LAN gestellt, ohne dass jemand danach gefragt hat. Die Configuration muss ihr `buildTarget` selbst mitbringen, weil `--configuration lan` das `defaultConfiguration: "development"` ersetzt und nicht ergänzt; `allowedHosts` ist Pflicht, weil der Vite-basierte Dev-Server fremde `Host`-Header abweist und NPM den angefragten Namen unverändert durchreicht. Twitch bekommt eine **zusätzliche** Redirect-URI in der bestehenden App statt einer zweiten App — eine zweite App wäre ein zweites Secret ohne Gegenwert, und die Cookies sind host-only, Produktions- und Testsitzung stören sich also ohnehin nicht.

### 2026-08-07 — Kein 7TV-Schreibzugriff ohne Maus: der Vertrag der Mobile-Ansicht, jetzt mit permanentem Testnetz

**Betrifft:** `web/playwright.config.ts` · `web/e2e/touch-mobile.e2e.spec.ts` (neu) · `web/e2e/audit/ui-audit.audit.ts` · `web/src/app/core/pointer/pointer-mode.service.ts` · `web/src/app/shared/ui/dialog-shell.ts` · `web/src/app/shared/ui/sheet-drag.ts` · `web/src/app/shared/ui/sheet-drag-policy.ts` · `web/src/app/shared/ui/button.ts` · `web/src/app/shared/ui/dialog.ts` (**unverändert** — der Eintrag erklärt gerade, warum) · `web/src/styles.css` · `web/src/app/shared/emotes/emote-sprite.ts` (neu) · `web/src/app/shared/emotes/emote-drilldown-dialog.ts` · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/src/app/features/voting/vote-session-detail-page.{ts,html}` · `web/src/app/features/voting/vote-session-list-page.html` · `web/src/app/features/overview/overview-page.html` · `web/public/i18n/{de,en}.json` · `docs/UI-Designsprache.md`

**Der Vertrag, den der ganze Branch `feat/mobile-ansicht` durchsetzt:** kein 7TV-Schreibzugriff ohne Maus. Darunter fällt Selektion im Atlas, 7TV-Mass-Delete, das Protokoll-Restore-Panel und — weil sie auf derselben Selektion aufsetzt — die Vote-Session-Anlage aus dem Dock. Ausdrücklich **nicht** betroffen: Lesen, der Drilldown-Dialog, Abstimmen, Export und die Slot-Budget-Leiste — alles, was kein 7TV-Schreib-Token braucht, bleibt auf dem Handy erreichbar. **Auch das Anlegen einer Vote-Session über das ganze Set bleibt auf touch** (Knopf auf der Voting-Listenseite): weggefallen ist allein die Anlage *aus dem Dock*, und die nur, weil sie die Selektion als Eingabe hat — sie ging mit dem Dock, nicht mit einem Token. Wer Spec §3.3 („Kuratieren ist Bildschirmarbeit") liest, könnte sonst den Unterschied für einen Fehler halten.

**Warum die Zeigerart und nicht die Bildschirmbreite das Gate ist:** Das Token für 7TV-Schreibzugriffe lässt sich ausschließlich aus der lokalen Speicheransicht der Entwicklertools auf 7tv.app kopieren — ein Vorgang, den ein Telefon nicht anbietet, eine Breite aber nichts darüber aussagt. Ein auf Handbreite verkleinertes Desktop-Fenster hat weiterhin Hover, den Group-Hover-Trigger der Zelle und präzise Klicks; nichts daran ist kaputt. `PointerModeService` fragt deshalb `(pointer: coarse)`, nicht `(any-pointer: coarse)` — ein Desktop mit angestecktem Touchscreen behält so alles, weil er trotzdem noch DevTools hat.

**Warum kein zweiter Overlay-Stack für die Sheet-Form:** Der Entwurf (Spec §5.1) sah vor, dass `openAppDialog()` bei coarse eine eigene Panel-Klasse wählt. Umgesetzt wurde es anders — die Sheet-Geometrie kommt vollständig aus `web/src/styles.css`, `dialog.ts` blieb unverändert. Zwei getrennte `@media (pointer: coarse)`-Regeln, nicht eine: die Andockung sitzt auf dem Wrapper, nicht auf der Pane (`.cdk-global-overlay-wrapper:has(> .app-dialog-panel) { align-items: flex-end !important; }`, `!important` Pflicht, weil CDKs `GlobalPositionStrategy` `align-items` als Inline-Style auf denselben Wrapper schreibt), die zweite bleibt auf der bestehenden `.cdk-overlay-pane.app-dialog-panel`-Klasse und ändert `max-width`/`max-height` (nicht `width`/`height` — die Basisregel setzt `width: 100%` bereits fest). Grund für die Media-Query statt einer gewählten Klasse: `openAppDialog` ist eine freie Funktion, aufgerufen aus Komponenten-Methoden ohne Injection-Context — dort ließe sich `PointerModeService` gar nicht injizieren, und die Alternative (die Zeigerart als Parameter durch alle neun `open…()`-Wrapper reichen) hätte neun Signaturen angefasst, um dieselbe Media-Query nachzubauen, die CSS schon kann. Nebeneffekt, nicht Nebensache: die Geometrie ist damit **live**, nicht beim Öffnen eingefroren — eine Drehung des Geräts mit offenem Dialog wechselt die Form mit.

**Warum das Pane der Scroll-Container ist, nicht der Shell-Body:** Zwischen der Pane-Höhe und dem Inhalt sitzen zwei Komponenten-Hosts (`app-<dialog>`, `app-dialog-shell`), beide `display: inline` per Default — eine Prozent-Höhenkette überlebt das nicht. Die Pane selbst zu scrollen ist eine Regel und fasst keine Komponente an; der Preis ist, dass Kopf und Aktionsreihe mitscrollen.

**Die Mindeststrecke von 24 px in `shouldDismiss`, die die Spec nicht vorsah.** Der Entwurf verlangte „Strecke ≥ 96 px **oder** Geschwindigkeit ≥ 0,5 px/ms" — wörtlich genommen schließt das schon ein vierpixliges Zucken beim Tippen, weil eine kurze Bewegung per Definition schnell ist (kurze Strecke, kurze Zeit, hoher Quotient). `web/src/app/shared/ui/sheet-drag-policy.ts` prüft deshalb zuerst `distancePx < SHEET_MIN_TRAVEL_PX` (24 px) und lässt beide Zweige erst danach zu — drei Zahlen, nicht zwei.

**Warum der Drilldown-Dialog bleibt, statt auf touch zu entfallen:** Auf dem Stimmzettel ist er die einzige Quelle für Kurve, Spitzensatz, Live-Tage und Erst-/Letztnutzung — die Voting-Seite ruft keinen Usage-Endpunkt auf, und `firstUsedDate` existiert ausschließlich in der Antwort von `/usage-stats/daily`. Ein Wegfall auf coarse hätte dem Stimmzettel dort die einzige Historie genommen.

**Kein Hinweistext, dass Löschen am Rechner passiert.** Der Dock, das Mass-Delete-Panel und das Restore-Panel verschwinden auf coarse einfach — visuell fehlt nichts, also gibt es auch nichts zu erklären. Ein Banner, das eine Abwesenheit ankündigt, wäre die einzige Stelle in der App, die von etwas redet, das gerade nicht da ist.

**Eine Ausnahme davon, und der Grund, warum es keine ist: der Kuratier-Link auf der Voting-Listenseite.** Unter „Neue Abstimmung erstellen" stand ein Link „Nur bestimmte Emotes zur Wahl stellen?" auf die Usage-Stats-Seite. Der Unterschied zu den drei Panels oben ist, dass hier nichts verschwindet, sondern etwas **stehen bleibt und ins Leere zeigt**: die Zielseite ist auf touch erreichbar und voll lesbar, nur das Dock, das aus einer Selektion einen Stimmzettel macht, ist dort `!isCoarse()`-gegatet. Der Link stellte damit eine Frage, die sein Ziel auf dem Handy nicht beantworten kann — kein toter Weg, ein gebrochenes Versprechen. Auf coarse tritt deshalb ein Satz an seine Stelle (`voting.list.wholeSetHintDesktopOnly`), rein per CSS-Variantenpaar (`pointer-coarse:hidden` am Link, `hidden pointer-coarse:inline` am Satz) statt über `PointerModeService` — der Dienst dokumentiert diese Arbeitsteilung selbst, für rein visuelles Verstecken ist die Tailwind-Variante das Mittel. **Das Erstellen selbst bleibt auf touch** (s. den Vertragsabsatz oben): eine Vote-Session schreibt in unsere eigene Datenbank, nicht nach 7TV, und Beenden wie Löschen bestehender Sessions sind aus demselben Grund geblieben. Sie zu sperren hätte den Vertrag über sein eigenes Argument hinaus gedehnt.

**Was bis zu diesem Commit fehlte:** jede coarse-Entscheidung der vorherigen elf Tasks war nur durch Wegwerf-Specs verifiziert, die ihre Autoren danach gelöscht haben — `web/playwright.config.ts` kannte bis hierhin ein einziges Desktop-Projekt ohne Touch, sodass nichts in der committeten Suite den coarse-Pfad je wieder erreichen konnte. Ein zweites Playwright-Projekt `mobile-chrome` (Gerätebeschreibung `devices['Pixel 5']`, `testMatch` nur auf `touch-mobile.e2e.spec.ts`) ist das erste dauerhafte Netz darunter — als eigenes Projekt statt als Pointer-Override in einem einzelnen Test, weil `pointer: coarse` aus `hasTouch`/`isMobile` des Gerätedeskriptors folgt und beides Context-Optionen sind, die ein einzelner Test nicht nachträglich setzen kann.

**Eine Kopplung ohne Test auf beiden Seiten, jetzt abgedeckt:** `DialogShell` rendert den Ziehgriff mit dem Attribut `data-sheet-handle`; `SheetDrag` sucht exakt danach, um zu entscheiden, ob eine Geste beginnen darf. Die Spec der Direktive baut sich ihre eigene Stand-in-Fixture und hätte eine Umbenennung auf einer der beiden Seiten stillschweigend überlebt. Die Sheet-**Erscheinung** (Andockung, volle Breite) ist davon unabhängig — sie kommt allein aus den beiden Media-Queries oben, nicht aus dem Griff, und hätte eine solche Umbenennung nicht angezeigt. `touch-mobile.e2e.spec.ts` deckt davon **eine** Hälfte ab: `expect(page.locator('.app-dialog-panel [data-sheet-handle]')).toHaveCount(1)` bindet den echten `DialogShell` an diesen Namen — eine Umbenennung dort fällt auf. Die andere Hälfte, das Literal in `SheetDrag.onPointerDown`, hängt weiterhin allein an der Spec der Direktive; eine Umbenennung nur dort liefe still durch. Die Ziehgeste selbst bleibt bewusst ungeprüft — die Entscheidung sitzt gut abgedeckt in `shouldDismiss` (`sheet-drag-policy.spec.ts`).

**`SheetDrag` merkt sich ein Drücken, handelt aber erst ab 4 px Bewegung — und das ist keine Politur.** Die erste Fassung nahm beim `pointerdown` sofort `setPointerCapture`, sobald die Direktive an war und die Pane oben stand (der Zustand jedes Dialogs, der ganz auf den Schirm passt) — auch bei einem Druck mitten auf einen Knopf. Pointer-Capture verlegt den folgenden `click` auf das Capture-Element, also auf die Hülle, unter der jeder Knopf des Dialogs liegt. Bei Zeigertyp `touch` läuft der Handler des Knopfes trotzdem, bei Zeigertyp `mouse` **nie** (in echtem Chrome nachgemessen, nicht hergeleitet). Ein echtes Telefon war damit in Ordnung; jede Konfiguration, in der `(pointer: coarse)` greift, während die Eingabe eine Maus ist — allen voran DevTools → Rendering → *Emulate CSS media feature `pointer: coarse`*, die der Doku-Kommentar von `PointerModeService` selbst als Weg in diesen Modus nennt — hatte in **jedem** Dialog jeden Knopf tot: Abbrechen, Bestätigen, Schließen. Genau so wird diese Ansicht von Hand geprüft. `pointerdown` merkt sich deshalb nur noch Startpunkt und Zulässigkeit; `setPointerCapture` und `transition: none` fallen erst beim ersten `pointermove` über `DRAG_START_THRESHOLD_PX`. **Ein bloßes Drücken bleibt vollständig unbeobachtbar** — kein Capture, kein Inline-Style, kein Aufräumen. Nebeneffekt derselben Umstellung: `pointermove`/`up`/`cancel` sind keine Dauer-Host-Bindings mehr (die in dieser zoneless-App pro Frame den Scheduler weckten, auch bei abgeschalteter Direktive), sondern hängen für die Dauer einer Geste an `window` — `window` und nicht am Host, weil der Finger vor dem Capture die Hülle verlassen kann und die Ereignisse nach dem Capture ohnehin hier durchlaufen. Der Regressionsschutz liegt in `touch-mobile.e2e.spec.ts` („a control inside the sheet still works — tapped and clicked"): die zweite Hälfte des Falls klickt mit **Maus**-Zeiger im coarse-Kontext und schlug gegen die alte Fassung fehl.

**Audit-Fall `overview-narrow-rows` ergänzt**, um die zweizeilige Zeilenform der Übersicht unterhalb `sm` festzuhalten — insbesondere den Satz „Noch nicht getrackt …", der als einziger Zweig der Zeile lang genug ist, um bei 360 px umzubrechen, und im Deutschen rund 26 % länger ist als im Englischen. Der Sheet-Zustand des Dialogs bleibt dem Playwright-Touch-Projekt vorbehalten — der Audit-Harness setzt keine Zeigerart, dort erscheint der Dialog weiterhin zentriert.

### 2026-08-07 — Der Bootstrap wartet auf die Übersetzungsdatei: `markForCheck` allein rendert in einer zoneless-App nichts nach

**Betrifft:** `web/src/app/app.config.ts` · `web/e2e/i18n-boot.e2e.spec.ts` (neu) · `web/e2e/usage-atlas.e2e.spec.ts` (Helper)

**Symptom:** der CI-Lauf zu `b484769` scheiterte an genau einem e2e-Fall (`channel-activity`, „a 7TV editor sees neither the tab nor the page"), dreimal in Folge inklusive beider Retries. Lokal war derselbe Test **isoliert grün** und fiel erst um, wenn die ganze Datei parallel lief.

**Ursache, gemessen statt vermutet.** Der Playwright-Snapshot des Fehlerzeitpunkts zeigte den gesuchten Link vorhanden, mit korrekter URL — und **ohne zugänglichen Namen**, genau wie den Up-Link darüber. Eine Sonde, die `/i18n/de.json` künstlich um 1,5 s verzögert, hat den Mechanismus festgenagelt: `TranslocoPipe.updateValue()` meldet die eingetroffene Übersetzung per `ChangeDetectorRef.markForCheck()`. Das markiert die View als schmutzig, **stößt in dieser zoneless-App aber keinen Change-Detection-Lauf an** (kein `zone.js` in den Polyfills). Vier Sekunden nach dem Laden stand das Label immer noch leer; nach einem beliebigen Klick war es sofort da. Unauffällig blieb alles, was **nach** dem Laden erzeugt wird — jedes `@if`, das auf eine aufgelöste Berechtigung umspringt —, weshalb der Schaden als „*einige* Labels fehlen" auftrat und nicht als „alle".

**Das war kein Testproblem.** Ein Nutzer mit kaltem Cache oder langsamer Leitung bekam einen Tab und einen Up-Link ohne zugänglichen Namen, dauerhaft bis zur nächsten Interaktion — beides ein Vertrag in `UI-Designsprache.md` §8.1/§8.6. Der Test hat es nur zuerst gesehen, weil ein CI-Runner unter sechs parallelen Browsern langsamer ausliefert als ein Entwicklungsrechner.

**Entscheidung:** ein zweiter `provideAppInitializer` blockiert den Bootstrap auf `TranslocoService.load(activeLang)`. Die App rendert nicht mehr, bevor sie sprechen kann. Das ist die ehrliche Stelle für den Fix — untranslatiert gibt es nichts Sinnvolles zu zeigen, und der Preis ist **eine** Same-Origin-JSON vor dem ersten Paint. Nebeneffekt: das Aufblitzen leerer Labels ist weg. Ein Fehlschlag der Anfrage wird gefangen (`.catch`) — ein unbeschriftetes UI ist schlecht, eine weiße Seite schlimmer.

**Verworfen:** den Test toleranter machen (behebt das Symptom, lässt den Fehler in der App), oder überall auf die `*transloco`-Strukturdirektive umstellen (dieselbe Wartezeit, verteilt über jedes Template).

**Ein zweiter, latenter Fehler kam dabei hoch.** Nach dem Fix fiel `usage-atlas` „the usage menu narrows the sheet…" unter Last um — nicht durch den Fix verursacht, sondern durch dessen verschobenes Timing freigelegt: `page.getByRole('status')` trifft **zwei** Elemente, die Zählzeile und das Skeleton, dem §6.1 ebenfalls ein `role="status"` gibt. `openAtlas()` wartete nur auf die Überschrift, die außerhalb des Lade-Zweigs steht und deshalb nichts über den Bogen aussagt. Der Helper wartet jetzt zusätzlich darauf, dass das Lade-`status` verschwunden ist. **Wer in diesem Projekt auf `role="status"` zielt, muss wissen, dass Skeletons denselben Rollennamen tragen.**

### 2026-08-07 — Die Designdokumentation führt nur noch den geltenden Stand; `Konzept-Light-Mode.md` entfällt

**Betrifft:** `docs/UI-Designsprache.md` · `docs/Konzept-Light-Mode.md` (**gelöscht**) · `web/src/styles.css` (zwei Kommentar-Verweise) · `web/.claude/CLAUDE.md` · `PRODUCT.md`

**Auftrag des Nutzers:** „alle Design Dokumente überarbeiten, sodass der aktuelle Stand korrekt dokumentiert ist. Alle Verweise auf das alte Design können raus."

**Die Regel, die das Dokument sich damit gibt**, steht jetzt in seinem Kopf: `UI-Designsprache.md` beschreibt **ausschließlich** den geltenden Stand — keine Vorgeschichte, keine Zwischenstufe, kein „bis dahin galt". Das ist die saubere Anwendung der ohnehin bestehenden Arbeitsteilung: dieses Dokument ist das **Wie**, dieses Log das **Warum**. Eine Regel, die ihre eigene Ablösungsgeschichte mitträgt, wird beim Lesen langsamer und beim Pflegen unzuverlässig — jede Änderung müsste zwei Zeitschichten korrekt halten.

**Was inhaltlich falsch war und mit korrigiert wurde** (nicht bloß umformuliert): der Fokusring war als „lila" beschrieben und ist die Leitfarbe (`--ep-accent`); `.app-page-glow` stand als Token in der Tabelle und existiert nicht mehr; der Input-Rand war als `slate-500` geführt statt als `border-border-field`; die Elevationsaussage rechnete mit `slate-50`. Solche Sätze sind schlimmer als eine Lücke — sie werden geglaubt.

**Neu dokumentiert, weil vorhanden und ungeführt:** §2.0a (Theme-Umschaltung: drei Zustände, `data-theme` auf `<html>`, `theme-init.js` vor dem ersten Paint, `localStorage`-Schlüssel, zwei `theme-color`-Metas) und §2.5 (das Sprite-Blatt: Pareto-Bänder statt fester Schwellen, Wert- statt Rangschnitt, Füllbalken gegen die Bandspitze, Sidecar ab `lg` neben dem bleibenden Drilldown-Dialog, `.app-dock` nur bei bestehender Auswahl, Tastaturgleichwertigkeit). Beides war gebaut, geprüft und stand in keinem Dokument.

**`docs/Konzept-Light-Mode.md` ist gelöscht.** Es trug im Kopf „Status: Entwurf zur Freigabe · Umsetzung: noch keine" und beschrieb den Weg von einer reinen Dunkel-App mit Slate-/Purple-Palette zu einem hellen Modus: Inventur von 414 Paletten-Utilities, fünf Migrationswellen, Aufwandsschätzung, neun offene Fragen an den Nutzer. Jede Zahl darin beschreibt einen Zustand, den es seit dem Redesign nicht mehr gibt, und §5 nannte Kontrastwerte, die schon vor dem 2026-08-07 nicht mehr stimmten. Sein noch gültiger Inhalt ist an seinen richtigen Ort gewandert (Umschaltmechanik → §2.0a, Emote-Leinwand → §2.4, Audit-Matrix → §12). Es bleibt über `git log -- docs/Konzept-Light-Mode.md` erreichbar; die beiden Verweise aus `styles.css` sind aufgelöst.

**Kein Kontrastnachweis-Dokument als Ersatz.** Das wäre dieselbe Falle wie eine zweite Wertetabelle in Markdown: Es driftet ab dem ersten nachgezogenen Token. Der gerechnete Nachweis gehört in die Commit-Message der Änderung, die ihn nötig macht; das jeweils **engste** Paar steht in §10 der Designsprache und wird dort mitgezogen.

### 2026-08-07 — Das helle Blatt ist nicht mehr reinweiß, und die ganze Rampe geht mit

**Betrifft:** `web/src/styles.css` (Tokenblock `:root[data-theme='light']`) · `docs/UI-Designsprache.md` (Kontrastzeile der Checkliste)

**Vom Nutzer nach dem Prod-Blick gemeldet:** „Der helle Modus ist sehr grell" — beobachtet auf einem OLED bei voller Helligkeit. Der Befund trifft nicht die Farbwelt, sondern zwei Tokens: `--ep-surface` und `--ep-field` standen auf `#fff`, und Flächen sind seit Zug 3 der größte Anteil jedes Bildschirms. Der Grund war mit `#f2f3f5` längst gedämpft, die Flächen darüber nicht.

**Entscheidung:** das Blatt auf `#fafbfc` senken — und die Nachbarn im selben Schritt mit. Nur die Spitze zu senken hätte den Abstand zwischen Blatt und Grund von 13 auf 8 Stufen gedrückt und damit genau die Tiefenwirkung gekostet, die im Hellen ohnehin die schwächere ist (Blatt gegen Grund sind 1,10:1, deshalb trägt das Overlay einen echten Schatten). Also: `page` `#f2f3f5` → `#eef0f2`, `surface`/`field` `#fff` → `#fafbfc`, `surface-inset` `#eaecef` → `#e5e8eb`, `surface-inset-hover` `#dfe2e6` → `#dadde1`.

**Die beiden Ränder mussten mit, und das ist der nicht offensichtliche Teil:** an seinem alten Wert wäre `--ep-border` (`#dde0e4`) *heller* geworden als das neue `surface-inset-hover` (`#dadde1`) — ein überfahrenes eingelassenes Feld hätte seine Kontur verloren. Deshalb `--ep-border` → `#d7dade` und `--ep-border-strong` → `#bec3ca`. `--ep-border-field` bleibt, es ist das 3:1-Token und hat mit der Fläche nichts zu tun.

**`--ep-on-accent` bleibt reines Weiß.** Das ist Schrift auf gefülltem Teal, keine Fläche — die Regel lautet „Flächen blenden", nicht „Weiß ist verboten".

**Die Tinte hat sich nicht bewegt**, der Spielraum war da (gerechnet, nicht geschätzt): `fg-muted` 5,91 → 5,71:1 auf dem Blatt und 5,00 → **4,81:1** auf der eingelassenen Fläche, `accent` 6,05 → 5,84:1 bzw. 5,11 → 4,92:1; die 3:1-Tore behalten Reserve (`border-field` auf dem Feld 4,58:1, `warning-dot` auf der eingelassenen Fläche 4,08:1). Der volle helle Audit-Durchlauf ist grün, axe in allen 82 Zuständen. **4,81:1 ist der engste Wert des Satzes** und steht deshalb ab jetzt in der Checkliste von `UI-Designsprache.md` — er reißt zuerst, wenn eine Fläche noch einmal verschoben wird.

**Nebenfund, der älter ist als diese Änderung:** die Kontrastzahlen in der Checkliste (7,0:1 dunkel / 7,6:1 hell) stammten aus der Slate-Palette *vor* dem Redesign und waren schon vorher falsch — tatsächlich 5,72 bzw. 5,91:1. Sie nannten außerdem „die Karte", die es seit Zug 2 nicht mehr gibt. Korrigiert. `docs/Konzept-Light-Mode.md` §5 trägt denselben Fehler im Großen; das Dokument ist ein datierter Konzept-Schnappschuss und bleibt in diesem Commit unangetastet.

### 2026-08-06 — Der Kopfbereich schweigt, solange der Worker gesund ist — und der Übersichts-Banner entfällt dafür

**Betrifft:** `web/src/app/features/shell/app-shell.ts` · `web/src/app/features/overview/{overview-page.ts,overview-page.html}` · `web/public/i18n/{de,en}.json` · `docs/UI-Designsprache.md` (§4.3, §4.4) · `web/e2e/audit/ui-audit.audit.ts`

**Vom Nutzer beim Durchsehen gemeldet:** „das ‚Worker verbunden' passt nicht mehr so richtig zur Designsprache. Entweder streichen wir das restlos, oder du machst einen Vorschlag." Der Befund stimmt, und die Begründung stand schon im eigenen Haus: Zug 3 hat die Prüffrage **„bemerkenswert wie oft?"** und die Regel **„gesund ist still"** (`HealthMarker`: `ok`/`idle` → Punkt, nur `warning`/`danger` → Pille) für jede Fläche der App durchgesetzt — nur nicht für das eine Element, das auf **jedem** Bildschirm in **jeder** Sitzung steht und praktisch immer dasselbe sagt.

**Entscheidung:** nicht streichen, sondern die Regel eine Stufe strenger anwenden, als `HealthMarker` sie selbst zieht. Der Kopfbereich zeigt bei `connected` **nichts** — kein Punkt, kein Text — und bei `stale` eine Warn-Pille. Für eine Seite ist Punkt-plus-Wort die stille Form; für einen Rahmen, der auf jedem Bildschirm mitläuft, ist auch das noch zu viel. `unknown` schweigt mit: das ist der Zustand vor der ersten Antwort des 30-Sekunden-Polls, und eine Warnung dort blitzt bei jedem Kaltstart auf.

**Der Text ist mitgewandert.** „Worker getrennt" war Fachjargon für einen Zustand, in dem der Leser wissen will, was *für ihn* nicht stimmt. Jetzt: „Chat wird nicht gezählt" / „Chat is not being counted". Das ist genau die Aussage, die `WorkerHealthStatus.Derive` trägt — `stale` fasst `disconnected` und „verbunden, aber seit 15 min kein Frame" zusammen, und beides heißt: die Zahlen wachsen gerade nicht.

**Folge, die erst das gerenderte Bild gezeigt hat:** die Übersicht sagte dieselbe Sache direkt darunter noch einmal, als `NoticeBanner`. Dessen Kommentar begründete ihn wörtlich damit, dass „der Header-Punkt allein ein 10-px-Signal ist, das niemand bemerkt" — eine Prämisse, die diese Änderung entfernt. Der Banner ist deshalb weg: die Pille ist lauter als der Punkt je war und steht auf **jeder** Seite statt nur auf der Eingangsseite, also dort, wo die Zahlen stehen, um die es geht. Zwei Aussagen derselben Tatsache auf einem Bildschirm sind genau das, was der Rest des Redesigns abgeräumt hat. Preis: der zweite Satz des Banners („erholt sich in der Regel von selbst") hat kein Zuhause mehr — bewusst in Kauf genommen, eine Warn-Pille mit einer Tatsachenbehauptung ist kein Alarm, der beruhigt werden müsste.

**Geprüft wurde das an der engsten Stelle**, nicht am Schreibtisch: bei 360 px teilt sich die Pille die Kopfleiste mit Wortmarke und Menüknopf, und nichts sonst in der App stellt ein drittes Element in diese Zeile. Dafür gibt es jetzt den Audit-Fall `overview-worker-stale` — der einzige Zustand, in dem der Kopf überhaupt etwas über den Worker sagt, hatte bis dahin kein einziges Bild.

### 2026-08-06 — Der Atlas verliert die Void-Platte; der Stimmzettel behält sie

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.html` · `web/src/styles.css` (`.app-sprite-cell-void`) · `docs/UI-Designsprache.md` (§2.4) · `web/e2e/audit/ui-audit.audit.ts`

**Vom Nutzer gemeldet:** „die Emotes bei ‚nie benutzt' haben immer noch einen anderen Background als die anderen — brauchen wir das überhaupt?" Nein. Auf dem Atlas stehen diese Emotes geschlossen unter einer Überschrift, die „nie benutzt" sagt, jede Zelle druckt eine `0`, und der Füllbalken fehlt ihnen. Die andere Platte war die vierte Aussage über denselben Sachverhalt — und weil sie in diesem Band ausnahmslos **jede** Zelle betrifft, unterschied sie dort nichts. Sie kostete nur die Gleichmäßigkeit des Bogens: ein ganzes Band las sich als eine andere *Art* Fläche statt als dieselbe Fläche mit einer Null darauf.

**Auf dem Stimmzettel bleibt sie**, und zwar aus dem Grund, für den eine eigene Platte da ist: dort markiert `.app-sprite-cell-void` ein **archiviertes** Emote, gemischt unter lebende, ohne Überschrift, die es sagen würde.

**Bemerkenswert am Fund:** §2.4 beschrieb diese Trennung bereits — beim Streichen des Alpha-Karos am selben Tag war notiert worden, die Klasse bleibe „wegen des Stimmzettels, nicht wegen des Atlas". Die Regel stand also da; der Atlas hielt sich nicht daran. Beim Redesign die Designdoku **gegen den Code** prüfen, nicht nur fortschreiben.

**Neuer Audit-Fall `usage-stats-dead-band`:** `usageEmotes()` erzeugt nie eine Null, deshalb enthielt kein einziger Screenshot je das Band, für das die ganze Seite existiert. Jetzt schon — und er ist zugleich der Beleg, dass ein Band identischer Zellen ohne eigene Platte weiterhin als „nie benutzt" liest.

### 2026-08-06 — Ein Batch-Endpunkt für die Tagesreihen des ganzen Sets, damit der Sidecar eine Kurve zeigen kann

**Betrifft:** `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs` · `src/EmotePurge.Core/Services/IUsageStatQueryService.cs` · `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs` · `tests/EmotePurge.Api.Tests/ChannelUsageSeriesWireFormatTests.cs` (neu) · `web/src/app/core/usage-stats/{usage-stat.model.ts,usage-stat.service.ts}` · `web/src/app/shared/emotes/usage-series.ts` · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}`

**Vom Nutzer vorgeschlagen:** „wie wär's, wenn wir im Sidecar noch den Nutzungsgraph anzeigen?" Gute Idee — der Sidecar hatte Platz und trug nur Zahlen, und die Kurve beantwortet die eine Frage, die eine Zahl nicht kann: 4.000 Verwendungen über drei Monate und 4.000 an einem Abend sind dieselbe Zahl und entgegengesetzte Urteile.

**Das Problem war der Datenweg, nicht die Anzeige.** Der Sidecar hängt am Zeiger; die Atlas-Zeile kennt aber nur `totalUseCount`. Tagesdaten gab es ausschließlich über `GET /usage-stats/daily` — **ein Aufruf pro Emote**, auf der Gruppen-Policy `ExternalApi` (40/min), und teuer ist dort nicht die Abfrage, sondern der `UsageStatsAccessAuthorizationFilter` davor mit zwei ungecachten 7TV-GraphQL-Aufrufen für jeden Aufrufer außer Admin/Broadcaster/Mod — also ausgerechnet für 7TV-Editoren. Damit wäre Mausbewegung zu Requests geworden: wer den „nie benutzt"-Band durchgeht und je 1,5 s hinsieht, produziert 40 Aufrufe pro Minute, exakt die Decke, geteilt mit dem Rest der Seite. Einen Tag nach dem `/permissions`-Eintrag oben wäre das dieselbe Lektion in die Gegenrichtung gewesen.

**Entscheidung:** `GET /usage-stats/series` liefert die Tagesreihen **des ganzen Sets** in einer Antwort, ein Aufruf pro (Channel, Zeitraum). Die Decke bleibt bei 40; die Nachfrage verschwindet.

**Die Antwort ist kodiert, nicht bloß serialisiert** — drei Entscheidungen, alle aus einem Grund: nichts zwischen Api und Browser komprimiert JSON. Die Api registriert keine `ResponseCompression`, und der nginx-Block auf dem VPS setzt keine `gzip`-Direktive (`docs/VPS-Reverse-Proxy.md` enthält keine); nginx' Voreinstellung komprimiert nur `text/html`. **Das ist zugleich der offene Handgriff daran:** ein `gzip_types application/json` auf dem VPS würde jeden Endpunkt betreffen, nicht nur diesen, und die Kodierung hier weitgehend überflüssig machen — sie ist trotzdem richtig, weil sie nicht von einer Proxy-Einstellung abhängt, die dieses Repo nicht kontrolliert. (1) **Tages-Offsets statt ISO-Daten**: `[3,42]` gegen `{"date":"2026-07-04","useCount":42}` ist rund ein Fünftel; bei ~7.500 Emote-Tagen im 90-Tage-Fenster eines großen Sets sind das ~260 KB gegen ~55 KB. (2) **Paare statt paralleler Arrays**, damit die beiden Hälften nicht auseinanderlaufen können. (3) **Emotes ohne Nutzung fehlen ganz** — bei 900 Emoten ist das tote Band der größte Teil der Antwort, und „fehlt" heißt eine Ebene tiefer, bei den ausgelassenen Tagen, ohnehin schon „keine Nutzung". `liveDays` liegt einmal oben statt pro Emote wiederholt, weil es eine Eigenschaft des Channels ist.

**Diese Kodierung ist ein sprachübergreifender Vertrag** und deshalb festgenagelt: `ChannelUsageSeriesWireFormatTests` prüft die erzeugte JSON-Zeichenkette. Ein gut gemeintes Aufräumen, das die Paare in ein Record verwandelt, würde in C# weiter kompilieren, jeden anderen Test grün lassen und den Sidecar flache Linien zeichnen lassen.

**Der Drilldown-Dialog bleibt** — die zweite Frage des Nutzers. Er ist auf dem Stimmzettel der einzige Weg zur Kurve (dort gibt es keinen Sidecar), unter `lg` ebenfalls, und mit Finger oder Tastatur sowieso. Er hört nur auf, der *einzige* Weg zu sein, und trägt weiterhin, was der Sidecar nicht hat: y-Achse, Spitzensatz, Live-Tag-Legende, erste Verwendung.

**Bewusst nicht bei Live-Events nachgeladen.** Die Totals daneben aktualisieren sich weiter; die Kurve nicht. Ein 30-Sekunden-Flush verschiebt den letzten Tag um ein paar Zähler — bei Sidecar-Breite unsichtbar — während die Antwort das ganze Set umfasst. Der Nachladevorgang würde also am meisten kosten und am wenigsten zeigen. Die Kurve gilt damit für den Zeitraum wie zuletzt gewählt, genau wie der Dialog es mit seinem eigenen Cache immer schon gehandhabt hat.

### 2026-08-06 — `/permissions` wird pro Channel 30 s zwischengespeichert, statt das Rate-Limit ein drittes Mal anzuheben

**Betrifft:** `web/src/app/core/channels/channel.service.ts` · `web/src/app/core/auth/auth.service.ts` · `web/src/app/core/channels/channel.service.spec.ts`

**Symptom:** normales Durchklicken der Oberfläche antwortet mit 429. Das ist die Policy `ExternalApi` in `Program.cs` — Fixed Window, 40 Requests/Minute, partitioniert nach Twitch-User-Id, `QueueLimit 0`, kein `Retry-After`, kein Body. Am 2026-07-30 stand dasselbe Symptom schon einmal hier im Log und wurde durch eine Verdopplung von 20 auf 40 beantwortet. Dass es wiederkommt, ist der Beleg, dass die Decke nicht das Problem war.

**Ursache:** ein einziges Öffnen eines Channel-Workspace kostet 6 Permits, und **drei davon sind derselbe Aufruf** — `GET /permissions`, angefragt von `usageStatsAccessGuard`, von `ChannelWorkspaceLayout` und von der `permissionsResource` der Seite. Alle drei sind für sich legitim: jeder braucht die Antwort für eine eigene Entscheidung, und keiner darf sich darauf verlassen, dass ein anderer sie schon geholt hat. Auf der Leitung waren es trotzdem drei identische Requests, deren jeder serverseitig eine echte Twitch-/7TV-Rollenprüfung auslöst. Übersicht → Workspace → zurück → Workspace sind 14 Permits; nach etwa fünf Wechseln ist das Minutenbudget leer.

**Entscheidung:** Der Cache liegt im `ChannelService`, nicht an den Aufrufstellen — die bleiben unverändert und dürfen weiter naiv fragen. `shareReplay({ bufferSize: 1, refCount: false })` deckt den Gleichzeitigkeitsfall (die drei Leser einer Navigation teilen sich eine Antwort, noch bevor die TTL überhaupt greift), die 30-Sekunden-TTL den Hin-und-Zurück-Fall. Der Schlüssel ist der **normalisierte** Name (Regel 9), sonst öffnet `HandOfBlood` einen zweiten Eintrag neben `handofblood`.

**Drei Details, die nicht optional sind.** (1) **Fehler werden nicht gecacht**: `shareReplay` spielt einen terminalen Fehler jedem späteren Abonnenten wieder vor und abonniert die Quelle nie neu — ein einzelnes 500 würde den Channel für 30 s aussperren. Ein `catchError` **vor** dem `shareReplay` löscht den Eintrag, bevor der Fehler durchgelassen wird. (2) `join`/`leave`/`purge` verwerfen den Eintrag des Channels, den sie ändern. (3) `AuthService.resetClientSession()` leert den ganzen Cache — heute kann er einen Nutzerwechsel ohnehin nicht überleben, weil jedes Login ein voller Seitenwechsel ist (`window.location.href`), aber das ist eine Eigenschaft des OAuth-Redirects, nicht des Caches.

**Bewusst nicht mitgecacht: `listMine()`.** Die Übersicht lädt das auf Live-Events neu; ein Cache würde genau die Aktualisierung mit dem Stand beantworten, den sie ersetzen soll. Das ist die Grenze des Verfahrens — zwischengespeichert wird nur, was kein Live-Event auffrischt.

**Das Limit selbst bleibt bei 40.** Es schützt nicht sich selbst, sondern die app-weite Helix-Quota; eine Anhebung wäre erst nach dieser Reduktion ungefährlich und ist jetzt nicht mehr nötig.

### 2026-08-06 — Zug 3, Skeletons: der Platzhalter zeichnet den Umriss des Inhalts, nicht irgendeinen

**Betrifft:** `web/src/app/shared/ui/skeleton-rows.ts` · `web/src/app/shared/ui/skeleton-sections.ts` (neu) · `web/src/app/features/admin/{admin-monitoring-page,admin-channel-detail-page}.ts` · `docs/UI-Designsprache.md` (§6.1, §11)

**Vom Nutzer beim Durchklicken gemeldet:** „Die Skeletons zeigen aktuell noch Karten an, ist das so gewollt?" — nein. Beim Umbau der Listen auf geriffelte Zeilen wurden die beiden handgerollten Skeletons (Atlas, Stimmzettel) mitgezogen, das **geteilte** `SkeletonRows` nicht. Es war zuletzt die einzige verbliebene `.app-card`-Nutzung der ganzen App: ein Kartenstapel mit `gap-2`, der auf sechs Seiten von einer randlosen `divide-y`-Liste abgelöst wurde. Sichtbar war das nicht nur als verschwindender Rahmen, sondern als **seitlich springender Text** — Skeleton `px-4` innerhalb einer Box gegen `-mx-3 … px-3` in der echten Zeile.

**Die Regel gab es schon, nur zu eng.** §6.1 forderte „Zellen in der Form des echten Inhalts" ausdrücklich für Grids. Genau dort wurde sie eingehalten; verletzt wurde sie da, wo sie nicht hingeschrieben war. Sie gilt jetzt für jedes Skeleton, inklusive Rahmen, Abstand und horizontaler Kante, und mit der Begründung dazu: **ein Skeleton, dessen Umriss vom Inhalt abweicht, lässt das Eintreffen des Inhalts wie einen Layoutfehler aussehen** — es kostet damit genau die Ruhe, für die es da ist.

**Zweiter Befund derselben Klasse:** `admin-monitoring-page` und `admin-channel-detail-page` bekamen ein Listen-Skeleton, obwohl ihr geladener Zustand gar keine Liste ist, sondern rule-getrennte Abschnitte mit zweispaltigen Definitionslisten. Das war schon vor dem Redesign eine Näherung. Neu: `SkeletonSections`, das die echte Sektion nachbildet — bis dahin, dass das `dt`/`dd`-Paar auf Telefonen auf einer Zeile liegt und ab `sm` umbricht.

**Verifiziert wurde das eigens**, weil der reguläre Audit-Harness Ladezustände nie zu sehen bekommt — er wartet auf Inhalt. Eine Wegwerf-Probe hat die Antworten der drei Seiten um sechs Sekunden verzögert und die Skeletons fotografiert. Ohne das wäre die Behauptung „passt jetzt" ungeprüft geblieben: keine Suite dieses Projekts sieht ein Skeleton.

### 2026-08-06 — Zug 3, Abschluss: die Kartenfläche ist abgeschafft, nicht nur ungenutzt

**Betrifft:** `web/src/styles.css` (`.app-card`, `.app-card-interactive`, `--ep-shadow-card*` je Modus entfernt) · `web/src/app/shared/ui/notice-banner.ts` (Kommentar) · `docs/UI-Designsprache.md` (§2.1–2.3)

**Mit dem Skeleton fiel die letzte Nutzung.** `.app-card` und `.app-card-interactive` waren danach toter Code, ebenso die beiden `--ep-shadow-card*`-Tokens, deren einzige Leser sie waren — alle vier sind aus `styles.css` entfernt. Das ist der eigentliche Abschluss der Flächen-Arbeit aus Zug 2/3: die Karte war nicht „gerade nirgends im Einsatz", sondern durch zwei Formen ersetzt, die ihre Aufgabe besser erledigen.

**`.app-card-link` bleibt** — der Name stammt aus der Kartenzeit, benennt aber das Stretched-Link-Pseudoelement und nicht die Fläche. Umbenennen hieße vier Aufrufstellen plus Doku anzufassen, um einen Kommentar zu ersparen; der Kommentar ist billiger.

**Die Designsprache lief der Umsetzung hinterher, und zwar bemerkt.** §2.1–2.3 führten die Karte bis heute als lebendes Primitive und behaupteten ausdrücklich, sie bleibe „für Flächen, deren Nachbarschaft tatsächlich andersartig ist (Formular-Boxen, Panels im Workspace)". Das stimmte schon beim Schreiben nicht mehr: genau diese Flächen waren zu dem Zeitpunkt längst getönte Blöcke (`rounded-md bg-surface-inset`). Der Satz stand da, weil er beim Umbau als Rückzugsoption formuliert und nie gegen den Code geprüft wurde. **Ein Dokument, das ein abgeschafftes Primitive als verfügbar führt, ist schlimmer als eines, das schweigt** — es lädt zum Wiedereinbau ein. §2.1 heißt jetzt „Flächen: geriffelte Zeile und randloser Abschnitt" und nennt die Prüffrage („grenzt das gegen etwas Andersartiges?") vor den Rezepten.

### 2026-08-06 — Zug 3, Breite: zwei Breiten gebaut, am selben Tag wieder verworfen

**Betrifft:** `web/src/app/features/shell/app-shell.ts` · `web/src/app/app.routes.ts` · `web/src/app/features/usage-stats/usage-stats-page.html` (Dock) · `docs/UI-Designsprache.md` (§8.4a)

**Die Analyse war richtig, die Lösung nicht.** „Shell breiter" wäre der falsche Schnitt gewesen: die App lief überall auf `max-w-5xl`, und für Listen, Admin-Tabellen, Monitoring-Grids und jeden Fließtext ist das genau richtig — auf 1536 px ist eine Metazeile keine Zeile mehr. Genau zwei Seiten wollen die Breite wirklich, und dort ist sie keine Dekoration, sondern Reihen: Atlas und Stimmzettel gewinnen pro 500 px eine weitere Emote-Spalte. Also entschied kurzzeitig die Route statt der Shell — `data: { wideLayout: true }` an den zwei Blatt-Routen, zwei Werte (64 rem / 96 rem), Header-Zeile und `<main>` teilen sich die Klasse.

**Im Gebrauch überwog etwas anderes, das die Analyse nicht gewichtet hatte: der Wechsel.** Wer zwischen einer Blattseite und einer Listenseite navigiert, sieht den Rahmen springen — Logo, Tab-Leiste und Inhaltskante rücken um 256 px. Das ist bei jeder Navigation da, der Breitengewinn nur auf zwei Seiten. Der Nutzer hat es genau so gemeldet („dann nehme ich lieber die reduzierte Breite in Kauf"), und das ist die richtige Abwägung: eine ruhige Hülle schlägt zwei optimale Spaltenbreiten. Zurück auf **eine** Breite, app-weit.

**Was bleibt, ist die Bedingung für einen zweiten Anlauf:** Wenn die Blätter ihre Breite je zurückbekommen, dann ohne den Rahmen zu bewegen — die Blattfläche muss *innerhalb* der konstanten Spalte ausbrechen, statt die Spalte selbst zu verbreitern. Ein erneuter Vorschlag über Routen-Daten ist derselbe Vorschlag.

**Ein Befund aus dem Versuch ist unabhängig davon wertvoll.** `AppShell` wird *während* der Navigation konstruiert, die sie aktiviert: die Kind-Routen existieren dann schon, ihre `snapshot`s nicht. `route.snapshot.data` im Feld-Initialisierer warf und riss die ganze Shell mit — sichtbar als **komplett leere Seite bei grünem `ng build`, 443 grünen Unit- und 68 grünen E2E-Tests**. Gefunden hat es allein der UI-Audit-Harness, an einem 5-KB-einfarbigen PNG statt der erwarteten 41 KB. Das ist das Argument dafür, layoutwirksame Änderungen dort gegenzuprüfen (§12), und nicht nur die Suiten laufen zu lassen.

### 2026-08-06 — Zug 3, Dialoge: die Hülle ist eine Primitive, das Öffnen gehört dem Dialog

**Betrifft:** `web/src/app/shared/ui/dialog.ts`, `dialog-shell.ts`, `name-preview-list.ts` (alle neu) · `confirm-dialog.ts` · `typed-confirm-dialog.ts` · `web/src/app/shared/seven-tv/{delete,restore}-confirm-dialog.ts`, `seven-tv-token-prompt-dialog.ts`, `mass-delete-panel.ts`, `restore-panel.ts` · `web/src/app/shared/export/export-dialog.ts` · `web/src/app/shared/emotes/emote-drilldown-dialog.ts` · `web/src/app/features/usage-stats/create-vote-session-dialog.ts` · sechs Aufrufer-Seiten · `web/e2e/audit/ui-audit.audit.ts` · `web/public/i18n/{de,en}.json` · `docs/UI-Designsprache.md` (§7)

**Es gab keine geteilte Dialog-Hülle.** Geteilt war nur das CDK-Chrome in `styles.css`; alles darin stand pro Datei — die Kette `rounded-lg bg-surface p-6 shadow-overlay` neunmal wortgleich, dazu fünf Überschriften-Varianten, drei Überschriften-Abstände und drei Breiten, von denen zwei die Pane-Kappung entweder wiederholten oder gegen sie arbeiteten. `<app-dialog-shell>` nimmt das; die Breite gehört seither dem Pane, die Abstände der Flex-Spalte statt einem `mb-*` je Kind.

**Das Öffnen war der eigentliche Vertragsbruch.** Der Dreizeiler `backdropClass`/`panelClass`/`ariaLabelledBy` stand an zwölf Aufrufstellen von Hand — und **fünf davon hatten die Benennung vergessen**, darunter beide Dialoge ohne Überschrift, die damit gar keinen Accessible Name hatten. Ein Aufrufer kann nicht wissen, welche `id` die Überschrift eines fremden Dialogs trägt; das ist Wissen des Dialogs. Also exportiert jeder Dialog jetzt seine eigene `open…()`-Funktion über `openAppDialog`, und die `id` ist ein einziges festes `app-dialog-title` — kollisionsfrei, weil nie zwei Dialoge gleichzeitig offen sind.

**Benannt wird auf zwei Wegen, weil es zwei Fälle gibt.** Sichtbare Überschrift → `ariaLabelledBy`. Die Confirm-Dialoge dagegen bekommen bewusst *keine* Überschrift: jede ihrer Meldungen beginnt schon mit der Aktion (»Channel #x wirklich verlassen?«), eine Überschrift darüber wäre derselbe Satz zweimal. Sie tragen ein `ariaLabel` mit der kurzen Aktionsphrase.

**Vier Warnfarben im Lösch-Dialog hießen: keine ist wichtig.** Rote Shared-Set-Box, gelbe »konnte nicht prüfen«-Box, `warning-fg`-Unwiderruflichkeit und ein grauer Nachsatz standen übereinander, bevor der Nutzer den Bestätigen-Knopf erreichte. Die beiden unteren gelten für *jeden* Durchlauf — Konstanten, die nicht laut sein dürfen; laut bleibt nur, was diesen Durchlauf von den anderen unterscheidet. Dieselbe Regel wie bei den Admin-Badges, eine Ebene tiefer. Die beiden handgebauten Kästen sind jetzt `<app-notice-banner>`, die zwei Dauerhinweise still.

**Der Lade-Text ist geblieben, gegen §6.1 — mit Grund.** Der Bestätigen-Knopf ist während des Shared-Set-Checks gesperrt, und ein gesperrter Knopf ohne Begründung ist keine wahrnehmbare Erklärung (WCAG). §6.1 verbietet Lade-*Text für ladende Inhalte*; das hier ist der Grund einer Sperre und steht deshalb — wie der Nachtipp-Hinweis im `TypedConfirmDialog` — als `mr-auto`-Zeile in der Aktionszeile, per `aria-describedby` verbunden.

**Der Export hatte drei Fußzeilen-Knöpfe für zwei Dinge.** »CSV« und »JSON« sind gleichrangige Optionen einer *Wahl*; nebeneinander als Buttons zwangen sie eine der beiden willkürlich in die leisere Variante. Die Wahl ist jetzt eine Radiogruppe im Body — parallel zur Bereichswahl direkt darüber — und die Fußzeile nennt eine Aktion.

**Nachzug im Lauf-Panel und der Token-Eingabe.** Beide hängen am selben Flow und hatten ihn nie mitgemacht: fünf Buttons an der `appButton`-Direktive vorbei (darunter ein handgebautes `danger-quiet` und eine Kette mit eigenem Kommentar zur 24-px-Grenze, die die Primitive längst einhält), zwei weitere Ad-hoc-Warnkästen statt `NoticeBanner`, und in `seven-tv-token-input.ts` ein `Validators.required` **ohne jede Fehleranzeige** — §5.3 führt genau diese Datei namentlich als die zu vermeidende Ausnahme. Der Absenden-Versuch setzt jetzt einen Fehler, der beim Tippen wieder verschwindet; die Control-eigenen `touched`/`dirty` wären zu früh dran, weil sie das Feld schon markieren, bevor der Nutzer den Knopf überhaupt gedrückt hat.

**Nebenbefund im Audit-Harness:** die beiden Drilldown-Fälle liefen seit dem Sprite-Umbau ins Leere. `[aria-label*="Emote1PogU"]` traf seither auch die Atlas-Kachel, `.first()` klickte sie an und wartete danach vergeblich auf den Dialog. Handle ist jetzt `:not([aria-pressed])` — der Sidecar-Trigger ist der einzige Treffer ohne Pressed-Zustand.

### 2026-08-06 — Zug 3, Workspace-Rahmen: der Tab ist eine Primitive, und zwei Warnflächen sind eine zu viel

**Betrifft:** `web/src/app/shared/ui/tab-link.ts` (neu) · `web/src/app/features/admin/admin-layout.ts` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `docs/UI-Designsprache.md` (§8.1)

**Die Tab-Klassenkette stand siebenmal von Hand da** — 4× im Admin-Layout, 3× im Workspace — mitsamt der Notiz in §8.1, die Vorkommen bei Änderungen synchron zu halten. Die Notiz sagte die Drift nicht nur voraus, sie war bereits selbst falsch (sie zählte 2 statt 3 im Workspace). Ein Vertrag, den die Designsprache seitenübergreifend nennt, gehört nicht in sieben kopierte String-Literale: `<app-tab-link>` nimmt `link` und `label`, und `ariaCurrentWhenActive="page"` ist damit nicht mehr vergessbar. `display: contents` auf dem Host, weil der Anker selbst das Flex-Kind sein muss — sonst zentriert er in einer eigenen Box, statt die `h-10` der Leiste zu tragen, die der `top`-Offset jeder Filter-Toolbar ist (§8.5).

**Die aufgeklappten Duplikat-Namen waren eine zweite bernsteinfarbene Fläche unter der ersten.** Das Banner nennt den Befund, die Fläche darunter ist sein *Beleg* — als zweite Warnfläche gerendert argumentierte der Beleg so laut wie der Befund. Das ist die „bemerkenswert wie oft?"-Regel eine Ebene über den Badges: eine Warnung, die weiter über sich selbst warnt, hört auf, eine zu sein. Der Beleg ist jetzt neutral (`border-border`, `bg-surface-inset`); das Banner behält seinen Ton.

**Die Aktivitätsseite selbst brauchte nichts.** Sie benutzt bereits `SegmentedControl`, `EmptyState` und das bedingte Zurücksetzen, und ihre Zeilen kommen aus `audit-log-list`, das mit dem Admin-Zug schon auf Linienzeilen umgestellt wurde.

### 2026-08-06 — „Nur ungenutzte (0×)" war nie ein eigener Filter, sondern min = 0 / max = 0

**Betrifft:** `web/src/app/shared/emotes/usage-range-menu.ts` (neu) · `web/src/app/shared/emotes/emote-usage-filter.{ts,spec.ts}` (`setRange` ersetzt `toggleUnused`) · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/src/app/features/voting/vote-session-detail-page.{ts,html}` · `web/e2e/usage-atlas.e2e.spec.ts` · `web/public/i18n/{de,en}.json` · `docs/UI-Designsprache.md` (§4.1)

```ts
isUnusedActive = computed(() => this.minCount() === 0 && this.maxCount() === 0);
```

**Der Schalter *war* die beiden Felder daneben.** Er stand direkt neben `Min` und `Max` und überschrieb sie still. Beide Richtungen dieser Kopplung waren korrekt und für niemanden vorhersagbar: tippte man von Hand 0 in beide Felder, leuchtete der Schalter auf; drückte man den Schalter und tippte dann in `Min`, ging er wieder aus. Die Werkzeugleiste zeigte damit **drei Filter in fünf Bedienelementen** — Nutzungsbereich, Name, Schonfrist.

Der Nutzungsbereich ist jetzt **ein** Control nach dem Muster des `DateRangeMenu`, das eine Zeile darüber schon genau das tut: Trigger nennt die aktuelle Einstellung, Panel enthält die benannten Bereiche (alle · nie benutzt · eigener Bereich) und tauscht seinen Inhalt gegen die Felder, sobald der eigene Bereich gewählt ist. Die Kopplung hört damit auf, ein Geheimnis zu sein — „nie benutzt" ist schlicht einer der Bereiche — und die zwei kryptischen Felder sind nicht mehr der erste Eindruck der Leiste. Auf der Nutzungsseite bleiben drei Dauer-Controls statt fünf; auf dem Stimmzettel, wo dieselben Filter hängen, zwei statt vier.

**`toggleUnused()` ist durch `setRange(min, max)` ersetzt.** Toggeln ist das falsche Verb, sobald die Bereiche als Auswahl auftreten: das bereits gewählte Radio erneut zu treffen darf es nicht abwählen. Der Spec-Fall dazu ist entsprechend umgeschrieben und prüft die Idempotenz jetzt ausdrücklich.

**Ein bedingtes „Filter zurücksetzen" kommt dazu**, wie es das Audit-Log schon hat. `reset()` gab es, angeboten wurde es aber nur aus dem Leerzustand — also nur, wenn der Filter das Blatt *komplett* geleert hatte. Hatte er es bloß verengt, musste man jedes Control einzeln zurückdrehen und sich merken, welche man angefasst hatte.

**Absichtlich nicht gestrichen:** „Neue ausblenden" (14 Tage Schonfrist) trägt als einziger Filter ein fachliches Urteil, das aus den Bändern nicht ableitbar ist, und die Namenssuche ist die einzige Möglichkeit, ein bestimmtes Emote unter 900 zu finden. Auch „nie benutzt" bleibt erreichbar: der Verdacht, es dupliziere nur das Nie-benutzt-Band, hält nicht — das Band zeigt dieselbe Menge, der Filter *verkürzt die Seite*, und bei 900 Emotes ist das der Unterschied zwischen an 770 Sprites vorbeiscrollen und nicht.

### 2026-08-06 — Zug 3, Werkzeugleiste: vier Schalter in drei Darstellungen, und `appButton` kannte keinen gedrückten Zustand

**Betrifft:** `web/src/app/shared/ui/button.ts` (`buttonPressed`) · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/public/i18n/{de,en}.json` (4 Schlüssel) · `docs/UI-Designsprache.md` (§4.1)

**Die Werkzeugleiste der Nutzungsseite war ein Lauf aus neun grauen Kästen, in dem vier verschiedene Arten von Ding gleich aussahen:** Zeitraum (Popover), Ordnung (2 Knöpfe), Filter (3 Eingaben + 2 Schalter) und ganz rechts die **Ergebniszahl** — als wäre sie ein weiteres Bedienelement. Sie ist jetzt in zwei Zeilen mit klarer Zuständigkeit geteilt: *was betrachte ich und in welcher Reihenfolge* (Zeitraum, Sortierung), darunter *was nehme ich heraus* (Filter), endend in dem, was übrig bleibt. Die Zahl behält ihren Platz am Ende der Filterzeile — sie ist deren Ergebnis und steht bei ihrer Ursache.

**Der Defekt darunter: vier `aria-pressed`-Controls in einer Reihe, drei Darstellungen.** Die beiden Sortier-Knöpfe zeigten ihren gedrückten Zustand ausschließlich über einen an das Label gehängten Pfeil — der Knopf selbst sah gedrückt aus wie ungedrückt, die Information erreichte also Screenreader und sonst niemanden. Die beiden Bool-Filter bauten sich ihren Aktiv-Zustand von Hand zusammen, inklusive einer Größenkette, die `SIZE_CLASSES` doppelte. **Ursache: `appButton` modellierte Schalter nicht**, also improvisierte jeder Aufrufer. Neu ist `[buttonPressed]`, mit derselben Füllung wie das gewählte Segment des `SegmentedControl` — „dieses hier ist an" soll gleich aussehen, ob allein oder als eines von mehreren.

**Die Sortierung war außerdem funktional versteckt.** Ein Klick auf den *bereits aktiven* Sortierknopf drehte die Richtung um. Das war die einzige Möglichkeit, aufsteigend zu sortieren, und nichts auf dem Bildschirm sagte es; der Pfeil erschien erst danach, als einziger Beleg für einen Zustand, den die Schaltfläche nie zeigte. Schlüssel und Richtung sind zwei Fragen und heute zwei `<app-segmented-control>` — dasselbe Primitiv, das eine Seite weiter im Audit-Log schon eins-aus-N beantwortet.

Die Auswahl-Semantik bleibt exakt erhalten: ein Wechsel des Schlüssels ordnet das ganze Gitter um und leert deshalb die Auswahl (Shift-Anker zeigten sonst auf eine Reihenfolge, die der Nutzer nie gesehen hat), ein Richtungswechsel dreht eine Liste um, die er gerade ansieht, und behält sie.

**Was bewusst NICHT passiert ist:** kein Control ist weggefallen. „Nur ungenutzte (0x)" beantwortet, was das Nie-benutzt-Band ohnehin als beschriftete Fläche zeigt, und Min/Max sind die kryptischsten Bedienelemente der Seite — das Ausdünnen ist besprochen und vertagt, nicht übersehen.

### 2026-08-06 — Das Alpha-Karo unter den Emotes fällt weg

**Betrifft:** `web/src/styles.css` (`.app-sprite-cell`, `.app-sprite-cell-void`, `--ep-emote-canvas-alt` entfernt) · `web/src/index.html` (Direction Contract) · `web/src/app/features/landing/set-shape.ts` (Kommentar)

Der Eintrag vom 2026-08-04 führte das Alpha-Karo als „Material aus der Sache selbst" ein: die Notation, die jedes Werkzeug für transparente Grafik benutzt, und der Grund, warum die Zelle ohne Rahmen als Zelle liest. Das war für die *Kachel* richtig gedacht und für die *Seite* falsch.

**Nutzerbefund, der es umgeworfen hat:** die Frage „wieso haben manche Emotes ein Schachbrett und andere nicht?" — gestellt, weil das Karo als Inkonsistenz gelesen wird, nicht als Aussage. Es *war* eine Aussage (Karo = mindestens einmal benutzt, Void-Platte = nie), aber eine, die die Bandüberschrift und der fehlende Füllbalken bereits machen. Die Entscheidung des Nutzers: **kein Mehrwert, die Kategorien unterteilen ja schon.**

Zwei Dinge stützen das über den Geschmack hinaus. Erstens beantwortet das Alpha-Karo eine Frage, die diese Seite nie stellt — *welche Pixel sind transparent* ist die Frage eines Bildeditors, nicht die eines Nutzungsberichts. Zweitens lag das zweite Feld absichtlich nur ~3 % neben dem Canvas (`#1f232a`/`#262b33`), damit 900 Kacheln keine Textur werden; bei dieser Delta hat es die eigentliche Aufgabe eines Alpha-Karos ohnehin nie erfüllt. Was blieb, war ein Muster, das das Nie-benutzt-Band wie eine **andere Art Ding** aussehen ließ statt wie dieselbe Sache mit einer Null darauf.

**Die Void-Platte bleibt**, und der Grund ist nicht der Atlas: auf dem Stimmzettel markiert dieselbe Klasse ein **archiviertes** Emote, und dort gibt es keine Bandüberschrift, die das ein zweites Mal sagt.

**Nicht per Screenshot verifizierbar** — der Audit-Harness stubt das 7TV-CDN mit einem 1×1-Pixel, das die Zelle vollflächig füllt, das Karo war dort nie sichtbar. Geprüft ist der Umbau über Build, beide Lint-Gates, 443 Unit- und 66 E2E-Tests; das Bild gehört an echte 7TV-Artwork.

### 2026-08-06 — Zug 3, Admin-Bereich: Die gesunde Seite trägt keine einzige farbige Pille

**Betrifft:** `web/src/app/shared/ui/health-marker.ts` (neu) · `web/src/app/features/admin/{admin-monitoring-page.ts,admin-roster-card.ts,admin-channels-page.ts,admin-users-page.ts,admin-channel-detail-page.ts}` · `web/src/app/shared/audit/audit-log-list.ts` · `docs/UI-Designsprache.md` (§2.1, §4.2, §4.3)

**Der Befund, der den Zug getragen hat, ist das Gegenstück zur Übersichtszeile — eine Ebene höher.** Das Monitoring badgete *jedes* Subsystem: grünes „Verbunden" auf Twitch-IRC, auf dem 7TV-Socket, auf dem Roster, auf dem Batch-Flush, dazu der SSE-Stream-Status im Kopf. Eine kerngesunde Seite trug damit fünf farbige Rechtecke — und ein kaputtes Subsystem musste sich gegen vier gesunde durchsetzen, um bemerkt zu werden. Der einzige Grund, diese Seite zu öffnen, ist aber genau das eine zu finden, das *nicht* normal ist.

**Deshalb ist normal jetzt still.** Der neue `<app-health-marker>` entscheidet die Darstellung anhand des Tons: `ok` und `idle` werden zu Punkt plus Wort, nur `warning` und `danger` bekommen die Pille. Eine gesunde Monitoring-Seite trägt damit keine einzige farbige Pille, und das erste auffällige Subsystem ist ohne Zutun das lauteste Element auf ihr. Das ist derselbe Satz wie in §4.3, nur andersherum gelesen: **die Aussage entsteht durch Abwesenheit, und Abwesenheit funktioniert nur, wenn nichts anderes sie ausgibt.** Im Audit-Harness gegengeprüft — das `degraded`-Szenario zeigt vier Pillen (Veraltet · Lückenhaft · Getrennt · Fehler in Folge: 5), das gesunde keine.

Zwei Nebenentscheidungen daraus: `connecting` des SSE-Streams ist `idle`, **nicht** `warning` — jeder Seitenaufruf läuft durch diesen Zustand, und eine gelbe Pille, die beim Laden aufblitzt, bringt einem Admin bei, gelbe Pillen zu übersehen. Und das Label geht als `label`-Input hinein statt als projizierter Inhalt: ein `<ng-content>` in zwei Zweigen eines Control-Flow-Blocks wird von Angular nur einmal befüllt, der Text wäre beim Wechsel zwischen Punkt und Pille still verschwunden.

**Karten fallen auch auf den Diagnose-Seiten weg**, aus demselben Grund wie bei den Listen und mit demselben Test: eine Karte grenzt gegen etwas **Andersartiges** ab. Auf `admin-monitoring-page.ts` und `admin-channel-detail-page.ts` war jede Sektion eine Karte, also grenzte keine gegen irgendetwas ab — der Rand war reine Wiederholung und konkurrierte mit dem Statusmarker daneben. Jetzt Instrumenten-Stapel: `border-t border-border pt-4` je Sektion, Überschrift links, Marker rechts.

**Die Admin-Listen sind auf die Zeilenform gezogen** (`admin-channels`, `admin-users`, `audit-log-list` — Letzteres wirkt zugleich im Channel-Aktivitätsfeed). Der Bot-Zustand und „Twitch verbunden" werden zu `<app-state-dot>`, offline zu stillem Text, LIVE behält die Pille — dieselbe Dreiteilung wie in der Übersicht.

**Der am 2026-08-05 offen gelassene Punkt ist damit entschieden, und andersherum als vermutet.** Der Eintrag darüber hielt fest, Purge und Session-Revoke wögen schwerer als eine gelöschte Vote-Session und blieben deshalb auf `danger`. Im gerenderten Bild war die Users-Seite mit **25 Zeilen** die schlimmste Farbleiter im ganzen Projekt — schlimmer als die zwanzig, an denen die Stufe überhaupt entstanden ist. **Schwere rechtfertigt keine Wiederholung**; was die Purge tatsächlich absichert, ist die typisierte Namensbestätigung dahinter, und die ist unverändert. Beide Auslöser stehen jetzt auf `danger-quiet`.

**Das Join-Formular der Channel-Seite bleibt bewusst über der Liste**, obwohl das Erstellen-Formular der Voting-Liste einen Tag zuvor darunter gewandert ist. Die Begründung dort war, dass ein sechs Felder tiefes Formular die Antwort „was läuft gerade?" vom ersten Bildschirm schiebt; dieses hier ist ein einzelnes Eingabefeld in einer Zeile. Die Regel mechanisch anzuwenden hätte einen Admin zwanzig Zeilen scrollen lassen und eine Zeile Platz eingebracht.

### 2026-08-06 — Zug 3, Listenflächen: Zeilen statt Karten, und die Prüffrage „bemerkenswert wie oft?"

**Betrifft:** `web/src/app/features/overview/overview-page.{ts,html}` · `web/src/app/features/voting/vote-session-list-page.{ts,html}` · `web/src/app/features/my-votings/my-votings-page.ts` · `web/src/app/shared/ui/{state-dot.ts (neu),empty-state.ts,button.ts}` · `web/src/app/shared/voting/vote-audience-badge.ts` · neun Aufrufstellen von `<app-empty-state>` · `docs/UI-Designsprache.md` (§2.1, §4.1, §4.2, §4.3, §6.2)

**Eine Karte grenzt gegen etwas Andersartiges ab — in einer Liste ist jede Zeile dasselbe.** Die drei Listenseiten stapelten `.app-card`-Kacheln; was der Rand dort leistete, war acht Rechtecke zu zeichnen, wo eine Linie „Liste" deutlicher sagt. Zeilenlisten laufen jetzt über `-mx-3 divide-y divide-border border-y border-border`, klickbare Zeilen über `hover:bg-surface-inset` statt `.app-card-interactive`. Der negative Rand lässt den Hover-Wisch über den Text hinausatmen, während die Inhalte auf der linken Kante der Seite stehen bleiben. `.app-card` bleibt unverändert für abgegrenzte Flächen gegen andersartige Nachbarn.

**Die eigentliche Entdeckung war die Übersichtszeile.** Eine getrackte Zeile konnte fünf Pillen gleichzeitig tragen — LIVE, Broadcaster, Moderator, 7TV-Editor, Bot aktiv — und hat damit drei völlig verschiedene Arten von Information zu einer Textur eingeebnet: **wer du hier bist** (eine Tatsache über dich, auf den meisten Konten in jeder Zeile dasselbe Wort), **ob gerade gestreamt wird**, und **ob überhaupt etwas gemessen wird**. Nur das Letzte ist der Zustand *der Zeile*, und es ist das, was entscheidet, ob dieser Channel Daten produziert.

Daraus folgt die Regel, die jetzt in §4.3 steht: **eine Pille markiert eine bemerkenswerte Eigenschaft, und die Prüffrage lautet „bemerkenswert wie oft?"** Was in jeder Zeile steht, markiert nichts mehr, sondern ist eine Farbleiter. Rollen → stiller Text. Offline → stiller Text (LIVE behält die Pille, weil es das Einzige ist, was *gerade jetzt* gilt; die Dreiwertigkeit inklusive des stillen `unknown` bleibt unangetastet). Bot-Zustand → neuer `<app-state-dot>`.

**`state-dot` trennt Zustand von Eigenschaft**: Punkt plus Wort, `tone="on"|"off"`, benutzt von allen drei Seiten (Bot misst · Session läuft). Die `-dot`-Tokens existieren genau dafür — eine kleine bedeutungstragende Grafik, die 3:1 schuldet statt 4,5:1, weil das Wort daneben die Bedeutung ohnehin allein trägt.

**Die Zielgruppen-Pille ist in zwei Schritten ganz verschwunden**, und der zweite kam aus dem gerenderten Bild. Erst verlor der offene Fall seine graue Pille (eine Pille für den Default). Dann zeigte die echte Liste, dass die Hälfte der Sessions eingeschränkt ist — zehn blaue Rechtecke in einer Spalte sind keine Ausnahme, sondern eine Leiter, und „bemerkenswert" kann nicht zehnmal gelten. „Nur Mods/Streamer" gegen „Alle Zuschauer" sagt die Sache vollständig als Text; die Einschränkung trägt jetzt eine Kontraststufe statt eines Kastens.

**Dritte Destruktiv-Stufe `danger-quiet`, ebenfalls aus dem Bild.** Nachdem die Karten weg waren, standen zwanzig rot umrandete „Löschen"-Knöpfe untereinander — die seltenste Aktion der Seite als ihr lautestes Element. Die Stufe ist an die **Wiederholung** gekoppelt, nicht an geringere Schwere: Hover-Wash und Bestätigungsdialog bleiben, nur der dauerhafte rote Kasten fällt weg. Die Admin-Listen wiederholen ihren Auslöser ebenfalls, bleiben aber bewusst auf `danger` — eine Handvoll Zeilen statt zwanzig, und Purge/Revoke wiegen schwerer als eine gelöschte Vote-Session; entschieden wird das im Bild, wenn diese Seiten drankommen.

**Der `icon`-Input von `EmptyState` ist ersatzlos entfernt**, nicht nur ungenutzt — so kann keine der neun erbenden Flächen ihr Emoji behalten. Sein eigener Kommentar beschrieb es als Echo der Landing-Feature-Kacheln, die es seit gestern nicht mehr gibt. Dazu fallen die gestrichelte Box und die Zentrierung: eine zentrierte Textspalte in einer linksbündigen Seite liest sich wie ein vergessener Platzhalter, und das ist für einen korrekten, erwarteten Zustand die falsche Lesart.

**Das Erstellen-Formular der Voting-Liste steht jetzt unter der Liste.** Wer den Tab öffnet, fragt, was läuft; als Karte über der Liste schob das Formular diese Antwort vom ersten Bildschirm. Es bleibt **vollständig sichtbar** statt in eine Aufklapp-Fläche zu wandern: ob überhaupt jemand Community-Voting will, ist die offene Frage zu diesem Feature (`PRODUCT.md`), und den Einstieg zu verstecken würde sie durch Auszehrung beantworten statt durch Beobachtung.

### 2026-08-06 — Zug 3, öffentliche Fläche: Die Landing zeigt statt vier Funktionskacheln die Form eines Emote-Sets

**Betrifft:** `web/src/app/features/landing/{landing-page.ts,landing-page.html,set-shape.ts}` · `web/src/app/features/login/login-page.ts` · `web/src/app/features/shell/app-shell.ts` · `web/src/app/shared/branding/links.ts` (neu) · `web/src/styles.css` · `web/public/i18n/{de,en}.json` · `web/e2e/{landing.e2e.spec.ts,auth.e2e.spec.ts}`

**Der Ausgangsbefund war messbar, nicht geschmacklich:** die Landing trug als einzige Fläche des Projekts alle sechs Muster gleichzeitig, an denen man eine generierte Seite erkennt — Badge-Pill über der H1, Gradient-Clip-Text, radialer Marken-Schein, fünf zentrierte Sektionen, vier gleich große Icon-Karten mit Emoji, ein zweites Vierspalten-Raster für die Schritte. Sie war zudem seit Zug 1 **halb außer Betrieb**: `--ep-page-glow-1/2` standen auf `transparent`, und die beiden Stops des Überschriften-Gradienten auf demselben Wert. Der „Verlauf" in der H1 war also längst eine einfarbige Fläche und der Schein ein leeres Div — die Tokens waren stillgelegt, die Templates nie nachgezogen. Beides ist mit diesem Umbau restlos entfernt: `.app-page-glow`, `--ep-page-glow-*`, `--ep-heading-gradient-*`, `--shadow-glow` und die drei Fundstellen im Markup.

**Die Seite verkauft nicht, sie erklärt** — das ist keine Stilfrage, sondern steht so in `PRODUCT.md` (Prinzip 5) und wird von `robots.txt` (`Disallow: /`) erzwungen: die Landing ist öffentlich erreichbar, aber nicht auffindbar. **Wer sie sieht, kam über eine Empfehlung.** Diese Person braucht keinen Trichter, sondern vier Antworten: was ist das, was macht es mit meinem Set, wo ist der Haken, wie fange ich an. Die Seite hat deshalb vier statt fünf Sektionen und zwei Sektionen weniger Wiederholung.

**Das Schaubild ist der Ersatz für den Produktbeweis, den es nicht gibt.** `PRODUCT.md` verbietet ausdrücklich, Screenshots, Zahlen oder Belege zu erfinden. Statt eines gefälschten Produktbilds zeigt die Seite den **Befund**, den das Werkzeug erzeugt: ein Feld aus 384 Zellen, absteigend nach Nutzung, in die vier Bänder des echten Atlas geteilt. Die Bandgrenzen kommen aus `usageBandThresholds`/`groupIntoUsageBands` — denselben Funktionen, die die echte Seite aufruft — und die Beschriftungen aus denselben Übersetzungsschlüsseln (`usageStats.bands.*`). Wer sich einloggt, trifft dieselbe Einteilung mit den eigenen Daten wieder. Die Zahlen dahinter sind eine Zipf-Kurve, und **die Bildunterschrift sagt das**; ohne diesen Satz wäre es genau die erfundene Evidenz, die `PRODUCT.md` untersagt.

**Was das Schaubild *nicht* vom Atlas übernimmt, ist die Zelle selbst.** Der erste Bau zeichnete jede als `.app-sprite-cell` mit dem Haarlinien-Balken des Atlas — eine Kohärenz-Entscheidung, die beim Rendern die Aussage umgedreht hat: das Alpha-Karo ist ohne Artwork darauf fast unsichtbar, sichtbar blieb pro Zelle ein 2-px-Strich, und das **tote** Band mit seinen Void-Platten wurde das Lauteste im Bild. Die Zellen sind jetzt Platten mit vertikaler Füllung, gemessen am **globalen** Spitzenwert statt am Bandmaximum (ein Bandmaximum füllt die Spitze *jedes* Bandes auf 100 % und ebnet genau das ein, was gezeigt werden soll). Die Kurve ist `(count/peak)^0.35` mit 8-%-Boden — am gerenderten Bild abgelesen, nicht hergeleitet: linear verschwindet alles ab Rang 12 unter einem Pixel, logarithmisch macht „regelmäßig" und „selten" zu zwei Halbtönen. Der Boden trägt die Aussage: *selten* darf nie aussehen wie *nie*.

**Die vier gleich großen Funktionskacheln sind drei ungleiche Stufen geworden.** Die Gleichheit war eine Falschaussage: Messen läuft wochenlang von selbst und ist die Grundlage, Abstimmen ist optional (die eigene Copy sagt das zweimal), Löschen passiert in einem fremden System und ist nicht rückholbar. Das Raster sagt das jetzt mit — Messen belegt die halbe Zeile, jede Stufe wird von dem einen Wort eingeleitet, das sie regiert (`dauerhaft` · `optional` · `endgültig`), und nur die endgültige trägt eine rote Haarlinie. Die vierte alte „Funktion" (Twitch-Login & Rollen) war nie eine, sondern eine Voraussetzung, und steht jetzt als Schritt 1 im Ablauf. Die Nummerierung dort bleibt, weil die Reihenfolge **Information trägt** — vor dem Join wird nichts gemessen, vor der Messung nichts gelöscht.

**Zwei Reibungspunkte stehen jetzt auf der öffentlichen Seite statt hinter dem Login:** dass die ersten Tage nach dem Join leer sind, und dass der Mass-Delete ein 7TV-Token verlangt, das man einmalig aus den Entwicklertools kopiert. Beides steht als Betriebsrealität in `PRODUCT.md`; beides vorher zu sagen kostet nichts und verlegt die Überraschung nicht nach hinten.

**Die Login-Seite verliert ihre Karte und benennt die Scopes.** Eine Karte ist eine Abgrenzung gegen Nachbarn, und auf dieser Seite gibt es keine — gezeichnet war ein Kasten um das einzige Element, schwebend im Nichts. An seiner Stelle steht die Antwort auf die Frage, die in diesem Moment tatsächlich gestellt wird: `user:read:email`, `user:read:moderated_channels`, `user:read:subscriptions`, wörtlich aus `TwitchOAuthDefaults.RequestedScopes`, jeweils mit einem Satz dazu. Der Besucher liest dieselben drei Zeilen einen Klick später auf Twitchs eigenem Zustimmungsdialog; eine Seite, die sie vorher kleinredet, verliert ihre Glaubwürdigkeit genau dort, wo sie zählt. **Kommt je ein Schreib-Scope dazu, muss diese Liste mitwachsen** — ein E2E-Fall pinnt sie.

**Der einzige neue Außenlink ist der Quelltext** (`shared/branding/links.ts`, öffentlich, AGPL-3.0, beides geprüft statt angenommen). Für ein Werkzeug, das nach einem 7TV-Schreib-Token fragt, ist das die einzige Zusicherung auf der Seite, die ein Besucher in einem Klick selbst nachprüfen kann; alles andere, was eine Marketingseite dafür anböte — Zahlen, Bewertungen, Screenshots — existiert nicht und darf nicht erfunden werden.

### 2026-08-06 — Zug 3, erste Fläche: Der Stimmzettel bekommt eine Zelle statt einer Karte — und wird auf dem Handy **größer**

**Betrifft:** `web/src/app/features/voting/vote-session-detail-page.{ts,html}` · `web/src/app/shared/grid/{atlas-grid.ts,grid-columns.ts,grid-columns.spec.ts,atlas-grid.spec.ts}` · `web/src/app/shared/emotes/emote-card-header.ts` (entfernt) · `web/e2e/vote-ballot.e2e.spec.ts` (neu) · `web/e2e/support/mocks.ts`

**Der Atlas wird hier ausdrücklich nicht mechanisch übernommen.** Auf `usage-stats` ist die Zelle etwas, das man in Masse markiert; hier trägt sie **zwei echte Abstimm-Ziele**, und die sind die Aufgabe. Ein 64-px-Sprite kann die nicht halten. Übertragen wird deshalb nur, was übertragbar ist: der Wegfall des Kartenrahmens, das Alpha-Karo als Zellgrund, die Lesefläche, die Name/Nutzung/Score einmal statt pro Kachel zeigt — und die Spaltenmessung.

**Auf schmalen Flächen wächst die Zelle, statt zu schrumpfen.** Das ist die inhaltliche Entscheidung dieses Umbaus: abstimmen tun Zuschauer, und Zuschauer sind auf dem Handy. Ihre Ziele auf 30 px zu schrumpfen, damit ein Moderator am Desktop weniger scrollt, wäre der falsche Handel. Unter ~600 px Containerbreite ist die Zelle **96 px** mit einem **44 px** hohen Stimmstreifen (Daumen-Symbol + Zahl); darüber 64 px mit 24 px (nur Zahl). Für den Zuschauer heißt das: das Emote wächst von 40 px auf 96 px, die Abstimm-Ziele behalten ihre 44 px Höhe, und es passen drei statt zwei in eine Zeile. Die Schwelle misst den **Container**, nicht das Fenster — ab `lg` steht rechts ein Sidecar, das Fenster sagt über den verfügbaren Platz also nichts.

**Der Streifen ist gleichzeitig Bedienelement und Auswertung.** Zwei Hälften, haarfein getrennt, die eigene Stimme **gefüllt** statt nur eingefärbt (Formänderung, nicht nur Farbe). Darunter ein 2-px-Verhältnisbalken in Grün/Rot: die Zahlen sagen *wie viele*, der Balken sagt *wohin es kippt* — und Letzteres ist das, was ein Blick über 240 Zellen tatsächlich liest. Bei verdeckter Auszählung entfällt beides ersatzlos; ein Platzhalter im Zahlenfeld läse sich wie ein Wert.

**Auswahl und Abstimmung sind räumlich getrennt:** das Sprite ist das Auswahlziel (nur für Manager, füttert den Mass-Delete), der Streifen darunter das Abstimmziel. Eine der beiden Handlungen endet in einem unwiderruflichen Löschen auf 7TV, die andere ist jederzeit zurücknehmbar — sie dürfen nicht ineinanderlaufen.

**`computeGridColumns` ist ersatzlos entfernt.** Es bildete die *Fenster*breite über Breakpoints auf eine Spaltenzahl ab, während die Shell den Inhalt auf 1024 px deckelt — derselbe Defekt, der auf `usage-stats` gemessen wurde. Beide Raster messen jetzt ihren eigenen Container über `atlasColumns`, das dafür ein Zellmaß als Parameter bekommen hat. **`EmoteCardHeader` ist ebenfalls entfernt**: es existierte nur, um den Namen auf jede Karte zu drucken, und beide Flächen tun das nicht mehr.

**Die Stimmzettel-Fläche hatte bis heute keine einzige Browser-Abdeckung** — die schlechteste denkbare Kombination mit einem Umbau ihres Bedienmodells. Neu: `vote-ballot.e2e.spec.ts` mit sechs Fällen (Stimme abgeben, durch erneuten Druck zurücknehmen, archivierte Emotes bleiben gelistet aber unwählbar, verdeckte Auszählung zeigt keine Zahlen, Lesefläche folgt dem Zeiger, Auswahl schlägt nicht in die Stimme durch). Dabei fiel auf, dass `mockVoteSessionResults` ein explizites `null` per `??` verschluckte — genau der Wert, mit dem der Server eine laufende Geheimabstimmung verdeckt; nur `undefined` fällt jetzt noch auf den Standardwert zurück.

### 2026-08-06 — Nachtrag zu Zug 2: Der Verlaufs-Auslöser sitzt auf der Zelle, und der Sidecar kommt doch

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/public/i18n/{de,en}.json` · `web/e2e/{usage-atlas,channel-workspace}.e2e.spec.ts`

**Der Satz „Der Sidecar aus dem genehmigten Entwurf ist deshalb eine Zeile geworden" aus dem Eintrag darunter ist damit revidiert** — aber nicht aus dem Grund, aus dem die Frage aufkam. Gemeldet wurde: der Verlaufs-Knopf in der Inspector-Zeile sei kaum anklickbar. Er war es nicht, und die Ursache ist strukturell: **eine Fläche, die dem Zeiger folgt, kann kein Klickziel enthalten.** Der Weg dorthin führt zwangsläufig über andere Zellen, und unterwegs zeigt die Fläche etwas anderes. Das ist eine Eigenschaft von Hover, keine der Position — ein rechter Sidecar hätte exakt dasselbe Problem gehabt, sobald er einen Knopf getragen hätte; im Prototyp fiel es nur nicht auf, weil dessen Sidecar reiner Text war.

**Der Auslöser sitzt deshalb auf der Zelle**: 20 px, oben links, eingeblendet bei Hover und solange die Zelle den Tabstopp des Rasters hält. Wegstrecke null, und nichts dazwischen kann ändern, worauf er sich bezieht. Oben links, weil unten links die Zahl steht und oben rechts die Auswahlmarke. Er ist **Geschwister** der Zelle in einem positionierten Rahmen, nicht deren Kind — ein `<button>` im `<button>` ist ungültiges HTML — und trägt `tabindex="-1"`, damit die Roving-Tabindex-Ordnung bei einem Stopp bleibt.

**Drei Eingabearten, ein Ziel.** Auf Zeigergeräten Hover. Auf groben Zeigern (`pointer: coarse`) ist der Knopf **dauerhaft** sichtbar, weil es dort kein Hover gibt, das ihn einblenden könnte. Für die Tastatur sind **Leertaste (markieren) und Enter (Verlauf öffnen) getrennt** — vorher taten beide dasselbe, weil beide auf einem `<button>` einen nativen Klick auslösen; die Trennung ist das, was der Tastatur einen Weg zum Auslöser gibt, ohne dem Raster einen zweiten Tabstopp zu geben. Ein Klick auf eine Zelle heftet außerdem die Lesefläche an sie: ohne Hover trieb dort gar nichts, und Safari fokussiert Buttons beim Klick ebenfalls nicht.

**Erst damit war der Sidecar wieder eine freie Entscheidung**, und sie ist bewusst gegen die bessere Fläche ausgefallen. Gemessen an 1600 px Fensterbreite und 900 Emotes: Zeile 14 Spalten / ~4.400 px Bogen · Sidecar bei gedeckelter Shell 10 Spalten / ~6.120 px (+39 % Scrollweg) · Sidecar bei auf 1536 px verbreiterter Shell 18 Spalten / ~3.400 px (−23 %). Gewählt ist die **mittlere**: sie berührt nur diese Seite, während die dritte den Breiten-Deckel **jeder** Seite ändern würde — Übersicht, Voting, Admin, Aktivität — und damit in den Zug gehört, in dem diese Flächen ohnehin drankommen. Der Sidecar-Code selbst ist von diesem späteren Wechsel nicht betroffen; es ist allein die Shell.

**Ab `lg` der Sidecar, darunter die Zeile — nie beide.** Die Zeile bleibt im DOM und wird per CSS ausgeblendet, was zwei Bestandstests als Strict-Mode-Verstoß aufgedeckt haben (derselbe Emote-Name zweimal auffindbar); sie sind jetzt auf `role="complementary"` eingegrenzt. Der Sidecar trägt **keinen** Verlaufs-Knopf, aus demselben Grund, aus dem die Zeile ihren verloren hat. Sein Sticky-Versatz wird an der Werkzeugleiste **gemessen** statt festgeschrieben: die Filterzeile bricht je nach Breite in eine, zwei oder drei Zeilen um, ein fester Wert würde am einen Ende klaffen und am anderen die erste Zeile des Panels abschneiden.

### 2026-08-06 — Visuelle Neuausrichtung, Zug 2: Die Emote-Karte ist weg, `usage-stats` ist ein Sprite-Bogen

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/src/app/shared/emotes/usage-bands.{ts,spec.ts}` (neu) · `web/src/app/shared/grid/atlas-grid.{ts,spec.ts}` (neu) · `web/src/app/shared/selection/list-selection.{ts,spec.ts}` · `web/src/styles.css` · `web/public/i18n/{de,en}.json` · `web/e2e/usage-atlas.e2e.spec.ts` (neu) · `web/e2e/channel-workspace.e2e.spec.ts`

**Der Befund ist messbar, nicht geschmacklich: die Karte war zu 92 % Rahmen.** Jedes Emote saß in einer `h-36`-Box (144 px) und war darin 40 px groß; der Rest war Name, Zähler, Trendpfeil, Datumszeile und ein Info-Knopf. Bei ~900 Emotes ergab das 18.000 bis 72.000 px Scrollweg für eine Aufgabe, die aus „welche davon benutzt niemand" besteht. Dazu ein zweiter, unabhängiger Fehler: `computeGridColumns` las `window.innerWidth`, während die Shell den Inhalt auf `max-w-5xl` (1024 px) deckelt — auf einem 2560er Monitor landeten also acht Karten in 992 px und wurden auf je 113 px gestreckt, ohne dass das Emote darin größer wurde. **Der Bogen dreht das um: das Sprite *ist* die Zelle**, 64 px, Kachelabstand 4 px, kein Kartenrahmen. Gemessen an derselben Inhaltsbreite sind das 14 statt 8 Spalten und rund ein Viertel des Scrollwegs, bei gleichzeitig 60 % größerem Emote.

**Gewicht kommt aus Reihenfolge, Band und einem Haarstrich — ausdrücklich nicht aus der Zellgröße.** Eine erste Fassung skalierte die Zelle nach Nutzung; sie war spektakulärer und schlechter, weil die Vergleichbarkeit über das Raster verloren ging und tote Emotes zu Punkten wurden. Der Dichtegewinn kam ohnehin aus dem Wegfall der Karte, nicht aus der Größenvariation. Geblieben ist ein 2-px-Füllstrich am unteren Zellrand, gemessen **gegen den Spitzenwert des eigenen Bandes**, wurzelskaliert — gegen den Set-Spitzenwert wäre jeder Strich im seltenen Band ein unsichtbarer Pixel.

**Die Bänder sind Pareto-Schnitte aus dem Set selbst, keine festen Schwellen.** „Ab 1.000 Treffern ist ein Emote tragend" stimmt für HandOfBlood und ist für einen Channel, dessen bestes Emote 40 Treffer hat, bedeutungslos — dort läge alles im selben Band und die Gruppierung trüge null Information. Stattdessen: *Tragende Emotes* sind die, die zusammen die erste Hälfte der gesamten Nutzung ausmachen, *Regelmäßig* reicht bis 80 %, *Selten* ist der Rest mit mindestens einem Treffer, *Nie benutzt* ist null. Das trägt auf jeder Größenordnung und sagt nebenbei etwas Wissenswertes („elf Emotes sind die Hälfte deines Chats"). Geschnitten wird nach **Nutzungswert, nicht nach Rang**, damit ein Gleichstand nicht mitten durchtrennt wird; die Schwellen kommen aus dem **ungefilterten** Set, damit ein Namensfilter kein Emote das Band wechseln lässt. Das Band ist die Gewichtsklasse, die vom Nutzer gewählte Sortierung ordnet *innerhalb* davon.

**Alle 900 Zellen teilen sich einen Tabstopp.** Die Karten waren 900 Tabstopps — die Tastaturroute durch die Seite war damit praktisch unbenutzbar. Jetzt gilt das WAI-ARIA-Roving-Tabindex-Muster: eine Zelle trägt `tabindex="0"`, die Pfeiltasten verschieben ihn, Home/End springen an die Enden. Die Bewegung rechnet **auf der Zeilenstruktur**, nicht per `index ± columns`: dazwischenliegende Band-Überschriften würden ArrowDown sonst in eine Spalte werfen, auf die niemand gezielt hat. Ist die Zielzelle virtualisiert, scrollt der Viewport erst per `scrollToIndex` und fokussiert im nächsten Frame.

**Band-Überschriften sind Zeilen wie jede andere, weil CDKs Fixed-Size-Strategie genau eine Item-Höhe kennt.** Sie bekommen dieselben 68 px und verbrauchen den größten Teil davon als Luft über der Beschriftung — was eine Überschrift ohnehin will. Nur das tote Band trägt „alle markieren": es ist die einzige Gruppe, deren Mitglieder per Definition Kandidaten sind. Die dafür neue `ListSelection.selectMany` ist bewusst **nur additiv** — ein Umschalter würde eine von Hand gebaute Auswahl mit einem Fehlklick löschen, und der nächste Schritt danach ist ein unwiderrufliches Löschen auf 7TV.

**Was 900-mal auf den Karten stand, steht jetzt einmal in der Inspector-Zeile.** Name, Rang, Band, Anteil, letzte Nutzung, Trend und Beobachtungsstatus folgen dem Zeiger bzw. dem Fokus, in der zweiten Zeile derselben Sticky-Leiste. Bewusst **kein** `aria-live`: bei jedem Hover eine Ansage wäre ein Rauschstrom — die Zelle selbst trägt Name und Zähler in ihrem `aria-label`. Der Sidecar aus dem genehmigten Entwurf ist deshalb eine Zeile geworden und keine rechte Spalte: 300 px Rail hätten von 992 px Inhaltsbreite ein Drittel gekostet (9 statt 14 Spalten), und die Alternative wäre gewesen, den Breiten-Deckel der Shell für **jede** Seite mitzuändern — eine Entscheidung, die nicht in diesen Zug gehört. Die eigentliche Historie bleibt der bestehende Drilldown-Dialog, den die Zeile per Knopf öffnet; auf Hover eine Serie nachzuladen wären 900 Requests.

**Die Sticky-Leiter bleibt zweistufig.** Der Inspector ist eine zweite Zeile **desselben** Sticky-Elements, keine dritte Sprosse — `top-24` (Shell-Header h-14 + Workspace-Tabs h-10) ist ein seitenübergreifender Vertrag (§8.5). Der Verteilungsstreifen ist deshalb ausdrücklich **nicht** sticky: er wird einmal bei der Ankunft gelesen. **Unterhalb von `sm` pinnt die Leiste gar nicht**: Werkzeugleiste plus Inspector sind rund 200 px, und die in ein 844-px-Viewport zu nageln lässt ein Viertel des Bildschirms für den Inhalt übrig.

**Der Verteilungsstreifen ist logarithmisch skaliert, und das ist eine Messung, keine Vorliebe.** Linear ergibt er einen Zacken und einen Boden; auch die Wurzel ließ bei 900 echten Emotes alles ab dem ersten Zehntel unter vier Pixeln — beides liest sich als Dekoration. Erst der Log-Maßstab bringt Knie und flachen Totenschwanz als unterscheidbare Formen aufs Bild. Gebündelt wird auf höchstens 96 Balken und **nie mehr als es Emotes gibt**: Hochtasten würde eine Auflösung behaupten, die die Daten nicht haben. Annotiert mit zwei echten Zahlen (tote Emotes, Konzentration des oberen Fünftels), damit er nie als Zierde gelesen werden muss.

**Die Aktionsleiste dockt unten an, statt dauerhaft über dem Raster zu parken.** Sie existiert nur, solange eine Auswahl existiert — das ist es, was die Fläche darüber zu nichts als Emotes macht. Sie bleibt aber auch durch einen *abgeschlossenen* Lauf hindurch montiert (`deleteService`/`restoreService` Queue nicht leer), weil die Laufzusammenfassung den Protokoll-Download trägt und diese Datei der einzige dauerhafte Beleg des Löschlaufs ist. `MassDeletePanel` selbst ist unverändert — es wandert nur in die Leiste; `RestorePanel` bleibt im Fluss, weil es eine Wiederherstellungs-Hilfe ist und nicht Teil des Markierens.

**Zwei Fundstellen wurden erst am gerenderten Ergebnis sichtbar und dort korrigiert.** Erstens: `inset-ring` auf der Zelle malt *unter* deren Kindern, die Auswahl-Lasur hat den Auswahlring also verdeckt — markierte Zellen kamen als blasser Farbstich ohne Kante heraus. Lasur und Ring liegen jetzt gemeinsam auf einem zuletzt gezeichneten Overlay. Zweitens die Log-Skala oben. Beides gefunden durch Rendern von 900 Emotes mit echtem Alpha-Bildmaterial, nicht durch Codelesen.

**Neue Token:** `--ep-emote-canvas-alt` (zweites Feld des Alpha-Karos) und `--ep-sprite-scrim`/`-fg` (Untergrund der auf das Sprite gedruckten Zahl). Das Alpha-Karo ist die Notation, die jedes Werkzeug für transparente Grafik benutzt — es ist der Grund, warum die Zelle ohne Rahmen als Zelle liest, und damit kein geliehenes Dekor, sondern Material aus der Sache selbst. Der Kontrast zum Canvas ist absichtlich winzig: 900 davon kacheln die Fläche.

### 2026-08-06 — Die Marke ist ein Vektor: eine Datei für alle Größen und beide Modi

**Betrifft:** `web/public/logo.svg` (neu) · `web/public/{logo,logo-light,logo-hero,logo-hero-light}.png` (entfernt) · `web/public/{favicon.ico,apple-touch-icon.png,icon-192,icon-512,icon-maskable-192,icon-maskable-512}.png` · `web/public/og-image.png` (neu) · `web/public/manifest.webmanifest` · `web/src/index.html` · `web/src/app/shared/branding/logo.ts` · `web/src/app/features/{shell/app-shell.ts,login/login-page.ts,landing/landing-page.{ts,html}}` · `web/e2e/theme.spec.ts` · `web/branding/README.md` · `web/branding/make-icons.ps1` (entfernt) · `web/.claude/CLAUDE.md`

**Die Marke selbst bleibt, nur ihre Farbe geht.** Nach dem Palettentausch war das violett-magenta Logo das einzige Bauteil der App in der alten Welt und damit der lauteste Fremdkörper. Der Entwurf einer neuen, atlas-eigenen Marke (Kachel mit Gesicht, ausgebrochene Ecke, durchgestrichenes Fragment) lag vor und wurde **verworfen**: sie war konzeptuell strenger, hielt aber kleine Größen schlechter (die Masse sitzt unten links, die Fragmente werden bei 24 px zu Punkten) und hätte den Wiedererkennungswert ohne Not weggeworfen. Die bestehende Marke gewinnt auf beiden Achsen.

**Das SVG ist nicht nachgezeichnet, sondern aus dem Original gewonnen.** Pro Pixel Klassifikation in Silhouette und fast-schwarze Gesichtszüge, Konturen per Marching Squares auf 512er-Raster, Vereinfachung mit Ramer-Douglas-Peucker bei ~1 px, Sprenkel unter 6 px Spannweite verworfen (Antialiasing an der Körper/Tinte-Grenze erzeugte 21 Störringe). Ergebnis: 4 Körper- und 4 Tinte-Konturen, 3,7 KB gegen 206 KB der Hero-PNG.

**Eine Datei ersetzt vier.** `logo.png`, `logo-light.png`, `logo-hero.png` und `logo-hero-light.png` sind weg. Der Grund ist der Farbwechsel selbst: eine flache Farbe trägt auf Graphit wie auf Papierweiß, also gibt es nichts mehr zu tauschen. Damit fällt auch die Mechanik weg, die das getan hat — `logo.ts` injizierte den `ThemeService` und rechnete den Dateinamen aus; es ist jetzt eine Konstante. Ebenso entfällt die Pflicht, helle und dunkle Zwillinge gegen dieselbe Bounding-Box zu schneiden, damit die Marke beim Moduswechsel nicht springt. Die Füllung ist eine CSS-Custom-Property mit Literal-Fallback: eingebettet folgt sie dem Theme, hinter einem `<img>` greift der Fallback.

**Der E2E-Test hat seine Zusage umgedreht statt sie zu verlieren.** `theme.spec.ts` prüfte, dass die Marke beim Moduswechsel *wechselt*. Sie tut es nicht mehr — also prüft der Test jetzt das Gegenteil: die `src` bleibt gleich, und der Wechsel bleibt still. Die Konsolen-Prüfung war ohnehin der wertvolle Teil (eine Laufzeit-Warnung von `NgOptimizedImage` hört weder der Build noch ein Unit-Test).

**`NgOptimizedImage` bleibt trotz schlechter Passung, mit Ansage.** Die Direktive ist für Rasterbilder gebaut; auf einem Vektor ist ihr Dichte-`srcset` sinnlos, weshalb alle drei Fundstellen `disableOptimizedSrcset` tragen. Sie zu entfernen wäre nicht billiger: `@angular-eslint/template/prefer-ngsrc` kennt keine Ausnahme nach Dateiendung, es bräuchte drei `eslint-disable`-Kommentare statt drei Attribute — dasselbe Rauschen plus eine Lint-Regel mit Löchern. Der Sonderfall steht jetzt in `web/.claude/CLAUDE.md` statt in niemandes Kopf.

**Raster-Assets neu abgeleitet, plus das fehlende OG-Bild.** Gerendert aus dem SVG in headless Chromium auf ein Canvas der Zielgröße; `favicon.ico` als PNG-eingebetteter ICO-Container über 48/32/16. Die maskable-Icons nutzen einen festen Rand von 21 % (Marke füllt ~58 % der Kante, sicher im 80-%-Safe-Zone-Kreis) — konservativer als die frühere radiale Messung, dokumentiert in `web/branding/README.md`. **`og-image.png` (1200×630) existierte bisher überhaupt nicht**, samt der zugehörigen `og:`- und `twitter:card`-Metas. Manifest-Farben folgen der neuen Seite (`#0d0f12`).

**`make-icons.ps1` ist entfernt, weil es gefährlich geworden war.** Das Skript leitete alle Assets aus dem violetten Quellpaar ab; ein einziger Lauf hätte die neue Marke still durch die alte ersetzt. Die Quell-PNGs unter `web/branding/` bleiben liegen — sie sind die Herkunft des Vektors und Archiv, kein Aufräumfall.

### 2026-08-06 — Visuelle Neuausrichtung, Zug 1: Slate + Lila raus, Graphit + Guide-Türkis rein; Schriften selbstgehostet

**Betrifft:** `web/src/styles.css` · `web/src/index.html` · `web/public/fonts/*` · `PRODUCT.md` (neu) · `docs/UI-Designsprache.md` (Werte-Verweise)

**Anlass ist kein Handwerksfehler, sondern eine Herkunftsfrage.** Die bisherige Designsprache ist gemeinsam mit Claude entstanden und beruhte in den *ästhetischen* Achsen auf Annahmen der KI, nicht auf Entscheidungen des Betreibers. Das Ergebnis war handwerklich sauber (Rollennamen, Kontrastnachweise, Lint-Gate) und trotzdem generisch: `slate-950/900/800` als Neutralramp plus `purple-500/600` als Akzent ist die statistisch häufigste Kombination, die ein Sprachmodell wählt, wenn ihm niemand etwas anderes sagt. **Getauscht werden deshalb gezielt die drei Achsen, die den Wiedererkennungswert tragen — Neutralramp, Akzent, Schrift — und ausdrücklich nicht die Semantiktöne.** Emerald für Erfolg, Amber für Warnung, Rot für Gefahr sind branchenweite Konvention und kein Tell; sie bleiben unverändert stehen.

**Der Neutralramp ist jetzt Graphit statt Slate, mit einer Spur Grün statt Slates Blau.** Die Begründung ist inhaltlich, nicht geschmacklich: der Inhalt dieser App sind mehrere hundert fremde, vollgesättigte Emote-Grafiken, und ein blaustichiger Grund tönt jede einzelne davon. Werte dunkel `#0d0f12 / #15181c / #1f232a / #282d35`, hell `#f2f3f5 / #fff / #eaecef / #dfe2e6`. `--ep-field` geht im Dunkeln jetzt **unter** die Seite (`#0a0c0f`) statt sie zu spiegeln — ein Eingabefeld liest sich als in die Fläche gedrückt.

**Der Akzent ist ein Guide-Türkis, und es ist ausdrücklich eine Guide-Farbe.** Packer- und Layout-Werkzeuge reservieren einen grellen Ton für Overlays, die nie mit dem Material verwechselt werden dürfen; genau diese Rolle hat `--ep-accent: #00d3bc` — Fokusring, Auswahlkante, Markierung, Links. Es **leuchtet nicht**: Guides sind Haarlinien, keine Lichtquellen.

**Gefüllte Akzentflächen tragen deshalb ein tiefes Teal (`#0a6f64`), nicht das Guide-Türkis — und das erhält die bestehende Hover-Regel.** Auf gesättigtem Türkis erreicht Weiß rund 2,5:1, es wäre also dunkle Tinte nötig gewesen. Damit hätte sich die Regel „gefüllte Flächen verdunkeln im Hover" für genau diese eine Familie umgedreht, und im hellen Modus ein zweites Mal. Der Tokenblock hatte diese Zwei-Regeln-Situation am 2026-08-02 bewusst beseitigt; sie zurückzuholen wäre der teuerste Teil des Umbaus gewesen. **Also bleibt `--ep-on-accent` in beiden Modi Weiß, `--ep-accent-solid-hover` verdunkelt weiter, und ein `--ep-on-tone`-Token wird nicht eingeführt.** Weiß auf `#0a6f64` sind 5,3:1, auf `#085a51` 6,6:1. Im hellen Modus spielt das tiefe Teal beide Rollen: bei 1,7:1 auf Weiß kann das Guide-Türkis dort nicht Guide sein, die Rolle bleibt aber dieselbe — die gesättigtste Farbe der Seite, nur dunkel statt hell.

**Die dekorative Markenaura ist app-weit stillgelegt, ohne ein Template anzufassen.** `--ep-page-glow-1/2` stehen in beiden Modi auf `transparent`; `.app-page-glow` liegt noch im Markup von App-Shell, Landing und Login und malt nichts mehr. Ein radialer Farbschleier hinter einem Hero ist der zuverlässigste einzelne Hinweis auf eine generierte Oberfläche, und diese Welt kennt keine Lichtquellen. Dieselbe Mechanik beim Landing-Headline-Verlauf: beide Stopps tragen denselben Wert, der Verlauf ist damit faktisch aufgehoben, ohne dass die Utility aus dem Template muss. `--ep-shadow-card-hover` ist von einem lila Halo auf einen echten neutralen Schatten umgestellt — ein farbiger Schein um eine Karte ist Dekoration im Kostüm von Tiefe.

**Archivo und Azeret Mono ersetzen den System-Stack, und beide werden selbst gehostet.** Kein `<link>` auf `fonts.googleapis.com`: das Hotlinken überträgt die IP-Adresse des Besuchers ohne Einwilligung an Google, wofür das LG München I (3 O 17493/20, 2022-01-20) Schadensersatz zugesprochen hat — bei einem Projekt, dessen Rechtstexte ohnehin noch offen sind (S2-20), ein vermeidbares Risiko. Die Dateien liegen unter `web/public/fonts/` und werden same-origin ausgeliefert. **Beide Familien sind variable Fonts**, deshalb eine `@font-face`-Regel je Unicode-Subset mit `font-weight: 400 700` statt der vier Einzelgewichte, die Googles CSS-API ausgibt und die dieselbe Datei viermal laden würden. Das Vietnamesisch-Subset ist weggelassen (Locales sind de/en). Gesamtgewicht 108 KB über vier Dateien, subsetgesteuert nachgeladen. Die Registrierung steht in einem **normalen** `@theme`-Block, nicht in `@theme inline`: anders als die Farben wechseln Schriften nicht pro Modus, ihr Wert darf also zur Buildzeit in die Utilities gebacken werden.

**Der Richtungsvertrag der neuen Welt steht als HTML-Kommentar in `web/src/index.html`**, als erstes Kind von `<body>`, und überlebt den Production-Build (verifiziert: der Seed `d5364856` steht in `dist/web/browser/index.html`). Ein Vertrag, den man im ausgelieferten Markup nicht lesen kann, wird von niemandem geprüft.

**Was Zug 1 ausdrücklich noch nicht tut:** die Flächen selbst sind unverändert. Die Karte pro Emote, das feste `h-36`-Raster, `.app-page-glow` als Element, die Eyebrow-Pille auf der Landing-Page und der Verlaufs-Utility-Chain bleiben stehen, bis die jeweilige Fläche umgebaut wird (Zug 2, beginnend mit `usage-stats`). Produktwahrheit und die für den Umbau bindenden Zusagen stehen ab jetzt in `PRODUCT.md` am Repo-Root.

### 2026-08-05 — `live.changed`: die Live-Badges bekommen Push-Updates, der B10-Verzicht auf SSE ist revidiert

**Betrifft:** `src/EmotePurge.Core/Messaging/LiveEvents.cs` · `src/EmotePurge.Worker/{TwitchLivePollWorker,LiveStatusDiff,LiveEventPublisher}.cs` · `src/EmotePurge.Api/Endpoints/LiveEndpoints.cs` · `src/EmotePurge.Infrastructure/Redis/RedisLiveEventStream.cs` · `tests/EmotePurge.Worker.Tests/LiveStatusDiffTests.cs` · `tests/EmotePurge.Api.Tests/{AuthFilterMatrixTests,LiveRouteStructureTests}.cs` · `web/src/app/core/i18n/api-error.ts` · `web/src/app/core/live/live-event.model.ts` · `web/src/app/features/overview/overview-page.ts` · `web/src/app/features/admin/admin-channels-page.ts`

**Der B10-Satz „Kein SSE-Anschluss: die Listen laden den Zustand beim Fetch mit, 5-Minuten-Granularität rechtfertigt keine Push-Updates" (Eintrag vom 2026-08-03) ist damit revidiert.** Der beobachtete Fall hat gezeigt, dass er die falsche Größe gemessen hat: ein Badge stand über 30 Minuten falsch, nicht weil der Poll zu grob ist, sondern weil ein offener Tab die Liste nie wieder lädt — die Granularität war nie die Obergrenze der Falschheit, das Fehlen jedes Rückwegs in den offenen Tab war es. Die zweite Hälfte der alten Begründung bleibt gültig: es kommt **kein** neues Dauer-Control dazu (Frontend-Zurückhaltung), das Update passiert still.

**Ein Thin-Event pro gewechseltem Channel, publiziert vom `TwitchLivePollWorker` nach Diff gegen die letzte eigene Publikation.** `live.changed` trägt wie `channel.synced` nur den Channel-Namen und keinen Zustand — die Clients refetchen, womit die Autorisierungsgrenze der Refetch bleibt und nicht das Event. Die ganze Entscheidung liegt pur und getestet in `LiveStatusDiff` (Muster `ReconnectPolicy`/`TwitchWatchdogPolicy`, container-frei in `Worker.Tests`, Regel 11); publiziert wird über dasselbe Swallow-and-log-`LiveEventPublisher`-Muster wie `channel.synced` — der Poll ist zu dem Zeitpunkt persistiert, ein Redis-Schluckauf darf ihn nicht nachträglich scheitern lassen. Die Baseline ist nach dem ersten Tick in-memory (den `worker:live-status`-Key jedes Mal zurückzulesen würde mit dem eigenen Schreiben rennen); **nach einem Worker-Neustart füllt der vorhandene Redis-Snapshot sie auf**, sodass ein Wechsel während eines kurzen Restarts sein Event trotzdem bekommt. Fehlt beides — erster Start überhaupt, oder der Snapshot war abgelaufen —, gibt es bewusst **keine** Events: eine verpasste Transition ist billiger als ein Event-Sturm über alle gejointen Channels beim Kaltstart.

**Neuer SSE-Endpoint `GET /api/channels/live-events`, bewusst ohne Per-User-Filterung.** Die Übersicht ist channel-übergreifend, sie kann also keinen der bestehenden Per-Channel-Streams abonnieren. Der neue Stream verlangt nur eine Session (kein Rollencheck) und schickt jedem Zuhörer jedes `live.changed`: die Events sind selten (echte Live-Wechsel getrackter Channels, ~alle 5 Minuten ein Poll), die Nutzlast ist ein Channel-Name, der ohnehin in jeder Kanalliste steht, und die eigentlichen Daten kommen erst aus dem autorisierten Refetch (`GET /api/channels/mine`) — dieselbe Arbeitsteilung wie beim Per-Channel-Stream. Kein Routen-Konflikt: Der konkurrierende Nachbar ist nicht `/api/channels/{channelName}/live` (das unterscheidet sich trivial in der Segmentzahl), sondern `GET /api/channels/{channelName}` — gleiche Segmentzahl, und dort entscheidet die Routing-Präzedenz, in der ein Literal den Parameter sticht. Zusätzlich fließt `live.changed` über die bestehenden Streams (`AdminTypes` für die Admin-Channel-Liste, `ChannelTypes` mit).

**Das Per-Login-SSE-Budget steigt dabei von 3 auf 6 (`LiveEventStreamOptions.MaxPerSubscriber`)**, weil die Übersicht jetzt dauerhaft einen Stream pro Tab hält und 3 damit im ganz normalen Mehr-Tab-Betrieb aufgebraucht ist (Übersicht + Channel-Workspace, oder Übersicht + Admin-Monitoring + Admin-Liste) — ein erschöpftes Budget beantwortet den EventSource-Handshake mit 503, was der Browser als endgültig behandelt: der Tab verliert seine Live-Updates still und dauerhaft. Die strukturelle Lösung wäre ein Connection-Sharing pro URL im `LiveUpdateService`; sie ist bewusst auf später vertagt, das höhere Budget reicht für die absehbare Nutzung.

**Beachtenswerte Kopplung:** Jedes `live.changed` löst pro offenem Übersichts-Tab einen Refetch von `GET /api/channels/mine` aus, und dieser Endpoint macht einen ungecachten Helix-Call auf die moderierten Kanäle. Die Frequenz ist damit durch `Twitch:LivePollIntervalSeconds` (300 s) gedeckelt — dieses Intervall darf nicht dauerhaft gesenkt werden, ohne diese Kopplung mitzudenken.

**Mitbehoben: der eingefrorene „Stand vor x min"-Tooltip auf beiden Seiten — ein echter Regel-14-Verstoß.** Das `computed()` las `Date.now()` direkt, blieb damit auf dem Wert des ersten Renderings stehen und behauptete auf einer stundenlang offenen Seite weiter „Stand vor 1 min". Jetzt tickt ein Signal im 30-Sekunden-Takt als Zeitquelle. Die Übersichtsseite ist dafür auf `rxResource` umgebaut (statt eines einmaligen Konstruktor-`subscribe`): Ein Resource behält seinen alten Wert während des *Ladens*, und ein `lastGoodChannels`-Signal hält ihn über einen *Fehler* hinweg (ein fehlgeschlagener Hintergrund-Reload würde die Liste sonst leeren) — die Zeilen blinken beim Push also nicht weg; Lade- und Aktionsfehler bleiben getrennte Signale — sonst löscht der Reload nach einer fehlgeschlagenen Aktion deren Meldung. Abonniert wird per `liveReload` mit 250-ms-Debounce, weil ein einzelner Poll-Tick mehrere Channels auf einmal kippen kann und das ein Refetch sein soll, nicht fünf.

### 2026-08-05 — S2-21-Kalibrierung: 512 M / 1.0 CPU mit Realdaten bestätigt, synthetischer Lasttest verworfen

**Betrifft:** `docker-compose.prod.yml` · `docker-compose.yml` · `docs/Review-2026-07-29-Umsetzung.md`

**Die Review-Startwerte sind jetzt Messwerte.** Prod am 2026-08-05, Limits aktiv: idle (kein Channel live) api 115 MB / worker 132,5 MB; mit gejointem 17k-Zuschauer-Live-Channel (trymacs, Test-Join) api 122 MB / worker 143–147 MB über vier Messpunkte, CPU-Grundrauschen 2–3 % mit einer 11,7-%-Spitze. **Der aufschlussreiche Befund ist das Live-Delta von nur ~10–15 MB:** Der Speicher wird von der Runtime-Baseline dominiert, nicht vom Chat-Volumen — Nachrichten werden gematcht und verworfen, die Usage-Counter sind Zähler pro Emote, kein wachsender Puffer. Mehr Chat-Durchsatz kostet CPU, nicht RAM; beide Limits haben Faktor ≥ 3–8 Luft. Sie bleiben bewusst so eng: ein echtes Speicherleck soll am Limit sichtbar werden (und dank .NETs cgroup-bewusstem GC vorher aggressiver aufgeräumt), statt den Host zu fressen.

**Ein synthetischer Lasttest ist damit verworfen.** Die Produktionswoche seit dem Launch der Limits-Diskussion deckt die Durchsatz-Achse mit echten großen Channels besser ab als jede Simulation. Die *andere* Achse — Channel-Anzahl (JOIN-Rate beim Reconnect, 100-Chatroom-Decke, 7TV-500-Subscription-Limit) — bleibt bewusst ungetestet: ein Test dafür würde Dutzende Joins auf der unverifizierten Verbindung verbrennen; sie wird erst bei realem Wachstum relevant und dann über den verifizierten Bot-Account gelöst, nicht über einen Test (s. „Bekannte offene Grenzen" in CLAUDE.md). Optionale Restbestätigung: einmal `docker stats` während eines HandOfBlood-Peaks.

### 2026-08-05 — S4-18-Abschluss: Lizenz ist AGPL-3.0

**Betrifft:** `LICENSE` · `README.md` (implizit) · gesamtes Repo

**Das öffentliche Repo trägt jetzt die AGPL-3.0 (Nutzerentscheidung vom 2026-08-05).** Vorher galt „all rights reserved" bei öffentlich einsehbarem Code — der unklarste aller Zustände. Die AGPL passt zum Betriebsmodell: Der Code bleibt Open Source, aber wer einen Klon **als Dienst betreibt**, muss seinen Quellcode offenlegen (die Netzwerk-Klausel, die der GPL fehlt) — genau das Szenario, das für eine gehostete Webanwendung wie emotepurge.app relevant ist. MIT wurde als zu permissiv verworfen (Closed-Source-SaaS-Kopie wäre legal), Proprietär als unnötig restriktiv für ein ohnehin öffentliches Repo. Kanonischer Volltext von gnu.org, unverändert.

### 2026-08-05 — S4-15/16/17: Actions auf Commit-SHAs, Dependabot statt Audit-Gate, Docker-Build-Kontext auf `src/`

**Betrifft:** `.github/workflows/publish.yml` · `.github/dependabot.yml` · `src/EmotePurge.Api/Dockerfile` · `src/EmotePurge.Worker/Dockerfile`

**S4-15 — alle sechs Actions auf Commit-SHA gepinnt** (mit Versions-Kommentar, Stand: checkout v4.4.0, setup-dotnet v4.3.1, setup-node v4.4.0, upload-artifact v4.6.2, login-action v3.7.0, build-push-action v6.19.2). Ein kompromittiertes Upstream-Tag kann damit nicht mehr unbemerkt in den Build gelangen, der mit `packages: write` auf GHCR schreibt. Die Pins veralten nicht still: das `github-actions`-Ökosystem in Dependabot aktualisiert SHA **und** Kommentar per PR.

**S4-16-Rest — Dependabot statt hartem Audit-Gate.** Drei Ökosysteme (`github-actions`, `npm` unter `/web`, `nuget` per `directories`-Wildcards `/src/*`+`/tests/*`, weil die Solution das neue `.slnx`-Format ist), wöchentlich, Minor+Patch gruppiert zu je einem PR, Majors einzeln. Bewusst **kein** `npm audit`-Gate in der CI — eine Advisory in einer transitiven Abhängigkeit soll ein reviewbarer PR sein, kein roter Haken, der jeden unabhängigen Push nach `main` blockiert. Ebenfalls bewusst weggelassen: der NuGet-Cache-Schritt aus dem Review (bräuchte `packages.lock.json` in allen Projekten für einen brauchbaren Cache-Key; der Restore kostet ~30 s — das Lockfile-Regime wäre teurer als der Gewinn). S4-16 ist damit vollständig abgeschlossen.

**S4-17 — `COPY src/ src/` statt `COPY . .`, tote `build`-Stage entfernt.** Der Repo-Root-Kontext zog `web/`, `tests/` und `docs/` in den Layer-Hash — eine reine Frontend-Änderung baute den Worker von Grund auf neu. Jetzt kopiert die Build-Stage nur `src/`, und der separate `dotnet build`-Schritt, dessen Output nie kopiert wurde, ist weg (`dotnet publish` baut selbst). Beide Images lokal neu gebaut, Stack healthy, `/api/health` 200.

### 2026-08-05 — Z1-Rest + S3-35 + S3-36: payloadfreier `GET /api/health`, Container-HEALTHCHECKs, Pull-Monitor als Dead-Man's-Switch

**Betrifft:** `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs` · `src/EmotePurge.Api/Program.cs` · `src/EmotePurge.Worker/WorkerHealthPublisher.cs` · `src/EmotePurge.Api/Dockerfile` · `src/EmotePurge.Worker/Dockerfile` · `tests/EmotePurge.Api.Tests/{ApiFactory,ApiHealthEndpointTests}.cs` · `docs/Architectur.md`

**Neuer öffentlicher `GET /api/health`: der Statuscode ist die ganze Antwort.** 200 nur, wenn der Worker-Snapshot existiert und `connected` ableitet; 503 bei `disconnected`, `stale` und fehlendem Snapshot — der Dead-Man-Fall „Worker weg ⇒ Redis-Key abgelaufen" liest sich damit als Ausfall, nicht als „keine Daten, alles gut". Der 7TV-Zustand fließt bewusst **nicht** ein: der periodische REST-Resync hält die Emote-Daten auch ohne Event-Socket korrekt, ein 7TV-Schluckauf ist „degraded", nicht „down". Eigene Rate-Limit-Policy `PublicHealth` (30/min, faktisch per IP): die legitimen Aufrufer sind Maschinen mit festem Takt. Das bestehende `GET /api/worker/health` (Badge, 200 mit Payload) bleibt unverändert — Z1 ist damit komplett: Admin-Detail admin-only, Badge minimal, Maschinen-Endpoint payloadfrei.

**S3-35 — HEALTHCHECK im Dockerfile, nicht in Compose.** Api: `curl -f http://localhost:8080/api/health` (curl wird im aspnet-Image nur dafür installiert); die Semantik „der Api-Container zeigt unhealthy, wenn die *Pipeline* steht" ist die vom Review gewollte — ein grüner Api-Container vor totem Worker ist genau der Zustand, der nicht gesund aussehen darf. Worker: `WorkerHealthPublisher` touched nach jedem **vollständig** erfolgreichen Publish-Zyklus die Datei aus `Worker:HeartbeatFilePath` (per `ENV` im Dockerfile auf `/tmp/alive` gesetzt — Pfad und HEALTHCHECK leben in derselben Datei; lokal ohne die ENV bleibt der Touch aus), der HEALTHCHECK prüft `find -mmin -2`. Ins Image statt in die Compose-Dateien, damit Portainer-Stack und Dev denselben Check aus demselben Artefakt bekommen. Wichtig fürs Erwartungsmanagement: `restart: unless-stopped` startet einen unhealthy Container **nicht** neu — der Check ist Sichtbarkeit in `docker ps`/Portainer plus Futter für Monitoring, kein Auto-Heal.

**S3-36-Abschluss — Pull statt Push.** Der im Review vorgeschlagene `healthchecks.io`-Push aus dem **Worker** ist verworfen — nicht mangels Konto (healthchecks.io ist seit dem 05.08. im Einsatz: es überwacht Kuma selbst per Heartbeat sowie die Backup-Jobs, Alarme bewusst per E-Mail als Zweitkanal), sondern weil er redundant wäre: der bestehende Kuma-Pull-Monitor wird von `https://emotepurge.app/` auf `https://emotepurge.app/api/health` umgestellt (Handgriff in der Kuma-UI), und mit der 503-Semantik **ist** der Pull der Dead-Man's-Switch — Worker tot ⇒ Key läuft ab ⇒ 503 ⇒ Telegram. Fällt stattdessen Kuma/NAS aus, meldet das der healthchecks.io-Heartbeat per E-Mail. Beide Ausfallrichtungen sind damit ohne Worker-Code abgedeckt. Die Log-Aggregation wird als bewusste Minimal-Lösung geschlossen: die Logs sind strukturiert (Messung 2026-08-01: 118/118 Templates), rotieren seit der VPS-Härtung hostweit, und der Zugriffsweg (Portainer/SSH) ist im infra-docs-Bericht dokumentiert — ein Aggregationsstack steht in keinem Verhältnis zu einem Zwei-Container-Deployment. Wächst der Bedarf, ist Backlog-Idee B3 (Health-Historie) das Ventil.

### 2026-08-05 — S2-21 + S3-38: Ressourcenlimits für `api`/`worker`, Dev-Compose-Namen von Prod getrennt

**Betrifft:** `docker-compose.yml` · `docker-compose.prod.yml` · `scripts/backup-postgres.sh` · `docs/Backup-und-Restore.md`

**S2-21 — `deploy.resources.limits` (1.0 CPU / 512 M) für `api` und `worker` in beiden Compose-Dateien.** Der VPS beherbergt eine zweite, projektfremde App; ohne Limit kann ein amoklaufender Container den ganzen Host mitreißen. `deploy.resources.limits` wird von Docker Compose v2 auch ohne Swarm angewendet. Die Werte sind bewusst Startwerte aus dem Review — der Lasttest (Modul E kommt vor dem Stresstest) kalibriert sie; `postgres`/`redis` bleiben ohne CPU-/RAM-Limit (Redis ist über `maxmemory` bereits gedeckelt, Postgres soll im Zweifel nicht das Opfer sein).

**S3-38 — nur die Dev-Seite wurde umbenannt, absichtlich nicht Prod.** `docker-compose.yml` heißt jetzt `name: emote-purge-dev` mit Containern `emotepurge-dev-*`; damit lösen Dev und Prod nicht mehr auf denselben Projekt-, Netz- und Volume-Namen auf, und ein versehentliches `docker compose up` aus einem Checkout auf dem VPS hängt sich nicht mehr an das Produktions-`postgres-data`. Der Report schlug vor, die **Prod**-Datei umzubenennen — das hätte aber das bestehende Prod-Volume verwaist (neuer Projektname ⇒ neuer Volume-Name ⇒ Api startet gegen leere DB). Lokale Dev-Daten wurden per Volume-Kopie (`emote-purge_*` → `emote-purge-dev_*`) übernommen; die alten Volumes liegen als Fallback weiter da. `scripts/backup-postgres.sh` zielt mit seinem `emotepurge-postgres`-Default seither nur noch auf Prod.

### 2026-08-05 — S3-34: Migration-Fail-Fast-Guard statt Auto-Migrate

**Betrifft:** `src/EmotePurge.Core/Services/IPendingMigrationGuard.cs` · `src/EmotePurge.Infrastructure/Services/PendingMigrationGuard.cs` · `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs` · `src/EmotePurge.Api/Program.cs` · `src/EmotePurge.Worker/Program.cs` · `tests/EmotePurge.Infrastructure.Tests/{Fixtures/PostgresFixture,Integration/PendingMigrationGuardTests}.cs` · `tests/EmotePurge.Api.Tests/ApiFactory.cs`

**`IPendingMigrationGuard` statt Auto-Migrate.** Migrationen bleiben in diesem Projekt bewusst manuell; der Guard macht den vergessenen Schritt sichtbar, statt ihn nachzuholen: Api **und** Worker rufen beim Start `EnsureNoPendingMigrationsAsync()` auf, das bei ausstehenden Migrationen mit deren Namen wirft — aus stillen 500ern (`42703`) wird eine Crash-Loop in Portainer, deren Log die fehlende Migration benennt. Konsequenz fürs Deployment: ein neues Image gegen eine nicht migrierte Prod-DB **startet jetzt absichtlich nicht mehr** — die Reihenfolge „erst Migration, dann Deploy" aus CLAUDE.md ist damit erzwungen, nicht mehr nur Disziplin. `ApiFactory` substituiert den Guard, weil die container-freien Api-Tests sonst beim Boot die Platzhalter-DB anwählen würden; `PostgresFixture` kann jetzt Kontexte auf eine zweite, unmigrierte DB desselben Containers ausstellen (`CreateDbContext(databaseName)`), womit der Throws-Pfad gegen echtes Postgres getestet ist.

### 2026-08-05 — Offene Frage 17: `Cache-Control: no-store` auf allem unter `/api/`

**Betrifft:** `src/EmotePurge.Api/Program.cs` · `tests/EmotePurge.Api.Tests/ApiCacheControlTests.cs`

**Kein API-Endpoint setzte bisher irgendein `Cache-Control`; ob ein Proxy Antworten cacht, war damit dessen Entscheidung.** API-Antworten sind per-User-Daten hinter Cookie-Auth und dürfen nie aus einem Shared Cache kommen. Gesetzt in der bestehenden Security-Header-Middleware (greift damit auch für den `/api/{**rest}`-404-Fallback, der an allen Endpoint-Gruppen vorbeiläuft); die Static-/SPA-Auslieferung behält ihre differenzierte `immutable`/`no-cache`-Strategie aus dem Eintrag vom 2026-07-31.

### 2026-08-05 — VPS-Härtung abgeschlossen: Repo-Doku an den neuen Host-Stand angeglichen

**Betrifft:** `docs/VPS-Reverse-Proxy.md` · `docs/Backup-und-Restore.md` · `docs/Review-2026-07-29-Umsetzung.md` · `CLAUDE.md`

**Die VPS-/Homelab-Härtung (19 Befunde, 2026-08-04/05) ist abgeschlossen; ihr Bericht lebt außerhalb dieses Repos** — privates Repo `sensitron/infra-docs`, `VPS-und-Homelab-2026-08-04.md`. Dorthin gehört künftige Infra-Doku; dieses Repo dokumentiert nur die Verträge, die App-Code und Host miteinander eingehen. Vier solcher Verträge hat die Härtung verändert, die Repo-Doku ist entsprechend nachgezogen:

1. **nginx:** `/api/` läuft jetzt über das Snippet `emotepurge-hardening.conf` — eigene Rate-Zone `api`, `proxy_buffering off` explizit, `proxy_read_timeout 3600s` (der SSE-Prod-Punkt aus dem Eintrag vom 2026-08-01 ist damit doppelt abgesichert), Anti-Bot-444-Regexe; hostweit Catchall-`444`-default_server und `server_tokens off`. Die alten Aussagen „60-s-Default-Timeout" und „eine geteilte Rate-Zone für alles" in `VPS-Reverse-Proxy.md` waren damit überholt. Security-Header setzt weiterhin **allein die Api** — nie in nginx doppeln.
2. **Backup (S1-2-Einrichtung):** Cron 03:00 → `/var/backups/emotepurge` (14 d) → täglicher NAS-Pull per rrsync-eingesperrtem Key (30 d, ohne `--delete`) → rclone-Crypt nach OneDrive (60 d). Off-Site also pull-seitig statt des dokumentierten B2-Push-Vorschlags; `OFFSITE_ENABLED` im Skript bleibt bewusst `0`.
3. **Docker-Log-Rotation** („Offene Frage 15" des Reviews vom 2026-07-29): hostweit in `daemon.json` (`json-file`, 10 MB × 3); greift je Container erst beim nächsten Redeploy.
4. **S3-36 teilerledigt:** externer Uptime-Check existiert (Uptime Kuma auf dem NAS, 60-s-Check auf `emotepurge.app`, Telegram-Alerts) — er sieht aber nur die SPA-Auslieferung. Dead-Man's-Switch für den Worker und Log-Aggregation bleiben offen; Welle E (S2-21, Z1+S3-35, S3-34, S3-38, S4-15/16/17/18-Reste, S2-20) bleibt insgesamt offen.

Betrieblich relevant für alle Anleitungen mit `<VPS-USER>`-Platzhaltern (Prod-Migration in `CLAUDE.md`, Backup-Doku): **SSH auf den VPS nur noch als sudo-User** — `PermitRootLogin no`, root-Passwort gesperrt. Außerdem wurden am 2026-08-04 das Prod-DB-Passwort und das Twitch-Prod-App-Secret rotiert (Werte in Portainer-Env + Vaultwarden); `AUTH_TWITCH_TOKEN_ENCRYPTION_KEY` bewusst **nicht** rotiert — nie exponiert, und eine Rotation würde alle gespeicherten Twitch-Tokens entwerten.

### 2026-08-04 — Duplikat-Emote-Namen: der ordinale Vergleich ist 7TVs eigene Semantik, nicht nur unsere Chat-Matching-Regel

**Betrifft:** `src/EmotePurge.Infrastructure/Services/{DuplicateEmoteNameQueryService,DuplicateEmoteNameTracker,SevenTvSyncService}.cs` · `docs/Feature-Ideen-2026-08-01.md` (A16)

**Der `StringComparer.Ordinal` in der Duplikat-Erkennung hat jetzt einen zweiten, härteren Grund als „spiegelt das Chat-Matching".** Anlass war eine Nutzerfrage: warum enthalten manche 7TV-Sets doppelte Namen, obwohl ein zweifach importiertes Purge-Protokoll abgelehnt wird? Gelesen in 7TVs Quellcode (`SevenTV/SevenTV`, `apps/api/src/http/v3/gql/mutations/emote_sets/`, zeichengleiche Logik in v4 `emote_set/operation.rs`): **beide** Schreibpfade prüfen die Alias-Eindeutigkeit — `ADD` mit `emote_set.emotes.iter().position(|e| e.alias == alias)` → `this emote has a conflicting name`, `UPDATE`/Rename mit `.any(|e| e.alias == new_name)` → `emote name conflict`. Reines Rust-`String`-`==`, kein `to_lowercase`, keine Normalisierung; die Validatoren prüfen nur den Zeichenvorrat. Unsere Vergleiche (Query-Service, `SevenTvSyncService.RefreshMatchCacheAsync`, `DuplicateEmoteNameTracker`) treffen diese Semantik damit exakt — was das Workspace-Banner meldet, ist auch für 7TV eine Kollision, und dass `Emote`/`emote` bei uns bewusst **keine** ist (Test seit 2026-08-03), ist keine Nachlässigkeit, sondern dieselbe Regel. Ein „hilfsbereiter" `OrdinalIgnoreCase` würde ab jetzt zwei Systeme auseinanderlaufen lassen.

**Ein fehlender Alias ist kein Schlupfloch, deshalb bleibt die Erkennung auf `Emote.Name` und braucht kein zweites Feld.** `EmoteSetEmote.alias` ist ein `String`, kein `Option<String>`, und wird beim Hinzufügen ohne Angabe mit `emote.default_name` befüllt (`let alias = name.unwrap_or_else(|| emote.default_name.clone())`). Einen „kein Alias gesetzt"-Zustand gibt es in 7TVs Datenmodell nicht; zwei aliaslose Emotes mit gleichem Basisnamen kollidieren genauso. Dass wir `emotes[].data.name` gar nicht erst deserialisieren, kostet für diesen Zweck also nichts.

**Woher die Duplikate dann kommen: aus Schreibpfaden *neben* der geprüften Mutation.** Belegt ist das Zusammenführen mehrerer **Origin-Sets** ([SevenTV/API#253](https://github.com/SevenTV/API/issues/253): „the resulting set will have … duplicates and some emotes go missing"; dazu passend existieren bis heute `EmoteSetEmoteFlag::OverrideConflicts` und `EmoteSet.origin_config`). Die Go→Rust-Datenmigration wäre ein zweiter solcher Weg — dafür wurde kein Code gelesen, das bleibt Vermutung. Ebenfalls nur *gelesene Abwesenheit*, nicht bewiesen: der Rust-Rewrite hat die ID-Prüfung des alten Go-Backends (`ErrEmoteAlreadyEnabled`, „dasselbe Emote zweimal") ersatzlos gestrichen, es scheint also nichts zu verhindern, dieselbe `EmoteId` mit abweichendem Alias erneut ins Set zu legen. Das deckt sich mit dem Live-Befund vom 2026-08-03 (`peepoSex` zweimal mit identischer Id in einem Set) und ist der Grund, warum die Koaleszierung in `RefreshMatchCacheAsync` und der Unique-Index `(ChannelId, SevenTvEmoteId)` beide bleiben müssen: 7TVs Set-Invarianten sind schwächer als unsere. **Folge für A16** (Emote-Liste importieren): ein Set-Kopieren muss damit rechnen, dass ein Teil der Liste an der Namensprüfung abprallt, und das als Ergebnis zeigen — den Alias wegzulassen ist nach obigem kein Ausweg.

### 2026-08-03 — LIVE-Badge in den Channel-Listen: der Worker publiziert den Live-Poll-Momentzustand als Redis-TTL-Key (B10)

**Betrifft:** `src/EmotePurge.Core/Services/{ITwitchLiveStatusReader,IAdminChannelQueryService,IMyChannelsService}.cs` · `src/EmotePurge.Infrastructure/Redis/TwitchLiveStatusStore.cs` · `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs` · `src/EmotePurge.Infrastructure/Services/MyChannelsService.cs` · `src/EmotePurge.Worker/TwitchLivePollWorker.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `tests/EmotePurge.Infrastructure.Tests/{Integration/TwitchLiveStatusStoreTests,Integration/MyChannelsServiceTests,Unit/ChannelLiveStatesTests}.cs` · `web/src/app/core/admin/{admin.model,admin.service}.ts` · `web/src/app/core/channels/channel.model.ts` · `web/src/app/features/admin/admin-channels-page.ts` · `web/src/app/features/overview/overview-page.{ts,html}` · beide Locale-Dateien

**Neuer Redis-Vertrag `worker:live-status`, Muster `worker:roster` — die Api bleibt Helix-frei.** `TwitchLivePollWorker` (A10 Stufe 1) verdichtete sein Poll-Ergebnis bisher nur zu `ChannelLiveDay`-Tagesminuten und warf den Momentzustand weg. Jetzt publiziert er nach jedem erfolgreichen Poll zusätzlich das Set der Live-Logins (normalisiert, Regel 9) plus Poll-Zeitstempel als JSON-TTL-Key (`JsonSerializerOptions.Web`, Reader und Writer in einer Infrastructure-Klasse `TwitchLiveStatusStore` hinter zwei Core-Interfaces — anders als beim Health/Roster-Paar hält der Worker hier keinen eigenen Zustand, also hätte eine Trennung nur das Wire-Format dupliziert). Publiziert wird **vor** dem `streams.Count == 0`-Early-Return: „niemand ist live" ist ein Poll-Ergebnis wie jedes andere; nur ein `null`-Ergebnis (Fehler) publiziert nichts — dieselbe Regel wie bei der Coverage. TTL ist 2× das Poll-Intervall: ein einzelner verpasster Tick liest sich nicht als „unbekannt", aber ein toter Worker läuft in „unbekannt" aus, statt ewig „offline" zu behaupten. Die Alternative — die Api fragt Helix selbst — war schon in der Idee verworfen: der Worker bleibt der einzige Halter des App-Access-Tokens (s. A10-Eintrag zu konkurrierenden Client-Credentials-Grants), Transportweg ist deshalb Redis.

**Dreiwertiger `liveState` statt Boolean, die Asymmetrie zentral in `ChannelLiveStates.Derive`.** Der Poll fragt nur bot-aktive Channels — Abwesenheit aus dem Live-Set beweist also nur für die etwas, und „Snapshot fehlt" ist keine Aussage über irgendwen. Daher drei Werte als String-Vertrag (`live`/`offline`/`unknown`, Wire-Wert = benannter Wert, gleiche Überlegung wie `ApiErrorCodes`): `offline` gibt es ausschließlich für einen gepollten Channel, der im Set fehlt; alles andere ist `unknown`. Beide Listen-Antworten tragen den Zustand je Channel plus einen snapshot-weiten `livePolledAtUtc`: `GET /api/admin/channels` wird dafür vom nackten Array zum `{ channels, livePolledAtUtc }`-Wrapper, `MyChannelsResultDto` bekommt das Feld trailing-defaulted. Der Admin-Drilldown reichert dieselbe Row-Shape an — Liste und Drilldown dürfen nicht verschiedene Zustände melden (dieselbe Regel, mit der `AdminChannelDto` überhaupt geteilt wird).

**Frontend: Badge ohne Echtzeit-Versprechen, `unknown` ohne Badge.** In beiden Listen ein `StatusBadge` — `success` für LIVE (das grüne „online"-Idiom, dasselbe Token wie die A10-Stream-Bänder im Drilldown), `neutral` für Offline, und `unknown` rendert **nichts**: fehlende Daten dürfen nicht wie „offline" aussehen. Der Tooltip „Stand vor x min" (aus `livePolledAtUtc`) trägt die Ehrlichkeit über den bis zu 5 Minuten alten Stand. Kein SSE-Anschluss: die Listen laden den Zustand beim Fetch mit, 5-Minuten-Granularität rechtfertigt keine Push-Updates — und kein neues Dauer-Control (Frontend-Zurückhaltung, so schon die Idee).

### 2026-08-03 — `TwitchConnectionWatchdog`: Stale-Erkennung misst empfangene IRC-Frames statt Chat-Nachrichten

**Betrifft:** `src/EmotePurge.Worker/{TwitchChatManager,ITwitchChatManager,TwitchConnectionWatchdog,TwitchWatchdogPolicy,WorkerHealthPublisher}.cs` · `src/EmotePurge.Core/Services/IWorkerHealthReader.cs` · `src/EmotePurge.Api/Health/WorkerHealthStatus.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `tests/EmotePurge.Worker.Tests/TwitchWatchdogPolicyTests.cs`

**Der Watchdog misst jetzt Verbindungs-Gesundheit statt Chat-Aktivität — und revidiert damit die Schwellen-Logik der Einträge vom 2026-07-25/26/27.** `LastMessageReceivedUtc` wird nur bei einer empfangenen Chat-Nachricht gestempelt; der Watchdog nutzte es als Stale-Referenz und erzwang deshalb nachts (alle Channels offline und still) alle ~5–10 Minuten einen Reconnect einer völlig gesunden Verbindung. Dabei liefert der Twitch-IRC-Server selbst ca. alle 5 Minuten ein `PING` an jeden Client — der Liveness-Beweis kam also längst an und wurde nur nicht beobachtet. Gegen die Quellen verifiziert (TwitchLib.Communication 2.0.1 @ `d1904be`, TwitchLib.Client 4.0.1): der `WebSocketClient` reicht jede Text-Frame ungefiltert an alle `OnMessage`-Abonnenten durch, und `TwitchClient` feuert vor dem Parsen jeder Zeile sein öffentliches `OnSendReceiveData` — dort hängt jetzt ein Handler, der bei `Direction == Received` `LastFrameReceivedUtc` stempelt (Interlocked-Ticks wie gehabt). Bewusst `OnSendReceiveData` statt eines eigenen Hooks auf dem Communication-Client: schon zeilenweise gesplittet, und der Hook läuft über das bestehende `WireUpClient`/`UnwireClient`-Muster automatisch beim Recreate mit. Der Handler muss synchron-exceptionfrei bleiben — ein Wurf vor dem ersten `await` würde die Multicast-Kette abbrechen, bevor TwitchLibs eigener PONG-Handler läuft.

**Schwelle 15 Minuten (3× Twitchs PING-Takt), Entscheidung pur in `TwitchWatchdogPolicy`.** Die Stale-Schwelle steigt von 5 auf 15 Minuten: ein ausgefallener Server-PING ist Jitter, drei in Folge sind ein toter Socket. Cooldown-Logik bleibt (getrennte 1-Minute-Schiene für „Client meldet sich selbst als getrennt"), liegt aber jetzt mit der ganzen Entscheidung uhr- und TwitchLib-frei in `TwitchWatchdogPolicy` (Muster `ReconnectPolicy`, Tests container-frei in `Worker.Tests` — Regel 11). Die echte Silent-Drop-Erkennung bleibt unverändert gültig: kein Frame = tot, nur dass „Frame" jetzt das misst, was es soll. Die Health-Kette zieht mit: `WorkerHealthSnapshot.TwitchLastFrameUtc` (trailing-defaulted wie immer, Rolling-Deploy-sicher), und `WorkerHealthStatus.StaleAfterSeconds` in der Api — dieselbe Zahl 300 an anderer Codestelle, die sonst auf gesunder Verbindung „stale" gemeldet hätte — geht auf 900 und leitet vorrangig aus `TwitchLastFrameUtc` ab (Fallback-Kette für ältere Worker-Payloads). Der Admin-Health zeigt `lastFrameReceivedUtc` analog `sevenTv.lastFrameUtc`; die öffentliche Badge-Antwort bleibt unverändert.

**Helix-Live-Check kehrt als Diagnose zurück, nicht als Entscheidung.** Der Eintrag vom 2026-07-27 hatte einen „ist überhaupt jemand live?"-Check verworfen, u. a. weil es keinen App-Token-Flow gab — den gibt es seit A10. Vor jedem erzwungenen Reconnect loggt der Watchdog jetzt best-effort, wie viele der gejointen Channels laut Helix live sind: Stille *erklären* kann der Check, Verbindungs-Gesundheit *beweisen* nicht (dafür ist der Frame-Zeitstempel da), deshalb fließt er ausdrücklich nicht in die Entscheidung ein.

### 2026-08-03 — Live-Abdeckung pro Tag: App-Access-Token, Helix-Streams-Poll und Stream-Tage im Drilldown (A10 Stufe 1)

**Betrifft:** `src/EmotePurge.Core/Twitch/{ITwitchAuthClient,ITwitchAppTokenProvider,ITwitchHelixClient,TwitchModels}.cs` · `src/EmotePurge.Infrastructure/Twitch/{TwitchAuthClient,TwitchAppTokenProvider,TwitchHelixClient,TwitchApiDtos}.cs` · `src/EmotePurge.Core/Entities/ChannelLiveDay.cs` · `src/EmotePurge.Core/Services/{ILiveCoverageService,IUsageStatQueryService}.cs` · `src/EmotePurge.Infrastructure/Services/{LiveCoverageService,UsageStatQueryService}.cs` · `src/EmotePurge.Infrastructure/Migrations/20260803114737_AddChannelLiveDay` · `src/EmotePurge.Worker/{TwitchLivePollWorker,Program}.cs` · beide Compose-Dateien + `.env.example` · `tests/EmotePurge.Infrastructure.Tests/{Unit/TwitchAppTokenProviderTests,Integration/LiveCoverageServiceTests,Integration/UsageStatQueryServiceTests}.cs` · `web/src/app/core/usage-stats/usage-stat.model.ts` · `web/src/app/shared/emotes/{usage-series,usage-sparkline,emote-drilldown-dialog}.ts` · beide Locale-Dateien

**Stufe 1 von Idee A10 — erst die Daten, dann die Metrik.** Das Drilldown-Chart fällt an stream-freien Tagen natürlich auf 0 und liest sich dann als „Emote stirbt", obwohl der Kanal schlicht offline war. Diese Runde erfasst deshalb nur die Live-Abdeckung und markiert sie im Chart; die eigentliche A10-Metrik („Nutzung pro Live-Stunde", umschaltbar) ist ausdrücklich Stufe 2 — eine still umgestellte Default-Metrik würde niemandem erklären, warum sich alle Zahlen geändert haben (so schon die Idee selbst).

**Der erste App-Access-Token im Projekt, bewusst getrennt vom User-Flow.** `GET /helix/streams` braucht keinen Scope und läuft per Client-Credentials-Grant — `ITwitchAuthClient.GetAppAccessTokenAsync` fasst die OAuth-User-Scopes (`RequestedScopes`) nicht an, denn jede Scope-Änderung erzwingt Re-Login aller Nutzer. Der Token (~60 Tage Laufzeit) liegt in einem Singleton-`TwitchAppTokenProvider` mit Single-Flight-Semaphor: Twitch invalidiert beim neuen Grant den vorherigen App-Token, parallele Grants würden sich also gegenseitig widerrufen — aus demselben Grund wird er auch nicht persistiert (ein Grant pro Prozessstart ist billig; zwei Prozesse, die sich einen gespeicherten Token teilen, kämpften darum). Schlägt ein Helix-Batch fehl, wird der Cache invalidiert (wahrscheinlichste Ursache: Secret-Rotation) und der nächste Tick holt frisch. Der Worker bekommt dafür erstmals `Auth__Twitch__ClientId/ClientSecret` in beiden Compose-Dateien; fehlen sie, deaktiviert sich der Poll mit genau einer Warnung statt im Takt zu jammern.

**Poll statt EventSub, Namen statt IDs, Minuten statt Flag.** `TwitchLivePollWorker` pollt alle `Twitch:LivePollIntervalSeconds` (Default 300 s) alle aktiven Channels in 100er-Batches über `user_login` — `Channel.TwitchChannelId` ist nullable und nicht überall gesetzt, die normalisierten Namen sind es (Helix liefert Logins lowercase, `LiveCoverageService` normalisiert trotzdem nach Regel 9). Offline = Channel fehlt im `data`-Array; ein `null`-Ergebnis (Fehler) wird ausdrücklich **nicht** als „alle offline" gelesen, der Tick wird übersprungen. Jeder Poll, der einen Channel live sieht, schreibt der Zeile `ChannelLiveDay(ChannelId, Date, LiveMinutes)` eine Intervalllänge gut (Covering-Unique-Index wie bei `UsageStat`, Kappung bei 1440): bewusst approximativ — der Konsument von Stufe 1 ist ein Tages-Marker, und für Stufe 2 reichen Minuten pro Tag. `StartedAtUtc` wird im DTO schon mitgeführt, aber noch nicht gespeichert. Ein Tag ohne Zeile heißt „keine Daten", nie „offline" — Daten existieren erst ab Einführung, und genau deshalb rendert das Frontend ältere Bereiche unmarkiert statt „0 Stream-Tage" zu behaupten.

**`/daily` liefert die Live-Tage mit, kein zweiter Request.** `EmoteUsageSeriesDto.LiveDays` ist — anders als `First-/LastUsedDate` — bereichsbegrenzt, weil der Konsument sie exakt unter das gerenderte Fenster legt. Im Chart sind es volle Hintergrund-Bänder hinter der Polyline (nicht ein Bodenstreifen: ein 0-Nutzungs-Tag zeichnet die Linie genau auf die Unterkante, also exakt dorthin, wo der Marker erklären soll), `success-dot`-Token mit niedriger Fill-Opacity — das grüne „online"-Idiom, und von den vorhandenen Dot-Tokens das einzige ohne falsche Warn-Konnotation. Die Bedeutung trägt wie beim Peak die Textzeile darunter („Live an x von y Tagen", mit Legenden-Swatch), die Grafik bleibt Verstärkung (WCAG 1.4.1, dieselbe Arbeitsteilung wie bei der Sparkline selbst).

---

### 2026-08-03 — Der Export-Dialog bekommt eine Bereichswahl: sichtbare Liste (Default) oder Grid-Auswahl

**Betrifft:** `web/src/app/shared/export/export-dialog.ts` · `web/src/app/shared/export/usage-export.{ts,spec.ts}` · `web/src/app/features/usage-stats/usage-stats-page.ts` · `web/src/app/features/voting/vote-session-detail-page.ts` · beide Locale-Dateien (`export.scope*`)

**Erweiterung der A12-Entscheidung „Exportiert wird die sichtbare Liste":** Existiert auf der Usage-Stats-Seite eine Grid-Selektion (dieselbe, die Mass-Delete und „Zur Abstimmung stellen" treibt), bietet der Export-Dialog zusätzlich „Auswahl (n)" an — als Radio-Wahl im Dialog, nicht als zweiter Button. Der Default bleibt die gefilterte Liste, auch bei bestehender Auswahl: die Selektion gehört primär den anderen beiden Kommandos, und ein Export, der sich stillschweigend auf sie verengt, wäre genau die Überraschung, die der A12-Eintrag für den umgekehrten Fall (900 statt 12 Zeilen) ausschließt. Die Zeilenzahl im Dialog folgt der Wahl; der `filteredHint` erscheint nur im Listen-Fall, weil er im Auswahl-Fall schlicht nicht stimmt.

**Der Dialog schließt jetzt mit `{format, scope}` statt nur `format`**, und die Usage-Seite baut das `UsageExportInput` erst nach dem Schließen aus dem gewählten Scope — `selection.selectedItems()` ist dafür aus demselben Grund vollständig wie bei `selectedForDelete`: jeder Pfad, der Zeilen aus `sortedEmotes()` entfernt, leert oder beschneidet die Selektion. Die Voting-Detailseite übergibt `selectionCount: 0` und bekommt nie eine Wahl angezeigt — ihr Subset ist der Ballot selbst. Im JSON-Envelope steht der gewählte Umfang als neues `meta.scope` (`visible` | `selection`), additiv ohne `formatVersion`-Bump; `meta.filtered` behält seine Bedeutung „ein Grid-Filter war aktiv" unabhängig vom Scope.

---

### 2026-08-03 — `FirstSeenAt` kommt jetzt aus der v4-GraphQL-API: das v3-`timestamp` war das Upload-Datum

**Betrifft:** `src/EmotePurge.Core/SevenTv/SevenTvModels.cs` · `src/EmotePurge.Infrastructure/SevenTv/{SevenTvApiDtos,SevenTvEmoteJsonMapper,SevenTvApiClient}.cs` · `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs` · `src/EmotePurge.Infrastructure/Migrations/20260803110452_ResetMisattributedFirstSeenAt.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/SevenTvSyncServiceTests.cs` · `docs/Architectur.md`

**Befund aus dem Live-Test (Nutzer: „mein 7TV gibt es erst seit ~200 Tagen, das Drilldown sagt 1183"):** Das `emotes[].timestamp` der v3-REST-Antwort ist **nicht** das Set-Beitrittsdatum, sondern das Upload-/Erstellungsdatum des Emotes. Live bewiesen 2026-08-03 an einem 968er-Set: alle 968 `timestamp`-Werte sind ms-genau identisch mit dem ULID-Erstellungszeitpunkt der Emote-Id, während das echte `addedAt` aus der v4-GraphQL-API um Tage bis über ein Jahr abweicht. Die Annahme der Entscheidung vom 2026-08-01 („Property des Set-Eintrags") war zum Zeitpunkt der Umsetzung ungeprüft — seit 7TVs v4-Migration füllt die v3-Compat-Schicht das Feld aus dem Emote selbst.

**Quelle gewechselt, nicht die Semantik:** `FirstSeenAt` soll weiterhin das Set-Beitrittsdatum sein — es kommt jetzt aus `EmoteSetEmote.addedAt` der v4-GraphQL-API (`POST /v4/gql`, host-absolut am v3-BaseAddress vorbei; 500er-Seiten, Seiten-Cap als Runaway-Guard). Der `SevenTvApiClient` legt die Werte nach dem v3-Abruf über die Emote-Liste; schlägt der v4-Call fehl, bleiben sie `null` und der nächste Resync füllt nach — ein Datum ist es nicht wert, den Channel-Sync scheitern zu lassen. Das v3-`timestamp` wird gar nicht mehr gemappt.

**Aus Write-once wird Korrektur-bei-Abweichung.** Der Backfill in `UpsertEmote` schrieb nur bei `FirstSeenAt is null` — damit wären alle falschen Bestandswerte für immer falsch geblieben. Jetzt gewinnt ein abweichender v4-Wert (nur REST-Pfad, `null` überschreibt weiterhin nie); dadurch wandert auch das Datum eines erneut ins Set gelegten Emotes auf seinen jüngsten Beitritt. Unverändert gilt: die Korrektur zählt **nicht** als Inventaränderung (kein `channel.synced`-Sturm beim ersten Resync nach dem Deploy). Der Dispatch-Pfad stempelt bei `push` jetzt `UtcNow` — ein Push *ist* der Beitrittsmoment; `updated`-Einträge bleiben datenlos und warten auf den Resync.

**Migration `ResetMisattributedFirstSeenAt` nullt die Spalte komplett.** Aktive Emotes bekommen ihr korrektes Datum innerhalb eines Resync-Ticks zurück; archivierte werden nie mehr gesynct und zeigen dann ehrlich „unbekannt" statt selbstbewusst falsch. Die Korrektur-Logik machte den Reset für aktive Zeilen streng genommen überflüssig — er bleibt trotzdem, wegen genau dieser archivierten Zeilen. Nebeneffekt der Reparatur: die 14-Tage-Karenz und die Trend-Unterdrückung hängen an demselben Feld — ein altes Emote, das frisch ins Set kommt, bekommt jetzt korrekt seine Schonfrist (vorher nicht, weil sein Upload-Datum alt war).

---

### 2026-08-03 — Audit-Papierspur für Delete und Restore: `sync-restored` kommt doch, das Delete-Audit hängt nicht mehr am Race

**Betrifft:** `src/EmotePurge.Core/Entities/AuditLogEntry.cs` (`emotes.syncRestored`) · `src/EmotePurge.Core/Services/IEmoteService.cs` · `src/EmotePurge.Infrastructure/Services/EmoteService.cs` · `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs` (`POST /sync-restored`) · `tests/EmotePurge.Api.Tests/AuthFilterMatrixTests.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/EmoteServiceTests.cs` · `web/src/app/core/seven-tv/seven-tv-restore.service.ts` · `seven-tv-delete.service.ts` · `web/src/app/core/emotes/emote-admin.service.ts` · `web/src/app/shared/seven-tv/{run-progress-panel,mass-delete-panel}.ts` · `web/src/app/shared/audit/audit-actions.ts` · `web/src/app/core/audit/audit.model.ts` · beide Locale-Dateien · `docs/Architectur.md`

**Befund aus dem Live-Test des A6-Durchlaufs (Nutzerfrage: „gibt es beim Löschen und Wiederherstellen kein Audit-Log?").** Zwei Lücken, dieselbe Wurzel: (1) `MarkDeletedAsync` schrieb seinen `emotes.syncDeleted`-Eintrag nur, wenn der Aufruf mindestens eine Zeile **neu** archivierte — mit aktiviertem EventAPI-Live-Sync gewinnt aber fast immer der Worker das Race, und der reale Mass-Delete hinterließ **keinen** Eintrag. (2) Der Restore erschien überhaupt nur als anonymer `channel.resync` ohne Details — und unter dem Resync-Cooldown (429 vor dem Service) gar nicht.

**Delete: auditiert wird der gemeldete Zielzustand, nicht der Schreib-Gewinn.** Der Eintrag entsteht jetzt, sobald der Bookkeeping-Call ≥ 1 bekanntes Emote des Channels betrifft (`emoteCount` = Zielzustands-Zählung, dieselbe Zahl, die der Nutzer als Erfolg gemeldet bekommt). Ein wiederholter Report kann dadurch eine zweite Zeile schreiben — das Log protokolliert die Reports, ein Duplikat ist billiger als eine Lücke. **Nur das `channel.synced`-Live-Event bleibt an eine echte Zustandsänderung gebunden** — ein No-op darf weiterhin niemanden refetchen lassen.

**Restore: `POST /sync-restored` als Spiegel von `sync-deleted` — Revision der gleichnamigen Entscheidung vom 2026-08-02.** Damals war „kein `sync-restored`-Gegenstück" richtig begründet über die Zustands-Heilung (konservative Richtung, `UpsertEmote` nullt binnen 60 s) — aber die Papierspur heilt nicht von selbst, und genau sie hat der Live-Test vermisst. Der neue Endpoint (gleiche Gruppe, gleiche `Bookkeeping`-Policy, gleiche Leer-Body-Validierung) entarchiviert idempotent (`IsArchived = false`, `ArchivedAt = null`, already-active zählt als Erfolg) und schreibt `emotes.syncRestored` mit `emoteCount`. **Der A8-Resync bleibt zusätzlich bestehen** — er ist Reconciliation gegen 7TV als Autorität (Alias-Kollisionen, sonstige Drift), der Report ist Buchführung + Audit; keiner ersetzt den anderen. Frontend-seitig meldet der Restore-Service den Lauf mit derselben Retry-Policy wie der Delete (`syncReport` inkl. `partial`), und die Sync-Hinweis-Keys des `run-progress-panel` folgen jetzt `labelPrefix` statt hart `massDelete.*`.

---

### 2026-08-02 — Restore läuft im Browser über dieselbe Lauf-Mechanik wie der Delete

**Betrifft:** `web/src/app/core/seven-tv/seven-tv-run-engine.ts` (neu, + Spec) · `seven-tv-delete.service.ts` (auf die Engine umgestellt) · `seven-tv-restore.service.ts` (neu, + Spec) · `web/src/app/shared/seven-tv/run-progress-panel.ts` (umbenannt aus `delete-progress-panel.ts`) · `mass-delete-panel.ts` · `restore-confirm-dialog.ts` (neu) · `restore-panel.ts` (neu) · `web/src/app/shared/export/purge-run-export.ts` (neu, + Spec) · `channel-workspace-layout.ts` · beide Locale-Dateien (`massDelete.summary.*`, Top-Level `restore.*`) · `web/e2e/audit/ui-audit.audit.ts`

**Der Rest von Idee A6: Protokoll und In-App-Restore.** Nach einem Mass-Delete-Lauf gibt es jetzt eine Zusammenfassung mit Zählzeile, das Protokoll als JSON/CSV-Download (dritte `kind`-Ausprägung des Export-Envelopes, `kind: 'purge-run'`) und einen Wiederherstellen-Pfad. **Grundsatz 4 gilt unverändert:** Der Restore läuft als GQL-`ADD` im Browser, das 7TV-Schreib-Token bleibt im `sessionStorage` — und **das Protokoll enthält kein Token**, nur Emote-Ids und -Namen (per Spec gepinnt), weil eine herunterladbare Datei aus einem Zero-Knowledge-Feature genau diese Sorge auslöst.

**Eine Engine, zwei Services — keine Vererbung, kein Modus-Flag.** Von den ~550 Zeilen des Delete-Service waren ~350 Lauf-Mechanik (adaptives Pacing, Rate-Limit-Backoff mit Countdown, Token-Handling, Queue, Cancel), die für `ADD` wortgleich gilt: beide Mutationen ziehen Tickets aus demselben 7TV-Bucket `emote_set_change`, daher auch keine neue Tarierung. Die Mechanik lebt jetzt in `SevenTvRunEngine`; Delete- und Restore-Service halten je eine **eigene** Instanz (`isRunning` bleibt eindeutig), die Operation liefert nur die Mutation. Die Fehler-Keys bleiben geteilt unter `massDelete.errors.*` — nicht „aufräumen". `seven-tv-delete.service.spec.ts` lief nach dem Umbau unverändert grün als Regressionsbeweis.

**Restore-UX v1: Post-Run-Button und Protokoll-Import — die DB-gestützte Archiv-Liste bewusst nicht.** Der Post-Run-Button deckt den häufigsten Panikfall (falsche Auswahl, Panel noch offen) zum Nulltarif; der Import ist das Einzige, was einen Reload überlebt, und macht das Protokoll erst zum Sicherheitsnetz statt zur Quittung. Eine Archiv-Liste bräuchte Endpoint, Query und Pagination — und böte vor allem Emotes an, die jemand auf 7TV bewusst entfernt hat: ein anderes, riskanteres Feature. **Nur `status: 'done'`-Zeilen sind wiederherstellbar** (ein fehlgeschlagener Delete heißt: das Emote hat das Set nie verlassen — Re-Add wäre bestenfalls No-op, schlimmstenfalls Alias-Kollision), und **der Import validiert gegen Channel und aktive Set-Id** — die Datei kann Tage alt sein, der Channel kann sein Set gewechselt haben.

**Nach dem Restore genügt der A8-Resync, es gibt kein `sync-restored`-Gegenstück zu `sync-deleted`.** Bei `sync-deleted` wäre der veraltete Zustand in die gefährliche Richtung irreführend (längst gelöschte Emotes stünden als Löschkandidaten). Hier ist die Richtung konservativ — ein zurückgeholtes Emote steht kurz noch als archiviert — und heilt über `UpsertEmote` (`IsArchived = false`, `ArchivedAt = null`) binnen des 60-s-Resyncs von selbst. Also einmal `POST /resync` am Laufende; `429`/`resync_cooldown_active` wird als „kommt gleich von allein" gemeldet, nicht als Fehler. Das spart Endpoint, Service-Methode, Audit-Aktion und Api-Testfall.

**Form-Entscheidungen:** Der Restore übernimmt die Zweistufigkeit der destruktiven Konvention (§4.2) in der **Form**, nicht in der **Farbe** — Outline löst aus, im Dialog vollzieht `primary`-Solid: Restore ist konstruktiv, `danger` wäre eine Falschaussage. Der Bestätigungsdialog projiziert `belegt + wiederherzustellen` gegen die Kapazität und **warnt bei Überlauf, blockiert aber nicht** — 7TV ist die Autorität über sein eigenes Set, die Ablehnungen landen in der Fehlerliste. Das umbenannte `run-progress-panel.ts` wählt seine Wortfamilie über `labelPrefix: 'massDelete' | 'restore'` (dynamische Transloco-Keys nach bestehendem Muster, die Union hält sie auffindbar); neben „Schließen" steht ein Hinweis, solange das Protokoll nicht heruntergeladen wurde — `reset()` löscht die Zusammenfassung, die Datei ist das einzige dauerhafte Artefakt.

---

### 2026-08-02 — `Emote.ArchivedAt` wird geschrieben, aber noch nicht ausgeliefert

**Betrifft:** `src/EmotePurge.Core/Entities/Emote.cs` · `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs` (drei Stellen) · `src/EmotePurge.Infrastructure/Services/EmoteService.cs` · `src/EmotePurge.Infrastructure/Migrations/20260802195616_AddEmoteArchivedAt` · `tests/EmotePurge.Infrastructure.Tests/Integration/{SevenTvSyncServiceTests,EmoteServiceTests}.cs`

**Der Datenteil von Idee A6.** „Was haben wir wann rausgeworfen" ist ohne Zeitstempel nicht beantwortbar, und die Spalte lässt sich rückwirkend nie befüllen — sie jetzt zu schreiben ist der ganze Wert, auch wenn v1 sie noch nirgends ausliefert. Der einzige Konsument wäre eine DB-gestützte Archiv-Liste, und die ist bewusst nicht Teil von v1 (siehe den Restore-Eintrag).

**Vier Schreibstellen, ein Muster:** REST-Reconcile (`ReconcileAsync`), EventAPI-Pull (`ApplyEmoteSetUpdateAsync`), Frontend-Bookkeeping (`EmoteService.MarkDeletedAsync`) setzen `ArchivedAt = UtcNow` beim Archivieren; `UpsertEmote` nullt es beim Un-Archive. Zwei bewusste Feinheiten: `MarkDeletedAsync` stempelt nur **neu** archivierte Zeilen — eine vom Live-Sync bereits archivierte Zeile behält ihr früheres (genaueres) Datum, dieselbe Idempotenz-Logik wie „already-archived zählt als Erfolg". Und in `UpsertEmote` steht das Nullen **im** Änderungsblock, nicht in der Bedingung — sonst würde ein reines Backfill zur Inventaränderung und feuerte `channel.synced` für jede offene Seite (dieselbe Falle, die der `FirstSeenAt`-Eintrag dokumentiert; der bestehende Regressionstest deckt das mit ab).

**Kein Backfill, kein Index.** `null` auf einer archivierten Zeile heißt „archiviert, Datum unbekannt" — `LastSyncedAt` wäre kein ehrlicher Ersatz, weil der REST-Reconcile-Pfad es beim Archivieren nie gestempelt hat. Kein Index, weil v1 nicht auf der Spalte filtert. Additiv → in Prod von Hand **vor** dem Image-Deploy nachziehen (Tunnel, `migrations list` → `update` → `list`).

---

### 2026-08-02 — Der Emote-Drilldown bekommt einen eigenen, auf ein Emote gefilterten Endpoint

**Betrifft:** `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs` · `src/EmotePurge.Core/Services/IUsageStatQueryService.cs` · `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/UsageStatQueryServiceTests.cs` · `web/src/app/core/usage-stats/usage-stat.{model,service}.ts` · `web/src/app/shared/emotes/usage-series.ts` · `web/src/app/shared/emotes/usage-sparkline.ts` · `web/src/app/shared/emotes/emote-drilldown-dialog.ts` · beide Feature-Seiten

**Umsetzung von Idee A5.** `GET /api/channels/{c}/usage-stats/daily?emoteId=&from=&to=` liefert die Tagesreihe **eines** Emotes — serverseitig gefiltert, wie es die Idee als Muss benennt (900 Emotes × 90 Tage clientseitig zu zerlegen war die ausgeschlossene Variante). Der bestehende `GET /usage-stats` bleibt der unveränderte Debug-Endpoint aus Modul A.2; ihn zu parametrisieren hätte genau die Semantik geändert, deren Unantastbarkeit sein eigener Log-Eintrag festhält.

**Die Antwort ist dünn besetzt (nur Tage mit Nutzung), das Zero-Fill passiert im Client.** Der Flush schreibt ausschließlich Zeilen für Tage mit echter Nutzung; serverseitig Nullzeilen zu erfinden, nur um sie zu transportieren, wäre der teurere Weg — der Client baut für die Polyline ohnehin ein Array fester Breite (`fillDailySeries` in `shared/emotes/usage-series.ts`, pur und getestet). `FirstUsedDate`/`LastUsedDate` sind bewusst nicht bereichsbegrenzt (dieselbe Begründung wie `LastUsedDate` im Totals-Eintrag).

**Der Endpoint bleibt auf `ExternalApi` (40/min) — die naheliegende Lockerung wäre die falsche.** Teuer ist nicht der Handler (drei Index-only-Queries), sondern `UsageStatsAccessAuthorizationFilter` mit seinen zwei ungecachten 7TV-GQL-Calls für 7TV-Editor-Aufrufer. Die Policy auf `Bookkeeping` (120/min) zu heben würde den Druck nicht senken, sondern genau die Aufrufe verdreifachen erlauben, die 7TV belasten. Die Gegenmaßnahme liegt im Client: `UsageStatService` cached pro `(channel, emoteId, from, to)` für die Lebensdauer der Seite (`shareReplay`), und die Reihe wird ausschließlich beim expliziten Öffnen des Drilldowns geladen — nie bei Selektion oder Hover.

**Sicherheitsrand:** `emoteId` ist ein Client-Wert und wird immer gegen den Channel der Route aufgelöst — ein fremdes oder unbekanntes Emote ist ein nacktes `404`, dieselbe Antwort in beiden Fällen, damit eine geratene Id nicht bestätigt, dass sie anderswo existiert. Der Fall ist als Integrationstest gepinnt. **`IsArchived` wird nicht gefiltert**: ein archiviertes Emote ist aus dem Usage-Grid unerreichbar, steht aber als Ballot-Mitglied einer Subset-Session weiter sichtbar — und seine Historie ist echt. Keine neuen `ApiErrorCodes`: die Leiter von `/totals` wird als geteilter Helper wiederverwendet, leeres `emoteId` nutzt das vorhandene `emote_id_empty`. Keine Filterketten-Änderung → kein neuer Pflichtfall in `EmotePurge.Api.Tests`.

**UI: CDK-Dialog, nicht Popover, geöffnet über eine zweite Affordanz in der Karte.** Der Trigger sitzt in einer virtualisierten Kartenzeile — ein Popover-Panel würde beim Wegscrollen recycelt, und `popover.ts` ist per Vertrag für nicht-modale Panels aus einer Sticky-Leiste heraus gedacht (§7.1). Der Icon-Button liegt absolut in der Karte (`stopPropagation` gegen die Selektion, dasselbe Muster wie die Vote-Buttons); **`ROW_HEIGHT_PX` bleibt auf beiden Seiten unangetastet** (160/192 sind Vertrag, §8.5). Der „Vote-Stand in laufenden Sessions" kommt in v1 aus den ohnehin geladenen Kartenwerten der Vote-Detailseite (`keepVotes`/`deleteVotes`/`score`/`myVote` — erbt `null = zurückgehalten` geschenkt); eine eigene Tally-Query hinter dem schwächeren `UsageStatsAccessAuthorizationFilter` hätte eine zweite Ableitung der Secret-Ballot-Regel gebraucht und unterbleibt bewusst. Die Sparkline ist eine handgerollte SVG-Polyline (`vector-effect="non-scaling-stroke"`, `role="img"`, Peak-Textzeile darunter) — keine Chart-Bibliothek für eine Linie.

---

### 2026-08-02 — Der Export ist eine Client-Serialisierung, und eine verdeckte Spalte fehlt statt leer zu sein

**Betrifft:** `web/src/app/shared/export/` (neu: `file-download.ts` · `csv.ts` · `export-envelope.ts` · `usage-export.ts` · `voting-export.ts` · `export-dialog.ts`, alle mit Spec) · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/src/app/features/voting/vote-session-detail-page.{ts,html}` · beide Locale-Dateien (`export.*`) · `web/e2e/audit/ui-audit.audit.ts`

**Umsetzung von Idee A12: Download-Button (CSV/JSON) auf der Usage-Stats-Seite und der Vote-Session-Detailseite.** Der Export ist eine reine Client-Serialisierung des bereits geladenen Read-Models — es gibt bewusst **keinen** Export-Endpoint. Das `null` im Read-Model *ist* die serverseitige Sichtbarkeitsentscheidung (`includeTallies`, `includeRawUsage`): ein CSV-Endpoint müsste dieselbe Regel ein zweites Mal treffen, und genau diese Doppelung ist es, die Regel 7 für Fehlertexte verbietet. Nebenwirkung: kein neuer ApiErrorCode, keine neue Rate-Limit-Fläche, und der Download kann konstruktionsbedingt nie mehr sehen als die Seite.

**Exportiert wird die sichtbare Liste** (gefiltert + aktuell sortiert), nicht der Rohabruf — 900 Zeilen zu liefern, während der Nutzer auf 12 schaut, wäre überraschend. Die Zeilenzahl steht im Export-Dialog, der außerdem der einzige Ort ist, an dem stehen kann, *warum* Spalten fehlen („verdeckte Ergebnisse — Stimmenspalten nicht enthalten"). *(Revidiert 2026-08-03: Der Button stand auf der Usage-Stats-Seite ursprünglich in der Filterleiste, um diese Kopplung sichtbar zu machen — er war dort aber das einzige Kommando zwischen lauter Filter-/Sort-Controls und ist auf die Header-Aktionszeile neben „Aktualisieren" umgezogen, spiegelbildlich zur Vote-Session-Detailseite. Die Kopplung „Export = sichtbare Liste" kommuniziert der Dialog selbst über `export.filteredHint` und die Zeilenzahl.)*

**Verdeckte Werte: Spalte fällt weg, nicht leer.** Ist ein Wert in **allen** Zeilen `null` (Secret Ballot, manager-only Usage), entfällt die CSV-Spalte komplett; ist er nur in einzelnen Zeilen `null` (nie benutzt, archiviertes Ballot-Mitglied, nicht abgestimmt), bleibt die Zelle leer. Eine durchgängig leere Tally-Spalte liest `=SUMME()` als „0 Stimmen" — exakt die Fehldeutung, gegen die der Secret Ballot existiert. Im JSON werden zurückgehaltene Felder aus den `rows` **weggelassen** und stattdessen im `withheld: string[]`-Feld benannt — bloßes Weglassen könnte „nicht vorhanden" nicht von „zurückgehalten" unterscheiden.

**Das Envelope-Format ist der Vertrag, auf den A6 aufsetzt.** Jeder JSON-Export trägt `source: 'emotepurge'`, `kind` (`usage` | `voting` | `purge-run`), `formatVersion`, `exportedAt`, `channelName`, `withheld`, `meta`, `rows`. Das Purge-Protokoll (A6) ist die dritte `kind`-Ausprägung desselben Formats und wird beim Re-Import gegen genau diese Felder validiert statt vertraut.

**Dateiform-Entscheidungen, alle mit demselben Argument („die Datei ist Daten, kein UI"):** CSV-Header sind englisches `snake_case` und sprachunabhängig — deutsche Header würden jedes Skript brechen, das gegen den Export geschrieben wurde, und mit der UI-Sprache die Dateiform ändern (dieselbe Begründungslinie wie `ApiErrorCodes`). Zahlen und Daten laufen nie durch `Intl` (`1234`, nicht `1.234`; ISO-Daten). UTF-8-BOM vorangestellt, sonst zerlegt Excel Emote-Namen mit Nicht-ASCII; CRLF und RFC-4180-Quoting; Werte, die mit `=`, `+`, `-`, `@`, Tab oder CR beginnen, bekommen ein führendes `'` (CSV-Injection — Emote-Namen sind fremdkontrollierter Text; eigene Zahlen sind davon ausgenommen, ein präfixierter negativer Score wäre nicht mehr summierbar). Der Session-Titel steht **nie** im Dateinamen (Freitext mit `/`, Anführungszeichen, Unicode) — der Dateiname trägt Channel + Session-Id + Datum, der Titel steht im JSON-`meta`.

---

### 2026-08-02 — Auslastungsbalken bekommen eine Schwellen-Leiter, das Roster-Badge nicht

**Betrifft:** `web/src/app/shared/ui/utilization-tone.ts` (+ `utilization-tone.spec.ts`, beide neu) · `web/src/app/shared/emotes/slot-budget.ts` · `web/src/app/features/admin/admin-roster-card.ts` · `web/src/app/features/admin/admin-monitoring-page.ts` · beide Locale-Dateien · `web/e2e/audit/ui-audit.audit.ts`

**Der Rest von Idee B2: die eigentliche Frühwarnung.** Der Twitch-Join-Balken färbte sich erst bei echter Überschreitung des Budgets, der 7TV-Subscription-Balken nie — beide Decken meldeten sich also genau dann, wenn es zu spät ist. Jetzt fahren beide dieselbe Schwellen-Leiter wie der Slot-Budget-Balken: `success` unter 80 %, `warning` ab 80 %, `danger` ab 95 %.

**Die Leiter ist aus `slot-budget.ts` nach `shared/ui/utilization-tone.ts` extrahiert, nicht importiert.** Der Admin-Bereich dürfte `shared/emotes/` zwar importieren — aber eine rein präsentationale Schwellen-Leiter aus der Emote-Domäne zu beziehen wäre eine Kopplung ohne inhaltlichen Grund. `SlotBudgetTone` bleibt als Alias-Re-Export bestehen, alle bisherigen Importeure sind unberührt; `slot-budget.spec.ts` lief unverändert grün als Regressionsbeweis der Extraktion.

**Der Ton wird aus dem ungekappten Prozentwert berechnet.** Die Balkenbreite bleibt auf 100 % gekappt (ein Über-Budget-Zustand darf den Balken nicht aus seiner Spur laufen lassen), aber 120 % muss `danger` ergeben und nicht knapp-unter-100 — deshalb existieren `rawBudgetPercent`/`rawUtilizationPercent` getrennt von den gekappten Breiten-Werten.

**Kein Nenner → kein Ton.** Meldet 7TV kein `subscription_limit` (älterer Worker-Snapshot) oder fehlt das Join-Budget, bleibt der Balken beim bedeutungsfreien `bg-accent` und es erscheint kein Warntext — dieselbe Ehrlichkeit, aus der `slotBudget()` bei fehlender Kapazität `null` liefert: ein grüner Balken ohne Nenner würde eine Gesundheit behaupten, die niemand gemessen hat. Der gesunde Zustand mit Nenner wechselt dafür sichtbar von `bg-accent` auf `bg-success-dot` — `accent` trägt keine Gesundheitsaussage, und die einzige bestehende Auslastungsanzeige der App (Slot-Budget) fährt die Leiter bereits.

**Ab `warning` steht eine Textzeile unter dem Balken** — der Farbwechsel allein wäre ein Verstoß gegen WCAG 1.4.1, und der Balken-Kontrast (1.4.11, 3:1 der `*-dot`-Token) ist vom Audit-Harness ohnehin nicht automatisiert prüfbar. Beim Join-Budget gibt es drei Textstufen (`warning`/`critical`/`exceeded`), weil „mehr getrackte Channels als Budget" eine andere Handlungsaufforderung ist als „nähert sich".

**Das Roster-Status-Badge bekommt bewusst keine `nearing-capacity`-Stufe.** Das Badge beantwortet „macht der Worker seinen Job" — Kapazität ist eine Prognose, kein Defekt. Ein voll gejointer, gesunder Worker als `warning` zu badgen würde genau das Signal verwässern, das „drei Channels fehlen" bedeutet. Ebenso unverändert: 7TV-REST bekommt weiterhin keinen Balken (kein veröffentlichtes Quota, kein ehrlicher Nenner), und die 90-%-Log-Warnung des Workers (`SevenTvEventClient`) bleibt als serverseitiges Gegenstück stehen.

---

### 2026-08-02 — Die URL trägt den Listenzustand: Seite *und* Filter, mit zwei unterschiedlichen Navigationsarten

**Betrifft:** `web/src/app/core/routing/list-query-state.ts` (+ `list-query-state.spec.ts`, beide neu) · `web/src/app/features/admin/admin-audit-log-page.ts` · `web/src/app/features/admin/admin-users-page.ts` · `web/src/app/features/channel-workspace/channel-activity-page.ts` · `web/src/app/features/my-votings/my-votings-page.ts` · `web/src/app/features/voting/vote-session-list-page.ts` · `web/e2e/channel-activity.e2e.spec.ts` · [UI-Designsprache.md](UI-Designsprache.md) §8.4

**Der Nachtrag zum Eintrag darunter.** Die Reposition beim Seitenwechsel war gebaut, die Seitennummer lag aber weiter in einem lokalen Signal — „Zeile öffnen, zurück" landete auf Seite 1, ein geteilter Link zeigte eine andere Liste als die gemeinte. Der Umbau macht die URL zur Quelle der Wahrheit.

**Seite allein hätte nicht gereicht.** Drei der fünf Seiten haben keine Filter, dort ist `?page=3` die vollständige Antwort. Die beiden Audit-Logs haben welche, und ein wiederhergestelltes „Seite 3" ohne den Filter, der Seite 3 bedeutungsvoll gemacht hat, ist schlechter als nutzlos. Deshalb tragen `action`/`channel`/`actor` genauso in der URL — der eigentliche Aufwandstreiber dieser Runde.

**Zwei Navigationsarten, und der Unterschied ist der ganze Punkt.** `goToPage` navigiert normal und erzeugt einen History-Schritt — dafür ist der Zurück-Button da. `setParams` navigiert mit `replaceUrl` und springt auf Seite 1 zurück. Filter greifen pro Tastenschlag; ohne `replaceUrl` hieße Zurück „einen Buchstaben rückgängig". Der Preis ist, dass Zurück die Filterung in einem Schritt verlässt statt sie aufzudröseln — akzeptiert, dafür gibt es den Zurücksetzen-Button.

**`scroll: 'manual'` löst die Kollision, die diesen Umbau fast verhindert hätte.** Ein Query-Param-Wechsel ist eine Router-Navigation, und `RouterScroller` springt bei imperativen Navigationen auf `[0, 0]` — das hätte bei jedem Tastenschlag gescrollt und beim Seitenwechsel die eben gebaute Anker-Reposition überschrieben, inklusive des `scroll-mt-*`, das den Anker aus dem Sticky-Stapel hält. Die erwogenen Auswege waren beide schlecht: `scrollPositionRestoration: 'disabled'` trifft app-weit auch die Landing-Anker, und die Reposition hinter die Navigation zu hängen hätte den Pager wieder umgebaut. `NavigationExtras.scroll: 'manual'` ist der dokumentierte Per-Navigation-Ausstieg und lässt beide Mechanismen unangetastet nebeneinander stehen.

**Der Helper besitzt die Drafts der Textfilter — das ist eine Korrektur an einem ersten Entwurf, den ein bestehender E2E-Test widerlegt hat.** Textfilter behalten ihren lokalen, sofort sichtbaren Draft und schreiben nur den ausgeruhten Wert in die URL (eine Navigation zwischen Taste und Zeichen frisst Eingaben). Der erste Entwurf ließ `onActionFilterChange` bloß URL-`action` und -`channel` in *einer* Navigation setzen und strich den bisherigen Short-Circuit gegen den nachlaufenden Debounce — mit dem Argument, die atomare Navigation mache ihn überflüssig. Sie macht ihn nur für den Fall überflüssig, in dem der Wert die URL schon erreicht hat: ein Channel, der noch im 300-ms-Fenster hängt, schrieb sich danach zurück, unter einer Aktion ohne Channel-Dimension. Der Test „selecting the channel-less revoke action disables and clears the channel filter" hat das gefangen. Statt die fehlende Zeile in beide Seiten zu kopieren, leert jetzt `setParams` die Drafts der Keys, die es schreibt: Draft und URL sind **ein** Zustand, und `setParams` ist die Stelle, an der sie sich treffen. Aus dem freien `urlTextFilter(fromUrl, toUrl, ms)` wurde dabei `query.textFilter(key, ms)` — die Registrierung des Drafts ist damit nicht mehr vergessbar.

**Default-Werte fliegen aus der URL** statt leer geschrieben zu werden (`?page=3`, nicht `?page=3&action=&channel=&actor=`), Seite 1 steht nie drin. **Nicht abgedeckt:** `?page=9999` wird durchgereicht — das Backend liefert eine leere Seite, die Liste zeigt ihren Leerzustand. Clamping bräuchte `totalPages`, das erst *nach* der Antwort existiert, die mit diesem Zustand angefordert wird. Nichtnumerische und Werte `< 1` werden dagegen auf 1 geklemmt: „Seite -3 von 12" ist kein Zustand, den der Pager rendern soll.

---

### 2026-08-02 — Ein Seitenwechsel repositioniert den Viewport: der Pager bekommt ein Scroll-/Fokus-Ziel

**Betrifft:** `web/src/app/shared/pagination/pager.ts` (+ `pager.spec.ts`, neu) · `web/src/app/features/admin/admin-audit-log-page.ts` · `web/src/app/features/admin/admin-users-page.ts` · `web/src/app/features/channel-workspace/channel-activity-page.ts` · `web/src/app/features/my-votings/my-votings-page.ts` · `web/src/app/features/voting/vote-session-list-page.html` · beide Locale-Dateien (`pager.label`) · [UI-Designsprache.md](UI-Designsprache.md) §8.4

**Der Auslöser war als „Sticky-Header und Pagination beißen sich" formuliert — das stimmte nicht, und die Diagnose ist der Inhalt dieser Entscheidung.** Es sind zwei getrennte Defekte. Der eigentliche: `onPageChange` setzte in allen fünf paginierten Seiten nur ein Signal, sonst nichts — im ganzen `web/src` gab es kein `scrollTo`, kein `scrollIntoView`, keinen `ViewportScroller`. Der Pager steht unter seiner Liste, der Klick passiert also immer am unteren Dokumentende; die Zeilen der nächsten Seite kamen komplett *oberhalb* des Viewports an, und der Leser schaute weiter auf das Ende einer Liste, durch die er nie gescrollt war. Das wäre ohne jede Sticky-Leiste derselbe Bug. Die Sticky-Ebenen aus §8.5 machen nur die *Reparatur* anspruchsvoller: sobald man repositioniert, verdecken sie das Sprungziel, weil nirgends `scroll-margin-top` gesetzt war.

**Die Reposition liegt im Pager, nicht in den fünf Seiten.** Sie ist eine Eigenschaft des Musters, nicht der einzelnen Seite; fünfmal derselbe Dreizeiler wäre fünfmal die Chance, ihn beim nächsten paginierten Screen zu vergessen. Der neue Input `scrollTarget` nimmt eine Template-Referenz auf den Kopf der Ergebnisregion — kein Directive mit DI-Registrierung, weil Anker und Pager Geschwister im selben Template sind und der Pager ein Geschwister-Directive nicht injizieren kann; das bräuchte einen Token auf Komponentenebene und wäre Maschinerie für nichts.

**`scroll-mt-*` als Tailwind-Utility an der Verwendungsstelle, keine neue CSS-Primitive.** Der Offset hängt davon ab, welche Leisten über der Seite liegen — dieselbe Begründung, aus der schon `.app-sticky-bar` seinen `top`-Wert an der Verwendungsstelle bekommt. `scroll-mt-24` unter einer Tab-Leiste (14 + 10), `scroll-mt-14` bei My-Votings, das direkt an der Shell hängt. Dass der Höhen-Vertrag aus §8.5 fest ist, ist genau das, was die Alternative — den Stapel zur Laufzeit messen — überflüssig macht. Die Filter-Toolbar bekommt keinen Anteil, obwohl sie beliebig hoch wrappen darf: sie ist selbst sticky und pinnt an ihrem eigenen `top`.

**Reihenfolge im Handler ist tragend: erst repositionieren, dann emittieren.** Der Emit setzt das Page-Signal, die Resource fällt zurück, Liste und Pager werden durch das Skeleton ersetzt. Was danach käme, liefe gegen ein detachtes Element — und der Fokus des Tastaturnutzers wäre schon auf `<body>` gefallen. Genau deshalb löst diese eine Änderung auch das Fokusproblem mit: es braucht **nicht** zusätzlich einen Pager, der während des Ladens stehen bleibt. Das war ursprünglich geplant und wurde nach einem Blick in Angulars `resource`-Implementierung verworfen: bei einem Params-Wechsel wird der Stream nicht übernommen (`previous.value.extRequest.request === request` schlägt fehl), `value()` liefert den `defaultValue`, `totalPages()` ist `0` — der Pager versteckt sich also ohnehin selbst, und ihn stehen zu lassen bräuchte in jeder Seite einen klebrigen Zweitzustand.

**Instant, nicht smooth.** `scrollIntoView()` ohne Optionen. Über mehrere Bildschirmhöhen ist eine Animation langsamer als der Leser und zusätzlich ein Motion-Trigger; ein Inhaltsaustausch ist kein geführter Rundgang. `focus({ preventScroll: true })` und `scrollIntoView()` sind zwei Aufrufe, weil `focus()` allein nur scrollt, wenn das Element off-screen ist, und nur auf „nearest" — die Reposition muss unbedingt und an den Anfang gehen.

**Ankerwahl je Seite.** Vier Seiten nehmen ihre Überschrift. Die Vote-Session-Liste hat über den Zeilen keine eigene, nur die Anlege-Karte — und genau dorthin darf ein Seitenwechsel nicht zurückspringen, also ist dort die `<ul>` der Anker. Ein eigenes leeres Anker-Element wurde verworfen: die Seiten sind `flex flex-col gap-*`, ein leeres `<div>` erzeugt eine sichtbare Lücke, und `sr-only` (`position: absolute`) würde das Scroll-Ziel an eine andere Stelle legen als die, die es markieren soll.

**Barrierefreiheit mit erledigt:** der Pager ist jetzt ein benanntes `<nav>` (`pager.label`, neuer Key in beiden Locales), und die Seitenzeile ist `role="status"` — die Fokusverschiebung sagt einem Screenreader *was* auf dem Schirm steht, nie *welche Seite* es ist. Vorher gab es dafür gar keine Ankündigung; `aria-live` kam im Repo überhaupt nicht vor.

**Nicht Teil dieser Runde, bewusst:** die Seitennummer bleibt reiner Signal-State statt Query-Param. Ein Query-Param wäre für Deep-Link und Zurück-Button richtig, hängt aber den Router in den Scroll-Pfad (`withInMemoryScrolling`/`scrollPositionRestoration` in `app.config.ts`) und überlagert sich mit der Anker-Reposition — das ist eine eigene Entscheidung, keine Fußnote dieser.

---

### 2026-08-02 — Resync als Self-Service: der weitere Filter, ein Per-Channel-Cooldown und eine achtmal strengere Policy

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Api/Program.cs` (Policy `ChannelResync`) · `src/EmotePurge.Api/Validation/ApiErrorCodes.cs` · `src/EmotePurge.Core/Services/IChannelResyncCooldown.cs` (neu) · `src/EmotePurge.Infrastructure/Redis/ChannelResyncCooldown.cs` (neu) · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` (Kommentar) · `web/src/app/core/i18n/api-error.ts` · beide Locale-Dateien

**Idee A8 aus [Feature-Ideen-2026-08-01.md](Feature-Ideen-2026-08-01.md).** `POST /api/channels/{channelName}/resync` gibt den 7TV-Vollsync für die Channel-Verwaltung frei. Der häufigste Support-Fall der App ist „ich hab ein Emote hinzugefügt, es taucht nicht auf"; die Antwort war bis hierher „warte den nächsten 60-Sekunden-Tick ab" oder „schreib dem Admin" — in einer App, die sonst durchgehend rollengegateter Self-Service ist.

**Der *weitere* Filter, direkt neben dem Audit-Log mit dem engeren.** Die beiden Routen entscheiden bewusst gegensätzlich, und beide Male aus demselben Prinzip: es zählt, was der Endpoint preisgibt bzw. auslöst, nicht wie „mächtig" er wirkt. Der Aktivitätsverlauf nennt Personen, deshalb Mod-Team. Der Resync liest nur von 7TV und schreibt nichts, was jemand anderem gehört — und die Person mit dem Problem ist typischerweise gerade der **7TV-Editor**, der das Emote eben hinzugefügt hat. Ihn auszuschließen hätte den Feature-Zweck verfehlt. Die Missbrauchsfläche ist hier Kosten, nicht Autorität.

**Und Kosten sind zwei getrennte Probleme, die zwei getrennte Mechanismen brauchen.** Das ist der eigentliche Inhalt dieser Entscheidung:

- Die Rate-Limit-Policy `ChannelResync` (5/min, gegenüber `ExternalApi`s 40) partitioniert **pro Nutzer** und schützt das app-weite 7TV-Budget davor, dass ein einzelner Account es leerläuft.
- `IChannelResyncCooldown` partitioniert **pro Channel** (Redis-Key `resync:cooldown:{name}`, `SET NX EX`, Default 60 s). Fünfzehn Moderatoren desselben Channels haben fünfzehn Nutzer-Budgets — gegen die einen Rate-Limiter grundsätzlich nichts ausrichten kann.

Keiner der beiden deckt den Fall des anderen ab, deshalb beide.

**Die teure Ressource ist nicht 7TV, sondern das Live-Event.** Ein Resync kostet einen 7TV-REST-Call und ein paar DB-Roundtrips — vergleichbar mit dem periodischen Worker-Sync, der ohnehin einmal pro Channel und Minute läuft. Was ihn wirklich teuer macht: der RESYNC-Pfad publiziert `channel.synced` **unbedingt**, wodurch jede offene Seite dieses Channels neu lädt, inklusive der schwersten Aggregat-Query der App. Die Kosten trägt jeder Zuschauer mit offener Seite, nicht die Person, die klickt. **Das unbedingte Publizieren bleibt so** (die periodischen und EventAPI-Pfade publizieren seit dem 2026-08-01 nur bei echter Bestandsänderung): die „angestoßen → abgeschlossen"-Rückmeldung im Admin-Bereich **und** im Workspace hängt daran, und ein änderungsgesteuerter Push ließe die Meldung bei einem Resync ohne Änderung — dem Normalfall ab dem zweiten Versuch — ewig auf „angestoßen" stehen. Restrisiko nach dem Cooldown: ein zusätzlicher Fanout pro Channel und Minute, dieselbe Größenordnung wie der Worker-Takt.

**Redis, nicht In-Process und nicht DB-Spalte.** Ein prozesslokaler Guard multipliziert sich mit der Zahl der Api-Replicas, mit denen ausdrücklich gerechnet wird. Eine Spalte bräuchte eine Migration, und der naheliegende Wiederverwendungskandidat `Channel.LastSyncedAtUtc` ist untauglich: der 60-Sekunden-Resync des Workers hält ihn dauernd frisch, ein Cooldown darauf würde manuelle Resyncs faktisch immer blockieren. `When.NotExists` ist der erste Einsatz dieses Musters im Repo — mit Kommentar und eigenem Integrationstest, insbesondere darauf, dass die TTL gesetzt ist: ein `SET NX` ohne `EX` wäre ein permanenter Lock, den nichts im Code je wieder löscht.

**`TryBegin`/`Release` statt einer Methode.** Der Slot wird **vor** dem Trigger geklaut, sonst gibt es ein Fenster zwischen Prüfen und Auslösen. Wenn der Resync dann gar nicht stattfand (Channel unbekannt oder inaktiv), gibt der Handler ihn sofort zurück — sonst blockierte ausgerechnet die Rückfrage nach einem gerade reaktivierten Channel den Resync, den der Broadcaster in dem Moment will.

**Im Handler, nicht in einem `IEndpointFilter`** — die einzige Stelle, an der diese Umsetzung von Regel 6 abweicht. Ein Filter müsste zur Freigabe den `IResult` des Handlers auf seinen konkreten Typ untersuchen; das ist brüchiger als drei Zeilen im Handler, und der Handler bleibt trotzdem dünn. Dass der Cooldown *nach* den Autorisierungsfiltern greift, ist dabei genau richtig: ein 403-Aufrufer darf keinen fremden Cooldown verbrennen.

**429 mit Body statt 409.** Die Semantik ist „zu oft, versuch's später", und `Retry-After` ist dafür der standardisierte Kanal. Dass der Rate-Limiter denselben Status ohne Body liefert, ist kein Konflikt, sondern nützlich: `apiErrorTranslationKey` bevorzugt einen bekannten `errorCode` vor dem Status, die beiden Fälle sind im Client also automatisch unterscheidbar. `retryAfterSeconds` steuert nur, wie lange der Button deaktiviert bleibt — der Text bleibt parameterlos, weil `apiErrorTranslationKey` einen Key zurückgibt und keine Parameter.

**Der Admin-Resync bleibt ohne Cooldown**, mit Kommentar an beiden Handlern. Er ist der Notausgang für den Support-Fall, in dem der Channel-Cooldown im Weg steht, und erreichbar nur für die Handvoll Logins in der Allowlist.

**Nebenbefund, den dieser Commit mit erledigt: Regel 7 stimmte nicht.** Sie behauptet, `api-error.spec.ts` prüfe die Locale-Dateien gegen `ApiErrorCodes` — das tat sie nie, keine Datei unter `web/src` las `public/i18n/*.json`. Der Beweis lag im Repo: `no_health_data` und `health_data_unreadable` existierten server-seitig und in **keiner** der beiden Locale-Dateien, wurden also stillschweigend auf die generische Statusmeldung heruntergestuft. Neu ist `api-error-locales.spec.ts`, das beide Richtungen prüft (kein Code ohne Übersetzung, keine Übersetzung ohne Code, identische Schlüsselmengen); die zwei Altlasten sind nachgetragen. Was der Test weiterhin nicht sieht, ist die C#-Seite — dafür müsste er eine `.cs`-Datei parsen, was den Handel nicht wert ist.

---

### 2026-08-02 — Der Channel bekommt seinen eigenen Audit-Log — hinter dem engeren Filter, mit dem Channel aus der Route

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Api/Validation/PagingQuery.cs` (neu) · `src/EmotePurge.Api/Endpoints/{AdminEndpoints.cs,VoteSessionEndpoints.cs}` · `tests/EmotePurge.Api.Tests/AuthFilterMatrixTests.cs` · `docs/Architectur.md` (Endpoint-Filter-Matrix)

**Idee A7 aus [Feature-Ideen-2026-08-01.md](Feature-Ideen-2026-08-01.md), Backend-Hälfte.** `GET /api/channels/{channelName}/audit-log` liefert dieselben Zeilen wie der Admin-Log, auf einen Channel eingegrenzt. Bis hierher konnte ein Broadcaster nicht sehen, welcher seiner Moderatoren eine Abstimmung gelöscht oder den Bot entfernt hat — diese Antwort lag ausschließlich im Global-Admin-Bereich. Es entsteht **kein** neuer Service und **keine** Migration: `IAuditLogQueryService.ListAsync` filtert bereits exakt auf den normalisierten Channel und wird vom Compound-Index `(ChannelName, OccurredAtUtc DESC)` bedient, der seit dem 2026-07-31 genau dafür existiert.

**Der engere Filter, nicht der weitere — bewusst gegen die Symmetrie.** Alle anderen Lese-Endpoints eines Channels hängen an `UsageStatsAccessAuthorizationFilter`, der zusätzlich die 7TV-Editoren des Channels zulässt. Der Aktivitätsverlauf hängt an `ChannelManagementAuthorizationFilter` und lässt sie **nicht** zu. Grund ist der Inhalt: Nutzungszahlen sind aggregiert, hier steht namentlich, wer was getan hat. Ein 7TV-Editor ist häufig ein externer Grafiker mit Set-Rechten, kein Mitglied des Mod-Teams — und die Empfindlichkeit von Add/Remove-Attribution ist belegt ([SevenTV/Extension#267](https://github.com/SevenTV/Extension/issues/267), ein Streamer wörtlich: „I don't really want that everyone knows if I add or remove an emote"). Die Zielgruppe ist das Mod-Team über sich selbst.

**Der Channel kommt aus dem Route-Wert und aus nichts anderem.** Der Handler bindet bewusst **keinen** `channel`-Query-Parameter, obwohl `AuditLogFilter` ein Feld dafür hat. Täte er es, könnte ein für Channel A autorisierter Aufrufer über seine eigene, korrekt autorisierte Route den Log von Channel B lesen — der Filter läuft gegen den Routen-Wert, die Query ginge ungeprüft an die Datenbank. Das ist die Art Lücke, die in einem Review wie eine Bequemlichkeit aussieht.

**Zwei Tests, die die beiden Sätze oben zu Verhalten machen.** `ChannelAuditLog_UsesTheManagementCheck_NotTheWiderOne` prüft nicht nur den 403, sondern per `DidNotReceive()`, dass `CanViewUsageStatsAsync` gar nicht erst gefragt wird — ohne das wäre der Editor-Ausschluss ein Kommentar. `ChannelAuditLog_ValidatesTheChannelName_BeforeAuthorizing` pinnt, dass die Route in der geteilten `/api/channels`-Gruppe liegt: die naheliegende Aufräum-Idee, sie wie `UsageStatsEndpoints` in eine eigene `MapGroup` zu ziehen, verliert stillschweigend den `ChannelNameValidationFilter` und damit den 400-vor-403-Vertrag.

**`Bookkeeping` statt gar keiner Policy.** Reiner DB-Read gegen einen Covering-Index, aber über den Actor-Filter pro Tastendruck erreichbar. `Bookkeeping` (120/min) ist die vorhandene Policy für „nur unsere eigene Infrastruktur, trotzdem nicht unbegrenzt"; `ExternalApi` wäre die falsche Aussage, weil hier nichts Externes berührt wird.

**Nebenbei: `PagingQuery` als vierte Kopie.** `page <= 0 ? 1 : page` und `pageSize <= 0 ? 20 : Math.Min(pageSize, 100)` standen wortgleich an drei Stellen; die neue Route wäre die vierte gewesen. Jetzt einmal in `Validation/PagingQuery.cs`, zusammen mit `NullIfBlank`. Die Query-Services dokumentieren ihre Argumente als „expected to be pre-validated by the endpoint" — das *ist* diese Validierung, und die Obergrenze kann jetzt nicht mehr zwischen Endpoints auseinanderlaufen. Weiterhin bewusst **kein** `ApiErrorCode` für Paging: Bereichsüberschreitungen werden still korrigiert, nicht abgelehnt, und müssen so auch nicht in zwei Locale-Dateien gespiegelt werden.

---

### 2026-08-02 — `DetailsJson` verlässt den Server nicht mehr roh: die Whitelist steht in `Core`, nicht im Client

**Betrifft:** `src/EmotePurge.Core/Services/IAuditLogQueryService.cs` · `src/EmotePurge.Infrastructure/Services/AuditLogQueryService.cs` · `web/src/app/core/audit/audit.model.ts` (neu) · `web/src/app/core/admin/{admin.model.ts,admin.service.ts}` · `web/src/app/features/admin/admin-audit-log-page.ts` · `tests/EmotePurge.Infrastructure.Tests/Integration/AuditLogQueryServiceTests.cs`

**Vorarbeit für den channel-gescopten Audit-Log (A7), aber die Begründung steht für sich.** `AuditLogEntry.DetailsJson` ist eine `jsonb`-Spalte, die per Entitäts-Kommentar *free-form on purpose* ist: jede der zehn Aktionen schreibt ihre eigene Form hinein, und nichts hindert einen künftigen Schreibpfad daran, dort etwas abzulegen, das sein Autor nie für fremde Augen gedacht hat. Bisher ging das Feld **roh** an den Client, und die einzige Whitelist stand in `admin-audit-log-page.ts` — in einem Frontend, dessen einziges Publikum ein einzelner Global-Admin ist. Sobald dieselbe Zeile an die Moderatoren eines fremden Channels geht, ist das keine Randbemerkung mehr.

**Die Whitelist wandert nach `Core`, statt sich zu vermehren.** Der naheliegende kleine Weg wäre gewesen, nur den neuen Channel-Endpoint filtern zu lassen. Das hätte dieselbe Entscheidung an drei Stellen gehabt: einmal im Channel-Endpoint, einmal im Admin-Endpoint (gar nicht), einmal im Client. Stattdessen projiziert `AuditLogQueryService.ProjectDetail` das Feld einmalig auf einen geschlossenen Satz von Formen — `AuditLogDetail(Kind, Count, Text)` mit `Kind` ∈ `emoteCount` | `removedEntries` | `title`, sprachneutral wie `AuditActions`. Was nicht in dieser Liste steht, existiert für **beide** Endpoints nicht, ohne dass irgendwer daran denken muss.

**`parseDetail()` im Client wurde gelöscht, nicht verschoben.** Das ist der Punkt, an dem die Sache strukturell wird statt nur ordentlich: der Client behält eine reine Anzeige-Tabelle `Record<kind, translationKey>`. Ein unbekanntes `kind` rendert nichts — das ist eine Darstellungsentscheidung, keine Sicherheitsentscheidung, und damit gibt es keine zweite Wahrheit mehr, die driften könnte. Hätte man den gefilterten JSON-String zurückserialisiert und das Feld `DetailsJson` gelassen, wäre der Zaun gebaut und das Tor offen geblieben.

**Zwei Felder fallen dabei aus dem DTO.** `DetailsJson` ersetzt durch `Detail`, und `ActorTwitchUserId` ersatzlos — nachgemessen, nicht vermutet: keine einzige Stelle im Frontend liest es, jede Oberfläche identifiziert den Aktor über `ActorLogin`. Die numerische Twitch-ID mitzuliefern hätte nur vergrößert, was die Moderatoren eines Channels künftig übereinander erfahren. Die Spalte selbst bleibt natürlich — sie ist die stabile Identität, wenn ein Login umbenannt wird. Wieder aufnehmen, wenn es einen Konsumenten gibt.

**`login` wird weiterhin erkannt und weiterhin verworfen**, samt Begründung, die aus dem Client mitgewandert ist: die nutzerbezogenen Aktionen tragen den Login des *Ziels* in ihren Details, und die Zeile läse sich dann „von sensitron · handofblood", ohne dass irgendetwas sagt, welcher der beiden Namen das Ziel ist.

**Projiziert wird nach dem Materialisieren, nicht im `Select`.** EF Core kann `JsonDocument.Parse` nicht übersetzen; im Query-Ausdruck wäre das ein Laufzeit-, kein Build-Fehler gewesen. Steht als Kommentar an der Stelle, weil es beim nächsten „Aufräumen" wieder verlockend aussieht.

**`Text` wird bei 200 Zeichen gekappt.** Vote-Session-Titel sind Nutzereingabe und nur durch ihre eigene Validierung begrenzt; eine Log-Zeile ist nicht der Ort für einen Aufsatz.

---

### 2026-08-02 — Der Produkt-Backlog wird zum dritten verbindlichen Dokument und trägt seinen Umsetzungsstand selbst

**Betrifft:** `docs/Feature-Ideen-2026-08-01.md` · `CLAUDE.md` (Kopf-Dokumentenliste)

**Der Auslöser war eine Frage, die niemand beantworten konnte:** „welche Ideen haben wir eigentlich schon umgesetzt?" Von den drei Top-Kandidaten der Ideenfindung vom 2026-08-01 waren zu dem Zeitpunkt zwei vollständig und einer teilweise gebaut — sichtbar war das nirgends. Das Ideen-Dokument stand unverändert seit dem Tag seiner Erstellung, kannte keinen einzigen Statusmarker, und war in `CLAUDE.md` nicht einmal verlinkt. Der Stand ließ sich nur rekonstruieren, indem man `DECISIONS.md` und das Git-Log gegeneinanderlas — und selbst dort nannten nur zwei Einträge eine Ideen-Kennung; der dritte (Roster/Drilldown) beschrieb inhaltlich `B1`/`B2`, ohne sie zu benennen. Wer allein das Ideen-Dokument las, hielt alles für offen.

**Die Ideentexte bleiben eingefroren, der Status kommt daneben.** Der naheliegende Weg wäre gewesen, die umgesetzten Abschnitte zu überarbeiten oder zu entfernen. Bewusst nicht: die Texte sind die *Begründung* einer Priorisierung zu einem Zeitpunkt, mit Marktbelegen und Recherchestand. Sie nachträglich anzupassen macht aus einer nachvollziehbaren Entscheidung eine geschönte. Stattdessen trägt jede umgesetzte Idee eine **Statuszeile direkt unter ihrer Überschrift** und das Dokument oben eine **Tabelle über alle 24 Ideen**. Dasselbe Muster wie beim Verschieben des Logs am 2026-07-30: Original unangetastet, Navigationshilfe außen herum.

**Die Statuszeile benennt Abweichungen, nicht nur den Vollzug.** Drei der vier fertigen Features weichen von ihrer Idee ab, und genau das ist die interessante Information: `A1` übernimmt `emote_count` von 7TV nicht, `A3` hat von zwei vorgeschlagenen Wegen den rückwirkend korrekten genommen, `B2` misst gegen ein Join-Budget von 20 statt gegen die 100er-Decke. Ein reines „✅" hätte drei Mal etwas anderes behauptet, als gebaut wurde. Bei `B1` und `B2` steht entsprechend explizit, was **fehlt** — bei `B1` die Audit-Zeilen und der Per-Channel-Flush, bei `B2` die Frühwarn-Schwellen. Das ist der eigentliche Gewinn: aus „teilweise" wird ein Rest, den man einplanen kann.

**Neue Pflicht, analog zur Regel oben:** wer eine Idee umsetzt, pflegt ihre Statuszeile im selben Commit. Bewusst **keine** neue nummerierte Regel in `CLAUDE.md` — die Regelliste beschreibt Code-Konventionen, und eine zwanzigste Regel für Dokumentationspflege würde sie verwässern. Der Hinweis steht dort, wo er gebraucht wird: im Dokument selbst und in seiner Kopfzeile.

**Die Arbeitsteilung der drei Dokumente ist damit trennscharf:** `Architectur.md` sagt, *wie es gebaut ist*, `DECISIONS.md` sagt, *warum es so gebaut ist*, `Feature-Ideen-…md` sagt, *was noch nicht gebaut ist und warum es sich lohnen würde*. Das dritte fehlte in der Kopfzeile von `CLAUDE.md`, obwohl es die am häufigsten gestellte Frage beantwortet — „was machen wir als Nächstes?".

---

### 2026-08-02 — Das Manifest bekommt maskable Icons, damit Android das App-Icon nicht auf eine weiße Platte legt

**Betrifft:** `web/public/manifest.webmanifest` · `web/public/icon-maskable-{192,512}.png` (neu) · `web/branding/{make-icons.ps1,README.md}`

**Befund aus der Praxis, nicht aus einem Test:** die auf einem Android-Gerät installierte PWA zeigte im App-Launcher das dunkle Icon in einem **weißen Rahmen**. Ursache ist nicht das Icon, sondern das Fehlen einer Angabe: das Manifest führte seine Icons ohne `purpose`, das ist per Default `"any"`. Android hat damit kein Adaptive Icon, auf das es seine Maske anwenden könnte, und fällt auf die Legacy-Behandlung zurück — PNG verkleinern, auf eine weiße Platte setzen, Systemmaske darüber. Jedes dunkle Icon sieht so aus, als hätte es einen weißen Rand.

**Behoben durch ein zweites Icon-Paar mit `purpose: "maskable"`**, nicht durch Ändern der vorhandenen. Die `"any"`-Einträge bleiben, wie sie sind: sie werden dort benutzt, wo *nicht* maskiert wird (Desktop-Installationen, Task-Switcher, Icon-Listen), und dort ist der heutige 10-%-Rand richtig. Ein Icon, das für beide Rollen taugt, gibt es nicht — maskable braucht randlose Fläche und viel Innenabstand, „any" braucht das Gegenteil.

**Der Innenabstand ist gemessen, nicht gesetzt.** Die Safe Zone einer maskable-Grafik ist ein Kreis mit 80 % des Kantenmaßes; nur der wird von jeder Launcher-Maske vollständig überdeckt. Der naheliegende Weg — die Bounding-Box in diesen Kreis legen — hätte den Mark auf 57 % der Kante gedrückt, weil eine Box-Diagonale √2 mal so lang ist wie ihre Seite. Der Mark ist aber eine Scheibe mit zwei Sichel-Ausläufern, seine Box-Ecken sind leer. `make-icons.ps1` misst deshalb neu (`MaxRadius`) den größten Abstand vom Zuschnitt-Mittelpunkt zu einem deckenden Pixel und wählt die Kantenlänge so, dass dieser Radius 40 % davon ist. Ergebnis: der Mark füllt **78,8 %** der Kante statt 57 %, und trotzdem kann keine Maske eine Sichelspitze abschneiden. Nachgeprüft, nicht angenommen: das erzeugte Icon unter Kreis-, Squircle- und Rounded-Square-Maske gerendert und angesehen.

**Das erneute Ausführen des Skripts hat kein Bestandsasset verändert** — `git status` zeigt nur die zwei neuen Dateien und das Skript selbst. Die Ableitung ist damit weiterhin deterministisch, was die Voraussetzung dafür ist, dass man sie überhaupt gefahrlos laufen lassen kann.

**Unverändert bleibt, dass das Manifest dunkel ist.** Das ist eine andere Frage als die hier — `theme_color`/`background_color` und die eingebrannte Fläche folgen weiter der Marke, nicht dem Seiten-Theme (Eintrag vom selben Tag). Neu ist nur, dass Android die Fläche jetzt als *seine* Icon-Fläche erkennt, statt eine eigene darunterzulegen.

**Zu wissen beim Ausrollen:** Der Launcher aktualisiert ein installiertes Icon nicht zuverlässig, wenn sich nur das Manifest ändert. Nach dem Deploy kann eine Neuinstallation der PWA nötig sein, um das Ergebnis überhaupt zu sehen.

---

### 2026-08-02 — Nach dem Ansehen: die Emote-Leinwand folgt doch dem Theme, Hover wird immer dunkler, Tones heißen nach Bedeutung

**Betrifft:** `web/src/styles.css` · `web/src/app/shared/ui/{status-badge.ts,theme-menu.ts}` · `web/src/app/shared/emotes/{slot-budget.ts,slot-budget-bar.ts,slot-budget.spec.ts}` · `web/src/app/shared/voting/vote-audience-badge.ts` · `web/src/app/features/{overview,voting,my-votings,admin}/*` · `docs/UI-Designsprache.md` (§2.0, §2.4, §4.3, §10)

Vier Korrekturen aus der ersten Sichtung des fertigen hellen Modus im Browser. Drei davon revidieren Entscheidungen, die weiter unten in diesem Log stehen — sie sind hier nicht nachgetragen, sondern überschrieben; die Originaleinträge bleiben als Historie stehen.

**Die Emote-Leinwand ist nicht mehr themefest.** Der Eintrag vom selben Tag („bleibt in beiden Modi dunkel") war gut begründet und trotzdem falsch — begründet im Einzelfall, falsch in der Menge. Das Argument stimmt weiterhin: 7TV-Emotes sind für dunkle Chats gezeichnet, ein Teil trägt weiße Schrift oder helle Outlines, und wir können das Fremdmaterial weder beeinflussen noch pauschal invertieren. Was das Argument nicht berücksichtigt hat: die Leinwand steht **auf jeder Kachel**, ein fast schwarzer Balken quer über jede Karte ist auf einer hellen Seite das lauteste Element der Seite, und die Emotes, die er schützt, sind die Minderheit. `--ep-emote-canvas` gibt es jetzt pro Modus (dunkel `slate-800`, hell `slate-200` — eine Stufe unter `surface-inset`, damit die Bildfläche auf der weißen Karte noch als eigene Platte liest). Das Token bleibt trotzdem ein **eigenes**: die Rolle „Fläche für Fremdmaterial" ist eine andere als „eingelassene Fläche", und wenn der Handel je zurückgedreht wird, muss das eine Zeile sein. **Damit gibt es keine themefeste Farbe mehr** — die Ausnahme in §2.0 ist gestrichen statt vermehrt.

**Gefüllte Buttons werden im Hover in beiden Modi dunkler.** Der Tokensatz hatte zwei Regeln: hell dunkelt ab, dunkel hellt auf. Die zweite kostet systematisch Kontrast, weil `on-accent` in beiden Modi weiß ist — ein hellerer Hover kann Weiß nur Kontrast wegnehmen. Gemessen: weiß auf `purple-500` (Primary-Hover, dunkel) **4,1:1**, weiß auf `emerald-600` (Keep-Vote-Hover, dunkel) **3,7:1**. Beide unter AA, beide seit dem Bestand, und **kein Werkzeug hätte sie je gemeldet**: axe wertet nur aus, was gerendert ist, und ein Hover ist nicht gerendert. Neue Regel, eine statt zwei: `*-solid-hover` liegt immer eine Stufe unter `*-solid`. Dunkel wird damit `accent` 600→**700** (7,1:1), `success` 700→**800** (7,6:1), `danger` 800→**900** (10,0:1). Die Ruhezustände bleiben unangetastet, es ändert sich nur der Hover. `danger` hat vorher schon AA gehalten (6,4:1) und zieht trotzdem mit — eine Regel mit einer Ausnahme ist keine Regel, und der Kommentar, der die Richtungsumkehr rechtfertigte, wäre für zwei von drei Tokens gelöscht worden. Dass ein dunkles UI beim Hover abdunkelt, ist nicht exotisch: Discords Primary-Button tut genau das.

**`StatusBadgeTone` und `SlotBudgetTone` heißen nach Bedeutung, nicht nach Farbe** — `accent · info · success · neutral · warning · danger` statt `purple · blue · emerald · slate · amber · red`. Die alten Namen stammen aus der Zeit, als der Badge einen Modus hatte und Name und Wert dasselbe waren. Seit dem hellen Modus sind sie es nicht mehr: `danger` ist dunkel `red-950`/`red-300` und hell `red-50`/`red-700`. Eine Aufrufstelle, die `red` verlangte, verlangte damit einen Wert, den ihr niemand mehr nennen konnte. Der Umbau berührt 19 Aufrufstellen und ist rein mechanisch — der Compiler findet jede, weil es String-Unions sind. `SlotBudgetTone` zieht mit, obwohl es ein unabhängiger Typ ist: einen der beiden umzubenennen und den anderen stehen zu lassen, wäre genau die Inkonsistenz, die den Rename überhaupt nötig macht.

**Das Theme-Menü zeigt gezeichnete Icons statt Emoji.** `☀`/`🌙`/`🖥` sind eine Schrift, keine Grafik: sie rendern auf jeder Plattform als ein anderes Bild, mindestens eines davon meist farbig statt monochrom, und sie nehmen `currentColor` nicht an — die ausgewählte Zeile, deren Text auf der Akzentfläche weiß wird, behielt also ein Glyph, das nicht mitging. Ersetzt durch drei 24er-Outline-Pfade in derselben Strichstärke wie die vorhandenen Shell-Icons, gekapselt in `<app-theme-icon>`, dessen `name` direkt eine `ThemePreference` nimmt — der Trigger reicht die *aufgelöste* Wahl (`'light'|'dark'`) durch, die eine Teilmenge davon ist, also braucht es keine Zuordnungstabelle.

---

### 2026-08-02 — Die Landing zieht mit, das Logo folgt dem Theme, und das Manifest bleibt dunkel

**Betrifft:** `web/src/app/features/landing/landing-page.{ts,html}` · `web/src/app/shared/branding/logo.ts` (neu) · `web/src/app/features/{shell/app-shell.ts,login/login-page.ts}` · `web/branding/{logo-mark-light.png,logo-full-light.png,make-icons.ps1,README.md}` · `web/public/{logo-light.png,logo-hero-light.png}` · `web/src/styles.css` · `web/scripts/check-color-tokens.mjs` · `web/e2e/theme.spec.ts`

**Der Logo-Tausch läuft über ein Signal, nicht über `<picture media="(prefers-color-scheme: …)">`.** Ein Media-Query beantwortet, was das *System* bevorzugt — diese App lässt den Nutzer das überstimmen. Bei einer expliziten Wahl gegen die Systempräferenz zeigte der CSS-Weg also die falsche Marke. `shared/branding/logo.ts` leitet den Dateinamen aus `ThemeService.resolved()` ab; `NgOptimizedImage` bleibt Pflicht, `[ngSrc]` erfüllt das mit gebundenem Wert. Größe und Position springen dabei nicht, weil beide Varianten gegen dieselbe Bounding-Box zugeschnitten sind und denselben Anteil der Kantenlänge füllen.

**Ein gebundenes `ngSrc` ist genau die Sorte Änderung, über die `NgOptimizedImage` sich zur Laufzeit beschwert — nicht beim Build.** Deshalb prüft der neue Fall in `theme.spec.ts` beim Moduswechsel nicht nur das `src`-Attribut, sondern **auch die Browser-Konsole** auf `error`/`warning`/`pageerror`. Ein Test, der nur das Attribut prüft, hätte eine Direktiven-Warnung nie bemerkt.

**Zwei neue Tokens für die Landing, beide mit gerechnetem Kontrast.** Das Gradient-Heading (`--ep-heading-gradient-from/-to`): die dunklen Stops erreichen auf hellem Grund 1,6:1 bzw. 2,1:1 — unbrauchbar selbst für Display-Type. Hell nimmt `purple-600` (5,2:1) und `pink-700` (5,9:1), womit beide Enden der Rampe 4,5:1 als Fließtext halten und es egal ist, wo das Auge landet. Und `shadow-glow` für die CTA-Halos: derselbe Wert wie der Hover-Schatten der interaktiven Karte — Markenglühen im Dunkeln, ein neutraler Lift im Hellen, weil ein additiver lila Schein auf Weiß ein Fleck ist und keine Tiefe.

**Das PWA-Manifest bleibt dunkel, und die App-Icons auch.** Ein Manifest trägt genau **eine** `theme_color`; der Splash-Screen der installierten App gehört optisch zum Icon, und das hat `#020617` eingebrannt (`branding/README.md`). Ein weißer Splash mit dunklem Icon-Kasten sähe kaputt aus. Die `<meta name="theme-color">`-Tags in `index.html` folgen dem Modus trotzdem — das ist der Browser-Chrome, nicht der Splash. Damit ist die letzte offene Entscheidung aus dem Konzept (§10 Nr. 8) beantwortet.

**Die Ausnahmeliste des Farbgates ist leer.** Sie hat getan, wofür sie gebaut war: die sieben Admin-Dateien und die Landing-Page haben ihre eigenen Einträge entfernt, weil das Stehenlassen den Lint genauso rot gefärbt hätte wie ein neuer Verstoß. Ein künftiger Eintrag ist eine befristete Schuld mit Namen, kein Weg, eine Datei dauerhaft auszuklinken.

---

### 2026-08-02 — Paletten-Utilities sind ab jetzt ein roter Lint, und der Audit-Harness rechnet Kontrast selbst

**Betrifft:** `web/scripts/check-color-tokens.mjs` (neu) · `web/package.json` · `web/e2e/audit/ui-audit.audit.ts` · `docs/UI-Designsprache.md` (§10, §11, §12)

**Die Token-Regel wird erzwungen, nicht erbeten.** `check-color-tokens.mjs` verbietet Tailwind-Paletten-Utilities unterhalb `web/src/app/` und hängt an `npm run lint`, läuft also im bestehenden CI-Schritt mit, ohne dass `publish.yml` angefasst werden muss. Bewusst ein Node-Skript statt eines `rg`-Schritts: ripgrep ist auf einem CI-Runner nicht garantiert. Kommentare werden vorher entfernt — eine Regel, die es verbietet, in einem Kommentar zu erklären, *warum* `bg-slate-900` hier mal stand, entmutigt genau die Kommentare, die sie verständlich machen.

**Die Ausnahmeliste kann nicht vergessen werden, und das ist ihr eigentlicher Zweck.** Sie startet mit den sieben Admin-Dateien und der Landing-Page. Ein Eintrag, dessen Datei **keine** Verstöße mehr hat, ist selbst ein Fehler und macht den Lint rot. Wer eine Datei aufräumt, kann den Eintrag also nicht liegen lassen — Vergessen färbt den Build genauso rot wie ein neuer Verstoß. Beide Fehlermodi sind vor dem Commit einzeln provoziert und beobachtet worden, nicht nur angenommen: injizierter Verstoß in einer migrierten Datei → rot, injizierter Verstoß in einer ausgenommenen Datei → grün, künstlich veralteter Eintrag → rot.

**Der Audit-Harness bekommt Theme als vierte Dimension — und einen Kontrast-Gate, der rechnet statt zu schauen.** `@axe-core/playwright` (neue Devdependency) fährt pro Zustand die Regel `color-contrast`; neue Metrik `contrastViolations`, Gate 0 auf `serious`/`critical`. **Bewusst nur diese eine Regel:** ein voller axe-Lauf fördert unabhängige Befunde zutage, die mit Theming nichts zu tun haben und jeden Audit rot färben würden, ohne dass dieser Harness etwas dagegen tun kann — die verdienen eine eigene Runde. Der erste Lauf hat den Nutzen sofort belegt: 224 Zustände, davon 12 rot, **alle zwölf hell und alle zwölf im Admin-Bereich** — also exakt die Dateien, die noch auf der Ausnahmeliste stehen. Der gesamte angemeldete Bereich und der dunkle Modus sind sauber. Damit bestätigt der Gate unabhängig, dass die Wellen 1 und 2 stimmen, und markiert punktgenau, was Welle 3 noch schuldet.

**Hell läuft nur bei 1280, dauerhaft.** Dieselbe Abwägung wie bei der `en`-Locale: Layoutbrüche sind theme-unabhängig, weil Farbe keine Kastengrößen ändert, und das, was am hellen Modus wirklich zu prüfen ist — der Kontrast — wird ohnehin in jedem Zustand gemessen. Laufzeit ~1,3× statt 2×. In der Welle, die einen Modus erstmals ausliefert, wird der Skip herausgenommen und die volle Matrix in beiden Modi durchgesehen.

**Nebenbefund, der in die Doku gehört:** die Ausgabedateien heißen jetzt `<szenario>--<viewport>--<locale>--<theme>`. Wer `.audit-out/` vor einem Lauf nicht leert, zählt beim Auswerten alte Dateien aus dem Vorher-Schema mit — beim ersten Lauf hier waren das 46 Einträge einer Metrik aus einem Lauf ohne Theme-Dimension.

---

### 2026-08-02 — Der Umschalter wird sichtbar, und die Emote-Kachel bleibt in beiden Modi dunkel

**Betrifft:** `web/src/app/features/shell/app-shell.ts` · `web/src/app/app.ts` · `web/src/app/features/{usage-stats,voting,channel-workspace,login,overview,my-votings}/*` · `web/e2e/theme.spec.ts` (neu) · `web/src/test-setup.ts` (neu) · `web/angular.json` · `docs/UI-Designsprache.md`

**Der helle Modus ist ab hier benutzbar, und deshalb ist der Umschalter jetzt im Header.** Er stand seit der Tokenebene bereit, war aber nicht gerendert — in einen halb migrierten Zustand schalten zu können wäre schlimmer gewesen als gar nicht schalten zu können. Mit dieser Welle ist die komplette angemeldete Nutzung themefähig; Admin-Bereich und Landing-Page ziehen nach und sind bis dahin dunkel-fest, aber in sich stimmig.

**Die Emote-Kachel bleibt in beiden Modi dunkel (`bg-emote-canvas`), und das ist Absicht.** Die Bilder kommen vom 7TV-CDN und sind für dunkle Chat-Hintergründe gezeichnet: weiße Schrift, helle Outlines, weiße Glow-Kanten. Auf einer weißen Karte verschwindet ein erheblicher Teil davon. Wir haben auf das Material keinen Einfluss und können es nicht pauschal invertieren, ohne die farbigen Emotes zu zerstören — eine bewusst gesetzte, abgerundete „Leinwand" ist gegenüber dem Material ehrlicher als ein hellerer Kasten, in dem manche Emotes einfach fehlen. Das ist das **einzige** themefeste Token; es ist als solches in der Designsprache §2.0 vermerkt und darf sich nicht vermehren. **Nebenwirkung, die dazugehört:** der Selektions-Wash lag bisher *unter* dem Bild (`bg-purple-950/40` auf der Karte, Bild ohne eigene Fläche). Er wandert damit auf die Karte, wo er nicht mehr mit dem Bild um dieselben Pixel kämpft; der `inset-ring` bleibt, wie §8.5 ihn vorschreibt.

**`ThemeService` wird in `App` injiziert, nicht in der Shell — gefunden durch den neuen E2E-Spec, nicht durch Nachdenken.** Ein `providedIn: 'root'`-Service wird erst konstruiert, wenn ihn jemand anfordert. Landing-Page und Login-Seite rendern **außerhalb** der App-Shell; auf beiden existierte der Service also nie, womit ein Systemwechsel bei geöffneter Seite dort nicht durchschlug und die Seite bei dem Wert stehenblieb, den `theme-init.js` beim Laden gestempelt hatte. `App` ist die einzige Komponente, die auf jeder Route rendert — dort gehört die Injektion hin. Zwei der fünf Fälle in `theme.spec.ts` sind genau darüber rot geworden.

**Der Header benutzt jetzt `.app-sticky-bar` statt einer eigenen Blur-Kopie.** Er hatte `bg-slate-950/80 backdrop-blur` handgeschrieben; die Deckung muss aber im Hellen höher sein als im Dunkeln (sonst schmutzt der Text durch den Blur), und dieser Wert lebt in `--ep-sticky-alpha`. Das `z-30` des Headers steht als Utility daneben und schlägt damit das `z-20` der Klasse — Utilities liegen in Tailwind hinter der Components-Ebene. Der Höhenvertrag aus §8.5 (`h-14`) bleibt unberührt.

**Neu: `web/src/test-setup.ts`, global über `angular.json` eingehängt.** jsdom implementiert `window.matchMedia` nicht. Sobald `App` den `ThemeService` zieht, fällt jeder Spec darüber, der die Wurzelkomponente instanziiert — aus einem Grund, der mit dem Testgegenstand nichts zu tun hat. Der Stub ist bewusst der dümpste mögliche (matcht nie, feuert nie); Specs, die Media-Query-Verhalten wirklich prüfen, stellen per `vi.stubGlobal` ihren eigenen, reicheren Fake davor.

---

### 2026-08-02 — Eine Tokenebene für Farbe, und warum der FOUC-Wächter eine eigene Datei ist

**Betrifft:** `web/src/styles.css` · `web/public/theme-init.js` (neu) · `web/src/index.html` · `web/src/app/core/theme/theme.service.{ts,spec.ts}` (neu) · `web/src/app/shared/ui/theme-menu.ts` (neu) · `web/public/i18n/{de,en}.json` · `web/src/app/features/{shell/app-shell.ts,landing/landing-page.html,login/login-page.ts}` · `docs/UI-Designsprache.md` (§2.0, §2.1, §10)

**Farbe ist ab jetzt eine Rolle, kein Palettenwert.** `web/src/styles.css` bekommt einen Tokenblock — Rohwerte als `--ep-*` unter `:root, :root[data-theme='dark']` bzw. `:root[data-theme='light']`, registriert über `@theme inline`. Damit heißt es `bg-surface` statt `bg-slate-900`, und die Kartenfläche ist wieder *eine* Entscheidung statt einer, die an 36 Stellen erneut getroffen wird. Bewusst **kein** `dark:`-Variantenansatz: der hätte 428 Utilities auf ~850 verdoppelt, jede spätere Farbänderung zu einer Suche über 36 Dateien gemacht und vor allem die Regel „es gibt genau eine Kartenfläche" (Designsprache §2.1) an jeder Verwendungsstelle zur Disposition gestellt — genau die Divergenz („warum ist diese Karte `bg-slate-50`, jene `bg-white`?"), an der `dark:`-Codebasen verfallen. Er hätte die harten Fälle auch gar nicht gelöst: Glow, Backdrop und Sticky-Bar sind keine Utilities und hätten ohnehin Variablen gebraucht.

**`@theme inline` ist Pflicht, nicht Geschmack.** Ohne `inline` backt Tailwind den Wert zur Build-Zeit in jedes Utility ein, und die Umschaltung erreicht sie nie. Die Kehrseite: mit `inline` emittiert Tailwind die `--color-*`-Namen **nicht** ins Ergebnis, handgeschriebenes CSS in derselben Datei muss also `var(--ep-*)` referenzieren und nicht `var(--color-surface)`. Verifiziert am kompilierten Stylesheet: alle 38 Utilities lösen auf `var(--ep-…)` auf, und der Alpha-Modifier (`bg-surface/70` → `color-mix`) funktioniert über Variablen hinweg.

**Das `data-theme`-Attribut sitzt auf `<html>`, nicht auf dem Shell-`<div>`.** Der CDK-Overlay-Container hängt als Geschwister der App **außerhalb** der Shell-DOM — das ist dieselbe Eigenschaft, die schon die explizite Textfarbe in `.cdk-overlay-pane.app-dialog-panel` erzwungen hat (§7, CDK-Falle 1). Säße das Attribut auf der Shell, behielten Dialoge und Popover den Modus, in dem sie *nicht* geöffnet wurden.

**Der Anti-FOUC-Wächter ist eine externe Datei, weil die Api eine CSP ausliefert.** `Program.cs` setzt auf **jeder** Response `script-src 'self'` ohne `'unsafe-inline'` — auch auf `wwwroot` und dem SPA-Fallback. Ein Inline-`<script>` in `index.html` wäre schlicht blockiert. Ein `'sha256-…'`-Eintrag in der CSP-Zeile wäre möglich, aber still brüchig: jede Reformatierung des Skripts bräche die Seite **ausschließlich im Container**, nie unter `ng serve`, wo keine CSP greift. `web/public/theme-init.js` fällt unter `'self'`, wird von `angular.json` unverändert nach `wwwroot` kopiert und bekommt von `ApplyStaticCacheHeaders` korrekt `no-cache`. Keine CSP-Änderung nötig — und das ist der Punkt.

**Drei Zustände, nicht zwei.** `'system' | 'light' | 'dark'`, Default `'system'`. Ein reiner Zweizustand-Toggle kann „folge dem System" nicht ausdrücken; wer einmal umgeschaltet hat, säße für immer fest — auch durch eine systemweite Änderung hindurch, die er absichtlich vorgenommen hat. `localStorage['emotepurge.theme']` ist dafür in Ordnung: eine Darstellungspräferenz ist keine Sitzungsinformation, die Regel „Auth-Session gehört nicht in `localStorage`" bleibt unberührt.

**`theme-color` sind zwei Tags mit `media`, und der Service korrigiert nur deren `media`.** Zwei Tags decken den `'system'`-Fall ganz ohne JavaScript ab. Bei expliziter Wahl gegen die Systempräferenz zeigte aber weiterhin das falsche Tag; der `ThemeService` schaltet deshalb `media` auf `all` bzw. `not all` und gibt sie bei `'system'` an die Media-Queries zurück. Die Farbwerte selbst bleiben in `index.html` und existieren dadurch an genau einer Stelle.

**Der Umschalter existiert, wird aber noch nicht gerendert.** `<app-theme-menu>` ist gebaut und über `<app-popover>` gelöst (kein `SegmentedControl` — drei Textsegmente kosten in einer `h-14`-Kopfzeile zu viel Breite; kein durchklickender Icon-Button — bei drei Zuständen ist der nächste nicht ansagbar). Eingebunden wird er erst, wenn die eingeloggte App themefähig ist, damit niemand in einen halb migrierten Zustand schalten kann.

**`.app-page-glow` ersetzt drei byte-identische Kopien** des Radial-Gradients (Shell, Landing, Login). Das war Voraussetzung, nicht Kür: was dreimal wortgleich dasteht, kann man nicht an einer Stelle themen. Im Hellen fallen die beiden Stops von `.14`/`.10` auf `.06`/`.04` — eine additive Lichtsimulation hat auf hellem Grund kein Äquivalent und wird zur Papierfärbung statt zum Leuchten.

**Keine `light:`-Variante registriert.** Das Konzept sah eine vor, für Fälle, in denen sich die Modi *strukturell* statt nur im Wert unterscheiden. Beim Bauen ist keiner übrig geblieben: Glow, Karten- und Overlay-Schatten sowie die Kalenderleiter sind Wertunterschiede und laufen über `--ep-page-glow` / `--ep-shadow-*` / Tokens, und der Logo-Tausch in Welle 4 läuft über ein Signal aus dem `ThemeService` statt über CSS — was dem Variantenweg überlegen ist, weil ein `prefers-color-scheme`-`<picture>` eine *explizite* Nutzerwahl nicht überstimmen kann. Eine Variante mit null Verwendungen ist toter Code; sie wird registriert, wenn ein echter Fall auftaucht.

**`color-scheme` wandert von `body` in den Tokenblock** und wird pro Modus gesetzt. Damit folgen `input[type="time"]` im DateTime-Picker, Scrollbars und Autofill-Hintergründe dem Modus von selbst — eine Zeile, die eine Menge Handarbeit ersetzt. Die entsprechende Zeile in §10 der Designsprache war damit falsch und ist korrigiert.

---

### 2026-08-02 — Der dunkle Modus verfehlte an zwei Stellen AA, bevor der helle überhaupt anfängt

**Betrifft:** `web/src/styles.css` (`.app-input`, `.app-input-sm`) · `web/src/app/shared/datetime/datetime-picker.ts` · 13 Vorkommen `text-slate-500` unter `web/src/app/` · `docs/UI-Designsprache.md` (§5.1, §10) · `docs/Konzept-Light-Mode.md` (neu)

**Beim Durchrechnen des Tokensatzes für den hellen Modus sind zwei Bestandsverstöße aufgefallen, die nichts mit dem hellen Modus zu tun haben.** `text-slate-500` erreicht auf der Kartenfläche `slate-900` nur **3,7:1** und auf der Seitenfläche `slate-950` **4,2:1** — beides unter den 4,5:1, die §10 der Designsprache als Minimum führt. Betroffen waren 13 Stellen: Hinweistexte unter Checkboxen, die Wochentagsköpfe des Kalenders, Meta-Zeilen, der inaktive Sprachumschalter, der Landing-Footer, der „Trend unverändert"-Pfeil. Alle gehen auf `slate-400` (7,0:1 bzw. 7,9:1). Die Stufe verschwindet damit als Textfarbe aus dem Projekt; sie war nie eine eigene Bedeutungsebene, sondern „noch etwas leiser als leise".

**Der zweite Verstoß ist der Rand der Eingabefelder, und er wiegt schwerer.** `.app-input`/`.app-input-sm` trugen `slate-700` — auf der Kartenfläche **1,7:1**. WCAG 1.4.11 verlangt 3:1 für die visuelle Begrenzung, die ein Bedienelement überhaupt erst als solches erkennbar macht; ein Eingabefeld ohne wahrnehmbaren Rand ist ein Stück Fläche. Neu ist `slate-500` (3,7:1 auf der Karte, 3,6:1 gegen die eigene Füllung `slate-950`). Mitgezogen sind die beiden Stellen im DateTime-Picker, die `.app-input` von Hand nachbauen statt es zu benutzen (Trigger und Zeitfeld) — beim Trigger dreht sich dabei der Hover mit, der vorher auf `slate-600` *dunkler* wurde als der neue Grundzustand.

**Warum das ein eigener, vorgelagerter Commit ist und nicht im Theming-Diff steckt:** Beide Änderungen sind im *dunklen* Modus sichtbar. Wären sie Teil der Token-Umstellung, ließe sich hinterher nicht mehr sauber trennen, ob ein „Dark sieht anders aus als vorher" ein beabsichtigter Fix oder ein Umbaufehler ist. So ist die Token-Ebene, die als Nächstes kommt, bitgleich zum Bestand — und jede Abweichung danach ist ein Bug.

**Ausdrücklich nicht mitgenommen:** `disabled:opacity-50` an `shared/ui/button.ts`. Das ist ausschließlich im hellen Modus ein Problem (Weiß auf 50 % `purple-600` über Weiß = 2,3:1); im Dunkeln fällt es nicht durch, und ein Disabled-Zustand ist von 1.4.3 ohnehin befreit. Es wird dort behoben, wo es entsteht. Ebenso bleiben die drei uneinheitlichen Purple-Stufen für „ausgewählt/primär" (`purple-600` beim Button, `purple-700` bei SegmentedControl und Zeitraum-Menü) vorerst stehen — sie sind eine Inkonsistenz, kein Kontrastfehler, und die Token-Ebene macht sie erst richtig sichtbar.

`docs/Konzept-Light-Mode.md` kommt als freigegebenes Eingangsdokument mit in diesen Commit; §5.3 dort ist die Rechnung, auf der er beruht.

---

### 2026-08-02 — Ein drittes Testprojekt für die Api

**Betrifft:** `tests/EmotePurge.Api.Tests/` (neu) · `src/EmotePurge.Api/Program.cs` · `src/EmotePurge.Api/EmotePurge.Api.csproj` · `EmotePurge.slnx` · `CLAUDE.md`

**Es gibt jetzt ein Testprojekt für die Api, und die alte Begründung dagegen wird ergänzt, nicht ersetzt.** Sie lautete: „die dünnen Handler delegieren an die getestete Infrastructure-Schicht" — das stimmt für die Handler und stimmt nicht für die fünf `IEndpointFilter`-Klassen davor. Die sind enge Verwandte voneinander: `VoteAudienceFilter` und `VoteEligibilityFilter` unterscheiden sich in genau einem Switch-Arm, `UsageStatsAccessAuthorizationFilter` und `ChannelManagementAuthorizationFilter` in genau einem Methodenaufruf. Ein Copy-Paste-Fehler an einer dieser Stellen öffnet Daten dem falschen Publikum, ohne dass irgendetwas rot wird — und die Handler-Delegations-Begründung deckt genau diesen Bereich nicht ab. `tests/EmotePurge.Api.Tests` fährt deshalb per `WebApplicationFactory` die **echte** Route-Tabelle ab statt nachgebauter Endpoints: die Hälfte des Werts liegt darin, dass ein Filter am richtigen Endpoint hängt. Neues Paket: `Microsoft.AspNetCore.Mvc.Testing`.

**Container-frei, und die Grenze dafür ist `IConnectionMultiplexer`.** Substituiert werden nur `IChannelAccessService`, `IVoteEligibilityService`, `IChannelService` und der Multiplexer; der restliche Service-Graph bleibt echt. Der Multiplexer muss mit hinein, weil `RequestDelegateFactory` die injizierten Services eines Handlers auflöst, **bevor** die Endpoint-Filter-Pipeline läuft — ein Request, den ein Filter gleich mit 403 abweist, konstruiert trotzdem den ganzen Graph, und `ConnectionMultiplexer.Connect` läuft in der DI-Registrierung sofort. Ohne die Substitution liefen sieben Fälle zwölf Sekunden in einen Redis-Timeout und antworteten 500 statt des Codes unter Test. In Produktion ist das folgenlos (längst verbundener Singleton), aber es ist die Eigenschaft, die man kennen muss, bevor man einen Handler mit einer teuren Abhängigkeit hinter einen Filter hängt.

**Zwei Zugeständnisse an die Testbarkeit im Produktionscode, beide bewusst.** `Program.cs` endet auf `public partial class Program;`, weil Top-Level-Statements sonst eine interne `Program`-Klasse erzeugen, die `WebApplicationFactory<T>` nicht erreicht. Und `EmotePurge.Api.csproj` bekommt `InternalsVisibleTo` für das Testprojekt: die Alternative wäre, `ApiErrorCodes`-Literale wie `"vote_session_ended"` im Test abzutippen — dann bricht ein umbenannter Code den Test nicht, sondern lässt ihn still gegen einen Wert prüfen, den niemand mehr zurückgibt. Die Api-Assembly wird dadurch minimal offener; der Gegenwert ist, dass die Tests die echte Pipeline fahren statt einer Kopie.

**Konventionserweiterung (Regel 11):** Ein neuer `IEndpointFilter` oder eine geänderte Filter-Reihenfolge an einer `MapGroup` bekommt seinen Fall in `tests/EmotePurge.Api.Tests`. Endpoint-*Handler* ausdrücklich nicht — die bleiben dünn, und für die gilt die alte Begründung unverändert weiter.

---

### 2026-08-02 — Die Schichtenregel für Core ist ein roter Build, und der Worker kennt EF Core nicht mehr

**Betrifft:** `tests/EmotePurge.Infrastructure.Tests/Unit/CoreAssemblyReferenceTests.cs` (neu) · `src/EmotePurge.Core/Services/IChannelService.cs` · `src/EmotePurge.Infrastructure/Services/ChannelService.cs` · `src/EmotePurge.Worker/Worker.cs` · `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs` · `CLAUDE.md`

**Die Schichtenregel für `EmotePurge.Core` ist ab jetzt ein roter Build.** `CoreAssemblyReferenceTests` prüft per Reflection, dass `typeof(Channel).Assembly.GetReferencedAssemblies()` nichts enthält, das mit `Microsoft.EntityFrameworkCore`, `StackExchange.Redis`, `Microsoft.AspNetCore` oder `System.Net.Http` beginnt — und in einem zweiten Fall, dass Core kein Geschwisterprojekt referenziert (die Gegenrichtung derselben Regel: eine Referenz auf `EmotePurge.Infrastructure` würde die Abhängigkeitsrichtung umdrehen, ohne eine der vier Technologien zu nennen). Bewusst **kein** NetArchTest: zwanzig Zeilen Reflection kosten kein Paket, und die Assert-Message nennt die verletzende Assembly beim Namen. Läuft im bestehenden CI-Gate mit, weil `publish.yml` die Solution fährt.

**`IChannelService.ListActiveChannelNamesAsync` ersetzt die letzten zwei direkten `AppDbContext`-Zugriffe außerhalb der Infrastructure.** `Worker.cs` und `SevenTvPeriodicResyncWorker.cs` schrieben zeichengleich dieselbe Query; „welche Channels sind aktiv" ist eine fachliche Frage und gehört hinter ein Interface. Beide `using`-Direktiven auf `EmotePurge.Infrastructure.Persistence` sind damit aus dem Worker verschwunden — er kennt EF Core nicht mehr. Die Schichtentreue-Tabelle in `CLAUDE.md` verliert ihre „aktuell noch 2 Verstöße"-Fußnote. `AsNoTracking`, weil der Resync-Worker die Query jede Minute für immer ausführt und niemand die Entitäten mutiert.

---

### 2026-08-01 — Ein Popover-Primitive, ein Zeitraum-Dropdown, und „Neu" hört auf, das Emote zu verdecken

**Betrifft:** `web/src/app/shared/ui/popover.ts` (neu) · `web/src/app/shared/datetime/date-range-menu.ts` (neu) · `web/src/app/shared/datetime/date-range-popover.ts` (gelöscht) · `web/src/app/shared/ui/segmented-control.ts` · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/src/app/features/admin/admin-monitoring-page.ts` · `web/e2e/audit/ui-audit.audit.ts` · `web/public/i18n/{de,en}.json` · `docs/UI-Designsprache.md`

**Das „relative Wrapper + absolutes Panel + document-click + Escape"-Muster war dreimal von Hand gebaut** — Custom-Range-Popover, Datetime-Picker, Shell-Disclosure — mit drei leicht verschiedenen Dismiss-Regeln, und nur zwei der drei hatten ein `max-w`, das ein 360-px-Viewport nicht sprengt. `shared/ui/popover.ts` ist jetzt die eine Stelle. Bewusst **kein** CDK-Overlay: diese Panels öffnen aus einer Sticky-Filterleiste heraus und müssen deren Stacking-Kontext erben (§8.5), was ein an `<body>` gehängter Container nicht kann. Modale Flächen bleiben Sache von `@angular/cdk/dialog` (§7); das Primitive ist der nicht-modale Fall und bringt deshalb **keinen** Fokus-Trap mit. Vertrag: gerendert = offen, das Panel versteckt sich nie selbst, der Host besitzt das Sichtbarkeits-Signal *und* die Fokus-Rückgabe. Padding bringt der Inhalt mit, damit Full-Bleed-Menüzeilen und gepolsterte Formulare beide hineinpassen.

**Der Zeitraum ist ein Dropdown statt vier Segmenten — und das aus drei Gründen, von denen Breite der schwächste ist.** Erstens hört der Custom-Range auf, ein Trick zu sein: er war ein Segment, das ein Panel öffnete, und Wiederöffnen hieß, das bereits gewählte Segment erneut zu klicken — `SegmentedControl.reselected` existierte für nichts anderes und ist mit diesem Commit weg. Zweitens ist der gewählte Zeitraum wieder lesbar: der Trigger benennt ihn, vorher stand er ausschließlich in einer Leiste, die beim Scrollen verschwand. Drittens laden Pfeiltasten nicht mehr nach — das Segmented Control selektiert bei Fokus, ein Durchpfeilen löste also pro Tastendruck einen vollen Reload über den `from`/`to`-Effect aus. APG erlaubt einer Radiogroup, Selektion von Fokus zu entkoppeln, genau wenn Selektieren einen Seiteneffekt hat; Pfeile bewegen jetzt den Fokus, Enter/Space committet.

**Der Einwand aus dem Eintrag weiter unten kippt, er gilt nicht mehr:** dort steht, ein eigener Trigger-Button habe die deutsche Toolbar auf eine dritte Zeile geschoben, weshalb das Popover trigger-los wurde. Das galt für einen Button **zusätzlich** zum Segmented Control. Dieser hier **ersetzt** es — die Leiste wird schmaler, nicht breiter. Die Preset-Labels wandern von `usageStats.preset*` nach `dateRange.*`, weil sie jetzt einer Shared-Komponente gehören.

**„Seit Beginn" ist der neue Standard-Zeitraum.** Die Seite beantwortet die Frage „ist dieses Emote je benutzt worden" — ein rollendes 7-Tage-Fenster beantwortete eine andere und lag dabei zweimal daneben: ein langgedientes Emote mit einer ruhigen Woche sah aus wie ein Löschkandidat, und auf einem frisch gejointen Channel reichte das Fenster zwangsläufig vor den Tracking-Beginn zurück, sodass das Erste, was ein neuer Nutzer sah, das Warnbanner über unvollständige Daten war. Genau dieses Banner wird für den `all`-Preset unterdrückt: „seit Beginn" *ist* die Definition, es vor sich selbst zu warnen wäre Rauschen bei jedem Seitenaufruf. **Preis, bewusst bezahlt:** die Trendpfeile verschwinden in der Standardansicht, weil `usageTrend` das Vorgängerfenster vor dem Tracking-Beginn korrekt als `unknown` meldet — sie kommen zurück, sobald jemand 7 oder 30 Tage wählt. **„Seit Beginn" reicht maximal 365 Tage zurück, und das ist ein Vertrag, keine Vorliebe.** `GET /api/channels/{name}/usage-stats/totals` beantwortet eine Spanne über `maxRangeDays = 366` mit `RangeTooLarge` — ein erster Entwurf mit einer festen Sentinel („2020-01-01", weit vor Projektbeginn) hat den Default-Seitenaufruf dadurch reproduzierbar in ein 400 laufen lassen: nach einem F5 war das Grid leer, nach manueller Zeitraumwahl wieder da, weil ab dem ersten Paint der echte `trackedSince` über den `earliest`-Input greift. `allTimeStart(earliest)` klemmt deshalb auf das breiteste Fenster, das der Endpunkt annimmt (365, einen Tag innerhalb der Grenze — `daysAgo` rechnet lokal, `toIsoDate` druckt UTC, und die beiden dürfen sich um einen Tag uneins sein). Für jeden Channel, der jünger als ein Jahr ist — heute alle —, liefert das exakt dieselben Zeilen wie der wahre Startpunkt. **Wenn der erste Channel ein Jahr Tracking erreicht (frühestens 2027-07), hört „Seit Beginn" auf, seinen Namen zu verdienen**; die Antwort darauf ist dann ein offenes `from` serverseitig, nicht eine größere Zahl im Frontend. `date-range-menu.spec.ts` pinnt genau diesen Fall, damit er nicht stillschweigend eintritt. Nicht auf `earliest` zu *warten* bleibt Absicht: der Wert kommt aus einem zweiten Request, und das erste Grid-Rendering müsste sich dahinter einreihen. Der „Nutzung zählen ab"-Prefill des Vote-Session-Dialogs bekommt weiterhin den Tracking-Beginn statt `from()` — sonst behauptete eine neue Session, sie zähle seit einem Jahr, auf einem Channel, der seit zwei Wochen läuft.

**Dieselbe Decke gilt für den Custom-Range, und die Seite sagt jetzt beides.** Ein von Hand gewählter Zeitraum über der Grenze lief in `range_too_large` — der Code ist seit jeher übersetzt, nur zeigte `loadTotals` stur `usageStats.errors.loadFailed`, also „Nutzungsdaten konnten nicht geladen werden": das liest sich als „der Server ist kaputt" statt als „dein Zeitraum ist zu breit". Der Fehlerpfad geht jetzt durch `apiErrorTranslationKey` und behält den generischen Schlüssel nur als Fallback für Antworten ohne Body. Davor liegt Prävention statt Belehrung: die beiden Datumsfelder begrenzen sich gegenseitig (`min`/`max` = der jeweils andere Wert, an den Rand des erlaubten Fensters geschoben, das Ende zusätzlich bei heute), sodass der Picker den unmöglichen Tag ausgraut. Getippte Werte kommen daran vorbei — ein Datumsfeld markiert einen Wert außerhalb `min`/`max` als invalid, emittiert ihn aber trotzdem —, weshalb die Fehlerbehandlung das eigentliche Sicherheitsnetz bleibt und nicht durch die Bounds ersetzt wird.

**Der Filter-Counter zieht in die Filterleiste.** „409 von 409 Emotes" stand als eigener Absatz direkt über der Slot-Bar — und damit lagen zwei unverwandte „X von Y"-Zeilen übereinander, die eine ein Filterergebnis, die andere ein Kapazitätszustand. Der Counter gehört zu den Controls, die ihn erzeugen: `ml-auto` am Ende der Toolbar, lila und fett sobald irgendein Filter greift, ungefiltert grau. Nebenbei richtet sich der Plural jetzt nach der Zahl, die zuerst dasteht (`shown`), nicht nach `total` — „1 von 409 Emotes" war schlicht falsch.

**Das „Neu"-Badge verdeckte das Emote, das es beschriftete.** Es lag mit `absolute inset-0` mittig über dem 40×40-Bild und hatte einen deckenden Hintergrund. Das Muster war von der Voting-Seite übernommen, wo es für „nicht mehr im Set" richtig ist — dort *soll* das Emote verschwinden, und das Bild wird zusätzlich auf `opacity-40` gedimmt. Für „neu" ist es die falsche Semantik. Der Hinweis steht jetzt auf der Zeit-Zeile der Karte (`Neu · 3 Tage`, blau), wo ohnehin „zuletzt benutzt" steht; der verdrängte Text wandert ins `title`. **Eine Ecken-Markierung oder ein farbiger Rahmen wurden verworfen**: auf einem Grid, das für Massenselektion gebaut ist, liest sich jede zusätzliche Rand-Auszeichnung als „ausgewählt", und §8.5 reserviert genau dafür `inset-ring-purple`. Die Kartenhöhe bleibt `h-36`, `ROW_HEIGHT_PX` also unangetastet. Der Tone `blue` ist damit auch nicht länger doppelt belegt (§4.3 führt ihn als „Moderator").

**Der „Aktualisieren"-Button im Admin-Monitoring bleibt — er war nur kaputt.** Er lud ausschließlich die Health-Resource; `AdminRosterCard` hat eine eigene `reload()`, die niemand aufrief, womit die Roster-Karte über kein UI erzwingbar war und ausschließlich am 60-s-`worker.roster`-Push hing. Jetzt lädt der Button beides. Entfernt wird er **nicht**: `LiveUpdateService` hat bewusst keinen Polling-Fallback (der Klassenkommentar nennt diesen Button ausdrücklich als die Gegenseite dieser Entscheidung), bei einem fatalen Close bleibt genau ein Retry beim Tab-Fokus — und das hier ist die Seite, auf der ein Admin das merkt. Damit er nicht vestigial aussieht, steht daneben ein Badge aus dem bislang ungenutzten `LiveUpdateService.status`, das sagt, in welchem der beiden Zustände die Seite gerade ist.

**Der Audit-Harness kann jetzt geöffnete Zustände.** `Scenario` bekommt ein optionales `afterLoad`; das erste Szenario, das es nutzt, öffnet das Zeitraum-Menü auf seinem Custom-Eintrag. Popovers waren für die Overflow- und Touch-Target-Metriken bisher unsichtbar — das trigger-lose Vorgänger-Popover hatte in keinem der drei Viewports je einen Screenshot.

---

### 2026-08-01 — Der Worker sagt, in welchen Channels er wirklich ist: Roster-Key, Support-Drilldown und ehrlich beschriftete Decken

**Betrifft:** `src/EmotePurge.Worker/{ITwitchChatManager,TwitchChatManager,WorkerIdentity,WorkerRosterPublisher,WorkerHealthPublisher,Program}.cs` · `src/EmotePurge.Worker/SevenTv/{ISevenTvEventClient,SevenTvEventClient,SevenTvSubscriptionRegistry}.cs` · `src/EmotePurge.Core/Services/{IWorkerRosterReader,IWorkerHealthReader,IAdminChannelQueryService}.cs` · `src/EmotePurge.Core/Entities/Channel.cs` · `src/EmotePurge.Core/Messaging/LiveEvents.cs` · `src/EmotePurge.Infrastructure/Redis/WorkerHealthReader.cs` · `src/EmotePurge.Infrastructure/Services/{AdminChannelQueryService,SevenTvSyncService}.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `src/EmotePurge.Api/Health/WorkerCapacity.cs` (neu) · `src/EmotePurge.Infrastructure/Migrations/20260801195038_AddChannelLastSyncedAt.cs` · `web/src/app/core/admin/admin.{model,service}.ts` · `web/src/app/core/live/live-event.model.ts` · `web/src/app/features/admin/{admin-roster-card,admin-channel-detail-page,admin-monitoring-page,admin-channels-page}.ts` · `web/src/app/app.routes.ts` · `web/public/i18n/{de,en}.json`

**Das Problem, das dieses Paket löst, ist stumm.** Ein Channel, der nach einem Reconnect nicht wieder gejoint wurde, zählt nichts mehr — und sagt das nirgends. Die Health-Karte bleibt grün (die Verbindung *steht* ja), die Usage-Stats zeigen weiter Zahlen, sie werden nur nicht mehr größer. Auf genau diesen Zahlen löscht dann jemand Emotes. Bis jetzt war der Zustand ausschließlich über SSH und `docker compose logs` sichtbar.

**Ein zweiter Redis-Key, nicht mehr Felder im Health-Snapshot.** `worker:roster`, 60-s-Takt, TTL 180 s (dieselbe Drei-Ticks-Regel wie beim Health-Key). Der Health-Key wird bei jedem `worker.health`-Event von jeder offenen Monitoring-Seite neu gelesen — alle 20 Sekunden — und trägt einen bewusst stabilen „gleiche Form auch wenn nicht verfügbar"-Vertrag; zwei unabhängige `snapshotAvailable`-Flags in einer Payload wären genau die Mehrdeutigkeit, die dieser Vertrag vermeidet. Dazu kommt: der Roster wächst mit der Channel-Zahl, der Health-Snapshot nicht. Entsprechend ein eigener Hosted Service (`WorkerRosterPublisher`, damit sieben statt sechs) statt eines zweiten Timers im `WorkerHealthPublisher` — der ist ein einzelner `PeriodicTimer` in do/while und hätte einen anderen Abhängigkeitssatz mitschleppen müssen.

**Veralterung wird über das Alter gemeldet, nicht über die TTL.** Eine längere TTL macht Veralterung gerade *unsichtbar*: der Key ist dann da und niemand weiß, wie alt. `GeneratedAtUtc` reist deshalb in der Payload mit und die Api rechnet daraus `ageSeconds`. Der Key wird beim Shutdown **nicht** gelöscht — ein sauberer Stopp und ein Absturz sollen für einen Leser gleich aussehen, die TTL erledigt beides.

**`BootRecoveryCompleted` ist die wichtigste Einzelangabe im Snapshot.** `_desiredChannels` ist nach einem Neustart leer, bis die Boot-Recovery alles gejoint hat; ohne dieses Flag meldete die Roster-Karte bei **jedem** Deploy eine Minute lang „alle Channels fehlen" und wäre nach dem dritten Mal Rauschen, das niemand mehr liest. Dasselbe gilt für `MarkAllChannelsUnconfirmed()` bei Disconnect/Recreate — Gegenmittel ist das Alter plus `ProcessStartedUtc`, nicht ein kürzerer Takt.

**Zwei Wahrheiten nebeneinander, nie zusammengeführt.** `GET /api/admin/channels/{name}` liefert die DB-Zeile und die Worker-Sicht als getrennte Objekte. Die Diagnose *ist* ihr Widerspruch: eine gesunde Zeile ohne Roster-Eintrag heißt „wird seit einem Reconnect nicht mehr gezählt". `roster.channel === null` bei `available: true` ist deshalb ein Befund, kein fehlender Wert, und wird als solcher benannt.

**`Channel.LastSyncedAtUtc` trennt „Sync lief" von „Inventar änderte sich".** Bisher war `AdminChannelDto.LastSyncedAtUtc` das `MAX(Emote.LastSyncedAt)` — und `UpsertEmote` stempelt eine Emote-Zeile nur bei tatsächlicher Änderung. Ein Channel, der seit drei Tagen minütlich erfolgreich syncht, dessen Set aber niemand anfasst, las sich damit als „zuletzt vor 3 Tagen synchronisiert": die exakt falsche Schlussfolgerung auf einer Support-Seite. Das Bestandsfeld heißt jetzt `LastInventoryChangeUtc`, das neue `LastSyncedAtUtc` wird im REST-Vollsync **unbedingt** geschrieben — aber außerhalb der Änderungserkennung und **nie** im Delta-Pfad, weil `ApplyEmoteSetUpdateAsync` über den ChangeTracker entscheidet. Der Empty-Set-Guard schreibt es ebenfalls nicht: dort wurde nichts gegen 7TVs echten Zustand abgeglichen, und ein Stempel behauptete das Gegenteil.

**Die Decken sind einzeln beschriftet, mit ihrer Herkunft.** Twitch bekommt **zwei** Zahlen, weil es zwei sind: die dokumentierte Decke von 100 gleichzeitig gejointen Chatrooms je Account (seit 2024-05-15, in `CLAUDE.md` festgehalten) und unser eigenes Betriebsbudget von 20 Channels, das darunter liegt und heute allein bindet — TwitchLib rejoint nach einem Reconnect ungedrosselt mit ~180 ms Abstand, oberhalb von etwa zwanzig Channels reißt ein einzelner Reconnect also Twitchs Fenster von 20 JOINs/10 s. Beide zu einer „Kapazität" zu verrechnen gäbe unsere Betriebsentscheidung als Twitchs Regel aus. Die 7TV-Auslastung bekommt endlich ihren echten Nenner: das `subscription_limit` aus dem letzten Hello-Frame, im Client absichtlich **über Disconnects hinweg gehalten** (die letzte Zahl von 7TV schlägt „unbekannt"), mit der 500er-Konstante nur noch als Fallback. Die 7TV-REST-Last ist **kein** Limit, sondern eine Rate — 7TV veröffentlicht keine Quote, also gibt es keinen ehrlichen Nenner, und die Seite nennt „N Requests je 60-s-Tick ≈ x/s" statt einen Balken zu zeigen. Dafür reist `SevenTv:ResyncIntervalSeconds` im Health-Snapshot mit; die Api kennt die Worker-Konfiguration nicht.

**Der Join passiert in der Api, nicht im Browser.** `/api/admin/roster` liest den Key plus die aktiven Channel-Namen (eigene schmale Methode — die Liste wird bei jedem `worker.roster` neu geholt und hat für drei Aggregat-Queries über Emotes und Votes keine Verwendung) und liefert Zählungen plus **gekappte** Defizitlisten mit ihren ungekappten Totals daneben. Der Publisher kappt zusätzlich hart bei 500 Channels und sagt es über `Truncated`: eine gekappte Liste, die vollständig aussieht, ist schlimmer als keine.

**Vertragsänderungen:** `AdminChannelDto.LastSyncedAtUtc` bedeutet jetzt etwas anderes (letzter erfolgreicher Sync), die alte Bedeutung heißt `LastInventoryChangeUtc`; dazu kommen `ActiveEmoteSetId`, `ActiveEmoteSetCapacity` und `TrackingResumedAt`. `GET /api/admin/health` liefert zusätzlich `sevenTv.resyncIntervalSeconds` und `worker.{instanceId,processStartedUtc}`, und `sevenTv.subscriptionLimit` kommt bevorzugt aus dem Hello. Neu: `GET /api/admin/roster`, `GET /api/admin/channels/{channelName}`, Live-Event-Typ `worker.roster` (im Admin-Stream), Route `admin/channels/:channelName`. Der Stretched Link der Admin-Channel-Liste zeigt jetzt auf den Drilldown statt in den Channel-Workspace — aus dieser Liste heraus lautet die nächste Frage fast immer „was ist damit los", nicht „zeig mir seine Emotes"; der Workspace bleibt als sekundäre Aktion im Drilldown erreichbar.

**`TwitchChatManager` bekommt bewusst keinen Test** (Regeln 11/16 — Transport-Klassen werden live verifiziert). Getestet sind die reinen Teile: `SevenTvSubscriptionRegistry.GetChannelStates()` (ein geteiltes Set macht **beide** Channels bestätigt, eine fehlende 7TV-User-ID heißt „keine User-Subscription gewünscht" und nie „ausstehend") und `WorkerRosterReader` inklusive des Falls, dass eine Payload älteren Schemas ohne Exception deserialisiert.

---

### 2026-08-01 — Nutzungs-Kontext statt nackter Summe: „zuletzt benutzt", Karenz und Trend

**Betrifft:** `src/EmotePurge.Core/Entities/Emote.cs` · `src/EmotePurge.Core/SevenTv/SevenTvModels.cs` · `src/EmotePurge.Core/Services/IUsageStatQueryService.cs` · `src/EmotePurge.Infrastructure/SevenTv/{SevenTvApiDtos,SevenTvEmoteJsonMapper}.cs` · `src/EmotePurge.Infrastructure/Services/{SevenTvSyncService,UsageStatQueryService,VoteSessionQueryService}.cs` · `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs` · `src/EmotePurge.Infrastructure/Migrations/20260801191203_AddEmoteFirstSeenAt.cs` · `web/src/app/core/usage-stats/usage-stat.model.ts` · `web/src/app/shared/emotes/{emote-context,emote-usage-filter}.ts` · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/public/i18n/{de,en}.json`

**Drei Angaben, ein Paket** — A2, A3 und A4 aus [Feature-Ideen-2026-08-01.md](Feature-Ideen-2026-08-01.md), bewusst nicht einzeln ausgeliefert. „0 Nutzungen in 30 Tagen" unterscheidet nicht zwischen „seit acht Monaten tot" und „letzte Woche noch aktiv"; „nie benutzt" ohne „getrackt seit" behauptet eine Aussage über Zeit, in der wir gar nicht gezählt haben; und ohne Karenz löscht ein Mod-Team beim ersten Durchlauf genau die Emotes, die vorgestern hinzugefügt wurden und notwendigerweise 0 Nutzungen haben. Einzeln ist jede der drei Angaben irreführend, zusammen ergeben sie ein Urteil.

**`LastUsedDate` ist absichtlich *nicht* vom gewählten Zeitraum begrenzt.** Begrenzt wäre es eine Umformulierung der Summe — „0 im Zeitraum" sagt das schon —, und ein Wechsel auf „7 Tage" meldete fast das ganze Set als nie benutzt. Es kostet nichts: `UsageStatFlushService` schreibt nur Zeilen für Tage *mit* Nutzung, also ist `MAX(Date)` je Emote direkt das gesuchte Datum und `null` die ehrliche Antwort für „nie benutzt", keine fehlende. Alle drei Aggregate (Summe im Fenster, Summe im Vorfenster, unbegrenztes Maximum) laufen als konditionale Summen in **einer** Query über den Covering-Index `(EmoteId, Date) INCLUDE (UseCount)` — der Index war für genau das gebaut.

**Das Vorfenster ist gleich lang und schließt unmittelbar an, nicht „die letzten 30 gegen die 30 davor".** Der Nutzer wählt den Zeitraum; ein fest verdrahtetes 30-Tage-Fenster vergliche etwas anderes als das, was in der Liste steht. Beide Grenzen sind inklusiv, das Vorfenster endet also am Tag vor `from`.

*(Überholt 2026-08-03, s. Eintrag oben: das v3-`timestamp` erwies sich als Upload-Datum des Emotes, nicht als Set-Beitritt — die Quelle ist jetzt `addedAt` aus der v4-GraphQL-API, aus dem Write-once-Backfill wurde eine Korrektur-bei-Abweichung.)* **`Emote.FirstSeenAt` kommt aus dem `timestamp` des Set-Eintrags, nicht aus „zuerst gesichtet".** Nur so ist die Angabe für Emotes korrekt, die seit Monaten im Set sind — ein Sicht-Stempel wäre erst ab dem Deploy richtig gewesen und hätte am Tag danach das ganze Set als „neu" markiert. Zwei Vorsichtsregeln hängen daran: geschrieben wird nur, wenn noch nichts bekannt ist (ein bekanntes Datum mit `null` zu überschreiben verlöre es beim nächsten Rename), und **der Backfill zählt nicht als Inventaränderung**. Täte er es, feuerte der erste Resync nach dem Deploy für jeden Channel gleichzeitig `channel.synced` und ließe jede offene Seite refetchen — für ein Datum, nach dem niemand sortiert. Aus demselben Grund backfillt **nur der REST-Pfad**: `ApplyEmoteSetUpdateAsync` entscheidet `NoChange` gegen `Applied` über `db.ChangeTracker.HasChanges()`, ein Backfill dort machte aus einem Nichts-Dispatch ein Live-Event. Der periodische Resync schließt dieselbe Lücke innerhalb eines Ticks, es geht also nichts verloren. Bei der Gelegenheit heißt der Pfad-Schalter in `UpsertEmote` jetzt `fromDispatch` statt `preserveImageUrlWhenLiveEmpty` — es hängen zwei Verhalten daran, und beide folgen aus „diese Payload ist dünner und weniger vertrauenswürdig als eine REST-Antwort".

**Die Karenz-Konstante liegt im Frontend** (`OBSERVATION_PERIOD_DAYS = 14`, in `shared/emotes/emote-context.ts` neben der reinen Funktion, die sie benutzt). Sie beeinflusst ausschließlich Darstellung — ein Badge und ein Filter —; serverseitig gibt es keine Löschkandidaten-Liste, und nach der Voting-Entscheidung vom selben Tag soll auch keine entstehen. 14 Tage, weil ein Streamer mit drei Sendungen pro Woche rund zwei Wochen braucht, bis ein Emote eine faire Chance hatte, und weil es unter dem 30-Tage-Preset bleibt. **Umzugs-Trigger:** sobald ein Server-Pfad frische Emotes von sich aus ausschließen soll (etwa beim Erstellen einer Vote-Session), wandert die Konstante nach `Core` — eine zweite Kopie wäre dann das Problem, nicht der Ort.

**Der Karenz-Filter ist ein eigener Schalter, kein Ausschluss im „Nur ungenutzte"-Filter.** Frische Emotes stumm aus der 0×-Liste zu werfen ließe die Zählzeile („x von y") nicht mehr aufgehen und der Mod fände das gerade hinzugefügte Emote nirgends. Stattdessen: Badge auf der Karte, damit die 0×-Liste auch ohne aktivierten Schalter selbsterklärend ist, plus ein opt-in-Schalter daneben. `FilterableEmote.firstSeenAt` ist **optional** — das Interface teilen sich beide Grids und `VoteSessionResult` trägt das Feld nicht; „fehlt" heißt „nicht in Karenz", denn ein Filter darf nie wegen fehlender Daten ausblenden (dieselbe Haltung wie beim bestehenden `totalUseCount === null`).

**Das Trend-Label wird unterdrückt statt geraten.** `usageTrend()` liefert `'unknown'` und die Karte zeigt dann gar nichts, wenn das Vorfenster vor dem Tracking-Beginn liegt (dessen Null ist unsere Lücke, nicht der Verfall des Emotes), wenn das Emote im Vorfenster noch nicht existierte (sonst liest sich jeder Neuzugang für immer als „im Aufwind" — genau das Artefakt, gegen das die Karenz existiert) oder wenn beide Fenster zusammen unter zehn Nutzungen liegen (zwei gegen eins sind auf dem Papier +100 % und bedeuten nichts). Die Klassifikation lebt im Frontend, weil sie eine Formulierungsentscheidung ist; das Backend liefert nur die Rohzahl. **Abgrenzung zur Entscheidung vom selben Tag:** das ist eine Kontextspalte, kein Rückweg von Chat-Nutzung in den Beliebtheits-Score.

**Vertragsänderungen:** `GET /api/channels/{c}/usage-stats/totals` liefert pro Emote zusätzlich `lastUsedDate`, `previousWindowUseCount` und `firstSeenAt` (weiter ein flaches Array, kein Envelope). `GetUsageTotalsAsync` entfällt zugunsten von `GetTotalsByEmoteIdsAsync`: der einzige verbliebene Aufrufer ist der Vote-Session-Pfad, der bislang die Totals des **ganzen** Channels inklusive Zero-Fill über ~1.000 Emotes holte, um daraus einen Wahlzettel von vielleicht zwanzig zu bedienen und den Rest per `ToDictionary` wegzuwerfen. Auf den Wahlzettel verengt ist der Aufruf billiger als vorher. Der Tracking-Beginn reist im DTO von `/emotes/active-set` mit, das beide Seiten ohnehin abrufen — kein dritter Request pro Seitenaufruf.

**Die Karte wächst von `h-28` auf `h-36`, `ROW_HEIGHT_PX` von 128 auf 160.** Die beiden Zahlen sind ein Vertrag (CDKs Fixed-Size-Strategie nimmt gleich hohe Zeilen an) und wandern immer gemeinsam. Die Karenz zeigt sich als Overlay über dem Bildblock statt als eigene Zeile — dasselbe Mittel, mit dem das Voting-Grid „Nicht mehr im Set" unterbringt. Die Vote-Session-Karte bekommt nichts davon: dort sind laut ihrem eigenen Höhenkommentar noch 6 px Luft.

---

### 2026-08-01 — Das Slot-Budget kommt aus unserer DB, die Kapazität aus 7TV — `emote_count` bleibt liegen

**Betrifft:** `src/EmotePurge.Core/SevenTv/SevenTvModels.cs` · `src/EmotePurge.Core/Entities/Channel.cs` · `src/EmotePurge.Core/Services/IEmoteSetStatusService.cs` (neu) · `src/EmotePurge.Infrastructure/SevenTv/{SevenTvApiDtos,SevenTvApiClient}.cs` · `src/EmotePurge.Infrastructure/Services/{SevenTvSyncService,ChannelService,EmoteSetStatusService}.cs` · `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs` · `src/EmotePurge.Infrastructure/Migrations/20260801183949_AddChannelSetCapacityAndTrackingResumed.cs` · `web/src/app/core/emotes/{emote-set-status.model,emote-admin.service}.ts` · `web/src/app/shared/emotes/{slot-budget,slot-budget-bar}.ts` (neu) · `web/src/app/features/usage-stats/usage-stats-page.{ts,html}` · `web/public/i18n/{de,en}.json`

**Aufräumen ist kein Selbstzweck — es passiert, weil neue Emotes rein sollen.** Bis hierher war genau dieses Ziel unsichtbar: die Oberfläche zeigte eine Liste, aber nirgends, wie voll das Set ist oder wie viel eine geplante Purge freiräumt. Der erste der drei Top-Kandidaten aus [Feature-Ideen-2026-08-01.md](Feature-Ideen-2026-08-01.md) (A1) schließt das, und zwar zum Preis von null zusätzlichen 7TV-Requests: `capacity` liegt bereits im `GET users/twitch/{id}`-Response, den der Sync ohnehin abholt und bislang verwarf.

**`emote_count` wird bewusst *nicht* mitgeschrieben.** Es ist definitionsgemäß `emotes.Length` derselben Antwort, auf die `ReconcileAsync` die DB in derselben Transaktion abgleicht — direkt nach jedem Sync ist es also immer identisch zu unserer eigenen Zahl. Eine Abweichung entsteht ausschließlich *zwischen* zwei Syncs und misst dort „es sind EventAPI-Deltas angekommen", also den Normalzustand eines gesunden Systems. Als Drift-Signal für die geplante Admin-Diagnose wäre es ein Fehlalarm-Generator; als zweite Wahrheitsquelle über die Belegung wäre es schlechter als die eigene, weil sie nach einem Delta sofort stimmt statt bis zum nächsten Resync zu driften. Die belegten Slots zählt deshalb `EmoteSetStatusService` aus den `Emote`-Zeilen — genau die Zeilen, die der Nutzer im Grid unter dem Balken sieht.

**`Capacity` ist nullable und wird nie durch 1000 ersetzt.** 7TV-Abonnenten haben größere Sets; ein hartkodierter Nenner wäre ausgerechnet dort falsch, wo Kopffreiheit zählt. `null` heißt „7TV hat keine Kapazität gemeldet", und der Balken rendert dann gar nicht, statt eine erfundene Bezugsgröße zu zeigen. Auf demselben Weg wird `0` beim Einlesen zu `null` — ein fehlendes Feld und eine echte Null sind im Response nicht unterscheidbar, und beides als „keine Slots" zu lesen ließe jedes Set voll aussehen.

**Geschrieben wird die Kapazität ausschließlich dort, wo auch `ActiveEmoteSetId` geschrieben wird** (`SyncChannelAsync`), nie im EventAPI-Delta-Pfad: der trägt keine Kapazität, und ein gewechseltes Set zeigte sonst bis zum nächsten Resync das Limit des alten. Sie steht dabei außerhalb der Änderungserkennung — ein vergrößertes Set ist kein verändertes Inventar und darf kein `channel.synced` und damit kein Refetch auf jeder offenen Seite auslösen. Ein Test pinnt genau das, ein zweiter, dass der Plausibilitätsguard („7TV meldet 0 Emotes, obwohl welche bekannt sind") auch die Kapazität nicht schreibt: ein 7TV-Teilausfall darf kein falsches Limit hinterlassen.

**`Channel.TrackingResumedAt` statt `CreatedAt` als Tracking-Beginn.** `LeaveAsync` behält die Zeile, `JoinAsync` reaktiviert sie — ein Channel, der zwei Monate verlassen und gestern wieder gejoint wurde, trägt weiter sein ursprüngliches `CreatedAt`. „Wir zählen seit …" auf dieser Basis behauptete Abdeckung über genau die Lücke hinweg, in der nichts gezählt wurde. Die Spalte wird nur bei einem Join gesetzt, der den Channel tatsächlich reaktiviert; ein Join auf einen bereits aktiven Channel ist für die Abdeckung ein No-Op und darf die beanspruchte Historie nicht verkürzen. `trackedSince = TrackingResumedAt ?? CreatedAt`.

**Der Vertrag hängt an `GET /api/channels/{c}/emotes/active-set`, nicht an einem neuen Endpunkt.** Die Route trug bisher nur `{ activeEmoteSetId }` und liefert jetzt zusätzlich `capacity`, `occupiedSlots` und `trackedSince`. Die Gruppe steht bereits unter `UsageStatsAccessAuthorizationFilter` und lässt damit auch 7TV-Editoren durch — exakt der Zuschnitt, den das Feature braucht, ohne eine Zeile Autorisierungsarbeit. Beide Seiten, die den Balken je brauchen, rufen diese Route ohnehin schon; ein eigener Endpunkt wäre ein dritter Request pro Seitenaufruf für Daten, die genau derselbe Personenkreis sehen darf.

**Der Balken ist eine eigene Shared-Komponente, kein Zusatz am `mass-delete-panel`.** Das Panel läuft auf zwei Seiten, und auf der Vote-Session-Detailseite ist die Auswahl ein Wahlzettel-Ausschnitt — deren Anzahl sagt nichts über die Belegung des Sets. Zwei weitere Pflicht-Inputs hätten beide Hosts zu Änderungen gezwungen, eine davon für eine Zahl ohne Bedeutung. `slot-budget.ts` ist die reine, getestete Funktion (klemmt beidseitig: eine Belegung über der Kapazität ist nach einem ausgelaufenen 7TV-Abo ein realer Zustand), `slot-budget-bar.ts` nur die Darstellung. Die Farbstufe richtet sich nach der *aktuellen* Belegung, nicht nach der Projektion — eine große Auswahl darf ein volles Set nicht grün färben, bevor tatsächlich gelöscht wurde.

---

### 2026-08-01 — Die Zielgruppe einer Abstimmung wird angezeigt, nicht nur beim Erstellen abgefragt

**Betrifft:** `src/EmotePurge.Core/Services/IVoteSessionQueryService.cs` · `src/EmotePurge.Infrastructure/Services/VoteSessionQueryService.cs` · `web/src/app/core/voting/vote-audience.ts` (neu) · `web/src/app/shared/voting/vote-audience-badge.ts` (neu) · `web/src/app/features/voting/{vote-session-list-page,vote-session-detail-page}.{ts,html}` · `web/public/i18n/{de,en}.json`

**`AllowedVoterRoles` war eine Schreib-nur-Angabe.** Die Rollen werden beim Erstellen gesetzt, sind danach unveränderlich — und tauchten in der gesamten Oberfläche nur im Erstellen-Formular auf. `VoteSessionSummaryDto` lieferte sie zwar mit, die Liste rendert sie aber nie; `VoteSessionResultsDto` führte sie gar nicht. Ein Mod, der eine laufende oder beendete Abstimmung ansah, konnte nicht mehr feststellen, ob sie an alle, nur an Subs oder nur ans Mod-Team gerichtet war. Genau das entscheidet aber, wie das Ergebnis zu lesen ist: 12 Stimmen aus dem Mod-Team sind etwas anderes als 12 Stimmen aus dem ganzen Chat.

**Die Zielgruppe geht an jeden Betrachter, nicht nur an Manager** — dieselbe Begründung wie bei `HideResultsUntilEnd`: sie beschreibt die *Session*, nicht die Rechte des Betrachters, und wer die Detailseite überhaupt sieht, ist ohnehin durch `VoteAudienceFilter` gekommen. `VoteSessionResultsDto` bekommt das Feld deshalb ungefiltert, an derselben Position wie im Summary-DTO.

**Die Flags-Maske wird im Frontend auf vier Fälle reduziert** (`voteAudience()`, rein und getestet): `everyone` · `subs` · `mods` · `restricted`. `Everyone` gewinnt über jedes andere Flag, weil `VoteEligibilityService` serverseitig genau so entscheidet — das Flag wird zuerst geprüft und lässt durch, bevor irgendeine andere Rolle betrachtet wird. Die Erstellen-Pfade erzeugen nur `Everyone`, `Subs` oder `Mods | Broadcaster`; die API nimmt aber die rohe Maske entgegen, und alles andere (Mischmasken, `VIPs`, leere Maske) fällt bewusst auf `restricted` statt als einer der drei Fälle falsch beschriftet zu werden.

**Tone-Wahl:** offene Abstimmung → `slate` (der unauffällige Normalfall), eingeschränkte → `blue`. Das ist keine Umdeutung der Rollen-Tabelle in [UI-Designsprache.md](UI-Designsprache.md) §4.3, sondern dieselbe Lesart, die der „Verdeckt"-Badge in derselben Zeile schon verwendet: `blue` = bemerkenswerte Eigenschaft dieser Session. Der Badge steht **unbedingt**, auch bei beendeten Sessions — anders als „Verdeckt", das nach dem Ende nichts mehr verbirgt und deshalb verschwindet.

---

### 2026-08-01 — `liveEvents()`/`liveReload()` statt handgebauter SSE-Pipelines auf jeder Seite

**Betrifft:** `web/src/app/core/live/live-reload.ts` (neu) · `web/src/app/core/live/live-reload.spec.ts` (neu) · `web/src/app/features/{usage-stats/usage-stats-page,voting/vote-session-detail-page,admin/admin-channels-page,admin/admin-monitoring-page}.ts` · `web/.claude/CLAUDE.md`

**Vier Seiten bauten dieselbe Kette von Hand**, zwei davon inklusive eines identischen Kniffs mit fast wortgleichem Kommentar: `toObservable(url) → switchMap(stream) → filter → tap → debounceTime → takeUntilDestroyed`. Der Kniff war ein mutables Feld `syncSeenSinceReload`, das über das Debounce-Fenster hinweg merkte, ob ein `channel.synced` unter den zusammengefassten Events war — nötig, weil `LiveUpdateService.stream()` **cold** ist und ein zweites Abonnement eine zweite `EventSource` geöffnet hätte. Dass diese Begründung an zwei Stellen fast identisch dastand, war der eigentliche Befund: nicht die Zeilen waren dupliziert, sondern die Erklärung.

**`liveReload()` gibt deshalb den *Satz der gesehenen Event-Typen* zurück, nicht bloß ein Signal „jetzt neu laden".** Damit ist der Flag-Trick ersatzlos verschwunden — der Aufrufer fragt `seen.has(LIVE_EVENT_TYPES.channelSynced)` und erfährt genau das, wofür vorher ein Feld quer durch die Klasse lief. Der Satz wird pro Emission frisch kopiert und geleert; ein Test pinnt genau das, weil ein durchgereichter Satz sonst nach dem ersten Sync für immer `channelSynced` meldet und die Seiten bei *jedem* Flush die aktive Set-ID nachladen würden.

**Zwei Funktionen, nicht eine — der erste Anlauf mit nur `liveReload()` war ein Fehlgriff.** `admin-channels-page` braucht das **einzelne** Event, weil sie aus dessen `channel` entscheidet, welche Zeile ihren Hinweis von „eingereiht" auf „abgeschlossen" hochstuft; ein zusammengefasster Burst wirft genau diese Information weg. Der Versuch, das trotzdem durch `liveReload` zu pressen, endete bei einem `filter`-Prädikat mit Seiteneffekten und `return false` — schlechter lesbar als der Zustand vorher. Deshalb jetzt `liveEvents(url, accept)` für Einzelevents und `liveReload(url, {accept, debounceMs})` darauf aufgebaut. Wo die Abstraktion nicht passt, ist die richtige Antwort eine zweite Form, nicht ein verbogener Aufruf.

**Beide nehmen `string | Signal<string>`** — die channel-gebundenen Seiten geben ein Signal herein und bekommen den `switchMap` samt Abbau der alten Verbindung geschenkt, die Admin-Seiten geben die konstante URL herein und zahlen keine `toObservable`-Indirektion. Beide holen sich ihren eigenen `DestroyRef` und wenden `takeUntilDestroyed()` selbst an; Aufrufer müssen daher aus einem Injection-Kontext heraus aufrufen, was durch Feld-Initialisierer oder Konstruktor ohnehin gegeben ist.

**Getestet mit dem `FakeEventSource` aus `live-update.service.spec.ts`** über `EVENT_SOURCE_FACTORY` — inklusive der Zusicherung, dass trotz Typ-Satz nur **eine** `EventSource` geöffnet wird, denn genau das war der Grund für den ursprünglichen Kniff.

---

### 2026-08-01 — Eine Root-`README.md` für Menschen; `CLAUDE.md` bleibt die Regelliste

**Betrifft:** `README.md` (neu) · `web/README.md` · `docs/Review-2026-07-29-Umsetzung.md` (S4-18)

**Das Repo hatte kein Dokument, das einen fremden Menschen zum Laufen bringt.** Einstieg war `CLAUDE.md` — dicht, präzise, aber ausdrücklich „Schneller Einstieg für Claude Code" und ohne README auf GitHub gar nicht auffindbar: dort sah man nur den Dateibaum. Das ist Befund **S4-18** aus Welle E und zugleich der Kern des Vorwurfs „kann das jemand anderes maintainen?" aus der Sichtung vom 2026-08-01. Die Antwort lautete: ja, aber erst ab Mittwoch.

**Die Aufteilung ist bewusst: `README.md` erklärt den Weg von `git clone` bis „läuft", `CLAUDE.md` bleibt die Regelliste.** Keine Verdopplung — die README verweist auf die Regeln, statt sie zu wiederholen, und trägt stattdessen das, was vorher nirgends stand: die Twitch-App-Registrierung samt **beider** Redirect-URIs (`:5151` für `dotnet run`, `:8080` für Compose — wer nur eine einträgt, verliert genau eine Betriebsart), die `dotnet tool install --global dotnet-ef`-Zeile statt der Behauptung „ist installiert", `ADMIN_TWITCH_LOGINS` als Pflichtfeld, und der Satz, dass **Migrationen in keiner Umgebung automatisch laufen** — im Code steht kein einziges `MigrateAsync`, was ohne Hinweis in eine leere Datenbank führt.

**Ein Abschnitt „Was beim ersten Beitrag überrascht" nimmt vier bewusste Eigenheiten vorweg**, statt sie den Neuen als Fehler erleben zu lassen: die Drei-Stellen-Zeremonie für eine neue Backend-Fähigkeit (Interface, Implementierung, DI-Zeile — die 22 Interfaces in `Core/Services/` werden nie gemockt, sie tragen die Schichtentrennung), Endpoints nur in `Endpoints/*.cs`, sprachneutrale Fehlercodes mit Pflicht-Eintrag in beiden Locale-Dateien, und dass praktisch kein Betriebsparameter zur Laufzeit konfigurierbar ist. Das Letzte ist keine Entschuldigung, sondern eine Warnung: ein Prod-Problem kostet hier Code-Änderung, Build und Deploy.

**Es gibt bewusst keine Seed-Daten.** Ohne getrackten Channel und echten Chat-Verkehr sind alle Seiten leer, und das ist naturgemäß so: Nutzungszahlen entstehen aus mitgelesenem Chat. Ein Fixture würde eine Datenlage vortäuschen, deren Zustandekommen gerade das ist, was man beim Onboarding verstehen muss. Die README sagt das explizit, damit niemand nach dem fehlenden Seed-Skript sucht.

**`web/README.md` wird ersetzt, nicht gelöscht** — der Eintrag „Alle Projektdokumente liegen in `docs/`" vom selben Tag hält fest, dass `README.md`-Dateien dort bleiben, wo ihr Ort selbst die Bedeutung trägt. Der bisherige Inhalt war allerdings die unveränderte Angular-CLI-Vorlage und **aktiv falsch**: Zeile 55 behauptete, das Projekt habe kein E2E-Framework, während Playwright samt sechs Specs konfiguriert ist. Sie enthält jetzt nur noch die `web/`-eigenen Kommandos und verweist fürs Setup nach oben.

---

### 2026-08-01 — Format und Lint sind ein CI-Gate, kein Pre-Commit-Hook

**Betrifft:** `.github/workflows/publish.yml` · `CLAUDE.md` (Regel 18/19) · `docs/Review-2026-07-29-Umsetzung.md` (S4-16)

**Werkzeuge, die niemand ausführt, sind Dekoration.** Prettier lag über Monate ungenutzt im Repo — genau deshalb wandern `format:check`, `lint` und `dotnet format --verify-no-changes` in die CI, und zwar in die **bestehenden** Jobs `test` und `test-web`. Ein neuer Workflow wäre unnötig: beide laufen bereits bei `pull_request` und gaten `publish` über `needs`.

**Die Prüfschritte stehen vor den Tests, nicht dahinter.** Sie dauern Sekunden, während `test` Testcontainers hochzieht und `test-web` Chromium herunterlädt. Ein Formatierungsfehler soll nach einer halben Minute auffallen, nicht nach dem vollständigen E2E-Lauf.

**Kein husky/lint-staged.** Das hätte zwei Dependencies, einen `prepare`-Hook und Reibung bei *jedem* Commit gekostet — für einen Bestand, der ohne jede Erzwingung bereits zu 87 % konform war und dessen Änderungen ohnehin durch dieselbe CI gehen. Der Hebel steht in keinem Verhältnis zum Preis; ein Hook lässt sich jederzeit nachrüsten, falls die CI-Rückmeldung sich als zu spät erweist.

**Voraussetzung war, dass das Gate lokal dasselbe Ergebnis liefert wie in CI** — sonst wird es zum Ritual, das man mit `--no-verify` umgeht. Deshalb sind BOMs und Working-Tree-Zeilenenden vorher bereinigt worden (eigener Eintrag); `dotnet format --verify-no-changes` und `npm run format:check` laufen seitdem auf beiden Seiten mit Exit 0.

**Damit ist der Format-/Lint-Teil von Befund S4-16 erledigt.** Offen bleiben dort Dependency-Scan, `npm audit`, NuGet-Cache und Dependabot — bewusst nicht mitgenommen, weil sie eine andere Frage beantworten (Lieferkette statt Stil) und eigene Entscheidungen brauchen.

---

### 2026-08-01 — ESLint mit schmalem Regelsatz erzwingt die Member-Reihenfolge im Frontend

**Betrifft:** `web/eslint.config.mjs` · `web/package.json` · `web/.claude/CLAUDE.md` · `web/src/app/features/{admin/admin-channels-page,channel-workspace/channel-workspace-layout,usage-stats/usage-stats-page,voting/vote-session-detail-page}.ts` · `docs/Review-2026-08-01-Struktur-und-Wartbarkeit.md`

**Der Vorwurf „der Code ist nicht aufgeräumt" hat sich in der Messung halbiert, aber nicht aufgelöst.** Eine faktische Reihenfolge existierte längst — 8 von 14 geprüften Angular-Dateien folgen ihr vollständig —, sie war nur nirgends aufgeschrieben und nirgends geprüft. Die Abweichungen waren entsprechend konkret statt chaotisch: 26 Verstöße in vier Dateien, davon zwei besonders sprechende, weil `syncSeenSinceReload` in `usage-stats-page.ts` und `vote-session-detail-page.ts` **mit wortgleichem Kommentar** nach dem Konstruktor stand. Das war kopiert, nicht entschieden.

**ESLint kann nur die Hälfte davon sehen, und das ist beim Regelentwurf der springende Punkt.** `@typescript-eslint/member-ordering` bekommt `field → constructor → public-method → protected-method → private-method`, wobei `field` bewusst **ein** Topf ist: `input()`, `inject()`, `signal()` und `computed()` sind für den Parser ununterscheidbar Property-Initializer. Erzwingbar ist damit genau das, was tatsächlich gedriftet ist — State nach dem Konstruktor und private Helper zwischen öffentlichen Methoden. Die Feinordnung *innerhalb* des Feldblocks (Inputs vor `inject()` vor State) steht als Konvention in `web/.claude/CLAUDE.md` und bleibt Review-Sache. Diese Trennung ist ausdrücklich dokumentiert, damit niemand später annimmt, ein grüner Lint-Lauf beweise die ganze Reihenfolge.

**`@angular-eslint/inject-at-top` ist geprüft und abgelehnt worden.** Die Regel verlangt `inject()` vor jedem anderen Member; der Bestand stellt `input()`/`output()` davor. Sie zu aktivieren hätte die Mehrheit der bestehenden Komponenten zu Verletzungen erklärt und eine gewachsene, sinnvolle Ordnung umgeworfen, um einer Werkzeugmeinung zu genügen. Dieselbe Linie wie bei `.prettierrc`: Die Config beschreibt den Bestand, sie schreibt ihn nicht um.

**Kein `recommended`-Satz.** Aktiviert sind neben `member-ordering` nur sechs Regeln, und jede einzelne bildet entweder eine bereits in `web/.claude/CLAUDE.md` ausformulierte Konvention ab (`prefer-inject`, `prefer-host-metadata-property`, `prefer-standalone`, `template/prefer-control-flow`, `template/prefer-ngsrc`) oder fängt eine echte Fehlerklasse (`computed-must-return`, `sort-lifecycle-methods`). **Alle sechs meldeten beim ersten Lauf null Verstöße** — der beste verfügbare Beleg dafür, dass die Konventionen nicht nur behauptet, sondern gelebt werden. Ein `recommended`-Satz hätte dagegen einen zweiten, unvermessenen Sweep über 94 Dateien ausgelöst. Der Maßstab ist: Ein roter Lint-Lauf muss „du hast eine dokumentierte Regel gebrochen" heißen, nie „der Linter hat eine Meinung, der niemand zugestimmt hat".

**`@angular-eslint/no-uncalled-signals` wäre wertvoll gewesen und ist trotzdem draußen.** Ihre Metadaten melden `requiresTypeChecking: false`, tatsächlich ruft sie `getParserServices()` und bricht ohne typisierten Parser ab — aufgefallen erst beim Ausprobieren, nicht beim Lesen der Doku. Sie zu aktivieren hieße, bei jedem Lint-Lauf ein vollständiges TypeScript-Programm zu bauen, für eine Verwechslung, die `ng build` und die Testsuite ohnehin sichtbar machen. Der Lauf bleibt damit syntaxbasiert und dauert rund 4 Sekunden; als Nebeneffekt erfasst er auch Dateien außerhalb jedes `tsconfig`, etwa `playwright.config.ts`.

**Fürs Backend gibt es bewusst kein Gegenstück.** Die C#-Reihenfolge ist als Konvention dokumentiert und ihre sechs Ausreißer sind von Hand begradigt; StyleCop hätte ~200 Regeln mitgebracht, von denen ~190 zu unterdrücken gewesen wären, um sechs Fundstellen zu erwischen.

---

### 2026-08-01 — Prettier für `web/`, `dotnet format` fürs Backend, kein CSharpier

**Betrifft:** `web/package.json` · `web/.prettierignore` · `web/.editorconfig` · `.editorconfig` · alle 22 Dateien mit UTF-8-BOM (`src/EmotePurge.*/**.csproj`, `src/EmotePurge.Infrastructure/Migrations/**`) · `docs/Review-2026-08-01-Struktur-und-Wartbarkeit.md`

**Anlass war eine externe Code-Sichtung, die fehlendes Formatter-Tooling monierte — der Vorwurf stimmt, seine vermutete Folge nicht.** Prettier lag seit jeher als devDependency in `web/package.json` und `.prettierrc` existierte, aber es gab kein `format`-Script, keine `.prettierignore` und keinen einzigen Format-Schritt in `publish.yml`. Werkzeuglos war der Bestand trotzdem konsistent: Der Trockenlauf fand **21 von ~130 Dateien** abweichend, `dotnet format --verify-no-changes` **0 handgeschriebene `.cs`** (die 9 Treffer waren generierte EF-Migrations). Dazu 146/146 file-scoped Namespaces, `var` mit genau einer Ausnahme, 97 private Felder ohne Konventionsverstoß, ein einziges `this.` im ganzen Backend — und das war ein Wort in einem Kommentar. Die Lücke war also eine Werkzeug-, keine Codelücke, und der gefürchtete „Sweep zerschießt `git blame`" entsprechend klein. Die vollständige Messung steht in [`Review-2026-08-01-Struktur-und-Wartbarkeit.md`](Review-2026-08-01-Struktur-und-Wartbarkeit.md).

**`.prettierrc` bleibt unverändert, weil sie den Bestand bereits trifft.** `printWidth: 100` passt: nur 1,63 % der TS-/HTML-Zeilen sind länger, und diese Überlängen sind fast durchweg Tailwind-Klassenlisten, keine Logik. `singleQuote: true` deckt sich mit 506 zu 1 bei den Import-Statements, Prettiers Default für Trailing Commas mit 400 zu 4 im Bestand. Eine Config, die den Bestand *umschreibt* statt ihn zu beschreiben, hätte den Sweep ohne Gegenwert vervielfacht.

**Der Prettier-Scope geht jetzt über ganz `web/`, nicht nur `web/src`** — sonst blieben `e2e/` (8 Dateien), `playwright.config.ts`, `angular.json` und die `tsconfig*.json` ungeprüft, also gerade die Dateien, die selten angefasst werden und deshalb am ehesten driften. **Markdown ist bewusst ausgenommen** (`*.md` in `.prettierignore`): Die Projektdoku liegt in `docs/` und damit außerhalb dieses Scopes, und nur `web/**/*.md` zu formatieren hätte die Dokumentation in zwei Formatierungswelten geteilt — die eine per CI erzwungen, die andere handgepflegt.

**Kein CSharpier.** `dotnet format` ist im SDK enthalten, braucht kein `.config/dotnet-tools.json` und keine Versionspflege, und der handgeschriebene Bestand ist bereits zu 100 % konform. Ein zweiter Formatter mit eigener Meinung über Zeilenumbrüche hätte einen repoweiten C#-Sweep *erzeugt*, den es ohne ihn nicht gibt — Kosten ohne erkennbaren Nutzen. Aus demselben Grund gibt es keine `Directory.Build.props` und keine Analyzer-Pakete; die Entscheidung gegen StyleCop für die Member-Reihenfolge steht in ihrem eigenen Eintrag.

**Die 22 UTF-8-BOMs sind weg, und zwar als Voraussetzung, nicht als Kosmetik.** `.editorconfig` fordert `charset = utf-8` (ohne BOM), 3 `.csproj` und 19 Dateien unter `Migrations/` trugen trotzdem eines — Visual Studio und das EF-Tooling schreiben so. Solange sie da waren, hätte ein `dotnet format --verify-no-changes`-Gate dauerhaft rot gestanden. Der Eingriff war byte-genau: 22 Dateien, je genau eine geänderte Zeile, nur die drei BOM-Bytes. `dotnet ef migrations add` wird künftig wieder BOM-behaftete Dateien erzeugen — das ist der bekannte Preis und der Grund, warum das Gate die Meldung sichtbar macht, statt sie zu unterdrücken.

**Zusätzlich sind die Zeilenenden im Working Tree einmalig auf LF normalisiert worden, obwohl der Index längst LF hielt.** `.gitattributes` (`* text=auto eol=lf`) und `core.autocrlf=input` sorgen dafür, dass beim *Commit* normalisiert wird, nicht beim Checkout — vom EF-Tooling frisch geschriebene Dateien bleiben deshalb lokal CRLF, und `dotnet format` meldete für 17 Dateien `ENDOFLINE`, die im CI-Checkout gar nicht auftreten. Nachgewiesen per `git ls-files --eol`: 0 Dateien CRLF im Index, 17 nur im Working Tree. Ein Gate, dessen Ergebnis lokal und in CI auseinanderfällt, ist wertlos — deshalb die Angleichung. Am Commit-Inhalt ändert sie nichts.

**`web/.editorconfig` hat sein `root = true` verloren.** Es kappte die Vererbung aus der Root-`.editorconfig`, sodass ausgerechnet `end_of_line = lf` unterhalb von `web/` nicht galt — also dort, wo die meisten Dateien liegen. Die Datei ergänzt jetzt nur noch die `web/`-spezifischen Regeln (2 Spaces, `quote_type = single`), statt einen zweiten, unvollständigen Regelsatz aufzumachen.

---

### 2026-08-01 — Rücknavigation ist ein hierarchischer Up-Link, kein Verlaufs-Zurück und kein Breadcrumb

**Betrifft:** `web/src/app/shared/ui/back-link.ts` · `web/src/app/shared/ui/back-link.spec.ts` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `web/src/app/features/admin/admin-layout.ts` · `web/src/app/features/my-votings/my-votings-page.ts` · `web/src/app/features/voting/vote-session-detail-page.{html,ts}` · `web/public/i18n/{de,en}.json` · `web/e2e/back-navigation.e2e.spec.ts` · `web/e2e/support/mocks.ts` · `docs/UI-Designsprache.md` (§8.6)

**Aus einer Vote-Session führte kein Weg zurück auf die Vote-Session-Liste desselben Channels.** Der einzige Rückweg auf der Detailseite war der `← Übersicht`-Link des Channel-Layouts, der zwei Ebenen hoch auf `/` sprang. Der Admin-Bereich hatte überhaupt keinen Ausweg (die Tab-Leiste navigiert nur innerhalb `/admin`), und My-Votings hatte einen — aber nur im Leerzustand, also genau dann, wenn es nichts zu tun gab. Ein Back-Link-Primitive gab es nicht, `history.back()`/`Location` kam nirgends vor, und `UI-Designsprache.md` §8 definierte für Rücknavigation kein Muster.

**Entschieden wurde gegen ein Verlaufs-Zurück, weil es hier nachweislich falsch zielt.** Die Detailseite wird regelmäßig per Deep-Link betreten und überspringt dabei die Liste: aus den Usage-Stats direkt nach dem Anlegen einer Session, aus My-Votings und aus dem Admin-Bereich. Browser-Back landet dort also gerade nicht auf der Liste. Gebraucht wird Materials „Up"-Semantik — der Elternknoten der *Informations*-Hierarchie, unabhängig davon, wie man hergekommen ist. Zielgenau konstruierbar ist der überall, weil `paramsInheritanceStrategy: 'always'` jeder Kindroute `channelName` und `sessionId` mitgibt. Der E2E-Test betritt die Seite deshalb bewusst per Deep-Link und prüft anschließend, dass Browser-Back wieder auf die Session führt: der Up-Link navigiert *vorwärts* zum Elternknoten, er poppt den Verlauf nicht.

**Und gegen ein Breadcrumb, weil es die Tab-Leiste verdoppelt hätte.** NN/g empfiehlt Breadcrumbs ab drei Hierarchie-Ebenen, aber die Ebenen 2/3 (Channel · Nutzung/Votings) zeigt die Tab-Leiste aus §8.1 bereits dauerhaft an, und auf Ebene 4 liegt mit `vote-sessions/:sessionId` genau **eine** Seite. Ein Breadcrumb wäre außerdem als vierte Leiste in den Sticky-Höhen-Vertrag aus §8.5 (`h-14`/`h-10`/`top-24`) hineingewachsen. Der Up-Link scrollt stattdessen mit dem Inhalt weg und ist keine Sticky-Ebene.

**Die Primitive liegt in `shared/ui/back-link.ts` und nimmt `link` + bereits übersetztes `label`** — dieselbe Aufteilung wie `EmptyState`/`StatusBadge`, deren Aufrufer die Texte auch fertig übersetzt hereinreichen. `link` akzeptiert `string | unknown[]`, damit aus Route-Parametern gebaute Ziele (`['/channels', channelName(), 'vote-sessions']`) genauso gehen wie feste Pfade. Der bestehende handgebaute Anker im Channel-Layout ist darauf umgestellt, die Optik (lila umrandete Pille) unverändert übernommen. Als Label gilt der **Eigenname des Ziels**: die Detailseite beschriftet ihren Up-Link mit `channelWorkspace.tabs.voting`, also demselben Key wie der Tab, auf dem sie landet — ein eigener `nav.voteSessions`-Key wäre ein Duplikat gewesen, das beim nächsten Umbenennen auseinanderläuft.

**Der zugängliche Name ist breiter als der sichtbare Text, und das ist die einzige Übersetzung *in* der Primitive.** Sichtbar steht dort „← Übersicht"; der Pfeil ist `aria-hidden`, ein Screenreader hörte sonst nur „Link, Übersicht" und damit ein Ziel ohne Funktion. `aria-label` setzt das auf `nav.backTo` = „Zurück zu {{target}}". Diese Formulierung ist Sache der Primitive und nicht der vier Aufrufer, deshalb steht die eine `TranslocoPipe`-Auflösung ausnahmsweise innen. WCAG 2.5.3 („Label in Name") hält, weil der sichtbare Text im zugänglichen Namen enthalten ist.

**Neu ist der `nav.*`-Namespace, und zwei Duplikate sind dabei weggefallen.** `channelWorkspace.backToOverview` und `myVotings.goToOverview` sagten beide „Übersicht" bzw. „Zur Übersicht" und sind durch `nav.overview` ersetzt; `nav.backTo` ist neu. Der CTA im My-Votings-Leerzustand ist ersatzlos entfallen — der Up-Link steht jetzt permanent über der Überschrift und damit auch im gefüllten Zustand, wo der Rückweg vorher fehlte.

**Getestet auf zwei Ebenen.** `back-link.spec.ts` (Vitest) prüft die Primitive isoliert — echtes `<a href>` statt Button, Auflösung des Array-Links, dekorativer Pfeil, `aria-label`-Verbreiterung. Das ist eine bewusste Ausnahme von CLAUDE.md Regel 12, die isolierte Komponententests ausklammert: hier ist der Prüfgegenstand kein Layout, sondern Semantik, die man im Browser nicht sieht. `back-navigation.e2e.spec.ts` (Playwright) deckt die drei geschlossenen Lücken als Flow ab. Dafür kamen `mockVoteSessionList`/`mockVoteSessionResults` in `e2e/support/mocks.ts` dazu; die Liste wird über ein `url.pathname`-Prädikat statt über ein Glob gematcht, weil Playwrights Glob das `?` von `?page=…` als Wildcard liest und ein abschließendes `**` zusätzlich die `/{sessionId}/results`-Subroute verschluckt hätte.

---

### 2026-08-01 — Selektionszustände malen inset: `inset-ring-2` statt `ring-2` auf den Emote-Karten

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.html` · `web/src/app/features/voting/vote-session-detail-page.html` · `web/src/styles.css` · `docs/UI-Designsprache.md` (§8.5)

**Selektierte Emote-Karten ließen links und rechts von den Sticky-Leisten je ~2 px Farbe durchschimmern.** Ursache ist eine Kette, die für sich genommen jeweils richtig ist: Scroll-Container und Sticky-Leisten sind beide exakt die Content-Box von `<main class="mx-auto max-w-5xl px-4 py-8">`; der `*cdkVirtualFor`-Zeilen-Wrapper hat nur `py-2`, also liegen die Karten der ersten und letzten Grid-Spalte bündig an deren Kante; und `ring-2` ist ein **outset** box-shadow, malt also 2 px *außerhalb* der Border-Box. `.app-sticky-bar` hat weder `max-width` noch horizontales Padding — ihr Hintergrundkasten endet an derselben Kante. An genau diesen 2 px lag damit gar kein Hintergrund. Abgeschnitten hätte das früher CDKs Paint-Containment, das aber bewusst auf `contain: layout style` reduziert worden war, weil `contain: content` Ring *und* Fokus-Outline der Randspalten kappte.

**Gewählt wurde `inset-ring-2 inset-ring-purple-500`, also die Ursache statt der Wirkung.** Der Ring wird damit innerhalb der Border-Box gezeichnet (`box-shadow: inset 0 0 0 2px`) und ist an allen vier Kanten gleich dick, bei jeder Spaltenzahl und auf beiden Seiten identisch. Die Alternativen wurden verworfen: die Sticky-Leiste per Pseudo-Element über die Content-Box hinaus zu ziehen hätte den Randeffekt nur **verdeckt** und jede künftige Leiste mitziehen lassen; horizontales Padding am Zeilen-Wrapper mit kompensierendem negativem Margin am Viewport verschiebt dasselbe Problem in eine Rechnung, die bei jeder Layout-Änderung neu stimmen muss; und eine `outline`-Variante mit `outline-offset: -2px` kollidiert direkt mit dem globalen `:focus-visible`-Ring — die Karten sind `tabindex="0"`, eine fokussierte selektierte Karte hätte ihre Selektionsanzeige an die Fokus-Outline verloren.

**Wichtig für Tailwind v4: `ring-inset` gibt es dort nicht mehr**, die Utility heißt `inset-ring-*`. Verifiziert wurde nicht nur der Build, sondern das erzeugte CSS: `.inset-ring-2{--tw-inset-ring-shadow: inset 0 0 0 2px …}`.

**Die Fokus-Outline bleibt bewusst outset und damit der einzige verbliebene Grund für die Containment-Lockerung.** Sie ist transient (nur bei Tastaturfokus) und darf an den Randspalten nicht abgeschnitten werden — der Kommentar am CDK-Block in `styles.css` ist entsprechend nachgezogen, damit die Regel bei der nächsten Aufräumrunde nicht als Altlast gelesen wird. Das transluzente Durchschimmern *innerhalb* der Leisten bleibt unverändert gewollt (§8.5).

---

### 2026-08-01 — Alle Projektdokumente liegen in `docs/`, im Root bleibt nur `CLAUDE.md`

**Betrifft:** `Architectur.md` → `docs/Architectur.md` · `Review-2026-07-29.md` → `docs/Review-2026-07-29.md` · `Review-2026-07-29-Umsetzung.md` → `docs/Review-2026-07-29-Umsetzung.md` · `CLAUDE.md` · `docs/DECISIONS.md` · `src/EmotePurge.Core/Twitch/ITwitchHelixClient.cs` · `src/EmotePurge.Worker/{TwitchChatManager,Worker}.cs` · `web/src/app/core/seven-tv/seven-tv-token.service.ts`

**Das Repo-Root hatte vier Markdown-Dateien, von denen nur eine dort hingehört.** `CLAUDE.md` muss im Root liegen, weil Claude Code sie dort erwartet; `Architectur.md` und die beiden Review-Dokumente vom 2026-07-29 lagen aus reiner Entstehungsgeschichte daneben, während `docs/` bereits sechs andere Projektdokumente hielt. Damit war aus dem Dateibaum nicht ablesbar, wo Projektdoku steht — die Antwort war „mal hier, mal dort".

**Ab jetzt gilt: Projektdokumentation liegt in `docs/`, ohne Ausnahme.** Im Root verbleibt `CLAUDE.md` als Einstiegspunkt und verlinkt von dort die Spezifikation und dieses Log. `README.md`-Dateien bleiben, wo sie liegen (`web/`, `web/branding/`), weil ihr Ort dort selbst die Bedeutung trägt; ebenso `web/.claude/CLAUDE.md`.

Verschoben wurde per `git mv`, damit die Historie durchgängig bleibt. Angepasst wurden alle relativen Markdown-Links (in `CLAUDE.md` nach `docs/…`, innerhalb von `docs/` auf Geschwister-Pfade ohne `docs/`-Präfix bzw. ohne `../`) sowie vier Pfad-Erwähnungen in Code-Kommentaren. Die `**Betrifft:**`-Zeilen älterer Einträge dieses Logs nennen weiterhin die alten Pfade — sie sind historischer Text und werden nicht rückwirkend umgeschrieben; der Dateiname bleibt greppbar.

---

### 2026-08-01 — Mass-Delete taktet sich selbst: 7TVs Rate-Limit wird zur Laufzeit gelernt statt geraten

**Betrifft:** `web/src/app/core/seven-tv/seven-tv-delete.service.ts` · `web/src/app/core/seven-tv/seven-tv-delete.service.spec.ts` · `web/src/app/shared/seven-tv/{mass-delete-panel,delete-progress-panel}.ts` · `web/public/i18n/{de,en}.json` · `Architectur.md`

**Die 275-ms-Verzögerung der Mass-Delete-Engine war eine Spec-Annahme ohne Beleg — und die Recherche hat ergeben, dass sich der richtige Wert grundsätzlich nicht statisch bestimmen lässt.** 7TVs Rate-Limiter ist im Quellcode vollständig einsehbar, die Zahlen sind es nicht: `RateLimitResource::EmoteSetChange` (String-Key `emote_set_change`) guardet in [`apps/api/src/http/v3/gql/mutations/emote_sets/mod.rs`](https://github.com/SevenTV/SevenTV/blob/main/apps/api/src/http/v3/gql/mutations/emote_sets/mod.rs) jede Emote-Set-Mutation mit einem Ticket pro Aufruf, aber die Quote selbst (`RateLimits { interval_seconds, requests, overuse_threshold, overuse_punishment }`) ist **pro Rolle in 7TVs MongoDB** hinterlegt und taucht nirgends im Open-Source-Baum auf — kein `config.toml`, keine Seed-Daten. Sie kann pro Nutzer verschieden sein und sich ohne Ankündigung ändern. Die kursierende Drittanbieter-Zahl „100 / 60 s" (merge7tv) ist ein hartcodierter, unbelegter UI-String; die dort danebenstehende Angabe „global 5000 / 24 h" ist nachweislich falsch (gemessen: 5000 / **60 s**), was den Vertrauenswert der ersten Zahl nicht hebt. Damit ist jede neue Konstante nur eine besser begründete Vermutung — die Engine muss die Zahl stattdessen erfragen.

**Sie erfragt sie, indem sie das Limit genau einmal pro Lauf berührt.** Der Startwert bleibt bewusst bei 275 ms: Würden wir vorsorglich auf ~600 ms verlangsamen und 7TVs echte Quote läge höher, wären alle Läufe dauerhaft doppelt so langsam, ohne dass wir je etwas lernen. Bei der ersten Ablehnung liefert 7TV alles Nötige mit; daraus wird die Taktung des restlichen Laufs auf `Fenster / Quote × 1,1` gesetzt (`Fenster` = bisher verstrichene Zeit plus gemeldetes `reset`, `Quote` aus dem Header oder ersatzweise die Zahl der bis dahin durchgekommenen Requests). Der Sicherheitsaufschlag ist nötig, weil 7TVs Limiter ein **Fixed Window** ist ([`limit.lua`](https://github.com/SevenTV/SevenTV/blob/main/apps/api/src/ratelimit/limit.lua)) — exakt am Limit zu takten legt jeden letzten Request eines Fensters auf die Grenze. Weil unsere Verzögerung *zwischen* Requests sitzt, die erreichte Rate aber von Verzögerung **plus** Latenz abhängt, wird die gemessene mittlere Round-Trip-Zeit abgezogen.

**Eine rate-limitierte Mutation ist ab jetzt kein Fehlschlag mehr, sondern eine Wartezeit.** Bisher wurde das Emote als `failed` markiert und die Kette lief mit unverändertem Tempo weiter — bei einem 900er-Set (Ziel-Use-Case HandOfBlood) wären das unter einem 100/60-s-Limit mehrere hundert dauerhaft verbrannte Emotes gewesen, die der Admin von Hand hätte nachfassen müssen. Stattdessen wird nach dem gemeldeten `reset` (+ 500 ms Puffer) **dasselbe** Emote erneut versucht, maximal fünfmal. Nie früher: `limit.lua` zählt abgelehnte Requests mit und ersetzt beim Überschreiten von `overuse_threshold` die Fenster-TTL durch `overuse_punishment` — ein Retry-Sturm handelt sich also eine längere Sperre ein als das Fenster selbst.

**Erkannt wird die Ablehnung im GraphQL-Body, nicht am HTTP-Status.** 7TV beantwortet eine rate-limitierte Mutation mit **HTTP 200**; `async_graphql`s `into_response` fasst den Status nicht an, der Guard-Fehler wird ein gewöhnlicher GraphQL-Error mit `extensions.code = "RATE_LIMIT_EXCEEDED"`, `extensions.status = 429` und den gespiegelten Headern unter `extensions.headers` ([`apps/api/src/http/error.rs`](https://github.com/SevenTV/SevenTV/blob/main/apps/api/src/http/error.rs), Werte als Strings). Der bisherige `error.status === 429`-Zweig war für GQL-Mutationen faktisch tot und griff nur beim globalen Bucket, der auf HTTP-Ebene geprüft wird — dieser Pfad bleibt erhalten, wartet mangels Zahlen aber blind ein volles 60-s-Fenster und darf ausdrücklich **keine** Taktung ableiten (aus „ein Request, dann blockiert" würde sonst eine Quote von 1/min gelernt).

**Proaktives Pacing ist im Browser konstruktionsbedingt unmöglich, deshalb ist die Reaktion der einzige Weg.** Auf dem Erfolgspfad setzt 7TV die `x-ratelimit-emote_set_change-*`-Header zwar auf die Antwort, aber [`apps/api/src/http/mod.rs`](https://github.com/SevenTV/SevenTV/blob/main/apps/api/src/http/mod.rs) exponiert per CORS nur `x-access-token`, `x-request-id` und `x-auth-failure` — `remaining` ist für unser JS also unsichtbar, solange wir noch Budget haben. Sichtbar wird die Information erst im Fehlerfall, weil sie dort im JSON-Body steckt. Die Alternative — die 7TV-Calls über unsere Api zu proxyen, wo CORS nicht gilt — wurde verworfen: sie verlegt das 7TV-Schreib-Token vom `sessionStorage` auf unseren Server und bricht damit das Zero-Knowledge-Prinzip aus [`Architectur.md`](Architectur.md) für einen Gewinn, den der reaktive Weg weitgehend mitliefert.

**Die Wartezeit ist sichtbar, sonst wirkt sie wie ein Absturz.** Eine bis zu 60 s stehende Fortschrittsanzeige ist von einem Hänger nicht zu unterscheiden, deshalb zählt `rateLimitPauseSeconds` im Delete-Panel sichtbar herunter; `cancel()` bricht die Pause mit ab. Jede Ablehnung schreibt zusätzlich `limit`, `used`, `reset`, die Zahl der Requests im Fenster und die neu berechnete Taktung nach `console.info`.

**Erste Messung, 2026-08-01: 230 Emotes am Stück, keine einzige Ablehnung — bei ~119 Requests pro 60-Sekunden-Fenster.** Die Rate ist aus der Messzeile eines Kontrolllaufs belegt (`averageRoundTripMs: 228` bei 275 ms Verzögerung, also ~503 ms Zykluszeit); der 230er-Lauf hielt dieses Tempo gut zwei Minuten durch. Damit ist „100 / 60 s" für ein normales Nutzer-Token nicht nur unbelegt, sondern quantitativ widerlegt — wir lagen dauerhaft rund 19 % darüber, ohne dass 7TV eine einzige Mutation abgelehnt hätte. Die Ursache ist so oder so bei 7TV zu suchen — entweder ist der Bucket produktiv gar nicht befüllt (`limit.lua` kennt diesen Fall: ohne Rollen-Eintrag ist `interval_seconds = 0` und `acquire()` gibt vor jeder Prüfung `Ok(None)` zurück, also **unbegrenzt**), oder die Quote liegt deutlich über unserem Tempo. Nach oben abgegrenzt ist sie damit nicht: bekannt ist nur, dass 7TV mindestens die erreichte Spitzenrate toleriert. Weil ein Lauf ohne Ablehnung bis dahin gar nichts protokollierte, schreibt jetzt **jeder** beendete Lauf eine Messzeile (`requestsSent`, `durationMs`, `peakRequestsPer60s`, `averageRoundTripMs`, `rateLimitHits`). Gemessen wird bewusst die Spitze in einem gleitenden 60-Sekunden-Fenster und nicht der Laufdurchschnitt — nur die ist mit einer `requests / interval_seconds`-Quote vergleichbar, und ein Durchschnitt über einen Lauf mit Wartepausen wäre systematisch zu niedrig. Der Startwert bleibt deshalb bei 275 ms: Es gibt keinen belegten Grund, langsamer zu sein, und der Backoff trägt den Fall, dass 7TV die Quote später scharfschaltet.

---

### 2026-08-01 — Verdeckte Abstimmung pro Session (`HideResultsUntilEnd`): Tallies serverseitig genullt, Sortierung im Verborgen-Modus alphabetisch statt nach Score

**Betrifft:** `src/EmotePurge.Core/Entities/VoteSession.cs` · `src/EmotePurge.Core/Services/{IVoteSessionQueryService,IVoteSessionService}.cs` · `src/EmotePurge.Infrastructure/Services/{VoteSessionQueryService,VoteSessionService}.cs` · `src/EmotePurge.Infrastructure/Migrations/20260801120155_AddVoteSessionHideResultsUntilEnd.cs` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs` · `web/src/app/core/voting/{vote-session.model,vote-session.service}.ts` · `web/src/app/features/voting/{vote-session-list-page,vote-session-detail-page}.{ts,html}` · `web/src/app/features/usage-stats/create-vote-session-dialog.ts` · `web/public/i18n/{de,en}.json` · `tests/EmotePurge.Infrastructure.Tests/Integration/{VoteSessionQueryServiceTests,VoteSessionServiceTests}.cs` · `web/e2e/audit/ui-audit.audit.ts`

**Beim Erstellen einer Abstimmung ist „Ergebnisse erst nach Ende zeigen" ankreuzbar: solange die Session läuft, sehen Nicht-Manager keine Stimmenzahlen.** Anlass ist die Beeinflussung, die eine offene Auszählung erzeugt — wer sieht, dass ein Emote schon bei −12 steht, stimmt anders ab als jemand, der es zuerst bewertet. Das Flag sitzt als `bool HideResultsUntilEnd` auf `VoteSession` (Migration mit `defaultValue: false`, Bestands-Sessions verhalten sich damit unverändert).

**Durchgesetzt wird es im Read-Model, nicht im Client.** `VoteSessionResultDto.KeepVotes/DeleteVotes/Score` sind jetzt `int?` und kommen zusammen als `null` zurück — dieselbe „null heißt vorenthalten, nicht 0"-Konvention wie bei `TotalUseCount` seit dem Subset-Redesign, und wieder ist die Datenpräsenz selbst das Berechtigungssignal (das Frontend leitet `talliesWithheld` daraus ab, kein zusätzlicher Permissions-Call). Der Tally-`GroupBy` wird bei verborgenen Sessions gar nicht erst ausgeführt, analog zur übersprungenen Usage-Abfrage. **`MyVote` bleibt immer befüllt** — die eigene Stimme ist nie Teil des Geheimnisses, sonst könnte niemand seine Wahl prüfen oder ändern.

**Der eigentliche Leak war nicht die Zahl, sondern die Reihenfolge.** Die Ergebnisliste ist nach `Score` aufsteigend sortiert (Delete-Kandidaten zuerst); mit genullten Zahlen hätte die Position jeder Karte das Ranking genauso präzise verraten wie die Zahlen davor. Im Verborgen-Modus sortiert der Server deshalb nach `EmoteName` (Tiebreaker `EmoteId`, damit die Ordnung deterministisch bleibt) — neutral, und für einen Voter, der einen Wahlzettel durcharbeitet, ohnehin die brauchbarere Reihenfolge. Nach dem Session-Ende gilt wieder die Score-Sortierung. Der Reihenfolgen-Freeze der Detail-Seite (`orderedEmoteIds`) friert damit die Namensordnung ein; die Enthüllung passiert dann über auftauchende Zahlen statt über springende Karten.

**`VoterCount` bleibt sichtbar.** Die Wahlbeteiligung sagt nichts darüber aus, wie abgestimmt wurde, und trägt den Dünne-Beteiligung-Hinweis, der bei einer verdeckten Session eher mehr wert ist als bei einer offenen.

**Die Sichtbarkeitsgrenze ist `CanManageChannelAsync`** — Admin/Broadcaster/Live-Mod, exakt die Grenze, die auch Erstellen und Beenden gated. Der bisherige Parameter `includeRawUsage` heißt deshalb jetzt `viewerIsManager` und speist beide Vorenthaltungen; der Endpoint entscheidet weiterhin nur „ist Manager", die Ableitung sitzt im Query-Service, der die Session kennt. Ein `CreatedBy` wurde bewusst **nicht** eingeführt: `VoteSession` hat keins, wer erstellt hat steht nur in der Audit-Zeile, und da der Create-Endpoint manager-gegated ist, ist jeder Ersteller zum Erstellzeitpunkt ohnehin Manager. 7TV-Editoren sind ausdrücklich **keine** Manager und sehen nichts.

**Das Flag ist ab Erstellung fix, es gibt keinen Toggle-Endpoint.** Ein Manager, der mitten in der Session umschalten könnte, könnte ein unliebsames Zwischenergebnis nachträglich verstecken oder genau im passenden Moment aufdecken — das wäre schlechter als beide festen Zustände. `IsActive` ist die einzige „ist vorbei"-Instanz im Code, also ist `EndAsync` automatisch der Moment der Enthüllung; kein Zeitfeld, kein Scheduler. Für die Nachvollziehbarkeit reist `hideResults` in den `details` der Create-Audit-Zeile mit (der Audit-Renderer ignoriert unbekannte Keys, die Anzeige bleibt unverändert).

**Restrisiko, bewusst akzeptiert: `vote.changed` bleibt ein Timing-Seitenkanal.** Jeder eingeloggte Zuhörer des Channel-SSE-Streams erfährt weiterhin, *wann* abgestimmt wird — Aktivitätsprofil, keine Richtung. Das Event trug nie eine Zahl oder eine Voter-Identität, und es zu unterdrücken würde den Live-Refetch der verdeckten Session mitnehmen, ohne den ein Voter seine eigene gerade abgegebene Stimme nicht mehr aktualisiert sähe. Vollständigkeitshalber geprüft und unbedenklich: Session-Listen-, `mine`-, Admin- und Audit-Endpoints liefern keine Tallies, `GetResultsAsync` ist die einzige Quelle.

**UI:** Checkbox in beiden Erstell-Einstiegen (Inline-Formular der Voting-Liste und `CreateVoteSessionDialog` — weiterhin bewusst dupliziert statt in `shared/` gezogen, siehe Subset-Eintrag), blaues „Verdeckt"-Badge auf den Listen-Karten **nur solange aktiv**, und auf der Detail-Seite ein Info-Banner in zwei Ausprägungen: für Voter „Zahlen erst nach Ende", für Manager „du siehst sie, die Voter nicht". Auf der Karte ersetzt „Ergebnis verborgen" die Score-Zeile (die Zeile bleibt belegt, sonst kippt die `ROW_HEIGHT_PX`-Höhenarithmetik), die Zähler in den Vote-Buttons entfallen ersatzlos statt durch einen Platzhalter ersetzt zu werden — ein Strich im Zahlen-Slot liest sich wie ein Wert. Die Buttons selbst funktionieren normal, die eigene Stimme bleibt markiert. `VoteSessionService.create()` nimmt jetzt ein Options-Objekt statt fünf positionaler Argumente; „alles default, aber verdeckt" war als `create(ch, title, roles, undefined, undefined, true)` nicht mehr lesbar.

### 2026-08-01 — `channel.synced` wird änderungsgesteuert: alle Sync-Pfade publizieren bei echter Bestandsänderung, No-op-Syncs bleiben still

**Betrifft:** `src/EmotePurge.Core/SevenTv/SevenTvModels.cs` · `src/EmotePurge.Core/Services/{ISevenTvSyncService,IEmoteService}.cs` · `src/EmotePurge.Infrastructure/Services/{SevenTvSyncService,EmoteService}.cs` · `src/EmotePurge.Worker/{LiveEventPublisher.cs (neu),Worker,SevenTvPeriodicResyncWorker}.cs` · `src/EmotePurge.Worker/SevenTv/SevenTvEventClient.cs` · `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/{SevenTvSyncServiceTests,EmoteServiceTests}.cs` · `Architectur.md` (Abschnitt 3, Rückkanal)

**Der Eintrag vom 2026-07-31 hatte `channel.synced` auf JOIN- und RESYNC-Kommandos beschränkt, weil ein bedingungsloser Publish aus dem minütlichen Resync jede offene Seite N-mal pro Minute hätte refetchen lassen.** Der Preis dafür war, dass die real interessanten Änderungen — ein Emote, das über die 7TV-EventAPI reinkommt, ein während der Worker-Downtime aufgelaufener Set-Wechsel, ein Mass-Delete — auf keiner offenen Seite ankamen; nur wer zufällig selbst geklickt hatte, sah sie. **Die richtige Unterscheidung ist nicht „beaufsichtigt vs. unbeaufsichtigt", sondern „hat sich etwas geändert".** Ein No-op-Resync erzeugt kein Event, jede echte Bestandsänderung schon — egal aus welchem Pfad.

Dafür meldet `SevenTvSyncResult` ein drittes Feld **`HasChanges`** (Emote angelegt/archiviert/ent-archiviert/umbenannt/Bild-URL geändert, oder aktives Set gewechselt). Der Wert wird **explizit** in `ReconcileAsync`/`UpsertEmote` mitgeführt statt aus `ChangeTracker.HasChanges()` abgeleitet: der Change-Tracker meldete auch das einmalige Nachtragen von `Channel.TwitchChannelId` als Änderung, was kein Emote betrifft, das eine UI zeigen könnte. Der Delta-Pfad braucht dagegen **kein** neues Feld — `SevenTvDeltaOutcome.Applied` gibt es schon und wird ohnehin nur nach einem persistierten Write zurückgegeben; die vier anderen Outcomes haben nichts geschrieben. Der Set-Wechsel zählt bewusst mit, auch wenn beide Sets identische Emotes enthalten.

Publiziert wird jetzt aus: `Worker.SyncSevenTvAsync` (`publishCompletion || HasChanges` — **JOIN und Admin-RESYNC bleiben unconditional**, weil die Admin-Seite das Event als Fertig-Rückmeldung nutzt und sonst nach einem folgenlosen Resync ewig „läuft" anzeigen würde; Boot-Recovery publiziert nur bei Änderung), `SevenTvPeriodicResyncWorker`, `SevenTvEventClient` (Delta-Anwendung sowie alle Follow-up- und Reconnect-Gap-Fill-Resyncs, gebündelt in `ResyncChannelAsync`) und neu die Api bei `POST /api/channels/{name}/emotes/sync-deleted`. Der gemeinsame Swallow-and-log-Vertrag der drei Worker-Stellen liegt einmal in `LiveEventPublisher` (interne Extension auf `IRedisPublisher`); die Api dupliziert ihn bewusst lokal wie schon bei `vote.changed` — sie kann `EmotePurge.Worker` nicht referenzieren.

**`sync-deleted` publiziert nur, wenn es selbst etwas archiviert hat.** Seit der Idempotenz-Änderung vom 2026-07-31 zählt `ArchivedCount` auch bereits archivierte Emotes mit (Ziel-Zustand erreicht) — genau der Normalfall, wenn der EventAPI-Live-Sync schneller war als die Buchhaltungs-Rückmeldung, und dann hat *dieser* Aufruf nichts geändert und der Live-Sync sein Event schon geschickt. Deshalb bekommt `SyncDeletedResultDto` ein zusätzliches **`NewlyArchivedCount`**, das dieselbe Menge zählt, die schon über die Audit-Zeile entscheidet. Es ist **nicht** Teil der HTTP-Antwort (`archivedCount`/`notFoundIds` bleiben wortgleich) — das Frontend braucht weiterhin den idempotenten Zähler, nicht den Schreibzähler.

Restrisiko, bewusst akzeptiert: bei N getrackten Channels mit hoher 7TV-Aktivität steigt die Event-Rate mit der realen Änderungsrate statt mit der Zeit — das ist genau die gewünschte Kopplung, aber ein Kanal mit Dauer-Umbau (Emote-Aufräumaktion) kann kurzzeitig viele Refetches auslösen. Der Broker verwirft bei Rückstau ohnehin die ältesten Events pro Verbindung (bounded Queue 64, DropOldest), und Notify-and-Refetch macht verlorene Events harmlos.

### 2026-08-01 — Voting-Redesign: Emote-Subset pro Session (`VoteSessionEmote`), Score = netto Keep−Delete ohne Usage-Anteil, `TotalUseCount` nullable statt genullt, Archiviert-Badge statt stillem Verschwinden

**Betrifft:** `src/EmotePurge.Core/Entities/{VoteSessionEmote (neu),VoteSession}.cs` · `src/EmotePurge.Core/Services/{IVoteSessionService,IVoteSessionQueryService}.cs` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs` · `src/EmotePurge.Infrastructure/Services/{VoteSessionService,VoteSessionQueryService}.cs` · `src/EmotePurge.Infrastructure/Migrations/20260801005055_AddVoteSessionEmote.cs` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs` · `src/EmotePurge.Api/Validation/ApiErrorCodes.cs` · `web/src/app/core/voting/{vote-session.model,vote-session.service}.ts` · `web/src/app/core/i18n/api-error.ts` · `web/src/app/features/voting/{vote-session-detail-page,vote-session-list-page}.{ts,html}` · `web/src/app/features/usage-stats/{usage-stats-page.{ts,html},create-vote-session-dialog.ts (neu)}` · `web/src/app/shared/emotes/emote-usage-filter.ts` · `Architectur.md` (Modul A/C/D)

**Der Vote-Ersteller wählt jetzt ein Subset des Emote-Sets zur Abstimmung** — Ziel-Use-Case HandOfBlood (~900er-Set): die Mods sortieren anhand der Usage-Stats selbst vor und stellen der Community nur die „unklaren" Emotes zur Wahl; Whole-Set-Voting ermüdet und verwässert die Ergebnisse. Datenmodell: `VoteSessionEmote` als reine Join-Tabelle (Composite-PK `(VoteSessionId, EmoteId)`, Cascade von beiden Seiten wie `Vote`). **Keine Rows = dynamisch „alle nicht-archivierten Emotes"** (exakt das Bestandsverhalten — Alt-Sessions brauchen keinen Backfill, ein explizites leeres Array ist dagegen ein Fehler, `emote_ids_empty`). Ein kuratierter Wahlzettel ist ab Erstellung fix und wird all-or-nothing validiert (unbekannte/fremde/archivierte Id → neues `emote_ids_invalid`, statt die Auswahl still zu schrumpfen). Die Membership-Prüfung beim Voten sitzt als zweite Stufe im einzigen Eligibility-Check von `CastVoteAsync`; `RetractVoteAsync` prüft **bewusst weiterhin kein Emote** — das Zurücknehmen eines Votes ist auch auf inzwischen archivierten oder ausgeschlossenen Emotes harmlos.

**Der Score verliert seinen Usage-Anteil: `Score = Keep − Delete`, ganzzahlig, aufsteigend sortiert** (Delete-Kandidaten zuerst, Tiebreaker mehr Gesamtstimmen, dann Name für deterministische Reihenfolge). Begründung: die Mods haben die Usage beim Vorsortieren schon verbraucht (Doppelzählung), und die min-max-normalisierten 0–100-Usage-Punkte dominierten rohe ±N-Votes strukturell. `NormalizedUsageScore` entfällt ersatzlos (wurde nirgends gerendert). **Damit stirbt auch die Begründung im SSE-Eintrag vom 2026-07-31** („jeder Vote ändert *alle* Scores → ein Delta-Push wäre ohnehin ein Voll-Push") — Notify-and-Refetch bleibt trotzdem richtig, denn das Read-Model ist weiterhin viewer-spezifisch (`MyVote`) und rollenabhängig (Usage nur für Manager). Der `usage.flushed`-Refetch der Detail-Seite bleibt ebenfalls: er speist jetzt die Manager-Kontextzahlen statt des Scores.

**`TotalUseCount` ist jetzt `int?` — null heißt „vorenthalten", nicht „0-mal benutzt".** Vorher nullte der Server für Nicht-Manager auf literal `0`, weshalb das Frontend einen separaten `getPermissions`-Call (`canSeeUsage`) brauchte, um „unbenutzt" von „darfst du nicht wissen" zu unterscheiden; jetzt ist die Datenpräsenz selbst das Berechtigungssignal und der Permissions-Call auf der Detail-Seite entfällt ersatzlos. Für archivierte Wahlzettel-Mitglieder ist der Wert auch für Manager null (die Usage-Totals rechnen Archivierte nicht — ein fabriziertes 0 wäre gelogen); als Nebengewinn überspringt der Query-Service die komplette Usage-Abfrage, wenn der Viewer sie ohnehin nicht sähe. Der Min/Max/„nur ungenutzte"-Filter behandelt null als „matcht nie".

**Mid-session archivierte Emotes bleiben bei Subset-Sessions sichtbar** (amber-Badge „Nicht mehr im Set" als Overlay über der Emote-Grafik — die `ROW_HEIGHT_PX`-Kartenhöhen-Arithmetik bleibt unangetastet), ihre Votes bleiben ablesbar, weitere Votes sind gesperrt (Buttons disabled mit sichtbarem Grund). Bei Dynamisch-alle-Sessions verschwinden Archivierte wie bisher — dort ist „nicht mehr relevant" von „nie relevant" ohnehin nicht unterscheidbar. **Neu im Results-DTO: `VoterCount`** (distinct Voter der Session) für den Beteiligungs-Hinweis (`NoticeBanner variant="info"` unter 5 Teilnehmern — dünne Ergebnisse sollen nicht wie ein Community-Urteil aussehen); **neu im Summary-DTO: `EmoteCount`** (null = ganzes Set) für die Listen-Karten, weshalb `EndAsync` die `SessionEmotes`-Collection explizit nachlädt (der End-Response patcht die Liste lokal). **Erstell-UX: zwei Einstiege** — das Inline-Formular der Voting-Liste erzeugt weiterhin „ganzes Set"-Sessions, und die bestehende Mehrfachauswahl im Usage-Stats-Grid bekommt „Zur Abstimmung stellen" (`CreateVoteSessionDialog`, feature-lokal; die drei Formularfelder sind bewusst dupliziert statt in `shared/` extrahiert — zweiter Konsument wäre der erste Anlass). Der Button ist auf `canManage` gegated (die Route lässt auch 7TV-Editoren rein, der Create-Endpoint nicht), die Filter-Toolbar der Detail-Seite erscheint erst ab 13 Emotes (kuratierte Kleinstlisten brauchen keine Filter).

### 2026-07-31 — Live-Updates per Server-Sent Events: `live:events`-Redis-Kanal mit dünnen Events, SSE-Broker in der Api (erster Api-Redis-Subscriber überhaupt), Notify-and-Refetch statt Payload-Push

**Betrifft:** `src/EmotePurge.Core/Messaging/{LiveEvents,ILiveEventStream}.cs` (neu) · `src/EmotePurge.Infrastructure/Redis/RedisLiveEventStream.cs` (neu) · `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs` · `src/EmotePurge.Core/Services/IUsageStatFlushService.cs` · `src/EmotePurge.Infrastructure/Services/UsageStatFlushService.cs` · `src/EmotePurge.Api/Endpoints/{LiveEndpoints (neu),AdminEndpoints,VoteSessionEndpoints}.cs` · `src/EmotePurge.Worker/{UsageFlushWorker,Worker,WorkerHealthPublisher}.cs` · `web/src/app/core/live/*` (neu) · `web/src/app/features/usage-stats/usage-stats-page.ts` · `web/src/app/features/voting/vote-session-detail-page.ts` · `web/src/app/features/admin/{admin-monitoring-page,admin-channels-page}.ts` · `web/e2e/support/mocks.ts`

**Die App bekommt ihren ersten Server→Client-Kanal: Usage-Stats aktualisieren sich nach dem Flush, Vote-Tallies live, Admin-Monitoring ohne Reload-Klick.** Transportwahl **SSE über das native `TypedResults.ServerSentEvents` von .NET 10, nicht SignalR und keine rohen WebSockets**: alle drei Anwendungsfälle sind reine Server→Client-Benachrichtigungen; ein SSE-Stream ist ein normaler `MapGet`, auf dem die bestehende Cookie-Auth (same-origin, EventSource sendet Cookies automatisch) und die bestehenden `IEndpointFilter` unverändert greifen — bei SignalR-Hubs griffe beides nicht (`IHubFilter`-Neubau der gesamten Channel-Autorisierung). Null neue Dependencies auf beiden Seiten; Reconnect macht der Browser nativ. Und weil **jede Api-Replica selbst auf Redis subscribed, braucht es nie ein Backplane** — die mitgedachte Replica-Zukunft (s. Data-Protection-Eintrag) bleibt offen, statt an Sticky Sessions gekettet zu werden.

**Die Events sind bewusst dünn** (`{type, channel, sessionId?}` — nie Daten, nie Voter-Identität): der Client refetcht über die bestehenden REST-Endpoints, an denen der echte Zugriffsschutz unverändert sitzt. Das ist hier keine Stilfrage: das Vote-Results-Read-Model ist viewer-spezifisch (`MyVote`) *und* rollenabhängig (`includeRawUsage` nur für Manager) — ein Payload-Broadcast würde Roh-Nutzungszahlen an Nicht-Manager verteilen; außerdem ist der Score min-max-normalisiert über den ganzen Channel, jeder einzelne Vote ändert *alle* Scores, ein Delta-Push wäre also ohnehin ein Voll-Push. Nebengewinn: verlorene Events sind harmlos (das nächste holt den Ist-Zustand nach), weshalb der Broker pro Verbindung eine bounded Queue (64, DropOldest) fahren darf, deren `TryWrite` den Redis-Handler nie blockiert.

**Ein Kanal (`live:events`), der `type`-String ist die Version, unbekannte Typen werden auf beiden Seiten still ignoriert** — dieselbe Deployment-Reihenfolge-Unabhängigkeit wie beim `RESYNC:`-Präfix; Konstanten und `LiveEvent`-Record leben wie `BotCommands` einmal in `Core/Messaging`. **Der Publish darf den Produzenten nie scheitern lassen:** im `UsageFlushWorker` steht er nach `RecordFlushSuccess` in eigenem try/catch, weil ein Redis-Ausfall sonst einen committeten Flush als Fehlschlag zählen und die Counts beim Requeue doppelt auf `(EmoteId, Date)` buchen würde. Das Vote-Event wird **im Endpoint** publiziert (Success-Arme von POST/DELETE), nicht im Service: `CastVoteAsync` hat durch die Race-Recovery zwei Erfolgs-Returns, mit Retract wären es drei Publish-Stellen im Service gegen zwei im Endpoint — und `IRedisPublisher` im Handler ist per Regel 4 ausdrücklich erlaubt. `channel.synced` kommt nur aus JOIN- und RESYNC-Kommandos, **nicht** aus dem minütlichen periodischen Resync (der würde die Admin-Channel-Liste bei N Channels N-mal pro Minute reloaden lassen, ohne dass je jemand geklickt hätte).

**Der SSE-Endpoint trägt bewusst keine Rate-Limit-Policy** — beide Policies sind Fixed-Window mit `QueueLimit=0`; ein Reconnect-Schwarm (Deploy, WLAN-Wechsel) liefe in harte 429, und `EventSource` gibt bei HTTP-Fehlern **endgültig** auf. Ersatz sind Verbindungs-Limits im Broker (3 pro User, 500 pro Replica, Überschreitung → 503 **vor** dem ersten Byte — deshalb gibt `SubscribeAsync` ein `Task<...?>` zurück statt lazy zu enumerieren). **Jede Verbindung wird serverseitig nach 10 Minuten gekappt:** `OnValidatePrincipal` (Session-Revocation) läuft nur beim Handshake, eine unbegrenzte Verbindung würde das serverseitig wirksame Logout aushebeln; der Browser-Reconnect durchläuft die volle Auth-Pipeline. 10 Minuten ist damit die maximale Revocation-Latenz eines offenen Streams.

**Autorisierungsgrenze des Channel-Streams (`GET /api/channels/{name}/live`): nur eingeloggt + valider Channel-Name** (Nutzer-Entscheidung), nicht `UsageStatsAccessAuthorizationFilter` — der kostet pro Aufruf bis zu zwei ungecachte 7TV-GraphQL-Calls (bei 10-min-Reconnects ein Vielfaches der heutigen Drittanbieter-Last) und würde normale Voter vom Live-Tally aussperren. Geleakt wird nur „in Channel X gab es Aktivität" — über die öffentlich einsehbare getrackte Channel-Liste ohnehin näherungsweise bekannt. `GET /api/admin/live` liegt in der `/api/admin`-Gruppe und erbt deren `GlobalAdminAuthorizationFilter` by construction. **Das öffentliche Worker-Health-Badge bleibt bewusst beim 30-s-Polling:** jedem anonymen Besucher eine persistente Verbindung für einen Statuspunkt zu geben wäre das schlechteste Kosten-Nutzen-Verhältnis des Features und ein unauthentifizierter SSE-Endpoint eine geschenkte Verbindungs-DoS-Fläche.

**Betriebsvertrag Richtung Proxy:** der Broker heartbeatet alle 15 s (`ping`), was **unter** dem kleinsten Read-Timeout aller Zwischenstationen bleiben muss (nginx-Default 60 s); die Antwort setzt `X-Accel-Buffering: no` + `Cache-Control: no-cache, no-transform`, damit nginx-Buffering pro Response aus ist, statt sich auf eine Server-Direktive zu verlassen, die dieses Repo nicht versioniert. Abnahmetest gegen Prod: `curl -N https://emotepurge.app/api/admin/live` — die Pings müssen einzeln über die Zeit eintreffen, nicht als Block. Frontend-seitig läuft `EventSource` **nicht** durch `HttpClient` (kein 401-Interceptor, kein `HttpTestingController`) — deshalb ist der Konstruktor hinter dem `EVENT_SOURCE_FACTORY`-InjectionToken gekapselt und die Playwright-Specs stubben `window.EventSource` per `addInitScript`. Kein automatisches Polling-Fallback: fällt der Stream aus, ist der Fehlermodus exakt das heutige Verhalten (manuelle Refresh-Buttons bleiben überall erhalten).

### 2026-07-31 — `sync-deleted` ist idempotent: bereits archivierte Emotes zählen als Erfolg, nicht als `notFoundIds`

**Betrifft:** `src/EmotePurge.Core/Services/IEmoteService.cs` · `src/EmotePurge.Infrastructure/Services/EmoteService.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/EmoteServiceTests.cs` (neu)

**Live-Test-Befund nach dem 7TV-WebSocket-Rollout: jeder erfolgreiche Mass-Delete zeigte die amber „Rückmeldung fehlgeschlagen"-Warnung.** Ursache ist eine Race, die seit dem EventAPI-Live-Sync der Normalfall ist, kein Randfall: das Frontend löscht auf 7TV, der Worker bekommt das `emote_set.update`-Dispatch und archiviert das Emote sofort (im Log ~1,3 s vor dem HTTP-Call) — dann trifft die `sync-deleted`-Buchhaltungs-Rückmeldung des Frontends ein, deren Query aber auf `NOT IsArchived` filterte, 0 Zeilen fand und `archivedCount: 0` + `notFoundIds` antwortete. Das Frontend wertet das korrekt als „partial" — nur war nichts partial, der Live-Sync war schlicht schneller als die Rückmeldung.

**Fix: der Endpoint ist jetzt ausdrücklich idempotent.** „Bereits archiviert" ist Erreichung des Zielzustands und zählt in `ArchivedCount`; in `notFoundIds` landen nur noch Ids, die unbekannt sind oder einem anderen Channel gehören (die bleiben unangetastet — der konservative Pfad ist unverändert). Auditiert wird weiterhin nur, was **neu** archiviert wurde: ein Call, der nichts verändert hat, ist kein Event. Die Frontend-Seite (`SyncReportState` succeeded/partial/failed) bleibt unverändert — die Unterscheidung ist weiterhin richtig, sie bekommt jetzt nur korrekte Zahlen. Drei Integrationstests decken neu-archiviert, schon-archiviert und fremd/unbekannt ab.

### 2026-07-31 — UI-Feinschliff-Batch: Emote-Grids scrollen per `scrollWindow` mit dem Dokument, Karten-Checkboxen entfallen, Vote-Buttons mobil 44px, Custom-Range als Popover, Delete-Panel channel-gebunden und wegklickbar, Akzentfarben eine Stufe dunkler

**Betrifft:** `web/src/app/features/usage-stats/usage-stats-page.{html,ts}` · `web/src/app/features/voting/vote-session-detail-page.{html,ts}` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `web/src/app/shared/emotes/emote-card-header.ts` · `web/src/app/shared/grid/grid-columns.ts` · `web/src/app/shared/datetime/date-range-popover.ts` (neu) · `web/src/app/shared/seven-tv/{delete-progress-panel,mass-delete-panel}.ts` · `web/src/app/shared/ui/{button,segmented-control}.ts` · `web/src/app/core/seven-tv/seven-tv-delete.service.ts` · `web/src/styles.css` · `docs/UI-Designsprache.md` (§8.5, §10)

**Live-Nutzungs-Feedback, acht Punkte in einem Batch.** Die architektonisch relevanten Entscheidungen:

**Die Emote-Grids scrollen jetzt mit dem Dokument (`cdk-virtual-scroll-viewport scrollWindow`) statt in einer eigenen Box.** Das ist die Fortführung der Sticky-Entscheidung vom 2026-07-31: dort wurde der App-Frame u. a. deshalb verworfen, weil die intern scrollenden CDK-Viewports am Fenster-Scroll hingen — jetzt entfällt der innere Scroll-Container ganz, die Virtualisierung bleibt (bei ~900-Emote-Sets nicht verhandelbar) und rechnet gegen den Fenster-Scroll. Damit fallen die `100dvh`-Clamp-Höhe und der Grid-Rahmen weg; neu dazu kommen `overflow-anchor: none` auf `[scrollWindow]`-Viewports (CDKs `.cdk-virtual-scrollable` mit ebendieser Eigenschaft wird bei `scrollWindow` nicht angewandt; ohne sie jittert das Browser-Scroll-Anchoring beim Zeilen-Recycling) und explizite `minBufferPx`/`maxBufferPx` (die CDK-Defaults 100/200px sind kleiner als eine Grid-Zeile — sichtbare weiße Zeilen bei Momentum-Scroll). Bekannte, akzeptierte Grenze: die Router-Scroll-Restoration greift bei Back-Navigation, bevor die Daten da sind, und bleibt damit an kurzer Seite hängen — kein Regress (der innere Container restaurierte nie), kein Fix in diesem Batch.

**Die Vote-Buttons sind unterhalb `sm` 44px hoch (`min-h-11 sm:min-h-6`), und damit wird die Virtual-Scroll-Zeilenhöhe breakpoint-abhängig.** Der Breakpoint ist bewusst dieselbe 640px-Grenze wie der 2→3-Spalten-Schritt von `computeGridColumns` — `isCompactViewport` in `grid-columns.ts` teilt sie als Konstante, damit CSS (`sm:`) und JS-Zeilenhöhe nie divergieren können. Auf der Voting-Seite speist ein einzelnes `viewportWidth`-Signal beide `computed`s (`columns`, `rowHeight`); die Kartenhöhen-Arithmetik steht als Kommentar an den Konstanten.

**Die Karten-Checkbox ist ersatzlos entfernt** — sie war seit der Clickable-Cards-Umstellung ein rein visueller Spiegel (ihr `checkboxClick`-Output war nirgends gebunden, `tabindex="-1"`); Selektions-Affordanz sind Karten-Klick + Ring. `EmoteCardHeader` ist damit ein reiner Namens-Header, und das damit tote `canManage`-Signal der Voting-Seite fällt mit (das Mass-Delete-Gating lief ohnehin über `activeEmoteSetId`).

**Das Mass-Delete-Fortschritts-Panel ist jetzt channel-gebunden und wegklickbar.** `SevenTvDeleteService.reset()` hatte null Produktions-Aufrufer — ein fertiger Run stand als Root-Service-Singleton für immer im Panel, auch nach Channel-Wechsel mit den Zahlen des falschen Channels. Neu: „Schließen"-Button nach Run-Ende (ruft `reset()`, das jetzt auch `currentChannelName`/`lastReportedIds` mitleert — ohne Panel gibt es nichts mehr nachzumelden) und `resetIfChannelChanged()` im `channelName`-Effect des `ChannelWorkspaceLayout` (der Choke-Point über beiden Grid-Seiten). Ein **laufender** Run wird bewusst nicht zurückgesetzt: ihn zu verstecken wäre schlimmer, als ihn auf der falschen Seite zu zeigen, und er braucht seinen Channel für den abschließenden Sync-Report.

**Custom-Range als Popover (`date-range-popover.ts`), bewusst trigger-los:** die vier inline eingefügten Flex-Items (2 Labels + 2 Date-Inputs) drückten die restliche Filter-Toolbar in die zweite Zeile. Ein erster Wurf mit eigenem Trigger-Button in der Toolbar verschob das Problem nur (deutsche Labels sind breiter — auf Deutsch brach die Zeile weiterhin um, mobil auf drei Zeilen). Endzustand: das Panel ankert **unter der SegmentedControl** (`relative`-Wrapper mit `data-date-range-anchor`), belegt also null Toolbar-Breite — kein Umbruch möglich, in keiner Locale. Wiederöffnen nach Dismiss läuft über den neuen `reselected`-Output der SegmentedControl (Klick auf das bereits selektierte Segment; das `value`-Model re-emittet denselben Wert nicht). Der Anchor-Wrapper zählt beim Outside-Click-Schließen als „innen", sonst schlösse der öffnende Segment-Klick das Panel im selben Event-Durchlauf wieder. Sichtbarkeit besitzt die Seite (`isCustomRangeOpen`), gerendert = offen; Panel-Vertrag sonst wie Datetime-Picker (Outside-Click/Escape/„Fertig", Änderungen wirken sofort).

**Nachtrag Vote-Buttons & Stat-Beschriftung (gleicher Tag, Live-Test):** unter `sm` liegen Behalten/Löschen jetzt **nebeneinander** (ikonzentriert mit Vote-Zähler, Label per `hidden sm:inline` versteckt, `gap-4` als Fehl-Tap-Abstand zwischen den beiden 44px-Zielen) statt gestapelt. Damit braucht die mobile Karte *weniger* Höhe als die Desktop-Karte, `h-44` gilt wieder einheitlich — die nur dafür eingeführte breakpoint-abhängige Zeilenhöhe (`isCompactViewport`/`COMPACT_ROW_HEIGHT_PX` samt Spec-Fällen) ist im selben Zug wieder ausgebaut. Außerdem sind die Kartenzahlen beschriftet („42x **Nutzung**", Voting zusätzlich „**Score** 3,5"; Tooltip trägt die ungetruncatete Fassung), neue Keys `usageStats.usageLabel` + `voting.detail.scoreLabel`.

**Nachtrag `scrollWindow` (gleicher Tag, Live-Test):** CDKs `.cdk-virtual-scroll-content-wrapper` trägt `contain: content`, dessen Paint-Containment alles abschneidet, was eine Karte außerhalb ihrer Box malt — nach dem Wegfall des `px-3`-Zeilen-Paddings waren Selection-Ring und Fokus-Outline der ersten/letzten Grid-Spalte gekappt. Für `[scrollWindow]`-Viewports überschreibt `styles.css` auf `contain: layout style` (Layout-Isolation bleibt, Clipping entfällt; es existieren ohnehin nur ~2 gerenderte Zeilen). **Der Override muss ungelayert stehen** — im ersten Anlauf lag er in `@layer components` und verlor lautlos gegen CDKs ungelayerte Component-Styles (ungelayerte Deklarationen schlagen jede `@layer`-Regel unabhängig von der Spezifität); gleiche Falle und gleicher Platz wie die CDK-Dialog-Chrome-Regeln am Dateiende, der Kommentar dort erklärt es. Die `overflow-anchor`-Regel zog mit um.

**Farben: Filter-Selected-Zustand `purple-600` → `purple-700`, `danger-solid` `red-700` → `red-800` (global, alle vier Call-Sites).** Bewusst getrennt vom `primary`-Button, der auf `purple-600` bleibt — Selected-Zustand und Primäraktion sind ab jetzt zwei verschiedene Intensitäten desselben Akzents. Der aktive Delete-Vote-Button bleibt `red-700`: Voting ist keine destruktive Ausführung, eine Stufe heller als „vollzieht" (§4.2) ist dort die korrekte Aussage.

**Betrifft:** `src/EmotePurge.Core/Messaging/BotCommands.cs` · `src/EmotePurge.Core/Entities/AuditLogEntry.cs` · `src/EmotePurge.Core/Services/{IChannelService,IModRoleCache,IUserService}.cs` · `src/EmotePurge.Infrastructure/Services/{ChannelService,UserService}.cs` · `src/EmotePurge.Infrastructure/Redis/ModRoleCache.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `src/EmotePurge.Worker/Worker.cs` · `web/src/app/core/admin/{admin.model.ts,admin.service.ts}` · `web/src/app/features/admin/{admin-channels-page.ts,admin-users-page.ts,admin-audit-log-page.ts}`

**Welle 3 des Admin-Bereichs: die beiden letzten Stellen, an denen ein Admin bisher von Hand an Redis oder auf den nächsten Timer-Tick warten musste, bekommen Knöpfe.** `POST /api/admin/channels/{name}/resync` stößt einen sofortigen 7TV-Vollsync eines aktiven Channels an, `POST /api/admin/users/{id}/invalidate-role-cache` löscht alle gecachten Rollen-Antworten (Mod/Sub/7TV-Editor) eines Users. Zweiteres ist der direkte Ausweg aus dem 2026-07-25 dokumentierten und bewusst offen gelassenen Staleness-Fall („`/unmod` → `/mod` liefert weiter 403, bis der Redis-Key manuell gelöscht wird") — dieselbe Logik wie beim Purge-UI-Entscheid: kein Button hieß nie „passiert nicht", sondern „passiert per Hand am Redis-Prompt".

**Der Resync läuft als drittes Redis-Kommando (`RESYNC:<channel>`) durch den Worker, nicht in-process in der Api** — obwohl die Api `ISevenTvSyncService` registriert hat. Ein Api-seitiger Sync hätte nur Postgres aktualisiert: `IEmoteMatchCache` und `ChannelSyncGate` sind prozesslokale Singletons, die EventAPI-Subscriptions (`SevenTvSubscriptionRegistry`) existieren nur im Worker-Prozess — und der Api-Prozess hätte den Per-Channel-Gate des Workers umgangen (Kollisionsrisiko auf dem `(ChannelId, SevenTvEmoteId)`-Unique-Index mit einem gleichzeitigen periodischen Resync). Der Worker-Handler macht exakt das, was ein Tick des periodischen Resyncs für diesen einen Channel täte (`EnsureJoinedAsync` als Konvergenznetz inklusive). Der Endpoint antwortet deshalb mit **202, nicht 200**: das Kommandoprotokoll ist einseitig, „Erfolg" heißt „publiziert", nie „fertig" — sichtbar wird die Fertigstellung als `LastSyncedAtUtc` in der Admin-Channel-Liste. Inaktive Channels werden mit 409/`channel_not_joined` abgelehnt (der Sync würde sonst eine EventAPI-Subscription für einen Channel anlegen, in dem der Bot gar nicht steht); beide Fehlerpfade nutzen bestehende `ApiErrorCodes`, kein neuer Code, keine Locale-Änderung. Auditiert wird wie der Always-Audit-Fall von `channel.join`: die Row bleibt unberührt, aber der Worker arbeitet wegen des Kommandos — also `channel.resync`-Eintrag in derselben Transaktion vor dem Publish.

**Nebenzug: das Kommandoprotokoll (Kanalname + `JOIN:`/`LEAVE:`/`RESYNC:`-Präfixe) steht jetzt einmal in `Core/Messaging/BotCommands`** statt als zweimal duplizierte `private const` in Producer (`ChannelService`) und Consumer (`Worker`). Beim zweiten Kommando war die Duplikation ein Schönheitsfehler, beim dritten wäre sie ein Drift-Risiko. Dass unbekannte Präfixe im Worker still ignoriert werden (kein `else`-Zweig), ist weiterhin gewollt — genau das machte das neue Kommando deployment-reihenfolge-unabhängig: eine alte Worker-Version überliest `RESYNC:` klaglos.

**Die Cache-Invalidierung bekommt `IModRoleCache.InvalidateUserAsync` — pro User, nicht pro Channel, und per SCAN.** Die Key-Struktur (`modcheck:{uid}:{channel}`, `subcheck:{uid}:{bid}`, `7tveditor:{uid}`) ist nach User indiziert, nicht nach Channel; „alle Einträge dieses Users" ist damit ein billiger, gezielter SCAN über drei Präfixe, „alle Einträge dieses Channels" wäre ein Scan über den gesamten Keyspace. Der Admin-Anwendungsfall („dieser eine User meldet ein falsches 403/Zugriffsrecht") ist ohnehin user-bezogen. Gelöscht werden bewusst **alle drei** Rollen-Arten statt nur `modcheck`: der Admin am Knopf weiß nicht, welche der drei Antworten die veraltete ist, und alle drei repopulieren beim nächsten Check für den Preis je eines Drittanbieter-Calls. Orchestriert wird in `UserService.InvalidateRoleCacheAsync` (Existenzprüfung → 404 statt Redis-SCAN mit beliebiger Routen-Eingabe; Audit `user.invalidateRoleCache` mit `removedEntries`-Zählwert **nach** der Löschung, weil Redis-Write und Audit-Row ohnehin keine gemeinsame Transaktion haben können und „Cache schon sauber, Audit fehlgeschlagen" die harmlose Richtung ist). Der Actor-Parameter ist hier wieder **nicht** nullable — anders als beim Revoke gibt es keinen Selbstbedienungs-Pfad, der unauditiert bleiben dürfte.: Header (`h-14`/`z-30`), Tab-Leisten (`h-10`/`top-14`) und Filter-Toolbars (`top-24`) bleiben beim Scrollen stehen; neue z-Leiter und `.app-sticky-bar`-Primitive

**Betrifft:** `web/src/styles.css` · `web/src/app/features/shell/app-shell.ts` · `web/src/app/features/landing/landing-page.html` · `web/src/app/features/admin/admin-layout.ts` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `web/src/app/features/admin/admin-audit-log-page.ts` · `web/src/app/features/usage-stats/usage-stats-page.html` · `web/src/app/features/voting/vote-session-detail-page.html` · `docs/UI-Designsprache.md` (§8.1, neu §8.5)

**Live-Test-Feedback nach Admin-Welle 2: Tab-Navigation und Filter scrollen aus dem Viewport — auf jeder Bereichsseite.** Nutzer-Entscheidung: alle drei Ebenen (Header + Tabs + Filter) bleiben sichtbar, und zwar als **Sticky im normalen Seitenfluss, ausdrücklich kein fester App-Frame** mit innerem `overflow-y`-Container. Der App-Frame wäre die optisch „sauberere" Trennung gewesen, hätte aber drei funktionierende Mechanismen gebrochen, die alle am Fenster-Scroll hängen: die intern scrollenden CDK-Virtual-Scroll-Viewports der Emote-Grids (deren `100dvh`-Clamp gegen das Fenster rechnet), `withInMemoryScrolling` (Scroll-Restoration + Anchor-Scrolling laufen über den Dokument-Scroll) und das Einklappen der Mobile-Browserleiste (das nur beim Dokument-Scroll passiert). Sticky erreicht dasselbe UX-Ziel ohne einen dieser Umbauten — der Header war ohnehin schon sticky, es fehlten nur die zwei Ebenen darunter.

**Feste Höhen sind ab jetzt Vertrag, nicht Optik.** Gestapelte Sticky-Ebenen brauchen exakte `top`-Offsets: der Header ist auf `h-14` fixiert (vorher `py-3` mit inhaltsabhängiger Höhe — der 44px-Menü-Button passt in die 56px), die Tab-Leisten auf `h-10` (Links `flex items-center` statt `py-2`, damit die Höhe exakt trägt), Filter-Toolbars pinnen bei `top-24` (= 14 + 10). Nur die unterste Ebene darf in der Höhe wrappen — ihr `top` hängt allein von den Ebenen über ihr ab. Neue Primitive `.app-sticky-bar` in `styles.css` (sticky, `z-20`, `color-mix`-Blur-Hintergrund) statt fünf kopierter Klassenketten; der `top`-Wert bleibt Tailwind an der Verwendungsstelle, weil er kontextabhängig ist.

**Die z-Leiter musste dafür neu geordnet werden — das war der eigentliche Fehlerteufel.** Vorher: Header `z-10`, Karten-Action-Container (Stretched-Link-Vertrag) ebenfalls `z-10`, Mobile-Disclosure `z-20` im Header-Kontext. Sticky-Leisten mit `z-10` hätten verloren (Content ist DOM-später — Join/Leave/Purge-Buttons hätten beim Drunterscrollen **über** der Tab-Leiste gemalt), Leisten mit `z-20` hätten das Mobile-Menü verdeckt (dessen `z-20` zählt nur **innerhalb** des Header-Stacking-Kontexts, verglichen wird der Header selbst). Neue verbindliche Leiter: Content-Aktionen `z-10` (unverändert, Stretched-Link-Vertrag bleibt) < Sticky-Leisten `z-20` < Header `z-30` (Landing-Header zieht auf `z-30` mit — eine Leiter, nicht zwei). Der Datetime-Dropdown (`z-30` im `z-10`-Karten-Kontext) bleibt unter den Leisten und öffnet nach unten — kein Konflikt. Dokumentiert als §8.5 der Designsprache samt Regel für neue Seiten („Filter-Toolbar ⇒ `app-sticky-bar top-24 py-2`").

**Bewusster Mobile-Trade-off:** auf 360px binden Header + Tabs + gewrappte Filter-Toolbar bis zu ~220px Viewport-Höhe. Vom Nutzer so gewählt (Alternative „Header scrollt weg" wurde verworfen); der dokumentierte Rückweg, falls es sich im Alltag zu eng anfühlt, ist ein `max-md:static` auf der jeweiligen Toolbar — eine Klasse, kein Umbau.

### 2026-07-31 — Admin-User-Übersicht: `GET /api/admin/users` mit ausschließlich abgeleitetem Token-Status; Revoke-Sessions als erzwungener Logout, auditiert über einen nullable `AuditActor`

**Betrifft:** `src/EmotePurge.Core/Services/IAdminUserQueryService.cs` · `src/EmotePurge.Core/Services/IUserService.cs` · `src/EmotePurge.Core/Entities/AuditLogEntry.cs` · `src/EmotePurge.Infrastructure/Services/{AdminUserQueryService,UserService}.cs` · `src/EmotePurge.Infrastructure/ServiceCollectionExtensions.cs` · `src/EmotePurge.Api/Endpoints/{AdminEndpoints,AuthEndpoints}.cs` · `web/src/app/core/admin/{admin.model.ts,admin.service.ts}` · `web/src/app/features/admin/{admin-users-page.ts,admin-layout.ts}` · `web/src/app/app.routes.ts`

**Zweiter Slice der Admin-Welle 2: `/admin/users`** — jeder jemals eingeloggte User, zuletzt gesehene zuerst, paginiert (anders als die Channel-Liste, die Twitchs JOIN-Limit deckelt, wächst diese Liste mit jedem Viewer-Login). **Der Token-Zustand verlässt die Datenbank nur als abgeleiteter Status:** `HasRefreshToken` ist ein Boolean, der in der SQL-Projektion über der verschlüsselten Spalte entsteht — die Ciphertexte materialisieren nicht einmal im API-Prozess, es gibt schlicht kein DTO-Feld, das sie tragen könnte. Ablaufzeitpunkt und Scopes sind reine Metadaten-Spalten und gehen mit; dazu `SessionsValidFromUtc`, damit ein Admin *sieht*, dass ein Revoke gegriffen hat. Es gibt kein Admin-Szenario, das die Tokens selbst bräuchte — also gibt es keinen Weg, sie zu leaken.

**`POST /api/admin/users/{id}/revoke-sessions` ist semantisch ein erzwungener Logout** und spiegelt exakt den bestehenden Selbst-Logout-Pfad: Session-Revocation (`SessionsValidFromUtc = now`, greift beim nächsten Request über `OnValidatePrincipal`) **plus** `ClearTwitchTokensAsync` — die dokumentierte Invariante „alle Twitch-Token-Spalten werden gemeinsam geleert, wenn Sessions enden" gilt unverändert, ein Admin-Revoke, der die Server-Fähigkeit zum Token-Refresh eines ausgesperrten Users behielte, wäre die inkonsistentere Variante. Auditiert wird als **eine** Aktion (`user.revokeSessions`, `TargetType/TargetId` auf den User, Login des Betroffenen als `DetailsJson`-Snapshot) — das Token-Löschen ist Konsequenz der Revocation, kein zweiter Eintrag. Unbekannter User → 404 über den generischen `errors.status.*`-Fallback, kein neuer `ApiErrorCode`.

**Die Signatur `RevokeSessionsAsync(userId, AuditActor? actor)` weicht bewusst vom Audit-Vertrag des MVP ab**, der Actor-Parameter *nicht optional* machte. Der Grund für die Ausnahme: dieselbe Methode bedient zwei Pfade mit gegensätzlichen Audit-Anforderungen — der Admin-Revoke *muss* auditieren, der Selbst-Logout *darf nicht* (Nutzer-Entscheidung des MVP: keine Login-/Logout-Events, das Log soll die seltenen destruktiven Aktionen zeigen, nicht Session-Rauschen). Ein nullable Parameter **ohne Default** erzwingt an jeder Call-Site die sichtbare Entscheidung (`actor: null` steht im Logout-Handler mit Begründungskommentar); ein Default-Wert hätte dagegen genau das stille Vergessen ermöglicht, das der MVP-Vertrag ausschließen wollte. Zusätzlich neu: `Task<bool>`-Rückgabe statt stillem No-op, weil der Admin-Endpoint einen unbekannten User als 404 melden können muss, während dem Logout das Ergebnis egal sein darf.

**Frontend:** vierter Tab `users` (gleiches RouterLink-Muster, erbt den `adminGuard` vom Parent), Karte pro User mit `StatusBadge` (`emerald` verbunden / `slate` keine Tokens, Farbsemantik §4.3). Der Revoke läuft über den **einfachen `ConfirmDialog`, nicht den TypedConfirm**: die Purge-Entscheidung definierte das Nachtippen als Schranke für *unwiderrufliche, zeilenbezogene* Löschungen — ein Revoke ist widerruflich (der User loggt sich neu ein), die getippte Bestätigung wäre Zeremonie ohne passendes Risiko. Der eigene Account ist absichtlich **nicht** ausgenommen (es ist ein normaler Logout), aber der Dialog sagt es dazu — ein Admin, den die Admin-Seite kommentarlos abmeldet, liest das sonst als Bug.

### 2026-07-31 — Audit-Log-Filterung (Welle 2): `action`/`channel`/`actor` als Query-Params — Channel exakt auf der normalisierten Form, Actor als Substring; der zurückgestellte Komposit-Index kommt damit

**Betrifft:** `src/EmotePurge.Core/Services/IAuditLogQueryService.cs` · `src/EmotePurge.Infrastructure/Services/AuditLogQueryService.cs` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs` · `src/EmotePurge.Infrastructure/Migrations/20260731134345_AddAuditLogChannelNameIndex.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `web/src/app/core/admin/{admin.model.ts,admin.service.ts}` · `web/src/app/features/admin/admin-audit-log-page.ts` · `web/src/app/shared/ui/segmented-control.ts` · `web/e2e/support/mocks.ts`

**Erster Slice der Admin-Welle 2: das Audit-Log ist filterbar** — nach Aktion, Channel und ausführendem User, AND-kombiniert, als optionale Query-Params auf dem bestehenden `GET /api/admin/audit-log` (`AuditLogFilter`-Record am `IAuditLogQueryService`; kein neuer Endpoint, kein neuer `ApiErrorCode` — unbekannte Filterwerte liefern eine leere Seite statt eines Fehlers, konsistent zur stillen Paging-Klammerung). **Kein Zeitraum-Filter** (Nutzer-Entscheidung): bei „neueste zuerst" plus Paginierung ist ein Von/Bis-Paar UI-Gewicht ohne realen Suchgewinn im heutigen Log-Volumen. Der `TotalCount` der Antwort ist der der *gefilterten* Menge — der Pager rechnet damit, alles andere ergäbe Geisterseiten.

**Die drei Filter matchen bewusst unterschiedlich.** `action` exakt gegen die `AuditActions`-Konstanten — das UI bietet ohnehin nur bekannte Aktionen als Segmente an („Alle" + die sieben Labels, dieselben i18n-Keys wie die Zeilen selbst). `channel` läuft serverseitig durch `ChannelName.Normalize` und matcht dann **exakt** (Regel 9: jeder Lookup auf der normalisierten Form; ein Admin, der `HandOfBlood` tippt, findet `handofblood`) — exakt statt Substring, weil Channel-Namen dem Admin bekannt sind und nur der exakte Match den Index nutzen kann. `actor` dagegen ist ein **case-insensitiver Substring** (`ILIKE %…%`): Actor-Logins sind anders als Aktionen und Channels nirgends aufgezählt, ein Teilstring ist hier die einzig brauchbare Sucheingabe; dass ein `_` im Suchtext als LIKE-Wildcard ein Zeichen zu viel matcht, ist als Obermenge harmlos und keine Escaping-Maschinerie wert.

**Der `AddAuditLog`-Eintrag hatte den Komposit-Index ausdrücklich auf „mit der Filter-UI" vertagt — die ist jetzt da, also kommt er:** `(ChannelName, OccurredAtUtc DESC)` (Migration `AddAuditLogChannelNameIndex`), passgenau für „dieser Channel, neueste zuerst". Aktion und Actor bekommen **keinen** Index: `action` hat eine Handvoll distinkte Werte, und dem `ILIKE`-Substring könnte ein B-Tree ohnehin nicht dienen — bei der Tabellengröße (sieben seltene Aktionstypen, gedeckelte Channel-Zahl) ist der Seq-Scan in diesen Zweigen die ehrliche Antwort.

**Frontend: Live-Filter mit Debounce — das erste Debounce-Muster des Frontends.** Die Textfelder filtern pro Tastendruck, ohne Enter/Blur: das Emote-Grid filtert bereits live, und zwei Filterleisten mit unterschiedlicher Bedienung wären die schlechtere Inkonsistenz (Live-Test-Feedback vom 2026-07-31 — die erste Fassung committete auf Enter/Blur). Der Unterschied zum Emote-Grid: dort ist der Filter clientseitig über ein bereits geladenes Array, hier steht ein paginierter Endpoint dahinter — deshalb laufen die Textwerte über `toObservable(...).pipe(debounceTime(300))` in die `rxResource`-Params, der `SegmentedControl` dagegen ungebremst (ein Klick ist ein bewusster einzelner Schritt). Jede Filter-Änderung springt auf Seite 1 (die alte Seitenzahl gehört zur alten Ergebnismenge), leere Filterwerte werden gar nicht erst als Params gesendet (stabile, cache-vergleichbare URLs), und der „keine Treffer"-Zustand ist ein eigener Empty-State mit Reset — unterscheidbar von „Log ist leer". **Der Channel-Filter deaktiviert sich, solange eine channel-lose Aktion gewählt ist** (heute nur `user.revokeSessions`): die Kombination könnte ausschließlich nichts matchen, also wird ein getippter Wert beim Umschalten geleert, das Feld disabled (Tooltip erklärt warum), und der Params-Bau schließt den in der Debounce-Lücke noch nachhängenden alten Wert explizit aus. Der Action-Filter nutzt den bestehenden `SegmentedControl` (Wiederverwendungsgebot der Designsprache §10); der bekam dafür `flex-wrap` samt Umstellung der Segment-Trenner von `border-l` auf Container-`gap-px` — bei acht Segmenten auf Mobilbreite muss der Control umbrechen dürfen, und nur der Gap-Ansatz zeichnet die Trennlinien auch zwischen Zeilen.

### 2026-07-31 — `docs/UI-Designsprache.md` als verbindliche Wie-Vorgabe; Destruktiv-Stufung nach Flow-Position präzisiert; dritte Typo-Ebene (Kartentitel); Tab-`aria-current` und Feldfehler-ARIA verbindlich

**Betrifft:** `docs/UI-Designsprache.md` · `web/.claude/CLAUDE.md` · `web/src/app/shared/ui/button.ts` · `web/src/app/features/admin/admin-monitoring-page.ts` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `web/src/app/features/voting/vote-session-list-page.html` · `web/src/app/features/admin/admin-channels-page.ts`

**Die Designsprache der UI/UX-Überarbeitung (Wellen 1–3) steht jetzt als eigenes Vorgabe-Dokument.** Ihre Regeln waren bis hierher über ~10 Einträge dieses Logs, Code-Kommentare und die `.audit-out`-Artefakte des Audit-Harness verstreut — wer neue UI baute, musste sie sich zusammensuchen, und das Admin-Bereich-Audit vom 2026-07-31 hat gezeigt, dass dabei trotzdem Inkonsistenzen entstehen. [`docs/UI-Designsprache.md`](UI-Designsprache.md) ist ab jetzt das **Wie** (pro Regel: was gilt, wann anwenden, Referenz-Implementierung), dieses Log bleibt das **Warum**; `web/.claude/CLAUDE.md` verweist verbindlich darauf. Bei Widerspruch zwischen einem älteren Log-Eintrag und dem Dokument gilt das Dokument — ältere Einträge bleiben, wie immer, unangetastet stehen.

**Destruktiv-Stufung präzisiert: Flow-Position, nicht Schwere.** Der Primitives-Eintrag vom 2026-07-30 formulierte `danger-solid` als „ausschließlich der 7TV-Mass-Delete" — der Bestand hat das nie gelebt: der generische `ConfirmDialog` bestätigt auch reversible Aktionen (Channel verlassen, Session löschen) mit `danger-solid`, der TypedConfirm des Channel-Purge ebenso, während ausgerechnet der unwiderrufliche Purge per `danger`-Outline **ausgelöst** wird. Diese Praxis ist in sich konsistent und wird zur Regel erklärt: `danger` (Outline) ist der auslösende destruktive Button im Seitenkontext, `danger-solid` (gefüllt) der ausführende Bestätigen-Button im Dialog sowie der Seiten-Haupt-CTA des Mass-Delete — Outline löst aus, Solid vollzieht. Eine Schwere-basierte Stufung hätte den `ConfirmDialog` umgebaut und dem Nutzer trotzdem nicht gesagt, *wo im Flow* er steht; die Flow-Regel deckt alle sieben bestehenden Verwendungsstellen ohne eine einzige Code-Änderung. Nur der Directive-Kommentar in `button.ts` zieht nach.

**Dritte Typografie-Ebene: Kartentitel `text-base font-semibold`.** Der Visual-Refresh-Eintrag definierte Seitentitel (`text-2xl font-bold tracking-tight`) und Sektionstitel (`text-lg font-semibold`), aber nichts darunter — mit dem Ergebnis, dass die Karten-`<h3>`s der Admin-Monitoring-Seite in Sektionsgröße liefen (Audit-Befund: `h2` und `h3` derselben Seite zeichengleich `text-lg font-semibold`, keine sichtbare Ebene Unterschied), die Landing-Karten dagegen `text-base` und die Listen-Karten `font-medium`-Links. Die Skala hat jetzt vier feste Stufen (Seite → Sektion → Kartentitel `text-base font-semibold` → Listen-Titellink `font-medium`); die drei Monitoring-`<h3>`s sind abgesenkt. Die Landing bleibt als Marketing-Ausnahme außerhalb der Skala.

**Zwei ARIA-Lücken geschlossen, beide als Muster verbindlich.** Erstens: die Admin-Tabs trugen `ariaCurrentWhenActive="page"`, die zeichengleich kopierten Workspace-Tabs nicht — Assistive Tech erfuhr dort nicht, welcher Tab aktiv ist. Das Attribut ist jetzt Teil des kanonischen Tab-Snippets im Dokument und auf beiden Workspace-Tabs nachgezogen (die Klassenkette bleibt bewusst 5× kopiert statt Primitive — sie ist stabil, und eine Komponente für zwei Vorkommen-Orte wäre Abstraktion ohne zweiten Nutzen; das Dokument benennt alle Kopien für den Synchron-Fall). Zweitens: Feldfehler hatten kein geregeltes Muster — `aria-describedby` existierte im gesamten Frontend genau einmal (TypedConfirm-Hint), kein Formular setzte `aria-invalid`, kein Fehler-`<p>` hatte eine `id`, und die Fehlergrößen drifteten (`text-xs` vs. `text-sm`). Das Muster ist jetzt festgeschrieben (Fehler-`<p>` mit `id` in `text-sm text-red-400`, Input mit `aria-invalid` + `aria-describedby` nur im Fehlerfall) und in den beiden validierenden Formularen (Vote-Session-Titel, Admin-Channel-Join) umgesetzt.

### 2026-07-31 — Overview-Admin-Sektion entfernt; `GET /api/channels` + `IChannelService.ListAllAsync` ersatzlos gestrichen — damit endet auch das 403-Probing im Frontend

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `src/EmotePurge.Api/Validation/ChannelNameValidationFilter.cs` · `src/EmotePurge.Core/Services/IChannelService.cs` · `src/EmotePurge.Infrastructure/Services/ChannelService.cs` · `tests/EmotePurge.Infrastructure.Tests/Integration/ChannelServiceTests.cs` · `web/src/app/features/overview/overview-page.{ts,html}` · `web/src/app/core/channels/{channel.service.ts,channel.model.ts}` · `web/public/i18n/{de,en}.json` · `web/e2e/support/mocks.ts` · `web/e2e/audit/ui-audit.audit.ts` · `Architectur.md`

**Nachzug zum Admin-Bereich: die Admin-Sektion der Übersichtsseite ist weg.** Sie zeigte einem globalen Admin unterhalb seiner eigenen Channels zusätzlich *alle* getrackten Channels plus ein Formular zum Joinen eines beliebigen Namens. Seit dem Channel-Verwaltungs-Slice (Eintrag unten, gleicher Tag) kann `/admin/channels` alles davon und mehr: dieselbe Liste mit Emote-/Vote-Session-Aggregaten, dasselbe Join-Formular (nur gegen den *normalisierten* Namen validiert, Regel 9), dazu Leave und Purge. Zwei Listen derselben Sache auf zwei Seiten sind kein Feature, sondern zwei Stellen, die auseinanderlaufen — der Eintrag unten hielt die Overview-Sektion noch bewusst unangetastet, weil ihr Ersatz im selben Slice erst entstand. Der Ersatz steht, also fällt das Original.

**Backend ersatzlos gestrichen statt deprecated:** `GET /api/channels` und `IChannelService.ListAllAsync` existierten ausschließlich für diese Sektion und haben nach ihrem Entfernen **null** Aufrufer. Nachfolger ist `GET /api/admin/channels` (`IAdminChannelQueryService`), ein Superset desselben Inhalts. Ein zurückgelassener toter Endpoint wäre Angriffsfläche und Pflegelast ohne Nutzen; die HTTP-Route ist damit endgültig frei. `ChannelNameValidationFilter` bekommt keine neue Logik — in der `/api/channels`-Gruppe gibt es jetzt nur noch *einen* Endpoint ohne `channelName`-Routenwert (`/mine`), sein Klassenkommentar zieht nach.

**Und damit ist das 403-Probing vollständig Geschichte.** Die Sektion war die letzte Stelle, die eine Berechtigung ermittelte, indem sie einen echten Endpoint aufrief und dessen Fehlschlag als Antwort las (`listAll()` → 403 = „kein Admin" → Sektion ausblenden). Die vier anderen Proben verschwanden mit `GET /api/channels/{c}/permissions` (S2-13, Eintrag weiter unten); diese fünfte überlebte, weil sie das Ergebnis *und* die Daten in einem Call holte. Sichtbarkeit entscheidet jetzt durchgängig ein gelesenes Feld (`adminGuard` liest `isGlobalAdmin` aus dem gecachten `/me`), nie ein provozierter Fehler — jede Anfrage im Log ist wieder eine gewollte Anfrage. Frontend-seitig entfallen dadurch das `adminChannels`-Signal, der `FormControl` samt eigenem Channel-Namens-Muster (das Muster lebt normalisiert in `core/channels/channel-name.ts` weiter) und die i18n-Keys `overview.admin.*` in beiden Locales; der Rest der Übersicht — „Meine Channels", Reaktivieren, Notices — bleibt unverändert. Im Audit-Harness ersetzen fünf `/admin/*`-Szenarien (Monitoring gesund/degradiert, Channel-Liste voll/leer, Audit-Log) das bisherige `overview-admin`, und der `mockAdminChannels`-Stub entfällt mitsamt seinem Endpoint.

### 2026-07-31 — Audit-Log-Vertrag: Einträge in derselben Transaktion wie die Aktion, Snapshot-Felder statt Fremdschlüsseln, unbegrenzte Retention

**Betrifft:** `src/EmotePurge.Core/Entities/AuditLogEntry.cs` · `src/EmotePurge.Core/Services/AuditActor.cs` · `src/EmotePurge.Core/Services/IAuditLogQueryService.cs` · `src/EmotePurge.Core/Services/IChannelService.cs` · `src/EmotePurge.Core/Services/IVoteSessionService.cs` · `src/EmotePurge.Core/Services/IEmoteService.cs` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs` · `src/EmotePurge.Infrastructure/Persistence/AuditLogWrites.cs` · `src/EmotePurge.Infrastructure/Migrations/20260731101655_AddAuditLog.cs` · `src/EmotePurge.Infrastructure/Services/{ChannelService,VoteSessionService,EmoteService,AuditLogQueryService}.cs` · `src/EmotePurge.Api/Auth/ClaimsPrincipalExtensions.cs` · `src/EmotePurge.Api/Endpoints/{AdminEndpoints,ChannelEndpoints,VoteSessionEndpoints,EmoteEndpoints}.cs` · `web/src/app/core/admin/` · `web/src/app/features/admin/admin-audit-log-page.ts`

**Audit-Log-Slice des Admin-Bereichs — der letzte des MVP.** Bis hierher existierte kein Nachweis darüber, wer einen Channel gejoint, verlassen oder gepurged, wer eine Abstimmung angelegt, beendet oder gelöscht und wer Emotes über 7TV entfernt hat. Genau diese **sieben Aktionen** sind jetzt auditiert (`AuditActions`: `channel.join`, `channel.leave`, `channel.purge`, `voteSession.create`, `voteSession.end`, `voteSession.delete`, `emotes.syncDeleted`). **Keine Login-Events** (Nutzer-Entscheidung): Logins sind Hintergrundrauschen mit hoher Frequenz und ohne Änderungswirkung, sie hätten das Log geflutet, in dem die seltenen destruktiven Aktionen sichtbar bleiben sollen. Die Wähler-Stimmen selbst sind ebenfalls nicht auditiert — sie *sind* bereits Daten mit Urheber (`Vote.UserId`), eine zweite Aufzeichnung wäre Redundanz.

**Geschrieben wird im Service-Layer, in der Transaktion der Aktion selbst.** `IChannelService.JoinAsync/LeaveAsync/PurgeAsync`, `IVoteSessionService.CreateAsync/EndAsync/DeleteAsync` und `IEmoteService.MarkDeletedAsync` bekommen einen zusätzlichen, **nicht optionalen** `AuditActor`-Parameter (Core-Record, aus dem Principal im Endpoint gebaut — `Core` und `Infrastructure` kennen kein `HttpContext`). Die Implementierungen hängen ihren `AuditLogEntry` über `db.AddAuditEntry(...)` an **denselben** Change-Tracker und lassen ihn vom bereits vorhandenen `SaveChangesAsync` mitschreiben: kein Fire-and-forget, kein zweiter Roundtrip, keine Möglichkeit, dass die Aktion committet und ihr Nachweis verlorengeht. Deshalb ist der Parameter auch verpflichtend und nicht `AuditActor? actor = null` — ein neuer Aufrufer soll nicht versehentlich unattributierte Historie erzeugen können. Die Redis-Publish-Reihenfolge bleibt **unverändert** (Join/Leave publizieren nach dem Persist, Purge publiziert LEAVE davor). Einzige Stelle mit expliziter Transaktion: `CreateAsync` — `VoteSession.Id` wird von der Datenbank vergeben, existiert also vor dem Insert nicht, und die `TargetId` des Eintrags soll darauf zeigen; zwei `SaveChangesAsync` in einer `BeginTransactionAsync`-Klammer halten die Atomarität trotzdem.

**No-ops schreiben nichts.** Ein Leave/Purge auf einen unbekannten Channel, ein Delete/End auf eine unbekannte Session, ein zweites `EndAsync` auf eine bereits beendete Abstimmung und ein `MarkDeletedAsync`, das keine Zeile trifft, kehren ohne Eintrag zurück — auditiert wird, was *passiert* ist, nicht was versucht wurde. Bewusste Ausnahme: `JoinAsync` schreibt auch dann, wenn der Channel schon aktiv war, denn jeder Join publiziert ein JOIN-Kommando und lässt den Worker den Channel (erneut) betreten.

**Alle Verweise sind Snapshot-Strings, ausdrücklich keine Fremdschlüssel.** Der Actor ist kein FK auf `User`, weil `Vote.UserId` bereits mit `DeleteBehavior.Restrict` verhindert, dass eine künftige Nutzerlöschung Abstimmungshistorie mitreißt — eine zweite restringierende Kante aus dem Audit-Log hieße, dass ausgerechnet das Protokoll die Löschung blockiert, die es protokollieren soll. Der Channel ist kein FK, weil `channel.purge` die Channel-Zeile **in derselben Transaktion** löscht, in der sein Eintrag entsteht: ein `Cascade` würde den Nachweis der Löschung mitlöschen, ein `Restrict` den Purge ganz verbieten. `ChannelName` hält die normalisierte Form (Regel 9), `ActorLogin` den Login zum Zeitpunkt der Aktion — ein später umbenanntes Konto zeigt weiterhin, was damals galt, was der Zweck eines Audit-Logs ist.

**`DetailsJson` ist `jsonb` und bewusst formfrei** (`{"emoteCount":12}` bei `emotes.syncDeleted`, `{"title":"…"}` bei den Vote-Session-Aktionen): jede Aktion darf mitgeben, was sie braucht, ohne Schemaänderung. `jsonb` statt `text`, damit ein späterer Filter auf ein Detailfeld eine Query bleibt und keine Migration wird. Das Frontend parst defensiv (`try/catch`, Typprüfung je Feld, unbekannt → nichts) — Postgres normalisiert `jsonb` beim Speichern, ein Byte-für-Byte-Vergleich wäre also ohnehin falsch (die Tests prüfen deshalb den geparsten Wert). Index heute nur `(OccurredAtUtc DESC)`, passend zum einzigen Zugriffsmuster „neueste zuerst, paginiert"; `(ChannelName, OccurredAtUtc)` kommt mit der Filter-UI in Welle 2.

**Retention: unbegrenzt, kein Cleanup-Job** (Nutzer-Entscheidung). Bei sieben seltenen Aktionstypen und der durch Twitchs JOIN-Limit gedeckelten Channel-Zahl wächst die Tabelle in einer Größenordnung, die auf Jahre irrelevant ist; ein Cleanup ließe sich ohne Formänderung nachrüsten, aber ein von Anfang an löschendes Audit-Log wäre die schlechtere Vorgabe. Gelesen wird über `GET /api/admin/audit-log?page=&pageSize=` (`IAuditLogQueryService`, dritter Endpoint der `/api/admin`-Gruppe, dadurch automatisch global-admin-only) mit derselben stillen Paging-Klammerung wie die Vote-Session-Listen (`pageSize` 1–100, Default 20) — **keine neuen `ApiErrorCodes`**. Sortiert wird `OccurredAtUtc DESC`, **`Id DESC` als Tiebreak**: Einträge aus einer Transaktion teilen sich den Zeitstempel auf den Tick, und ohne totale Ordnung liefert `Skip/Take` dieselbe Zeile auf zwei Seiten und lässt eine andere aus.

### 2026-07-31 — Umkehr von S1-1: Channel-Purge bekommt eine UI — hinter getippter Namensbestätigung; neuer Aggregat-Endpoint `/api/admin/channels`

**Betrifft:** `src/EmotePurge.Core/Services/IAdminChannelQueryService.cs` · `src/EmotePurge.Infrastructure/Services/AdminChannelQueryService.cs` · `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `web/src/app/shared/ui/typed-confirm-dialog.ts` · `web/src/app/core/channels/channel-name.ts` · `web/src/app/core/channels/channel.service.ts` · `web/src/app/core/admin/` · `web/src/app/features/admin/admin-channels-page.ts`

**Channel-Verwaltungs-Slice des Admin-Bereichs — und die bewusste Umkehr des Review-Befunds S1-1.** `DELETE /api/channels/{name}/purge` existierte seit dem Review vom 2026-07-29 absichtlich **ohne** UI: die Aktion ist die einzige unwiderrufliche Datenlöschung der Anwendung (Channel → Emotes → UsageStats, Channel → VoteSessions → Votes, alles `Cascade`), und ein Button dafür schien das Risiko nicht wert. In der Praxis hieß „kein Button" aber nicht „passiert nicht", sondern: der Admin baut sich die Anfrage von Hand gegen die Produktion — ohne Vorschau darauf, *was* an dem Channel überhaupt hängt, und ohne jede Bestätigung. Das ist die schlechtere Variante. Mit dem dedizierten Admin-Bereich existiert jetzt ein Ort, an dem die Aktion hingehört, und drei unabhängige Schranken davor: die admin-only Route (`adminGuard`), der serverseitige `GlobalAdminAuthorizationFilter` am Endpoint (unverändert — er war nie die Schwachstelle) und **eine getippte Namensbestätigung**.

**Dafür neu: `TypedConfirmDialog` in `shared/ui/`** — der härtere Zwilling des `ConfirmDialog` für Aktionen, die unwiderruflich *und* zeilenbezogen sind. Der Bestätigen-Button (`danger-solid`, dieselbe Stufe wie der 7TV-Mass-Delete) entsperrt erst, wenn der Nutzer den Channel-Namen exakt nachtippt. Begründung: ein normaler Ja/Nein-Dialog beweist nur, dass geklickt wurde; das Nachtippen beweist, *welche* Zeile gemeint war — und genau der Fehlgriff eine Zeile daneben ist der Schaden, den hier keine Rückfrage sonst abfängt. Vergleich case-sensitiv (nur getrimmt), weil ein in falscher Schreibweise getippter Name ein Hinweis darauf ist, dass der Name nicht aus der angeklickten Zeile stammt. Der Grund für die Deaktivierung steht als **Text** neben dem Button (`common.typedConfirmHint`) — ein ausgegrauter Button allein erklärt nichts (WCAG). Enter im Eingabefeld läuft durch dieselbe Prüfung wie der Klick, nicht am `disabled`-Attribut vorbei. Kein isolierter Component-Spec (Regel 12), stattdessen im Playwright-Flow abgedeckt: disabled → Beinahe-Treffer bleibt disabled → exakter Treffer aktiviert → DELETE.

**Neuer Lese-Endpoint `GET /api/admin/channels`** (`IAdminChannelQueryService`, zweiter Endpoint der `/api/admin`-Gruppe, dadurch automatisch global-admin-only). Er ist ein **Superset** des bestehenden `GET /api/channels`, ersetzt es aber nicht: die Overview-Sektion bleibt unangetastet an ihrem schmaleren Vertrag. Die Antwort trägt Zähler paarweise als **Gesamtzahl + Teilmenge** (`emoteCount`/`archivedEmoteCount`, `voteSessionCount`/`activeVoteSessionCount`) statt als disjunkte Zahlen — so kann die UI „903 (17 archiviert)" rendern, ohne selbst zu rechnen und dabei von der Datenbank abweichen zu können. `lastSyncedAtUtc` ist bewusst `null`-fähig: ein frisch gejointer Channel ohne Emote-Zeilen ist etwas anderes als einer, dessen Sync lange her ist. Die Implementierung folgt Regel 10 — drei einfache Abfragen (Channels, Emote-Aggregat, VoteSession-Aggregat) mit `GroupBy` auf der **skalaren FK-Spalte** einer einzelnen Tabelle und anschließendem In-Memory-Stitch, statt einer Mega-Query über Navigations-Joins, die EF Core/Npgsql wieder auf Client-Evaluation zurückfallen ließe. Bei der Channel-Größenordnung (Twitchs JOIN-Limit deckelt sie ohnehin) ist das nicht nur unkritisch, sondern die lesbarere Lösung. Zero-Filling ist Pflicht: ein Channel ohne Emotes darf nicht aus der Liste fallen — das ist der Fall, in dem der Admin ihn am dringendsten sehen muss.

**Nebenbei entstanden: `core/channels/channel-name.ts`** mit `normalizeChannelName` + `channelNameValidator` (Regel 9). Das Admin-Join-Formular validiert den **normalisierten** Wert gegen ein rein kleingeschriebenes Muster — exakt das, was `ChannelNameValidation.IsValid` serverseitig tut. Die Overview-Sektion behält vorerst ihre eigene Prüfung (sie ist in diesem Slice bewusst unangetastet), kann den Helper aber übernehmen. Keine neuen `ApiErrorCodes`; Fehler laufen über den `errors.status.*`-Fallback, keine Rate-Limit-Policy (Präzedenz der Gruppe).

### 2026-07-31 — Health-Split (Review Z1): öffentlicher `/api/worker/health` bleibt minimal, neuer `/api/admin/health` liefert den vollen Snapshot

**Betrifft:** `src/EmotePurge.Api/Endpoints/AdminEndpoints.cs` · `src/EmotePurge.Api/Endpoints/WorkerHealthStatus.cs` · `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs` · `src/EmotePurge.Core/Services/IWorkerHealthReader.cs` · `src/EmotePurge.Worker/WorkerStats.cs` · `src/EmotePurge.Worker/WorkerHealthPublisher.cs` · `src/EmotePurge.Worker/UsageFlushWorker.cs` · `src/EmotePurge.Worker/EmoteUsageCounter.cs` · `web/src/app/core/admin/` · `web/src/app/features/admin/admin-monitoring-page.ts`

**Monitoring-Slice des Admin-Bereichs — und die Umsetzung des Z1-Befunds aus dem Review vom 2026-07-29.** `GET /api/worker/health` ist **unauthentifiziert** und wird von jedem Besucher alle 30 Sekunden für das Status-Badge im Header gepollt; genau deshalb bleibt seine Antwort **wortgleich unverändert** minimal (Status, `isConnected`, letzte Chat-Nachricht, 7TV-Status/-Dispatch). Betriebsdetails — wie viele Subscriptions das 7TV-Socket hält, wie viele davon unbestätigt sind, wie oft der Batch-Flush hintereinander gescheitert ist — sind operative Innenansicht und gehören nicht in eine öffentliche Antwort. Sie laufen daher über den neuen, global-admin-only `GET /api/admin/health` als erstem Endpoint der `/api/admin`-Gruppe (`RequireAuthorization()` + `GlobalAdminAuthorizationFilter` auf **Gruppenebene**, damit ein künftiger Endpoint die Absicherung nicht vergessen kann; keine Rate-Limit-Policy, Präzedenz der bestehenden Admin-Endpoints).

**Beide Endpoints leiten ihre Status aus derselben Stelle ab.** Die Ableitungslogik samt der beiden Schwellwerte (300 s Twitch, 150 s 7TV) lag vorher als Literale in `WorkerHealthEndpoints.cs` und ist nach `WorkerHealthStatus.Derive(snapshot, nowUtc)` gezogen worden — zwei Sichten auf denselben Snapshot dürfen nicht auseinanderlaufen und unterschiedliche Zustände melden. Dort wohnt auch `SevenTvSubscriptionLimit = 500` (7TVs dokumentiertes `subscription_limit` pro Verbindung), das an die Admin-Antwort mitgeliefert wird, damit der Auslastungsbalken seinen Nenner nicht ein zweites Mal in TypeScript hartkodiert.

**Die Metriken entstehen erst im Worker.** Neu ist `WorkerStats` — eine konkrete Singleton-Klasse ohne Core-Interface, wie `SevenTvSubscriptionRegistry`: worker-intern, kein externer Zugriff, und wie `ReconnectPolicy` **uhrfrei** (der Aufrufer reicht `nowUtc` herein), damit sie containerfrei deterministisch testbar bleibt. `UsageFlushWorker` führt sein `_consecutiveFailures` nicht mehr privat, sondern meldet dorthin; das Requeue-/Drop-Verhalten nach fünf Fehlversuchen ist unverändert. `IEmoteUsageCounter` bekommt `PendingEmoteCount` — bewusst die Zahl **verschiedener** gepufferter Emotes, nicht die Summe ihrer Treffer: Letzteres hieße, bei jedem Health-Publish das ganze Dictionary zu durchlaufen, und als Gesundheitssignal sagen beide dasselbe (eine über mehrere Publishes wachsende Zahl heißt „der Flush räumt nicht ab").

**Die Snapshot-Erweiterung ist rückwärtskompatibel.** Die sieben neuen Felder in `WorkerHealthSnapshot` sind `int?`/`DateTime?` mit Default `null` — ein während eines Rolling Deploys von einem älteren Worker geschriebener Payload deserialisiert weiterhin, und zwar als *unbekannt* statt als „null Subscriptions, null Fehler". Das ist der Grund für `null` statt `0`: eine 0 wäre auf der Monitoring-Seite von einem gesunden Leerlauf nicht zu unterscheiden. Aus demselben Grund antwortet `/api/admin/health` bei fehlendem Redis-Key (Worker tot oder Publisher verklemmt) nicht mit einem kürzeren Fehlerkörper, sondern mit **derselben Form** und `snapshotAvailable: false` — die Seite rendert ein Layout statt auf einen zweiten Vertrag zu verzweigen. Neue `ApiErrorCodes` gibt es nicht (Befund S3-29 wird nicht verschärft); Health-Daten sind reine Datenfelder.

### 2026-07-31 — `/api/auth/me` liefert `isGlobalAdmin`; eigener `/admin`-Bereich statt 403-Probing

**Betrifft:** `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs` · `web/src/app/core/auth/auth.model.ts` · `web/src/app/core/auth/admin.guard.ts` · `web/src/app/app.routes.ts` · `web/src/app/features/admin/` · `web/src/app/features/shell/app-shell.ts`

**Fundament-Slice des Admin-Bereichs.** `GET /api/auth/me` trägt jetzt zusätzlich `isGlobalAdmin` — abgeleitet per `IChannelAccessService.IsGlobalAdmin(principal)` aus der Config-Allowlist `Auth:AdminTwitchLogins`, also ein synchroner Lookup ohne DB- oder HTTP-Berührung, der pro Request nichts kostet. Die Erweiterung ist rein additiv: ältere Clients ignorieren das Feld. Damit entfällt das bisherige Muster der Overview-Sektion, den Admin-Status durch einen Probe-Aufruf auf einen Admin-Endpoint zu erraten und dessen **403 als Berechtigungsbit** zu lesen — ein Request weniger, und eine Fehlerantwort ist wieder ein Fehler statt ein Steuersignal. Der neue Frontend-Bereich `/admin` (Shell-Child, lazy `AdminLayout` mit den Tabs `monitoring`/`channels`/`audit-log`) hängt an einem `adminGuard`, der ausschließlich das gecachte `/me` liest. Der Guard leitet Nicht-Admins auf `/` statt auf `/login` und stasht **keine** Return-URL: Wer hier abprallt, dem fehlt nicht die Session, sondern die Rolle — ein Login-Umweg würde ihn nur zurück in dieselbe Sperre schicken. Der Nav-Eintrag „Admin" in beiden Shell-Branches (Desktop + Mobile-Disclosure) ist entsprechend reine **Sichtbarkeits**-Steuerung; autoritativ bleibt serverseitig der `GlobalAdminAuthorizationFilter` auf jedem Admin-Endpoint. Die drei Tab-Seiten sind in diesem Slice bewusst Stubs (`EmptyState` + Platzhaltertext) und werden von den Folge-Slices Monitoring, Channel-Verwaltung und Audit-Log ersetzt; ihre i18n-Namespaces (`admin.monitoring`, `admin.channels`, `admin.audit`) sind schon jetzt so geschnitten, dass die Folge-Slices darauf aufbauen.

### 2026-07-31 — Auth-Cookie persistent (`IsPersistent = true`): Sessions überleben den Browser-Neustart

**Betrifft:** `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs`

**Live-Befund 2026-07-31: erzwungener Re-Login am Morgen, obwohl der Twitch-Refresh-Token-Flow seit dem 2026-07-30 läuft.** Untersuchung ergab: weder der Refresh-Flow (kein einziger seiner Fehlerpfade invalidiert die Cookie-Session — `invalid_grant` löscht nur die Token-Spalten und setzt `ReauthRequired`) noch der Data-Protection-Keyring (Volume `dataprotection-keys` + `DataProtection__KeyPath=/keys` in Prod verifiziert) waren die Ursache. Tatsächlich wurde `SignInAsync` ohne `AuthenticationProperties` aufgerufen — damit sendet ASP.NET Core das Auth-Cookie **ohne `Expires`/`Max-Age`** als reines Browser-Session-Cookie, und die konfigurierten 14 Tage `ExpireTimeSpan` + `SlidingExpiration` galten nur für das *Ticket im Cookie*, nie für dessen Ablage im Browser: Browser zu, Session weg. Jetzt wird mit `IsPersistent = true` angemeldet; das Cookie trägt die 14-Tage-Lebensdauer und überlebt den Browser-Neustart. Der Mechanismus zum serverseitigen Beenden von Sessions bleibt unverändert die `SessionsValidFromUtc`-Revocation aus dem Session-Hardening (Logout wirkt weiterhin global pro Nutzer). Live verifiziert: Login → Browser komplett schließen → wieder öffnen → Session besteht.

### 2026-07-31 — Visual Refresh „Landing-Qualität in die App": `.app-card`(-interactive), Marken-Glow, Typo-Hierarchie, Skeletons statt Lade-Texte

**Betrifft:** `web/src/styles.css` · `web/src/app/shared/ui/skeleton-rows.ts` · `web/src/app/shared/ui/empty-state.ts` · `web/src/app/features/shell/app-shell.ts` · `web/src/app/features/login/login-page.ts` · alle Feature-Seiten

**Welle 3 des UI/UX-Umbaus (Nutzer-Entscheidung „Landing-Qualität in die App", 2026-07-30).** Vier Konventionen: **(1) `.app-card`** in `styles.css` ist die einzige Karten-Oberfläche der App (feiner `slate-800`-Rand + `slate-900`-Fläche, `radius-lg`) — vorher waren App-Karten randlose Rechtecke, die Landing-Karten hatten Ränder, das war die halbe „zwei Design-Sprachen"-Diagnose (D1/D2). **`.app-card-interactive`** ergänzt den Landing-Hover (lila Rand + weicher Glow-Schatten) und gehört **nur** auf tatsächlich klickbare Karten (Stretched-Link-Zeilen) — statische Sektionen bleiben beim Basisstil, damit Hover nie einen Klick verspricht, den es nicht gibt. **(2) Marken-Glow:** Shell, Login **und** Landing teilen denselben dezenten Radial-Gradient oben (identische Werte), ausgeblendet über eine `mask-image`-Verlaufsmaske — ohne Maske reißt der Gradient an der Containerkante sichtbar ab (zweimal live gefunden: Shell-Erstwurf, alter Landing-Hero mit `overflow-hidden`-Beschnitt). Der Hintergrund liegt zudem auf dem **`body`** (plus `color-scheme: dark`): absolut positionierte Popups (DateTimePicker) verlängern den scrollbaren Bereich übers Seiten-`div` hinaus, und der Überstand war sonst browserweiß. **(3) Typo-Hierarchie:** Seitentitel `text-2xl font-bold tracking-tight`, Sektions-Titel `text-lg font-semibold` — vorher war alles `text-lg font-medium`. `EmptyState` bekommt ein optionales Emoji-Icon im lila Quadrat (Landing-Feature-Karten-Stil). **(4) Skeletons statt Lade-Texte** (NN/g: Skeleton für Seitenladevorgänge, Spinner nur für isolierte Aktionen): `.app-skeleton` (eigene Keyframes, bewusst unabhängig von Tailwinds `animate-pulse`-Emission) + `SkeletonRows`-Komponente für Listen; die Grids zeigen Zellen in Emote-Karten-Form. Das behebt nebenbei das Aufblitzen der Empty-States während `rxResource`-Loads (defaultValue griff vor der Antwort). A11y-Muster: ein `role="status"`-Element mit übersetztem Label, die Schimmer-Blöcke `aria-hidden`.

### 2026-07-31 — Router scrollt: `anchorScrolling` + `scrollPositionRestoration` + `onSameUrlNavigation: 'reload'`; keine nackten `href="#…"`-Anker

**Betrifft:** `web/src/app/app.config.ts` · `web/src/app/features/landing/landing-page.html` · `web/src/app/features/landing/landing-page.ts`

**Live-Befund 2026-07-31: die Landing-Anker („Features anzeigen", Header-/Footer-Nav) taten nichts.** Ursache ist die Base-Href-Falle: ein nacktes `href="#features"` löst der Browser gegen `<base href="/">` auf — also `/#features`, eine **volle Seitennavigation** weg von `/welcome`; dort greift `homeGuard` und leitet zurück, Fragment weg, kein Scroll. Kaputt vermutlich seit die Landing von `/` nach `/welcome` umzog. Konvention ab jetzt: **In-Page-Anker laufen über `routerLink` + `fragment`**, nie über nackte Hash-Hrefs; der Router scrollt selbst via `withInMemoryScrolling({ anchorScrolling: 'enabled', scrollPositionRestoration: 'enabled' })`. `scrollPositionRestoration` ändert dabei bewusst auch das App-Verhalten: Routenwechsel starten oben statt auf der alten Scroll-Position (Back/Forward stellt die Position wieder her). `onSameUrlNavigation: 'reload'` gehört dazu, weil `anchorScrolling` nur auf *abgeschlossene* Navigationen reagiert — ein zweiter Klick auf denselben Anker wäre sonst eine ignorierte Same-URL-Navigation und würde nicht mehr scrollen. Nebenwirkung: Same-URL-Navigationen re-runnen ihre Guards; alle Guards sind idempotent und gecacht (`ensureLoaded`), daher unkritisch. Verifiziert per DOM-Probe gegen den Container (Scroll-Positionen, kein Reload-Marker-Verlust).

### 2026-07-30 — Alle Dialoge auf `@angular/cdk/dialog` (S3-16); `window.confirm` abgeschafft; Spezifitäts-Falle der CDK-Laufzeit-Styles

**Betrifft:** `web/src/app/shared/ui/confirm-dialog.ts` · `web/src/app/shared/seven-tv/delete-confirm-dialog.ts` · `web/src/app/shared/seven-tv/seven-tv-token-prompt-dialog.ts` · `web/src/app/shared/seven-tv/mass-delete-panel.ts` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts` · `web/src/app/features/voting/vote-session-list-page.ts` · `web/src/styles.css`

**Review-Rest S3-16 (2026-07-29), umgesetzt in Welle 2 des UI-Umbaus.** Der handgebaute Mass-Delete-Bestätigungsdialog (eigenes Fixed-Overlay, `A11yModule`-Fokus-Trap, manuelles Escape) läuft jetzt über `Dialog.open()` aus `@angular/cdk/dialog` — Fokus-Trap, Escape, Backdrop-Klick, `aria-modal` und Fokus-Rückgabe kommen nativ; kein neues Package (cdk war vorhanden). Dasselbe Muster ersetzt die zwei verbliebenen `window.confirm`-Aufrufe (Channel verlassen, Session löschen — Nutzerbefund: gebrochener Stilbruch) durch den generischen **`ConfirmDialog`** unter `shared/ui/` (`DIALOG_DATA` mit `message`/`confirmLabel`, `DialogRef<boolean>`); der 7TV-Token-Prompt ist ein eigener Dialog, der sich per `effect()` selbst schließt, sobald der Token gespeichert ist. Der Mass-Delete-Flow ist dadurch imperativ statt Template-`@if`-Kaskade: Confirm → ggf. Token-Prompt → Confirm, mit **live durchgereichten Signals** in den Dialog-Daten (der Dialog liest z. B. die Auswahlgröße weiter reaktiv). **Zwei Fallen, beide live gefunden:** (1) Der CDK-Overlay-Container hängt **außerhalb** der App-Shell-DOM-Hierarchie — er erbt deren Textfarbe nicht; Dialog-Panel und `.app-input` brauchen explizites `color`. (2) Der CDK injiziert seine strukturellen Overlay-Styles zur **Laufzeit als `<style>`-Tag hinter allen Bundle-Stylesheets** — dessen `.cdk-overlay-pane { max-width: 100% }` schlägt jede gleich-spezifische eigene Regel unabhängig von Quellreihenfolge und Layern. Panel-Chrome muss deshalb unlayered und mit erhöhter Spezifität (`.cdk-overlay-pane.app-dialog-panel`) definiert werden; per DOM-Probe verifiziert (448 px statt Vollbreite). Konvention ab jetzt: **jeder** neue Dialog läuft über `Dialog.open(..., { backdropClass: 'app-dialog-backdrop', panelClass: 'app-dialog-panel' })` — kein `window.confirm`, kein handgebautes Overlay.

### 2026-07-30 — `shared/ui`-Primitives (Button, StatusBadge, NoticeBanner, EmptyState, `.app-input`) ersetzen die kopierten Utility-Ketten

**Betrifft:** `web/src/app/shared/ui/button.ts` · `web/src/app/shared/ui/status-badge.ts` · `web/src/app/shared/ui/notice-banner.ts` · `web/src/app/shared/ui/empty-state.ts` · `web/src/styles.css` · `web/src/app/shared/emotes/emote-usage-filter.ts` · alle Feature-Seiten

**Design-System-Konsolidierung aus Welle 2 des UI/UX-Audits vom 2026-07-30.** Vorher existierte jeder Button, jedes Badge, jede Fehlerbox und jeder Leerzustand als frei kopierte Tailwind-Klassenkette (Button-Kette 8×, mit driftenden Varianten — Befunde K5/K6/D3/D6/D7). Jetzt gilt: **(1) `appButton`-Direktive** (Attribut-Direktive statt Wrapper-Komponente, damit `<button>`/`<a routerLink>` selbst das Host-Element bleiben — kein verschachteltes Fokus-/Klick-Ziel) mit den Varianten `primary`/`neutral`/`outline`/`danger`/`danger-solid` und `buttonSize` `md`(Default)/`lg`. Bewusst **zwei** Destruktiv-Stufen: `danger` (Outline, z. B. „Channel verlassen"/Session-Löschen — destruktiv, aber bestätigt/umkehrbar) und `danger-solid` (gefüllt, ausschließlich der 7TV-Mass-Delete als endgültigste Aktion der App). **(2) `StatusBadge`** mit festen Tönen (`purple`/`blue`/`emerald`/`slate`/`amber`/`red`) für Rollen, Bot-, Session- und Worker-Status — Bedeutung liegt beim Aufrufer, der Baustein kennt nur Töne. **(3) `NoticeBanner`** (`info`/`warning`/`error`; `role="alert"` nur bei `error`, die anderen bleiben still) mit `[notice-action]`-Slot — ersetzt Reauth-Karte, Helix-down-Fließtext und alle Ad-hoc-Fehlerboxen; die Overview zeigt Worker-down jetzt zusätzlich zum Header-Dot als Banner (abgeleitet aus dem vorhandenen `WorkerHealthService`-Signal). **(4) `EmptyState`** (Titel + optionale Erklärung + CTA-Slot): jeder Leerzustand sagt, *warum* leer und was als Nächstes zu tun ist; der Filter-Leerzustand der Emote-Grids bekommt dafür `EmoteUsageFilter.reset()`/`isAnyActive()` (+ neue Spec, Regel 12). **(5) `.app-input`/`.app-input-sm`** in `styles.css` als einziger Input-Stil — mit explizitem `color`, weil die Klasse auch im CDK-Overlay-Container außerhalb der App-Shell-DOM-Hierarchie verwendet wird (s. Dialog-Eintrag). Komponenten/Direktive bewusst ohne isolierte Specs (Regel 12: Komponenten live verifizieren, Utilities testen — `EmoteUsageFilter` ist der Utility-Fall). Der Toggle „Nur ungenutzte" behält ein **konstantes Label** mit `aria-pressed` + Füllung statt Label-Tausch („Alle anzeigen") — der Labelwechsel änderte die Buttonbreite und ließ die Toolbar bei jedem Klick umbrechen; die `showAll`-Keys sind aus beiden Locales entfernt.

### 2026-07-30 — Rate-Limit-Policy `ExternalApi` von 20/min auf 40/min pro Nutzer angehoben

**Betrifft:** `src/EmotePurge.Api/Program.cs`

**Live-Befund beim Durchklicken der neuen UI (2026-07-30):** Normale Navigation produzierte 429er auf `/api/channels/mine`. Ursache ist kein Bug, sondern Arithmetik: Die `ExternalApi`-Policy (20/min pro Nutzer, Fixed Window) deckt *mehrere* Endpunkte, und ein gewöhnlicher Seitenwechsel verbrennt gleich mehrere Permits — jede Rückkehr zur Übersicht 1× `/channels/mine`, jeder Workspace-Einstieg zusätzlich `/permissions` + Usage-Stats. Nach ~7 Seitenwechseln in einer Minute war das Budget leer. **Anhebung auf 40/min**, nicht mehr: Der Schutzzweck der Policy (ein loopender Account darf nicht die app-weite Helix-Quota von ~800 Punkten/min leeren, s. Ursprungs-Eintrag der Policy) bleibt gewahrt — selbst der Worst Case (40 × `/mine` mit bis zu 10 Helix-Calls) bleibt unterhalb der App-Quota, real liegen die Werte weit darunter. Fenster-Mechanik (Fixed Window, QueueLimit 0) unverändert: Queuing würde Requests bis zum Fensterende festhalten und die UI einfrieren lassen statt sauber zu fehlern. **Notiert als nachhaltigere Alternative, falls das Limit wieder drückt:** ein kurzer serverseitiger Cache (30–60 s) für `/channels/mine` — der eigentliche Kostentreiber sind dessen bis zu 10 ungecachte Helix-Calls pro Aufruf, nicht die Request-Zahl.

### 2026-07-30 — Listen-Karten vollflächig klickbar über das Stretched-Link-Pattern (`.app-card-link`)

**Betrifft:** `web/src/styles.css` · `web/src/app/features/voting/vote-session-list-page.html` · `web/src/app/features/my-votings/my-votings-page.ts` · `web/src/app/features/overview/overview-page.html` · `web/e2e/channel-workspace.e2e.spec.ts`

**Befund des UI/UX-Audits vom 2026-07-30:** Vote-Session-, My-Votings- und Channel-Karten sahen wie klickbare Karten aus, reagierten aber nur auf einen ~24 px hohen Titel-Link bzw. einen kleinen „Öffnen"-Button — inkonsistent zu den vollflächig klickbaren Emote-Karten. Jetzt gilt für alle Listen-Karten das **Stretched-Link-Pattern** (Heydon Pickerings „Inclusive Components: Cards", auch Bootstrap `.stretched-link`): Der Titel- bzw. „Öffnen"-Link bleibt der einzige echte Link (Screenreader hören weiter nur den kurzen Namen), ein `::after`-Pseudoelement der neuen globalen Utility-Klasse `.app-card-link` dehnt seine Klickfläche über die ganze Karte. **Vertrag der Klasse:** Kartencontainer ist `relative`, jede Sekundäraktion in der Karte (Link kopieren, Beenden, Löschen, Bot reaktivieren) liegt per `relative z-10` über dem Overlay und bleibt separat klick- und fokussierbar. Bewusst **nicht** gewählt: die ganze Karte in ein `<a>` wrappen (ungültig bei verschachtelten Buttons, unbrauchbar langer Accessible Name) oder ein JS-Klick-Handler auf der Karte (müsste Textauswahl erkennen, hilft Tastaturnutzern nicht). Karten zeigen Hover-Feedback (`hover:bg-slate-800/70`), damit die Klickbarkeit sichtbar ist; auf der Overview ist „Öffnen" dafür vom `<button (click)>` zum `<a routerLink>` geworden (e2e-Selektor entsprechend `getByRole('link')`). Die Sekundäraktionen der Vote-Session-Karten sind zugleich von 20-px-Textlinks auf echte Buttons ≥ 32 px angehoben, „Löschen" rechts abgesetzt.

### 2026-07-30 — `SegmentedControl` als erster `shared/ui/`-Baustein; Filterwechsel beschneiden die Auswahl statt sie zu löschen (S2-16)

**Betrifft:** `web/src/app/shared/ui/segmented-control.ts` · `web/src/app/shared/selection/list-selection.ts` · `web/src/app/features/usage-stats/usage-stats-page.ts` · `web/src/app/features/usage-stats/usage-stats-page.html` · `web/src/app/features/voting/vote-session-detail-page.ts`

**Zwei Entscheidungen aus dem UI/UX-Audit vom 2026-07-30.** Erstens: Die drei Zeitraum-Presets der Usage-Stats-Seite („Heute/7 Tage/30 Tage") zeigten nie einen Aktiv-Zustand — direkt neben dem „Nur ungenutzte"-Toggle, der einen hat. Sie sind jetzt ein **Segmented Control** (wenige, sich gegenseitig ausschließende Optionen ⇒ das passende Pattern laut Audit-Referenzen; Chips wären Multi-Select-Semantik, ein Dropdown versteckt die Alternativen). Neues viertes Segment „Eigener": erst dessen Wahl blendet die beiden Datumsfelder ein — der aktive Zustand ist ein eigenes `rangePreset`-Signal, nicht aus `from`/`to` zurückgeraten. Die Komponente liegt als **erster Baustein unter `web/src/app/shared/ui/`** (dem künftigen Ort für generische Design-System-Primitives, Abgrenzung zu den funktionsgebundenen `shared/`-Bausteinen): generische `options`/`model()`-API, `radiogroup`-Semantik mit Roving-Tabindex und Pfeiltasten-Navigation. Als Komponente bewusst ohne isolierten Spec (Konvention aus Regel 12: Komponenten werden live verifiziert, Utilities getestet).

Zweitens **S2-16** (Review-Rest aus 2026-07-29): `EmoteUsageFilter` rief bei jeder Filteränderung `selection.clear()` — die mühsam zusammengeklickte Emote-Auswahl war bei jedem Tippen im Namensfilter weg. Die seit Welle B keyed `ListSelection` macht die sichere Variante möglich: neues `retainVisible()` behält, was sichtbar bleibt, und wirft nur die weggefilterten Keys raus — denn `selectedKeys` ist die autoritative Menge für den Delete-Pfad, ein unsichtbar-ausgewähltes Emote dürfte dort nie landen (deshalb *kein* simples „Auswahl komplett behalten"). Der Anker der Shift-Klick-Range wird mit zurückgesetzt, wenn die verankerte Zeile wegfällt. Gilt auf beiden Grid-Seiten (Usage-Stats + Voting-Detail); das `clear()` bei Reload/Zeitraumwechsel in `loadTotals` bleibt bewusst bestehen (dort ändern sich die Zahlen unter der Auswahl, s. Kommentar dort). Zwei neue Unit-Tests in `list-selection.spec.ts`.

### 2026-07-30 — Frontend-Konvention: globaler `:focus-visible`-Ring, kein `focus:outline-none` ohne Ersatz, Touch-Targets ≥ 24 px

**Betrifft:** `web/src/styles.css` · `web/src/app/shared/emotes/emote-card-header.ts` · `web/src/app/shared/seven-tv/seven-tv-token-input.ts` · `web/src/app/features/landing/landing-page.html`

**Befund des UI/UX-Audits vom 2026-07-30:** Im gesamten Frontend existierte kein einziger eigener Fokus-Stil, und drei Inputs entfernten den nativen Fokusring per `focus:outline-none` ersatzlos — Tastaturnutzer hatten dort gar keinen Fokusindikator (WCAG 2.4.7). Jetzt gilt: **ein** globaler `:focus-visible`-Stil in `styles.css` (lila 2-px-Ring mit Offset, erfüllt 1.4.11) für alle interaktiven Elemente; Komponenten dürfen ihn nicht ohne gleichwertigen Ersatz unterdrücken — die drei `focus:outline-none` sind entfernt, der `focus:border-purple-500`-Akzent der Inputs bleibt zusätzlich. Zweitens **Touch-Target-Untergrenze 24 × 24 px** (WCAG 2.2 SC 2.5.8, AA) für alles Interaktive; 44 px bleiben der Komfort-Zielwert für primäre Aktionen. Die Emote-Checkbox in `EmoteCardHeader` bleibt bewusst 16 px: die vollflächig klickbare Karte ist das eigentliche ≥ 24-px-Ziel, womit die „equivalent target"-Ausnahme von 2.5.8 greift — als solche im Template dokumentiert. Messbar gehalten wird beides über die Metriken des UI-Audit-Harness (s. eigenen Eintrag).

### 2026-07-30 — UI-Audit-Screenshot-Harness als dauerhaftes Repo-Werkzeug (getrennt von der e2e-Suite)

**Betrifft:** `web/playwright.audit.config.ts` · `web/e2e/audit/ui-audit.audit.ts` · `web/.gitignore`

**Entstanden im UI/UX-Audit vom 2026-07-30, bewusst behalten statt weggeworfen.** Das Harness rendert 20 UI-Zustände (alle Routen inkl. Leer-, Fehler-, Reauth-, Admin-, Shared-Set-Varianten und absichtlich langer Namen) in 3 Viewports (360/768/1280) und beiden Sprachen gegen die vorhandenen `page.route`-Mocks — 100 Screenshots plus JSON-Metriken pro Zustand (horizontaler Overflow in px, interaktive Elemente < 24 px [WCAG 2.5.8] bzw. < 44 px, Elemente jenseits der rechten Viewport-Kante). Zweck: reproduzierbare Vorher/Nachher-Belege je Umbau-Welle und harte Regressions-Gates (Overflow = 0, keine neuen Sub-24-px-Targets). **Getrennt von der regulären e2e-Suite:** eigene Config mit `testMatch: '**/*.audit.ts'` — die Haupt-Config matcht nur `*.spec.ts`, CI und `npm run e2e` fahren das Audit also nie mit; Aufruf manuell per `npx playwright test --config=playwright.audit.config.ts`. Output liegt unter `web/.audit-out/` (gitignored). Die 7TV-CDN-Bilder werden mit einem 1×1-PNG gestubbt, damit der Lauf offline-deterministisch bleibt.

### 2026-07-30 — CI: Doku-only-Pushes überspringen die Pipeline, PRs bekommen Test-Jobs (kein Publish)

**Betrifft:** `.github/workflows/publish.yml`

**Trigger-Umbau des einzigen Workflows — 2026-07-30.** Vorher lief bei **jedem** Push auf `main` die volle Pipeline (Backend-Tests mit Testcontainers, Vitest + Playwright, Build + Push beider GHCR-Images) — auch für reine `docs:`-Commits. Das kostete ~2–2,5 min pro Push und verschob den `latest`-Tag auf ein bit-identisches Image mit neuem Digest, sodass ein blinder Portainer-Re-Pull die Container grundlos neu gestartet hätte. Jetzt: `paths-ignore` (`**.md`, `docs/**`) am `push`- **und** `pull_request`-Trigger — Doku-only-Pushes lösen nichts mehr aus; gemischte Pushes (Code + Doku) laufen unverändert voll. Zusätzlich, als Umsetzung von Review-Befund S3-37: `pull_request`-Trigger für die beiden Test-Jobs, damit optionale Branches für riskante Umbauten CI bekommen — der `publish`-Job ist per `if: github.event_name != 'pull_request'` ausgenommen, Images entstehen weiterhin ausschließlich aus `main` (Push/manuell). **Bewusst kein Wechsel auf einen PR-Pflicht-Workflow:** Trunk-based bleibt für Team-Größe 1 die richtige Wahl; die zentrale Schutzeigenschaft — kaputter Code kann keine Images publizieren, weil `publish` per `needs` an beiden Test-Jobs hängt — bestand schon vorher und bleibt unverändert.

### 2026-07-30 — Twitch-Refresh-Token eingeführt: DB-Speicherung (AES-GCM) + On-Demand-Refresh mit Single-Flight und Validate-on-Use

**Betrifft:** `src/EmotePurge.Core/Entities/User.cs` · `src/EmotePurge.Core/Services/ITokenCipher.cs` · `src/EmotePurge.Core/Services/IUserService.cs` · `src/EmotePurge.Core/Services/ITwitchUserTokenService.cs` · `src/EmotePurge.Core/Twitch/ITwitchAuthClient.cs` · `src/EmotePurge.Core/Twitch/TwitchModels.cs` · `src/EmotePurge.Infrastructure/Services/AesGcmTokenCipher.cs` · `src/EmotePurge.Infrastructure/Services/TwitchUserTokenService.cs` · `src/EmotePurge.Infrastructure/Services/TwitchTokenRefreshGate.cs` · `src/EmotePurge.Infrastructure/Services/UserService.cs` · `src/EmotePurge.Infrastructure/Twitch/TwitchAuthClient.cs` · `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs` · `docker-compose.yml` · `docker-compose.prod.yml` · `.env.example`

**Schließt die bekannte Grenze „Kein Twitch-Refresh-Token" (CLAUDE.md) — 2026-07-30, Umkehrung der expliziten Nutzerentscheidung vom 2026-07-25 („bewusst kein Refresh-Token gespeichert"), deren Eintrag unverändert bleibt.** Das beim Code-Exchange mitgelieferte `refresh_token` wurde bis dahin stillschweigend verworfen (`TwitchTokenResponseDto` deserialisierte es nicht); nach ~4 h Claim-Ablauf degradierten alle Helix-Live-Checks bis zum Re-Login. Grundlage der Mechanik-Aussagen ist die offizielle Doku (dev.twitch.tv/docs/authentication/refresh-tokens/ u. a.): Rotation ist bei confidential clients nicht zugesichert, aber „refresh tokens **may** change" ⇒ das zurückgelieferte Token wird nach jedem Refresh persistiert; confidential-Refresh-Tokens laufen nicht ab; ungültig werden sie durch Passwortwechsel/App-Disconnect/Twitch-Revoke, erkennbar als **HTTP 400** `"Invalid refresh token"` (endgültig ⇒ Re-Login) — alles andere gilt als transient.

**Speicherort DB, nicht Cookie-Claim:** Neue `Users`-Spalten (`TwitchRefreshToken`, `TwitchAccessToken`, `TwitchAccessTokenExpiresAtUtc`, `TwitchTokenScopes`; Migration `AddTwitchTokenColumns`). Gründe: (1) Rotation verlangt garantierte serverseitige Persistenz des *neuen* Refresh-Tokens — beim Cookie hinge sie am erfolgreichen Ausliefern der Response an genau den auslösenden Tab, parallele Tabs hätten divergierende Stände; (2) die Konsumenten (`ModeratorCheckService` etc.) liegen in Infrastructure und können kein Cookie re-issuen (`SignInAsync` ist Api-Schicht); (3) Single-Flight braucht einen shared store; (4) `OnValidatePrincipal` etabliert bereits einen DB-Lookup pro authentifiziertem Request. Zero-Knowledge (Architectur.md Grundsatz 4) betrifft laut Eintrag vom 2026-07-25 explizit nur 7TV-Tokens. **Verschlüsselung at rest:** beide Token-Spalten via `ITokenCipher`/`AesGcmTokenCipher` (AES-256-GCM, Schlüssel `Auth:Twitch:TokenEncryptionKey` aus user-secrets/.env, `AUTH_TWITCH_TOKEN_ENCRYPTION_KEY`) — ein `pg_dump`-Backup allein exponiert damit keine nutzbaren Credentials. Bewusst **nicht** Data Protection: dessen Registrierung ist Api-only und konditional, das Package wäre eine ASP.NET-Core-Referenz in Infrastructure, und ein eigener Schlüssel entkoppelt die DB-Tokens vom Cookie-Key-Ring. Schlüsselprüfung lazy beim ersten Gebrauch (nicht im Ctor): der Cipher hängt an `UserService`, das auch `OnValidatePrincipal` bedient — ein fehlender Schlüssel darf nur die Token-Pfade brechen, nicht jeden authentifizierten Request.

**Auslösung lazy On-Demand, kein Background-Job:** `ITwitchUserTokenService.GetValidAccessTokenAsync(principal)` ersetzt in den vier Konsumenten (`ModeratorCheckService`, `MyChannelsService`, `VoteEligibilityService`, `EmoteSetOwnershipService`) den direkten `TwitchPrincipalInfo.AccessToken`-Zugriff. Reihenfolge: Claim-Token solange gültig (Fast-Path ohne DB) → DB-Access-Token (60 s Sicherheitsabstand) → Single-Flight-Refresh. Der Lock ist ein per-User-`SemaphoreSlim` im Singleton `TwitchTokenRefreshGate` (Muster `ChannelSyncGate`), mit Double-Check (DB-Re-Read im Lock) — Twitch empfiehlt explizit „refresh in one thread". **Validate-on-Use:** vor Herausgabe eines Tokens, dessen letzte Prüfung >1 h zurückliegt, läuft `GET /oauth2/validate` (In-Memory-Zeitstempel pro User im Gate); das erfüllt Twitchs harte Stundenpflicht („must validate … hourly", mit angedrohtem API-Key-Entzug) für alle *benutzten* Tokens und entdeckt Revocation binnen 1 h statt erst am Ablauf. Cookie-Semantik unverändert: kein Re-Issue, `SessionIssuedAtUtc` unangetastet ⇒ die Logout-Revocation greift weiter; Logout löscht zusätzlich die Token-Spalten (Logout ist ohnehin global pro User). Scope-Drift (gespeicherte ≠ aktuell angeforderte Scopes, als Set verglichen) ⇒ `ReauthRequired`, denn neue Scopes gibt es nur über den Authorize-Flow.

**Fehlerpfad:** `InvalidGrant` (400) ⇒ Token-Spalten löschen, `TwitchUserTokenResult(null, ReauthRequired: true)` — die Konsumenten degradieren wie bisher still, `MyChannelsResultDto.ReauthRequired` macht den Zustand im Overview-Banner sichtbar (Re-Login-Button); transiente Fehler ⇒ Refresh-Token bleibt, einmalige Degradation, nächster Bedarf versucht erneut. Kein neuer `ApiErrorCodes`-Wert (kein neues Fehler-Response-Format). **Bewusste Grenzen:** Single-Flight ist in-process (korrekt bei genau einem Api-Container; Scale-out bräuchte einen verteilten Lock — Twitch toleriert bis 50 gültige Access-Tokens je Refresh-Token als Netz); kein Helix-401-Force-Refresh (hätte alle drei `ITwitchHelixClient`-Signaturen aufgebohrt, Validate-on-Use deckt den Fall binnen 1 h); idle gespeicherte Tokens werden nicht stündlich validiert (Doku lässt offen, ob nötig). Testbarkeits-Knob `Auth:Twitch:AccessTokenLifetimeOverrideSeconds` (nur wenn gesetzt) verkürzt die Token-Lebensdauer für Live-Tests des Refresh-Pfads; der echte Twitch-Token-Endpunkt bleibt bewusst ungemockt (Regel 16, Live-Verifikation).

### 2026-07-30 — 7TV-EventAPI-WebSocket wieder eingeführt: hybrider Sync (Live-Dispatch + REST-Reconciliation)

**Betrifft:** `src/EmotePurge.Worker/SevenTv/ISevenTvEventClient.cs` · `src/EmotePurge.Worker/SevenTv/SevenTvEventClient.cs` · `src/EmotePurge.Worker/SevenTv/SevenTvSubscriptionRegistry.cs` · `src/EmotePurge.Worker/SevenTv/SevenTvBackoffPolicy.cs` · `src/EmotePurge.Worker/SevenTvEventWorker.cs` · `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs` · `src/EmotePurge.Worker/Worker.cs` · `src/EmotePurge.Worker/WorkerHealthPublisher.cs` · `src/EmotePurge.Infrastructure/SevenTv/SevenTvDispatchParser.cs` · `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs` · `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs` · `src/EmotePurge.Core/SevenTv/SevenTvModels.cs` · `src/EmotePurge.Core/SevenTv/ISevenTvApiClient.cs` · `src/EmotePurge.Core/Services/ISevenTvSyncService.cs` · `src/EmotePurge.Core/Services/IWorkerHealthReader.cs` · `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs` · `docker-compose.yml` · `docker-compose.prod.yml` · `.env.example`

**7TV-EventAPI-WebSocket wieder eingeführt — 2026-07-30, als Umkehrung der Entfernung vom 2026-07-25 (s. dortigen Eintrag, der bewusst unverändert bleibt).** Grundlage ist die Re-Untersuchung [docs/Untersuchung-7TV-WebSocket-2026-07-30.md](Untersuchung-7TV-WebSocket-2026-07-30.md): Die damalige Begründung „Dispatches kommen nachweislich nicht zuverlässig an" ist **nicht mehr haltbar** — die 2026-07-Implementierung hatte zwei eigenständig fatale Bugs (Resubscribe lief in der Reconnect-Schleife *vor* dem Verbindungsaufbau gegen `_socket == null`; der Dispatch-Parser las `added`/`removed`, das echte Wire-Format ist `pushed`/`pulled`), und die nachgerüstete channel-scoped Subscription ist serverseitig ein Presence-Scope, der Channel-Set-Updates strukturell nie liefern konnte. Live-Proben am 2026-07-30 (Adds/Removes/Renames bis 442-Emote-Sets, zwei Set-Wechsel) belegen: `{object_id}`-Dispatches kommen unmittelbar an. **Motivation:** Latenz (bis 60 s Resync-Takt, real bis 10–30 min durch 7TVs nicht invalidierten REST-Cache, SevenTV/SevenTV#81) kostet Usage-Stats; der Zielfall HandOfBlood verschärft das.

**Architektur — WS zusätzlich, nie statt REST:** Der `SevenTvPeriodicResyncWorker` bleibt als Reconciliation zwingend bestehen (7TVs EventAPI hat kein Resume/Replay — der Server ackt op 34 hart mit `success:false` —, trennt jede Verbindung nach ~1 h TTL, und hat belegte Publish-Lücken). Sein Takt ist jetzt konfigurierbar (`SevenTv:ResyncIntervalSeconds`, Default 60; bewusst **nicht** automatisch ans Feature-Flag gekoppelt — Strecken auf z. B. 300 s ist ein manueller, beobachtbarer Schritt nach bewährtem Live-Betrieb). Der neue Pfad: `SevenTvEventWorker` (6. Hosted Service) treibt `SevenTvEventClient` — eine Verbindung, eine Session pro Schleifendurchlauf, awaited statt fire-and-forget (der `_ = RunConnectionLoopAsync(...)`-Fehler von damals). Subscriptions sind Desired-State-first in der `SevenTvSubscriptionRegistry` (S2-3-Muster: Absicht vor Versuch, Ack-Bestätigung separat, Reset bei jedem Hello); die Konvergenz Richtung Socket läuft **nach jedem Hello** über eine Send-Pump mit 100-ms-Frame-Abstand (Lehre aus dem Twitch-JOIN-Limit) und deduplicated je `(type, object_id)` — zwei Channels mit demselben Set (live belegt: `sensitron`/`olaf_olaf_son`) ergeben eine Subscription, Refcount beim Abbestellen inklusive. Pro Channel zwei Subscriptions: `emote_set.*` (`{object_id: <setId>}`) für In-Set-Änderungen, `user.*` (`{object_id: <7tvUserId>}`) für Set-Wechsel (Chatterino-Muster, Payload live verifiziert; die 7TV-User-ID liefert der bestehende `users/twitch/{id}`-Sync-Response gratis mit — `GetEmoteSetForTwitchUserAsync` wurde dafür zu `GetChannelStateForTwitchUserAsync`, `SyncChannelAsync` gibt `SevenTvSyncResult(EmoteSetId, SevenTvUserId)` zurück). Heartbeat-Watchdog: 3 × `heartbeat_interval` ohne Frame ⇒ Session-Ende; op 4/7 ⇒ proaktiver Reconnect; op 5/6 auf Info/Warning sichtbar (damals Debug und damit unsichtbar); Close 4009 bekommt eine 60-s-Backoff-Untergrenze. Reconnect-Pacing über die eigene, pure `SevenTvBackoffPolicy` (2 s → ×2 → Cap 60 s, ±20 % Jitter, **kein** Versuchslimit — S2-1-Lehre) statt einer Parametrisierung von `ReconnectPolicy`: die modelliert Reconnect-vs-Recreate-vs-Wait eines langlebigen TwitchLib-Objekts, ein `ClientWebSocket` ist single-use; zentrale Zusicherung mit eigenem Test: der stündliche TTL-Reconnect (`ServerRequestedReconnect`) baut **keinen** Backoff auf (fix 1 s). Nach jedem Reconnect außer der ersten Session des Prozesses: Gap-Filling-Vollsync aller gewünschten Channels (verpasste Dispatches sind unwiederbringlich).

**Dispatch-Verarbeitung:** streng sequenziell im Receive-Loop (S2-8, kein `_ = Task.Run`). Deltas werden **direkt angewendet** statt nur einen Vollsync zu triggern — Grund ist ausgerechnet Issue #81: der REST-Cache kann das gerade hinzugefügte Emote noch gar nicht kennen, der Dispatch trägt die Emote-Daten dagegen im Payload. Neuer Parser `SevenTvDispatchParser` liegt in Infrastructure (die Wire-DTOs/`SevenTvEmoteJsonMapper` sind dort `internal`; ein Worker-Parser hätte eine zweite Emote-JSON-Abbildung gebraucht), liest `pushed[].value` / `pulled[].old_value.id` / `updated[].value` mit `key=="emotes"`-Filter und loggt unbekannte Formen auf **Warning** (damals Debug — unsichtbar). Die Anwendung `ISevenTvSyncService.ApplyEmoteSetUpdateAsync(channelName, emoteSetId, delta)` läuft **pro Channel** (nicht pro Set — die alte `SingleOrDefault(ActiveEmoteSetId == …)`-Signatur wäre am geteilten Set gescheitert) unter demselben `ChannelSyncGate` wie der Vollsync, verweigert Alt-Set-Dispatches (`SetNotActive`), hat ein S3-12-Pendant (ein Delta, das einen nicht-leeren Bestand auf 0 brächte ⇒ `ImplausibleSkipped`, Aufrufer macht Vollsync — **außerhalb** der Gate-Klammer, das Gate ist nicht reentrant) und endet im unveränderten `RefreshMatchCacheAsync`-Voll-Reload (kein inkrementelles Cache-Patchen). Leere `ImageUrl` aus einem Dispatch überschreibt nie eine bekannte. Beim `user.update`-Set-Wechsel wird die Event-Set-ID sofort abonniert (Event schlägt REST-Cache) und der Inhalt per Vollsync geholt. `ReconcileAsync` loggt als reine Messung (keine Schutzregel), wenn der REST-Resync ein <15 min altes Emote archiviert — der bekannte Flapping-Fall aus #81 wird damit quantifizierbar, bevor eine Schutzregel die Reconciliation schwächt.

**Rollout & Health:** Feature-Flag `SevenTv:EventApi:Enabled` (Default `false`; `appsettings.Development.json` `true`; Compose beider Umgebungen via `SEVENTV_EVENTAPI_ENABLED` — Rollback ist ein Env-Wechsel ohne Rebuild). `WorkerHealthSnapshot` (ein Vertrag, S4-11) trägt fünf neue Felder mit Defaults (rollende Deploys deserialisieren Alt-Payloads weiter); `GET /api/worker/health` liefert ein `sevenTv`-Unterobjekt mit Status `disabled/disconnected/stale/connected` — Staleness hängt an **Frames** (Heartbeats, ~45 s), nie an Dispatches (ein Channel ohne Set-Änderungen wäre sonst dauerhaft „stale"), Root-`status` bleibt unverändert Twitch (Frontend unberührt). **Bewusst ungetestet** bleibt der Transport selbst (Client/Session-Loop): ein Fake-WS-Server testete nur eigene Annahmen statt des Server-Vertrags — exakt die Verwechslung hinter der TwitchLib-Episode (S2-1); die entscheidungstragende Logik ist dafür in die drei puren, getesteten Klassen (`SevenTvSubscriptionRegistry`, `SevenTvBackoffPolicy`, Parser) gezogen, Live-Verifikation nach Regel 16 inkl. TTL-Reconnect-Durchlauf. **Bekannte Grenzen** (Untersuchung §7/§8): Zustellqualität bei ~900er-Sets ungemessen (Worst Case = heutiger Zustand, REST läuft weiter), kein Connection-Sharding (`subscription_limit=500` ⇒ nötig ab ~250 Channels, bis dahin nur eine 90-%-Warnung), 7TV-Account-Wechsel eines Channels fängt nur der periodische Resync.

### 2026-07-30 — Worker-Testprojekt `tests/EmotePurge.Worker.Tests` eingeführt (container-frei)

**Betrifft:** `tests/EmotePurge.Worker.Tests/EmotePurge.Worker.Tests.csproj` · `EmotePurge.slnx` · `tests/EmotePurge.Worker.Tests/ReconnectPolicyTests.cs` · `tests/EmotePurge.Worker.Tests/SevenTvSubscriptionRegistryTests.cs` · `tests/EmotePurge.Worker.Tests/SevenTvBackoffPolicyTests.cs`

**Neues Testprojekt für die puren Entscheidungs-/Zustandsklassen der Worker-Transporte — 2026-07-30.** Auslöser war die WS-Wiedereinführung (s. Eintrag darüber): `SevenTvSubscriptionRegistry` und `SevenTvBackoffPolicy` sind Verbindungs-/Subscription-Zustand eines Worker-Transports; sie nach Infrastructure zu verschieben, nur um sie im bestehenden Testprojekt testen zu können, hätte die Platzierungsentscheidung „Transport lebt im Worker wie `TwitchChatManager`" unterlaufen. Das Projekt ist bewusst **ohne** Testcontainers-Pakete (schnell, kein Docker nötig); alles mit Postgres/Redis-Berührung gehört weiterhin in `tests/EmotePurge.Infrastructure.Tests`. Im selben Zug erledigt: die offene Welle-D-Schuld **`ReconnectPolicy`-Unit-Tests** (Review 2026-07-29, S3-6 — „Die Tests dazu sind Welle D") mit den fünf dokumentierten Entscheidungsfällen. `dotnet test EmotePurge.slnx` und die CI greifen das Projekt ohne Workflow-Änderung automatisch mit. Regel 11 in `CLAUDE.md` ist entsprechend erweitert: neue reine Logik im Worker bekommt ihren Test hier.

### 2026-07-30 — Doku-Umbau: Entscheidungslog aus `CLAUDE.md` hierher, `Architectur.md` auf den Ist-Zustand

**Betrifft:** `CLAUDE.md` · `docs/DECISIONS.md` · `Architectur.md`

Befund S3-27 des Reviews vom 2026-07-29. `CLAUDE.md` war auf 96 KB / 190 Zeilen angewachsen, davon rund drei Viertel Änderungslog und ein einzelner 7,7-KB-Absatz „Umsetzungsstand" — und die eigene Regel „diese Liste ergänzen, nicht nur lesen" war nach `bcd6f65` dreimal nicht befolgt worden. Gleichzeitig widersprach `Architectur.md` dem Code an fünf Stellen; die Datei, auf die `CLAUDE.md` für Architektur-Fragen ausdrücklich zuerst verweist, behauptete unter anderem, `User`/`VoteSession`/`Vote` seien „noch nicht implementiert" (seit dem 2026-07-25 falsch) und die Voting-Routen seien „bewusst ohne Login-Zwang" (am 2026-07-27 umgekehrt). Falsche Doku ist teurer als fehlende.

**Umgesetzt:** Die 72 Log-Einträge sind **wortgleich** hierher verschoben (Überschrift und `**Betrifft:**`-Zeile neu, der Eintragstext unangetastet — verifiziert per Skript: jede der 72 Original-Zeilen kommt zeichengleich in dieser Datei vor). `CLAUDE.md` ist auf 14 KB geschrumpft: Überblick, Umsetzungsstand als Sechs-Zeilen-Tabelle, Commands, die Schichtentreue-Tabelle aus Anhang E des Reviews, und 17 *geltende Regeln* als imperative Liste, jede ohne die Historie dahinter. **Abweichung:** Der Report nannte ≤ 12 KB; 14 KB ist das Ergebnis, wenn man die Commands vollständig behält und die Regeln so ausschreibt, dass sie ohne Nachschlagen anwendbar sind. Weiter zu kürzen hätte Inhalt gekostet, nicht Redundanz.

Undatierte Einträge (Anfangsphase, Modul 1 und Modul A) sind **nicht** nachträglich datiert worden und stehen am Ende — ein geschätztes Datum wäre schlechter als keins.

### 2026-07-30 — Welle C des Reviews vom 2026-07-29: strukturelle Bereinigung

**Betrifft:** `web/src/app/core/http/api-auth.interceptor.ts` · `web/src/app/core/i18n/api-error.ts` · `src/EmotePurge.Core/Entities/ChannelName.cs` · `src/EmotePurge.Infrastructure/Persistence/ChannelQueries.cs` · `src/EmotePurge.Core/Services/ISevenTvEditorService.cs` · `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Api/Validation/ChannelNameValidationFilter.cs` · `src/EmotePurge.Core/Services/IWorkerHealthReader.cs` · `src/EmotePurge.Worker/ReconnectPolicy.cs` · `web/tsconfig.json`

Zehn Befunde, Reihenfolge nach Abschnitt 8 des Reports. Kein Verhaltenswechsel außer den unten ausdrücklich genannten.

- **S2-13 — ein HTTP-Interceptor statt sechs Kopien.** `if (error.status === 401) { handleSessionExpired(); return; }` stand sechsmal, fünfmal wortgleich, und in `channel-workspace-layout.ts` fehlte der Zweig ganz. Jetzt `core/http/api-auth.interceptor.ts`, registriert per `withInterceptors`. Die erste Zeile `if (!req.url.startsWith('/api/'))` ist der tragende Teil: die 7TV-Mass-Delete-Engine spricht direkt mit `7tv.io/v3/gql` und mit einer **anderen** Credential (dem 7TV-Schreib-Token aus `sessionStorage`) — ein `401` von dort heißt „dieses Token ist abgelaufen", nie „deine EmotePurge-Session ist weg", und darf niemanden ausloggen. Diese Unterscheidung existierte vorher nur implizit darin, dass niemand den 401-Block in den 7TV-Service kopiert hatte. `/api/auth/me` und `/api/auth/logout` sind ausgenommen: Ersteres antwortet für **jeden** anonymen Besucher mit 401, und das als Ablauf zu behandeln hätte die öffentliche Landing-Page auf `/login` umgeleitet.
- **S2-13 — Status-Fallback im Fehler-Mapping.** `apiErrorTranslationKey` las ausschließlich `error.error.errorCode`; alle vier Autorisierungsfilter antworten aber body-los (`Results.Forbid()`), der Rate Limiter mit einem nackten 429, ein Verbindungsabbruch mit Status 0. Das fiel alles auf „Etwas ist schiefgelaufen. Bitte versuch es erneut." — und sagte damit einem frisch gemoddeten Nutzer, er solle etwas wiederholen, das bis zum Ablauf des Mod-Cache nicht gelingen kann. Neu: `errors.status.*` für 0/401/403/404/409/429/≥500, mit dem 10-Minuten-Hinweis im 403-Text. **Über den Report hinaus:** 429 ist mit aufgenommen, weil der Policy-Schnitt aus Welle B (`ExternalApi`, 20/min) diesen Fall real erreichbar gemacht hat. Der konkurrierende Status-Switch in `vote-session-detail-page.ts` ist abgelöst — **bis auf 403**: dort kommt der Code von `VoteEligibilityFilter` und bedeutet genau eine Sache („falsche Rolle für *diese* Session"), während der generische 403-Text auf den Mod-Cache zeigt und in einer Subs-Session irreführend wäre. Vier ungenutzte Keys unter `voting.detail.errors.*` sind entfallen.
- **S3-26 — `ChannelName.Normalize` und `LoadChannelSessionAsync`.** `Trim().ToLowerInvariant()` stand 28-mal; der Gewinn liegt nicht in gesparten Zeichen, sondern darin, dass die stille Invariante „`Channel.ChannelName` ist in der DB immer getrimmt und lowercase" jetzt einen Namen und eine XML-Doc hat. Der Block „Channel laden → Session per `Id == sessionId && ChannelId == channel.Id` laden" stand sechsmal in drei Services — und dieses `ChannelId`-Prädikat ist die **einzige** Absicherung dagegen, dass Channel A eine Session von Channel B beendet, löscht oder abstimmt. Jetzt einmal in `Infrastructure/Persistence/ChannelQueries.cs` (kein generisches Repository). Die Entitäten bleiben getrackt geladen wie an jeder der sechs Stellen vorher, weil `EndAsync`/`DeleteAsync` das brauchen.
- **S3-32 — `ISevenTvEditorService`.** Die Kette `ResolveSevenTvIdentityAsync` → `GetEditorOfChannelsAsync` → Login-Vergleich existierte zweimal mit **zwei verschiedenen** Vergleichsstrategien (`OrdinalIgnoreCase` in `ChannelAccessService`, `ToLowerInvariant`-Dictionary-Keys in `MyChannelsService`), und „Ist X 7TV-Editor von Y?" ist eine Autorisierungsfrage. **Abweichung vom Report:** Die Rückgabe ist nicht `IReadOnlySet<string>`, sondern ein `SevenTvEditorGrants`-Record mit Logins **und** Twitch-IDs — eine reine Login-Menge hätte die S3-2-Härtung aus Welle B (Vergleich auf der unveränderlichen Twitch-ID, weil Twitch freigewordene Logins zur Neuregistrierung freigibt) für den Editor-Pfad zurückgedreht. Rückgabetyp ist außerdem nullable: `null` = „7TV konnte nicht antworten", unterscheidbar von der leeren Menge = „antwortete: editiert nichts". **Redis-Topologie geändert:** aus dem Per-Channel-Bool `7tveditor:{uid}:{channel}` ist der Grant-Satz `7tveditor:{uid}` geworden (JSON). Der 7TV-Call liefert ohnehin alle Grants, der alte Schnitt zahlte die zwei Calls für den zweiten Channel desselben Nutzers erneut, und die Übersicht (die die volle Liste braucht) konnte ihn gar nicht nutzen — sie lief bei **jedem** Aufruf ungecacht.
- **S2-13 Schritt 3 — `GET /api/channels/{channelName}/permissions`.** Löst vier UI-Sonden ab, die je einen echten Endpoint aufriefen und dessen Erfolg/Fehlschlag als Rechte-Bit lasen: zwei in `ChannelWorkspaceLayout`, eine in `VoteSessionListPage`, eine im `usageStatsAccessGuard` (eine vollständige, sofort weggeworfene Aggregat-Query pro Navigation). Bewusst **hinter keinem** Autorisierungsfilter — der Endpoint berichtet, ob man sie passieren würde. `CanViewUsageStatsAsync` wird nur aufgerufen, wenn `CanManageChannelAsync` schon `false` war: der Unterschied zwischen beiden ist der 7TV-Editor-Zweig, und den in den Request-Pfad jedes Managers zu ziehen wäre teurer als der Endpoint spart. Payload trägt zusätzlich `isTracked`/`isBotActive`, weil der Reaktivieren-Button des Workspace daran hängt (ein Leave deaktiviert seit Welle B nur noch). **Abweichung:** kein `isSevenTvEditor` — es hat heute keinen Konsumenten und würde genau die obige Abkürzung zunichte machen; kommt mit seinem ersten Konsumenten. Der Guard klärt jetzt zuerst den Login (`ensureLoaded`), damit sein Aufruf nicht 401t und dabei mit dem Redirect des Interceptors um dieselbe Navigation konkurriert.
- **S3-31 / Zielkonflikt Z4 — `ChannelNameValidationFilter` auf Gruppenebene.** Nutzerentscheidung: Gruppen-Filter, **kein** Route-Constraint. Ein Constraint `{channelName:regex(...)}` wäre kürzer, liefert aber 404 ohne Body statt 400 mit `invalid_channel_name` — das ändert den Fehlercode-Vertrag, den S2-13 gerade erst nutzbar gemacht hat — und kann nicht normalisieren, hätte also gemischt geschriebene Logins wie `HandOfBlood` abgelehnt. Der Filter läuft als **erster** in jeder Gruppe, vor den Autorisierungsfiltern; diese Reihenfolge *ist* der Vertrag. Damit entfallen zehn inline-Prüfungen, und die fünf channel-scoped Endpoints, die gar keine hatten (`end`, `delete`, `results`, Vote setzen, Vote zurücknehmen), sind mit abgedeckt. Die Prüfung in den vier Autorisierungsfiltern bleibt als letzte Instanz vor dem ersten Redis-Key stehen. Zweiter Teil: **der Service ist die Wahrheit, der Endpoint übersetzt** — `CreateAsync` gibt `CreateVoteSessionResult` zurück (Muster von `VoteCastResult`), die 24 Zeilen Vorab-Validierung im Endpoint sind weg, und die vier `ArgumentException`-Regeln im Service, die vorher unerreichbar waren, sind jetzt der einzige Ort. Vorher divergierten die Ausfallmodi: eine fünfte Regel nur im Service ergab eine 500 statt einer 400, nur im Endpoint einen für jeden anderen Aufrufer durchlässigen Service.
- **S3-30 — `rxResource`-Pilot auf `VoteSessionListPage`** (Nutzerentscheidung für diese Seite: zwei Parameter, keine RxJS-Poll-Nachbarschaft wie auf der Usage-Stats-Seite). `params: () => ({ channel, page })` ersetzt `effect(() => this.load())` und löst dabei denselben Grund, aus dem der Effect ursprünglich existierte (NG0950 beim Lesen eines Route-Inputs im Konstruktor) mit — ohne dessen Falle, dass jeder weitere Signal-Read in `load()` still zum Reload-Trigger wird. `create`/`delete` laden jetzt neu statt die Liste optimistisch zu patchen: beides verschiebt die Paginierung, und die alte Variante konnte eine Seite auf `pageSize + 1` Zeilen wachsen lassen oder eine neue Session auf Seite 3 anzeigen. `end` patcht weiterhin lokal (In-Place-Feldänderung an einer sichtbaren Zeile, keine Paginierungswirkung). Die anderen vier Loader-`effect()`s bleiben unverändert, bis dieser Pilot sich getragen hat.
- **S3-33 — `strict`, `strictTemplates` und `typeCheckHostBindings` aktiviert.** Vorab gemessen statt geschätzt: `tsc --strict` über `tsconfig.app.json` **und** `tsconfig.spec.json` → 0 Fehler, ein voller Produktions-`ng build` mit allen drei Flags → 0 Fehler. Die Datei kam ohne sie aus dem Initial-Scaffold, ein frisch generiertes Angular-22-Projekt setzt alle drei per Default — es war eine Lücke, keine Entscheidung. Der Wert liegt nicht in den heutigen 0 Fehlern, sondern darin, dass die nächste Datei sie nicht mehr unbemerkt verletzen kann.
- **S4-11 — `IWorkerHealthReader`.** Der einzige Ort, an dem die Api an ihrem eigenen Service-Layer vorbei auf Infrastruktur zugriff: `IConnectionMultiplexer` direkt im Handler, der Key `"worker:health:twitch"` als Literal, das Payload in ein privat definiertes Record deserialisiert — dasselbe Wire-Format also zweimal deklariert, in Api und Worker, ohne Verbindung (die Projekte referenzieren sich nicht). Jetzt besitzt `WorkerHealthKeys` den Key und die TTL, `WorkerHealthSnapshot` (Core) das Format, und beide Seiten serialisieren denselben Typ. `grep StackExchange.Redis src/EmotePurge.Api` findet danach nichts mehr.
- **S3-6 Teil 1 — `ReconnectPolicy` extrahiert.** Jede Regel darin stammt aus einem Produktionsausfall (2026-07-26 zweimal, 2026-07-27), und validiert hat sie nur der jeweils nächste Ausfall: die Logik saß in der längsten Datei des Projekts, verwoben mit TwitchLib-Event-Wiring. Jetzt eine TwitchLib-freie Klasse ohne Uhr (die verstrichene Zeit wird hereingegeben) mit den vier Zuständen als `Decide(TimeSpan?) → {Reconnect, Recreate, Wait}` plus den beiden `Interlocked`-Zählern. Entscheidungslogik zeichengleich zur vorherigen Fallunterscheidung; `TwitchChatManager` behält nur den Transport. Die Tests dazu sind Welle D.

### 2026-07-28 — `EmotePurge.Api`: Endpoints aus `Program.cs` in `Endpoints/*.cs`-Extension-Methods aufgeteilt statt auf Controllers umgestiegen

**Betrifft:** `src/EmotePurge.Api/Program.cs` · `src/EmotePurge.Api/Endpoints/` · `src/EmotePurge.Api/Validation/ChannelNameValidation.cs`

**`EmotePurge.Api`: Endpoints aus `Program.cs` in `Endpoints/*.cs`-Extension-Methods aufgeteilt statt auf Controllers umgestiegen — 2026-07-28.** Auslöser: `Program.cs` war auf ~680 Zeilen/~20 Endpoints angewachsen und unübersichtlich geworden; naheliegender Reflex wäre der Umstieg auf ASP.NET-Core-Controllers gewesen. Bewusst dagegen entschieden — das Problem war Datei-Organisation, nicht der Minimal-API-Ansatz, und ein Controller-Umstieg hätte alle 6 `IEndpointFilter`-Klassen (`ChannelManagementAuthorizationFilter`, `UsageStatsAccessAuthorizationFilter`, `GlobalAdminAuthorizationFilter`, `VoteEligibilityFilter`, `VoteAudienceFilter`) auf Action-Filter/Policies umbauen müssen, `AddControllers()`/`MapControllers()`/`UseAuthorization()`-Reihenfolge zurückgebracht und praktisch jeden Endpoint-Response-Shape angefasst — hoher Diff/Regressionsrisiko ohne funktionalen Gewinn, hätte außerdem die bereits dokumentierte "Minimal API statt Controllers"-Entscheidung ohne neuen inhaltlichen Grund gekippt. **Stattdessen:** `Program.cs` schrumpft auf Bootstrapping (Services, Middleware-Pipeline, Aufruf der `Map*Endpoints()`-Methoden); die Endpoints selbst wandern nach Domäne sortiert in `src/EmotePurge.Api/Endpoints/{Channel,Emote,UsageStats,VoteSession,Auth,WorkerHealth}Endpoints.cs`, je eine `public static class XEndpoints { public static void MapXEndpoints(this WebApplication app) { ... } }`, registriert über `app.MapGroup(prefix)` statt einzelner `app.Map*("/api/channels/{channelName}/...")`-Aufrufe — spart dabei nebenbei die pro-Endpoint wiederholten `.RequireAuthorization()`/Filter-Zuweisungen, wo eine ganze Gruppe denselben Filter teilt (z. B. `EmoteEndpoints`/`UsageStatsEndpoints`: `UsageStatsAccessAuthorizationFilter` einmal auf Gruppenebene statt 3×/2× einzeln). Die überall duplizierte channelName-Regex-Validierung (`^[a-z0-9_]{4,25}$`, vorher ~10x inline) ist jetzt in `Validation/ChannelNameValidation.cs` zentralisiert — bewusst als reiner Helfer im Handler-Body aufgerufen, nicht als zusätzlicher `IEndpointFilter`, um die bestehende Ausführungsreihenfolge (Autorisierungsfilter lesen den rohen, noch ungeprüften Route-Value zuerst) nicht zu verändern. Verhalten 1:1 identisch zum vorherigen `Program.cs` — verifiziert per `dotnet build`/`dotnet test` (39/39 grün) sowie einem Live-Smoke-Test aller Routen gegen eine lokal laufende Api (jede Route liefert exakt denselben Auth-Statuscode wie vorher; drei `POST`-Routen mit Pflicht-Body lieferten ohne `Content-Type: application/json` `404` statt `401` — bestehendes ASP.NET-Core-Minimal-API-Verhalten für Body-gebundene Routen ohne passenden Content-Type, keine Regression, mit Body verifiziert `401`).

**Nachtrag 2026-07-30 — die Begründung „bewusst als reiner Helfer im Handler-Body, nicht als `IEndpointFilter`" ist überholt.** Sie trug für ihr eigenes Ziel (Verhalten 1:1 identisch halten), sagte aber nichts darüber, dass der ungeprüfte Route-Value bis in einen persistenten Redis-Key durchlief — genau das war Befund S3-3, behoben in Welle B durch die Prüfung *innerhalb* der vier Autorisierungsfilter. Seit dem 2026-07-30 ist die Validierung ein `ChannelNameValidationFilter` auf Gruppenebene, registriert **vor** jedem Autorisierungsfilter; die Reihenfolge, die der Eintrag oben bewusst nicht antasten wollte, ist damit absichtlich umgedreht (Zielkonflikt Z4). Der Fehlercode-Vertrag bleibt: 400 mit `invalid_channel_name`, nicht 404. Details im Welle-C-Eintrag vom 2026-07-30.

### 2026-07-28 — `web/`: echte Testabdeckung eingeführt (Unit + E2E), reversiert die "kein hartes Testabdeckungs-Gebot fürs Frontend"-Haltung vom 2026-07-27-Eintrag oben

**Betrifft:** `web/src/app/core/` · `web/e2e/` · `web/playwright.config.ts` · `.github/workflows/publish.yml`

**`web/`: echte Testabdeckung eingeführt (Unit + E2E), reversiert die "kein hartes Testabdeckungs-Gebot fürs Frontend"-Haltung vom 2026-07-27-Eintrag oben — 2026-07-28, explizite Nutzeranforderung.** Umfang bewusst zuerst auf Kernlogik statt Komponenten gelegt (Komponententests bleiben Live-Testen im Browser überlassen, s. u.). **Unit-Tests (Vitest, `@angular/build:unit-test`, bereits vorhandener Runner — kein Zoneless-Sonderfall, `zone.js` ist in diesem Workspace ohnehin nicht installiert, `HttpTestingController`/`vi.useFakeTimers()` funktionieren beide unverändert):** alle Core-Services (`auth`, `channel`, `vote-session`, `usage-stat`, `emote-admin`, `worker-health`, `seven-tv-token`, `seven-tv-delete`), alle drei Route-Guards (`auth`, `usage-stats-access`, `vote-session-access`) und die beiden reinen Shared-Utilities (`ListSelection`, `grid-columns`) — 14 Spec-Dateien, 78 Tests. Guards sind reine Funktionen (`CanActivateFn`), getestet über `TestBed.runInInjectionContext(() => guardFn(...))`, nicht über eine Router-Navigation — schneller und direkter, da nur die Guard-Logik selbst (nicht das Routing drumherum) unter Test steht. `SevenTvDeleteService` (`concatMap`+`delay(275ms)`-Queue) ist der aufwändigste Test — verifiziert sequenzielle Ausführung, 401-Token-Invalidierung, Cancel-Mid-Run und den abschließenden `sync-deleted`-Call, alles per `vi.useFakeTimers()`/`vi.advanceTimersByTime()` statt echtem Warten. **E2E (Playwright, neu `web/playwright.config.ts` + `web/e2e/`):** Zwei Spec-Dateien (`auth.e2e.spec.ts`, `channel-workspace.e2e.spec.ts`), vier Tests — unauthentifizierter Redirect zu `/login` inkl. echtem Klick-Trigger auf den Twitch-Login-Redirect, Return-URL-Stashing bei einem geschützten Deep-Link, authentifizierte Overview (Anzeigename/Logout im Header) und der volle Overview→Channel-Öffnen→Usage-Stats-Pfad. **Bewusst kein Live-Stack** (kein Postgres/Redis/echte Twitch-OAuth-App im Test): jeder `/api/**`-Call wird über `page.route()` gemockt (`web/e2e/support/mocks.ts`), der `ng serve`-Dev-Server läuft reell (rendert echtes Routing/Guards/Components), sein Proxy-Target (`:5151`) wird aber nie erreicht, da alle Requests bereits im Browser abgefangen werden — macht die Suite CI-tauglich ohne Docker-Compose-Abhängigkeit, kostet aber echte Backend-Integrationsabdeckung (die bleibt weiterhin Live-Testen im Browser vorbehalten, s. `feedback_live_test_before_commit`-Memory). **CI:** neuer `test-web`-Job in `.github/workflows/publish.yml` (Node 22, `npm ci`, `npm test -- --watch=false`, `npx playwright install --with-deps chromium`, `npm run e2e`), `publish` hängt jetzt an `needs: [test, test-web]` statt nur `test`. **Konvention ab sofort (ersetzt den 2026-07-27-Eintrag):** Neue Services/Guards/reine Utilities in `web/src/app/core/`+`web/src/app/shared/` bekommen einen Vitest-Spec nach demselben Muster (Co-located `*.spec.ts`, `HttpTestingController` für alles mit `HttpClient`, `TestBed.runInInjectionContext` für Guards). Größere User-Flows, die mehrere Seiten/Guards/Services verketten, bekommen zusätzlich (nicht stattdessen) einen Playwright-E2E-Test mit gemockten `/api/**`-Responses. Komponententests (isolierte TestBed-Component-Harness-Tests einzelner Seiten) sind weiterhin bewusst **nicht** Teil der Konvention — Live-Testen im Browser bleibt dort das primäre Mittel, wie im 2026-07-27-Eintrag begründet.

### 2026-07-27 — Künftiges Refresh-Token-Handling (geplant, noch nicht umgesetzt)

**Betrifft:** `src/EmotePurge.Infrastructure/Services/ModeratorCheckService.cs` · `src/EmotePurge.Infrastructure/Services/VoteEligibilityService.cs`

**Künftiges Refresh-Token-Handling (geplant, noch nicht umgesetzt) — 2026-07-27.** Aktuell läuft der Twitch-Access-Token nach ~4h ab; danach schlägt nur der Live-Moderator-/Subscriber-Check fehl (`ModeratorCheckService`, `VoteEligibilityService`), Admin-/Broadcaster-Pfade bleiben unbegrenzt gültig. Skizze für eine spätere Nachrüstung: (1) **Neue DB-Spalte** nötig (z. B. `User.RefreshToken`/`RefreshTokenExpiresAt`) — ein Refresh-Token soll länger leben als das Cookie selbst, kann daher nicht wie der Access-Token einfach als Cookie-Claim mitgeführt werden; erfordert eine `User`-Erweiterung + Migration. (2) **Refresh-Strategie**: entweder ein periodischer Background-Job (analog `SevenTvPeriodicResyncWorker`) oder On-Demand-Refresh direkt in `ModeratorCheckService`/`VoteEligibilityService` bei einem `401` von Helix (Token-Exchange mit `grant_type=refresh_token`, analog zum bestehenden `ITwitchAuthClient`-Muster) — On-Demand ist der naheliegendere erste Schritt (kein neuer Hosted Service nötig, Fehlerfall bleibt lokal im ohnehin vorhandenen Call-Pfad). (3) **Scope-/Consent-Implikationen** bei Twitch noch zu verifizieren, sobald umgesetzt. Nicht umgesetzt: Re-Login als Workaround für den seltenen Fall (Session > 4h aktiv) wurde als aktuell ausreichend bewertet (explizite Nutzerentscheidung, 2026-07-27). Ein separater, live gefundener Bug — Login-Status im Header verschwand fälschlich auf bestimmten Routen — hatte damit **nichts** zu tun (reine Frontend-Routing-Lücke, s. `AppShell`-Fix vom selben Tag), wurde aber im selben Zuge behoben.

### 2026-07-27 — `TwitchConnectionWatchdog`: Cooldown zwischen erzwungenen Reconnects statt Re-Trigger bei jedem 60s-Tick

**Betrifft:** `src/EmotePurge.Worker/TwitchConnectionWatchdog.cs`

**`TwitchConnectionWatchdog`: Cooldown zwischen erzwungenen Reconnects statt Re-Trigger bei jedem 60s-Tick — 2026-07-27, per Log-Analyse gefunden (Nutzerverdacht: Problem tritt v. a. auf, wenn alle gejointen Channels offline sind).** Log vom 2026-07-26 22:48–22:53 zeigte, dass `CheckOnceAsync` **jede einzelne Minute** erneut `ForceReconnectAsync()` auslöste (22:49/22:50/22:51/22:52/22:53, jeweils exakt 60s auseinander, "Keine Nachricht seit"-Wert stetig um 60s steigend), statt nur einmalig beim Überschreiten der 5-Minuten-Schwelle. Ursache: Ein erzwungener Reconnect aktualisiert `LastMessageReceivedUtc` nicht (das passiert nur bei einer tatsächlich empfangenen Chat-Nachricht) — solange kein Chat reinkommt (typischerweise weil der/die Broadcaster offline sind, aber nicht zwingend nur dann), bleibt `idleFor` dauerhaft über der Schwelle und der Watchdog feuert unbegrenzt im Minutentakt weiter. Im selben Log kippten die Reconnects ab dem dritten Versuch (22:51) von "erfolgreich" auf `Fatal network error` — naheliegende Erklärung: Twitch (oder die zugrunde liegende WebSocket-Lib) wertet gehäufte Reconnects von derselben anonymen `justinfanXXXXX`-Verbindung in kurzer Folge als missbräuchlich und kappt sie hart. **Verworfene Alternative:** Vorab per Twitch Helix ("Get Streams") prüfen, ob mindestens ein gejointer Channel live ist, und Reconnect nur dann erzwingen. Löst zwar den vom Nutzer beobachteten Hauptfall, adressiert aber nicht die eigentliche Ursache (fehlende Cooldown-Logik im Watchdog) und hätte einen neuen App-Access-Token-Flow (Client-Credentials-Grant) im Worker erfordert, den es dort bisher nicht gibt (die bestehenden Helix-Clients in der Api hängen an User-OAuth eingeloggter Nutzer, nicht an einem App-Token) — Komplexität ohne vollständige Fix-Wirkung, daher nicht umgesetzt. **Fix:** Neues `_lastForcedReconnectUtc`-Feld in `TwitchConnectionWatchdog`; `CheckOnceAsync` erzwingt einen Reconnect jetzt nur, wenn zusätzlich zur Idle-Schwelle auch seit dem letzten erzwungenen Reconnect mindestens `StaleThreshold` (5 Min) vergangen ist — unabhängig davon, ob zwischenzeitlich eine Nachricht ankam. Reduziert die Reconnect-Frequenz im Worst Case (dauerhaft stille Channels) von "jede Minute für immer" auf "alle 5 Minuten", was die Twitch-seitige Abuse-Erkennung deutlich seltener treffen sollte, und behebt das Problem unabhängig davon, ob die Ursache tatsächlich Offline-Channels oder ein anderer stiller Freeze war. Ein echter Freeze wird weiterhin erkannt und nach spätestens 5 Minuten (statt bisher 1) neu versucht — bewusst in Kauf genommen, da die bisherige 60s-Wiederholung ohnehin nutzlos war, solange die zugrunde liegende Verbindung tatsächlich hängt. **Noch nicht live gegen Prod verifiziert** — nächster Channel-Offline-Zeitraum ist der Praxistest.

### 2026-07-27 — Anonyme Voting-Share-Links entfernt

**Betrifft:** `src/EmotePurge.Infrastructure/Services/VoteEligibilityService.cs` · `src/EmotePurge.Api/Auth/VoteAudienceFilter.cs` · `web/src/app/core/voting/vote-session-access.guard.ts`

**Anonyme Voting-Share-Links entfernt — 2026-07-27, explizite Nutzerentscheidung, reversiert eine frühere bewusste Design-Entscheidung.** Modul D hatte Voting-Seiten (`vote-sessions`-Liste + `-Detail`) bisher absichtlich ohne Login-Zwang erreichbar gehalten (s. mehrere ältere Einträge oben, u. a. "Vote-Ergebnisse um MyVote erweitert" und "AuthService.login(returnUrl?)"), damit ein geteilter Link auch von anonymen Zuschauern angesehen werden kann — Login wurde erst beim tatsächlichen Abstimmen verlangt. Auf expliziten Wunsch umgekehrt: Jede Voting-Seite verlangt jetzt Login, die Detail-Seite zusätzlich echte Stimmberechtigung für genau diese Session. **Backend:** `IVoteEligibilityService` um `EvaluateAudienceAsync` erweitert (`src/EmotePurge.Infrastructure/Services/VoteEligibilityService.cs`) — dieselbe Rollen-/Identitätsprüfung wie `EvaluateAsync`, aber ohne den `IsActive`-Gate: eine beendete Session soll für ihr ursprüngliches Zielpublikum weiterhin sichtbar bleiben, "Voting geschlossen" ist nicht dasselbe wie "Zugriff entzogen". Beide Methoden teilen sich jetzt private `LoadSessionAsync`/`EvaluateRoleAsync`-Helper (vorher ein einzelner Methodenkörper). Neuer `VoteAudienceFilter` (`src/EmotePurge.Api/Auth/`, analog zu `VoteEligibilityFilter`) hängt an `GET .../vote-sessions/{sessionId}/results` (jetzt zusätzlich `.RequireAuthorization()`); `GET .../vote-sessions` (Liste) bekam nur `.RequireAuthorization()` ohne Session-spezifische Prüfung, da eine Liste kein einzelnes `AllowedVoterRoles` hat. **Frontend:** Neuer `voteSessionAccessGuard` (`web/src/app/core/voting/vote-session-access.guard.ts`) folgt demselben Muster wie `channelManagementGuard` — ruft den echten `getResults()`-Endpoint auf und interpretiert Erfolg/Fehlschlag als Autorisierungssignal, statt einen dedizierten "darf ich"-Endpoint zu bauen; bei fehlendem Login Redirect nach `/login` (Return-URL gestasht), bei fehlender Berechtigung Redirect zurück zur (jetzt ebenfalls login-pflichtigen) Session-Liste des Channels. `authGuard` stasht dabei jetzt selbst die versuchte Ziel-URL vor dem Redirect (`AuthService.stashReturnUrl`, aus `login()` extrahiert) — vorher ging dieser Kontext beim generischen Overview-Guard verloren, fiel dort aber nicht auf, weil Overview zufällig mit dem Standard-Post-Login-Ziel übereinstimmt; jetzt relevant, weil die neuen Voting-Guards denselben Mechanismus brauchen. **Bewusst nicht gebaut:** ein separater Eligibility-Read-Endpoint — hätte dieselbe Logik dupliziert, die `results` ohnehin schon serverseitig durchsetzen muss.

### 2026-07-27 — Erstes Backend-Testprojekt (`tests/EmotePurge.Infrastructure.Tests`) eingeführt, Testcontainers statt Mocks

**Betrifft:** `tests/EmotePurge.Infrastructure.Tests/` · `.github/workflows/publish.yml`

**Erstes Backend-Testprojekt (`tests/EmotePurge.Infrastructure.Tests`) eingeführt, Testcontainers statt Mocks — 2026-07-27.** Deckt `EmotePurge.Infrastructure` ab: Integrationstests laufen gegen echte, ephemere `postgres:16-alpine`/`redis:7.2-alpine`-Container (Testcontainers), nicht gegen In-Memory-EF-Core-Provider oder gemockte `IConnectionMultiplexer` — konsistent mit der bereits bestehenden Nutzerpräferenz, Backend-Features vor dem Commit gegen echte Infrastruktur zu verifizieren (s. Memory `feedback_live_test_before_commit`), nur jetzt automatisiert statt manuell. Reine Logik ohne externe Abhängigkeit (`EmoteMatchCache`) bekommt stattdessen einen einfachen Unit-Test ohne Container. CI (`.github/workflows/publish.yml`) gated den Image-Publish jetzt auf `dotnet test`. **Konvention ab sofort:** Neue Services/Logik in `EmotePurge.Infrastructure` (DB-Queries, Redis-Caches, Business-Logik mit Seiteneffekten) bekommen einen Test in diesem Projekt, nach demselben Muster (Fixture pro externer Abhängigkeit unter `Fixtures/`, `Integration/`- oder `Unit/`-Ordner je nachdem ob echte Infrastruktur gebraucht wird). Kein Testprojekt für `EmotePurge.Api`/`EmotePurge.Worker` vorgesehen — die dünnen Minimal-API-Handler bzw. Hosted Services delegieren ohnehin komplett an die (getestete) Infrastructure-Schicht, ein Test dort würde primär Wiring statt Logik prüfen. `web/` bleibt bei Vitest (`npm --prefix web test`), ebenfalls kein hartes "jedes neue Feature braucht einen Test" für UI-Komponenten — dort bleibt Live-Testen im Browser (s. `Cleanup before HandOfBlood test`-Memory) das primäre Verifikationsmittel.

### 2026-07-26 — Produktions-Deployment auf demselben VPS wie eine bestehende App, über GHCR + Portainer-Stack

**Betrifft:** `docker-compose.prod.yml` · `.github/workflows/publish.yml`

**Produktions-Deployment auf demselben VPS wie eine bestehende App, über GHCR + Portainer-Stack — 2026-07-26.** Neue `docker-compose.prod.yml` (Sibling zu `docker-compose.yml`, welches unverändert für lokales `docker compose up --build` bleibt) referenziert `image: ghcr.io/sensitron/emotepurge-{api,worker}:latest` statt `build:` — das ist die Datei, die als Portainer-Stack importiert wird, analog zum bestehenden Deployment-Muster des Nutzers (Compose-Datei auf GitHub, GitHub Action baut/pusht Images, Portainer-Redeploy zieht sie). Neuer `.github/workflows/publish.yml`-Workflow baut bei Push auf `main` beide Images und pusht sie nach ghcr.io (`docker/login-action` mit `GITHUB_TOKEN`, `packages: write`-Permission). Da Host-Ports `4200`/`8080` und eine eigene Postgres-Instanz auf dem VPS bereits von einer anderen App belegt sind: `api` mappt in `docker-compose.prod.yml` auf `127.0.0.1:4300:8080` (hinter einem bereits eingerichteten host-level Reverse Proxy für die Domain `emotepurge.app`, TLS-Termination dort), `postgres`/`redis` auf `127.0.0.1:5433`/`127.0.0.1:6380` statt der Standard-Ports (nur Host-seitiges Debug-Mapping betroffen, interne Service-zu-Service-Kommunikation läuft weiterhin über Servicename + Standard-Port). Eigene, isolierte Postgres-/Redis-Container statt Mitnutzung der bestehenden DB-Instanz der anderen App — explizite Nutzerentscheidung, keine Kopplung zwischen den beiden Apps.

### 2026-07-26 — `ForwardedHeadersMiddleware` mit explizit geleertem `KnownIPNetworks`/`KnownProxies` ergänzt

**Betrifft:** `src/EmotePurge.Api/Program.cs`

**`ForwardedHeadersMiddleware` mit explizit geleertem `KnownIPNetworks`/`KnownProxies` ergänzt — 2026-07-26, notwendig für Betrieb hinter host-level Reverse Proxy.** Der `api`-Container ist nur über einen `127.0.0.1`-gebundenen Host-Port erreichbar; ein davor geschalteter, nativ auf dem VPS-Host laufender Reverse Proxy (nicht containerisiert) verbindet sich damit aber nicht über Loopback, sondern über die Docker-Bridge-Gateway-IP (Docker-NAT) — der Default-Trust von `ForwardedHeadersMiddleware` (nur Loopback) hätte `X-Forwarded-Proto` sonst stillschweigend ignoriert, obwohl der Proxy den Header korrekt setzt. Ohne diesen Fix hätte `httpContext.Request.IsHttps` (OAuth-State-Cookie-`Secure`-Flag, `UseHttpsRedirection()`) intern immer `false` gesehen, selbst wenn der Nutzer über `https://emotepurge.app` zugreift. Geleerte `KnownIPNetworks`/`KnownProxies` sind hier vertretbar (sonst ein Sicherheitsrisiko, da beliebige Clients dann selbst `X-Forwarded-Proto` gegen einen öffentlich erreichbaren Endpunkt fälschen könnten), weil der Container ausschließlich über den lokal gebundenen Host-Port erreichbar ist — der einzig mögliche Absender ist der vertrauenswürdige, lokale Proxy selbst. Setzt voraus, dass der Proxy-vhost `X-Forwarded-Proto`/`X-Forwarded-For` tatsächlich setzt (Nutzer-Konfiguration, nicht Teil dieses Repos).

### 2026-07-26 — Modul D, Rest-Umfang: neue Backend-Endpoints für die Dashboard-Übersicht

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Infrastructure/Services/MyChannelsService.cs` · `src/EmotePurge.Api/Auth/GlobalAdminAuthorizationFilter.cs`

**Modul D, Rest-Umfang: neue Backend-Endpoints für die Dashboard-Übersicht — 2026-07-26.** `GET /api/channels` (alle getrackten Channels, Admin-only über neuen `GlobalAdminAuthorizationFilter` — Route-Value-unabhängig, im Gegensatz zu `ChannelManagementAuthorizationFilter`) und `GET /api/channels/mine` (`RequireAuthorization()` ohne weiteren Filter, da keine Channel-Route-Value existiert — "über mich"-Abfrage). Letzterer nutzt einen neuen `IMyChannelsService`: Helix' `GetModeratedChannelLoginsAsync` liefert bereits die volle Liste moderierter Channels, wurde bisher aber nur für einen einzelnen bool'schen Mod-Check konsumiert (`ModeratorCheckService.IsModeratorAsync`) — `MyChannelsService` nutzt jetzt die volle Liste, ergänzt sie um den Broadcaster-Self-Login (Helix liefert den eigenen Channel **nicht** in der moderierten Liste) und gleicht beides gegen Postgres ab (`IsTracked`/`IsBotActive`). Bei abgelaufenem/fehlendem Access-Token oder transientem Helix-Fehler liefert der Endpoint `HelixUnavailable=true` und nur den Self-Eintrag, statt den Request scheitern zu lassen — gleiches Graceful-Degradation-Muster wie in `ModeratorCheckService`.

### 2026-07-26 — `IsGlobalAdmin`-Check aus `ChannelAccessService.CanManageChannelAsync` in eine eigene, öffentliche Methode extrahiert

**Betrifft:** `src/EmotePurge.Infrastructure/Services/ChannelAccessService.cs`

**`IsGlobalAdmin`-Check aus `ChannelAccessService.CanManageChannelAsync` in eine eigene, öffentliche Methode extrahiert — 2026-07-26.** War bisher als reine Inline-Prüfung innerhalb der Channel-spezifischen Autorisierungsentscheidung vergraben; die neue Admin-Übersicht (`GET /api/channels`) braucht denselben Allowlist-Check aber ohne jede Channel-Route-Value. Bewusst synchron (`bool`, nicht `Task<bool>`) statt async — reiner In-Memory-Config-Lookup, kein I/O, ein `async`-Wrapper wäre nur Zeremonie ohne Nutzen.

### 2026-07-26 — `POST /api/channels/{channelName}/emotes/sync-deleted` — Soft-Archive (`IsArchived=true`), kein Hard-Delete

**Betrifft:** `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs` · `src/EmotePurge.Infrastructure/Services/EmoteService.cs`

**`POST /api/channels/{channelName}/emotes/sync-deleted` — Soft-Archive (`IsArchived=true`), kein Hard-Delete — 2026-07-26.** Neuer `IEmoteService`/`EmoteService`, hinter `ChannelManagementAuthorizationFilter` wie `join`/`leave`. Zwei Gründe gegen Hard-Delete: (1) `SevenTvSyncService` markiert aus dem 7TV-Set verschwundene Emotes bereits so, und der ohnehin laufende 1-Minuten-`SevenTvPeriodicResyncWorker` würde dieselben Zeilen unabhängig davon genauso markieren — ein zweiter, konkurrierender "gone"-Mechanismus (Hard-Delete) wäre inkonsistent. (2) `Emote`-Zeilen kaskadieren auf `UsageStat`/`Vote` (`OnDelete: Cascade`) — ein Hard-Delete hätte exakt die Nutzungs-/Vote-Historie zerstört, auf der Usage-Stats- und Voting-Seiten aufbauen. IDs, die nicht zum Channel gehören, unbekannt sind oder schon archiviert sind, landen in `NotFoundIds` statt die ganze Batch scheitern zu lassen (gleiches Muster wie `UsageStatFlushService.FlushAsync`). Route ist channel-scoped (`/api/channels/{channelName}/emotes/sync-deleted`), nicht der in `Architectur.md` ursprünglich skizzierte globale Pfad `/api/emotes/sync-deleted` — siehe Abweichungs-Hinweis dort. **Nachtrag 2026-07-29:** Der Filter hier ist inzwischen `UsageStatsAccessAuthorizationFilter`, nicht mehr `ChannelManagementAuthorizationFilter` (7TV-Editoren dürfen mittlerweile ebenfalls über diesen Endpoint archivieren — s. dessen Klassenkommentar in `src/EmotePurge.Api/Auth/UsageStatsAccessAuthorizationFilter.cs` für die aktuelle Endpoint-Liste und Begründung).

### 2026-07-26 — `GetUsageTotalsAsync` jetzt zusätzlich hinter `ChannelManagementAuthorizationFilter`, plus zwei Korrekturen

**Betrifft:** `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs` · `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs`

**`GetUsageTotalsAsync` jetzt zusätzlich hinter `ChannelManagementAuthorizationFilter`, plus zwei Korrekturen — 2026-07-26.** War komplett öffentlich/ohne Auth, obwohl der einzige reale Consumer (`UsageStatsPage`) ein Management-Tool ist — ein unbeteiligter oder gar anonymer Nutzer hätte die Nutzungsdaten jedes Channels abrufen können. Bei der Gelegenheit zwei Korrekturen: (1) Filtert jetzt `!IsArchived` — vorher hätten bereits gelöschte Emotes weiter als Löschkandidaten auftauchen können, obwohl sie in 7TV längst weg sind. (2) Zero-fillt jetzt **alle** aktiven Emotes statt nur die mit vorhandener `UsageStat`-Zeile (vorher: Inner-Join-via-`GroupBy` ließ ungenutzte Emotes komplett verschwinden) — konsistent mit dem bereits in `VoteSessionQueryService.GetResultsAsync` etablierten "alle aktiven Emotes, nullgefüllt"-Muster. `EmoteUsageTotalDto` trägt jetzt außerdem `SevenTvEmoteId`/`ImageUrl` (fürs Grid/die Mass-Delete-Engine gebraucht).

### 2026-07-26 — Vote-Ergebnisse um `MyVote` (+ `SevenTvEmoteId`) erweitert statt neuem Endpoint

**Betrifft:** `src/EmotePurge.Infrastructure/Services/VoteSessionQueryService.cs` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs`

**Vote-Ergebnisse um `MyVote` (+ `SevenTvEmoteId`) erweitert statt neuem Endpoint — 2026-07-26.** Die Detail-Seite braucht Aggregat-Ergebnisse **und** den eigenen Vote in einem Render-Pass; ein separater Endpoint hätte nur für eingeloggte Betrachter einen zweiten Roundtrip erzwungen, ohne Vorteil. `GetResultsAsync` bekommt einen optionalen `viewerTwitchUserId`-Parameter; der Endpoint bleibt bewusst ohne `RequireAuthorization()` (anonyme Share-Link-Besucher müssen weiter Ergebnisse sehen können) und liest den Principal nur optional über `TryBuildTwitchPrincipal()` — `null` bei Anonym, kein `401`.

### 2026-07-26 — Frontend: `channelManagementGuard` zusätzlich zu `authGuard`

**Betrifft:** `web/src/app/core/channels/usage-stats-access.guard.ts`

**Frontend: `channelManagementGuard` zusätzlich zu `authGuard` — 2026-07-26, live als Sicherheitslücke gefunden.** Die `usage-stats`-Route war nur mit `authGuard` (= "irgendwie eingeloggt") geschützt — jeder eingeloggte Nutzer konnte die Nutzungsdaten **jedes** Channels über die URL aufrufen, nicht nur die eigenen. Neuer `channelManagementGuard` (`web/src/app/core/channels/channel-management.guard.ts`, heute `usage-stats-access.guard.ts`) probt `ChannelService.getStatus(channelName)` (= derselbe `ChannelManagementAuthorizationFilter`-Check wie serverseitig) und redirected bei Fehlschlag auf die (öffentliche) Voting-Seite desselben Channels statt auf die globale Übersicht — bleibt im Kontext. Die Route ist damit der Enforcement-Teil; `ChannelWorkspaceLayout` probt zusätzlich denselben Endpoint, um den "Nutzung"-Tab und den "Channel verlassen"-Button für Nicht-Manager gar nicht erst anzuzeigen (reine UI-Sichtbarkeit, kein Ersatz für den Guard).

### 2026-07-26 — `ListSelection`: von `@angular/cdk/collections`' `SelectionModel` auf einen eigenen Signal-Wrapper umgestellt

**Betrifft:** `web/src/app/shared/selection/list-selection.ts`

**`ListSelection`: von `@angular/cdk/collections`' `SelectionModel` auf einen eigenen Signal-Wrapper umgestellt — 2026-07-26, live gefundener Bug.** Ursprüngliche Implementierung hielt die Auswahl in einer plain-mutable `SelectionModel`-Instanz; ein `computed()` anderswo, das `selection.model.selected` las, um die Mass-Delete-Button-Beschriftung/`disabled`-Zustand zu berechnen, blieb dadurch **für immer auf dem Erststand einfroren** — `computed()` trackt nur tatsächliche Signal-Reads, eine Mutation auf einem plain Object gibt ihm nichts zum Invalidieren. Symptom: Checkbox-Klicks funktionierten visuell, der "Löschen (n)"-Button blieb aber dauerhaft bei `(0)` und `disabled`. Fix: `ListSelection` hält die Auswahl jetzt in einem `signal<ReadonlySet<T>>`, `.selected` ist ein `computed()` darüber — Mutation läuft über `.set()`, dadurch wird jeder Consumer korrekt benachrichtigt. Faustregel für künftigen Code: Ein `computed()`, das über eine Klasseninstanz auf mutable State zugreift (statt auf ein Signal), reagiert nie auf Änderungen an diesem State, egal wie oft die Instanz mutiert wird.

### 2026-07-26 — Voting-Seiten (`VoteSessionListPage`/`VoteSessionDetailPage`): `load()` aus dem Konstruktor in `effect()` verschoben

**Betrifft:** `web/src/app/features/voting/vote-session-list-page.ts` · `web/src/app/features/voting/vote-session-detail-page.ts`

**Voting-Seiten (`VoteSessionListPage`/`VoteSessionDetailPage`): `load()` aus dem Konstruktor in `effect()` verschoben — 2026-07-26, live gefundener Bug (leere Seite).** Beide riefen `this.load()` direkt im Konstruktor auf, welches synchron `this.channelName()` liest — ein `input.required<string>()`, das vom Router erst **nach** Konstruktor-Ausführung gesetzt wird (gilt auch für Signal-Inputs, nicht nur die alte `@Input()`-Decorator-Variante). Ergebnis: `NG0950`-Exception direkt bei Komponenten-Erzeugung, die Seite blieb komplett leer (kein Fehler sichtbar, kein Rendering). `UsageStatsPage` hatte dasselbe Problem nie, weil sie ihren Ladeaufruf von Anfang an in ein `effect()` gewickelt hatte (Bindungen werden vor dem ersten `effect()`-Lauf angewendet). Faustregel: **Nie** ein required Signal-Input im Konstruktor lesen — immer über `effect()` oder `ngOnInit()` deferren.

### 2026-07-26 — 7TV-Mass-Delete-Engine: eine Engine (`SevenTvTokenService`/`SevenTvDeleteService`, Singletons), zwei UI-Einstiegspunkte (`MassDeletePanel`, je eine Instanz auf Usage-Stats- und Voting-Ergebnisseite)

**Betrifft:** `web/src/app/core/seven-tv/` · `web/src/app/shared/seven-tv/mass-delete-panel.ts`

**7TV-Mass-Delete-Engine: eine Engine (`SevenTvTokenService`/`SevenTvDeleteService`, Singletons), zwei UI-Einstiegspunkte (`MassDeletePanel`, je eine Instanz auf Usage-Stats- und Voting-Ergebnisseite) — 2026-07-26.** Bewusst kein globaler, immer sichtbarer Delete-Widget in der `AppShell` — Auswahl ist seiten-lokaler State (`ListSelection` pro Host-Seite), das Löschen selbst (Token/Queue/Fortschritt) ist dagegen zustandslos genug, um als Singleton geteilt zu werden. Nebeneffekt (bewusst in Kauf genommen, kein Feature): Läuft ein Löschvorgang und man navigiert zur anderen Seite, taucht der Fortschritt dort weiter auf, gebunden an denselben Lauf. Löschbestätigung ist bewusst simpel gehalten (Anzahl+Vorschau-Liste, Start/Abbrechen, keine Pause/Resume-Logik bei Einzelfehlern) — explizite Nutzerentscheidung gegen die aufwändigere Variante.

### 2026-07-26 — Grid statt Ein-Spalten-Liste für Usage-Stats/Voting-Emotes, per Zeilen-Virtualisierung

**Betrifft:** `web/src/app/shared/grid/grid-columns.ts` · `web/src/app/features/usage-stats/usage-stats-page.ts`

**Grid statt Ein-Spalten-Liste für Usage-Stats/Voting-Emotes, per Zeilen-Virtualisierung — 2026-07-26.** Bei Channels mit bis zu ~1.000 Emotes war eine Liste mit einem Emote pro Zeile trotz `CdkVirtualScrollViewport` unpraktikabel lang zum Scrollen (das eigentliche Nutzerfeedback). `CdkVirtualScrollViewport`/`*cdkVirtualFor` virtualisiert nur eine Dimension; für ein Grid wird die flache, sortierte Emote-Liste stattdessen in Zeilen zu je `columns()` Emotes gechunkt (`shared/grid/grid-columns.ts`, `chunkIntoRows`) und **diese Zeilen** virtualisiert — jede Zeile rendert intern per CSS Grid `columns()` Karten. Spaltenzahl reagiert auf `(window:resize)` (Host-Binding, kein `@HostListener`-Decorator) über `computeGridColumns(width)` (Tailwind-Breakpoint-ähnliche Stufen, 2–8 Spalten). Erkauft sich dadurch bewusst **keine** exakte 1:1-CSS-Media-Query-Responsivität (Spaltenzahl ist ein JS-Wert, kein reines CSS-Grid-`auto-fill`) — nötig, weil die feste Zeilenhöhe für `itemSize` sonst nicht zur tatsächlich gerenderten Spaltenzahl passen würde.

### 2026-07-26 — Frontend: Admin kann über die Übersicht einen beliebigen Channel-Namen joinen

**Betrifft:** `web/src/app/features/overview/overview-page.ts`

**Frontend: Admin kann über die Übersicht einen beliebigen Channel-Namen joinen — 2026-07-26.** Beim Übersichts-Redesign (Ersatz der alten `DashboardPage`) ging die manuelle Texteingabe zum Joinen verloren — Admins moderieren aber nicht notwendigerweise jeden Channel, den sie verwalten dürfen (globale Allowlist-Berechtigung, s. `IsGlobalAdmin`), brauchen also weiterhin einen Weg, einen ihnen unbekannten Channel-Namen einzugeben. Formular lebt bewusst nur innerhalb der Admin-Sektion der `OverviewPage` (die für Nicht-Admins gar nicht rendert, s. `adminChannels()`), kein zusätzlicher Rechte-Check nötig.

### 2026-07-26 — `AuthService.login(returnUrl?)` + `consumeReturnUrl()`

**Betrifft:** `web/src/app/core/auth/auth.service.ts` · `web/src/app/features/shell/app-shell.ts`

**`AuthService.login(returnUrl?)` + `consumeReturnUrl()` — 2026-07-26, notwendig durch die Share-Link-Anforderung an Voting-Seiten.** Ein anonymer Besucher, der über einen geteilten Vote-Session-Link einloggt, landete vorher immer auf dem festen `Auth:Twitch:PostLoginRedirectUrl` (`/`) statt zurück auf der Seite, von der aus er den Login-Klick ausgelöst hat — der Backend-Callback-Redirect ist fest verdrahtet und kennt kein "zurück wohin". Fix bleibt komplett im Frontend: `login()` stasht den aktuellen Pfad in `sessionStorage` (`ep_return_url`) bevor der volle Browser-Redirect zu `/api/auth/twitch/login` losgeht; `AppShell` konsumiert den Wert per `effect()` auf `currentUser()`, sobald die Session nach dem Login-Roundtrip wieder geladen ist, und navigiert dorthin. Wird beim Logout/Session-Expiry vorsichtshalber mitgelöscht (`SevenTvTokenService.clearToken()` läuft aus demselben Grund mit, s. dortigen Kommentar).

### 2026-07-26 — Fehlendes `libgssapi-krb5-2`-Systempaket im Worker-Docker-Image verhinderte die Twitch-IRC-Verbindung komplett

**Betrifft:** `src/EmotePurge.Worker/Dockerfile`

**Fehlendes `libgssapi-krb5-2`-Systempaket im Worker-Docker-Image verhinderte die Twitch-IRC-Verbindung komplett — 2026-07-26, live gefunden, hoher Diagnose-Aufwand.** Symptom: `GET /api/worker/health` zeigte dauerhaft `disconnected`, `TwitchConnectionWatchdog` loggte wiederholt `Keine Chat-Nachricht seit Ns empfangen, erzwinge Reconnect`, jeder Reconnect-Versuch scheiterte mit `TwitchChatManager`-Log `TwitchClient-Verbindungsfehler für justinfan5216: Fatal network error.` — obwohl 7TV-REST-Aufrufe (ebenfalls HTTPS) im selben Container klaglos funktionierten. Diagnose: rohe TCP-Verbindungen zu `irc-ws.chat.twitch.tv:443`/`irc.chat.twitch.tv:6697` funktionierten sowohl vom Host als auch von **innerhalb** des Worker-Containers (`/dev/tcp`-Bash-Trick, kein `curl`/`wget` im schlanken Runtime-Image) — also kein DNS-/Firewall-/Netzwerkproblem. Der eigentliche Fehler stand erst tiefer im Container-Log: `Error: libgssapi_krb5.so.2: cannot open shared object file: No such file or directory`. .NET's `SslStream` lädt unter Linux für Teile des TLS-Handshakes dynamisch `libgssapi_krb5` — im schlanken `mcr.microsoft.com/dotnet/runtime:10.0`-Image standardmäßig nicht enthalten. Warum nur Twitch IRC betroffen war und nicht die 7TV-REST-Aufrufe: vermutlich unterschiedliche TLS-Codepfade (WebSocket-Handshake vs. plain HTTPS) — nicht abschließend verifiziert, aber empirisch reproduzierbar (Fix behebt beides Male exakt das beobachtete Symptom). **Fix:** `src/EmotePurge.Worker/Dockerfile`s `base`-Stage installiert jetzt `libgssapi-krb5-2` via `apt-get`. Nach Rebuild verbindet sich der Worker sofort fehlerfrei. **Mögliche Rückwirkung:** Dies könnte die tatsächliche Ursache der bereits früher dokumentierten "stillen Freezes" (s. `TwitchConnectionWatchdog`-Eintrag, 2026-07-25) gewesen sein, nicht nur ein TwitchLib-seitig unerkannter Disconnect — nicht rückwirkend beweisbar, aber ein naheliegender Verdacht, falls das Verbindungsproblem nach diesem Fix nicht wieder auftritt.

### 2026-07-26 — `TwitchChatManager`: Full-Client-Recreation nach mehreren aufeinanderfolgenden Reconnect-Fehlschlägen

**Betrifft:** `src/EmotePurge.Worker/TwitchChatManager.cs`

**`TwitchChatManager`: Full-Client-Recreation nach mehreren aufeinanderfolgenden Reconnect-Fehlschlägen — 2026-07-26, live auf Prod gefunden, zweiter Ausfall trotz `libgssapi`-Fix.** Symptom auf `emotepurge.app`: Worker verlor die Twitch-IRC-Verbindung um 18:55 Uhr, danach schlug **jeder** minütliche `ForceReconnectAsync()`-Versuch des Watchdogs über 45+ Minuten identisch fehl (`TwitchClient-Verbindungsfehler für justinfanXXXXX: Fatal network error.`), ohne sich je zu erholen. Diagnose auf dem VPS ausgeschlossen: `libgssapi-krb5-2` war installiert, Container-Ressourcen unauffällig (117 MB RAM, ~220 FDs, Ulimit 1 Mio., kein Socket-Leck — `/proc/$PID/net/tcp` zeigte nur 2 harmlose interne Verbindungen), rohe TCP/TLS-Erreichbarkeit von `irc-ws.chat.twitch.tv:443` funktionierte sowohl vom Host (`curl`) als auch aus dem Container (`/dev/tcp`-Trick) anstandslos, DNS lieferte alle 8 Twitch-IPs korrekt. Damit blieb als einzige Erklärung: Die einzige, seit Prozessstart langlebige `TwitchClient`-Instanz (s. Architektur-Grundsatz "nie pro Channel/Join neu instanziieren") geriet nach einem fatalen Fehler in einen Zustand, aus dem `ReconnectAsync()` auf demselben Objekt sie nie wieder herausholen konnte — ein bei TwitchLib.Communication bekanntes Verhaltensmuster. **Fix:** `TwitchChatManager` zählt aufeinanderfolgende `OnConnectionError`-Events (`_consecutiveConnectionErrors`, per `Interlocked` verwaltet, zurückgesetzt bei jedem erfolgreichen `OnConnected`/`OnReconnected`). Ab `MaxConsecutiveConnectionErrorsBeforeRecreate` (3) instanziiert `ForceReconnectAsync()` statt eines bloßen `ReconnectAsync()` einen **komplett neuen** `TwitchClient` (`RecreateClientAsync`: alten Client per `UnwireClient` von allen Events abmelden, `DisconnectAsync()` best-effort aufräumen, neuen Client per `WireUpClient` verdrahten und verbinden — `OnReconnected` übernimmt danach wie gewohnt das Rejoin aller `_joinedChannels`). Ein `SemaphoreSlim` in `ForceReconnectAsync` verhindert überlappende Reconnect/Recreate-Durchläufe, falls der Watchdog schneller feuert als ein Versuch abschließt. `_client`-Feld dafür von `readonly` auf normal umgestellt. **Noch nicht live gegen Prod verifiziert** (Fix wurde nach dem Vorfall geschrieben) — nächster echter Ausfall ist der Praxistest; bis dahin bleibt ein manueller `docker restart emotepurge-worker` das bekannte Sofortmittel.

### 2026-07-25 — 7TV-EventAPI-WebSocket (Live-Dispatch) komplett entfernt, ersetzt durch periodischen REST-Resync

**Betrifft:** `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs` · `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs`

**7TV-EventAPI-WebSocket (Live-Dispatch) komplett entfernt, ersetzt durch periodischen REST-Resync — 2026-07-25.** Ursprünglich implementiert als **eine gemeinsame** WebSocket-Verbindung zu `wss://events.7tv.io/v3` (`ISevenTvEventClient`/`SevenTvEventClient`, `emote_set.update`-Subscription je Channel auf einer Connection, `SevenTvDispatchParser` für die Dispatch-Payload, `ApplyEmoteSetUpdateAsync` in `ISevenTvSyncService` für inkrementelle Deltas — alles inzwischen gelöscht). **Warum entfernt:** Über mehrere Live-Tests am 2026-07-24/25 (Channels `vassilly`, `sensitron`) systematisch untersucht, weil ein live hinzugefügtes Emote ("REITEN" auf `vassilly`) nie per Dispatch ankam, obwohl ein anderer 7TV-Client (DankChat) das Update korrekt erhielt. Befund: Die Subscriptions waren serverseitig korrekt (per `Ack`-Frame bestätigt, `subscription_limit` aus dem `Hello`-Frame weit unausgeschöpft), das Subscribe-Frame-Format war korrekt (`op: 35`, `condition: {object_id: <setId>}`) — Dispatches kamen aber nachweislich unzuverlässig an: teils mehrminütige Verzögerung (ein Test-Emote "COPIUM" kam nach ~10 Minuten live an), teils gar nicht (REITEN nie, auch nicht nach 18+ Stunden). Als Gegenprobe wurde der Quellcode der offiziellen 7TV-Browser-Extension analysiert (github.com/SevenTV/Extension, `src/worker/worker.http.ts`): Sie abonniert `emote_set.*` (Wildcard) statt `emote_set.update`, plus zusätzlich eine channel-scoped Subscription (`condition: {ctx: "channel", platform: "TWITCH", id: <TwitchChannelId>}`, übersteht Set-Wechsel ohne Resubscribe). Beide Anpassungen wurden testweise nachgerüstet, ohne die Zuverlässigkeit messbar zu verbessern (ein per Set-Wechsel-Test beobachteter "Live-Fang" erwies sich bei genauer Zeitmessung als Zufallstreffer des periodischen Resync-Ticks, nicht als echte Live-Zustellung). Der REST-Vollsync (`SyncChannelAsync`) war dagegen in **jedem** Test zuverlässig. **Entscheidung:** WebSocket-Pfad komplett entfernt (spart zusätzlich einiges an Komplexität — Reconnect-Logik, Subscription-Tracking, Dispatch-Parsing), `SevenTvPeriodicResyncWorker` ruft stattdessen `SyncChannelAsync` für alle aktiven Channels im **1-Minuten-Takt** auf (Kosten: ein 7TV-REST-Request pro aktivem Channel/Minute, bei aktueller Channel-Zahl vernachlässigbar). Deckt sowohl In-Set-Änderungen als auch komplette Set-Wechsel gleichermaßen ab, da `SyncChannelAsync` bei jedem Aufruf das aktuell aktive Set neu auflöst — kein Unterschied im Handling mehr nötig. `IEmoteMatchCache`-Refresh (voller Reload statt inkrementellem Patchen, s. `RefreshMatchCacheAsync`) bleibt unverändert bestehen, läuft jetzt nur noch nach `SyncChannelAsync` statt zusätzlich nach `ApplyEmoteSetUpdateAsync`.

### 2026-07-25 — `TwitchConnectionWatchdog` + `ITwitchChatManager.IsConnected`/`LastMessageReceivedUtc`/`ForceReconnectAsync()`

**Betrifft:** `src/EmotePurge.Worker/TwitchConnectionWatchdog.cs` · `src/EmotePurge.Worker/TwitchChatManager.cs`

**`TwitchConnectionWatchdog` + `ITwitchChatManager.IsConnected`/`LastMessageReceivedUtc`/`ForceReconnectAsync()` — 2026-07-25, Absicherung gegen einen live beobachteten stillen Freeze.** Beim Live-Testen auf Channel `vassilly` fror das Chat-Matching für ~6 Minuten komplett ein, **ohne** dass TwitchLib je ein `OnDisconnected` feuerte — der obige Reconnect-Fix griff also nicht, weil das auslösende Event nie kam. `TwitchChatManager` trackt jetzt einen `LastMessageReceivedUtc`-Zeitstempel (aktualisiert bei **jeder** empfangenen Nachricht, nicht nur gematchten). `TwitchConnectionWatchdog` (eigener `BackgroundService`, prüft jede Minute) erzwingt einen `ForceReconnectAsync()` (nutzt `TwitchClient.ReconnectAsync()`, feuert danach normal `OnReconnected` inkl. Rejoin-Logik), wenn 5 Minuten lang keine Nachricht ankam. Schwelle bewusst großzügig über dem beobachteten 6-Minuten-Fall gewählt, um einen schlicht ruhigen Channel nicht fälschlich als Freeze zu werten — bekannte Grenze bei Channels mit sehr seltener Chat-Aktivität, für die Zielkanäle (aktive Twitch-Streams) aber unproblematisch.

### 2026-07-25 — `WorkerHealthPublisher` + `GET /api/worker/health` über einen Redis-TTL-Key, nicht direkte Api↔Worker-Kommunikation

**Betrifft:** `src/EmotePurge.Worker/WorkerHealthPublisher.cs` · `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs`

**`WorkerHealthPublisher` + `GET /api/worker/health` über einen Redis-TTL-Key, nicht direkte Api↔Worker-Kommunikation — 2026-07-25.** Api und Worker haben architekturell keine direkte Verbindung (Api ist ein reiner Web-Host ohne Kenntnis vom Worker-Prozess). `WorkerHealthPublisher` schreibt alle 20s `{isConnected, lastMessageReceivedUtc}` als JSON in den Redis-Key `worker:health:twitch` mit 60s-TTL (3x Puffer über dem Schreib-Intervall); die Api liest den Key direkt über die ohnehin injizierte `IConnectionMultiplexer` (dieselbe Redis-Infrastruktur wie `channel:bot:commands`). Fehlt der Key (abgelaufen oder Worker nie gestartet), liefert der Endpoint explizit `{status: "unknown", ...}` statt eines Fehlers — das Fehlen des Keys ist selbst schon die aussagekräftige Information ("Worker tot oder Health-Publisher hängt").

### 2026-07-25 — Modul B: Twitch-OAuth hand-rolled statt `AddOAuth`/Community-Package (`AspNet.Security.OAuth.Twitch`)

**Betrifft:** `src/EmotePurge.Core/Twitch/ITwitchAuthClient.cs` · `src/EmotePurge.Infrastructure/Twitch/TwitchAuthClient.cs` · `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs`

**Modul B: Twitch-OAuth hand-rolled statt `AddOAuth`/Community-Package (`AspNet.Security.OAuth.Twitch`) — 2026-07-25.** Bewusste Abweichung von der naheliegenden Wahl eines fertigen OAuth-Remote-Handler-Providers: Das Projekt hat für jede externe API bereits ein etabliertes Muster (Interface in Core, typisierter `HttpClient` in Infrastructure, explizites Fehlerhandling — s. `SevenTvApiClient`), gegen das die implizite Challenge/Callback-Ceremony von `AddOAuth` reibt, und macht die für den Live-Mod-Check nötige exakte Kontrolle über Cookie-Claims (Access Token, Ablaufzeit) einfacher nachvollziehbar. `ITwitchAuthClient` (Token-Exchange gegen `id.twitch.tv/oauth2/token`, Client-Credentials im Form-Body) und `ITwitchHelixClient` (`api.twitch.tv/helix`, `Bearer`-Token im Header) sind getrennte Interfaces, weil sie unterschiedliche Base-URLs/Auth-Schemata haben. Claims-Aufbau (`ClaimsPrincipal`/`SignInAsync`) liegt bewusst in `Program.cs`, nicht in Infrastructure — Infrastructure ist ein reines `Microsoft.NET.Sdk`-Class-Library ohne ASP.NET-Core-Framework-Referenz, die Twitch-Clients geben daher nur plain records (`TwitchTokenResult`, `TwitchUserInfo`) zurück.

### 2026-07-25 — Live-Mod-Check nur über Twitch Helix "Get Moderated Channels" (`/helix/moderation/channels`) möglich

**Betrifft:** `src/EmotePurge.Infrastructure/Twitch/TwitchHelixClient.cs` · `src/EmotePurge.Infrastructure/Services/ModeratorCheckService.cs`

**Live-Mod-Check nur über Twitch Helix "Get Moderated Channels" (`/helix/moderation/channels`) möglich — 2026-07-25.** Der naheliegende Ansatz ("frage Twitch, wer Moderator von Channel X ist") würde einen OAuth-Token *des Broadcasters* mit Scope `moderation:read` erfordern — nicht praktikabel, da dafür jeder einzelne Streamer die App separat autorisieren müsste. Twitch bietet stattdessen die Umkehrung: mit Scope `user:read:moderated_channels` auf dem **eigenen** Access Token eines eingeloggten Nutzers liefert `/helix/moderation/channels`, welche Channels dieser Nutzer moderiert. Deshalb wird dieser Scope beim Login mit angefragt (`user:read:email user:read:moderated_channels`), und der nutzereigene Access Token muss für die Dauer der Session verfügbar bleiben, um den Live-Check überhaupt ausführen zu können (s. nächster Punkt). Live getestet: Account `olaf_olaf_son` wurde reaktiv per `/mod` in `sensitron`s Chat zum Mod gemacht, danach lieferte der Helix-Call korrekt `sensitron` in der moderierten-Channels-Liste, `GET /api/channels/knirpz/join` (nicht gemoddeter Channel) wurde dagegen korrekt mit `403` abgelehnt.

### 2026-07-25 — Access Token als Claim im verschlüsselten Auth-Cookie, nicht in Postgres persistiert

**Betrifft:** `src/EmotePurge.Api/Auth/TwitchClaimTypes.cs` · `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs`

**Access Token als Claim im verschlüsselten Auth-Cookie, nicht in Postgres persistiert — 2026-07-25.** B.2 verlangt einen *live* Rollen-Check pro Aktion (nicht nur einen Snapshot beim Login), Redis-Cache nur zur Rate-Limit-Schonung (`Auth:ModCheckCacheTtlMinutes`, Default 10 Min). Dafür muss der Twitch-Access-Token über die Cookie-Session hinweg verfügbar bleiben. Statt einer neuen DB-Spalte wird er als zusätzlicher Claim im ASP.NET-Core-Cookie mitgeführt — das Cookie ist über Data Protection ohnehin verschlüsselt/signiert, keine neue Krypto-Logik nötig. Das "Zero-Knowledge für Schreib-Tokens"-Prinzip aus `Architectur.md` (Grundsatz 4) bezieht sich explizit auf 7TV-Tokens, nicht auf Twitch — kein Widerspruch. Bewusst **kein** Refresh-Token gespeichert (explizite Nutzerentscheidung): Twitch-Access-Tokens laufen nach ~4h ab; danach schlägt nur der Live-Moderator-Check fehl (Re-Login nötig), Admin-/Broadcaster-Pfad bleiben unbegrenzt gültig, da sie den Token gar nicht brauchen. Falls das im Alltag stört, ist Refresh-Handling in `ChannelAccessService` nachrüstbar.

### 2026-07-25 — Autorisierung über `IEndpointFilter` statt ASP.NET-Core-Policy/`IAuthorizationHandler`

**Betrifft:** `src/EmotePurge.Api/Auth/ChannelManagementAuthorizationFilter.cs`

**Autorisierung über `IEndpointFilter` statt ASP.NET-Core-Policy/`IAuthorizationHandler` — 2026-07-25.** Die Entscheidung "Admin ODER Broadcaster ODER Moderator dieses Channels" ist eine Resource-based-Authorization-Frage, für die ASP.NET Core eigentlich `IAuthorizationHandler<TRequirement,TResource>` vorsieht — das ist aber auf typisierte Resources und meist MVC-Action-Filter zugeschnitten; in einer Minimal API ohne Controller ist die Route (ein reiner String, `channelName`) die einzige "Resource", und `IEndpointFilter` ist dafür das direktere, projektkonsistentere Werkzeug (analog zur "kein Controller"-Konvention). `ChannelManagementAuthorizationFilter` liest Claims + Route-Value, ruft `IChannelAccessService.CanManageChannelAsync` (Core-Interface, Infrastructure-Implementierung, folgt dem etablierten Service-Layer-Muster) über `HttpContext.RequestServices` (nicht Konstruktor-Injection — garantiert korrektes Request-Scoping unabhängig davon, wie der Filter selbst instanziiert wird) und gibt bei Ablehnung `Results.Forbid()` (`403`) zurück.

### 2026-07-25 — Admin-Allowlist als Config-Array (`Auth:AdminTwitchLogins`), nicht als DB-Tabelle/`User.IsAdmin`-Flag

**Betrifft:** `src/EmotePurge.Api/appsettings.json` · `src/EmotePurge.Infrastructure/Services/ChannelAccessService.cs`

**Admin-Allowlist als Config-Array (`Auth:AdminTwitchLogins`), nicht als DB-Tabelle/`User.IsAdmin`-Flag — 2026-07-25.** Aktuell nur ein Admin (`sensitron`), trivial per `appsettings.json` bzw. `Auth__AdminTwitchLogins__0`-Env-Override pflegbar, ohne Migration. Bewusst minimal — eine DB-gestützte Admin-Verwaltung (mehrere Admins, Admin-UI) ist der naheliegende nächste Schritt, sobald das gebraucht wird, aber keine Vorab-Komplexität für einen einzelnen bekannten Admin.

### 2026-07-25 — `dotnet user-secrets` für lokale Twitch-Client-Credentials, `.env`/Docker-Compose-Env für den Container-Fall

**Betrifft:** `src/EmotePurge.Api/EmotePurge.Api.csproj` · `.env.example` · `docker-compose.yml`

**`dotnet user-secrets` für lokale Twitch-Client-Credentials, `.env`/Docker-Compose-Env für den Container-Fall — 2026-07-25**, neue Konvention für dieses Repo. Anders als Postgres/Redis-Passwörter (unkritische, committete Local-Dev-Defaults) ist ein Twitch-Client-Secret an die persönliche, echte Twitch-App-Registrierung des Nutzers gebunden und darf nie ins Repo. Lokal: `dotnet user-secrets set "Auth:Twitch:ClientId"/"Auth:Twitch:ClientSecret"` im `EmotePurge.Api`-Projekt (Secrets liegen außerhalb des Repos unter `%APPDATA%\Microsoft\UserSecrets`). Docker: `TWITCH_CLIENT_ID`/`TWITCH_CLIENT_SECRET` in `.env` (gitignored, Platzhalter in `.env.example`), gemappt auf `Auth__Twitch__ClientId`/`ClientSecret`/`RedirectUri` im `api`-Service in `docker-compose.yml`.

### 2026-07-25 — Modul C: Score-Formel normalisiert Chat-Nutzung relativ zum Channel statt roher Summe

**Betrifft:** `src/EmotePurge.Infrastructure/Services/VoteSessionQueryService.cs`

**Modul C: Score-Formel normalisiert Chat-Nutzung relativ zum Channel statt roher Summe — 2026-07-25.** `Architectur.md` lässt `f(Chat-Nutzung)` in `Score = f(Chat-Nutzung) + (Keep-Votes − Delete-Votes)` bewusst offen; Chat-Nutzung kann vierstellig sein, Votes bewegen sich meist im niedrigen Bereich — eine rohe Summe hätte Votes praktisch bedeutungslos gemacht. Entscheidung: Min-Max-Normalisierung der `UseCount`-Werte **relativ zu den anderen aktiven Emotes desselben Channels** (nicht global) auf 0–100 über den Session-Zeitraum (`VoteSession.StartedAt` bis `EndedAt ?? now`, via das unveränderte `IUsageStatQueryService.GetUsageTotalsAsync`), macht `NormalizedUsageScore` durch die Konstruktion vergleichbar mit typischen Vote-Deltas. Grenzfall `max == min` (u. a. alles ungenutzt) → `0` für alle statt Division durch Null. `VoteSessionQueryService.GetResultsAsync` liefert dabei **alle** aktiven Emotes des Channels, nicht nur die mit Votes — erfüllt nebenbei den älteren, separat vermerkten Wunsch nach einer "auch ungenutzte Emotes sichtbar machen"-Ansicht (s. Memory `project_needed_all_emotes_endpoint`).

### 2026-07-25 — `VIPs` in `AllowedRoles` wird beim Erstellen einer `VoteSession` mit `400` abgelehnt

**Betrifft:** `src/EmotePurge.Core/Services/IVoteSessionService.cs` · `src/EmotePurge.Infrastructure/Services/VoteSessionService.cs` · `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs`

**`VIPs` in `AllowedRoles` wird beim Erstellen einer `VoteSession` mit `400` abgelehnt — 2026-07-25, echte Twitch-API-Grenze, keine Design-Entscheidung.** Der einzige Helix-Weg, VIP-Status zu prüfen (`GET /helix/channels/vips`), verlangt das Token des **Broadcasters oder eines Mods** mit Scope `channel:read:vips` — anders als beim Mod-Check (`user:read:moderated_channels`, s. Modul B) gibt es keine Selbstauskunft, mit der ein beliebiger Voter seinen eigenen VIP-Status belegen könnte. `Subs` dagegen ist ein Selbstauskunfts-Endpoint (`GET /helix/subscriptions/user`, Scope `user:read:subscriptions`, `404` = bestätigt nicht abonniert) und wurde umgesetzt — Login-Scope in `Program.cs` entsprechend erweitert (jetzt 3 Scopes).

### 2026-07-25 — Mod-Check-Logik aus `ChannelAccessService` in eigenständigen `IModeratorCheckService`/`ModeratorCheckService` extrahiert

**Betrifft:** `src/EmotePurge.Infrastructure/Services/ModeratorCheckService.cs` · `src/EmotePurge.Api/Auth/ClaimsPrincipalExtensions.cs`

**Mod-Check-Logik aus `ChannelAccessService` in eigenständigen `IModeratorCheckService`/`ModeratorCheckService` extrahiert — 2026-07-25.** Modul C braucht denselben Cache→Live-Helix-Fallback-Check (Redis-Key `modcheck:{twitchUserId}:{channelName}`, `Auth:ModCheckCacheTtlMinutes`) sowohl für "darf Channel verwalten" (`ChannelAccessService`) als auch für "darf in einer Mods-Session abstimmen" (`VoteEligibilityService`) — 1:1 dieselbe Logik jetzt an einer Stelle, `ChannelAccessService` behält nur noch Admin-Allowlist + Broadcaster-Check und delegiert den Rest. Analog wurde das Claims→`TwitchPrincipalInfo`-Bauen aus `ChannelManagementAuthorizationFilter` in eine geteilte `Auth/ClaimsPrincipalExtensions.TryBuildTwitchPrincipal`-Extension-Method verschoben, die jetzt auch `VoteEligibilityFilter` nutzt.

### 2026-07-25 — Mods/Broadcaster dürfen in jeder Vote-Session ihres eigenen Channels immer abstimmen, unabhängig von `AllowedVoterRoles`

**Betrifft:** `src/EmotePurge.Infrastructure/Services/VoteEligibilityService.cs`

**Mods/Broadcaster dürfen in jeder Vote-Session ihres eigenen Channels immer abstimmen, unabhängig von `AllowedVoterRoles` — 2026-07-25, explizite Nutzerentscheidung, live im Test aufgefallen.** Ursprüngliche Implementierung prüfte `Broadcaster`/`Mods`-Bit strikt additiv wie `Everyone`/`Subs` (reine `[Flags]`-Bitmaske, keine Hierarchie) — eine "nur Subs"-Session hätte den eigenen Mod damit ausgeschlossen, es sei denn der Ersteller setzt explizit `Subs|Mods`. Beim Live-Test bewusst hinterfragt: Nutzer wollte, dass Mods/Broadcaster analog zu ihrer Berechtigung, die Session überhaupt zu erstellen/beenden (`ChannelManagementAuthorizationFilter`), auch immer mitstimmen dürfen. `VoteEligibilityService.EvaluateAsync` prüft Broadcaster-/Mod-Status jetzt **vor** und unabhängig von den gesetzten `AllowedVoterRoles`-Flags; nur `Everyone`/`Subs` bleiben weiterhin an ihr jeweiliges Flag gebunden. Live verifiziert: `olaf_olaf_son` wurde in einer reinen `Subs`-Session (`AllowedVoterRoles=2`) ohne Mod-Status korrekt mit `403` abgelehnt, mit Mod-Status (nach `/mod`, Cache manuell invalidiert) korrekt mit `200` zugelassen.

### 2026-07-25 — Redis-Mod-Cache ist symmetrisch "stale" in beide Richtungen — live beim Testen aufgefallen

**Betrifft:** `src/EmotePurge.Infrastructure/Redis/ModRoleCache.cs`

**Redis-Mod-Cache ist symmetrisch "stale" in beide Richtungen — live beim Testen aufgefallen, 2026-07-25.** Ein negativ gecachtes Ergebnis (`TryGetIsModeratorAsync` → `false`) bleibt für die volle TTL (Default 10 Min) bestehen, auch wenn der Nutzer zwischenzeitlich per `/mod` befördert wird — nicht nur der bereits dokumentierte Fall (entmoddeter Nutzer bleibt autorisiert), sondern auch umgekehrt (frisch gemoddeter Nutzer bleibt zunächst abgelehnt). Beim Live-Test reproduziert: `/unmod` → `/mod` unmittelbar danach lieferte weiterhin `403`, bis der Redis-Key manuell gelöscht wurde. Nutzer stufte das als unkritisch ein ("nicht so zeitkritisch mit dem mod/unmod") — bewusst nicht behoben, gleiche Kosten/Nutzen-Abwägung wie die bereits dokumentierte Cache-Design-Entscheidung aus Modul B.

### 2026-07-25 — Angular-Frontend (Modul D) lebt in `web/`, Sibling zu `src/`, kein eigenes Monorepo-Tool (Nx/Turborepo)

**Betrifft:** `web/` · `EmotePurge.slnx`

**Angular-Frontend (Modul D) lebt in `web/`, Sibling zu `src/`, kein eigenes Monorepo-Tool (Nx/Turborepo) — 2026-07-25.** `EmotePurge.slnx` referenziert ausschließlich Pfade unter `src/`, ein Top-Level-Sibling-Ordner erfordert dafür keine Änderung. Bewusst kein Monorepo-Tool eingeführt — zwei unabhängige Toolchains (`dotnet build`/`npm run build`), die nur an einer Stelle zusammenlaufen (Docker-Build-Stage, s. u.), rechtfertigen aktuell keine zusätzliche Komplexität.

### 2026-07-25 — Produktions-Topologie: Angular-Build wird von der Api als Static Files ausgeliefert, kein eigener Frontend-Container

**Betrifft:** `src/EmotePurge.Api/Dockerfile` · `src/EmotePurge.Api/Program.cs` · `web/proxy.conf.json`

**Produktions-Topologie: Angular-Build wird von der Api als Static Files ausgeliefert, kein eigener Frontend-Container — 2026-07-25.** Bewusste Abweichung vom sonst pro Service getrennten Docker-Compose-Muster (`api`/`worker` als eigene Container): Eine neue `web-build`-Stage in `src/EmotePurge.Api/Dockerfile` (Node-Image, `npm ci && npm run build`) kopiert den Angular-Build (`dist/web/browser/`) in die `final`-Stage nach `./wwwroot`; `Program.cs` fügt `UseStaticFiles()` (vor `UseAuthentication`, da Assets unauthentifiziert ladbar sein müssen) und einen `MapFallbackToFile("index.html")`-SPA-Fallback hinzu, mit einem vorgeschalteten `MapFallback("/api/{**rest}", ...)`, damit unbekannte `/api/*`-Pfade weiterhin echte `404`s bleiben statt der SPA-Shell (live verifiziert). Ergebnis: same-origin in Produktion, kein CORS nötig. Für `ng serve`-Dev-Betrieb übernimmt stattdessen `web/proxy.conf.json` (`/api` → `http://localhost:5151`) denselben Zweck, ebenfalls ohne CORS.

### 2026-07-25 — `web/.claude/CLAUDE.md` statt separatem `web/CLAUDE.md`

**Betrifft:** `web/.claude/CLAUDE.md`

**`web/.claude/CLAUDE.md` statt separatem `web/CLAUDE.md` — 2026-07-25.** Ursprünglich geplant, zwei vom Nutzer mitgebrachte Angular-Guidance-Dateien manuell in ein `web/CLAUDE.md` zu verschmelzen. Beim tatsächlichen `ng new`-Scaffold (Angular CLI 22, deutlich neuer als angenommen) stellte sich heraus, dass `--ai-config claude` genau diese Datei automatisch an `web/.claude/CLAUDE.md` generiert — inhaltlich 1:1 identisch mit einer der beiden mitgebrachten Dateien. Plan entsprechend angepasst: Generierte Datei bleibt Quelle der Wahrheit, nur um einen projektspezifischen Abschnitt ergänzt (Cookie-Auth-Modell, Dev-Workflow-Port-Falle 5151 vs. 8080, Service-Layer-Pflicht, Commit-Konvention) statt dupliziert.

### 2026-07-25 — `.mcp.json` registriert den Angular-CLI-eigenen MCP-Server (`ng mcp`)

**Betrifft:** `.mcp.json`

**`.mcp.json` registriert den Angular-CLI-eigenen MCP-Server (`ng mcp`) — 2026-07-25.** Vor dem Festschreiben verifiziert (`npx ng mcp --help` im Scaffold), kein geratenes Kommando. Pfad zeigt direkt auf `web/node_modules/@angular/cli/bin/ng.js`, damit der Server unabhängig vom Arbeitsverzeichnis auflösbar ist, aus dem Claude Code den MCP-Prozess startet.

### 2026-07-25 — Neuer Endpoint `GET /api/channels/{channelName}` (Join-Status-Read) hinter demselben `ChannelManagementAuthorizationFilter` wie `join`/`leave`

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Core/Services/IChannelService.cs`

**Neuer Endpoint `GET /api/channels/{channelName}` (Join-Status-Read) hinter demselben `ChannelManagementAuthorizationFilter` wie `join`/`leave` — 2026-07-25.** Bislang gab es keinen Read-Endpoint für den aktuellen `IsBotActive`-Status eines Channels (nur `POST join`/`DELETE leave`), den die neue Join/Leave-Dashboard-Seite aber braucht, um den richtigen Button-Zustand zu zeigen. `IChannelService` um `GetByNameAsync` (Core) / Implementierung mit `AsNoTracking()` (Infrastructure) ergänzt, da reiner Read anders als `JoinAsync`/`LeaveAsync`. Autorisierung bewusst identisch zu `join`/`leave` gewählt statt eines offeneren Musters — einziger Consumer ist aktuell das Management-Dashboard, kein treibender Use-Case für einen öffentlicheren Read.

### 2026-07-25 — OAuth-Callback (`GET /api/auth/twitch/callback`) leitet jetzt zurück ins Frontend statt eine nackte JSON-Seite zu zeigen

**Betrifft:** `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs` · `src/EmotePurge.Api/appsettings.Development.json`

**OAuth-Callback (`GET /api/auth/twitch/callback`) leitet jetzt zurück ins Frontend statt eine nackte JSON-Seite zu zeigen — 2026-07-25.** Bug, der beim Anschluss des Angular-Frontends auffiel: Der Callback endete bisher mit `Results.Ok(new { userInfo.Login, ... })` — für eine reine API ok, aber der Callback wird von Twitch per volle Browser-Navigation angesteuert (kein XHR), stranded den Nutzer also auf einer nackten JSON-Seite. Jetzt `Results.Redirect(configuration["Auth:Twitch:PostLoginRedirectUrl"] ?? "/")`, neue Config analog zu `Auth:Twitch:RedirectUri`: `http://localhost:4200/` in `appsettings.Development.json` (zurück in den laufenden `ng serve`), `/` (relativ, same-origin) in `docker-compose.yml`/`appsettings.json`.

### 2026-07-25 — `docker compose up -d <service>` (ohne `--build`) reused stillschweigend ein bereits vorhandenes, potenziell uraltes Image — live aufgefallen

**Betrifft:** `docker-compose.yml`

**`docker compose up -d <service>` (ohne `--build`) reused stillschweigend ein bereits vorhandenes, potenziell uraltes Image — live aufgefallen, 2026-07-25.** Der `worker`-Container lief mit einem Image vom 2026-07-24-Erst-Scaffold (noch der leere `dotnet new worker`-Platzhalter, minutenlang nur `"Worker running at: {time}"` geloggt) weiter, obwohl `Worker.cs` seit Modul A längst die echte Join-/7TV-Sync-/Chat-Matching-Logik enthält — sichtbares Symptom war ein über die Api erfolgreich gejointer Channel (`IsBotActive=true` in Postgres), dessen `usage-stats`-Endpoint aber dauerhaft leer blieb, weil der Worker den Channel nie wirklich auf Twitch IRC gejoint oder 7TV-synct hatte (0 `Emote`-Zeilen). Ursache: Der reguläre Dev-Loop läuft über den Dev-Container + VS-Code-F5-Debugger (s. oben), nicht über `docker compose up --build`; das Worker-Image war seit dem allerersten Scaffold nie neu gebaut worden, und ein bloßes `docker compose up -d worker` (ohne `--build`) baut nur, wenn **gar kein** Image mit dem Namen existiert — ein veraltetes wird klaglos weiterverwendet. Fix war ein expliziter `docker compose build worker` vor dem nächsten `up`. **Faustregel:** Nach Code-Änderungen an `Api`/`Worker` vor jedem `docker compose up -d <service>`-Test entweder `--build` mitgeben oder vorher explizit `docker compose build <service>` laufen lassen — `up` allein ist kein verlässliches Signal, dass der Container aktuellen Code enthält.

### 2026-07-25 — Zwei Angular-Bugs beim ersten Live-Test des Join/Leave-Dashboards gefunden

**Betrifft:** `web/src/app/features/`

**Zwei Angular-Bugs beim ersten Live-Test des Join/Leave-Dashboards gefunden — 2026-07-25.** Beide beheben grundsätzliche Fallstricke, die in späteren Modul-D-Slices (Voting-UI, Usage-Stats-UI) wieder auftreten könnten: (1) Der Client-seitige `Validators.pattern`-Regex für Channel-Namen wurde 1:1 vom Backend-Regex (`^[a-z0-9_]{4,25}$`) übernommen — das Backend lowercased den Wert aber *vor* dem Matching, während der Frontend-Validator den rohen Input prüft. Mixed-Case-Namen wie `HandOfBlood` (Twitch-Namen werden praktisch immer mit Großbuchstaben getippt) scheiterten dadurch still an der Client-Validierung, ohne dass überhaupt ein Request abgeschickt wurde. Fix: Frontend-Pattern auf `^[a-zA-Z0-9_]{4,25}$` gelockert (Normalisierung passiert ohnehin vor dem Request) — Faustregel für künftige Formulare: Client-Validatoren müssen die serverseitige *Normalisierung* mit einbeziehen, nicht nur die serverseitige Regex kopieren. (2) `<form (ngSubmit)="...">` auf einem `<form>`-Tag mit nur einem standalone `[formControl]` (kein `[formGroup]`, kein importiertes `FormsModule`) feuert **nie** — `ngSubmit` ist kein natives DOM-Event, sondern ein Output von `NgForm`/`FormGroupDirective`, das ohne eine der beiden Direktiven schlicht fehlt. Symptom war ein stiller Fallback auf das native Browser-Formular-Submit-Verhalten (Full-Page-Reload), das im Network-Tab wie "nichts passiert" aussah, weil ein Page-Reload das Network-Log leert (ohne "Preserve log"). Fix: `(submit)="onSubmit($event)"` mit explizitem `event.preventDefault()` statt `(ngSubmit)` — funktioniert unabhängig davon, ob ein `FormGroup` vorhanden ist.

### 2026-07-24 — `TwitchChatManager`: `OnDisconnected`/`OnReconnected`/`OnFailureToReceiveJoinConfirmation`/`OnConnectionError` von TwitchLib jetzt explizit behandelt

**Betrifft:** `src/EmotePurge.Worker/TwitchChatManager.cs`

**`TwitchChatManager`: `OnDisconnected`/`OnReconnected`/`OnFailureToReceiveJoinConfirmation`/`OnConnectionError` von TwitchLib jetzt explizit behandelt — 2026-07-24.** Vorher waren nur `OnConnected`/`OnJoinedChannel`/`OnLeftChannel`/`OnMessageReceived` verdrahtet. TwitchLib rejoint Channels nach einem Reconnect **nicht** automatisch — ohne `OnReconnected`-Handler wäre ein Channel nach einem erkannten Verbindungsabbruch dauerhaft stumm geblieben. `TwitchChatManager` trackt dafür jetzt intern `_joinedChannels` (`ConcurrentDictionary<string, byte>`) und rejoint bei `OnReconnected` alle getrackten Channels. `OnFailureToReceiveJoinConfirmation`/`OnConnectionError` waren komplett unbehandelt (fielen unter "Unbehandelter Opcode"-artiges Schweigen) — jetzt als `LogWarning` sichtbar, da ein stiller Join-Fehler sonst auch nach einem Reconnect stumm bliebe (nur bereits erfolgreich gejointe Channels werden für Rejoin getrackt). **Korrektur 2026-07-30:** Die Annahme „TwitchLib rejoint nicht automatisch" ist falsch. `TwitchClient._client_OnReconnected` (4.0.1) sendet den Handshake, rejoint alle Channels aus seiner eigenen Liste, leert diese Liste per `_joinedChannelManager.Clear()` und feuert **erst danach** unser `OnReconnected`; zusätzlich feuert `Handle004` bei jedem abgeschlossenen IRC-Handshake unser `OnConnected`. Weil die Liste vor unserem Handler geleert wird, greift auch die Duplikatsprüfung in `JoinChannelAsync` nicht mehr — jeder Reconnect setzte dadurch jeden JOIN doppelt ab (live auf Prod am 2026-07-29: 12 Join-Bestätigungen für 6 Channels). Seit 2026-07-30 rejoint `TwitchChatManager` deshalb nur noch auf einem **frischen** Client (Boot, Recreate), wo TwitchLibs Liste leer ist; alles andere deckt `EnsureJoinedAsync` im Minutentakt ab.

---

## Undatierte Einträge (Anfangsphase, Modul 1 und Modul A)

### undatiert — Emote-PK statt 7TV-ID

**Betrifft:** `src/EmotePurge.Core/Entities/Emote.cs` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs`

**Emote-PK statt 7TV-ID** — s. Abschnitt oben.

### undatiert — `redis:7.2-alpine` statt `redis:7-alpine`

**Betrifft:** `docker-compose.yml` · `docker-compose.prod.yml`

**`redis:7.2-alpine` statt `redis:7-alpine`** in `docker-compose.yml`: letzte BSD-lizenzierte Redis-Version vor dem Lizenzwechsel auf RSALv2/SSPL ab Redis 7.4.

### undatiert — `depends_on` mit `condition: service_healthy`

**Betrifft:** `docker-compose.yml` · `docker-compose.prod.yml`

**`depends_on` mit `condition: service_healthy`** + Healthchecks für `postgres`/`redis`: verhindert, dass `api`/`worker` starten und crashen, bevor die Datenbank überhaupt Verbindungen annimmt.

### undatiert — `Microsoft.OpenApi` explizit auf `2.7.5` gepinnt

**Betrifft:** `src/EmotePurge.Api/EmotePurge.Api.csproj`

**`Microsoft.OpenApi` explizit auf `2.7.5` gepinnt** in `EmotePurge.Api.csproj`: patcht GHSA-v5pm-xwqc-g5wc (Stack-Overflow-DoS bei zirkulären OpenAPI-`$ref`s), das transitiv über `Microsoft.AspNetCore.OpenApi 10.0.10` reinkommt.

### undatiert — Dev Containers statt Debugger-Attach an einen laufenden Produktions-Container

**Betrifft:** `.devcontainer/`

**Dev Containers statt Debugger-Attach an einen laufenden Produktions-Container**: `.devcontainer/` startet einen eigenen SDK-Container im selben Compose-Netzwerk wie `postgres`/`redis`; `api`/`worker` laufen dort nicht als vorgebautes Docker-Image, sondern direkt über den VS-Code-Debugger.

### undatiert — `.vscode/launch.json` und `tasks.json` sind von `.gitignore` ausgenommen

**Betrifft:** `.gitignore` · `.vscode/launch.json` · `.vscode/tasks.json`

**`.vscode/launch.json` und `tasks.json` sind von `.gitignore` ausgenommen** (`.vscode/*` + gezielte `!`-Ausnahmen), damit die Team-Debug-Config versioniert wird, während sonstige lokale VS-Code-Dateien ignoriert bleiben.

### undatiert — Commit-Konvention: Conventional Commits

**Betrifft:** _keine Datei — Konvention/Arbeitsweise_

**Commit-Konvention: Conventional Commits** (`feat:`, `fix:`, `chore:`, `docs:`, …), in mehreren logisch getrennten Commits statt einem Sammel-Commit pro Feature.

### undatiert — Minimal API statt Controllers in `EmotePurge.Api`

**Betrifft:** `src/EmotePurge.Api/Program.cs`

**Minimal API statt Controllers in `EmotePurge.Api`**: explizite Nutzerentscheidung. `AddControllers()`/`MapControllers()` (Template-Boilerplate ohne je genutzte Controller) entfernt, Endpoints direkt in `Program.cs`. `app.UseAuthorization()` ebenfalls entfernt — ohne `AddAuthorization()`/`[Authorize]`-Endpoints wirft es beim Start eine `InvalidOperationException`.

### undatiert — `Channel.TwitchChannelId` ist `string?` (nullable)

**Betrifft:** `src/EmotePurge.Core/Entities/Channel.cs` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs`

**`Channel.TwitchChannelId` ist `string?` (nullable)**: ohne Twitch-Auth (Modul B, noch nicht implementiert) kann die echte numerische Twitch-ID nicht aufgelöst werden; bei non-nullable Default (`""`) hätte der Unique-Index beim zweiten angelegten Channel einen Constraint-Verstoß ausgelöst (leerer String ≠ NULL für Unique-Indizes). Bleibt `null`, bis Modul B sie nachträgt.

### undatiert — TwitchLib.Client 4.0.1, anonyme/read-only Verbindung

**Betrifft:** `src/EmotePurge.Worker/TwitchChatManager.cs`

**TwitchLib.Client 4.0.1, anonyme/read-only Verbindung** (`new ConnectionCredentials()` ohne Parameter): kein Bot-Account/OAuth-Token nötig für Join+Chat-Lesen. Kann keine Nachrichten senden — für die aktuelle Bot-Funktionalität irrelevant. `net10.0` wird von TwitchLib.Client 4.0.1 explizit als Dependency-Group unterstützt. **Nachtrag 2026-07-30 — Skalierungsgrenze:** „Kein Bot-Account nötig" gilt nur bis etwa 20 getrackte Channels. Twitch erlaubt einer nicht-verifizierten Verbindung 20 JOINs pro 10 Sekunden; TwitchLib drosselt JOINs überhaupt nicht (sie umgehen den `ThrottlingService`, der nur Chat-Nachrichten betrifft) und feuert sie so schnell, wie Bestätigungen eintreffen — ~180 ms pro Channel, auf Prod gemessen. Ein Reconnect rejoint alle Channels am Stück, 20 Channels liegen damit exakt auf der Grenze. Die von uns selbst ausgelösten Joins sind seit 2026-07-30 in `TwitchChatManager.TryJoinAsync` auf 600 ms Abstand gedrosselt; TwitchLibs eigener Rejoin nach einem Reconnect lässt sich nicht drosseln. Ein **verifizierter Bot-Account** (2.000 JOINs/10 s) ist deshalb die vorgesehene Richtung vor einem größeren Ausbau — Alternative wäre Sharding über mehrere Clients, wobei ungeklärt ist, ob Twitch anonyme Verbindungen pro `justinfan`-Kennung oder pro IP limitiert.

### undatiert — Redis-Protokoll für Join-Kommandos

**Betrifft:** `src/EmotePurge.Core/Messaging/IRedisPublisher.cs` · `src/EmotePurge.Infrastructure/Redis/RedisPublisher.cs` · `src/EmotePurge.Worker/Worker.cs`

**Redis-Protokoll für Join-Kommandos**: Channel `channel:bot:commands`, Message-Format `JOIN:<channelName>` (reiner String-Präfix, kein JSON) — matcht `IRedisPublisher`/`IRedisSubscriber`s ohnehin string-basierte Signaturen.

### undatiert — `appsettings.json`-Default-Connection-Strings korrigiert

**Betrifft:** `src/EmotePurge.Api/appsettings.json` · `.env.example`

**`appsettings.json`-Default-Connection-Strings korrigiert**: zeigten seit Schritt 1 auf `Username=postgres;Password=postgres` (Postgres) bzw. kein Passwort (Redis) — passte nie zu den tatsächlichen `.env.example`-Credentials (`emotepurge`/`change-me`) bzw. zu `docker-compose.yml`s `--requirepass`. Erst bei der ersten echten DB-Migration aufgefallen. Jetzt auf `emotepurge`/`change-me` (Postgres) und `localhost:6379,password=change-me` (Redis) korrigiert, damit lokales `dotnet run` gegen `docker compose up postgres redis` ohne manuelles Override funktioniert.

### undatiert — Zeilenenden auf LF normalisiert

**Betrifft:** `.gitattributes` · `.editorconfig` · `.vscode/settings.json`

**Zeilenenden auf LF normalisiert**: `.gitattributes` (`* text=auto eol=lf`), `.editorconfig` und ein geteiltes `.vscode/settings.json` (`files.eol`) sorgen dafür, dass Repo-Inhalt und Checkout konsistent LF sind — vorher schrieb Windows-Checkout CRLF auf die Platte, obwohl im Repo bereits LF gespeichert war, was bei jedem `git add` Warnungen auslöste. `.vscode/settings.json` ist wie `launch.json`/`tasks.json` von `.gitignore` ausgenommen, da EOL-Konvention Team-weit gilt, nicht persönliche Editor-Einstellung ist.

### undatiert — `DELETE /api/channels/{channelName}` löscht die Zeile hart

**Betrifft:** `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs` · `src/EmotePurge.Infrastructure/Services/ChannelService.cs`

**`DELETE /api/channels/{channelName}` löscht die Zeile hart** (nicht nur `IsBotActive = false`) und published `LEAVE:<channelName>`. Ursprünglich damit begründet, dass `Emote`/`UsageStat` noch leer waren — das stimmt seit dem 7TV-Sync (Modul A.3) nicht mehr: ein Leave löscht jetzt kaskadierend alle synchronisierten `Emote`-Zeilen des Channels (`OnDelete: Cascade` in `AppDbContext`). Noch nicht revidiert; falls Emote-Historie über einen Leave hinweg erhalten bleiben soll, hier auf Soft-Deactivate (`IsBotActive = false`, Zeile bleibt) umstellen.

### undatiert — Service-Layer + Interfaces für DI/Testbarkeit

**Betrifft:** `src/EmotePurge.Core/Services/` · `src/EmotePurge.Infrastructure/Services/` · `src/EmotePurge.Worker/ITwitchChatManager.cs`

**Service-Layer + Interfaces für DI/Testbarkeit** (explizite Nutzeranforderung): `AppDbContext` darf nicht direkt aus Minimal-API-Handlern aufgerufen werden — dafür `IChannelService`/`ChannelService` (Interface in Core, Implementierung in Infrastructure, wie beim Redis-Pub/Sub-Muster) eingeführt, der die Join/Leave-Business-Logik kapselt. Ausdrücklich **kein** generisches Repository-Pattern über EF Core. Aus demselben Grund hat `TwitchChatManager` ein Interface (`ITwitchChatManager`), gegen das `Worker` sich verdrahtet — nicht die konkrete Klasse. Faustregel für neuen Code: Klassen mit nicht-trivialer Logik oder externen Abhängigkeiten (DB, Redis, TwitchLib, 7TV) bekommen ein Interface, reine Daten-/DTO-Typen nicht. Ausnahme: Jeder `BackgroundService`, der ausschließlich per `AddHostedService<T>()` läuft und nirgends injiziert wird, wird direkt als konkrete Klasse registriert, kein Interface — er wird nie durch eine andere Implementierung ersetzt oder direkt injiziert, nur vom Host verwaltet (Beispiele: `Worker`, `UsageFlushWorker`, `SevenTvPeriodicResyncWorker`, `TwitchConnectionWatchdog`, `WorkerHealthPublisher`).

### undatiert — Vor jedem `git commit` erst den Nutzer fragen

**Betrifft:** _keine Datei — Konvention/Arbeitsweise_

**Vor jedem `git commit` erst den Nutzer fragen** — gilt auch, wenn Code-Änderungen selbst ohne Rückfrage umgesetzt werden dürfen (z. B. nach freigegebenem Plan-Mode-Plan). Die Freigabe für Code-Edits deckt nicht automatisch den Commit-Schritt ab.

### undatiert — 7TV API v3, nicht v4

**Betrifft:** `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs`

**7TV API v3, nicht v4**: v4 existiert bereits als GraphQL-API (`https://7tv.io/v4/gql`, live verifiziert per Introspection). Ursprünglich auch damit begründet, dass nur v3 eine EventAPI/WebSocket hat — diese Begründung ist mit der Entfernung des WebSockets (s. u.) hinfällig, v3 bleibt aber weiterhin die genutzte Version für REST+GQL, ein Wechsel auf v4 wurde nicht weiterverfolgt.

### undatiert — Channel→7TV-Auflösung über 7TVs eigene GraphQL-Nutzersuche, nicht Twitch Helix API

**Betrifft:** `src/EmotePurge.Core/SevenTv/ISevenTvApiClient.cs` · `src/EmotePurge.Infrastructure/SevenTv/SevenTvApiClient.cs`

**Channel→7TV-Auflösung über 7TVs eigene GraphQL-Nutzersuche, nicht Twitch Helix API**: 7TVs REST-Lookup (`/v3/users/twitch/{id}`) akzeptiert nur die numerische Twitch-ID, keinen Usernamen; eine Twitch-App-Registrierung (Helix, Client-Credentials-Flow) wäre robuster (kein Risiko einer falschen 7TV-Fuzzy-Suche), wurde aber bewusst verschoben — `ISevenTvApiClient.ResolveTwitchUserIdAsync` ist als eigene, isolierte Interface-Methode geschnitten, ein späterer Wechsel auf Helix betrifft nur ihre Implementierung, nicht `SevenTvSyncService`/`Worker`.

### undatiert — `RefreshMatchCacheAsync` baut das `{Name → Id}`-Dictionary manuell mit `TryAdd` statt `ToDictionaryAsync`

**Betrifft:** `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs`

**`RefreshMatchCacheAsync` baut das `{Name → Id}`-Dictionary manuell mit `TryAdd` statt `ToDictionaryAsync`**: Ursprüngliche Annahme war, dass sich keine zwei aktiven Emotes desselben Channels denselben Namen teilen (7TV-Invariante) — live gegen den echten Channel `ronnyberger` (983 aktive Emotes) getestet, widerlegt: Das Set enthielt tatsächlich zwei Namenskollisionen (`LL`, `WW`, je 2x). `ToDictionaryAsync` hätte dort mit `ArgumentException` geworfen; da das aus dem fire-and-forget-Redis-Handler (s. nächster Punkt) aufgerufen wird, wäre das komplett unsichtbar gewesen — der Channel wäre gejoint, aber nie 7TV-synct/matched worden, ohne jede Fehlermeldung. Duplikate werden jetzt koalesziert (erster geladener Eintrag gewinnt) und mit `LogWarning` sichtbar gemacht statt zu crashen.

### undatiert — `RedisSubscriber.SubscribeAsync` fängt Exceptions aus dem Handler jetzt explizit ab und loggt sie

**Betrifft:** `src/EmotePurge.Infrastructure/Redis/RedisSubscriber.cs`

**`RedisSubscriber.SubscribeAsync` fängt Exceptions aus dem Handler jetzt explizit ab und loggt sie** (`HandleSafelyAsync`), statt sie als unobserved Task Exception stillschweigend verschwinden zu lassen. Der Handler-Aufruf ist weiterhin bewusst fire-and-forget (StackExchange.Redis-Callbacks sind synchron), aber ohne dieses Logging war jede unbehandelte Exception in einem `JOIN:`/`LEAVE:`-Handler faktisch unsichtbar — genau das hat den obigen Duplicate-Name-Bug beim ersten Live-Test vollständig verschluckt, bis er über einen leeren `usage-stats`-Response und manuelles DB-Nachschauen aufgefallen ist.

### undatiert — `IEmoteMatchCache`/`IUsageStatFlushService`/`IUsageStatQueryService` in Core+Infrastructure, `IEmoteUsageCounter` nur in `EmotePurge.Worker`

**Betrifft:** `src/EmotePurge.Core/Services/IEmoteMatchCache.cs` · `src/EmotePurge.Worker/IEmoteUsageCounter.cs`

**`IEmoteMatchCache`/`IUsageStatFlushService`/`IUsageStatQueryService` in Core+Infrastructure, `IEmoteUsageCounter` nur in `EmotePurge.Worker`**: Erstere überqueren die Infrastructure/Worker- bzw. Infrastructure/Api-Grenze (von `SevenTvSyncService` geschrieben, von `TwitchChatManager` gelesen bzw. von API-Endpoints gelesen) und folgen damit dem `IRedisPublisher`-Muster. `IEmoteUsageCounter` wird ausschließlich innerhalb von `EmotePurge.Worker` geschrieben (`TwitchChatManager`) und gelesen (`UsageFlushWorker`) — folgt daher stattdessen dem `ITwitchChatManager`-Muster (Worker-internes Interface, nicht in Core).

### undatiert — `UsageStatFlushService.FlushAsync` filtert verwaiste `EmoteId`s vorab statt die ganze Batch bei einem FK-Verstoß zu verlieren

**Betrifft:** `src/EmotePurge.Infrastructure/Services/UsageStatFlushService.cs`

**`UsageStatFlushService.FlushAsync` filtert verwaiste `EmoteId`s vorab statt die ganze Batch bei einem FK-Verstoß zu verlieren**: Ein Channel-Leave löscht `Emote`-Zeilen hart-kaskadierend (s. oben); liegt zu diesem Zeitpunkt noch ein gepufferter Usage-Count für eine gerade gelöschte Emote-Id vor, würde ein naiver Upsert die komplette 30-Sekunden-Batch (inkl. aller anderen, weiterhin aktiven Channels) an einem FK-Verstoß scheitern lassen. Stattdessen prüft ein günstiges `WHERE Id IN (...)` vorab, welche IDs noch existieren, verwirft nur die verwaisten (mit Sammel-Warnung geloggt) und upserted den Rest normal. Ein minimales TOCTOU-Restfenster zwischen Check und `SaveChangesAsync` bleibt bewusst unadressiert (extrem unwahrscheinlich, der `try/catch` in `UsageFlushWorker` fängt es als Fallback ohnehin ab).

### undatiert — `GET /api/channels/{channelName}/usage-stats`

**Betrifft:** `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs`

**`GET /api/channels/{channelName}/usage-stats`**: reiner Debug-/Verifikations-Endpoint für Modul A.2 (kein Teil der künftigen Voting-Engine, Modul C), damit sich der Batch-Flush ohne manuelles SQL testen lässt. Liefert `UsageStat`-Zeilen mit Emote-*Name* statt interner ID.

### undatiert — `UsageStat.Date`: `DateTime` → `DateOnly` (Postgres `timestamp with time zone` → `date`)

**Betrifft:** `src/EmotePurge.Core/Entities/UsageStat.cs` · `src/EmotePurge.Infrastructure/Migrations/20260724150000_ConvertUsageStatDateToDateOnly.cs`

**`UsageStat.Date`: `DateTime` → `DateOnly` (Postgres `timestamp with time zone` → `date`)**: Der Spaltenwert war schon immer nur Mitternacht UTC — reine Code-Konvention (Kommentar), nicht typsicher durchgesetzt. `DateOnly` macht "Kalendertag, keine Instant" compilerseitig verbindlich, spart Speicher (`date` = 4 Byte vs. `timestamptz` = 8 Byte) und macht Range-Vergleiche (`Date >= from && Date <= to`) eindeutig ohne Zeitzonen-/Uhrzeit-Anteil. Migration `ConvertUsageStatDateToDateOnly` castet dafür explizit `(("Date" AT TIME ZONE 'UTC')::date)` statt eines rohen `ALTER COLUMN ... TYPE date` — letzteres verlässt sich auf Postgres' `timestamptz→date`-Assignment-Cast, der von der **Session-Timezone** abhängt (in `docker-compose.yml` nicht auf UTC gepinnt) und Zeilen sonst potenziell um einen Tag verschieben könnte.

### undatiert — `GET /api/channels/{channelName}/usage-stats/totals?from=&to=` als neuer, zusätzlicher Endpoint statt Erweiterung von `usage-stats`

**Betrifft:** `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs` · `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs`

**`GET /api/channels/{channelName}/usage-stats/totals?from=&to=` als neuer, zusätzlicher Endpoint statt Erweiterung von `usage-stats`**: Liefert pro Emote die über einen beliebigen Zeitraum aufsummierte `UseCount` (`IUsageStatQueryService.GetUsageTotalsAsync`) — Grundlage für ein künftiges Dashboard mit Tag-/Woche-/Monat-/Custom-Zeiträumen sowie später den "Chat-Nutzung"-Term im Modul-C-Score. Der bestehende `usage-stats`-Endpoint bleibt bewusst unangetastet (explizit als Debug-Endpoint dokumentiert, s.o.), statt seine Semantik/JSON-Form zu verändern. `from`/`to` sind reine ISO-Datums-Query-Parameter (`yyyy-MM-dd`); Range-Presets wie "letzte 7 Tage"/"dieser Monat" sind UX-Konzepte und bleiben bewusst im künftigen Angular-Frontend, nicht im Backend. Serverseitiges Cap von 366 Tagen als billige Absicherung gegen Tippfehler. Die Query gruppiert bewusst nach `EmoteId` (nicht nur `Emote.Name`) — s. oben dokumentierte Namenskollisionen (`ronnyberger`: `LL`, `WW` je 2x) hätten bei reiner Namensgruppierung die Usage unterschiedlicher Emotes fälschlich zusammengeführt. Die Tages-Granularität von `UsageStat` bleibt dabei unverändert (kein Rollup/keine feinere Granularität nötig) — ein Zeitraum ist einfach `SUM(UseCount) WHERE Date BETWEEN from AND to`, effizient beantwortet über den bestehenden `(EmoteId, Date)`-Index (jetzt mit `UseCount` als Include-Spalte für Index-Only-Scans).

### undatiert — `UsageStatQueryService.GetUsageTotalsAsync`: Emote-IDs des Channels vorab separat laden statt direkt auf `UsageStats` mit Navigations-Join zu gruppieren

**Betrifft:** `src/EmotePurge.Infrastructure/Services/UsageStatQueryService.cs`

**`UsageStatQueryService.GetUsageTotalsAsync`: Emote-IDs des Channels vorab separat laden statt direkt auf `UsageStats` mit Navigations-Join zu gruppieren**: Ein `GroupBy`+`Sum` auf einer per `Emote.Channel.ChannelName` gefilterten Query ließ sich von EF Core/Npgsql nicht übersetzen (`InvalidOperationException`, fiel auf Client-Eval `g.AsQueryable().Sum(...)` zurück und warf) — reproduzierbar unabhängig davon, ob der GroupBy-Key `EmoteId` alleine oder `{EmoteId, Name}` war. Fix: Erst `Emotes` nach `ChannelName` filtern und die IDs in eine einfache Liste laden, danach `UsageStats.Where(u => emoteIds.Contains(u.EmoteId))` rein skalar gruppieren (kein Navigations-Join mehr im GroupBy-Query) — das übersetzt sauber. Bei ähnlichen Aggregat-Queries in `EmotePurge.Infrastructure` auf dasselbe Muster achten: Navigations-Joins vor einem `GroupBy` erst auf eine skalare ID-Liste reduzieren, nicht direkt darauf gruppieren.

### undatiert — `AddAuthorization()` für Modul B wieder eingeführt

**Betrifft:** `src/EmotePurge.Api/Program.cs`

**`AddAuthorization()` für Modul B wieder eingeführt** (war beim Controller-Removal komplett entfernt, s. oben) — jetzt notwendig für `.RequireAuthorization()` auf den Channel-Endpoints. Cookie-Auth-Defaults (`Events.OnRedirectToLogin`/`OnRedirectToAccessDenied`) wurden auf reine `401`/`403`-Statuscodes umgebogen, da Cookie-Auth per Default sonst auf eine (nicht existierende) Login-Seite redirected — für eine JSON-API falsch, live verifiziert (`401` ohne Cookie, kein `302`).
