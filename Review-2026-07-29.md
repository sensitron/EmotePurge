# Code-Review EmotePurge — 29.07.2026

Konsolidiert aus sechs unabhängigen Review-Spuren: Security & Autorisierung (Track 1), Worker-Nebenläufigkeit (Track 2a), Datenintegrität & Mass-Delete (Track 2b), UX/Usability/Accessibility (Track 3), Architektur & Wartbarkeit (Track 4), Infrastruktur/CI/Betrieb (Track 5). Die Severity-Skala der einzelnen Tracks wurde nachträglich vereinheitlicht; jede geänderte Einstufung ist beim Befund begründet.

---

## 1. Executive Summary

Das Projekt ist deutlich reifer, als die Länge dieser Befundliste vermuten lässt. Eine strenge CSP, Rate Limiting, ein sauber fail-closed gebauter OAuth-State-Schutz, Non-Root-Container, Testcontainers-Integrationstests gegen echtes Postgres/Redis, ein Core-Projekt mit null Paketreferenzen und ein außergewöhnlich gründliches Entscheidungslog sind bereits da. Track 1 hat den zentralen Sicherheitsanspruch explizit nachverfolgt und bestätigt: **Ein Mod oder 7TV-Editor von Channel A kann nichts mit Channel B tun** — es gibt keine Filter/Handler-Divergenz, jede Session-, Emote- und Vote-Abfrage ist an `ChannelId` gebunden.

Die Lücken liegen eine Ebene tiefer: nicht darin, ob etwas erlaubt ist, sondern darin, was passiert, wenn etwas schiefgeht — ein abgebrochener Löschlauf, ein toter TwitchClient, ein fehlgeschlagener Backend-Call, ein verlorenes Docker-Volume. Genau diese Klasse trifft der geplante HandOfBlood-Stresstest zuerst.

**Die fünf wichtigsten Punkte:**

1. **Zwei unabhängige Wege zu unwiederbringlichem Datenverlust:** „Channel verlassen" löscht hart und kaskadierend die gesamte Historie (S1-1), und es existiert kein Postgres-Backup (S1-2) — der eine Fix ist ein Einzeiler, der andere ein Cronjob.
2. **Der Mass-Delete kann Emotes löschen, die niemand ausgewählt hat:** Die Auswahl driftet nach Sortier-/Refetch-Vorgängen still von der Anzeige ab (S1-3), und „Löschen starten" ist klickbar, bevor die Fremd-Set-Prüfung antwortet (S1-4). Beides sind S-Aufwand-Fixes.
3. **Die Ursache der Prod-Ausfälle ist gefunden und liegt nicht im eigenen Code:** TwitchLibs `ReconnectionPolicy` erlaubt per Default 10 Verbindungsversuche pro Client-Instanz und setzt den Zähler bei Reconnects nie zurück (S2-1). Der gesamte Verbindungs-Cluster S2-1 bis S2-8 sollte vor dem Stresstest erledigt sein.
4. **Data-Protection-Keys und das Cookie-Secure-Flag müssen zusammen angefasst werden** (S2-9, S2-10): Der naheliegende Einzelfix für die Keys entfernt den letzten vorhandenen Session-Notaus.
5. **Vor dem Launch fehlen vier billige Dinge:** Rechtstexte (S2-20), Ressourcenlimits (S2-21), Mindest-Monitoring (S3-36) und das Backup (S1-2) — zusammen ein Abend, und `robots.txt` bleibt bis dahin so, wie sie ist.

---

## 2. Befundübersicht

**Verteilung:** 4 × S1 · 21 × S2 · 38 × S3 · 18 × S4 = **81 Befunde**

### S1 — Kritisch

| ID | Titel | Bereich | Aufwand |
|---|---|---|---|
| S1-1 | Channel-Leave löscht hart und kaskadierend die gesamte Channel-Historie | Daten/Security | S–M |
| S1-2 | Keine Backup-Strategie für Postgres | Betrieb | S–M |
| S1-3 | Auswahl driftet still von der angezeigten Liste ab (Sortierung und Refetch) | Frontend/Daten | S–M |
| S1-4 | „Löschen starten" ist klickbar, bevor die Shared-Set-Prüfung antwortet | Frontend/Daten | S |

### S2 — Hoch

| ID | Titel | Bereich | Aufwand |
|---|---|---|---|
| S2-1 | TwitchLib-`ReconnectionPolicy`: 10 Versuche pro Client-Instanz, danach dauerhaft tot | Worker | S–M |
| S2-2 | Watchdog ist blind, solange nie eine Chat-Nachricht ankam → absorbierender Totzustand | Worker | S |
| S2-3 | `_joinedChannels` trackt nur bestätigte Joins → fehlgeschlagener Join wird nie wiederholt | Worker | S–M |
| S2-4 | `ConnectAsync()`-Rückgabewert an beiden Aufrufstellen verworfen | Worker | S |
| S2-5 | Boot-Recovery ohne Fehlerisolierung → Crash-Loop des ganzen Worker-Prozesses | Worker | S–M |
| S2-6 | Resync-Channel-Query außerhalb jedes try/catch → Postgres-Aussetzer beendet den Prozess | Worker | S |
| S2-7 | `_isConnected` beim Recreate nie zurückgesetzt → Health-Endpoint meldet „connected" auf totem Client | Worker | S |
| S2-8 | Fire-and-forget-Redis-Handler geben die Reihenfolgegarantie auf | Worker/Infra | M |
| S2-9 | Data-Protection-Keys flüchtig **und** kein serverseitiger Session-Widerruf | Security/Betrieb | M |
| S2-10 | Cookie-Secure-Flag und TLS-Durchsetzung hängen fail-open an einem Proxy-Header | Security | S |
| S2-11 | Autorisierung löst ungecachte, ungeratelimitete Fremd-API-Aufrufe aus | Security/Architektur | M |
| S2-12 | `sync-deleted` ohne Error-Handler; optimistisches UI-Update feuert trotzdem | Frontend/Daten | M |
| S2-13 | Kein HTTP-Interceptor: 401-Duplikat, zwei Fehler-Mappings, 3–4 redundante Requests pro Navigation | Frontend/Architektur | S–M |
| S2-14 | Erstnutzung zeigt 1.000 × „0x" ohne Erklärung — lädt zur Totallöschung ein | UX | M |
| S2-15 | Namensfilter ist Exakt-Match statt Teilstring-Suche | UX | S |
| S2-16 | Jeder Filterwechsel löscht die komplette Auswahl; es gibt kein „Alle auswählen" | UX | M |
| S2-17 | Voll-Refetch aller ~1.000 Emote-Ergebnisse nach jedem einzelnen Vote | UX/Performance | M |
| S2-18 | Dialog behauptet „Set gehört nicht diesem Channel", obwohl die Prüfung nur fehlschlug | UX/Daten | S |
| S2-19 | „Beenden" und „Löschen" als gleichfarbige Nachbarn; Bestätigung ohne Session-Bezug | UX/Daten | S |
| S2-20 | Fehlende Rechtstexte vor öffentlichem Launch | Recht/Betrieb | S–M |
| S2-21 | Keine Ressourcenlimits für `api`/`worker` auf einem geteilten VPS | Betrieb | S |

### S3 — Mittel

| ID | Titel | Bereich | Aufwand |
|---|---|---|---|
| S3-1 | Global-Admin ist im Voting-Pfad kein Manager — zwei divergierende Definitionen | Security | S |
| S3-2 | Channel-Identität hängt am mutablen Twitch-Login statt an der Twitch-User-ID | Security | M |
| S3-3 | Roher, ungeprüfter `channelName` wird zum Redis-Key; Redis ohne `maxmemory` | Security/Betrieb | S |
| S3-4 | Vote-Ergebnisse liefern rohe Nutzungszahlen an jeden Everyone-Teilnehmer | Security | M |
| S3-5 | Die Autorisierungs-Services sind die einzigen Infrastructure-Services ohne Tests | Tests | S–M |
| S3-6 | Worker-Zustandsmaschine (308 Zeilen, 4 Zähler) vollständig ungetestet | Tests | M |
| S3-7 | Zwei gleichzeitige Votes laufen in den Unique-Index → 500; kein globales Exception-Handling | Backend | S |
| S3-8 | Laufender Löschlauf: kein `beforeunload`, kein globaler Fortschritt, kein Abbruch von außen | Frontend | S–M |
| S3-9 | Löschlauf bricht bei keinem systemischen Fehler ab und nutzt einen verworfenen Token weiter | Frontend | S |
| S3-10 | `setId` kann beliebig veraltet sein und wird vor dem Lauf nie revalidiert | Frontend/Daten | S |
| S3-11 | Watchdog nutzt Chat-Nachrichten als einzigen Lebendbeweis | Worker | M |
| S3-12 | Erfolgreiche 7TV-Antwort mit leerem Set archiviert alles und leert den Match-Cache | Worker/Daten | S |
| S3-13 | `UsageStatFlushService` macht Read-then-Insert; ein Konflikt verwirft den kompletten Batch | Worker/Daten | M |
| S3-14 | Log-Zeile beim Recreate ist im proaktiven Pfad sachlich falsch | Worker | S |
| S3-15 | Emote-Grid und Vote-Buttons sind per Tastatur/Screenreader nicht bedienbar | A11y | M–L |
| S3-16 | Dialoge ohne Fokusfalle/Escape, Statusmeldungen ohne `aria-live` | A11y | S |
| S3-17 | Formularfelder ohne Label; Placeholder- und Sekundärtext unter AA-Kontrast | A11y | S |
| S3-18 | Ausgewählte Emote-Karten haben keinen sichtbaren Auswahlzustand | UX | S |
| S3-19 | Guards leiten stumm um; während der Guard-Probe passiert sichtbar nichts | UX | S |
| S3-20 | Spaltenzahl folgt `window.innerWidth`, der Container ist auf 1024 px gedeckelt | UX | M |
| S3-21 | Karten auf der Voting-Seite wirken für reine Voter klickbar, tun aber nichts | UX | S |
| S3-22 | Logout ohne Fehlerbehandlung — bei Fehlschlag passiert überhaupt nichts | UX/Security | S |
| S3-23 | Vote-Session-Liste zeigt weder Zielgruppe noch Zeitraum noch Beteiligung | UX | S |
| S3-24 | 7TV-Token lässt sich nicht vorab hinterlegen | UX | S |
| S3-25 | Ergebnis eines Löschlaufs ist nicht abschließbar; `reset()` wird nie aufgerufen | UX | S |
| S3-26 | Channel-/Session-Lookup sechsmal kopiert; `Normalize` an 27 Stellen ausgeschrieben | Architektur | S |
| S3-27 | `Architectur.md` widerspricht dem Code; `CLAUDE.md` (93 KB) seit drei Features ungepflegt | Doku | M |
| S3-28 | Worker greift zweimal direkt auf `AppDbContext` zu; kein automatischer Schichten-Check | Architektur | S |
| S3-29 | Fehlercode-Kontrakt lebt manuell synchron in vier Dateien | Architektur | S |
| S3-30 | `effect()` als Datenlader: eine Stelle feuert jeden Request doppelt | Frontend | S–M |
| S3-31 | Validierung doppelt in Endpoint und Service, mit divergenten Ausfallmodi | Backend | S |
| S3-32 | 7TV-Editor-Auflösung an zwei Stellen unabhängig implementiert, ungecacht | Architektur | S |
| S3-33 | `web/tsconfig.json` ohne `strict` — das Aktivieren kostet nachweislich nichts | Frontend | S |
| S3-34 | Migrationen rein manuell, kein Schutz gegen Schema-Drift | Betrieb | S |
| S3-35 | Keine Healthchecks für `api`/`worker`; `restart` greift nur bei Prozess-Exit | Betrieb | S–M |
| S3-36 | Keine strukturierten Logs, keine Metriken, kein Alerting | Betrieb | S |
| S3-37 | CI testet erst nach dem Merge auf `main` | CI | S |
| S3-38 | Identischer Projekt-/Container-/Volume-Name in beiden Compose-Dateien | Betrieb | S |

### S4 — Niedrig

| ID | Titel | Bereich | Aufwand |
|---|---|---|---|
| S4-1 | Filterkommentar und Entscheidungslog beschreiben die `sync-deleted`-Absicherung falsch | Doku/Security | S |
| S4-2 | `GET /api/worker/health` ist der einzige Endpoint ohne Auth und ohne Rate Limit | Security | S |
| S4-3 | CSP ohne `form-action`; `style-src 'unsafe-inline'` als letzter Aufweichungspunkt | Security | S |
| S4-4 | `DrainAndReset` vs. `Increment`: schmales Verlustfenster | Worker | S |
| S4-5 | `_connected` nicht-volatil, nie zurückgesetzt, redundant | Worker | S |
| S4-6 | Alter `WebSocketClient` wird beim Recreate nicht disposed | Worker | S |
| S4-7 | Kein sauberer IRC-Disconnect beim Shutdown | Worker | S |
| S4-8 | `DecimalPipe` formatiert Scores immer nach en-US | Frontend | S |
| S4-9 | Untranslierter Placeholder im Admin-Formular | Frontend | S |
| S4-10 | `ROW_HEIGHT_PX` muss von Hand zur Tailwind-Höhenklasse passen | Frontend | S |
| S4-11 | `WorkerHealthEndpoints` greift direkt auf Redis zu und parst das Payload nach | Architektur | S |
| S4-12 | Verwaiste Reste aus entfernten Features und stale Datei-Verweise | Aufräumen | S |
| S4-13 | `Worker` fehlt in der dokumentierten `BackgroundService`-Ausnahmeliste | Doku | S |
| S4-14 | Sprachmischung ist systematisch, aber nirgends als Regel festgehalten | Doku | S |
| S4-15 | GitHub Actions auf Floating-Tags statt SHA gepinnt | CI | S |
| S4-16 | Fehlende CI-Hygiene: kein Lint/Format/Audit, kein NuGet-Cache, kein Dependabot | CI | S |
| S4-17 | Dockerfiles: überflüssige `build`-Stage und `COPY . .` vom Repo-Root | Betrieb | S |
| S4-18 | Repo-Hygiene: kein README/LICENSE, leeres `marketing/` | Aufräumen | S |

---

## 3. Befunde im Detail

### S1 — Kritisch

> **S1-1 — Channel-Leave löscht hart und kaskadierend die gesamte Channel-Historie**
> **S1** · Daten/Security · **S** (Soft-Deactivate) / **M** (inkl. Purge-Aktion + Dialog) · Gefunden von Track 1 (B1), Track 2b (B7), Track 3
> **Ort**: `src/EmotePurge.Infrastructure/Services/ChannelService.cs:44-45` · `src/EmotePurge.Infrastructure/Persistence/AppDbContext.cs:29-32,43-46,58-61,69-77` · `src/EmotePurge.Api/Endpoints/ChannelEndpoints.cs:77-90` · `src/EmotePurge.Infrastructure/Redis/ModRoleCache.cs:20` · `web/src/app/features/channel-workspace/channel-workspace-layout.ts:114-132`
> **Was**: `LeaveAsync` führt `db.Channels.Remove(channel)` aus — einen echten Hard-Delete. Daran hängen vier Cascade-Kanten: `Channel→Emote`, `Emote→UsageStat`, `Channel→VoteSession`, `VoteSession→Vote` und `Emote→Vote`. Der Endpoint steht hinter `ChannelManagementAuthorizationFilter`, der Mods gleichrangig mit Broadcaster und Admin zulässt; der positiv gecachte Mod-Status überlebt ein `/unmod` bis zu 10 Minuten (`ModRoleCache.cs:18-20`). Der einzige weitere Schutz ist ein natives `window.confirm` mit vorfokussiertem OK-Button.
> **Warum es zählt**: Broadcaster `vassilly` läuft seit Wochen mit EmotePurge. Ein Mod wird nach einem Streit entmoddet und setzt innerhalb der nächsten 10 Minuten `DELETE https://emotepurge.app/api/channels/vassilly` mit seinem Session-Cookie ab — das UI-Confirm ist dabei irrelevant. Weg sind: alle Emote-Zeilen, die komplette Tages-Nutzungsstatistik seit Beitritt, alle Vote-Sessions und alle abgegebenen Stimmen. Nichts davon ist rekonstruierbar: 7TV liefert beim Rejoin nur das aktuelle Set, und die Quelle der Nutzungsdaten (Twitch-Chat in Echtzeit) ist nicht nachträglich abfragbar. Für HandOfBlood wäre das ein Fehlklick + Enter für ~1.000 Emote-Zeilen und alle Community-Votes. Dass `UsageStatFlushService.FlushAsync` (`:23-33`) verwaiste `EmoteId`s vorfiltern muss, existiert ausschließlich, um diese Kaskade zu überleben. `CLAUDE.md:121` benennt das Problem selbst („Noch nicht revidiert"); die damalige Begründung („`Emote`/`UsageStat` waren noch leer") ist seit Modul C/D doppelt überholt.
> **Fix**: (1) `LeaveAsync` auf `channel.IsBotActive = false; await db.SaveChangesAsync(); await redisPublisher.PublishAsync(CommandsChannel, $"LEAVE:{normalized}")` umstellen — `SevenTvPeriodicResyncWorker.cs:36-39` und `Worker.cs:27` filtern bereits auf `IsBotActive`, `JoinAsync` reaktiviert bereits. (2) Falls echtes Löschen gebraucht wird: eigener Endpoint hinter `GlobalAdminAuthorizationFilter`, plus eine dritte Stufe `CanDeleteChannelAsync` in `ChannelAccessService`, die nur Admin + Broadcaster per String-Vergleich zulässt (kein Redis-Cache, also kein Stale-Fenster). (3) Frontend: `window.confirm` durch das bestehende `DeleteConfirmDialog`-Muster ersetzen — konkrete Zahlen nennen („#handofblood: 987 Emotes, 41 Tage Nutzungsdaten, 6 Abstimmungen mit 2.318 Stimmen"), Eingabe des Channel-Namens verlangen, Bestätigen erst danach aktivieren.

> **S1-2 — Keine Backup-Strategie für Postgres**
> **S1** · Betrieb · **S** (lokal) / **M** (mit Off-Site) · Gefunden von Track 5 (I1)
> **Ort**: `docker-compose.yml:36-37`, `docker-compose.prod.yml:33-34` (`postgres-data:/var/lib/postgresql/data`, `driver: local`); kein Backup-Job im Repo
> **Was**: Der komplette Datenbestand (Channels, Emotes, UsageStats, VoteSessions, User-Logins) liegt ausschließlich in einem lokalen Docker-Volume auf dem VPS. Keine Datei im Repo erzeugt einen Dump.
> **Warum es zählt**: `docker compose down -v` (Tippfehler), VPS-Crash, versehentliches Postgres-Major-Upgrade oder Hosting-Zwischenfall = alles unwiederbringlich weg. Ohne Monitoring (S3-36) würde ein defektes Volume erst auffallen, wenn längst wieder geschrieben wurde. Dieser Befund ist außerdem der Grund, warum S1-1 keinen Notausgang hat.
> **Fix**: Host-Cronjob (nicht im Container), nächtlich, rotierend:
> ```
> 0 3 * * * root docker exec emotepurge-postgres pg_dump -U emotepurge emotepurge | gzip > /var/backups/emotepurge/emotepurge-$(date +\%F).sql.gz
> 5 3 * * * root find /var/backups/emotepurge -mtime +14 -delete
> ```
> Ein lokaler Dump schützt nicht vor VPS-Totalverlust — zusätzlich täglich per `rclone`/`rsync`/`scp` auf ein zweites Ziel (Backblaze B2 Free Tier o. ä.).

> **S1-3 — Auswahl driftet still von der angezeigten Liste ab (zwei unabhängige Wege)**
> **S1** · Frontend/Daten · **S** (beide Einzelfixes) / **M** (robuste Variante) · Gefunden von Track 2b (B2), Track 3
> **Ort**: `web/src/app/shared/selection/list-selection.ts:22,28-32` · `web/src/app/features/usage-stats/usage-stats-page.ts:104-106` · `web/src/app/features/voting/vote-session-detail-page.ts:137-143,156-158,178`
> **Was**: Zwei Mechanismen, dieselbe Folge. **(a) Index-Anker:** `onRowClick` merkt `lastIndex` als Positionsindex in `items()`, der Shift-Bereich ist `items().slice(start, end+1)`. `toggleSort()` ändert die komplette Reihenfolge **ohne** `selection.clear()`; über `EmoteUsageFilter` ist der Fall sauber abgedeckt (`emote-usage-filter.ts:38,55,61,66,77`), über die Sortierung nicht. **(b) Objektidentität:** `ListSelection` hält Objekt-Referenzen (`selectedSet().has(item)`). `vote()` ruft `load({freeze:false})`, dessen `next`-Handler `results` durch frisch deserialisierte Objekte ersetzt — anders als `usage-stats-page.ts:125` **ohne** `clear()`. Danach ist `isSelected(neuesObjekt)` für alle `false`, `selectedForDelete()` mappt aber weiterhin die alten Objekte auf gültige `emoteId`s.
> **Warum es zählt**: (a) 400 Emotes absteigend nach Nutzung: Mod klickt Position 380, kehrt die Sortierung um, Shift-klickt Position 399. Erwartet: die 20 unbenutztesten. Tatsächlich: `slice(380,400)` der aufsteigenden Liste = die **20 meistgenutzten Emotes des Channels**. Die Karten sind virtuell weggescrollt, `previewEmotes` schneidet bei >50 ab (`delete-confirm-dialog.ts:87`). (b) Ein HoB-Mod markiert 50 Löschkandidaten, votet noch drei Emotes durch — alle 50 Checkboxen springen auf leer. Er markiert 45 davon erneut, der Button zeigt „Löschen (95)", der Dialog listet 50 Namen plus „…und 45 weitere". Er bestätigt: 95 Emotes verschwinden unwiderruflich, 45 davon Doppelungen, die er nie bewusst zweimal ausgewählt hat.
> **Fix**: Sofort: `selection.clear()` in `toggleSort()`/`refresh()` und im `getResults`-`next` ergänzen — konsistent mit dem Filter- und dem Usage-Stats-Pfad. Robust und dauerhaft: `ListSelection` auf einen Key statt Objektidentität umstellen (`constructor(items, keyFn: (t:T)=>string)`, `Set<string>`) und den Shift-Anker als **Item** statt Index speichern, zur Klickzeit über `items().indexOf(anchorItem)` auflösen, bei nicht gefundenem Anker auf Einzeltoggle zurückfallen. Die robuste Variante ist Voraussetzung dafür, S2-16 (Auswahl über Filterwechsel erhalten) überhaupt sicher bauen zu können — siehe Zielkonflikt Z5.

> **S1-4 — „Löschen starten" ist klickbar, bevor die Shared-Set-Prüfung antwortet**
> **S1** · Frontend/Daten · **S** · Gefunden von Track 2b (B1), Track 3
> **Ort**: `web/src/app/shared/seven-tv/delete-confirm-dialog.ts:68-74` (kein `[disabled]`), i. V. m. `web/src/app/shared/seven-tv/mass-delete-panel.ts:101-122`
> **Was**: `openConfirm()` setzt `showConfirm=true`/`warningLoading=true` und startet *danach* `getSetWarning()`. Der Dialog rendert sofort mit dem Text „Prüfe geteilte Sets…", der Bestätigen-Button hat aber — im Gegensatz zum Auslöse-Button (`mass-delete-panel.ts:30`) — **keine `[disabled]`-Bindung**. Der Fehlerpfad ist konservativ gebaut (`:111-120`, „an unresolved check must not read as all clear"), der Ladepfad hat dieselbe Lücke offen.
> **Warum es zählt**: `EmoteSetOwnershipService.CheckAsync` ist der einzige Schutz davor, Emotes aus einem **fremden** Set zu löschen. Tier 1 sind zwei sequenzielle 7TV-Calls, Tier 3 ein Helix-Call plus ein 7TV-Lookup pro moderiertem Channel bei `SemaphoreSlim(5)` (`EmoteSetOwnershipService.cs:63-115`) — bei 20 Channels real 1–3 Sekunden. Ein Mod markiert 300 Emotes und klickt reflexartig „Löschen starten"; 400 ms später wäre `notOwnSet: true` gekommen. 300 Emotes sind aus einem fremden Set weg, zwei fremde Channels verlieren sie mit. Die Shared-Set-Warnung ist im Entscheidungslog nicht dokumentiert (nur Commit `640d77b`) — es gibt keine bewusste Entscheidung, die dem widerspräche.
> **Fix**: `[disabled]="warningLoading()"` an `delete-confirm-dialog.ts:68`, plus `disabled:opacity-50 disabled:cursor-not-allowed`. Bei `hasSharedSetWarning() !== null` zusätzlich eine explizite zweite Bestätigung (Checkbox) statt nur eines roten Kastens.

---

### S2 — Hoch

Die Befunde S2-1 bis S2-8 bilden einen zusammenhängenden Cluster: die Verbindungs- und Fehlerbehandlung des Workers. Track 2a hatte S2-1 bis S2-4 als S1 eingestuft. **Herabgestuft auf S2**, weil in keinem dieser Fälle bestehende Daten zerstört werden oder fremde Channel-Daten zugänglich werden — es sind Verfügbarkeits- und Erfassungsausfälle, die laut Track 2a unter der erwarteten Last aber „praktisch sicher" eintreten und damit exakt die S2-Definition treffen.

> **S2-1 — TwitchLibs `ReconnectionPolicy` erlaubt per Default 10 Verbindungsversuche pro Client-Instanz**
> **S2** · Worker · **S** (Code) / **M** (inkl. Ausbau der obsoleten Heuristik) · Gefunden von Track 2a (B1)
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:34` und `:116`
> **Was**: `new TwitchClient(loggerFactory: loggerFactory)` erzeugt intern einen `WebSocketClient` mit Default-`ClientOptions`. Die XML-Doku des Pakets sagt zweifach wörtlich: *„The Default Policy applied is 10 reconnection attempts with 3 seconds between each attempt."* `ClientBase.OpenPrivateAsync(bool isReconnect, …)` ruft `ReconnectionPolicy.Reset(isReconnect)`, und `Reset` kehrt bei `isReconnect == true` sofort zurück, ohne `_attemptsMade` zu nullen; `ReconnectAsync()` ruft immer mit `isReconnect: true`. Rechnung: initialer Connect → 1, Reconnect 1…9 → 10, Reconnect 10 → `AreAttemptsComplete()` ist wahr, die Open-Schleife läuft null Mal, `RaiseFatalError()` → `"Fatal network error."`. Zusätzlich bricht TwitchLibs interner `ConnectionWatchDog` nach einem gescheiterten Reconnect mit `break` aus seiner Monitor-Schleife und läuft nie wieder.
> **Warum es zählt**: Das erklärt exakt die Live-Beobachtung „genau beim 10. Reconnect, in zwei unabhängigen Umgebungen" und den 45-Minuten-Totalausfall vom 2026-07-26. Zwei Konsequenzen für den Stresstest: (1) Der „proaktive" Recreate ist nicht proaktiv — `_reconnectCountSinceCreation >= 8` wird ausschließlich in `ForceReconnectAsync` (`:77`) geprüft, also nur wenn der eigene Watchdog feuert. Bei HandOfBlood ist `LastMessageReceivedUtc` dauernd frisch, der eigene Watchdog feuert faktisch nie; der TwitchLib-interne WatchDog reconnectet autonom und erhöht den Zähler über `OnReconnected` still mit. (2) Die Marge ist 1: bei `_reconnectCountSinceCreation == 8` steht `_attemptsMade` bereits bei ≥ 9, und jeder fehlgeschlagene Teilversuch erhöht nur den TwitchLib-Zähler. Dann greift erst der 3-Fehler-Pfad mit 3 × 5 min Cooldown ≈ **15–20 Minuten Ausfall**.
> **Fix** (ersetzt beide Heuristiken, ~5 Zeilen):
> ```csharp
> using TwitchLib.Communication.Clients;
> using TwitchLib.Communication.Models;
>
> private TwitchClient CreateClient() => new(
>     client: new WebSocketClient(
>         // Default wäre maxAttempts:10 — und der Zähler wird bei Reconnects nie zurückgesetzt.
>         // Parameterloser ctor == unbegrenzt, Intervall 3s→30s.
>         new ClientOptions(new ReconnectionPolicy()),
>         loggerFactory.CreateLogger<WebSocketClient>()),
>     loggerFactory: loggerFactory);
> ```
> `TwitchClient(IClient client, …)` und `WebSocketClient(IClientOptions, ILogger<WebSocketClient>)` sind beide öffentlich (per Reflection verifiziert); `TwitchLib.Communication 2.0.1` für Klarheit explizit als `PackageReference` aufnehmen. Danach `MaxReconnectsBeforeProactiveRecreate` ersatzlos streichen (`:23-30, 77-84, 245-248`) — die Prämisse „zählbasiertes Twitch-seitiges Limit pro Identität" ist widerlegt, das Limit war client-seitig. `MaxConsecutiveConnectionErrorsBeforeRecreate` als Sicherheitsnetz behalten. **Vor dem Umbau verifizieren** (10 Minuten): `ReconnectionPolicy.cs` in `github.com/TwitchLib/TwitchLib.Communication`, Tag `2.0.1`, Commit `d1904be` — Track 2a konnte die Methodenkörper nicht dekompilieren.

> **S2-2 — Der Watchdog ist blind, solange nie eine Nachricht ankam → absorbierender Totzustand**
> **S2** · Worker · **S** · Gefunden von Track 2a (B2) · *herabgestuft von S1: permanenter Ausfall, aber kein Verlust bestehender Daten*
> **Ort**: `src/EmotePurge.Worker/TwitchConnectionWatchdog.cs:37-41`
> **Was**: `if (lastMessage is null) return;` — `LastMessageReceivedUtc` ist genau dann `null`, wenn `_lastMessageReceivedUtcTicks == 0`, und der wird ausschließlich in `OnMessageReceived` (`TwitchChatManager.cs:286`) gesetzt.
> **Warum es zählt**: Container startet neu (Deploy, VPS-Reboot, OOM-Kill), Twitch IRC ist in dem Moment nicht erreichbar. `ConnectAsync()` liefert `false` — wird verworfen (S2-4) — `_connected = true`. Die Boot-Joins werfen `ClientNotConnectedException`, werden gefangen und als `LogWarning` abgelegt; `_joinedChannels` bleibt leer. Nie eine Nachricht → **der Watchdog kehrt bei jedem einzelnen Tick sofort zurück, für immer**. Kein Recreate, kein Reconnect, kein Crash, kein Docker-Healthcheck (S3-35), und `restart: unless-stopped` greift nicht, weil der Prozess nicht abstürzt. Ausweg nur per Hand. Die Wahrscheinlichkeit steigt genau dann, wenn man sie am wenigsten braucht: nach einem Deploy oder einem Twitch-Incident.
> **Fix**: (1) Ersatz-Referenzpunkt statt `return`: `ITwitchChatManager` um `ConnectAttemptedUtc` erweitern und `var reference = twitchChatManager.LastMessageReceivedUtc ?? twitchChatManager.ConnectAttemptedUtc;` rechnen. (2) Zusätzliche Bedingung `!twitchChatManager.IsConnected`: Weiß der Manager selbst, dass er getrennt ist, soll der Watchdog unabhängig von `LastMessageReceivedUtc` handeln — dann mit eigenem, kürzerem Cooldown (60 s), weil ein bekannt getrennter Client kein Abuse-Risiko durch Reconnects darstellt.

> **S2-3 — `_joinedChannels` trackt nur bestätigte Joins → ein fehlgeschlagener Join wird nie wiederholt**
> **S2** · Worker · **S** (Umsortierung) / **M** (mit Reconciliation) · Gefunden von Track 2a (B4) · *herabgestuft von S1: Erfassungslücke pro Channel, keine Zerstörung bestehender Daten*
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:165-174`
> **Was**: `_joinedChannels[channelName] = 0;` steht **hinter** `await _client.JoinChannelAsync(channelName)` im `try`. Die Menge enthält damit die *gelungenen*, nicht die *gewünschten* Joins. `RejoinTrackedChannelsAsync` (`:252-268`) iteriert genau diese Menge — ein Channel, der nie drinstand, wird von keinem Reconnect und keinem Recreate je nachgeholt. Der Guard darüber (`:159`, `if (!_connected)`) schützt nicht, weil `_connected` nie auf `false` zurückgesetzt wird (S4-5).
> **Warum es zählt**: Man joint HandOfBlood per UI, während gerade ein `RecreateClientAsync` läuft. Zwischen `:118` (`_client = newClient`) und dem Abschluss von `ConnectAsync()` in `:122` liegt ein Fenster von bis zu **165 Sekunden**, in dem `_client` gesetzt, aber nicht verbunden ist. Der Redis-Handler (`Worker.cs:46`) trifft dieses Fenster → Exception → Warnung → Channel nie gejoint. `SyncSevenTvAsync` (`Worker.cs:47`) läuft danach trotzdem durch und füllt DB *und* Match-Cache. Von außen sieht alles korrekt aus: Channel in der Übersicht, 1.000 Emotes synchronisiert, `IsBotActive = true` — und **null Usage-Daten, für immer**.
> **Fix**: Absicht **vor** dem Versuch tracken:
> ```csharp
> _joinedChannels[channelName] = 0;
> try { await _client.JoinChannelAsync(channelName); }
> catch (Exception ex) { logger.LogWarning(ex, "Join fehlgeschlagen für {Channel} — wird beim nächsten Reconnect nachgeholt.", channelName); }
> ```
> Als Sicherheitsnetz zusätzlich in `SevenTvPeriodicResyncWorker.ResyncOnceAsync` (liest ohnehin jede Minute alle aktiven Channels) ein `twitchChatManager.EnsureJoinedAsync(channelName)` — schließt auch verlorene Redis-Kommandos und die Boot-Recovery-Lücke.

> **S2-4 — `ConnectAsync()` gibt `Task<bool>` zurück; der Rückgabewert wird an beiden Aufrufstellen verworfen**
> **S2** · Worker · **S** · Gefunden von Track 2a (B3) · *herabgestuft von S1: wirkt über S2-2/S2-7, kein eigenständiger Datenschaden*
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:57-61` und `:122`
> **Was**: Per Reflection verifiziert: `Task<bool> ConnectAsync()`. TwitchLib gibt `false` zurück, wenn `OpenAsync()` fehlschlägt — es wirft nicht. `await _client.ConnectAsync(); _connected = true;` setzt bedingungslos. Der Fehlschlag ist damit im Code nicht vom Erfolg unterscheidbar.
> **Warum es zählt**: Boot-Pfad siehe S2-2. Recreate-Pfad: Schlägt `ConnectAsync` in `:122` fehl, hat der frische Client sofort sein komplettes 10er-Kontingent verbraucht (Open-Schleife 3+6+…+30 s ≈ 165 s), danach ist auch der neue Client tot; `RejoinTrackedChannelsAsync` (`:129`) läuft trotzdem und wirft pro Channel eine gefangene Exception — 5 Warnungen, kein Abbruch, kein Signal.
> **Fix**:
> ```csharp
> _connected = await _client.ConnectAsync();
> if (!_connected)
>     logger.LogError("Initialer TwitchClient-Connect fehlgeschlagen — Worker startet ohne IRC-Verbindung.");
> ```
> In `RecreateClientAsync` analog: bei `false` `_isConnected = false` setzen, `RejoinTrackedChannelsAsync()` überspringen und die Zähler-Nullung aus `:119-120` erst *nach* erfolgreichem Connect vornehmen.

> **S2-5 — Boot-Recovery-Schleife ist nicht pro Channel abgesichert → Crash-Loop des ganzen Prozesses**
> **S2** · Worker · **S** (try/catch) / **M** (mit Start-Serialisierung) · Gefunden von Track 2a (B5)
> **Ort**: `src/EmotePurge.Worker/Worker.cs:31-36`
> **Was**: `await SyncSevenTvAsync(channelName, stoppingToken);` hat — anders als `JoinChannelAsync` — kein eigenes try/catch. `SevenTvPeriodicResyncWorker` macht es an der analogen Stelle richtig (`:43-52`), die Boot-Recovery nicht. `SyncChannelAsync` kann werfen: `SevenTvApiClient` fängt nur `HttpRequestException`/`TaskCanceledException` (`:51`, `:81`), eine `JsonException` propagiert; ebenso eine `DbUpdateException` am Unique-Index `(ChannelId, SevenTvEmoteId)` durch einen parallel laufenden Sync.
> **Warum es zählt**: Die Exception verlässt `ExecuteAsync`. Der .NET-Default ist `BackgroundServiceExceptionBehavior.StopHost` und wird nicht überschrieben → **der komplette Worker-Prozess fährt herunter**. `restart: unless-stopped` startet ihn neu, die Boot-Recovery läuft in derselben Reihenfolge → dieselbe Exception → Crash-Loop; alle Channels hinter dem fehlerhaften werden nie gejoint. Bei HandOfBlood ist die Kollisionswahrscheinlichkeit erhöht, weil der Sync von 1.000 Emotes länger dauert und damit sicherer in den 60-s-Tick des Resync-Workers hineinragt.
> **Fix**: `try/catch` um `SyncSevenTvAsync` und der Symmetrie halber um den ganzen Schleifenkörper, mit demselben Kommentarmuster wie `SevenTvPeriodicResyncWorker.cs:49-51`. Zusätzlich den Resync-Worker erst starten lassen, wenn die Boot-Recovery durch ist (`IsBootRecoveryRunning`-Flag oder `TaskCompletionSource`) — das entschärft gleichzeitig S2-8, Szenario 1.

> **S2-6 — `SevenTvPeriodicResyncWorker`: Channel-Liste-Abfrage ungeschützt → jeder Postgres-Aussetzer beendet den Prozess**
> **S2** · Worker · **S** · Gefunden von Track 2a (B6)
> **Ort**: `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs:36-39`
> **Was**: `await db.Channels.Where(c => c.IsBotActive).Select(c => c.ChannelName).ToListAsync(ct);` liegt außerhalb jedes try/catch; das `try` beginnt erst innerhalb der Schleife (`:43`). Auch `scopeFactory.CreateScope()`/`GetRequiredService` (`:32-34`) sind ungeschützt. `ResyncOnceAsync` wird in `ExecuteAsync` (`:26`) direkt awaited → Exception verlässt `ExecuteAsync` → `StopHost`.
> **Warum es zählt**: Diese Abfrage läuft **jede Minute, für immer**. Ein Postgres-Neustart, ein Failover, ein voller Connection-Pool oder ein Netzwerk-Hickser im Docker-Bridge-Netz killt den Worker. Er kommt zwar zurück, aber: neue justinfan-Identität, kompletter Rejoin inklusive NAMES-Burst, `LastMessageReceivedUtc` wieder `null` (→ Risiko S2-2), und die bis zu 30 s gepufferten Counts gehen verloren, weil der finale Flush in `UsageFlushWorker.StopAsync` gegen dieselbe kaputte DB läuft. Eintrittswahrscheinlichkeit über einen mehrtägigen Stresstest: hoch.
> **Fix**: `ResyncOnceAsync`-Rumpf komplett in try/catch (Muster: `TwitchConnectionWatchdog.cs:63-67`). Bewusst **kein** `BackgroundServiceExceptionBehavior.Ignore` global setzen — das beendet den betroffenen Service trotzdem still; try/catch *innerhalb* der `while`-Schleife jedes `BackgroundService` ist die saubere Form (drei von vier machen es bereits richtig).

> **S2-7 — `_isConnected` wird beim Recreate nie auf `false` gesetzt → der Health-Key meldet „connected" auf einem toten Client**
> **S2** · Worker · **S** · Gefunden von Track 2a (B7)
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:99-130`, Auswirkung in `WorkerHealthPublisher.cs:34` und `WorkerHealthEndpoints.cs:34`
> **Was**: `_isConnected` wird nur in `OnConnected`/`OnReconnected` auf `true` und in `OnDisconnected` auf `false` gesetzt. `RecreateClientAsync` meldet den alten Client per `UnwireClient` (`:106`) **von allen Events ab, bevor** `DisconnectAsync()` (`:109`) läuft — `OnDisconnected` feuert also nie. Anschließend kann `ConnectAsync()` (`:122`) fehlschlagen, ohne dass irgendetwas `_isConnected` korrigiert.
> **Warum es zählt**: Beim proaktiven Recreate (Pfad `:77-84`, der greift, während der Client *läuft*) bleibt `_isConnected` dauerhaft `true`. `WorkerHealthPublisher` schreibt alle 20 s `isConnected: true`, `/api/worker/health` leitet `status` **ausschließlich** aus diesem Flag ab und meldet „connected", obwohl nichts mehr ankommt. `secondsSinceLastMessage` wird mitgeliefert, aber nicht in den Status eingerechnet. Der Health-Key kann also lügen — in exakt dem Zustand, in dem man ihn am dringendsten braucht, und er ist die Grundlage für den geplanten Uptime-Check (siehe Zielkonflikt Z1).
> **Fix**: Direkt nach `UnwireClient(oldClient)`: `_isConnected = false;` mit Kommentar („Unwire unterdrückt OnDisconnected"). Connect-Erfolg auswerten (S2-4). Ergänzend einen dritten Zustand `"stale"` publizieren, wenn `IsConnected && LastMessageReceivedUtc` älter als `StaleThreshold` ist, bzw. `secondsSinceLastMessage` in `WorkerHealthEndpoints.cs:34` in den Status einrechnen.

> **S2-8 — Fire-and-forget-Redis-Handler geben die Reihenfolgegarantie auf**
> **S2** · Worker/Infra · **M** · Gefunden von Track 2a (B8)
> **Ort**: `src/EmotePurge.Infrastructure/Redis/RedisSubscriber.cs:12-19`, Konsument `src/EmotePurge.Worker/Worker.cs:40-56`
> **Was**: StackExchange.Redis liefert Nachrichten pro Channel geordnet und sequenziell an den synchronen Callback. Zeile 18 (`_ = HandleSafelyAsync(...)`) gibt diese Garantie auf. Der Entscheidungslog-Eintrag (`CLAUDE.md:128`) begründet ausschließlich das *Exception-Logging*; die aufgegebene Reihenfolge-Garantie ist dort nicht adressiert.
> **Warum es zählt**, nach Wahrscheinlichkeit: (1) **Resync-Tick × JOIN auf demselben Channel** — zwei Scopes, zwei `AppDbContext`, beide führen `ReconcileAsync` aus und fügen dieselben `(ChannelId, SevenTvEmoteId)`-Zeilen ein; der Verlierer bekommt eine `DbUpdateException`, im Boot-Pfad reißt sie den Prozess mit (S2-5). Bei HoB mit 1.000 Emotes dauert der Erst-Sync merklich lange → großes Kollisionsfenster. (2) **Zwei JOINs kurz hintereinander** (Doppelklick, zwei Mods) → doppelter `JoinChannelAsync`, für den zweiten kommt keine Bestätigung → irreführende `OnFailureToReceiveJoinConfirmation`-Warnung. (3) **JOIN gefolgt von LEAVE**: Läuft LEAVE zuerst durch, joint JOIN danach einen Channel, dessen DB-Zeile bereits hart gelöscht ist. `_joinedChannels` enthält ihn dauerhaft, er wird nach jedem Reconnect brav rejoint, `SyncChannelAsync` loggt jede Minute „nicht in Postgres gefunden", und der Worker sitzt bis zum Neustart in einem Channel, den er verlassen sollte.
> **Fix**: `ChannelMessageQueue` statt synchronem Callback — `queue.OnMessage(async msg => { try { await handler(...); } catch ... })` verarbeitet streng sequenziell und awaited jeden Handler. Zusätzlich einen Per-Channel-`SemaphoreSlim` in `SevenTvSyncService` oder im `Worker`, damit Redis-Sync und periodischer Resync sich nicht überschneiden (löst Szenario 1 unabhängig).

> **S2-9 — Data-Protection-Schlüssel sind flüchtig, und es gibt keinen serverseitigen Widerruf für 14-Tage-Sessions**
> **S2** · Security/Betrieb · **M** · Gefunden von Track 1 (B3), Track 5 (I2)
> **Ort**: `src/EmotePurge.Api/Program.cs:18-35` · `src/EmotePurge.Api/Dockerfile:27-34` · `docker-compose.prod.yml:43-57`
> **Was**: `AddDataProtection()` wird nirgends aufgerufen, es gibt kein `PersistKeysToFileSystem` und kein Volume für den API-Container. Der Container läuft als `appuser`, angelegt mit `--no-create-home` — es existiert kein beschreibbares Home-Verzeichnis, das Key-Repository ist ephemer. Jeder Container-Neustart erzeugt einen neuen Schlüsselring. Gleichzeitig gibt es keinen `ValidatePrincipal`-Hook, keine Session-Tabelle und keine Token-Version; `POST /api/auth/logout` ruft nur `SignOutAsync` (`AuthEndpoints.cs:108`), das Cookie selbst bleibt kryptografisch gültig.
> **Warum es zählt**: Szenario A (heute): Jeder Deploy wirft alle eingeloggten Nutzer raus — bisher kaschiert dadurch, dass für den Live-Mod-Check ohnehin gelegentlich ein Re-Login nötig ist; spätestens bei zwei API-Replicas schlagen Logins sporadisch fehl. Szenario B (nach dem naiven Fix): Wird nur das Volume angehängt, überlebt jede Session Redeploys — und damit verschwindet der einzige vorhandene Notausgang. Ein exfiltriertes Cookie (siehe S2-10) ist dann **14 Tage lang durch nichts invalidierbar**, inklusive der darin mitgeführten Broadcaster-/Mod-Rechte und `DELETE /api/channels/{c}` (S1-1). Die `SlidingExpiration` von 14 Tagen verlängert sich zudem unbegrenzt, solange das Cookie alle 7 Tage einmal benutzt wird.
> **Fix**: Beides zusammen, nicht nur das erste. (1) Keys persistieren:
> ```csharp
> builder.Services.AddDataProtection()
>     .PersistKeysToFileSystem(new DirectoryInfo("/keys"))
>     .SetApplicationName("EmotePurge");
> ```
> plus `RUN mkdir -p /keys && chown appuser:appgroup /keys` in der `final`-Stage vor `USER` und `volumes: [dataprotection-keys:/keys]` in beiden Compose-Dateien. (2) Widerrufbarkeit: `User.SessionsValidFromUtc` (Migration), beim Callback (`AuthEndpoints.cs:83-90`) einen Claim `twitch:session_issued_at` setzen, in `options.Events.OnValidatePrincipal` dagegen prüfen und bei Unterschreitung `RejectPrincipal()` + `SignOutAsync`; `logout` setzt `SessionsValidFromUtc = UtcNow`. (3) Solange (2) nicht steht: `ExpireTimeSpan` von 14 auf 7 Tage reduzieren — der Twitch-Token läuft ohnehin nach ~4 h ab.

> **S2-10 — Secure-Flag und TLS-Durchsetzung hängen fail-open an einem Header, den die App nicht garantieren kann**
> **S2** · Security · **S** · Gefunden von Track 1 (B2)
> **Ort**: `src/EmotePurge.Api/Program.cs:18-35` (Cookie), `:70-77` (ForwardedHeaders + HttpsRedirection), `:83-100` (Header-Middleware)
> **Was**: `AddCookie(...)` setzt nur `ExpireTimeSpan`/`SlidingExpiration`; `Cookie.SecurePolicy` bleibt beim Default `SameAsRequest`. Das Secure-Flag wird also pro Request aus `Request.IsHttps` abgeleitet, und `IsHttps` entsteht in dieser Topologie ausschließlich daraus, dass die `ForwardedHeadersMiddleware` ein `X-Forwarded-Proto: https` vom Host-Proxy sieht. Zusätzlich ist `app.UseHttpsRedirection()` (`:77`) im Container ein No-Op (Kestrel lauscht nur auf `http://+:8080`, keine `ASPNETCORE_HTTPS_PORT`), und in der handgeschriebenen Header-Liste fehlt als einziger Eintrag `Strict-Transport-Security`.
> **Warum es zählt**: Der Betreiber tauscht den Reverse Proxy aus oder legt einen zweiten vhost an und vergisst `proxy_set_header X-Forwarded-Proto $scheme;`. Ab dem Moment liefert der OAuth-Callback ein `Set-Cookie` **ohne `Secure`** aus — stillschweigend, ohne Log. Ein Nutzer im offenen WLAN tippt „emotepurge.app" ohne Schema; ohne HSTS geht der erste Request per `http://`, ein sslstrip-Angreifer liest das Cookie im Klartext und besitzt damit 14 Tage lang die Session inklusive des verschlüsselt mitgeführten Twitch-Access-Tokens — mit allen Broadcaster-/Mod-Rechten des Opfers. **In 10 Sekunden prüfbar**: `curl -sI https://emotepurge.app/api/auth/twitch/login | grep -i set-cookie` — steht dort kein `secure`, ist die Annahme heute schon verletzt.
> **Fix**: (1) In `AddCookie`: `options.Cookie.SecurePolicy = CookieSecurePolicy.Always;` plus explizit `SameSite = SameSiteMode.Lax` und `HttpOnly = true`. Damit ist das Flag unabhängig von der Proxy-Konfiguration, und ein fehlender Header führt zu einem sichtbar kaputten Login statt zu einem unsicheren funktionierenden. (2) In der Header-Middleware (`:88`): `headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains";` — bewusst als Response-Header statt `app.UseHsts()`, weil letzteres denselben `IsHttps`-Umweg nimmt. (3) `UseHttpsRedirection()` entfernen (mit Kommentar) oder per `ASPNETCORE_HTTPS_PORT=443` funktionsfähig machen; aktuell suggeriert es einen Schutz, den es nicht leistet.

> **S2-11 — Autorisierungsentscheidungen lösen ungecachte, ungeratelimitete Fremd-API-Aufrufe aus**
> **S2** · Security/Architektur · **M** · Gefunden von Track 1 (B5), Track 4 (A-1) · *heraufgestuft von Trackspezifisch S3, weil jeder Testaccount damit einen App-weiten Autorisierungsausfall auslösen kann*
> **Ort**: `src/EmotePurge.Infrastructure/Services/ChannelAccessService.cs:29-46` · `MyChannelsService.cs:21-95` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs:125-134` → `VoteEligibilityService.cs:84-101` · `src/EmotePurge.Api/Program.cs:41-56`
> **Was**: Die `ExpensiveOps`-Policy (20 Requests/Min/Nutzer) hängt an genau fünf Endpoints. Nicht daran hängen: **`GET /api/channels/mine`** — bis zu 10 Helix-Requests plus `ResolveSevenTvIdentityAsync` plus `GetEditorOfChannelsAsync`, also **bis zu 12 ausgehende Requests pro eingehendem**, ohne jeden Cache; **`usage-stats`, `usage-stats/totals`, `emotes/active-set`** — `CanViewUsageStatsAsync` macht für jeden Nicht-Admin/Broadcaster/Mod zwei ungecachte 7TV-GraphQL-Calls allein für die Autorisierungsentscheidung; **`GET .../vote-sessions`** — für Nicht-Manager läuft `EvaluateAudienceAsync` über *alle* Sessions, jede mit `Subs`-Flag löst einen eigenen, ungecachten Helix-Call mit 10 s Timeout aus.
> **Warum es zählt**: Ein beliebiger eingeloggter Nutzer ohne jeden Kanalbezug schleift `GET /api/channels/mine` mit 50 Requests/s — das erzeugt bis zu 600 Helix-/7TV-Requests pro Sekunde von der Server-IP. Twitchs Bucket ist pro Client-ID, also App-weit: `GetModeratedChannelLoginsAsync` liefert für **alle** Nutzer `null`, `ModeratorCheckService` gibt `false` zurück (korrekt ohne zu cachen) → **jeder Moderator jedes Channels verliert seine Berechtigung**. Parallel riskiert der 7TV-Anteil eine IP-Sperre, wodurch auch der Resync-Worker ausfällt. Zweite Wirkung ohne Angreifer: bei `429` gibt `GetUserSubscriptionStatusAsync` `null` zurück, und `VoteEligibilityService.cs:96` (`if (isSubscribed == true)`) wirft `null` und `false` in denselben Zweig — Sessions verschwinden ohne Fehlermeldung aus der Liste eines Berechtigten.
> **Fix**, gestaffelt: (1) **Sofort, S:** `.RequireRateLimiting("ExpensiveOps")` auf `GET /api/channels/mine` (`ChannelEndpoints.cs:59`), auf die `UsageStatsEndpoints`-Gruppe (`:14`), auf die ganze `EmoteEndpoints`-Gruppe (`:16`) statt nur zwei ihrer drei Endpoints, und auf `GET .../vote-sessions` (`VoteSessionEndpoints.cs:138`) — alle tragen bereits `RequireAuthorization()`, die Partitionierung nach `NameIdentifier` greift sauber. (2) **S/M:** 7TV-Editor-Grants analog zum Mod-Check cachen (`IModRoleCache` um `TryGet/SetIsSevenTvEditorAsync`, Key `7tveditor:{uid}:{channel}`, gleiche TTL) — der teuerste Autorisierungspfad fällt von 2 Netzwerk-Calls auf 1 Redis-Get. (3) **S:** Sub-Status pro `(principal, channel)` **einmal** statt pro Session auswerten (`EvaluateAudienceAsync` nimmt ein vorberechnetes `bool? isSubscriber` entgegen); optional zusätzlich `TryGet/SetIsSubscriberAsync` (Key `subcheck:{uid}:{broadcasterId}`), dabei `null` bewusst **nicht** cachen.

> **S2-12 — `sync-deleted` ohne Error-Handler; das optimistische UI-Update feuert trotzdem**
> **S2** · Frontend/Daten · **M** · Gefunden von Track 2b (B3)
> **Ort**: `web/src/app/core/seven-tv/seven-tv-delete.service.ts:152-154`, i. V. m. `web/src/app/shared/seven-tv/mass-delete-panel.ts:86-98`
> **Was**: `syncDeleted(...).subscribe();` — kein `error`-Callback, kein `catchError`. Parallel und entkoppelt emittiert der `effect()` im Panel `deleted` allein aus dem `isRunning`-Flankenwechsel. Das zurückgelieferte `SyncDeletedResult` (`archivedCount`/`notFoundIds`, `EmoteService.cs:35`) wird nirgends gelesen — `{archivedCount: 0, notFoundIds: [alle 300]}` sieht aus wie voller Erfolg.
> **Warum es zählt**: Realistischer Auslöser ist das Rate-Limit: `sync-deleted` und `set-warning` teilen die Policy `ExpensiveOps` (`EmoteEndpoints.cs:37,54`) mit `PermitLimit=20/min, QueueLimit=0` gemeinsam mit `join` und den Vote-Endpoints — mehrere Batches in einer Minute erzeugen ein 429, das verschluckt wird. Zweiter Auslöser: Die Cookie-Session läuft während eines langen Laufs ab → 401 → ebenfalls verschluckt. Der Nutzer sieht eine leere Liste und glaubt, alles sei erledigt. Die Konvergenz über den 60-s-Resync greift danach — **das allein wäre S3**. S2 ist es, weil diese Konvergenz an genau dem Prozess hängt, der laut Entscheidungslog wiederholt ausgefallen ist und manuelle `docker restart` brauchte, und weil `/api/worker/health` ausschließlich Twitch-IRC meldet: für den Resync-Loop existiert kein Signal. Steht der Worker, divergiert die DB unbemerkt und dauerhaft.
> **Fix**: (1) `subscribe({error: ...})` in `finish()`, Ergebnis in ein `syncFailed`-Signal, im Progress-Panel anzeigen („Löschung bei 7TV erfolgt, Meldung ans Backend fehlgeschlagen — wird automatisch nachgezogen") mit Retry-Button; bei 429/5xx zusätzlich 1–2× `retry({delay})`. (2) `deleted` erst nach erfolgreichem `sync-deleted` emittieren, sonst `load()` statt optimistischem Filter. (3) `sync-deleted` aus `ExpensiveOps` herausnehmen — ein reiner Buchhaltungs-Call ohne Downstream-Kosten, ausgerechnet der, der nie verloren gehen darf (siehe Zielkonflikt Z3). (4) Heartbeat des Resync-Loops analog `worker:health:twitch` publishen.

> **S2-13 — Kein HTTP-Interceptor: 401-Behandlung sechsfach, zwei konkurrierende Fehler-Mappings, 3–4 redundante Requests pro Navigation**
> **S2** · Frontend/Architektur · **S** (Interceptor + Mapping) / **M** (Permissions-Endpoint) · Gefunden von Track 3, Track 4 (A-4)
> **Ort**: `web/src/app/core/auth/auth.service.ts:74`; Duplikate in `overview-page.ts:77-83`, `my-votings-page.ts:79-85`, `vote-session-list-page.ts:146-152`, `vote-session-detail-page.ts:186-201`, `usage-stats-page.ts:128-135`, `channel-workspace-layout.ts:126-130`; Mapping in `web/src/app/core/i18n/api-error.ts:31-34`
> **Was**: Drei Probleme mit derselben Wurzel. (1) `if (error.status === 401) { this.authService.handleSessionExpired(); return; }` steht sechsmal, fünfmal wortgleich — und in `channel-workspace-layout.ts` fehlt der Zweig ganz. (2) Zwei Fehler-Mapping-Strategien: `apiErrorTranslationKey()` liest ausschließlich `error.error.errorCode`, während drei Autorisierungs-Filter (`ChannelManagementAuthorizationFilter.cs:25` → `Results.Forbid()`, ebenso `UsageStatsAccessAuthorizationFilter`, `VoteAudienceFilter`, `VoteEligibilityFilter`) und `VoteSessionEndpoints.cs:65,79` **body-lose** Antworten liefern; alles davon plus `status === 0` und `5xx` fällt auf `errors.generic`. (3) Für **eine** Navigation nach `/channels/:c/usage-stats` laufen vier Requests, drei davon auf `GetUsageTotalsAsync` (je zwei DB-Roundtrips), bevor die Seite etwas anzeigt.
> **Warum es zählt**: Der Redis-Mod-Cache ist bis zu 10 Minuten in beide Richtungen stale. Ein frisch gemoddeter HoB-Mod klickt in der Übersicht auf „Hinzufügen" → 403 → **„Etwas ist schiefgelaufen. Bitte versuch es erneut."** Er versucht es erneut. Und erneut. Nichts sagt ihm, dass ihm die Rechte fehlen, geschweige denn dass es in zehn Minuten von selbst klappt. Strukturell: Jede Seite von Modul E kopiert das Muster ein siebtes Mal, und der 7TV-Direktpfad (`seven-tv-delete.service.ts:122`) darf gerade *nicht* mit umgeleitet werden — diese Unterscheidung existiert heute nur implizit.
> **Fix**: (1) `core/http/api-auth.interceptor.ts` als funktionaler Interceptor, registriert per `provideHttpClient(withFetch(), withInterceptors([apiAuthInterceptor]))`; erste Zeile `if (!req.url.startsWith('/api/')) return next(req);` — damit ist der 7TV-Pfad per Konstruktion ausgenommen und der Grund steht an genau einer Stelle. (2) `apiErrorTranslationKey` um einen Status-Fallback **vor** `errors.generic` erweitern: `401 → unauthorized`, `403 → forbidden` („Dir fehlen die Rechte für diese Aktion. Falls du gerade erst Mod geworden bist: in etwa 10 Minuten erneut versuchen."), `404`, `409`, `0 → offline`, `>=500 → server`. Damit werden alle Aufrufer schlagartig brauchbar, der Sonderweg in `vote-session-detail-page.ts:192-201` kann auf `apiErrorTranslationKey` umgestellt werden (die Codes `vote_session_ended`/`vote_session_not_found` existieren bereits in `ApiErrorCodes.cs:28-29` und beiden Locale-Dateien). (3) `GET /api/channels/{channelName}/permissions` → `{ canManage, canViewUsageStats, isGlobalAdmin, isSevenTvEditor }`, gespeist aus `IChannelAccessService`; `usage-stats-access.guard.ts`, `channel-workspace-layout.ts:99-112` und `vote-session-list-page.ts:71-74` ziehen daraus. Modul E braucht dann ein Feld mehr statt eines neuen Probe-Pfads.

> **S2-14 — Erstnutzung nach „Hinzufügen": leeres Grid bzw. 1.000 × „0x" ohne jede Erklärung**
> **S2** · UX · **M** · Gefunden von Track 3
> **Ort**: `web/src/app/features/overview/overview-page.ts:53-58` · `web/src/app/features/usage-stats/usage-stats-page.ts:52-53` · `usage-stats-page.html:93`
> **Was**: `join()` navigiert bei Erfolg direkt in den Channel-Workspace. Zu diesem Zeitpunkt hat der Worker den Redis-`JOIN` gerade erst empfangen, der 7TV-Vollsync ist nicht durch → **„Keine aktiven Emotes für diesen Zeitraum."** Ist der Sync durch, erscheinen ~1.000 Karten mit durchgehend **„0x"**, weil `UsageStat` erst ab dem Join-Zeitpunkt vorwärts zählt, der Default-Filter aber 7 Tage zurückschaut. Nirgends steht, dass das Tracking nur vorwärts läuft.
> **Warum es zählt**: Das ist der allererste Eindruck des HoB-Mod-Teams, und beide Zustände lesen sich als „kaputt". Der naheliegende nächste Klick ist „Nur ungenutzte (0x)" — der zeigt dann **alle 1.000 Emotes als Löschkandidaten**. Ein zu schnelles Mod-Team löscht damit im Extremfall das gesamte Emote-Set eines Millionen-Follower-Channels, weil die App den Eindruck erweckt, nichts davon werde benutzt. Track 3 stuft das als den plausibelsten Weg zu echtem Schaden im ganzen Tool ein.
> **Fix**: (1) `channelId`-DTO um `createdAt` erweitern (Feld existiert in `Channel.cs`) und dauerhaft einblenden: „Nutzung wird seit {{ createdAt }} erfasst — Zeiträume davor enthalten naturgemäß keine Daten." (2) Ist `createdAt` jünger als der gewählte `from`-Wert, den Zeitraum automatisch darauf klemmen und das sichtbar machen. (3) Ist `createdAt` < 24 h alt, eine Hinweisbox statt der leeren Liste. (4) Den „Nur ungenutzte (0x)"-Filter in genau diesem Fenster deaktivieren oder mit einer Warnung versehen.

> **S2-15 — Namensfilter ist Exakt-Match statt Teilstring-Suche**
> **S2** · UX · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/shared/emotes/emote-usage-filter.ts:8-15`
> **Was**: `globToRegExp` baut `new RegExp('^' + escaped + '$', 'i')` — ohne Wildcards ein voll verankerter Vergleich. `*`/`?` werden übersetzt, müssen aber vom Nutzer selbst getippt werden.
> **Warum es zählt**: Ein HoB-Mod tippt bei ~1.000 Emotes `peepo` ins Namensfeld — **0 Treffer**, obwohl 40 Emotes `peepo…` heißen. Die Anzeige springt auf „0 von 1000 Emotes" und darunter „Keine aktiven Emotes für diesen Zeitraum". Wer den Placeholder-Hinweis (`„Name (z.B. *cat*, ?og)"`) nicht als Glob-Syntax erkannt hat, hält das Tool schlicht für kaputt — und Suche ist bei 1.000 Emotes der einzige praktikable Weg, ein bestimmtes Emote zu finden.
> **Fix**: Default auf Teilstring: Enthält die Eingabe kein `*`/`?`, `new RegExp(escaped, 'i')` ohne Anker verwenden; Glob-Verhalten bleibt erhalten, sobald ein Wildcard getippt wird. Placeholder auf „Name suchen…" vereinfachen, den Glob-Hinweis ins `title` verschieben.

> **S2-16 — Jeder Filter-/Suchwechsel löscht die komplette Auswahl; es gibt kein „Alle auswählen"**
> **S2** · UX · **M** · Gefunden von Track 3
> **Ort**: `web/src/app/shared/emotes/emote-usage-filter.ts:56,62,66,77` i. V. m. `usage-stats-page.ts:61` und `vote-session-detail-page.ts:88` (`onChange = () => this.selection.clear()`)
> **Was**: Jede Änderung an Min/Max/Name/„Nur ungenutzte" ruft `onChange()` → `selection.clear()`. Ein „Alle (gefilterten) auswählen"- oder „Auswahl aufheben"-Button existiert nirgends; der einzige Weg zu einer großen Auswahl ist Einzel- oder Shift-Klick über den aktuell gefilterten Bereich.
> **Warum es zählt**: Der reale Aufräum-Workflow bei ~1.000 Emotes ist iterativ („`*peepo*` filtern, Müll markieren → `*pepe*` filtern, Müll markieren → am Ende alles löschen") und ist konstruktiv unmöglich. Selbst der einfachste Fall — „lösche alle 400 nie genutzten Emotes" — verlangt: Filter klicken, erste Karte klicken, 80 Zeilen im 512-px-Fenster scrollen, letzte Karte shift-klicken. Es gibt keine Affordanz, die das andeutet.
> **Fix**: (1) „Alle sichtbaren auswählen" / „Auswahl aufheben" neben die Emote-Zähl-Zeile (`usage-stats-page.html:73-75`); `ListSelection` braucht dafür nur `selectAll(items)`. (2) `onChange` nicht mehr hart clearen, sondern die Auswahl behalten und in der Zähl-Zeile anzeigen „50 ausgewählt (12 davon aktuell ausgeblendet)". **Voraussetzung**: die keyed-`ListSelection` aus S1-3 — ohne sie ist das Behalten der Auswahl über Filterwechsel gefährlicher als das Löschen (siehe Zielkonflikt Z5).

> **S2-17 — Voll-Refetch aller ~1.000 Emote-Ergebnisse nach jedem einzelnen Vote**
> **S2** · UX/Performance · **M** · Gefunden von Track 3
> **Ort**: `web/src/app/features/voting/vote-session-detail-page.ts:178` (`next: () => this.load({ freeze: false })`), zusätzlich `:147` (`getStatus` läuft bei jedem `load()` mit)
> **Was**: Jeder Daumen-Klick löst nach der Vote-Antwort einen kompletten `GET …/results` aus — bei ~1.000 Emotes ein Payload mit 1.000 Objekten inklusive `imageUrl` — plus einen zweiten Call auf `GET /api/channels/{name}`. Backend-seitig hängt daran die volle Score-Berechnung inklusive `GetUsageTotalsAsync`.
> **Warum es zählt**: Ein Mod, der eine 1.000-Emote-Session durcharbeitet, feuert 1.000 Voll-Refetches ab: zweistellige Megabytes Traffic und eine spürbare Verzögerung nach jedem Klick, bei der die Karte ihre Farbe erst verzögert ändert. Der Vote fühlt sich unzuverlässig an, also klickt man nochmal — und nimmt damit den gerade gesetzten Vote versehentlich zurück (`:172-175`, gleicher Typ = Rücknahme).
> **Fix**: `castVote` gibt bereits `CastVoteResult` zurück — die Antwort lokal auf das eine Emote anwenden (`results.update`, `myVote`/`keepVotes`/`deleteVotes` patchen) statt neu zu laden; optimistisch färben und bei Fehler zurückrollen. `getStatus` aus `load()` herauslösen und nur einmal beim Mount aufrufen. Löst nebenbei den Refetch-Teil von S1-3 mit.

> **S2-18 — Dialog behauptet „Set gehört nicht diesem Channel", obwohl die Prüfung nur fehlgeschlagen ist**
> **S2** · UX/Daten · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/shared/seven-tv/delete-confirm-dialog.ts:32-34` und `:95-102`; `EmoteSetWarning.available` in `web/src/app/core/emotes/emote-admin.service.ts:11-15` wird nirgends ausgewertet
> **Was**: Das Backend unterscheidet sauber zwischen „Prüfung konnte nicht laufen" (`Available: false`, `EmoteSetOwnershipService.cs:33,40-47`) und „Prüfung sagt: fremdes Set" (`IsOwnSet: false`). Das Frontend liest ausschließlich `isOwnSet` und rendert als Tatsachenbehauptung „Das aktive Set gehört nicht dem eigenen 7TV-Account dieses Channels." Der Fallback in `mass-delete-panel.ts:113-118` setzt bei einem Fehler des `set-warning`-Calls sogar selbst `{available:false, isOwnSet:false}` — und erzeugt damit garantiert diesen Fehlalarm.
> **Warum es zählt**: 7TV ist nachweislich unzuverlässig (eigenes Entscheidungslog, 2026-07-25). Jedes Mal, wenn die Ownership-Auflösung scheitert, bekommt der Mod einen roten Alarm, der faktisch falsch ist. Nach dem dritten Fehlalarm klickt er ihn routiniert weg — **und übersieht ihn genau dann, wenn das Set tatsächlich geteilt ist und er fremde Channels leerräumt**. Das ist das teuerste Alarm-Fatigue-Szenario in dieser App und untergräbt direkt den Schutz aus S1-4.
> **Fix**: In `hasSharedSetWarning` `w.available` mitprüfen. Bei `available === false` einen eigenen, neutral formulierten Block rendern (neuer Key `massDelete.ownershipCheckUnavailable`: „Wir konnten gerade nicht prüfen, ob dieses Set wirklich diesem Channel gehört — bitte vor dem Löschen selbst auf 7tv.app kontrollieren."), gelb statt rot. `notOwnSet` nur noch bei `available && !isOwnSet`.

> **S2-19 — „Beenden" und „Löschen" als gleichfarbige Nachbarn; die Löschbestätigung nennt die Abstimmung nicht**
> **S2** · UX/Daten · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/features/voting/vote-session-list-page.html:88-97`, Bestätigung in `vote-session-list-page.ts:117-120`, Text in `de.json:181`
> **Was**: In derselben `flex gap-3`-Zeile stehen „Beenden" (`text-red-400`, fachlich harmlos) und „Löschen" (`text-red-500`, irreversibel inklusive aller Votes) — zwei Textlinks in nahezu identischer Farbe, ~12 px auseinander, ohne Icons oder unterschiedliche Gewichtung. Die Bestätigung lautet generisch „Diese Abstimmung wirklich unwiderruflich löschen?" — ohne Titel, ohne Erwähnung, dass die Community-Stimmen mitverschwinden.
> **Warum es zählt**: Ein HoB-Mod will die abgelaufene Juli-Aktion beenden, trifft ein Element weiter rechts, bekommt einen Dialog, der nicht sagt *welche* Session gemeint ist — bei 20 Sessions in der Liste keinerlei Korrekturchance — und vernichtet die Abstimmung, in der 3.000 Zuschauer gevotet haben.
> **Fix**: `deleteConfirm` mit Interpolation: „Abstimmung ‚{{ title }}' unwiderruflich löschen? Alle abgegebenen Stimmen gehen dabei verloren." „Löschen" optisch absetzen (Icon, `border-red-800`-Outline oder Overflow-Menü), „Beenden" neutral einfärben — es ist keine destruktive Aktion.

> **S2-20 — Fehlende Rechtstexte vor öffentlichem Launch**
> **S2** · Recht/Betrieb · **S–M** · Gefunden von Track 5 (I3)
> **Ort**: keine `Impressum`/`Datenschutz`-Route in `web/`; `web/public/robots.txt` (`Disallow: /`)
> **Was**: Die App speichert Twitch-User-IDs, Login-Sessions und laut `CLAUDE.md:140` zeitweise einen Access-Token-Claim im Cookie — ohne jede Rechtstext-Seite.
> **Warum es zählt**: Kein akutes Risiko, solange die Seite nicht öffentlich ist (robots.txt blockt, nur Testaccounts). Es ist aber die einzige Position auf der Pre-Launch-Liste, die nicht rein technisch lösbar ist und deshalb den längsten Vorlauf braucht — die Entscheidung invite-only vs. Veröffentlichung von Name/Adresse steht laut Projektstand seit dem 2026-07-28 offen.
> **Fix**: Siehe Pre-Launch-Checkliste, Abschnitt 5 — Klärung zuerst, danach Impressums- und Datenschutz-Route plus Footer-Link, `robots.txt` als letzter Schritt.

> **S2-21 — Keine Ressourcenlimits für `api`/`worker`**
> **S2** · Betrieb · **S** · Gefunden von Track 5 (I4)
> **Ort**: beide Compose-Dateien, Services `api`/`worker` (kein `deploy.resources.limits`, kein `mem_limit`/`cpus` — verifiziert)
> **Warum es zählt**: Auf dem VPS läuft eine zweite, fremde Anwendung. Der angekündigte Stresstest erzeugt hohe Nachrichtenraten im Worker und viele parallele Api-Requests. Ohne Limit kann ein Container den Host-RAM/die CPU vollständig beanspruchen, die Nachbar-App beeinträchtigen oder den OOM-Killer eskalieren lassen — ausgerechnet dann, wenn am wenigsten Fehlertoleranz besteht.
> **Fix**: `docker compose` v2 wertet `deploy.resources.limits` auch ohne Swarm aus:
> ```yaml
>     deploy:
>       resources:
>         limits: { cpus: "1.0", memory: 512M }
> ```
> Nach dem ersten Lasttest kalibrieren und in beiden Dateien fixieren.

---

### S3 — Mittel

> **S3-1 — Der Global-Admin ist im Voting-Pfad kein Manager: zwei divergierende Definitionen derselben Frage**
> **S3** · Security · **S** · Gefunden von Track 1 (B4)
> **Ort**: `src/EmotePurge.Infrastructure/Services/VoteEligibilityService.cs:61-104` (kein `IsGlobalAdmin`) vs. `ChannelAccessService.cs:12-27` (mit `IsGlobalAdmin`); sichtbar in `VoteSessionEndpoints.cs:110` vs. `:154`
> **Was**: `EvaluateRoleAsync` prüft `Everyone` → Broadcaster-Login → `IsModeratorAsync` → `Subs`. Ein `IsGlobalAdmin`-Zweig existiert nicht, während die Session-Liste über `CanManageChannelAsync` entscheidet, das mit `IsGlobalAdmin` beginnt.
> **Warum es zählt**: Fail-closed, also kein Rechte-Leck — aber ein reproduzierbarer Funktions- und Diagnosefehler. Admin `sensitron` bekommt gemeldet, dass in Channel `vassilly` eine Mods-only-Abstimmung falsche Ergebnisse zeigt. Er öffnet die Liste (Manager-Sicht, Session sichtbar), klickt sie an → `voteSessionAccessGuard` ruft `getResults()` → 403 → Redirect zurück auf die Liste. Er sieht einen Eintrag, der beim Anklicken kommentarlos auf sich selbst zurückspringt — kann die Session aber löschen und beenden. Strukturell gefährlicher: „Wer ist Manager dieses Channels?" ist an zwei Stellen unterschiedlich implementiert, und der Code enthält keinen Hinweis darauf.
> **Fix**: In `VoteEligibilityService` `IChannelAccessService` injizieren und in `EvaluateRoleAsync` direkt nach dem `Everyone`-Zweig `if (channelAccessService.IsGlobalAdmin(principal)) return VoteEligibilityResult.Allowed;` ergänzen. Sauberer: den Broadcaster-/Mod-Block komplett durch `await channelAccessService.CanManageChannelAsync(...)` ersetzen — dann existiert die Manager-Definition nur noch einmal. Das `IsActive`-Gate in `EvaluateAsync` (`:26-29`) muss unberührt bleiben.

> **S3-2 — Channel-Identität hängt am mutablen Twitch-Login statt an der unveränderlichen Twitch-User-ID**
> **S3** · Security · **M** · Gefunden von Track 1 (B6)
> **Ort**: `ChannelAccessService.cs:21-23` und `:44-45` · `src/EmotePurge.Core/Entities/Channel.cs:6-7` · `src/EmotePurge.Api/Endpoints/AuthEndpoints.cs:86`
> **Was**: „Ist Broadcaster" wird per `string.Equals(principal.TwitchLogin, normalizedChannel, …)` entschieden. `Channel.TwitchChannelId` — die unveränderliche numerische ID — existiert, wird von `SevenTvSyncService.cs:41` befüllt, ist per Unique-Index abgesichert und von **keiner einzigen Autorisierungsentscheidung** gelesen. Dasselbe im 7TV-Editor-Pfad: verglichen wird `grant.TwitchChannelLogin`, obwohl `SevenTvEditorGrant` die Twitch-ID mitliefert.
> **Warum es zählt**: Twitch erlaubt Namensänderungen und gibt alte Namen nach einer Sperrfrist frei. Broadcaster `vassilly` benennt sich in `vassilly_live` um; die Channel-Zeile heißt weiter `vassilly` (kein Rename-Pfad im Code). Nach der Sperrfrist registriert ein Dritter den Namen, loggt sich ein und erhält `twitch:login = vassilly` — `CanManageChannelAsync("vassilly")` liefert `true` über den reinen String-Vergleich, ohne dass Twitch, 7TV oder die DB konsultiert würden. Er ist „Broadcaster" eines fremden Channels: volle Nutzungsstatistik, Vote-Sessions anlegen und beenden, und `DELETE /api/channels/vassilly` (S1-1). Die korrekte `TwitchChannelId` steht in derselben Zeile und hätte es verhindert.
> **Fix**: (1) `CanManageChannelAsync`/`CanViewUsageStatsAsync` laden die `Channel`-Zeile (über das bestehende `IChannelService.GetByNameAsync`) und stellen den Broadcaster-Check auf `channel?.TwitchChannelId is { } id && id == principal.TwitchUserId` um, mit dem Login-Vergleich nur noch als Fallback für Channels mit `TwitchChannelId == null`. Analog im 7TV-Pfad `SevenTvEditorGrant.TwitchChannelId` gegen `channel.TwitchChannelId`. (2) `Channel.ChannelName` beim Join/Sync aus der Helix-/7TV-Antwort nachziehen — Achtung: `AppDbContext.cs:19` hat einen Unique-Index darauf, Kollisionen behandeln.

> **S3-3 — Roher, formal ungeprüfter `channelName` wird zum Redis-Key; Redis ohne Speichergrenze**
> **S3** · Security/Betrieb · **S** · Gefunden von Track 1 (B7)
> **Ort**: `src/EmotePurge.Infrastructure/Redis/ModRoleCache.cs:23` · `src/EmotePurge.Api/Auth/ChannelManagementAuthorizationFilter.cs:10-23` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs:56-70,72-81,156-189,191-213`
> **Was**: `BuildKey` baut `$"modcheck:{twitchUserId}:{channelName}"` ohne Längen- oder Zeichenbeschränkung. Der Wert kommt ungefiltert aus `RouteValues["channelName"]`; die Filter prüfen nur auf leer, `ChannelNameValidation.IsValid()` läuft erst im Handler — und bei den vier Handlern `end`, `DELETE {sessionId}`, `votes` und `votes/{emoteId}` überhaupt nicht.
> **Warum es zählt**: Ein eingeloggter Testaccount schickt `POST /api/channels/<7900 Zeichen>/vote-sessions/1/end`. Der Filter lehnt ab, aber vorher schreibt `SetIsModeratorAsync` einen ~8 KB großen Redis-Key mit 10-Minuten-TTL, sofern der vorgeschaltete Helix-Call erfolgreich war. Der Endpoint hat kein Rate Limit; in 10 Minuten lassen sich mehrere hundert MB in einen Redis-Container schreiben, der weder `maxmemory` noch eine Eviction-Policy hat (`docker-compose.prod.yml:12` setzt nur `--requirepass`). Läuft Redis voll, fällt nicht nur der Mod-Cache aus, sondern auch `channel:bot:commands` und `worker:health:twitch` — der Bot verliert die Steuerung. Zweitrangig: Channelnamen mit Zeilenumbrüchen landen unmaskiert in Log-Zeilen (`ModeratorCheckService.cs:26`). Ein Key-Kollisionsangriff zwischen zwei Nutzern ist **nicht** möglich, weil das erste Feld immer die numerische Twitch-ID ist (geprüft).
> **Fix**: (1) In `ChannelManagementAuthorizationFilter:11`, `UsageStatsAccessAuthorizationFilter:14`, `VoteEligibilityFilter:12` und `VoteAudienceFilter:17` das `string.IsNullOrEmpty`-Prädikat durch `!ChannelNameValidation.IsValid(channelName)` ersetzen. Da die Regex `^[a-z0-9_]{4,25}$` genau die Menge gültiger Twitch-Logins beschreibt, ändert das **keine Zelle der Autorisierungsmatrix** — nur der Statuscode für Müll-Eingaben wird 400 statt 403. (2) `IsValid()` in den vier abweichenden Handlern nachziehen (nach (1) redundant, aber sie sind die einzige Abweichung vom Muster ihrer Geschwister) — alternativ als Gruppen-Filter, siehe S3-31 und Zielkonflikt Z4. (3) `maxmemory` + `maxmemory-policy allkeys-lru` in `docker-compose.prod.yml:12` ergänzen.

> **S3-4 — `GET .../vote-sessions/{id}/results` liefert rohe Nutzungszahlen an jeden Session-Teilnehmer**
> **S3** · Security · **M** · Gefunden von Track 1 (B8)
> **Ort**: `src/EmotePurge.Infrastructure/Services/VoteSessionQueryService.cs:68-101` · `src/EmotePurge.Core/Services/IVoteSessionQueryService.cs:7-9` · vs. `src/EmotePurge.Api/Endpoints/UsageStatsEndpoints.cs:12-14`
> **Was**: `GetResultsAsync` ruft intern `usageStatQueryService.GetUsageTotalsAsync(...)` — exakt denselben Service wie `GET .../usage-stats/totals` — und gibt das Ergebnis als `TotalUseCount` je Emote aus, zusammen mit `EmoteName`, `SevenTvEmoteId` und `ImageUrl`. Der Zugang läuft aber über `VoteAudienceFilter`, und der lässt bei einer Session mit `AllowedRoles.Everyone` **jeden eingeloggten Nutzer** durch (`VoteEligibilityService.cs:66-69`, erster Zweig).
> **Warum es zählt**: `CLAUDE.md:164` dokumentiert ausdrücklich, dass `GetUsageTotalsAsync` genau deshalb hinter einen Filter gestellt wurde („ein unbeteiligter oder gar anonymer Nutzer hätte die Nutzungsdaten jedes Channels abrufen können") — diese Entscheidung ist durch den Voting-Pfad teilweise wieder aufgehoben, ohne dass der Zusammenhang irgendwo vermerkt wäre. Konkret: Broadcaster startet eine `Everyone`-Abstimmung (der Default), ein beliebiger eingeloggter Nutzer ruft `/results` auf und erhält die absoluten Chat-Nutzungszahlen jedes aktiven Emotes. Da `EndedAt ?? UtcNow` das Fenster laufend erweitert und `StartedAt` beim Anlegen frei rückdatiert werden darf (`VoteSessionEndpoints.cs:41-44` verbietet nur die Zukunft), umfasst eine einmalig rückdatierte Everyone-Session die **gesamte** Historie. Geprüft, ob es beabsichtigt ist: Die Score-Formel braucht nur den normalisierten Wert, und der wird ohnehin separat als `NormalizedUsageScore` geliefert.
> **Fix**: `GetResultsAsync` um `bool includeRawUsage` erweitern; `TotalUseCount` für Nicht-Manager auf `null`/`0` setzen, `NormalizedUsageScore` und die Sortierung bleiben unverändert. Im Handler aus `channelAccessService.CanManageChannelAsync(...)` speisen — derselbe Aufruf, den die Listen-Route bereits macht. Frontend zeigt die Roh-Spalte nur bei gesetztem `canManage()`-Signal. Zusätzlich das rückdatierte `StartedAt` begrenzen (max. 366 Tage, konsistent mit dem Range-Cap in `UsageStatsEndpoints.cs:53`).

> **S3-5 — Die Autorisierungs-Services sind die einzigen Infrastructure-Services ohne Tests**
> **S3** · Tests · **S** (ChannelAccessService) / **M** (VoteEligibilityService) · Gefunden von Track 1 (B10), Track 4 (A-3)
> **Ort**: `tests/EmotePurge.Infrastructure.Tests/` — vorhanden sind `ChannelServiceTests`, `EmoteSetOwnershipServiceTests`, `ModRoleCacheTests`, `UsageStatQueryServiceTests`, `VoteSessionQueryServiceTests`, `VoteSessionServiceTests`
> **Was**: `ChannelAccessService`, `ModeratorCheckService`, `VoteEligibilityService` und `MyChannelsService` — der komplette Entscheidungskern der Autorisierungsmatrix — haben kein Testpendant. Getestet sind die Datenpfade, nicht die Zugriffspfade. Die Projekt-Konvention (`CLAUDE.md:177`) verlangt das Gegenteil, und die Begründung für den Verzicht auf Api-Tests („die dünnen Handler delegieren an die getestete Infrastructure-Schicht") setzt genau die Abdeckung voraus, die hier fehlt. Frontend-seitig sind alle drei Guards getestet — die Client-Seite hat mehr Abdeckung als die durchsetzende Server-Seite.
> **Warum es zählt**: Konkret prüfbare Invarianten, die heute nur durch manuelle Live-Tests belegt sind: (a) `IsModeratorAsync` liefert `false`, wenn `principal.AccessToken is null`; (b) ein Helix-`null` wird **nicht** gecacht, sonst würde ein transienter Twitch-Ausfall alle Mods 10 Minuten aussperren; (c) `EvaluateRoleAsync` lässt bei `Everyone` sofort durch und bei `Subs` ohne `TwitchChannelId` nicht; (d) `CanViewUsageStatsAsync` gibt bei 7TV-`null` `false` zurück statt fail-open. Punkt (d) ist besonders heikel: Der Unterschied zwischen `?? false` und einem übersehenen `?? true` in `ChannelAccessService.cs:45` ist ein Zeichen und öffnet die Usage-Stats jedes Channels für jeden eingeloggten Nutzer. Das Projekt hat bereits zweimal live eine Autorisierungslücke gefunden — beide Male durch manuelles Testen mit zwei Twitch-Accounts, eine Methode, die pro Modul teurer wird.
> **Fix**: Vier Testklassen in `tests/EmotePurge.Infrastructure.Tests/Unit/` — **kein Container nötig**, alle Abhängigkeiten sind Interfaces bzw. `IConfiguration` (`ConfigurationBuilder.AddInMemoryCollection`), Muster liefert `EmoteSetOwnershipServiceTests`: `ChannelAccessServiceTests` (Allowlist case-insensitive, Broadcaster-Match, 7TV-Editor-Match, 7TV-`null` → `false`), `ModeratorCheckServiceTests` (Cache-Hit positiv/negativ, `AccessToken == null` → `false` ohne Helix-Call, Helix-`null` → `false` **ohne** Cache-Schreibvorgang), `VoteEligibilityServiceTests` (volle Rollenmatrix pro `AllowedRoles`-Kombination plus `IsActive`-Gate nur in `EvaluateAsync`), `MyChannelsServiceTests` (Self-Login immer enthalten, Degradations-Flags). Das ist zugleich die Testliste, mit der sich S3-1, S3-2 und S3-4 regressionsfrei einbauen lassen.

> **S3-6 — Die Worker-Zustandsmaschine ist die komplexeste Logik des Projekts und vollständig ungetestet**
> **S3** · Tests · **M** · Gefunden von Track 4 (A-2), gestützt von Track 2a
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:21-130` · `src/EmotePurge.Api/Auth/ClaimsPrincipalExtensions.cs:9-27` · `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs:107-137`
> **Was**: Die dokumentierte Entscheidung „kein Testprojekt für Api/Worker" stimmt für vier der fünf Hosted Services. Sie stimmt nicht für `TwitchChatManager` (308 Zeilen, die längste Datei des Projekts: zwei Schwellwerte, eine Recreate-Zustandsmaschine mit Event-Ab-/Anmeldung, zwei `Interlocked`-Zähler, ein `SemaphoreSlim`, ein nachgezogener Rejoin — **null** Infrastructure-Delegation), nicht für `TryBuildTwitchPrincipal` (entscheidet per `DateTime.TryParse`, ob Live-Rollenprüfungen überhaupt laufen — eine Autorisierungsentscheidung) und nicht für die ~30 Zeilen Fachlogik in `VoteSessionEndpoints.cs:107-137`. `VoteAudienceFilter` und `VoteEligibilityFilter` unterscheiden sich in genau einem `switch`-Arm; ein Copy-Paste-Fehler dort öffnet die Ergebnisseite dem falschen Publikum, ohne dass etwas rot wird.
> **Warum es zählt**: Jede Regel in `TwitchChatManager` stammt aus einem eigenen Produktionsausfall (2026-07-26 zweimal, 2026-07-27) — also aus dem Bereich mit der schlechtesten Erfolgsquote im ganzen Projekt. Track 2a hat sieben Befunde allein in dieser Datei gefunden, davon vier ursprünglich als S1 eingestuft. Die komplette Zustandsmaschine (sieben Zustände, vier Zähler, drei nebenläufige Akteure) ist ausschließlich durch Produktionsausfälle validiert.
> **Fix**: Kein neues Testprojekt pro Assembly. (1) Die Reconnect-**Policy** aus `TwitchChatManager` in eine eigene, TwitchLib-freie Klasse `ReconnectPolicy` extrahieren (`RegisterConnectionError()`, `RegisterReconnect()`, `Decide() → {Reconnect, Recreate}`); `TwitchChatManager` behält nur den Transport. Danach ist die Logik in `tests/EmotePurge.Infrastructure.Tests/Unit/` testbar, ohne neues Projekt und ohne TwitchLib. (2) Reine Unit-Tests für `ReconnectPolicy` und `EmoteUsageCounter.DrainAndReset`. (3) Für die Api ein einziger `WebApplicationFactory`-Test, der die Filter-Matrix (401/403/404/409) über alle Filter-Pfade abfährt. Den Log-Eintrag ergänzen, nicht ersetzen — mit Datum, wann und warum die alte Begründung nicht mehr trug.

> **S3-7 — Zwei gleichzeitige Votes laufen in den Unique-Index → 500; kein globales Exception-Handling**
> **S3** · Backend · **S** · Gefunden von Track 2b (B8), bestätigt von Track 1
> **Ort**: `src/EmotePurge.Infrastructure/Services/VoteSessionService.cs:107-127` · `src/EmotePurge.Api/Program.cs:100-105` (kein `UseExceptionHandler`)
> **Was**: `SingleOrDefaultAsync` → `null` → `Add` → `SaveChangesAsync`, ohne `try/catch` auf `DbUpdateException`. Der Unique-Index `(VoteSessionId, EmoteId, UserId)` ist die einzige Absicherung. Ohne `UseExceptionHandler` endet das in einem 500.
> **Warum es zählt**: Doppelklick auf Daumen-hoch. `vote()` (`vote-session-detail-page.ts:160-181`) deaktiviert den Button während des Requests nicht und liest `emote.myVote` aus dem noch nicht neu geladenen Ergebnis — beide Klicks nehmen den `castVote`-Pfad, der zweite kracht. Der Nutzer sieht generisches `voteFailed`, im Log steht ein Stacktrace. Zweiter Pfad in dieselbe Exception: `Vote.UserId` → `Users.Id` mit `Restrict` (`AppDbContext.cs:81-84`) — ein gültiges Cookie ohne `Users`-Zeile erzeugt eine FK-Violation. Positiv und geprüft: Ohne Exception-Handler-Middleware bricht Kestrel body-los ab, es leckt **kein** Stacktrace — es ist ein Fehlerbild-, kein Informationsleck-Problem.
> **Fix**: `try/catch (DbUpdateException)` in `CastVoteAsync`, bei Unique-Verletzung neu laden und als Update behandeln. Frontend: Vote-Button während des Requests disablen (löst sich mit S2-17 mit). Global: `UseExceptionHandler` mit ProblemDetails und stabilem `errorCode`, damit `apiErrorTranslationKey` (S2-13) auch 500er abbilden kann.

> **S3-8 — Laufender Löschlauf: kein Schutz gegen Tab-Schließen, kein globaler Fortschritt, kein Abbruch von außen**
> **S3** · Frontend · **S–M** · Gefunden von Track 2b (B4, B5), Track 3 · *Track 3 hatte S2; herabgestuft, weil Track 2b nachgewiesen hat, dass der Resync die DB in ≤60 s in Deckung bringt und der Schaden damit auf das Protokoll begrenzt bleibt*
> **Ort**: `web/src/app/core/seven-tv/seven-tv-delete.service.ts:73-87` (kein `beforeunload`/`CanDeactivate` im gesamten `web/src/`) · `web/src/app/shared/seven-tv/mass-delete-panel.ts:36-42`
> **Was**: Bei `DELETE_DELAY_MS = 275` dauert ein 800-Emote-Lauf ≥ 4 Minuten reine Wartezeit, realistisch 6–10 Minuten mit Latenz; die Queue lebt ausschließlich als In-Memory-Signal in einem `providedIn: 'root'`-Singleton. `DeleteProgressPanel` inklusive Abbrechen-Button ist nur gemountet, solange `UsageStatsPage`/`VoteSessionDetailPage` gerendert ist. Zusätzlich nutzt `delay()` `setTimeout`; Chrome drosselt Timer in Hintergrund-Tabs auf ≥ 1 s und nach 5 Minuten auf bis zu 1×/Minute.
> **Warum es zählt**: Der Mod startet den großen Purge und arbeitet vernünftigerweise in einem anderen Tab weiter — der Lauf kriecht auf Stunden hoch, ohne dass irgendetwas das sagt. Schließt er den Tab („dauert eh ewig"), bricht der Lauf bei 340/800 ab: 340 Emotes sind bei 7TV weg, `sync-deleted` läuft nie, und welche 460 offen sind, weiß niemand mehr — die Fehlerliste ist ersatzlos fort. Navigiert er innerhalb der App zur Übersicht, läuft die irreversible Löschung minutenlang weiter, **ohne Anzeige und ohne Stopp-Möglichkeit** außer Tab schließen. Der Entscheidungslog-Eintrag „kein globales Delete-Widget" ist über „man navigiert zur anderen Seite" formuliert und behandelt „man navigiert von beiden Seiten weg" gar nicht.
> **Fix**: (1) `window.addEventListener('beforeunload', …)` mit `preventDefault()`, solange `isRunning()`, plus `CanDeactivate`-Guard auf beiden Host-Routen. (2) Queue nach jedem `setStatus` in `sessionStorage` spiegeln. (3) Minimaler globaler Fortschritts-Streifen in `AppShell`, gebunden an `isRunning()`/`progress()`, mit Abbrechen — die Auswahl-Logik bleibt seiten-lokal, damit bleibt die dokumentierte Entscheidung intakt. (4) Im Bestätigungsdialog eine Laufzeit-Schätzung anzeigen („~{{count}} Emotes, geschätzt {{min}} Minuten. Tab bitte geöffnet und im Vordergrund lassen.").

> **S3-9 — Der Löschlauf bricht bei keinem systemischen Fehler ab und nutzt einen verworfenen Token weiter**
> **S3** · Frontend · **S** · Gefunden von Track 2b (B6)
> **Ort**: `web/src/app/core/seven-tv/seven-tv-delete.service.ts:64-67, 75-81, 117, 121-135`
> **Was**: Der Token wird einmal gelesen und in der `concatMap`-Closure gefangen. `deleteOne` fängt jeden HTTP-Fehler in `of({success:false})` (`:117`), es gibt keine Abbruchbedingung. Bei 401/403 löscht `describeHttpError` den Token (`:124`) — der Lauf feuert danach **denselben** Token weiter. Zweite Lücke: `describeHttpError` hängt am `catchError`-Pfad; ein GraphQL-typischer Auth-Fehler als HTTP 200 mit `errors[]` geht durch `map` (`:113-116`), der Token wird **nicht** invalidiert.
> **Warum es zählt**: Ein abgelaufener Token bei einem 500-Emote-Lauf bedeutet ~2,3 Minuten lang 500 garantiert scheiternde 401-Requests im 275-ms-Takt. Bei 429 dasselbe mit dem Risiko verschärfter Abuse-Erkennung — dass gehäufte Retries zum harten Kappen eskalieren, hat das Projekt bei Twitch bereits gelernt (`CLAUDE.md:175`). Kein Datenverlust, deshalb S3.
> **Fix**: Sentinel `{success:false, fatal:true}` für 401/403/429; im `concatMap` bei `fatal` `EMPTY`/`takeWhile`, sodass die Kette normal komplettiert (`finish()`/`sync-deleted` laufen für die Erledigten) und der Rest als `cancelled` markiert wird. Zusätzlich in `map` (`:113`) die GraphQL-`errors` auf 7TVs Auth-Code prüfen und `clearToken()` auslösen.

> **S3-10 — `setId` kann beliebig veraltet sein und wird vor dem Lauf nie revalidiert**
> **S3** · Frontend/Daten · **S** · Gefunden von Track 2b (B9)
> **Ort**: `web/src/app/features/usage-stats/usage-stats-page.ts:117-120` · `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs:71-75` · `mass-delete-panel.ts:131`
> **Was**: `activeEmoteSetId` kommt aus `Channel.ActiveEmoteSetId`, geschrieben nur vom 60-s-Resync (`SevenTvSyncService.cs:42`). Das Frontend holt den Wert einmal pro `load()` und reicht ihn in `startDelete` ungeprüft in jede Mutation.
> **Warum es zählt**: Wechselt der Broadcaster sein aktives Set, arbeitet das Frontend mindestens 60 s — bei stundenlang offenem Tab beliebig lange — gegen die **alte** Set-ID. Die Mutationen zielen dann auf ein Set, das nicht mehr das des Channels ist, aber existiert und einem Dritten gehören kann. Schmaler Auslöser, im Eintrittsfall aber unwiderruflich.
> **Fix**: In `startDelete` `getActiveEmoteSetId()` neu abrufen und vergleichen, bei Abweichung abbrechen. `set-warning` liest denselben Wert (`EmoteSetOwnershipService.cs:31,38`) — die Prüfung kann in dieselbe Antwort, dann kostet sie keinen zusätzlichen Roundtrip.

> **S3-11 — Der Watchdog nutzt Chat-Nachrichten als einzigen Lebendbeweis**
> **S3** · Worker · **M** · Gefunden von Track 2a (B9)
> **Ort**: `src/EmotePurge.Worker/TwitchConnectionWatchdog.cs:13, 43-55`; Datenquelle `TwitchChatManager.cs:282-286`
> **Was**: `StaleThreshold = 5 min` gilt gleichzeitig als Freeze-Schwelle und als Cooldown. Die Cooldown-Arithmetik selbst ist korrekt (geprüft), der Ansatz greift aber zu kurz.
> **Warum es zählt**: **Stiller Channel** (Broadcaster offline): `LastMessageReceivedUtc` bleibt stundenlang alt, der Cooldown reduziert von „jede Minute für immer" auf „alle 5 Minuten für immer" — die Schleife bleibt unendlich. Nach 8 solchen Reconnects greift der proaktive Recreate mit neuer justinfan-Identität; über eine Nacht sind das ~12 Reconnects und ~1,5 Identitätswechsel pro Stunde, dauerhaft — genau das Muster, das in `CLAUDE.md:175` als mutmaßlicher Abuse-Trigger identifiziert wurde, abgeschwächt statt beseitigt. **HandOfBlood**: Die 5-Minuten-Schwelle ist zu großzügig; HoB hat auch in ruhigen Momenten mehrere Nachrichten pro Sekunde, 5 Minuten Stille bedeuten dort mit an Sicherheit grenzender Wahrscheinlichkeit einen Defekt — man wartet trotzdem 5 min plus bis zu 60 s Tick.
> **Fix**: Den Lebendbeweis von der Anwendungs- auf die IRC-Ebene ziehen. `OnSendReceiveData` feuert für **jede** empfangene IRC-Zeile inklusive Twitchs `PING` (~alle 5 min auch auf stillen Verbindungen):
> ```csharp
> client.OnSendReceiveData += (_, e) => {
>     if (e.Direction == SendReceiveDirection.Received)
>         Interlocked.Exchange(ref _lastIrcActivityUtcTicks, DateTime.UtcNow.Ticks);
>     return Task.CompletedTask;
> };
> ```
> Der Watchdog prüft dann `LastIrcActivityUtc` (Socket lebt?) statt `LastMessageReceivedUtc` (Chat lebt?). Damit verschwindet der Churn auf stillen Channels vollständig, `StaleThreshold` kann auf 2–3 min gesenkt werden, und `LastMessageReceivedUtc` bleibt als reines Health-/Diagnosefeld erhalten.

> **S3-12 — Eine erfolgreiche 7TV-Antwort mit leerem Emote-Set archiviert alles und leert den Match-Cache**
> **S3** · Worker/Daten · **S** · Gefunden von Track 2a (B10)
> **Ort**: `src/EmotePurge.Infrastructure/Services/SevenTvSyncService.cs:44-46, 88-94, :72`
> **Was**: Bei einem *Fehler* bricht `SyncChannelAsync` korrekt ab, ohne den Cache anzufassen (`:31-33`, `:36-39` — geprüft und gut). Bei einer **erfolgreichen Antwort mit leerer Emote-Liste** läuft dagegen der Vollpfad: `ReconcileAsync` setzt `IsArchived = true` auf *alle* bestehenden Emotes, `RefreshMatchCacheAsync` filtert auf `!e.IsArchived` und `ReplaceChannel` schreibt ein leeres Dictionary.
> **Warum es zählt**: Danach bricht `TwitchChatManager.cs:292` für jede Nachricht sofort ab → **kompletter Matching-Ausfall bis zum nächsten erfolgreichen Sync (bis zu 60 s)**. Bei HandOfBlood-Frequenz sind das Tausende verlorener Treffer pro Vorfall. Auslöser: 7TV liefert ein Set mit `emotes: []` (Set-Wechsel in Bearbeitung, 7TV-Teilausfall, Owner leert das Set kurzzeitig). Ein echter Set-Wechsel und ein einzelner Fehl-Response sind im Ergebnis nicht unterscheidbar.
> **Fix**: Plausibilitätsgrenze vor `ReconcileAsync`:
> ```csharp
> if (emoteSet.Emotes.Count == 0 && await db.Emotes.CountAsync(e => e.ChannelId == channel.Id && !e.IsArchived, ct) > 0)
> {
>     logger.LogWarning("7TV meldet 0 aktive Emotes für {Channel}, obwohl bisher welche bekannt waren — Sync übersprungen.", normalized);
>     return emoteSet.Id;
> }
> ```
> Optional strenger: einen Rückgang um >90 % erst nach zwei aufeinanderfolgenden identischen Antworten anwenden.

> **S3-13 — `UsageStatFlushService` macht Read-then-Insert statt Upsert; ein Konflikt verwirft den kompletten Batch**
> **S3** · Worker/Daten · **M** · Gefunden von Track 2a (B11)
> **Ort**: `src/EmotePurge.Infrastructure/Services/UsageStatFlushService.cs:35-51`, Aufrufer `src/EmotePurge.Worker/UsageFlushWorker.cs:21-26`
> **Was**: `FlushAsync` liest die existierenden Zeilen und entscheidet pro Emote zwischen `+=` und `Add`. Das ist nur korrekt, solange es genau einen Schreiber gibt. `UsageFlushWorker.StopAsync` ruft `FlushOnceAsync` **bevor** `base.StopAsync` das `_stoppingCts` auslöst — die `PeriodicTimer`-Schleife ist also noch scharf. Fällt ein 30-s-Tick in die Laufzeit des finalen Flush, laufen zwei `FlushOnceAsync` parallel: beide sehen „Zeile existiert nicht" und fügen sie ein → Unique-Violation, ein kompletter Batch wird verworfen.
> **Warum es zählt**: Das Fenster ist eng, aber bei HandOfBlood ist ein Batch bis zu 30 s Chatnutzung über ~1.000 Emotes. Und der Flush ist ohnehin nicht retry-fähig: `DrainAndReset()` leert den Zähler **vor** dem `try`, jeder Fehler bedeutet Totalverlust dieses Batches. Der Log-and-drop-Kommentar begründet zu Recht, warum nicht geworfen wird — er begründet nicht, warum nicht wiederholt wird.
> **Fix**: (1) Reihenfolge in `StopAsync` umdrehen: erst `await base.StopAsync(ct)` (beendet die Schleife), dann `await FlushOnceAsync(ct)`. (2) Auf atomaren Upsert umstellen: `INSERT … ON CONFLICT ("EmoteId","Date") DO UPDATE SET "UseCount" = "UsageStats"."UseCount" + EXCLUDED."UseCount"` — das spart zugleich den `existingStats`-Roundtrip mit ~1.000er-`IN`-Liste alle 30 s und macht den Pfad mehrschreiberfest. Die `validIds`-Vorabprüfung bleibt nötig. (3) Optional: bei Fehlschlag die Counts per `usageCounter.Merge(counts)` zurückschreiben statt zu verwerfen, mit Obergrenze.

> **S3-14 — Die Log-Zeile beim Recreate ist im proaktiven Pfad sachlich falsch**
> **S3** · Worker · **S** · Gefunden von Track 2a (B12) — *Severity bewusst nicht S4, weil Logs hier das einzige Forensik-Werkzeug sind*
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:101-103`
> **Was**: `RecreateClientAsync` loggt bedingungslos „{Count} aufeinanderfolgende Verbindungsfehler — TwitchClient wird komplett neu instanziiert". Über den proaktiven Pfad (`:77-84`) steht `_consecutiveConnectionErrors` typischerweise auf **0** — im Log erscheint dann „0 aufeinanderfolgende Verbindungsfehler", direkt unter der korrekten Begründung des Aufrufers (`:79-81`). Zwei widersprüchliche Warnungen untereinander.
> **Warum es zählt**: Zwei der drei Prod-Ausfälle wurden über Log-Archäologie diagnostiziert, und beide aktuellen Fixes sind noch nicht live verifiziert. Beim nächsten Vorfall ist genau diese Zeile der Beleg dafür, welcher Recreate-Pfad gegriffen hat — und sie behauptet in einem der Pfade das Gegenteil der Wahrheit. Eintrittswahrscheinlichkeit: 100 %, sobald der proaktive Pfad das erste Mal feuert.
> **Fix**: Grund als Parameter übergeben (`RecreateClientAsync(string reason)`) und ausgeben; die redundanten Warnungen in `:70-72`/`:79-81` entfallen. Nach S2-1 fällt der proaktive Pfad ohnehin weg, die Signatur mit `reason` bleibt trotzdem die robustere Form.

> **S3-15 — Emote-Grid und Vote-Buttons sind per Tastatur und Screenreader nicht bedienbar**
> **S3** · A11y · **M** (Grid-Semantik) / **L** (Roving-Tabindex über die Virtualisierung) · Gefunden von Track 3 · *Track 3 hatte S2; nach der vereinheitlichten Skala S3, weil weder Datenverlust noch Launch-Blocker im definierten Sinn — der Befund bleibt aber der schwerwiegendste der A11y-Gruppe*
> **Ort**: `usage-stats-page.html:102-105` · `vote-session-detail-page.html:78, 85-88, 95-124` · `web/src/app/shared/emotes/emote-card-header.ts:11`
> **Was**: Die Karte ist ein `<div class="… cursor-pointer" (click)="selection.onRowClick(…)">` ohne `role`, `tabindex` oder Keydown-Handler; die einzige semantisch fokussierbare Alternative — die Checkbox darin — trägt explizit `tabindex="-1"`. Im gesamten Usage-Stats-Grid gibt es damit **kein einziges fokussierbares Element**, weshalb auch der `cdk-virtual-scroll-viewport` per Tastatur nicht scrollbar ist. Auf der Voting-Seite kommen dazu: statische `aria-label` auf beiden Vote-Buttons, die die Stimmenzahl überschreiben und keinen Emote-Namen enthalten; `myVote` ausschließlich über Textfarbe kodiert, kein `aria-pressed`; bei beendeter Session bleiben beide Buttons aktiv (Klick → 409 → Fehlerbanner).
> **Warum es zählt**: Ein Mod, der die Maus nicht nutzt (RSI, temporäre Verletzung, Screenreader), kann die Kernaufgabe des Tools — Emotes auswählen und löschen — überhaupt nicht ausführen; es gibt keinen Workaround und keine alternative Ansicht. Auf der Voting-Seite hört ein Screenreader-Nutzer 1.000-mal „Behalten, Schaltfläche", ohne zu wissen, um welches Emote es geht oder ob er bereits gestimmt hat, und ein Tastaturnutzer kommt bei `ROW_HEIGHT_PX = 176` in einem 512-px-Viewport über die ersten ~14 Emotes nicht hinaus, weil `*cdkVirtualFor` den Rest gar nicht rendert. Für eine Voting-Funktion, die per geteiltem Link an alle Zuschauer geht, schließt das eine Nutzergruppe vollständig aus.
> **Fix**: `tabindex="-1"` in `emote-card-header.ts:11` entfernen und die Checkbox als echtes Bedienelement nutzen (`[attr.aria-label]="name()"`, den bereits existierenden, nirgends gebundenen `checkboxClick`-Output verdrahten). Auf der Karte `role="checkbox"` + `[attr.aria-checked]` + `tabindex="0"` + `(keydown.space)`/`(keydown.enter)` → `onRowClick`; `(keydown.shift.arrowdown/arrowup)` auf Range-Erweiterung mappen. Vote-Buttons: `[attr.aria-label]="(… | transloco) + ': ' + emote.emoteName"`, `[attr.aria-pressed]`, `[disabled]="!session.isActive"`, plus einen nicht-farblichen Zustandsindikator. Für die Erreichbarkeit über die Virtualisierung hinaus: Roving-Tabindex (`FocusKeyManager` aus `cdk/a11y` bzw. `(keydown.arrowdown)` → `viewport.scrollToIndex(nextRow)` + Fokus setzen) — als pragmatische Zwischenlösung Filter auf ≤ 100 Treffer plus Paginierung als alternative Ansicht. Referenz im eigenen Repo: `shared/i18n/language-switcher.ts:15-16` und `datetime-picker.ts`.

> **S3-16 — Modale Dialoge ohne Fokusfalle, Escape und Beschriftung; Statusmeldungen ohne `aria-live`**
> **S3** · A11y · **S** · Gefunden von Track 3
> **Ort**: `delete-confirm-dialog.ts:11,13` · `mass-delete-panel.ts:54` · `overview-page.html:123`, `usage-stats-page.html:87`, `vote-session-list-page.html:60,83`, `vote-session-detail-page.html:72`, `channel-workspace-layout.ts:36`
> **Was**: `role="dialog" aria-modal="true"` ist gesetzt, es fehlen aber `aria-labelledby` auf die `<h2>`, Initialfokus, Fokusfalle, Escape-zum-Schließen, Klick-auf-Overlay und Fokus-Rückgabe. Alle Fehlerbanner und das „✓ Kopiert"-Feedback erscheinen über `@if` als normale `<p>` ohne `role="alert"`/`aria-live`.
> **Warum es zählt**: Ein Screenreader-Nutzer bekommt `aria-modal="true"` gemeldet, tabbt und landet im inaktiven Hintergrund inklusive der Emote-Karten. Ein Tastaturnutzer, der den Löschdialog wegdrücken will, drückt Escape — nichts; die einzige Abbruchmöglichkeit ist ein Mausklick. Und wer „Beenden" klickt und einen 403 bekommt, erfährt gar nichts: Der DOM ändert sich, die Ansage bleibt aus, er wiederholt die Aktion.
> **Fix**: `@angular/cdk` ist bereits Dependency — `cdkTrapFocus` + `cdkFocusInitial` aus `A11yModule` decken Fokusfalle und Initialfokus ab, dazu `(keydown.escape)="cancelled.emit()"` auf dem Overlay und `[attr.aria-labelledby]` mit statischer Id auf die Überschrift. Mittelfristig sauberer: `Dialog` aus `@angular/cdk/dialog`. Für die Meldungen: `role="alert"` auf alle Fehlerbanner, `role="status"` auf das Kopier-Feedback und die „{{shown}} von {{total}} Emotes"-Zeile.

> **S3-17 — Formularfelder ohne Label; Placeholder- und Sekundärtext unter dem AA-Kontrastminimum**
> **S3** · A11y · **S** · Gefunden von Track 3
> **Ort**: `usage-stats-page.html:23-61` (2 Datumsfelder + Min + Max + Name, kein einziges `<label>`), `vote-session-detail-page.html:26-48`, `overview-page.html:77-82`, `vote-session-list-page.html:6-11`, `seven-tv-token-input.ts:37-42`; Kontrast u. a. `vote-session-detail-page.html:6,93`, `overview-page.html:43,62,104`, `vote-session-list-page.html:73`, `my-votings-page.ts:36`, `landing-page.html:154`
> **Was**: Alle genannten Felder haben ausschließlich ein `placeholder`-Attribut; die beiden `<input type="date">` nicht einmal das. `placeholder:text-slate-600` gegen `bg-slate-950` ergibt ≈2,5:1 (AA-Minimum 4,5:1) — betroffen ist auch das 7TV-Token-Feld. `text-slate-500` als Standard für Sekundärtext ergibt ≈3,8–4,0:1, fast durchgängig bei `text-xs`/`text-sm`. Auffällig inkonsistent: `usage-stats-page.html:110` nutzt für dieselbe Information (`{{ totalUseCount }}x`) `text-slate-400` (≈7:1).
> **Warum es zählt**: Der Placeholder verschwindet beim Tippen — wer nach einer Unterbrechung zurückkommt, sieht zwei nackte Zahlenfelder und weiß nicht mehr, welches Min und welches Max ist; ein Screenreader liest „Bearbeiten, leer" ohne Namen. Und „Bot inaktiv", „Beendet", „Noch nicht getrackt" sowie die Score-Zahl auf jeder Emote-Karte sind genau die Informationen, an denen ein Mod eine Löschentscheidung festmacht — sie sind am schlechtesten lesbar, ausgerechnet auf den hellen, schlecht kalibrierten Zweitmonitoren typischer Streamer-Setups.
> **Fix**: `<label class="sr-only">` bzw. mindestens `[attr.aria-label]` auf jedes Feld (`language-switcher.ts:16` macht es bereits richtig), sichtbare Labels „von"/„bis" für die Datumsfelder. `placeholder:text-slate-600` → `placeholder:text-slate-400`, und projektweit `text-slate-500` → `text-slate-400` für Textinhalte (nicht für Icons/Trenner) — ein Suchen-und-Ersetzen, das sofort ≈7:1 bringt.

> **S3-18 — Ausgewählte Emote-Karten haben keinen sichtbaren Auswahlzustand außer einer 14-px-Checkbox**
> **S3** · UX · **S** · Gefunden von Track 3
> **Ort**: `usage-stats-page.html:103` (statisches `class`), `vote-session-detail-page.html:86`, Checkbox `emote-card-header.ts:6-14` (`h-3.5 w-3.5`)
> **Was**: Der Karten-Container hat eine feste Klassenliste (`border-slate-800 bg-slate-900`); ob ein Emote ausgewählt ist, zeigt ausschließlich die 14×14-px-Checkbox oben links.
> **Warum es zählt**: Bei 8 Spalten sind die Karten ~110 px breit. Um zu prüfen, welche der ~36 sichtbaren Emotes markiert sind, muss man 36 winzige Checkboxen einzeln absuchen. Vor einer irreversiblen Löschung von 300 Emotes ist „Ich sehe auf einen Blick, was markiert ist" die wichtigste Kontrolle überhaupt — und sie fehlt. Verschärft S1-3 zusätzlich.
> **Fix**: `[class]` binden: bei `selection.isSelected(emote)` → `ring-2 ring-purple-500 bg-purple-950/40`, sonst der bisherige Stil. Ein Zweizeiler pro Template.

> **S3-19 — Guards leiten stumm um; während der Guard-Probe passiert sichtbar nichts**
> **S3** · UX · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/core/channels/usage-stats-access.guard.ts:26-28` · `web/src/app/core/voting/vote-session-access.guard.ts:33-35`
> **Was**: Beide Guards feuern einen echten HTTP-Call, *bevor* navigiert wird, fangen jeden Fehler ab und geben ein `UrlTree` auf eine andere Route zurück — ohne Grund zu speichern, ohne Meldung, ohne Query-Param.
> **Warum es zählt**: Ein Streamer teilt den Link zu einer „nur Subs"-Abstimmung in seinem Discord. Ein Nicht-Sub klickt, ist eingeloggt, und landet nach 0,5–3 Sekunden sichtbarer Untätigkeit auf der Session-**Liste** desselben Channels. Kein Wort dazu, warum. Er klickt die Session dort an, dasselbe passiert erneut. Das sieht aus wie ein kaputter Link, nicht wie eine Zugriffsregel. Dieselbe Mechanik wirft einen Mod ohne Nutzungsdaten-Recht von der URL auf die Voting-Seite.
> **Fix**: Grund im `UrlTree` mitgeben (`{ queryParams: { denied: 'audience' } }`) und auf der Zielseite als Hinweisbanner rendern. Zusätzlich einen globalen Router-Fortschrittsbalken (`router.events` → `NavigationStart/End`) in `AppShell`, damit die Guard-Wartezeit nicht als „nichts passiert" wahrgenommen wird.

> **S3-20 — Spaltenzahl folgt `window.innerWidth`, der Container ist aber auf 1024 px gedeckelt**
> **S3** · UX · **M** · Gefunden von Track 3
> **Ort**: `web/src/app/shared/grid/grid-columns.ts:6-13` gegen `web/src/app/features/shell/app-shell.ts:65` (`max-w-5xl`)
> **Was**: `computeGridColumns` staffelt bis 8 Spalten ab 1536 px Fensterbreite, der Inhaltsbereich ist aber auf ~992 px begrenzt. Ab 1024 px Fensterbreite wächst nur noch die Spaltenzahl, nie die Fläche.
> **Warum es zählt**: Auf einem 2560-px-Monitor sitzen 8 Karten à ~110 px in einem 992-px-Streifen mitten auf einem leeren Bildschirm; der Emote-Name (`truncate`, `text-xs`) ist bei 110 px für die meisten Emotes abgeschnitten — man muss hovern, um zu sehen, was man löscht. In Kombination mit `h-128` (512 px feste Viewport-Höhe an vier Stellen) sieht der Nutzer bei 1.000 Emotes ~36 bzw. ~24 auf einmal, während 60 % des Bildschirms ungenutzt bleiben.
> **Fix**: (1) Auf den beiden Grid-Seiten aus dem `max-w-5xl`-Container ausbrechen (`max-w-none` per Route oder eigenes Layout). (2) `computeGridColumns` die tatsächliche Container-Breite füttern statt `window.innerWidth` — `ResizeObserver` auf dem Viewport-Element, das löst nebenbei die fehlende Entprellung. (3) `h-128` durch `h-[calc(100vh-22rem)] min-h-96` ersetzen. (4) Falls (1) zu invasiv: mindestens die Breakpoints an die effektiven ~992 px anpassen, statt bis 8 Spalten hochzugehen.

> **S3-21 — Karten auf der Voting-Seite wirken für reine Voter klickbar, tun aber nichts Sichtbares**
> **S3** · UX · **S** · Gefunden von Track 3
> **Ort**: `vote-session-detail-page.html:86` (`cursor-pointer` + `hover:bg-slate-800/70` unbedingt) i. V. m. `:89` (`[showCheckbox]="canManage()"`)
> **Was**: Für Nutzer ohne Management-Rechte wird die Checkbox nicht gerendert, der Klick-Handler auf der Karte bleibt aber aktiv, ebenso `cursor-pointer` und Hover-Effekt. `onRowClick` toggelt eine Auswahl, die nie sichtbar und nie nutzbar wird.
> **Warum es zählt**: Ein Sub klickt auf die Emote-Karte, um abzustimmen — die naheliegendste Geste. Es passiert sichtbar nichts. Er klickt nochmal. Die eigentlichen Vote-Buttons sind zwei ~26-px-Icons am unteren Kartenrand.
> **Fix**: `[class.cursor-pointer]="canManage()"` und den Klick-Handler nur bei `canManage()` binden. Optional für Nicht-Manager den Kartenklick auf „Behalten" legen, mit deutlicher Zustandsanzeige.

> **S3-22 — Logout ohne Fehlerbehandlung — schlägt die Anfrage fehl, passiert überhaupt nichts**
> **S3** · UX/Security · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/core/auth/auth.service.ts:64-71`
> **Was**: `this.http.post('/api/auth/logout', {}).subscribe(() => { … })` — nur `next`, kein `error`. Bei Netzwerkfehler oder 5xx feuert der Callback nie: `currentUser` bleibt gesetzt, das 7TV-Token bleibt in `sessionStorage`, keine Navigation, keine Meldung.
> **Warum es zählt**: Ein Mod loggt sich am Rechner des Streamers oder im Studio aus, klickt „Logout" — der Header zeigt weiterhin seinen Namen. Er hält das für Trägheit und geht. Session-Cookie und **7TV-Schreib-Token** bleiben aktiv; bei einem Token, das laut eigener Warnung (`de.json:246`) vollen Schreibzugriff auf den 7TV-Account gewährt, ist das kein kosmetisches Problem.
> **Fix**: `error`-Handler ergänzen, der den clientseitigen State in jedem Fall zurücksetzt (`currentUser.set(null)`, `sevenTvTokenService.clearToken()`, Navigation zu `/login`) und zusätzlich warnt, dass die Server-Session eventuell noch aktiv ist. Ein serverseitiger Logout-Fehler darf den clientseitigen Reset nicht blockieren.

> **S3-23 — Vote-Session-Liste zeigt weder Zielgruppe noch Zeitraum noch Beteiligung**
> **S3** · UX · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/features/voting/vote-session-list-page.html:68-99`
> **Was**: Pro Eintrag nur Titel plus „Aktiv"/„Beendet". `VoteSessionSummary` liefert bereits `allowedVoterRoles`, `startedAt` und `endedAt` (`vote-session.model.ts:16-23`) — nichts davon wird gerendert.
> **Warum es zählt**: Ein HoB-Mod hat drei parallele Abstimmungen laufen („Alle", „Nur Subs", „Nur Mods") und kann sie nur am frei getippten Titel unterscheiden. Beim Kopieren des Share-Links weiß er nicht, welche Zielgruppe er gerade in den Chat postet — und Nicht-Berechtigte, die klicken, werden kommentarlos umgeleitet (S3-19). Ebenso fehlt jeder Anhaltspunkt, welche Session sich zum Beenden lohnt.
> **Fix**: Rollen-Badge (analog `overview.roleBadges`) plus formatiertes `startedAt`/`endedAt`; `formatDateTime` aus `vote-session-detail-page.ts:114-119` in einen geteilten Helper heben, damit der Sprachwechsel greift. Wenn günstig zu beschaffen: Stimmenzahl pro Session.

> **S3-24 — 7TV-Token lässt sich nicht vorab hinterlegen**
> **S3** · UX · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/shared/seven-tv/mass-delete-panel.ts:44-62` (Token-Eingabe nur im `@if (showConfirm())`-Zweig), Anleitung in `seven-tv-token-input.ts:19-49` / `de.json:240-248`
> **Was**: Der Token-Dialog erscheint ausschließlich, nachdem Emotes selektiert und „Löschen (n)" geklickt wurde. Es gibt keine Einstellungs- oder Profilseite zur Vorbereitung.
> **Warum es zählt**: Die Erklärung selbst ist gut — vierstufige Anleitung mit browser-spezifischen Menünamen, `7tv-token` als `<code>` hervorgehoben, `type="password"`, ehrliche Sicherheitswarnung. Der **Zeitpunkt** ist das Problem: Der Mod hat gerade 300 Emotes markiert und wird dann in einen 4-Schritte-DevTools-Ausflug geschickt. Öffnet er 7tv.app im selben Tab, ist die App neu geladen und die Auswahl komplett weg. Da der Token in `sessionStorage` liegt (begründete Entscheidung), wiederholt sich das bei jeder neuen Browser-Session.
> **Fix**: `SevenTvTokenInput` zusätzlich als jederzeit erreichbaren, ausklappbaren Bereich oberhalb des Grids anbieten, mit Statusanzeige („7TV-Token gesetzt ✓ / nicht gesetzt"). In Schritt 2 der Anleitung explizit „in einem **neuen Tab**" ergänzen; ein kleines Screenshot/GIF senkt die Hürde für nicht-technische Mods spürbar.

> **S3-25 — Das Ergebnis eines Löschlaufs ist nicht abschließbar und nicht als „fertig"/„abgebrochen" erkennbar**
> **S3** · UX · **S** · Gefunden von Track 3, Track 2b (B10)
> **Ort**: `web/src/app/shared/seven-tv/delete-progress-panel.ts:11-18, 41-45` · `web/src/app/core/seven-tv/seven-tv-delete.service.ts:100-103` (`reset()` hat außerhalb der Spec keinen Aufrufer)
> **Was**: Das Panel zeigt nur „{{finished}} / {{total}} verarbeitet" plus Balken — kein Abschluss-Text, kein Resümee, kein Schließen-Button; der Abbrechen-Button verschwindet, sobald `isRunning()` false ist. `finished()` zählt nur `done`+`failed`, nach einem Abbruch bleibt der Balken bei z. B. 6 % stehen, ohne dass irgendwo „abgebrochen" steht. Da `mass-delete-panel.ts:36` schon bei `queue().length > 0` rendert und `reset()` nie gerufen wird, bleibt das Panel für die gesamte SPA-Session sichtbar.
> **Warum es zählt**: Nach einem 800-Emote-Lauf steht ein Balken bei 94 % und darunter eine rote Liste mit 47 Namen. Ist der Lauf fertig? Hängt er? Sind die 47 gelöscht? Wechselt der Mod danach zu einem anderen Channel, begrüßt ihn dort dasselbe fremde Ergebnis-Panel von Channel A, und bei einem späteren Lauf sind alte und neue Fehler nicht auseinanderzuhalten. Die 47 fehlgeschlagenen Emotes lassen sich nicht mit einem Klick erneut versuchen — sie müssen im Grid manuell wiedergefunden werden, was durch S2-16 zusätzlich erschwert wird.
> **Fix**: Abschluss-Zeile („Fertig: 753 gelöscht, 47 fehlgeschlagen, 0 abgebrochen"), Abbruch-Zustand als eigener Text, „Schließen"-Button → `deleteService.reset()`, „Fehlgeschlagene erneut versuchen" → `startDelete` mit den `failed`-Items, plus Reset bei Channel-Wechsel in `startDelete`.

> **S3-26 — Channel-/Session-Lookup sechsmal kopiert; `Normalize` an 27 Stellen ausgeschrieben**
> **S3** · Architektur · **S** · Gefunden von Track 4 (A-5, A-6)
> **Ort**: `VoteSessionService.cs:54-66, 81-93, 133-145, 165-177` · `VoteSessionQueryService.cs:41-52` · extrahierter Helper: `VoteEligibilityService.cs:47-59`; Normalisierung u. a. in `ChannelService.cs:15,36,53`, `MyChannelsService.cs:23,45,70`, `ChannelAccessService.cs:14,36`, `UsageStatQueryService.cs:11,28`, `EmoteService.cs:11`, `SevenTvSyncService.cs:19`, `ChannelNameValidation.cs:9`
> **Was**: Der Block „Channel per normalisiertem Namen laden → wenn null raus → Session per `Id == sessionId && ChannelId == channel.Id` laden → wenn null raus" existiert sechsmal in drei Dateien. `VoteEligibilityService` hat ihn am 2026-07-27 als privates `LoadSessionAsync` extrahiert — die anderen beiden Services haben davon nichts mitbekommen. Dieselbe `Trim().ToLowerInvariant()`-Normalisierung steht 27-mal; `EmoteMatchCache.cs:21` hat sie als private Methode, also in der richtigen Form, aber nicht wiederverwendbar.
> **Warum es zählt**: `s.ChannelId == channel.Id` ist die einzige Absicherung dagegen, dass Channel A eine Session von Channel B beendet, löscht oder abstimmt — sie steht sechsmal handgeschrieben, und genau eine dieser Stellen (`DeleteAsync`) ist getestet. Bei der Normalisierung liegt der Gewinn nicht in gesparten Zeichen, sondern darin, dass der Ausdruck die stille Invariante „`Channel.ChannelName` in der DB ist immer lowercase und getrimmt" trägt — sie steht nirgends geschrieben und wird nirgends durchgesetzt; ein künftiger Schreibpfad kann sie brechen, und dann findet **keiner** der 27 Lookups mehr etwas.
> **Fix**: (1) Ein internes `LoadChannelSessionAsync(this AppDbContext db, string channelName, long sessionId, CancellationToken ct)` in `Infrastructure/Persistence/` (kein generisches Repository), plus eine Ein-Argument-Variante nur für den Channel; die sechs Call-Sites rufen sie auf. (2) `public static class ChannelName { public static string Normalize(string value) => value.Trim().ToLowerInvariant(); }` in `EmotePurge.Core/Entities/`, mit XML-Doc, der die Invariante ausspricht; `EmoteMatchCache.Normalize` und `ChannelNameValidation.cs:9` darauf umstellen.

> **S3-27 — `Architectur.md` widerspricht dem Code; `CLAUDE.md` (93 KB) wird seit drei Feature-Commits nicht mehr gepflegt**
> **S3** · Doku · **M** · Gefunden von Track 4 (A-7, A-8), Randbefund auch Track 5 (I14)
> **Ort**: `Architectur.md:21, 116, 143, 213, 240-330, 337` · `CLAUDE.md:11` (7.716 B in einer Zeile), `:104-179` (72 Einträge, 74,5 % der Datei), `:106`
> **Was**: (a) `Architectur.md:143` behauptet, `User`/`VoteSession`/`Vote`/`AllowedRoles` seien *„noch nicht implementiert"* — sie existieren seit dem 2026-07-25 samt Migration; der eingebettete Code-Block trägt weiterhin den Kommentar „Noch nicht implementiert (Modul B/C)". `:116` nennt einen Guard, der nicht mehr existiert, und behauptet, die Voting-Routen seien *„bewusst ohne Login-Zwang"* — am 2026-07-27 wurde genau das umgekehrt. `:21` verspricht Wiederherstellung von „WebSocket"-Verbindungen, die es seit dem 2026-07-25 nicht mehr gibt. Der Produktionspfad (GHCR, Portainer, `docker-compose.prod.yml`, `publish.yml`) hat **0 Treffer**; der eingebettete Compose-Block zeigt `api.ports: "8080:8080"` statt der Loopback-Bindung — also genau die Konfiguration, unter der `ForwardedHeaders` mit leerem `KnownProxies` eine Sicherheitslücke wäre. `my-votings`, `/welcome`, i18n und Pagination fehlen in beiden Dokumenten. (b) `CLAUDE.md` hat 179 Zeilen bei 93.244 Bytes; die eigene Regel `:106` („diese Liste ergänzen, nicht nur lesen") wurde nach `bcd6f65` dreimal nicht befolgt: `/welcome` als neue öffentliche Route, der stabile Fehlercode-Kontrakt und die komplette i18n-Schicht haben keinen Log-Eintrag.
> **Warum es zählt**: `CLAUDE.md:9` weist explizit an, vor Architektur-Fragen zuerst `Architectur.md` zu lesen. Wer dem folgt, baut das nächste Modul gegen ein Datenmodell, von dem die Spec sagt, es sei zur Hälfte nicht implementiert, und gegen ein Auth-Modell, das seit vier Wochen umgekehrt wurde — falsche Doku ist teurer als fehlende. Und die drei fehlenden Log-Einträge sind genau die Sorte Entscheidung, für die das Log existiert: Der Fehlercode-Vertrag lebt jetzt manuell synchron in vier Dateien (S3-29).
> **Fix**: (a) `Architectur.md`: `:143`/`:213` durch einen Hinweis ersetzen, dass alle sechs Entitäten implementiert sind, mit Verweis auf `src/EmotePurge.Core/Entities/` — den C#-Block auf die Abweichungs-Hinweise eindampfen statt den Code zu spiegeln. `:116` neu schreiben (aktuelle Routen inkl. `/welcome`, `/my-votings`, korrekte Guard-Namen, Login-Pflicht). `:21` „WebSocket" → „7TV-REST-Vollsync". Abschnitt 6 in **6a Lokal** / **6b Produktion** teilen und statt beider YAMLs nur die Unterschiede tabellarisch plus Dateiverweis — der eingebettete Block ist genau deshalb veraltet, weil er eine Kopie ist. Modul D um i18n, Fehlercode-Vertrag und Pagination ergänzen. (b) `CLAUDE.md` auf ≤ 12 KB: Projekt-Überblick (5 Zeilen), Umsetzungsstand als Sechs-Zeilen-Tabelle statt 7,7-KB-Absatz, Commands unverändert, die Schichtentreue-Tabelle aus Anhang E, und aus den 72 Log-Einträgen die ~14 *geltenden Regeln* als imperative Liste. Die vollständige Historie wandert **wortgleich** nach `docs/DECISIONS.md`, absteigend nach Datum, jeder Eintrag mit Kopfzeile `### <Datum> — <Titel>` + `**Betrifft:** <Pfad>` (macht das Log per `grep <dateiname>` durchsuchbar). Ein einziger `docs:`-Commit für beides, damit `git show` belegt, dass verschoben und nicht gelöscht wurde. Als Rückfallsperre eine Regel: *„Ein Feature-Commit, der eine Konvention, einen Vertrag oder eine Topologie ändert, enthält den Log-Eintrag im selben Commit."*

> **S3-28 — Worker greift zweimal direkt auf `AppDbContext` zu; kein automatischer Schichten-Check**
> **S3** · Architektur · **S** · Gefunden von Track 4 (A-9)
> **Ort**: `src/EmotePurge.Worker/Worker.cs:25-29` und `src/EmotePurge.Worker/SevenTvPeriodicResyncWorker.cs:33-39`
> **Was**: Beide Hosted Services holen sich `AppDbContext` aus dem Scope und schreiben zeichengleich `db.Channels.Where(c => c.IsBotActive).Select(c => c.ChannelName).ToListAsync(ct)`. Die dokumentierte Regel verbietet direkten `AppDbContext`-Zugriff explizit nur „aus Minimal-API-Handlern"; für den Worker gibt es keine Aussage.
> **Warum es zählt**: „Welche Channels sind aktiv" ist eine fachliche Frage, die es als `IChannelService.ListAllAsync` fast schon gibt. Wichtiger: Das ist die einzige Stelle im Repo, an der die Schichtenregel faktisch umgangen wird, und nichts hält das auf — `Api` hat über den transitiven Infrastructure-Verweis ebenfalls vollen Zugriff auf `AppDbContext`; dass heute kein Endpoint ihn nutzt, ist Disziplin, keine Struktur.
> **Fix**: (1) `IChannelService` um `Task<IReadOnlyList<string>> ListActiveChannelNamesAsync(CancellationToken)` erweitern, beide Worker rufen das — vier Zeilen, und die Query wird testbar (Postgres-Fixture existiert). (2) Ein Architektur-Test in `tests/EmotePurge.Infrastructure.Tests/Unit/`, reines Reflection, kein NetArchTest nötig: `typeof(Channel).Assembly.GetReferencedAssemblies()` darf nichts enthalten, das mit `Microsoft.EntityFrameworkCore`, `StackExchange.Redis`, `Microsoft.AspNetCore` oder `System.Net.Http` beginnt. Zwanzig Zeilen im bestehenden CI-Gate, und aus der Kernregel des Projekts wird ein roter Build statt eines Review-Kommentars.

> **S3-29 — Der Fehlercode-Kontrakt lebt manuell synchron in vier Dateien, ohne jeden Abgleich**
> **S3** · Architektur · **S** · Gefunden von Track 4 (A-10)
> **Ort**: `src/EmotePurge.Api/Validation/ApiErrorCodes.cs:11-32` (22 Konstanten) · `web/src/app/core/i18n/api-error.ts:5-26` (20 Einträge) · `web/public/i18n/de.json` + `en.json` (je 20 `errors.api.*`-Keys)
> **Was**: Der Kontrakt ist heute korrekt. Die zwei „fehlenden" C#-Konstanten (`NoHealthData`, `HealthDataUnreadable`) gehören zu einem **anderen** Vertrag — sie werden als `reasonCode` in einem 200-Body geliefert (`WorkerHealthEndpoints.cs:19,25`), nicht als `errorCode` in einem Fehler-Body. Beide teilen sich aber eine Konstantenklasse, deren XML-Doc nur den ersten Vertrag beschreibt.
> **Warum es zählt**: Wer einen Code hinzufügt, hat vier Stellen zu treffen und **null** Signale, wenn er eine vergisst. Der Ausfallmodus ist sanft (`api-error.ts:33` fällt auf `errors.generic` zurück), aber unsichtbar — er äußert sich als generische Fehlermeldung im Produktivbetrieb statt als roter Test. Die stille Zwei-Verträge-in-einer-Klasse-Situation ist genau die Falle, die beim vierundzwanzigsten Code zuschlägt.
> **Fix**: (1) Ein Vitest in `web/src/app/core/i18n/api-error.spec.ts`, der `KNOWN_API_ERROR_CODES` gegen die `errors.api.*`-Keys **beider** JSON-Dateien in beide Richtungen abgleicht — läuft im bestehenden `test-web`-Job. (2) Die zwei Health-Konstanten in eine eigene `WorkerHealthReasonCodes`-Klasse verschieben und im XML-Doc von `ApiErrorCodes` festhalten, dass jede Konstante dort einen Eintrag in `api-error.ts` **und** beiden Locale-Dateien braucht. Die C#↔TS-Hälfte bleibt bewusst manuell — ein Generator wäre hier mehr Maschinerie als die Sache wert.

> **S3-30 — `effect()` als Datenlader: fünf Stellen, eine davon feuert jeden Request doppelt**
> **S3** · Frontend · **S** (Doppel-Request) / **M** (rxResource-Pilot) · Gefunden von Track 4 (A-11)
> **Ort**: `web/src/app/features/voting/vote-session-list-page.ts:57` und `:77-80`; weitere Loader-Effects in `channel-workspace-layout.ts:95,96`, `usage-stats-page.ts:86`, `vote-session-detail-page.ts:107`
> **Was**: `effect(() => this.load())`, und `load()` liest im reaktiven Kontext sowohl `channelName()` als auch `page()`. `onPageChange` macht beides: `this.page.set(newPage)` markiert den Effect als dirty, und `this.load()` läuft direkt — pro Seitenwechsel zwei `GET .../vote-sessions` und zwei `GET /api/channels/{c}`. `my-votings-page.ts:74-77` hat identischen `onPageChange`-Code, ruft `load()` aber im Konstruktor und verhält sich deshalb korrekt: zwei Seiten, gleicher Code, unterschiedliches Verhalten.
> **Warum es zählt**: Der Effect-Loader ist im Log als *Bugfix* dokumentiert (NG0950 beim Lesen eines Route-Inputs im Konstruktor). Die Lösung war richtig, hat aber eine Falle eingebaut: Sobald `load()` einen weiteren Signal-Zustand liest, wird dieser stillschweigend zur Reload-Trigger-Quelle. Eine künftige Seite mit Filtern **und** Pagination tritt genau da hinein.
> **Fix**: Akut das `this.load()` in `vote-session-list-page.ts:79` streichen — der Effect erledigt es bereits. Danach dieselbe Seite als Pilot auf `rxResource({ params: () => ({ channel: this.channelName(), page: this.page() }), stream: ({params}) => this.voteSessionService.list(params.channel, params.page) })` umstellen; `sessions`/`totalPages` werden `computed()` auf `resource.value()`, `errorMessage` auf `resource.error()`. Erst wenn das eine Seite überzeugend trägt, die anderen nachziehen.

> **S3-31 — Validierung doppelt in Endpoint und Service, mit divergenten Ausfallmodi und lückenhafter Anwendung**
> **S3** · Backend · **S** · Gefunden von Track 4 (A-12)
> **Ort**: `src/EmotePurge.Api/Endpoints/VoteSessionEndpoints.cs:21-44` vs. `src/EmotePurge.Infrastructure/Services/VoteSessionService.cs:12-30`; `ChannelNameValidation.IsValid` in 9 von 15 channel-scoped Endpoints
> **Was**: Vier Regeln (Titel nicht leer, Rollen nicht leer, keine VIPs, `StartedAt` nicht in der Zukunft) stehen zweimal — im Endpoint als `400 { errorCode }`, im Service als `ArgumentException`, wobei die Service-Variante heute unerreichbar ist. `IsValid` fehlt bei `POST /{sessionId}/end`, `DELETE /{sessionId}`, `GET /{sessionId}/results`, `POST /{sessionId}/votes` und `DELETE /{sessionId}/votes/{emoteId}`.
> **Warum es zählt**: Die Doppelung ist als Defense in Depth verteidigbar, aber die Ausfallmodi divergieren: Wer eine fünfte Regel nur im Service ergänzt, produziert eine 500 statt einer 400; wer sie nur im Endpoint ergänzt, hat einen Service, der bei direktem Aufruf (Test, Worker, Folgemodul) durchlässig ist. Die `IsValid`-Lücken haben heute keine Auswirkung, machen aber aus einer Konvention eine Gewohnheit — man kann am Code nicht ablesen, ob die Prüfung fehlt oder bewusst weggelassen wurde.
> **Fix**: Eine Entscheidung treffen und aufschreiben, statt beide Seiten auszubauen. Vorschlag: Der **Service** ist die Wahrheit (er ist der getestete Layer), der Endpoint übersetzt — `CreateAsync` gibt statt `ArgumentException` ein `CreateVoteSessionResult`-Enum zurück (das Muster existiert bereits als `VoteCastResult`), der Endpoint mappt auf `ApiErrorCodes`; die 24 Zeilen Vorab-Validierung entfallen. Für `IsValid`: Gruppen-Filter auf `:13` — ein Route-Constraint `{channelName:regex(...)}` wäre die andere Option, liefert aber 404 statt 400 (siehe Zielkonflikt Z4).

> **S3-32 — 7TV-Editor-Auflösung an zwei Stellen unabhängig implementiert und ungecacht**
> **S3** · Architektur · **S** · Gefunden von Track 4 (A-13)
> **Ort**: `src/EmotePurge.Infrastructure/Services/ChannelAccessService.cs:38-45` und `MyChannelsService.cs:54-73`
> **Was**: Beide führen die Kette `ResolveSevenTvIdentityAsync` → `GetEditorOfChannelsAsync` → Vergleich von `grant.TwitchChannelLogin` gegen einen normalisierten Channel-Namen aus — einmal mit `string.Equals(..., OrdinalIgnoreCase)`, einmal mit `.Trim().ToLowerInvariant()` als Dictionary-Key. Beide sind ungetestet.
> **Warum es zählt**: „Ist X 7TV-Editor von Y?" ist eine Autorisierungsfrage — sie entscheidet über `/usage-stats` und alle `/emotes`-Endpoints. Zwei Implementierungen mit zwei Vergleichsstrategien heißt: Eine Änderung an 7TVs Grant-Semantik muss zweimal richtig nachgezogen werden, und ein Test für die eine sagt nichts über die andere. Zusätzlich ruft `MyChannelsService` die Kette bei jedem Overview-Aufruf ungecacht (siehe S2-11).
> **Fix**: `ISevenTvEditorService` mit `Task<IReadOnlySet<string>> GetEditableChannelLoginsAsync(string twitchUserId, CancellationToken)` — normalisiert die Logins genau einmal. `CanViewUsageStatsAsync` wird zu `(await editorService.GetEditableChannelLoginsAsync(...)).Contains(normalizedChannel)`, `MyChannelsService` iteriert dasselbe Set. Ein Redis-Cache lässt sich dann an genau einer Stelle nachrüsten.

> **S3-33 — `web/tsconfig.json` ohne `strict` und ohne `strictTemplates`; das Aktivieren kostet nachweislich nichts**
> **S3** · Frontend · **S** (`strict`), unbekannt (`strictTemplates`, erst messen) · Gefunden von Track 4 (A-14)
> **Ort**: `web/tsconfig.json:5-16` (`compilerOptions`), `:18-22` (`angularCompilerOptions`)
> **Was**: Es fehlen `"strict": true`, `"strictTemplates": true` und `"typeCheckHostBindings": true` — alle drei setzt ein frisch generiertes Angular-22-Projekt per Default. `git log --follow` zeigt genau einen Commit (das Initial-Scaffold): Die Datei wurde nie geschwächt, sie kam so. In `CLAUDE.md` gibt es 0 Treffer für „strict" — es ist eine Lücke, keine Entscheidung. Track 4 hat die Kosten **gemessen**: `npx tsc -p tsconfig.app.json --noEmit --strict` über alle 53 Nicht-Spec-Dateien → **0 Fehler**. Die neun `$any()`-Casts sind ausnahmslos die kanonische Form `$any($event.target).value` und funktionieren unter `strictTemplates` unverändert.
> **Warum es zählt**: `strict` ist heute gratis und wird mit jeder weiteren Datei teurer. Der Wert liegt nicht in den heutigen 0 Fehlern, sondern darin, dass die nächste Seite ihn nicht mehr unbemerkt verletzen kann — `strictNullChecks` ist der Unterschied zwischen „Guard hat `null` zurückgegeben" als Compile-Fehler und als Laufzeit-Weißseite.
> **Fix**: `"strict": true` sofort setzen (verifiziert kostenlos). `"strictTemplates"` und `"typeCheckHostBindings"` in einem eigenen Commit danach: einmal `ng build`, Fehler zählen, dann entscheiden — die Erwartung „gering" ist eine Erwartung, keine Messung.

> **S3-34 — Migrationen rein manuell, kein Schutz gegen Schema-Drift**
> **S3** · Betrieb · **S** · Gefunden von Track 5 (I5)
> **Ort**: `src/EmotePurge.Infrastructure/DependencyInjection.cs:21-22` (nur `AddDbContext`/`UseNpgsql`); kein `Migrate`/`EnsureCreated` im gesamten `src/` (verifiziert)
> **Warum es zählt**: Portainer zieht das neue `:latest`; wird die Migration vergessen, startet der Container **erfolgreich** (Postgres ist erreichbar), aber jede Query gegen eine neue Spalte wirft `PostgresException 42703`. Der Prozess crasht nicht, er liefert nur für betroffene Endpoints 500er — ohne Healthcheck und Monitoring bleibt das leicht wochenlang unbemerkt.
> **Abwägung**: Auto-Migration beim Start wäre für eine Einzelinstanz technisch unproblematisch, hat aber eine Nebenwirkung: `api` und `worker` starten unabhängig gegen dieselbe DB (`depends_on` wartet nur auf `postgres`/`redis`, nicht aufeinander) — würde nur die Api migrieren, liefe der Worker kurzzeitig gegen ein nicht migriertes Schema.
> **Fix**: **Kein** Auto-Migrate, sondern ein Fail-Fast-Guard, der „stille 500er" in „sichtbarer Crash-Loop in Portainer" verwandelt:
> ```csharp
> using (var scope = app.Services.CreateScope())
> {
>     var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
>     var pending = db.Database.GetPendingMigrations().ToList();
>     if (pending.Count > 0)
>         throw new InvalidOperationException($"Nicht angewendete Migrationen: {string.Join(", ", pending)}.");
> }
> ```

> **S3-35 — Keine Healthchecks für `api`/`worker`**
> **S3** · Betrieb · **S–M** · Gefunden von Track 5 (I6), Track 2a (B16)
> **Ort**: beide Compose-Dateien (nur `redis`/`postgres` haben `healthcheck:`) · `src/EmotePurge.Worker/Dockerfile` (kein `HEALTHCHECK`) · `WorkerHealthEndpoints.cs:11-39`
> **Was**: `/api/worker/health` liefert **immer HTTP 200** — auch bei `status: "unknown"` — und ist in keinem Compose-Healthcheck referenziert. Der Worker hat gar keinen.
> **Warum es zählt**: `restart: unless-stopped` greift nur bei Prozess-Exit. Hängt die Api (Deadlock, Thread-Pool-Erschöpfung) oder sitzt der Worker im absorbierenden Totzustand aus S2-2, bleibt der Container „healthy", weil kein Healthcheck existiert. Das ist der Grund, warum S2-2 nicht extern aufgefangen wird, und der Kern des „zwei Wochen nicht hingeschaut"-Szenarios.
> **Fix**: `curl` in die `base`-Stage (Nachrüsten von Systempaketen ist im Worker-Dockerfile bereits gelebte Praxis), dann für die Api:
> ```yaml
>   api:
>     healthcheck:
>       test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
>       interval: 30s
>       timeout: 5s
>       retries: 3
>       start_period: 15s
> ```
> Der Endpoint muss bei `unknown`/`disconnected` einen Nicht-200-Status liefern, damit `curl -f` das erkennt — siehe Zielkonflikt Z1, dort ist auch geklärt, welcher Pfad dafür der richtige ist. Für den Worker (kein HTTP-Port) ein Datei-Heartbeat: `TwitchChatManager` berührt bei IRC-Aktivität `/tmp/alive`, dazu
> ```dockerfile
> HEALTHCHECK --interval=60s --timeout=5s --start-period=120s --retries=3 \
>   CMD test $(( $(date +%s) - $(stat -c %Y /tmp/alive) )) -lt 600
> ```
> Damit wird der absorbierende Totzustand nach 10 Minuten hart durch einen Container-Restart aufgelöst — das billigste verfügbare Sicherheitsnetz gegen alle „nicht selbstständig zurückfinden"-Zustände, auch die hier nicht gefundenen.

> **S3-36 — Keine strukturierten Logs, keine Metriken, kein Alerting**
> **S3** · Betrieb · **S** · Gefunden von Track 5 (I7)
> **Ort**: `src/EmotePurge.Api/appsettings.json` (nur `Logging.LogLevel`, Konsolen-Standardprovider); keine `Serilog`-Referenz im Repo
> **Warum es zählt**: Die einzige Möglichkeit, einen seit gestern Nacht kaputten Dienst zu bemerken, ist manuelles Nachschauen. Bei einem Ein-Personen-Projekt mit unregelmäßigen Zeitfenstern ist das der Kernrisikofall — und er potenziert jeden Worker-Befund aus dem S2-Cluster.
> **Fix** (bewusst kein Log-Stack, sondern ein Dead-Man's-Switch, an einem Abend machbar): Externer Uptime-Check (UptimeRobot Free, 5-min-Intervall) gegen den öffentlichen Health-Pfad. Zusätzlich ein Dead-Man's-Switch für den Worker selbst, der „Prozess lebt, tut aber nichts" abfängt, was ein reiner Api-Check nicht sieht: `WorkerHealthPublisher` pingt bei jedem erfolgreichen Zyklus `healthchecks.io` (Free); bleibt der Ping aus, kommt automatisch eine Benachrichtigung. Beide ohne Pflegeaufwand.

> **S3-37 — CI testet erst nach dem Merge auf `main`**
> **S3** · CI · **S** · Gefunden von Track 5 (I8)
> **Ort**: `.github/workflows/publish.yml:3-6` (`on: push: branches: [main]`, kein `pull_request`)
> **Was**: Positiv bereits vorhanden: `needs: [test, test-web]` vor `publish` (Z. 72) — ein fehlschlagender Test verhindert, dass ein kaputtes Image nach GHCR geht, `:latest` bleibt beim letzten guten Stand. Aber `main` bleibt bis zum Fix-Commit rot, ohne dass das vor dem Merge sichtbar war.
> **Fix**:
> ```yaml
> on:
>   push: { branches: [main] }
>   pull_request: { branches: [main] }
>   workflow_dispatch:
>   publish:
>     if: github.event_name == 'push'
>     needs: [test, test-web]
> ```

> **S3-38 — Identischer `name:`/`container_name:`/Volume-Name in beiden Compose-Dateien**
> **S3** · Betrieb · **S** · Gefunden von Track 5 (I9)
> **Ort**: `docker-compose.yml:1,9,25,44,62` · `docker-compose.prod.yml:1,9,20,39,54`
> **Was**: Empirisch per `docker compose -f <datei> config` geprüft: Beide lösen trotz verschobener Host-Ports auf **denselben** Projektnamen, **dieselbe** Netzwerk-Bezeichnung `emote-purge_emotepurge-network` und **denselben** Volume-Namen `emote-purge_postgres-data` auf.
> **Warum es zählt**: Die dokumentierte Port-Verschiebung erfüllt ihren Zweck (Konfliktvermeidung mit der fremden App). Ungelöst bleibt: Laufen beide EmotePurge-Varianten je einmal auf demselben Host — etwa `docker-compose.prod.yml` zur Fehlersuche per SSH gestartet, während lokale Reste existieren —, verweigert Docker wegen identischem `container_name` entweder den Start, oder, schlimmer, falls die Container gerade nicht laufen, hängt sich ein neuer Start unbemerkt an **dasselbe** `postgres-data`-Volume: Ein lokaler Test-Build liefe direkt gegen echte Produktionsdaten.
> **Fix**: In `docker-compose.prod.yml` `name: emote-purge-prod` und `container_name: emotepurge-prod-*`. Service-zu-Service-Kommunikation bleibt unverändert, sie läuft über Servicenamen.

---

### S4 — Niedrig

> **S4-1 — Filterkommentar und Entscheidungslog beschreiben die `sync-deleted`-Absicherung falsch**
> **S4** · Doku/Security · **S** · Gefunden von Track 1 (B9)
> **Ort**: `src/EmotePurge.Api/Auth/UsageStatsAccessAuthorizationFilter.cs:6-8` · `src/EmotePurge.Api/Endpoints/EmoteEndpoints.cs:14-16` · `CLAUDE.md:163`
> **Was**: Der Klassenkommentar behauptet, der Filter hänge „only for the two usage-stats read endpoints" und alles andere — ausdrücklich inklusive `sync-deleted` — bleibe hinter dem strengeren Filter. Tatsächlich hängt er als Gruppenfilter an **fünf** Endpoints, darunter der Schreib-Endpoint `POST .../emotes/sync-deleted`. `CLAUDE.md:163` sagt dasselbe Veraltete. Beides ist seit Commit `640d77b` überholt; die Umstellung war bewusst, nur Doku und Log wurden nicht nachgezogen. Die Sachlage selbst ist vertretbar: Ein 7TV-Editor kann Emotes ohnehin per 7TV löschen, `MarkDeletedAsync` setzt nur `IsArchived = true`, und der Resync setzt das Flag im Minutentakt zurück — ein Missbrauch heilt sich selbst.
> **Warum es zählt**: Der Kommentar sitzt genau an der Stelle, an der ein Entwickler die Blast-Radius-Frage beantwortet. Wer der `EmoteEndpoints`-Gruppe später einen echten Hard-Delete oder ein „Emote umbenennen" hinzufügt, liest ihn, schließt „nur Lesezugriffe" und lässt den Gruppenfilter unverändert — der neue Schreib-Endpoint wäre dann für 7TV-Editoren offen, ohne dass das je jemand entschieden hätte. Derselbe Mechanismus hat den Kommentar bereits einmal falsch werden lassen.
> **Fix**: Den Kommentar auf den Ist-Zustand korrigieren (fünf Endpoints, `sync-deleted` als bewusst eingeschlossener Schreibpfad samt Begründung, plus der Satz: alles mit echter Management-Semantik gehört hinter den strengeren Filter). `CLAUDE.md:163` um einen Nachtrag-Satz ergänzen.

> **S4-2 — `GET /api/worker/health` ist der einzige Endpoint ohne jede Auth und ohne Rate Limit**
> **S4** · Security · **S** · Gefunden von Track 1 (B11) — *siehe Zielkonflikt Z1*
> **Ort**: `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs:11-39`
> **Was**: Kein `RequireAuthorization()`, kein Filter, kein Rate Limit. Der Antwortkörper wurde geprüft: `status`, `isConnected`, `lastMessageReceivedUtc`, `secondsSinceLastMessage` — **keine** Channelnamen, keine Nutzerdaten, keine Connection-Strings; der Redis-Wert wird in ein festes anonymes Objekt projiziert, es gibt keinen Pfad, auf dem Redis-Inhalt roh durchgereicht würde.
> **Warum es zählt**: Der Informationsgehalt ist gering, aber nicht null — ein anonymer Beobachter kann per Polling ableiten, wann die Erfassung ausfällt, also genau das Zeitfenster, in dem Emote-Nutzung unbemerkt nicht gezählt wird. Praktisch relevanter: Jeder Aufruf erzeugt einen Redis-Roundtrip ohne Auth und ohne Limit — bei öffentlich erreichbarer Domain der einzige kostenlose Hebel gegen die Redis-Instanz, die gleichzeitig das Join/Leave-Kommandoprotokoll trägt. Solange die Seite nicht öffentlich ist, kein akutes Problem; vor dem Launch der billigste zu schließende Punkt.
> **Fix**: Siehe Zielkonflikt Z1 — Aufteilung in einen minimalen öffentlichen `/api/health` (Status ohne Payload, korrekter Statuscode) und den detaillierten `/api/worker/health` hinter `.RequireAuthorization()`. Der einzige Consumer des detaillierten Endpoints ist `WorkerHealthService` in der `AppShell`, die ausschließlich unter geguardeten Routen rendert — es bricht kein anonymer Consumer weg.

> **S4-3 — CSP ohne `form-action`; `style-src 'unsafe-inline'` als letzter Aufweichungspunkt**
> **S4** · Security · **S** · Gefunden von Track 1 (B12)
> **Ort**: `src/EmotePurge.Api/Program.cs:89-98`
> **Was**: Die CSP ist überdurchschnittlich streng — `script-src 'self'` ohne `unsafe-inline`/`unsafe-eval`, `object-src 'none'`, `base-uri 'self'`, `frame-ancestors 'none'`, `connect-src 'self' https://7tv.io`, `img-src` auf 7TV-CDN plus `data:`. Zwei Lücken: `form-action` fehlt (fällt anders als `frame-src`/`connect-src` **nicht** auf `default-src` zurück), und `style-src 'unsafe-inline'` ist für Angular-Komponentenstyles üblich, aber über `ngCspNonce`/`CSP_NONCE` vermeidbar.
> **Warum es zählt**: Für den 7TV-Schreib-Token ist die CSP die tragende Verteidigungslinie — er liegt in `sessionStorage` und ist für jedes same-origin-JS lesbar. `script-src 'self'` macht Injection sehr schwer, `connect-src` verhindert die fetch-basierte Exfiltration selbst bei gelungener Skriptausführung. `form-action` ist die verbliebene Lücke dieser Kette: Ein injiziertes Formular, das ohne Skriptausführung auskommt, kann Daten an einen fremden Origin senden und den Nutzer dorthin navigieren — ein Phishing-Sprung von der echten Domain aus, unter Umgehung sämtlicher `connect-src`-Beschränkungen.
> **Fix**: `"form-action 'self'; "` in `Program.cs:97` ergänzen — es gibt in der gesamten App kein Formular, das an einen anderen Origin postet, der Zusatz ist funktional folgenlos. Optional mittelfristig `style-src 'self' 'nonce-…'` mit Angulars `ngCspNonce`; mit `MapFallbackToFile("index.html")` nicht ohne Weiteres möglich, deshalb bewusst kein Blocker.

> **S4-4 — `DrainAndReset` vs. `Increment`: schmales Verlustfenster**
> **S4** · Worker · **S** · Gefunden von Track 2a (B13)
> **Ort**: `src/EmotePurge.Worker/EmoteUsageCounter.cs:9-13`
> **Was**: `Increment` liest das Feld `_counts` und ruft darauf `AddOrUpdate`; `DrainAndReset` tauscht das Feld per `Interlocked.Exchange`. Zwischen Feldlesung und `AddOrUpdate` kann der Tausch stattfinden.
> **Warum es zählt (und warum fast nie)**: Landet der Increment auf einem bereits vorhandenen Key, wird er sogar noch mitgezählt; verloren geht er nur, wenn er einen neuen Key nach `emoteIds = usageCounts.Keys.ToList()` anlegt. Entscheidend: Es gibt genau **einen** Producer — `OnMessageReceived` wird von TwitchLibs einzigem Listen-Task sequenziell awaited, es gibt eine Socket-Verbindung für alle Channels. Das Fenster ist wenige Nanosekunden alle 30 Sekunden, der erwartete Verlust praktisch null, auch bei HandOfBlood. **Nicht fixwürdig** — aber wissenswert, falls je auf mehrere `TwitchClient`-Instanzen (Sharding) umgestellt wird, dann wird es real.
> **Fix** (nur bei Sharding): `Increment`/`DrainAndReset` über ein `ReaderWriterLockSlim` koppeln oder auf zwei alternierende Puffer mit `Interlocked`-Generation umstellen.

> **S4-5 — `_connected` ist nicht-volatil, wird nie zurückgesetzt und ist redundant**
> **S4** · Worker · **S** · Gefunden von Track 2a (B14)
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:35, 60, 159, 179` und `:119-120`
> **Was**: `private bool _connected;` ohne `volatile`, geschrieben auf dem `ExecuteAsync`-Thread, gelesen auf Threadpool-Threads aus dem Redis-Handler. Zusätzlich sind `_consecutiveConnectionErrors = 0` und `_reconnectCountSinceCreation = 0` (`:119-120`) Plain-Stores, während dieselben Felder anderswo per `Interlocked` angefasst werden.
> **Warum es zählt**: Das Sichtbarkeitsproblem ist theoretisch (zwischen Schreiben und erstem möglichen Lesen liegen `await` und Threadpool-Queue, beides mit Barrieren). Das eigentliche Problem ist die **Semantik**: `_connected` wird nie auf `false` zurückgesetzt und bedeutet damit „ein Connect wurde einmal versucht", nicht „ist verbunden" — genau das macht den Guard in S2-3 wirkungslos. Mit `_isConnected` existiert bereits ein korrekt gepflegtes, `volatile` deklariertes Feld.
> **Fix**: `_connected` ersatzlos streichen und in `:159`/`:179` gegen `_isConnected` tauschen (bzw. den Guard nach S2-3 ganz entfernen, weil `_joinedChannels` dann die Absicht trägt). `:119-120` auf `Interlocked.Exchange` umstellen.

> **S4-6 — Der alte `WebSocketClient` wird beim Recreate nicht disposed, wenn `DisconnectAsync` wirft**
> **S4** · Worker · **S** (im Rahmen von S2-1) · Gefunden von Track 2a (B15)
> **Ort**: `src/EmotePurge.Worker/TwitchChatManager.cs:107-114`
> **Was**: `await oldClient.DisconnectAsync();` in einem `try/catch`, das den Fehler bewusst schluckt. `TwitchClient` kapselt den `IClient` privat und exponiert kein `Dispose`. Wirft `DisconnectAsync` — etwa weil der Client bereits tot ist, und genau dann wird recreatet — werden `CloseAsync`/`ClosePrivateAsync` nie erreicht und der interne `ClientWebSocket` samt `CancellationTokenSource` nie freigegeben.
> **Warum es zählt (begrenzt)**: Pro Recreate ein kleiner Leak; die Monitor-Task des alten Clients terminiert selbst, Zombie-Verbindungen entstehen nicht. Bei der Diagnose zu Ausfall 2 wurden ~220 FDs gemessen — Größenordnungen von einem Problem entfernt, aber bei häufigen Recreates auf stillen Channels (S3-11) summiert es sich über Wochen.
> **Fix**: Fällt mit S2-1 praktisch von selbst weg — dort wird der `WebSocketClient` ohnehin selbst erzeugt; die Referenz neben `_client` behalten und nach dem best-effort-Disconnect `oldWebSocket.Dispose()` aufrufen, zusätzlich vorher `if (oldClient.IsConnected)` prüfen.

> **S4-7 — Kein sauberer IRC-Disconnect beim Shutdown**
> **S4** · Worker · **S** · Gefunden von Track 2a (B16)
> **Ort**: `src/EmotePurge.Worker/Worker.cs:58-60`
> **Was**: `Worker` überschreibt `StopAsync` nicht; beim Shutdown wirft `Task.Delay(Timeout.Infinite, stoppingToken)` und der Prozess endet, ohne dass der `TwitchClient` je `DisconnectAsync` sieht — Twitch bekommt einen abrupten Socket-Abbruch statt eines `QUIT`.
> **Warum es zählt**: Kosmetisch, könnte aber zur Abuse-Heuristik-Theorie aus `CLAUDE.md:175` beitragen, wenn bei Deploys mehrfach hintereinander hart abgerissen wird.
> **Fix**: `Worker.StopAsync` überschreiben → `twitchChatManager.DisconnectAsync()` (neue Interface-Methode) vor `base.StopAsync`.

> **S4-8 — `DecimalPipe` formatiert Scores immer nach en-US**
> **S4** · Frontend · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/features/voting/vote-session-detail-page.html:93` (`{{ emote.score | number: '1.0-1' }}`)
> **Was**: `DecimalPipe` liest `LOCALE_ID`, das nirgends gesetzt wird und damit auf `'en-US'` steht. `locale.ts:3-5` dokumentiert bewusst, dass Datumsformate deshalb über `Intl` laufen — die einzige Zahlenformatierung der App wurde dabei übersehen.
> **Warum es zählt**: Deutsche Nutzer sehen „12.5" statt „12,5"; bei einem einstelligen Score mit einer Nachkommastelle liest sich das kurz wie „125".
> **Fix**: `formatScore(value)` analog zu `formatDateTime` mit `Intl.NumberFormat(toLocale(this.languageService.lang()), {maximumFractionDigits:1})` — reagiert dann auch korrekt auf den Laufzeit-Sprachwechsel.

> **S4-9 — Untranslierter Placeholder im Admin-Formular**
> **S4** · Frontend · **S** · Gefunden von Track 3
> **Ort**: `web/src/app/features/overview/overview-page.html:80`
> **Was**: `placeholder="twitch-channel-name"` — der einzige hartkodierte, nutzersichtbare String im gesamten Frontend (Prüfung über alle `.html`/`.ts`: sonst nur der Markenname „Emote Purge").
> **Warum es zählt**: Nur Admins betroffen, rein kosmetisch — aber es bricht eine sonst lückenlos durchgezogene Konvention.
> **Fix**: Key `overview.admin.channelNamePlaceholder` in beide JSONs, `[placeholder]="… | transloco"`.

> **S4-10 — `ROW_HEIGHT_PX` muss von Hand zur Tailwind-Höhenklasse passen**
> **S4** · Frontend · **S** · Gefunden von Track 3
> **Ort**: `usage-stats-page.ts:22` (112) gegen `usage-stats-page.html:98/103` (`py-2` + `h-24`); `vote-session-detail-page.ts:23` (176) gegen `:81/86` (`py-2` + `h-40`)
> **Was**: Beide Werte sind **aktuell korrekt** (96+16 bzw. 160+16), aber nichts hält sie gekoppelt — die Kommentare warnen nur.
> **Warum es zählt**: Kein heutiger Nutzerschaden. Ändert jemand später `h-40` auf `h-44`, driftet die Virtualisierung: Karten überlappen oder Lücken entstehen, und zwar erst weit unten im Scrollbereich — genau dort, wo es beim Testen mit wenigen Emotes niemandem auffällt, bei 1.000 aber sofort.
> **Fix**: Höhe aus der Konstante ableiten (`[style.height.px]="rowHeight - 16"`), oder mindestens einen Vitest-Guard, der beide Konstanten gegen die im Template genutzten Klassen prüft.

> **S4-11 — `WorkerHealthEndpoints` greift direkt auf Redis zu und parst das Worker-Payload nach**
> **S4** · Architektur · **S** · Gefunden von Track 4 (A-15)
> **Ort**: `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs:11-22,43` gegenüber `src/EmotePurge.Worker/WorkerHealthPublisher.cs:134,151-157`
> **Was**: Der Endpoint zieht `IConnectionMultiplexer` direkt in den Handler, liest den Literal-Key `"worker:health:twitch"` und deserialisiert in ein lokal definiertes Record. Der Worker definiert dasselbe Format als anonymes Objekt und den Key als `public const string RedisKey` — die Konstante wird von der Api nicht genutzt (und könnte es nicht, die Projekte referenzieren sich nicht). Es ist der einzige Fall, in dem die Api an ihrem eigenen Service-Layer vorbei auf Infrastruktur zugreift (siehe Anhang E).
> **Warum es zählt**: Die Wirkung heute ist klein (zwei Felder). Aber der Health-Payload ist genau die Stelle, an die als Nächstes etwas angehängt wird — dann sind es drei Definitionen desselben Formats an drei Orten (Worker, Api, `web/src/app/core/health/worker-health.model.ts`). Die Entscheidung „Api und Worker kommunizieren nur über Redis" bleibt richtig; sie sagt nur nichts darüber, wo der *Vertrag* wohnt.
> **Fix**: `IWorkerHealthReader` in `Core/Services/` plus Implementierung in `Infrastructure/Redis/`, die Key und Record besitzt; das Record wandert nach `Core` zu den anderen geteilten DTOs. Worker und Api serialisieren denselben Typ, der Endpoint schrumpft auf drei Zeilen, die Schichtentabelle wird verstoßfrei, und der Reader ist mit der bestehenden `RedisFixture` testbar.

> **S4-12 — Verwaiste Reste aus entfernten Features und stale Datei-Verweise**
> **S4** · Aufräumen · **S** · Gefunden von Track 4 (A-16), Track 5 (I14)
> **Ort**: `src/EmotePurge.Infrastructure/SevenTv/SevenTvEmoteJsonMapper.cs:17-23` · `web/src/app/features/voting/vote-session-detail-page.ts:184` · `src/EmotePurge.Core/Services/IVoteSessionQueryService.cs:23,25` · `CLAUDE.md:166` · `web/src/app/core/shared/` · `web/.claude/CLAUDE.md` („EmotePurge-specific conventions", vorletzter Punkt)
> **Was**: `SevenTvEmoteJsonMapper.MapFromJsonElement` hat keinen Aufrufer im gesamten Repo; der Kommentar darüber verweist auf den am 2026-07-25 entfernten EventAPI-Dispatch-Parser. Drei Kommentare nennen `Program.cs` als Fundort von Endpoint-Logik, die seit dem 2026-07-28 in `Api/Endpoints/*.cs` liegt — dieselbe Aussage steht auch in `web/.claude/CLAUDE.md`. `CLAUDE.md:166` verweist auf `channel-management.guard.ts`, die seit dem 2026-07-27 `usage-stats-access.guard.ts` heißt. `web/src/app/core/shared/` existiert neben dem Top-Level-`web/src/app/shared/` — zwei Ordner gleichen Namens mit unterschiedlicher Bedeutung.
> **Warum es zählt**: Einzeln trivial, zusammen bilden sie das Muster, das die Orientierung am meisten kostet: Die Kommentare sind ausführlich und überzeugend geschrieben, und mehrere zeigen auf Dinge, die es nicht mehr gibt. Wer ihnen folgt, verliert Zeit und — schlimmer — Vertrauen in alle anderen Kommentare, von denen die überwiegende Mehrheit exzellent ist.
> **Fix**: `MapFromJsonElement` samt Kommentar löschen (Git hält die Historie). Die `Program.cs`-Verweise auf `VoteSessionEndpoints.cs` bzw. `Endpoints/*.cs` umschreiben, auch in `web/.claude/CLAUDE.md`. `CLAUDE.md:166` um einen Klammerzusatz *(heute `usage-stats-access.guard.ts`)* ergänzen — den Originaltext **nicht** umschreiben, das Log ist ein zeitliches Protokoll. `core/shared/` nach `core/http/` oder `core/models/` umbenennen.

> **S4-13 — `Worker` fehlt in der dokumentierten `BackgroundService`-Ausnahmeliste**
> **S4** · Doku · **S** · Gefunden von Track 4 (A-17)
> **Ort**: `CLAUDE.md` (Eintrag „Service-Layer + Interfaces für DI/Testbarkeit") gegen `src/EmotePurge.Worker/Program.cs:174-178`
> **Was**: Dokumentiert sind vier Ausnahmen; registriert sind fünf Hosted Services — `Worker` selbst ist ebenfalls interfacelos und nur per `AddHostedService<T>()` verdrahtet, steht aber nicht in der Liste.
> **Warum es zählt**: Für sich genommen egal — aber die Liste ist enumerativ formuliert, und eine enumerative Regel, der ein Element fehlt, lädt genau zu der Frage ein, die sie beantworten sollte.
> **Fix**: `Worker` aufnehmen, oder robuster: die Regel von der Aufzählung auf das Kriterium umstellen — *„jeder `BackgroundService`, der ausschließlich per `AddHostedService<T>()` läuft und nirgends injiziert wird"*.

> **S4-14 — Die Sprachmischung ist projektweise systematisch, aber nirgends als Regel festgehalten**
> **S4** · Doku · **S** · Gefunden von Track 4 (A-18)
> **Ort**: Verteilung über alle `src/`-Dateien; Ausreißer `src/EmotePurge.Api/Program.cs` und `Endpoints/WorkerHealthEndpoints.cs`
> **Was**: Die Verteilung folgt einer Projektgrenze, nicht dem Zufall: `EmotePurge.Worker` überwiegend deutsche Kommentare (`TwitchChatManager.cs` 18 von 32), `Core`/`Infrastructure`/`Api` praktisch vollständig englisch (0 deutsche Kommentare in `UsageStatQueryService.cs`, `EmoteEndpoints.cs`, `AppDbContext.cs`, `VoteSessionEndpoints.cs`). Log-Meldungen: durchgängig deutsch. Bezeichner: durchgängig englisch. Frontend: Kommentare englisch, UI-Text über Transloco. Doku deutsch, Commits gemischt.
> **Warum es zählt**: Das ist konsistent **genug**, um wartbar zu bleiben — deutlich besser, als „gemischt deutsch/englisch" nahelegt. Was fehlt, ist der Satz, der es festhält: Heute muss jemand, der eine neue Datei anlegt, die Nachbardateien lesen, um die Sprache zu erraten — und in `Program.cs` fände er die falsche Antwort.
> **Fix**: Vier Zeilen in `CLAUDE.md` unter „Sprache", ohne Bestandscode umzuschreiben: Bezeichner/Typen/öffentliche APIs englisch · Kommentare in **neuem** Code englisch (Bestand im Worker bleibt deutsch, nur nicht fortführen) · Log- und `throw`-Messages deutsch · Projektdokumentation deutsch, Commit-Messages englisch.

> **S4-15 — GitHub Actions auf Floating-Tags statt SHA gepinnt**
> **S4** · CI · **S** · Gefunden von Track 5 (I10)
> **Ort**: `.github/workflows/publish.yml:18,21,36,39,65,86,89,96`
> **Warum es zählt**: Ein kompromittiertes Upstream-Tag könnte unbemerkt in den Build gelangen, der `secrets.GITHUB_TOKEN` mit `packages: write` nutzt. Bei einem unregelmäßig aktiven Betreiber ist das Zeitfenster lang.
> **Fix**: SHA-Pinning mit Versionskommentar, z. B. `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`. Mit Dependabot (`github-actions`) aktualisieren sich die Pins danach automatisch per PR.

> **S4-16 — Fehlende CI-Hygiene: kein Lint/Format/Scan/Audit, kein NuGet-Cache, kein Dependabot**
> **S4** · CI · **S** · Gefunden von Track 5 (I11)
> **Ort**: `.github/workflows/publish.yml` · `.github/` (kein `dependabot.yml`, keine Renovate-Config — verifiziert) · `web/package.json` (kein `lint`/`format`-Script, `eslint` nicht installiert, `prettier` nur ungenutzte DevDependency)
> **Fix**: `dependabot.yml` mit drei Ecosystems (`nuget`, `npm`, `github-actions`), wöchentlich; `cache: true` bei `actions/setup-dotnet` (der Node-Job hat bereits `cache: "npm"`); `dotnet format --verify-no-changes` und `npx prettier --check .` als Steps.

> **S4-17 — Dockerfiles: überflüssige `build`-Stage und `COPY . .` vom Repo-Root**
> **S4** · Betrieb · **S** · Gefunden von Track 5 (I12)
> **Ort**: `src/EmotePurge.Api/Dockerfile:12-25` · `src/EmotePurge.Worker/Dockerfile:13-26`
> **Was**: Non-Root-User ist in beiden korrekt (verifiziert). Aber die `build`-Stage (`dotnet build -o /app/build`) erzeugt Output, der nirgends kopiert wird — nur `publish` landet in `final`. `COPY . .` kopiert den gesamten Repo-Root, für den Worker inklusive `web/` und `tests/`.
> **Warum es zählt**: Kein Korrektheitsproblem, aber eine reine Frontend-Änderung invalidiert wegen `COPY . .` auch den Worker-Build-Cache.
> **Fix**: `build`-Stage entfernen, `publish` direkt aus `restore`; `COPY . .` → `COPY src/ src/` (Context bleibt `.`, weil die Api zusätzlich `web/` für die `web-build`-Stage braucht).

> **S4-18 — Repo-Hygiene: kein README/LICENSE/CONTRIBUTING, leeres `marketing/`**
> **S4** · Aufräumen · **S** · Gefunden von Track 5 (I13)
> **Ort**: Repo-Root (verifiziert per `ls`) · `marketing/` (leer und ungetrackt, per `git ls-files`) · `web/README.md` (unverändertes `ng new`-Boilerplate)
> **Fix**: Root-`README.md` mit Kurzbeschreibung und Verweis auf `Architectur.md`/`CLAUDE.md`; `LICENSE` je nach gewünschter Lizenzierung (bei Closed Source explizit *kein* OSS-Text, sondern ein kurzer Copyright-Hinweis); `marketing/` befüllen oder entfernen.

---

## 4. Zielkonflikte

Fünf Stellen, an denen zwei Tracks unabhängig voneinander in unterschiedliche Richtungen zeigen. Sie werden hier bewusst als Konflikt stehen gelassen, statt stillschweigend zugunsten einer Seite aufgelöst zu werden — jeweils mit einer Empfehlung, die beide Anliegen bedient.

### Z1 — `GET /api/worker/health`: absichern (Track 1) vs. öffentlich lassen (Track 5)

**Track 1 (S4-2)** bemängelt, dass dieser Endpoint als einziger keinerlei Auth und kein Rate Limit hat: Jeder Aufruf kostet einen Redis-Roundtrip gegen genau die Instanz, die auch das Join/Leave-Kommandoprotokoll trägt, und der Antwortkörper verrät, wann die Erfassung ausfällt. Empfehlung dort: `.RequireAuthorization()`.
**Track 5 (S3-36)** baut auf exakt dieser Eigenschaft seinen Monitoring-Vorschlag auf: ein externer UptimeRobot-Check gegen `https://emotepurge.app/api/worker/health`, ausdrücklich weil der Endpoint unauthentifiziert erreichbar ist. **Track 5 (S3-35)** braucht denselben Pfad zusätzlich für einen Compose-`healthcheck` per `curl -f` — und dafür muss er bei `unknown`/`disconnected` einen Nicht-200-Status liefern, was er heute nicht tut. Ein authentifizierter Endpoint wäre für beide Zwecke unbrauchbar (weder UptimeRobot noch der Container-interne `curl` haben ein Cookie).

**Empfehlung — aufteilen, dann bedienen beide Seiten sich selbst:**
1. **Neu `GET /api/health`, öffentlich, ohne Payload**: liefert `200` bei „Worker verbunden und Chat-Aktivität frisch", `503` bei `disconnected`/`unknown`/`stale`. Kein Body, keine Zeitstempel, keine Zahlen — damit entfällt Track 1s Informationsargument vollständig. Zusätzlich `.RequireRateLimiting(...)` mit einer eigenen, großzügigen IP-Policy (z. B. 60/min), das deckt Track 1s Redis-Hebel ab und ist für einen 5-Minuten-Uptime-Check weit ausreichend. Dieser Pfad ist gleichzeitig das Ziel des Compose-Healthchecks aus S3-35.
2. **Bestehendes `GET /api/worker/health` hinter `.RequireAuthorization()`**: behält den detaillierten Body (`lastMessageReceivedUtc`, `secondsSinceLastMessage`) für die `AppShell`-Anzeige. Einziger Consumer ist `WorkerHealthService`, das ausschließlich unter geguardeten Routen rendert — es bricht nichts weg.
3. **Für den Worker selbst** bleibt der Dead-Man's-Switch aus S3-36 (`healthchecks.io`-Ping aus `WorkerHealthPublisher`) der richtige Weg, weil ein Api-Uptime-Check „Prozess lebt, tut aber nichts" grundsätzlich nicht sieht.

**Wichtig**: Punkt 1 ist erst dann verlässlich, wenn **S2-7** behoben ist — heute kann `isConnected` auf einem toten Client dauerhaft `true` bleiben, und ein Healthcheck, der genau in diesem Zustand „grün" meldet, ist schlimmer als keiner.

### Z2 — Data Protection: Keys persistieren (Track 5) macht die Sicherheitslage schlechter (Track 1)

**Track 5 (I2)** empfiehlt `PersistKeysToFileSystem("/keys")` plus Volume, damit nicht jeder Deploy alle Nutzer ausloggt.
**Track 1 (B3)** weist darauf hin, dass der Redeploy heute faktisch der **einzige** vorhandene Session-Widerruf ist: Es gibt keinen `ValidatePrincipal`-Hook, keine Session-Tabelle, keine Token-Version, und `logout` löscht nur die Browser-Kopie. Wird nur das Volume angehängt, ist ein gestohlenes Cookie 14 Tage lang durch **nichts** mehr invalidierbar.

**Empfehlung**: Beides in einem Zug, wie in S2-9 beschrieben — Keys persistieren **und** `User.SessionsValidFromUtc` + `OnValidatePrincipal` + serverseitig wirksames Logout einführen. Wer die Widerrufbarkeit auf später verschieben will, muss im selben Commit `ExpireTimeSpan` von 14 auf 7 Tage senken; das ist der einzige akzeptable Zwischenstand.

### Z3 — Rate Limiting: mehr Endpoints absichern (Track 1) vs. `sync-deleted` befreien (Track 2b)

**Track 1 (S2-11)** will `ExpensiveOps` auf deutlich mehr Endpoints legen, weil dort ungecachte Fremd-API-Aufrufe hängen.
**Track 2b (S2-12)** will `sync-deleted` **aus** `ExpensiveOps` herausnehmen, weil ein 429 dort den einzigen Buchhaltungs-Call verschluckt, der nie verloren gehen darf — er teilt die 20/min-Policy heute mit `join` und den Vote-Endpoints.

Kein echter Widerspruch, sondern eine falsch geschnittene Policy: `ExpensiveOps` mischt „teuer für fremde APIs" (Helix/7TV) mit „teuer für uns" (DB-Schreibpfad).
**Empfehlung**: Zwei Policies. `ExternalApi` (streng, 20/min) für alles, was Helix oder 7TV anfasst — dazu gehören nach Track 1 zusätzlich `channels/mine`, die `UsageStatsEndpoints`- und die ganze `EmoteEndpoints`-Gruppe sowie `GET .../vote-sessions`. `Bookkeeping` (großzügig, z. B. 120/min) für `sync-deleted`, das nichts weiter tut, als ein Flag zu setzen. Damit bekommen beide Tracks, was sie brauchen, ohne dass eine Seite zurückstecken muss.

### Z4 — `channelName`-Validierung: im Filter (Track 1) vs. als Route-Constraint (Track 4)

**Track 1 (S3-3)** will `ChannelNameValidation.IsValid()` in die vier Autorisierungsfilter ziehen, damit kein ungeprüfter Wert zum Redis-Key wird; das ändert keine Zelle der Autorisierungsmatrix und liefert 400 statt 403 für Müll-Eingaben.
**Track 4 (S3-31)** schlägt für dasselbe Problem ein Route-Constraint `{channelName:regex(^[a-z0-9_]{4,25}$)}` auf der Gruppe vor — eleganter, aber es liefert **404** statt 400 und ändert damit den Fehlercode-Kontrakt.
Zusätzlich dokumentiert `CLAUDE.md:178` die Reihenfolge „Filter vor Validierung" als bewusste Entscheidung des Endpoint-Refactorings — deren Begründung („Verhalten 1:1 identisch") trägt für das Refactoring-Ziel, sagt aber nichts darüber, dass der ungeprüfte Wert in einen persistenten Redis-Key wandert.

**Empfehlung**: Track 1s Variante umsetzen (Prüfung in den Filtern, 400 bleibt 400), und Track 4s Gruppen-**Filter**-Option als Umsetzungsform wählen, damit es eine Stelle statt vier wird. Das Route-Constraint bewusst nicht, weil ein stiller Wechsel von 400 auf 404 den Fehlercode-Vertrag und `apiErrorTranslationKey` (S2-13) betrifft. Der Entscheidungslog-Eintrag `:178` bekommt einen Nachtrag.

### Z5 — Auswahl: bei Reihenfolgewechsel leeren (S1-3) vs. über Filterwechsel erhalten (S2-16)

**S1-3** verlangt, die Auswahl zu leeren, sobald sich die Reihenfolge oder die Objektidentität ändert — sonst löscht der Shift-Bereich die falschen Emotes.
**S2-16** verlangt genau das Gegenteil für den Filterpfad: Der iterative Aufräum-Workflow bei 1.000 Emotes ist nur möglich, wenn die Auswahl einen Filterwechsel überlebt.

**Empfehlung**: Der Konflikt existiert nur, solange `ListSelection` an Index und Objektidentität hängt. Mit der keyed Variante aus S1-3 (Auswahl als `Set<emoteId>`, Shift-Anker als Item statt Index) lösen sich beide Anforderungen gleichzeitig: Die Auswahl überlebt Sortierung, Refetch **und** Filterwechsel, und der Shift-Bereich wird immer zur Klickzeit gegen die aktuell sichtbare Liste aufgelöst. **Reihenfolge zwingend**: erst die Sofortfixes (`clear()` an den drei fehlenden Stellen), dann die keyed Umstellung, und **erst danach** S2-16. Die Auswahl über Filterwechsel zu erhalten, bevor sie keyed ist, wäre eine Verschlimmerung von S1-3.

---

## 5. Pre-Launch-Checkliste

In dieser Reihenfolge abzuarbeiten; `robots.txt` ist bewusst der letzte Schritt.

- [ ] **Klären, ob und welche Rechtstexte für diesen Betrieb nötig sind** (Zielgruppe, Sitz des Betreibers, Hosting-Ort, invite-only vs. öffentlich). Die App verarbeitet Twitch-User-IDs, Logins/Sessions und zeitweise einen Access-Token-Claim im Cookie. Grundlage für alles Weitere. *(S2-20, S)*
- [ ] **Data-Protection-Keys persistieren — zusammen mit der Widerrufbarkeit oder mindestens mit `ExpireTimeSpan` 14 → 7 Tage.** *(S2-9, M — siehe Z2)*
- [ ] **Cookie `SecurePolicy.Always` + HSTS-Header setzen, `UseHttpsRedirection` klären.** Vorher einmal `curl -sI https://emotepurge.app/api/auth/twitch/login | grep -i set-cookie` — steht dort kein `secure`, ist die Annahme heute schon verletzt. *(S2-10, S)*
- [ ] **Ressourcenlimits setzen, bevor der Stresstest läuft** — sonst Risiko für die Nachbar-App auf dem VPS. *(S2-21, S)*
- [ ] **Mindest-Monitoring: externer Uptime-Check + Dead-Man's-Switch für den Worker.** *(S3-36, S — setzt Z1 Punkt 1 und S2-7 voraus)*
- [ ] **Backup-Strategie einrichten**, bevor echte Nutzerdaten in Menge anfallen; Off-Site-Kopie nicht vergessen. *(S1-2, S–M)*
- [ ] **Health-Endpoints aufteilen** (`/api/health` öffentlich ohne Payload mit korrektem Statuscode, `/api/worker/health` hinter Auth) und die Compose-Healthchecks für `api` und `worker` verdrahten. *(S4-2, S3-35, Z1, S–M)*
- [ ] **Redis `maxmemory` + `maxmemory-policy allkeys-lru` setzen** und die `channelName`-Validierung in die Filter ziehen. *(S3-3, S)*
- [ ] **Fail-Fast-Guard für ausstehende Migrationen** einbauen, damit ein vergessenes `dotnet ef database update` als Crash-Loop sichtbar wird statt als stille 500er. *(S3-34, S)*
- [ ] **`docker-compose.prod.yml` umbenennen** (`name:`, `container_name:`), damit ein lokaler Start nie auf das Produktions-Volume greifen kann. *(S3-38, S)*
- [ ] **Cookie-Hinweis/Consent**, falls die Klärung aus Punkt 1 das verlangt (technisch notwendige Cookies sind oft consent-frei — Teil der Klärung, nicht selbst zu entscheiden). *(S–M)*
- [ ] **Impressums-Seite**, sobald die Betreiberangaben feststehen. *(S2-20, S)*
- [ ] **Datenschutzerklärungs-Seite** (Route + Footer-Link): welche Twitch-Daten (User-ID, Login-Name, Rollen-Check-Ergebnisse) wie lange gespeichert und verarbeitet werden. *(S2-20, M)*
- [ ] **GHCR-Package-Sichtbarkeit prüfen** — falls privat, sicherstellen, dass der VPS-Docker-Daemon dauerhaft gültige Pull-Credentials hat (`docker/login-action` schreibt keine Auth für den Host). *(S, ungeklärt)*
- [ ] **`robots.txt` `Disallow: /` entfernen — als LETZTER Schritt**, erst wenn Impressum/Datenschutz/Cookie-Hinweis stehen. Aktuell korrekt für den Testbetrieb, muss aber am Launch-Tag aktiv geändert werden, sonst bleibt die Seite dauerhaft aus Suchmaschinen ausgeschlossen. *(S, leicht zu vergessen)*

---

## 6. Vor dem HandOfBlood-Stresstest

Was unter ~1.000 Emotes und sehr hoher Chat-Frequenz zuerst bricht — Reihenfolge nach Eintrittswahrscheinlichkeit, aus Track 2a (Abschnitt 3), Track 2b und Track 3.

**1. Die Verbindung, nach 10 Reconnects — Tage nach dem Start, ohne Vorwarnung.** Der wahrscheinlichste Bruch, und die Last ist dabei nicht einmal der Auslöser, sondern nur die **Uptime**. Bei HoB ist `LastMessageReceivedUtc` dauernd frisch, der eigene Watchdog feuert faktisch nie; TwitchLibs interner WatchDog reconnectet autonom bei jedem Netz-Hickser und verbrennt das 10er-Kontingent still mit. Bester Fall ~6 Minuten Ausfall, realistischer Fall 15–20 Minuten — bei HoB-Frequenz mehrere zehntausend verlorene Emote-Treffer. → **S2-1** (eliminiert die Klasse vollständig), abgesichert durch **S2-2**, **S2-4**, **S2-7**.

**2. Der JOIN-/NAMES-Burst bei jedem (Re-)Join.** TwitchLib fordert unbedingt `CAP REQ twitch.tv/membership` an (in der DLL verifiziert, über die genutzte Konstruktion nicht abschaltbar). Beim Join eines Channels dieser Größe schickt Twitch die komplette NAMES-Liste, die TwitchLib zeilenweise parst — und das bei **jedem** Reconnect erneut. Kritisch nur in Kombination mit Punkt 1. Treten im Test CPU-Spitzen oder Matching-Verzögerungen direkt nach einem Reconnect auf, ist das die Ursache. Gegenmittel: `WebSocketClient` selbst instanziieren (passiert für S2-1 ohnehin); `OnUserJoined`/`OnUserLeft`/`OnExistingUsersDetected` werden korrekterweise bereits nicht abonniert.

**3. Ein stiller, permanenter Ausfall für genau den einen Channel.** Wird HoB per UI gejoint, während gerade ein Recreate läuft, fällt der Join in ein bis zu 165 Sekunden langes Fenster, verschwindet als Warnung im Log und wird nie nachgeholt — während der 7TV-Sync danach durchläuft und DB und Match-Cache füllt. Von außen sieht alles korrekt aus: Channel in der Übersicht, 1.000 Emotes, `IsBotActive = true`, **null Usage-Daten, für immer**. → **S2-3**.

**4. Der Flush-Pfad.** Bei ~1.000 aktiven Emotes macht `UsageStatFlushService` alle 30 s zwei `SELECT` mit ~1.000-elementiger `IN`-Liste und bis zu 1.000 einzelne `UPDATE`/`INSERT` über den EF-Change-Tracker. Für einen Channel unkritisch, bei zehn aktiven Channels der dominante DB-Kostenfaktor — und jeder Fehlschlag verwirft den kompletten 30-s-Batch ersatzlos. → **S3-13** (`ON CONFLICT`-Upsert reduziert das auf einen Roundtrip und macht es mehrschreiberfest).

**5. Ein einzelner 7TV-Fehl-Response kostet bis zu 60 Sekunden Matching.** Liefert 7TV ein Set mit `emotes: []`, archiviert der Sync alles und schreibt ein leeres Match-Dictionary; bei HoB-Frequenz sind das Tausende verlorener Treffer pro Vorfall. → **S3-12**.

**6. Prozesstod durch Nebensächlichkeiten.** Ein `pg`-Hickser in der ungeschützten Resync-Query oder eine `JsonException` in der Boot-Recovery beenden den kompletten Worker-Prozess; die Boot-Recovery-Variante läuft danach in einen Crash-Loop. Über einen mehrtägigen Test ist die Eintrittswahrscheinlichkeit hoch. → **S2-5**, **S2-6**, verschärft durch **S2-8** (paralleler Sync erzeugt genau die `DbUpdateException`, die S2-5 tödlich macht).

**7. Das UI wird bei 1.000 Emotes zur eigentlichen Gefahrenquelle.** Die Suche findet nichts, weil sie Exakt-Match ist (**S2-15**); jeder Filterwechsel wirft die Auswahl weg und es gibt kein „Alle auswählen" (**S2-16**); jeder Vote löst einen Voll-Refetch von 1.000 Objekten aus (**S2-17**); die Auswahl driftet nach Sortierung und Refetch still von der Anzeige ab (**S1-3**); und der Bestätigen-Button ist klickbar, bevor die Fremd-Set-Prüfung antwortet, die bei vielen moderierten Channels 1–3 Sekunden braucht (**S1-4**). Der plausibelste Weg zu echtem Schaden ist dabei die Erstnutzung: 1.000 Karten mit „0x", ein Klick auf „Nur ungenutzte" — und das gesamte Emote-Set steht als Löschkandidat da (**S2-14**).

**Nicht das Problem, trotz Intuition** (aus Track 2a geprüft): das Matching selbst (~1.600 Dictionary-Lookups/s bei 200 msg/s), der Emote-Cache-Swap (atomarer Referenztausch, rennfrei), `EmoteUsageCounter`-Contention (ein einziger Producer-Thread), und Duplikat-Emotenamen (live gegen 983 Emotes verifiziert).

**Empfohlenes Minimum vor dem Test**: S2-1, S2-2, S2-3, S2-4, S2-5, S2-6, S2-7, S3-12, S1-3, S1-4, S2-14, S2-15. Alles davon ist S- oder M-Aufwand.

---

## 7. Was gut ist

Konsolidiert aus den „Geprüft und in Ordnung"-, „Solide"-, „Gut gelöst"- und „Gut strukturiert"-Abschnitten aller sechs Tracks. Ohne diesen Abschnitt ist der Report nicht bewertbar.

**Sicherheit und Autorisierung**
- **Kein Cross-Channel-Zugriff, explizit nachverfolgt.** Kein Request-Body-Record trägt einen `channelName` (`CreateVoteSessionRequest`, `SyncDeletedRequest`, `CastVoteRequest` — alle ohne), es gibt also keine Filter/Handler-Divergenz. Jede Session-, Emote- und Vote-Abfrage ist an `ChannelId` gebunden; `RetractVoteAsync` ist zusätzlich auf `UserId == eigener` gescoped.
- **Alle fünf `IEndpointFilter` sind durchweg fail-closed.** `TryBuildTwitchPrincipal` liefert bei fehlendem `NameIdentifier` **oder** `twitch:login` `null`; ein unparsbarer oder abgelaufener Token führt zu `AccessToken = null`, nicht zu „gültig". Der Default-Zweig im `switch` ist `Results.Forbid()` — ein neu hinzugefügter Enum-Wert führt zu Verweigerung, nicht zu Zulassung. Die Filter beziehen ihren Service über `HttpContext.RequestServices` statt Konstruktorinjektion — das ist bei `AddEndpointFilter<T>()` **nicht** der Service-Locator-Fehlgriff, für den man es halten könnte, sondern der einzig korrekte Weg für scoped Services.
- **Der OAuth-Flow ist sauber.** State ist 16 Byte aus `RandomNumberGenerator`, im HttpOnly-Cookie mit 5 Minuten Ablauf, **vor** der Prüfung gelöscht (verhindert Replay), Prüfung fail-closed inklusive `expectedState is null`. PKCE fehlt, ist für einen Confidential Client aber nicht erforderlich. Kein Open Redirect: `PostLoginRedirectUrl` kommt ausschließlich aus der Konfiguration, die Return-URL ist rein clientseitig und läuft über `router.navigateByUrl`.
- **Der Twitch-Access-Token verlässt den Server nie** — Claim im verschlüsselten Cookie, von `/api/auth/me` bewusst nicht zurückgegeben (`:98-104` liefert nur `tokenExpiresAtUtc`).
- **Kein Interna-Leak trotz fehlendem Exception-Handler**: Ohne Middleware bricht Kestrel body-los ab — kein Stacktrace, keine Exception-Message, kein Connection-String. `MapOpenApi()` ist korrekt auf Development beschränkt.
- **Das 7TV-Zero-Knowledge-Design trägt.** `sessionStorage` statt `localStorage`, Löschung bei Logout, bei Session-Ablauf und bei 401/403 von 7TV, Token ausschließlich als `Authorization`-Header an `7tv.io` — er berührt EmotePurges Backend nie und taucht damit in keinem Log, keinem Backup und keinem DB-Dump auf. Der Schutz gegen XSS ist die CSP, und diese Kette ist tragfähig.
- **Frontend-Guards und Backend-Filter laufen nicht auseinander**, weil die Guards den **echten** Endpoint proben statt eines separaten „darf ich"-Endpoints — das kann per Konstruktion nicht divergieren. Track 1 hat keine Stelle gefunden, an der sich ausschließlich der Client auf eine Prüfung verlässt.
- Sonstiges bestätigt: Range-Cap 366 Tage mit `TryParseExact`/`InvariantCulture`, `pageSize`-Cap 100 in beiden Paginierungspfaden, `TotalCount` für Nicht-Manager aus der gefilterten Menge (kein Existenz-Leak), `MapFallback("/api/{**rest}")` vor `MapFallbackToFile`, alle EF-Queries LINQ-parametrisiert, kein Raw SQL im Repo.

**Worker und Nebenläufigkeit**
- **Die `EmoteMatchCache`-Publikation ist rennfrei.** `RefreshMatchCacheAsync` baut das Dictionary vollständig lokal auf und publiziert es mit einem einzigen Indexer-Set; ein Leser sieht immer entweder den vollständigen alten oder den vollständigen neuen Stand — nie einen halbfertigen. Es gibt **kein** Verlustfenster durch den Reload.
- **Transiente 7TV-Fehler leeren den Cache nicht** — `SyncChannelAsync` kehrt zurück, *bevor* irgendetwas geschrieben wird.
- **Duplikat-Emotenamen werden koalesziert statt zu werfen**, live gegen 983 Emotes verifiziert — die HoB-Größenordnung ist an dieser Stelle kein Neuland.
- **Der Resync-Worker isoliert Channel-Fehler korrekt**, und Läufe können sich konstruktionsbedingt nicht überlappen.
- **Die Cooldown-Arithmetik des Watchdogs ist korrekt** — `_lastForcedReconnectUtc` wird vor dem Reconnect gesetzt, eine spätere echte Freeze-Erkennung wird nicht länger als 5 Minuten blockiert. Die Kritik in S3-11 gilt der Signalquelle, nicht der Logik.
- **`LastMessageReceivedUtc` ist über `Interlocked` auf `long`-Ticks korrekt gegen Tearing abgesichert**, auch auf 32-Bit.
- **Drei von vier Hosted Services haben ihren Catch-all am richtigen Platz** und jeweils passend kommentiert; `UsageFlushWorker.StopAsync` hängt nicht (Host-Shutdown-Token wird durchgereicht und von EF respektiert).
- **`LeaveChannelAsync` entfernt aus `_joinedChannels` im `finally`** — richtig, auch ein gescheitertes PART soll den Channel aus dem Rejoin-Set nehmen.
- **`libgssapi-krb5-2` im Worker-Dockerfile** ist ein Root-Cause- statt Symptom-Fix und sauber dokumentiert; die Log-Unterdrückung von TwitchLibs `LogUnaccountedFor`-Rauschen trifft die richtige Kategorie.

**Daten und Persistenz**
- **Cross-Channel-Archivierung über `sync-deleted` ist unmöglich** — `MarkDeletedAsync` filtert auf `ChannelId`, fremde IDs fallen in `notFoundIds`.
- **Die Konvergenz-Annahme trägt tatsächlich.** `ReconcileAsync` archiviert, `UpsertEmote` entarchiviert — beide Richtungen, innerhalb ≤60 s; `IsBotActive` wird nirgends auf `false` gesetzt (per Grep verifiziert). Das ist der Grund, warum alles nach dem GraphQL-Call höchstens S3 ist.
- **Archivierung während einer laufenden Vote-Session ist sauber**: `GetResultsAsync` filtert `!IsArchived`, `Vote`-Zeilen überleben, `CastVoteAsync` lehnt archivierte Emotes mit `EmoteNotEligible` ab.
- **Alle vier Migrationen sind unkritisch**, kein `DropColumn`/`DropTable` in einem `Up`; `ConvertUsageStatDateToDateOnly` ist die einzige mit Datenkontakt und sorgfältig gebaut (expliziter `USING`-Cast, funktionierendes `Down`). Der Modell-Snapshot deckt sich mit den Entitäten — bei manuell angewendeten Migrationen der eigentliche Risikofall.
- **`Vote.User` bewusst `Restrict` statt `Cascade`** — die einzige nicht kaskadierende Kante, und die richtige Wahl. Genau diese Sorgfalt fehlt der Channel-Kante (S1-1).
- **Die FK-Vorprüfung in `UsageStatFlushService`** ist notwendig und richtig; die TOCTOU-Bewertung aus dem Entscheidungslog trägt weiterhin.

**Frontend und UX**
- **i18n ist vollständig und diszipliniert**: beide Dateien exakt 183 Schlüssel, **kein Drift in beide Richtungen** (maschinell verglichen), alle im Code referenzierten Keys existieren, genau ein hartkodierter Nutzerstring im gesamten Frontend. Der Laufzeit-Sprachwechsel ist konsequent durchdacht (kein Doppel-Fetch, `document.documentElement.lang` gepflegt, `locale.ts` umgeht sauber die Bootstrap-Statik von `LOCALE_ID`).
- **Fehlertexte sind nutzergerecht, nicht technisch** — „Live-Moderator-Liste konnte nicht geladen werden (Twitch-Zugriffstoken abgelaufen?) … Neu einloggen, um die volle Liste zu sehen" nennt Ursache **und** Handlungsschritt.
- **Graceful Degradation auf der Übersicht** (`helixUnavailable`/`sevenTvUnavailable`) zeigt an, *welcher Teil* unvollständig ist, statt den Request scheitern zu lassen — vorbildlich.
- **Der Mass-Delete-Bestätigungsdialog ist inhaltlich stark**: Anzahl im Titel, Namensvorschau, Unwiderruflichkeits-Hinweis, und — bemerkenswert selbstkritisch — der Hinweis, dass fremde Channels mit demselben Set grundsätzlich nicht erkennbar sind. Diese Ehrlichkeit über die eigene Erkennungsgrenze ist besser als das, was die meisten Tools bieten.
- **Die 7TV-Token-Sicherheitswarnung** erklärt Tragweite, Speicherort und Lebensdauer in zwei Sätzen ohne Jargon, und `type="password"` verhindert Shoulder-Surfing im Stream.
- **Der `orderedEmoteIds`-Freeze** über Post-Vote-Reloads ist ein Detail, das man erst nach echtem Live-Testen findet — mit Kommentar, der genau sagt warum.
- **`alt=""` an den Emote-Bildern ist hier korrekt** (der Name steht unmittelbar darüber als Text), und **`language-switcher.ts`/`datetime-picker.ts` sind die A11y-Referenz im eigenen Repo** — `aria-pressed`, übersetztes `aria-label`, vollständig aus `<button>`-Elementen gebaut. Das Team kann A11y; es ist im Emote-Grid schlicht nicht angewendet worden.
- **`usage-stats-page.ts` ist die einzige Seite mit vollständigem Zustandstrio** (Laden / Leer / Fehler) und taugt als Muster für die übrigen fünf.

**Architektur, Tests und CI**
- **Die Kern-Schichtung hält, ohne Werkzeug.** `EmotePurge.Core` hat **null** Paket- und **null** Projektreferenzen; über 22 Dateien gibt es genau eine projektfremde `using`-Direktive, und die zeigt auf Core selbst. Nach sechs Tagen Feature-Druck ist das nicht selbstverständlich.
- **Die Interface-Konvention ist ausnahmslos durchgehalten** — jeder Service in `Infrastructure` hat sein Interface in `Core`, kein generisches Repository, und die dokumentierten `BackgroundService`-Ausnahmen decken sich mit dem Code (bis auf die Auslassung in der Liste selbst, S4-13).
- **Die Endpoint-Aufteilung war die richtige Entscheidung**: `Program.cs` von ~680 auf 117 Zeilen, sechs `Endpoints/*.cs` nach Domäne statt nach technischer Schicht geschnitten. Controller hätten hier nichts verbessert.
- **Die Kommentarqualität ist außergewöhnlich** — sie erklären fast durchweg *warum* und nennen oft die verworfene Alternative mit (`UsageStatQueryService.cs:152-158`, `VoteSessionEndpoints.cs:116-124`, `TwitchChatManager.cs:23-30`). Das ist genau der Kommentartyp, den man nach sechs Monaten braucht.
- **Die Testauswahl folgt einem Prinzip**: Testcontainers gegen echtes Postgres/Redis statt In-Memory-Provider — genau die Entscheidung, die die EF-Übersetzungsprobleme aus dem Log überhaupt reproduzierbar macht. `VoteSessionQueryServiceTests` deckt die Score-Formel inklusive `max == min` ab.
- **Die Angular-Schichtung ist sauberer als die Backend-Seite**: 0 Verstöße in allen drei Richtungen.
- **`ApiErrorCodes` war die richtige Investition** — sprachneutrale, stabile Codes statt Prosa im API-Body, genau die Trennung, die den nachträglichen Einbau von Transloco billig gemacht hat.
- **CI ist richtig verdrahtet**: `needs: [test, test-web]` gated den GHCR-Push auf zwei unabhängige Jobs, Permissions sind gescoped (`contents: read`, `packages: write` nur im `publish`-Job), und SHA-Tags neben `:latest` ermöglichen manuelles Rollback.
- **Non-Root-Container in beiden Dockerfiles korrekt**, Layer-Caching für `dotnet restore` richtig vorbereitet, Healthchecks für `postgres`/`redis` inklusive `depends_on: condition: service_healthy`, `.env` korrekt gitignored und nie getrackt, `.dockerignore` sinnvoll geschnitten. **Redis ohne Volume ist hier kein Bug** — Pub/Sub, Health-TTL, Mod-Cache und Rate-Limit-State sind bewusst ephemer.

---

## 8. Empfohlene Reihenfolge der Umsetzung

### Welle A — Quick Wins (mechanisch, sofortige Wirkung)

Alles hier ist S-Aufwand und weitgehend unabhängig voneinander; zusammen ein Abend.

S1-4 (`[disabled]="warningLoading()"`) · S1-3 Sofortteil (`clear()` an den drei fehlenden Stellen) · S3-18 (sichtbarer Auswahlzustand) · S2-15 (Teilstring-Suche) · S2-18 (`available` auswerten) · S2-19 (Session-Titel in der Löschbestätigung, Buttons trennen) · S3-22 (Logout-Fehlerpfad) · S3-30 (doppeltes `load()` streichen) · S3-14 (Recreate-Log-Grund) · S3-17 (Labels + Kontrast, Suchen-und-Ersetzen) · S3-16 (`role="alert"`, `cdkTrapFocus`) · S4-3 (`form-action 'self'`) · S4-8, S4-9, S4-13, S4-14, S4-1, S4-12 (Doku und Aufräumen).

### Welle B — Sicherheit & Korrektheit (alle S1/S2 aus Security und Datenintegrität)

**B1 — vor dem Stresstest, Worker-Cluster:** S2-1 (zuerst, ersetzt zwei Heuristiken) → S2-4 → S2-7 → S2-2 → S2-3 → S2-5 → S2-6 → S2-8. Danach S3-12 und S3-13, ebenfalls vor dem Test.
**B2 — Datenverlust schließen:** S1-1 (Soft-Deactivate) → S1-2 (Backup, unabhängig davon sofort) → S1-3 keyed `ListSelection` → S2-12 (`sync-deleted`-Fehlerpfad).
**B3 — Session und Transport:** S2-9 und S2-10 gemeinsam (siehe Z2) → S3-3 (Redis-Key + `maxmemory`) → S2-11 (Rate Limiting nach dem Policy-Schnitt aus Z3) → S3-1, S3-4, S3-7 → S3-2 (Twitch-User-ID als Identität).

### Welle C — Refactorings (strukturelle Bereinigung)

S2-13 Schritt 1+2 (Interceptor mit `/api/`-Beschränkung, Status-Fallback im Fehler-Mapping) → S3-26 (`LoadChannelSessionAsync` + `ChannelName.Normalize`) → S3-32 (`ISevenTvEditorService`) → S2-13 Schritt 3 (`/permissions`-Endpoint, löst gleichzeitig die vier UI-Sonden ab) → S3-31 (Validierung im Service, Gruppen-Filter statt Route-Constraint, siehe Z4) → S3-30 (`rxResource` als Pilot auf einer Seite) → S3-33 (`strict`, danach separat `strictTemplates` messen) → S4-11 (`IWorkerHealthReader`) → S3-6 Teil 1 (`ReconnectPolicy` extrahieren) → S3-27 (Doku-Umbau in einem `docs:`-Commit).

### Welle D — Tests (Abdeckung für autorisierungs- und datenkritische Lücken)

In dieser Reihenfolge, sortiert nach Risiko pro Aufwand — die ersten beiden brauchen **keinen** Container:
1. `ChannelAccessServiceTests` (S3-5) — der zentrale Zugriffsgate, bestes Risiko-pro-Aufwand-Verhältnis im ganzen Projekt.
2. `VoteEligibilityServiceTests` (S3-5) — volle Rollenmatrix plus die Abgrenzung `EvaluateAsync` ↔ `EvaluateAudienceAsync`; nagelt die kontraintuitivste Regel des Projekts fest, die heute nur in einem Kommentar lebt.
3. `VoteSessionService.CastVoteAsync` — Insert-vs-Update, archiviertes Emote, Emote/Session aus fremdem Channel; reine Ergänzungen in einer vorhandenen Datei mit vorhandener Fixture.
4. `SevenTvSyncService.ReconcileAsync`/`RefreshMatchCacheAsync` — läuft jede Minute für jeden aktiven Channel und ist der einzige Schreiber auf `IsArchived`; sichert zugleich S3-12 ab.
5. `UsageStatFlushService.FlushAsync` — einziger Schreibpfad für die Kerndaten, verfälscht im Fehlerfall **still**; sichert S3-13 ab.
6. `ReconnectPolicy` + `EmoteUsageCounter` als reine Unit-Tests (S3-6), nachdem C die Extraktion gemacht hat.
7. Zwei Struktur-Tests, die Konventionen in Struktur verwandeln: Reflection-Test über die Core-Assembly-Referenzen (S3-28) und ein Vitest über die Fehlercode-Keys beider Locale-Dateien (S3-29) — laut Track 4 der billigste Hebel im ganzen Report.

### Welle E — Infra & Launch

S2-21 (Ressourcenlimits, vor dem Stresstest) → Z1-Aufteilung der Health-Endpoints + S3-35 (Compose-Healthchecks) → S3-36 (Uptime-Check + Dead-Man's-Switch) → S3-34 (Migrations-Guard) → S3-38 (Compose-Namen) → S3-37 (`pull_request`-Trigger) → S4-15, S4-16 (SHA-Pinning, Dependabot, Format-Checks) → S4-17, S4-18 (Dockerfile- und Repo-Hygiene) → S2-20 (Rechtstexte) → `robots.txt` öffnen.

**Reihenfolge-Hinweise, die nicht offensichtlich sind:**
- S2-1 **vor** allen anderen Worker-Fixes — es macht die proaktive Recreate-Heuristik obsolet, die sonst in S3-14 und S4-6 mitgeschleppt würde.
- S2-7 **vor** dem Health-Monitoring aus Welle E — sonst meldet der neue Healthcheck im kritischsten Zustand „grün".
- S1-3 keyed `ListSelection` **vor** S2-16 — siehe Z5.
- S2-13 Schritt 3 (`/permissions`) **vor** S3-19 — der Guard-Umbau ist danach billiger.
- S3-5 (Autorisierungs-Tests) idealerweise **vor** S3-1, S3-2 und S3-4 — das sind genau die Tests, mit denen sich diese drei Fixes regressionsfrei einbauen lassen.

---

## 9. Anhänge

Die Pflicht-Artefakte der Tracks, unverändert übernommen. Sie sind das Belegmaterial für die Befunde oben.

### Anhang A — Autorisierungsmatrix (Track 1)

**Legende**
`✅` ERLAUBT · `❌` VERWEIGERT (401 = kein Cookie, 403 = Filter lehnt ab) · `⚠️` bedingt/UNKLAR
`✅*` = Mod nur, wenn **entweder** ein positiver Redis-Cache-Eintrag (`modcheck:{uid}:{channel}`, TTL 10 Min, `ModRoleCache.cs:20`) existiert **oder** der Twitch-Access-Token noch gültig ist (< ~4 h, `ClaimsPrincipalExtensions.cs:20-26`) **und** Helix erreichbar ist. Sonst `❌` — der Pfad ist **fail-closed** (`ModeratorCheckService.cs:22-35`).
`⚠️S` = hängt an `VoteSession.AllowedVoterRoles` der konkreten Session, nicht an einer Endpoint-Regel.

| # | Endpoint | Admin | Broadcaster | Twitch-Mod | 7TV-Editor | Sub | User | Anon | Durchsetzung (gelesen) |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `GET /api/auth/twitch/login` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | kein Filter, `AuthEndpoints.cs:22` — by design |
| 2 | `GET /api/auth/twitch/callback` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | State-Cookie-Vergleich `AuthEndpoints.cs:58-64` (fail-closed) |
| 3 | `GET /api/auth/me` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 401 | `.RequireAuthorization()` `AuthEndpoints.cs:104` |
| 4 | `POST /api/auth/logout` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ (No-Op) | **kein** `RequireAuthorization()`, `AuthEndpoints.cs:106-110` — `SignOutAsync` ohne Session ist folgenlos |
| 5 | `GET /api/channels/{c}` | ✅ | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `ChannelEndpoints.cs:28` → `ChannelAccessService.cs:12-27` |
| 6 | `GET /api/channels` | ✅ | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `ChannelEndpoints.cs:44` → `IsGlobalAdmin` `ChannelAccessService.cs:48-52` |
| 7 | `GET /api/channels/mine` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 401 | nur Gruppen-`RequireAuthorization()` `ChannelEndpoints.cs:11`; Inline-Logik `:51-55` liefert **ausschließlich Selbstauskunft** (Helix-eigene Mod-Liste + eigene 7TV-Editor-Grants), kein Fremdzugriff möglich → korrekt |
| 8 | `POST /api/channels/{c}/join` | ✅ (jeder Name) | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `ChannelEndpoints.cs:74`; Admin-Freiheit dokumentiert (`CLAUDE.md:171`) und serverseitig durchgesetzt |
| 9 | `DELETE /api/channels/{c}` | ✅ | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `ChannelEndpoints.cs:90`; **Wirkung:** Hard-Delete + Cascade → **S1-1** |
| 10 | `POST .../emotes/sync-deleted` | ✅ | ✅ | ✅* | ✅ | ❌ 403 | ❌ 403 | ❌ 401 | Gruppenfilter `EmoteEndpoints.cs:16` → `CanViewUsageStatsAsync` `ChannelAccessService.cs:29-46`. **Schreib-Endpoint hinter dem schwächeren Filter** → **S4-1** |
| 11 | `GET .../emotes/set-warning` | ✅ | ✅ | ✅* | ✅ | ❌ 403 | ❌ 403 | ❌ 401 | dito; Antwort enthält *fremde* Channelnamen mit gleichem Emote-Set (`EmoteSetOwnershipService.cs:50-53`) — Feature-Zweck, akzeptabel |
| 12 | `GET .../emotes/active-set` | ✅ | ✅ | ✅* | ✅ | ❌ 403 | ❌ 403 | ❌ 401 | dito; **ohne** `RequireRateLimiting` → **S2-11** |
| 13 | `GET .../usage-stats` | ✅ | ✅ | ✅* | ✅ | ❌ 403 | ❌ 403 | ❌ 401 | `UsageStatsEndpoints.cs:14` |
| 14 | `GET .../usage-stats/totals` | ✅ | ✅ | ✅* | ✅ | ❌ 403 | ❌ 403 | ❌ 401 | dito; Range-Cap 366 Tage `:53-57` |
| 15 | `POST .../vote-sessions` | ✅ | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `VoteSessionEndpoints.cs:54` |
| 16 | `POST .../vote-sessions/{id}/end` | ✅ | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `VoteSessionEndpoints.cs:70`; Session→Channel-Bindung in `VoteSessionService.cs:61-62` (kein Cross-Channel via ID) |
| 17 | `DELETE .../vote-sessions/{id}` | ✅ | ✅ | ✅* | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 | `VoteSessionEndpoints.cs:81`; Bindung `VoteSessionService.cs:172-173` |
| 18 | `GET .../vote-sessions` | ✅ alle | ✅ alle | ✅* alle | ⚠️S | ⚠️S | ⚠️S | ❌ 401 | Inline: Manager-Zweig `:110` (`CanManageChannelAsync`), sonst Per-Session-`EvaluateAudienceAsync` `:125-134`. `TotalCount` wird für Nicht-Manager korrekt aus der **gefilterten** Liste gebildet (`:137`) — kein Existenz-Leak |
| 19 | `GET .../vote-sessions/{id}/results` | ⚠️ **nein**, sofern nicht zusätzlich BC/Mod/Sub | ✅ | ✅* | ⚠️S | ⚠️S | ⚠️S | ❌ 401 | `VoteAudienceFilter.cs:29` → `VoteEligibilityService.cs:61-104` — **kennt `IsGlobalAdmin` nicht** → **S3-1**; Payload enthält rohe Usage-Zahlen → **S3-4** |
| 20 | `POST .../vote-sessions/{id}/votes` | ⚠️ wie 19 | ✅ | ✅* | ⚠️S | ⚠️S | ⚠️S | ❌ 401 | `VoteEligibilityFilter.cs:24` (zusätzlich `IsActive`, `VoteEligibilityService.cs:26-29`); Emote-zu-Channel-Bindung `VoteSessionService.cs:100-105` |
| 21 | `DELETE .../votes/{emoteId}` | ⚠️ wie 19 | ✅ | ✅* | ⚠️S | ⚠️S | ⚠️S | ❌ 401 | `VoteSessionEndpoints.cs:212`; Löschung auf `UserId == eigener` gescoped `VoteSessionService.cs:152-153` |
| 22 | `GET /api/vote-sessions/mine` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 401 | `.RequireAuthorization()` `:236`; Query auf `voterTwitchUserId` gescoped `VoteSessionQueryService.cs:112-113` |
| 23 | `GET /api/worker/health` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **keinerlei Auth**, `WorkerHealthEndpoints.cs:11` → **S4-2** |

**Auflösung der `⚠️S`-Zellen** (aus `VoteEligibilityService.EvaluateRoleAsync:61-104` gelesen, nicht aus Namen abgeleitet): `Everyone`-Flag gesetzt → **jeder eingeloggte Nutzer** ✅ (Zeile 66-69, vor allen anderen Prüfungen). Broadcaster ✅ immer (74-77). Mod ✅ immer (79-82) — beides unabhängig von den Flags, dokumentiert in `CLAUDE.md:149`. `Subs`-Flag → Live-Helix-Selbstauskunft (84-101), **fail-closed** bei fehlendem Token/`TwitchChannelId`. 7TV-Editor hat hier **keinen** Sonderweg → ❌, sofern nicht anderweitig qualifiziert.

**Kernbefund zur Leitfrage**: Ein Mod/Editor von Channel A kann **nichts** mit Channel B tun. Alle fünf Filter lesen `RouteValues["channelName"]`, alle Handler binden denselben Route-Wert, kein Request-Body und kein zweiter Parameter trägt einen abweichenden Channel-Namen — es gibt keine Filter/Handler-Divergenz. Session-IDs sind durchweg an `ChannelId` gebunden (`VoteSessionService.cs:61-62/140-141/172-173`, `VoteEligibilityService.cs:56-57`), Emotes an `ChannelId` (`VoteSessionService.cs:100-101`, `EmoteService.cs:20`). Cross-Tenant-Zugriff ist **nicht** reproduzierbar.

### Anhang B — Verbindungs-Zustandsmaschine des Workers (Track 2a)

**Beteiligte Akteure — es sind drei, nicht zwei:**

| Akteur | Ort | Was er tut |
|---|---|---|
| `TwitchConnectionWatchdog` | `TwitchConnectionWatchdog.cs:24-31` | 60-s-Tick, ruft `ForceReconnectAsync()` bei ≥5 min Chat-Stille + ≥5 min Cooldown |
| **`ConnectionWatchDog<T>` (TwitchLib-intern)** | `TwitchLib.Communication.Services.ConnectionWatchDog\`1` | Pollt `IsConnected`, ruft **selbstständig** `ReconnectInternalAsync()`. Belegt über die Log-Strings `Client isn't connected anymore` → `Try to reconnect` → `Client reconnected` / `Client couldn't reconnect` in der DLL. **Dieser Akteur fehlt im mentalen Modell und in `CLAUDE.md` komplett.** |
| `TwitchChatManager` | `TwitchChatManager.cs` | Reagiert nur auf Events, hält die Zähler |

```
                        ┌──────────────────────────────────────────────────────┐
                        │  Z0  NIE VERBUNDEN                                   │
                        │  _isConnected=false, _connected=false                │
                        │  _lastMessageReceivedUtcTicks = 0                    │
                        └───────────────────────┬──────────────────────────────┘
                                                │ Worker.cs:19-20
                                                │ Initialize() + ConnectAsync()
                                                │ Rückgabewert Task<bool> WIRD VERWORFEN
                                                │ _connected = true (TwitchChatManager.cs:60)
                                                │
                        ┌───────────────────────┴──────────────────────────────┐
                        │                                                      │
          ConnectAsync() == true                                  ConnectAsync() == false
          → OnConnected (TCM.cs:199-205)                          → RaiseFatalError → OnConnectionError
          _isConnected=true, errors:=0                            (TCM.cs:228-239), _consecErr=1
                        │                                          _isConnected bleibt FALSE
                        ▼                                                      ▼
   ┌─────────────────────────────────────────┐         ╔═══════════════════════════════════════╗
   │  Z1  VERBUNDEN                          │         ║ Z6  TOT & UNENTDECKBAR  (ABSORBIEREND)║
   │  OnMessageReceived → Ticks (TCM.cs:286) │         ║ LastMessageReceivedUtc == null        ║
   └───┬───────────┬──────────────┬──────────┘         ║ → Watchdog return @ TCW.cs:38-41      ║
       │           │              │                    ║ Boot-Joins werfen ClientNotConnected  ║
       │           │              │                    ║ → _joinedChannels BLEIBT LEER          ║
       │           │              │                    ║ KEIN Ausweg ohne Prozess-Neustart     ║
       │           │              │                    ╚═══════════════════════════════════════╝
       │           │              │
       │           │              └──── Socket tot, ABER kein Event (dokumentierter „stiller Freeze")
       │           │                    ▼
       │           │            ┌──────────────────────────────────────────────┐
       │           │            │ Z2  STILLER FREEZE                           │
       │           │            │ _isConnected == true (LÜGT), Health sagt      │
       │           │            │ „connected" (WorkerHealthPublisher.cs:34)     │
       │           │            └───────────────┬──────────────────────────────┘
       │           │                            │ TCW.cs:43-47 idle ≥ 5 min
       │           │                            │ + TCW.cs:49-55 Cooldown ≥ 5 min
       │           │                            │ TCW.cs:60-61 → ForceReconnectAsync()
       │           │                            ▼
       │           │  ┌──────────────────────────────────────────────────────────────┐
       │           │  │ Z3  FORCIERTER RECONNECT (unter _reconnectLock, TCM.cs:65)   │
       │           │  │  a) _consecErr >= 3  → Z5 Recreate      (TCM.cs:68-75)       │
       │           │  │  b) _reconnectCnt >= 8 → Z5 Recreate    (TCM.cs:77-84)       │
       │           │  │  c) sonst _client.ReconnectAsync()      (TCM.cs:91)          │
       │           │  └────────┬──────────────────────────────────────┬──────────────┘
       │           │           │ Erfolg → OnReconnected               │ Fehlschlag
       │           │           │ (TCM.cs:241-250)                     │ → OnConnectionError
       │           │           │ _isConnected=true                    │ _consecErr++
       │           │           │ _consecErr := 0  (Zeile 244)         │ _reconnectCnt bleibt!
       │           │           │ _reconnectCnt++  (Zeile 245)         │
       │           │           │ RejoinTrackedChannels (Zeile 249)    ▼
       │           │           └──────► Z1              ╔═══════════════════════════════════════╗
       │           │                                   ║ Z4  CLIENT ENDGÜLTIG TOT              ║
       │           │  TwitchLib-interner WatchDog       ║ ReconnectionPolicy._attemptsMade==10   ║
       │           └──► erkennt Disconnect,             ║ OpenPrivateAsync kehrt SOFORT zurück, ║
       │                reconnectet AUTONOM             ║ ohne einen einzigen Socket-Versuch    ║
       │                → feuert OnReconnected          ║ → immer „Fatal network error."        ║
       │                → _reconnectCnt++               ║ TwitchLib-WatchDog hat `break`-t und   ║
       │                OHNE dass euer Watchdog         ║ läuft NIE WIEDER                      ║
       │                je beteiligt war!               ╚═══════════════════════╤═══════════════╝
       │                                                                        │ 3× OnConnectionError
       │  OnDisconnected (TCM.cs:207-215)                                        │ à 5 min Cooldown
       │  _isConnected=false, sonst nichts                                       ▼
       │                                            ┌────────────────────────────────────────────┐
       └───────────────────────────────────────────►│ Z5  RECREATE (TCM.cs:99-130)               │
                                                    │ 106 UnwireClient(old)                      │
                                                    │ 109 old.DisconnectAsync() best-effort      │
                                                    │ 116 neuer TwitchClient (NEUE justinfan-ID) │
                                                    │ 117 WireUpClient(new)                      │
                                                    │ 118 _client = new     ◄── Fenster beginnt  │
                                                    │ 119 _consecErr = 0    (plain, kein Ilk.)   │
                                                    │ 120 _reconnectCnt = 0 (plain, kein Ilk.)   │
                                                    │ 122 ConnectAsync()  ◄── bool VERWORFEN      │
                                                    │     _isConnected wird NIE auf false gesetzt│
                                                    │ 129 RejoinTrackedChannelsAsync()           │
                                                    └───────┬───────────────────────┬────────────┘
                                                            │ Erfolg → OnConnected  │ Fehlschlag
                                                            ▼                       ▼
                                                           Z1        Z4' (neuer Client, _attemptsMade
                                                                     bereits 10 nach dem gescheiterten
                                                                     Open-Loop → sofort wieder tot)
```

**Absorbierende Zustände**: Z6 (kein Ausweg ohne Prozess-Neustart, → S2-2) und ein zweiter *pro Channel*: Ein Channel, dessen Join scheitert, landet nie in `_joinedChannels` (`TwitchChatManager.cs:167-168`, das Tracking steht *hinter* dem `await`) und wird von keinem Rejoin je wieder angefasst (→ S2-3).

**Driftende Zähler**: (1) `ReconnectionPolicy._attemptsMade` (TwitchLib-intern) steigt monoton über die Lebensdauer der Client-Instanz und wird bei Reconnects bewusst nicht zurückgesetzt — der eigentliche Killer. (2) `_reconnectCountSinceCreation` unterzählt systematisch, weil er nur bei **erfolgreichem** Reconnect steigt, `_attemptsMade` aber bei **jedem** Versuch. (3) `_consecutiveConnectionErrors` wird als Plain-Store genullt, während es anderswo per `Interlocked` inkrementiert wird. Zusätzlich driftet `_isConnected`, weil es beim Recreate nie auf `false` gesetzt wird.

**Nebenläufigkeit**: Der abgesicherte Fall kann gar nicht eintreten — der Watchdog ist der einzige Aufrufer von `ForceReconnectAsync`, `PeriodicTimer` puffert maximal einen Tick, der `SemaphoreSlim` schützt heute faktisch nichts. Die echten Nebenläufigkeiten laufen komplett außerhalb des Locks: der TwitchLib-interne WatchDog (reconnectet autonom, startet `RejoinTrackedChannelsAsync`, ohne je `_reconnectLock` anzufassen) und die fire-and-forget-Redis-Handler, die `_client` und `_joinedChannels` ohne Lock lesen und schreiben, während `RecreateClientAsync` `_client` austauscht.

### Anhang C — Mass-Delete-Kette und Abbruchszenarien (Track 2b)

**Ablauf**: (1) Emote-Liste + `setId` laden (`usage-stats-page.ts:113-137`, `vote-session-detail-page.ts:131-154`) → (2) Auswahl im Grid (`list-selection.ts:25-41`) → (3) „Löschen (n)" → `openConfirm()` feuert **asynchron** `GET .../emotes/set-warning` (`mass-delete-panel.ts:101-122`) → (4) Dialog rendert **sofort**, `warningLoading=true`, Bestätigen bereits klickbar (`delete-confirm-dialog.ts:27-28, 68-74`) → (5) Token aus `sessionStorage` (`seven-tv-token.service.ts:12`) → (6) `startDelete()` → `from(emotes).pipe(concatMap(...), delay(275ms))`, Token **einmalig** in Closure gefangen (`seven-tv-delete.service.ts:59-87`) → (7) pro Emote `POST 7tv.io/v3/gql` `emoteSet(id:$setId){emotes(id:$emoteId, action:REMOVE)}` (`:105-119`) → (8) Erfolg = `errors[0].message` fehlt; jeder Fehler → `of({success:false})`, Kette läuft weiter (`:113-118`) → (9) `finish()`: `doneIds` → `POST .../emotes/sync-deleted` **ohne Error-Handler** (`:143-155`) → (10) parallel emittiert `effect()` im Panel `deleted` allein aus dem `isRunning`-Flankenwechsel, **unabhängig von (9)** (`mass-delete-panel.ts:82-99`) → (11) Host-Seite entfernt IDs optimistisch → (12) Backend `MarkDeletedAsync` setzt `IsArchived=true` nur für `ChannelId == channel.Id && !IsArchived` (`EmoteService.cs:19-35`) → (13) unabhängig: `SevenTvPeriodicResyncWorker` (60 s) archiviert Verschwundene / **ent**archiviert Wiederaufgetauchte (`SevenTvSyncService.cs:88-108`).

| Abbruchpunkt | Bei 7TV | In DB | Was der Nutzer sieht | Befund |
|---|---|---|---|---|
| Bestätigt, bevor `set-warning` antwortet | gelöscht, evtl. aus **fremdem/geteiltem** Set | archiviert | Lauf wirkt normal, Warnung erscheint nie | **S1-4** |
| Shift-Klick nach `toggleSort()` | **falsche** Emotes gelöscht | falsche archiviert | Zahl + max. 50 Namen; Karten virtuell weggescrollt | **S1-3** |
| Tab schließen mitten im Lauf | k von n gelöscht | **nichts** archiviert (`finish()` läuft nie) | nach Neuöffnen alle n wieder da, kein Protokoll | S3-8, konvergiert ≤60 s |
| Reload mitten im Lauf | k gelöscht | nichts archiviert | Token überlebt; erneute Auswahl scheitert folgenlos | S3-8 |
| Navigation auf andere Route | läuft weiter bis Ende | korrekt archiviert | **kein Fortschritt, kein Abbrechen** — irreversible Operation läuft unsichtbar | **S3-8** |
| Navigation auf anderen Channel | läuft weiter für A | korrekt für A | Panel auf B zeigt A's Fortschritt/Fehler | S3-25 |
| 7TV liefert 401 | Rest bleibt | nur davor erfolgreiche | Token gelöscht (`:124`), **Lauf feuert mit gefangenem Token weiter** — bei 500 Emotes ~2,3 min garantiert scheiternder Requests | **S3-9** |
| 401 als GraphQL-Error (HTTP 200) | wie oben | wie oben | Token wird **nicht** invalidiert (`describeHttpError` nur im `catchError`-Pfad, `:117`) | **S3-9** |
| 7TV liefert 429 | Rest bleibt | nur davor erfolgreiche | kein Backoff, kein Abbruch — Queue hämmert weiter | **S3-9** |
| 7TV 500 bei Einzel-Emote | dieses bleibt | nicht archiviert | rote Zeile, Rest läuft | korrekt |
| `sync-deleted` schlägt fehl | alles gelöscht | **nichts archiviert** | Liste trotzdem leer (optimistisch); Fehler nur unhandled in Konsole | **S2-12** |
| Abbruch bei 50 % | 50 % + evtl. der In-Flight-Request | `done`-Hälfte archiviert | Rest als `cancelled` — suggeriert „nicht gelöscht", obwohl In-Flight weg sein kann | S3-8 |
| Resync während des Laufs | Teilmenge gelöscht | Resync archiviert korrekt, `sync-deleted` meldet Rest als `notFoundIds` | nichts | sauber |
| Resync nach falsch-positivem `done` | Emote existiert noch | archiviert → 60 s später **ent**archiviert | Emote verschwindet und kommt wieder | Konvergenz funktioniert |
| Worker-Container down | beliebig | **keine Konvergenz, dauerhaft** | nichts; `/api/worker/health` meldet nur Twitch-IRC | Teil von S2-12 |

**Kernbefund**: Die Konvergenz-Annahme trägt tatsächlich — `ReconcileAsync` korrigiert **beide** Richtungen innerhalb ≤60 s, `IsBotActive` wird nirgends auf `false` gesetzt. **Alles nach dem GraphQL-Call ist damit höchstens S3.** Der unwiderrufliche Schaden entsteht ausschließlich *vor* Schritt 6 — bei Auswahl und Bestätigung. Dort liegen beide S1.

### Anhang D — Flow-Tabelle Frontend (Track 3)

| Route | Ladezustand | Leerzustand | Fehlerzustand | Was der Nutzer bei API-Fehler sieht |
|---|---|---|---|---|
| `/welcome` (`landing-page.html`) | – (keine API) | – | – | – (rein statisch). Login-Klick = volle Navigation zu `/api/auth/twitch/login`; ist die Api tot, landet der Nutzer auf der rohen Browser-Fehlerseite, ohne Rückweg in die App. |
| `/login` (`login-page.ts`) | – (keine API) | – | – | Identisch: kein SPA-seitiges Feedback möglich. |
| `/` Overview (`overview-page.html:2`) | **fehlt** — `@if (myChannels(); as channels)`, `null` beim Start → komplett leere Seite unter dem Header, solange `/api/channels/mine` (Live-Helix-Call!) läuft | ✅ `overview.noChannels` + ✅ Teil-Degradation `helixUnavailable`/`sevenTvUnavailable` (**vorbildlich**) | ✅ `errorMessage` — aber **ganz unten** (`overview-page.html:122`) | Leere Seite, darunter am Seitenende ein roter Kasten. Bei `join()`-403 (Mod-Status weg / 10-Min-Cache stale): **„Etwas ist schiefgelaufen. Bitte versuch es erneut."** — die Kanalliste bleibt unverändert stehen, kein Hinweis auf den echten Grund. |
| `/my-votings` (`my-votings-page.ts:23`) | **fehlt** | ⚠️ vorhanden, aber **falsch-positiv**: `sessions().length === 0` ist auch während des Ladens und nach jedem Fehler wahr | ✅ generisch | Roter Kasten **und gleichzeitig** „Du hast noch in keiner Abstimmung mitgemacht." — zwei widersprüchliche Aussagen. |
| `/channels/:c/usage-stats` (`usage-stats-page.html:90-93`) | ✅ `isLoading()` → „Lädt…" (**einzige Seite mit echtem Ladezustand**) | ✅ `usageStats.noActiveEmotes` | ✅ `usageStats.errors.loadFailed` | Klare, spezifische Meldung. **Aber:** `getActiveEmoteSetId`-Fehler (`usage-stats-page.ts:119`) ist stumm → das gesamte Mass-Delete-Panel verschwindet kommentarlos. |
| `/channels/:c/vote-sessions` (`vote-session-list-page.html:63`) | **fehlt** | ⚠️ falsch-positiv (s. o.) | ✅ generisch (`apiErrorTranslationKey`) | Roter Kasten + „Noch keine Abstimmungen für diesen Channel." gleichzeitig. Bei 403/404 (Beenden/Löschen) nur „Etwas ist schiefgelaufen". |
| `/channels/:c/vote-sessions/:id` (`vote-session-detail-page.html:2, 75`) | **fehlt** | ⚠️ falsch-positiv: `emotes().length === 0` gilt auch während des Ladens → **„Keine aktiven Emotes."** wird beim Öffnen jeder Session kurz (bei 1.000 Emotes länger) gezeigt | ✅ `loadFailed` fürs Laden, ✅ **differenzierter** Status-Switch (403/409/404/400) fürs Voten | Header (Titel/Status/Zeitraum) fehlt komplett (steckt in `@if (results())`), stattdessen roter Kasten + „Keine aktiven Emotes." |
| **`ChannelWorkspaceLayout`** (Parent von 3 Routen) | **fehlt** | – | ⚠️ nur für `leave()` (`channel-workspace-layout.ts:126-130`) | Die beiden Rechte-Proben (`:99-112`) schlucken **jeden** Fehler inkl. 401 → Tab „Nutzung" und Button „Channel verlassen" verschwinden lautlos. Ein Mod mit abgelaufener Session sieht einen Workspace, in dem plötzlich die Hälfte fehlt, ohne jede Erklärung. |

**Zusätzlich, quer über alle Routen**: Es gibt **keinerlei Navigations-Feedback während der Guards**. `usageStatsAccessGuard` und `voteSessionAccessGuard` feuern einen echten HTTP-Call *bevor* navigiert wird — Klick auf „Öffnen" → 0,5–3 s passiert sichtbar nichts, die alte Seite bleibt stehen. Und bei Fehlschlag wird kommentarlos umgeleitet.

### Anhang E — Schichtentreue (Track 4)

**Backend (.NET)** — Projektverweise plus alle `using`-Direktiven, vollständig ausgewertet:

| Schicht | Erlaubt laut Konvention | Tatsächlich vorhanden | Verstöße |
|---|---|---|---|
| **`EmotePurge.Core`** | keine EF Core, Redis, HTTP, ASP.NET; nur BCL | `csproj`: **0** `PackageReference`, **0** `ProjectReference`. Einzige projektfremde `using` über alle 22 Dateien: `EmotePurge.Core.Entities` (4×). Keine `Microsoft.*`, kein `System.Net.Http`, kein `StackExchange.*` | **keine** |
| **`EmotePurge.Infrastructure`** | → Core; EF/Redis/HTTP erlaubt | `ProjectReference` nur auf Core. Pakete: Npgsql.EFCore, StackExchange.Redis, MS.Extensions.{Http, Configuration.Abstractions, DI.Abstractions}. Keine ASP.NET-Referenz, kein Rückverweis auf Api/Worker | **keine** |
| **`EmotePurge.Api`** | → Infrastructure, → Core; **kein** direkter `AppDbContext` aus Handlern | `ProjectReference` Infra + Core. **Kein einziges** `AppDbContext`/`Microsoft.EntityFrameworkCore`-Vorkommen im Quellcode. Ausnahme: `StackExchange.Redis` direkt in `src/EmotePurge.Api/Endpoints/WorkerHealthEndpoints.cs:3,11,16` | **1** — Redis-Zugriff ohne Service-Interface (S4-11) |
| **`EmotePurge.Worker`** | → Infrastructure, → Core | `ProjectReference` Infra + Core. **2× direkter `AppDbContext`-Zugriff**: `Worker.cs:3-4,25-29` und `SevenTvPeriodicResyncWorker.cs:2-3,33-39` — beide mit identischer Query `Channels.Where(c => c.IsBotActive).Select(c => c.ChannelName)` | **2** — Persistenz-Zugriff am Service-Layer vorbei (S3-28) |

Die Kernregel (`Api`/`Worker` → `Infrastructure` → `Core`, Core bleibt rein) ist **eingehalten**. Es gibt keinen Zyklus, keinen Rückverweis, keine EF-/ASP.NET-Leckage nach Core. Die drei Verstöße sind lokal und in ihrer Wirkung harmlos — aber es gibt keinen automatischen Check, der das so hält (S3-28).

**Interface-Konvention**: ausnahmslos eingehalten. Jede Klasse in `Infrastructure/Services/`, `Infrastructure/Redis/`, `Infrastructure/SevenTv/`, `Infrastructure/Twitch/` hat ein Interface in `Core`. Die dokumentierten `BackgroundService`-Ausnahmen stimmen — **bis auf eine**: `Worker` selbst ist ebenfalls ein `AddHostedService<Worker>()` (`Program.cs:174`), steht aber nicht in der Liste. Es sind fünf Hosted Services, dokumentiert sind vier (S4-13).

**Frontend (Angular)** — alle Imports in `web/src/app/{core,features,shared}` ausgewertet:

| Schicht | Erlaubt | Tatsächlich | Verstöße |
|---|---|---|---|
| **`core/`** | kein `features/`, kein `shared/` | 0 Treffer auf `features/`. 0 Treffer auf `../../shared/`. (`core/shared/paged-result.model.ts` ist ein *eigener* Unterordner, kein Verweis nach oben) | **keine** |
| **`shared/`** | `core/` ja, `features/` nein | 10 Imports auf `../../core/...` (i18n, emote-admin, seven-tv). 0 Treffer auf `features/` | **keine** (formal) |
| **`features/`** | `core/` + `shared/` | ja, durchgängig | **keine** |

Anmerkung ohne Befundstatus: `shared/seven-tv/mass-delete-panel.ts:73-75` injiziert drei `core`-Singletons — `shared/` ist damit nicht „ohne core wiederverwendbar", sondern eine zweite Feature-Schicht ohne eigene Route. Für ein Ein-Personen-Projekt tragfähig; nur der Ordnername verspricht mehr Neutralität, als er hält.

---

## 10. Offene Fragen

Konsolidiert aus den „Unklar geblieben"-Abschnitten aller Tracks. Das sind die Punkte, die der Betreiber selbst beantworten oder nachmessen muss — kein Track konnte sie aus dem Repo heraus klären.

**Sofort prüfbar, mit direkter Konsequenz**
1. **Setzt der Host-Reverse-Proxy `X-Forwarded-Proto`?** Davon hängt heute ab, ob das Session-Cookie in Produktion das `Secure`-Flag trägt. In 10 Sekunden prüfbar: `curl -sI https://emotepurge.app/api/auth/twitch/login | grep -i set-cookie` — steht dort kein `secure`, ist die Annahme verletzt. (Der Fix aus S2-10 macht die Frage irrelevant, statt eine Antwort zu verlangen.)
2. **`ReconnectionPolicy.Reset(bool isReconnect)` — Verhalten nicht dekompilierbar.** Belegt sind Parametername, Felder, Methodennamen, der Default „10 Versuche" (XML-Doku, zweifach) und der Fehlerstring. Das *Verhalten* (kein Reset bei Reconnect) ist aus der Live-Beobachtung abgeleitet. Vor dem Umbau von S2-1 die 12 Zeilen `ReconnectionPolicy.cs` in `github.com/TwitchLib/TwitchLib.Communication`, Tag `2.0.1`, Commit `d1904be` nachlesen — 10 Minuten.
3. **Wie viele Vote-Sessions häuft ein Channel typisch an?** Die Schwere des Sub-Check-Teils von S2-11 hängt daran: Bei 5 Sessions ist es Rauschen, bei 50 ein Ausfallrisiko. `SELECT "ChannelId", COUNT(*) FROM "VoteSessions" GROUP BY 1` gegen die Produktions-DB entscheidet, ob es vor oder nach dem Rest kommt.
4. **Existiert außerhalb des Repos bereits eine Backup-Strategie?** Relevant für die Einordnung von S1-1 und S1-2 — beide Befunde nehmen an, dass es keine gibt.
5. **Kosten von `strictTemplates`**: `--strict` für TypeScript wurde gemessen (0 Fehler über 53 Dateien), `strictTemplates` + `typeCheckHostBindings` nicht. Ein `ng build` mit gesetzter Flagge beantwortet es in unter zwei Minuten.

**Im Stresstest messbar**
6. **Liefert Twitch für Channels der HoB-Größe tatsächlich JOIN/PART-Membership-Events**, oder unterdrückt es sie ab einer Zuschauerzahl? Das entscheidet, ob der NAMES-/Membership-Burst ein Randthema oder der eigentliche CPU-Treiber ist. Messbar, indem `OnSendReceiveData` temporär mit einem Zähler pro IRC-Command verdrahtet und eine Minute gezählt wird — was man für S3-11 ohnehin abonnieren würde.
7. **Reale Antwortzeiten bei ~1.000 Emotes.** Alle Skalierungsaussagen von Track 3 (Guard-Wartezeit, Voll-Refetch pro Vote, „Keine aktiven Emotes"-Falschanzeige während des Ladens) beruhen auf Code-Analyse. Wie lange `GET …/results` bei einem HoB-großen Set real braucht, entscheidet, ob die betroffenen Befunde spürbar oder nur theoretisch sind. Dasselbe gilt für die reale Laufzeit von `set-warning` — bei wenigen moderierten Channels könnte das Fenster in S1-4 unter 500 ms liegen (der Fix ist trotzdem ein Einzeiler).
8. **Wie viele Emotes löscht ein Mod in der Praxis pro Lauf?** Bei 20–50 sind S2-16, S3-8 und S3-25 deutlich weniger schmerzhaft als bei 300–800. Die Priorisierung oben unterstellt den Großlauf, weil die HandOfBlood-Einführung genau dafür geplant ist.
9. **Warum kippte das Watchdog-Log am 2026-07-26 ab dem *dritten* Reconnect**, während der 2026-07-27-Befund den *zehnten* nennt? Erklärung von Track 2a: unterschiedliche Nullpunkte (10 zählt ab Prozessstart über alle Reconnects inklusive der TwitchLib-internen, 3 zählte nur die sichtbaren Watchdog-Reconnects jenes Abends). Konsistent, aber nicht bewiesen — jeden `OnConnectionError` **und** `OnReconnected` mit laufender Nummer seit Client-Erzeugung zu loggen, klärt das beim nächsten Vorfall in einer Zeile.

**Externe Abhängigkeiten, nicht aus dem Repo beantwortbar**
10. **Ist `7tv-token` heute noch der korrekte `localStorage`-Schlüssel auf 7tv.app?** Die gesamte Anleitung hängt daran; stimmt der Name nicht mehr, scheitert der komplette Löschflow an Schritt 3, ohne dass irgendetwas im Code das merkt.
11. **7TV-Editor-Berechtigungsstufen**: `GqlEditorOfQuery` fragt nur `editor_of { user { connections … } }` ab, **nicht** das Permissions-Bitfeld. 7TV vergibt Editor-Rechte granular (Emotes verwalten, Profil bearbeiten, Editoren verwalten) — in EmotePurge zählt jeder Grant gleich viel, ein Nutzer mit nur „Profil bearbeiten" bekäme dieselben Rechte wie ein Emote-Manager. Falls `editor_of { permissions }` verfügbar ist, wäre eine Einschränkung auf das Emote-Manage-Bit die saubere Verschärfung.
12. **Genaues Fehlerformat von 7TV v3 GQL bei abgelaufenem Schreib-Token** (HTTP 401 vs. HTTP 200 mit `errors[]`) — die zweite Hälfte von S3-9 hängt daran; die erste ist unabhängig belegt. Offen ist außerdem, ob 7TV je HTTP 200 mit leerem `errors[]` liefert; `deleteOne` würde das als Erfolg werten (robuster wäre, zusätzlich `data.emoteSet` auf non-null zu prüfen).
13. **GHCR-Package-Sichtbarkeit** (public/private) und ob der VPS-Daemon dauerhaft authentifiziert ist — GitHub-Einstellung, nicht im Repo sichtbar.
14. **Branch-Protection auf `main`** (werden überhaupt PRs genutzt?) — relevant dafür, wie stark der fehlende `pull_request`-Trigger aus S3-37 wehtut.
15. **Docker-Log-Rotation auf dem Host** (`daemon.json`) — ohne Limit könnten Logs über Monate die Disk füllen; hängt an der Host-Konfiguration.
16. **Greift `Auth:AdminTwitchLogins` in Produktion wie erwartet?** `appsettings.json:14` setzt `["sensitron"]` und wird ins Image gebacken; ob der Portainer-Stack zusätzliche Environment-Variablen injiziert, die den Wert überschreiben oder leeren, war nicht prüfbar. Ein leeres Array wäre fail-closed (unkritisch), die Admin-Übersicht aber still tot.
17. **Cacht der Proxy Antworten?** Keine API-Antwort setzt `Cache-Control`. Browserseitig praktisch kein Risiko, proxyseitig hängt es an einer Konfiguration außerhalb des Repos. Billige Absicherung: eine Zeile in der Header-Middleware, die für Pfade unter `/api/` `Cache-Control: no-store` setzt.

**Produktentscheidungen**
18. **`VoteSession.IsActive` wird nie automatisch beendet** — Sessions laufen unbegrenzt, `to` ist dann `DateTime.UtcNow`. Ob das gewollt ist, war weder aus dem Code noch aus `CLAUDE.md` ableitbar; es ist zugleich der Hebel, der S3-4 verschärft.
19. **Verhalten von `window.confirm()`, wenn der Browser „Weitere Dialoge unterdrücken" aktiv hat**: Chrome liefert dann `false` — der Leave würde still nicht ausgeführt (harmlose Richtung), aber ohne jede Rückmeldung, was als „Button kaputt" wahrgenommen würde. Erledigt sich mit dem Dialog-Fix aus S1-1.
20. **Kontrastwerte** wurden rechnerisch aus den Tailwind-v3-Hex-Werten der `slate`-Palette bestimmt; Tailwind v4 nutzt OKLCH-Definitionen. Die Ergebnisse sollten innerhalb weniger Prozent identisch sein, wurden aber nicht mit einem Contrast-Checker im gerenderten DOM gegengeprüft.
21. **`marketing/`** ist ein leeres, ungetracktes Verzeichnis, angelegt am 2026-07-28. Ob dort etwas geplant ist, das strukturelle Konsequenzen hat, war nicht feststellbar.
